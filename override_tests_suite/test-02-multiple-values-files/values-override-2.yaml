# Second override file - higher precedence than first
environment: "override-2"
version: "2.0.0"
database:
  host: "override2-db.local"
  port: 5433
config:
  timeout: 60
  features:
    caching: true

{
  "suite": "render_problems",
  "testNumber": 11,
  "testName": "Map/Dict Operation Errors",
  "chartPath": "C:\\Users\\miabs\\GolandProjects\\helm-tests\\render_problems\\test-11-map-dict-error",
  "renders": [
    {
      "toggle": "dict",
      "dataType": "nilValue",
      "valuesOptions": {
        "values": [
          "testDict=true",
          "testDataType=nilValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n  dictResult: \"{\\\"key1\\\":null,\\\"key2\\\":\\\"value2\\\"}\"\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  dictResult: \"{\\\"key1\\\":null,\\\"key2\\\":\\\"value2\\\"}\"\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n  dictResult: \"{\\\"key1\\\":null,\\\"key2\\\":\\\"value2\\\"}\"\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  dictResult: \"{\\\"key1\\\":null,\\\"key2\\\":\\\"value2\\\"}\"\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "dict",
      "dataType": "stringValue",
      "valuesOptions": {
        "values": [
          "testDict=true",
          "testDataType=stringValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n  dictResult: \"{\\\"key1\\\":\\\"Hello World\\\",\\\"key2\\\":\\\"value2\\\"}\"\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  dictResult: \"{\\\"key1\\\":\\\"Hello World\\\",\\\"key2\\\":\\\"value2\\\"}\"\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n  dictResult: \"{\\\"key1\\\":\\\"Hello World\\\",\\\"key2\\\":\\\"value2\\\"}\"\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  dictResult: \"{\\\"key1\\\":\\\"Hello World\\\",\\\"key2\\\":\\\"value2\\\"}\"\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "dict",
      "dataType": "numberValue",
      "valuesOptions": {
        "values": [
          "testDict=true",
          "testDataType=numberValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n  dictResult: \"{\\\"key1\\\":42,\\\"key2\\\":\\\"value2\\\"}\"\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  dictResult: \"{\\\"key1\\\":42,\\\"key2\\\":\\\"value2\\\"}\"\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n  dictResult: \"{\\\"key1\\\":42,\\\"key2\\\":\\\"value2\\\"}\"\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  dictResult: \"{\\\"key1\\\":42,\\\"key2\\\":\\\"value2\\\"}\"\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "dict",
      "dataType": "boolValue",
      "valuesOptions": {
        "values": [
          "testDict=true",
          "testDataType=boolValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n  dictResult: \"{\\\"key1\\\":true,\\\"key2\\\":\\\"value2\\\"}\"\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  dictResult: \"{\\\"key1\\\":true,\\\"key2\\\":\\\"value2\\\"}\"\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n  dictResult: \"{\\\"key1\\\":true,\\\"key2\\\":\\\"value2\\\"}\"\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  dictResult: \"{\\\"key1\\\":true,\\\"key2\\\":\\\"value2\\\"}\"\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "dict",
      "dataType": "listValue",
      "valuesOptions": {
        "values": [
          "testDict=true",
          "testDataType=listValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n  dictResult: \"{\\\"key1\\\":[1,2,3],\\\"key2\\\":\\\"value2\\\"}\"\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  dictResult: \"{\\\"key1\\\":[1,2,3],\\\"key2\\\":\\\"value2\\\"}\"\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n  dictResult: \"{\\\"key1\\\":[1,2,3],\\\"key2\\\":\\\"value2\\\"}\"\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  dictResult: \"{\\\"key1\\\":[1,2,3],\\\"key2\\\":\\\"value2\\\"}\"\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "dict",
      "dataType": "dictSimple",
      "valuesOptions": {
        "values": [
          "testDict=true",
          "testDataType=dictSimple"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n  dictResult: \"{\\\"key1\\\":{\\\"key\\\":\\\"value\\\",\\\"name\\\":\\\"test\\\"},\\\"key2\\\":\\\"value2\\\"}\"\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  dictResult: \"{\\\"key1\\\":{\\\"key\\\":\\\"value\\\",\\\"name\\\":\\\"test\\\"},\\\"key2\\\":\\\"value2\\\"}\"\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n  dictResult: \"{\\\"key1\\\":{\\\"key\\\":\\\"value\\\",\\\"name\\\":\\\"test\\\"},\\\"key2\\\":\\\"value2\\\"}\"\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  dictResult: \"{\\\"key1\\\":{\\\"key\\\":\\\"value\\\",\\\"name\\\":\\\"test\\\"},\\\"key2\\\":\\\"value2\\\"}\"\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "dict",
      "dataType": "dictEmpty",
      "valuesOptions": {
        "values": [
          "testDict=true",
          "testDataType=dictEmpty"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n  dictResult: \"{\\\"key1\\\":{},\\\"key2\\\":\\\"value2\\\"}\"\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  dictResult: \"{\\\"key1\\\":{},\\\"key2\\\":\\\"value2\\\"}\"\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n  dictResult: \"{\\\"key1\\\":{},\\\"key2\\\":\\\"value2\\\"}\"\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  dictResult: \"{\\\"key1\\\":{},\\\"key2\\\":\\\"value2\\\"}\"\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "dict",
      "dataType": "dictNested",
      "valuesOptions": {
        "values": [
          "testDict=true",
          "testDataType=dictNested"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n  dictResult: \"{\\\"key1\\\":{\\\"level1\\\":{\\\"another\\\":\\\"data\\\",\\\"level2\\\":\\\"deepValue\\\"},\\\"topKey\\\":\\\"topValue\\\"},\\\"key2\\\":\\\"value2\\\"}\"\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  dictResult: \"{\\\"key1\\\":{\\\"level1\\\":{\\\"another\\\":\\\"data\\\",\\\"level2\\\":\\\"deepValue\\\"},\\\"topKey\\\":\\\"topValue\\\"},\\\"key2\\\":\\\"value2\\\"}\"\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n  dictResult: \"{\\\"key1\\\":{\\\"level1\\\":{\\\"another\\\":\\\"data\\\",\\\"level2\\\":\\\"deepValue\\\"},\\\"topKey\\\":\\\"topValue\\\"},\\\"key2\\\":\\\"value2\\\"}\"\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  dictResult: \"{\\\"key1\\\":{\\\"level1\\\":{\\\"another\\\":\\\"data\\\",\\\"level2\\\":\\\"deepValue\\\"},\\\"topKey\\\":\\\"topValue\\\"},\\\"key2\\\":\\\"value2\\\"}\"\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "dict",
      "dataType": "dictDeepNested",
      "valuesOptions": {
        "values": [
          "testDict=true",
          "testDataType=dictDeepNested"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n  dictResult: \"{\\\"key1\\\":{\\\"a\\\":{\\\"b\\\":{\\\"c\\\":{\\\"d\\\":\\\"deepest\\\",\\\"value\\\":\\\"found\\\"},\\\"shallow\\\":\\\"here\\\"},\\\"mid\\\":\\\"level\\\"},\\\"root\\\":\\\"value\\\"},\\\"key2\\\":\\\"value2\\\"}\"\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  dictResult: \"{\\\"key1\\\":{\\\"a\\\":{\\\"b\\\":{\\\"c\\\":{\\\"d\\\":\\\"deepest\\\",\\\"value\\\":\\\"found\\\"},\\\"shallow\\\":\\\"here\\\"},\\\"mid\\\":\\\"level\\\"},\\\"root\\\":\\\"value\\\"},\\\"key2\\\":\\\"value2\\\"}\"\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n  dictResult: \"{\\\"key1\\\":{\\\"a\\\":{\\\"b\\\":{\\\"c\\\":{\\\"d\\\":\\\"deepest\\\",\\\"value\\\":\\\"found\\\"},\\\"shallow\\\":\\\"here\\\"},\\\"mid\\\":\\\"level\\\"},\\\"root\\\":\\\"value\\\"},\\\"key2\\\":\\\"value2\\\"}\"\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  dictResult: \"{\\\"key1\\\":{\\\"a\\\":{\\\"b\\\":{\\\"c\\\":{\\\"d\\\":\\\"deepest\\\",\\\"value\\\":\\\"found\\\"},\\\"shallow\\\":\\\"here\\\"},\\\"mid\\\":\\\"level\\\"},\\\"root\\\":\\\"value\\\"},\\\"key2\\\":\\\"value2\\\"}\"\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "dict",
      "dataType": "dictMixedTypes",
      "valuesOptions": {
        "values": [
          "testDict=true",
          "testDataType=dictMixedTypes"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n  dictResult: \"{\\\"key1\\\":{\\\"boolKey\\\":true,\\\"listKey\\\":[1,2,3],\\\"nestedKey\\\":{\\\"inner\\\":\\\"value\\\"},\\\"nullKey\\\":null,\\\"numberKey\\\":123,\\\"stringKey\\\":\\\"hello\\\"},\\\"key2\\\":\\\"value2\\\"}\"\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  dictResult: \"{\\\"key1\\\":{\\\"boolKey\\\":true,\\\"listKey\\\":[1,2,3],\\\"nestedKey\\\":{\\\"inner\\\":\\\"value\\\"},\\\"nullKey\\\":null,\\\"numberKey\\\":123,\\\"stringKey\\\":\\\"hello\\\"},\\\"key2\\\":\\\"value2\\\"}\"\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n  dictResult: \"{\\\"key1\\\":{\\\"boolKey\\\":true,\\\"listKey\\\":[1,2,3],\\\"nestedKey\\\":{\\\"inner\\\":\\\"value\\\"},\\\"nullKey\\\":null,\\\"numberKey\\\":123,\\\"stringKey\\\":\\\"hello\\\"},\\\"key2\\\":\\\"value2\\\"}\"\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  dictResult: \"{\\\"key1\\\":{\\\"boolKey\\\":true,\\\"listKey\\\":[1,2,3],\\\"nestedKey\\\":{\\\"inner\\\":\\\"value\\\"},\\\"nullKey\\\":null,\\\"numberKey\\\":123,\\\"stringKey\\\":\\\"hello\\\"},\\\"key2\\\":\\\"value2\\\"}\"\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "dict",
      "dataType": "dictWithListOfDicts",
      "valuesOptions": {
        "values": [
          "testDict=true",
          "testDataType=dictWithListOfDicts"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n  dictResult: \"{\\\"key1\\\":{\\\"items\\\":[{\\\"name\\\":\\\"item1\\\",\\\"value\\\":100},{\\\"name\\\":\\\"item2\\\",\\\"value\\\":200},{\\\"name\\\":\\\"item3\\\",\\\"value\\\":300}]},\\\"key2\\\":\\\"value2\\\"}\"\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  dictResult: \"{\\\"key1\\\":{\\\"items\\\":[{\\\"name\\\":\\\"item1\\\",\\\"value\\\":100},{\\\"name\\\":\\\"item2\\\",\\\"value\\\":200},{\\\"name\\\":\\\"item3\\\",\\\"value\\\":300}]},\\\"key2\\\":\\\"value2\\\"}\"\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n  dictResult: \"{\\\"key1\\\":{\\\"items\\\":[{\\\"name\\\":\\\"item1\\\",\\\"value\\\":100},{\\\"name\\\":\\\"item2\\\",\\\"value\\\":200},{\\\"name\\\":\\\"item3\\\",\\\"value\\\":300}]},\\\"key2\\\":\\\"value2\\\"}\"\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  dictResult: \"{\\\"key1\\\":{\\\"items\\\":[{\\\"name\\\":\\\"item1\\\",\\\"value\\\":100},{\\\"name\\\":\\\"item2\\\",\\\"value\\\":200},{\\\"name\\\":\\\"item3\\\",\\\"value\\\":300}]},\\\"key2\\\":\\\"value2\\\"}\"\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "dict",
      "dataType": "dictSpecialKeys",
      "valuesOptions": {
        "values": [
          "testDict=true",
          "testDataType=dictSpecialKeys"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n  dictResult: \"{\\\"key1\\\":{\\\"key with spaces\\\":\\\"value4\\\",\\\"key-with-dash\\\":\\\"value1\\\",\\\"key.with.dots\\\":\\\"value2\\\",\\\"key_with_underscore\\\":\\\"value3\\\"},\\\"key2\\\":\\\"value2\\\"}\"\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  dictResult: \"{\\\"key1\\\":{\\\"key with spaces\\\":\\\"value4\\\",\\\"key-with-dash\\\":\\\"value1\\\",\\\"key.with.dots\\\":\\\"value2\\\",\\\"key_with_underscore\\\":\\\"value3\\\"},\\\"key2\\\":\\\"value2\\\"}\"\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n  dictResult: \"{\\\"key1\\\":{\\\"key with spaces\\\":\\\"value4\\\",\\\"key-with-dash\\\":\\\"value1\\\",\\\"key.with.dots\\\":\\\"value2\\\",\\\"key_with_underscore\\\":\\\"value3\\\"},\\\"key2\\\":\\\"value2\\\"}\"\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  dictResult: \"{\\\"key1\\\":{\\\"key with spaces\\\":\\\"value4\\\",\\\"key-with-dash\\\":\\\"value1\\\",\\\"key.with.dots\\\":\\\"value2\\\",\\\"key_with_underscore\\\":\\\"value3\\\"},\\\"key2\\\":\\\"value2\\\"}\"\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "dict",
      "dataType": "dictNumericKeys",
      "valuesOptions": {
        "values": [
          "testDict=true",
          "testDataType=dictNumericKeys"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n  dictResult: \"{\\\"key1\\\":{\\\"1\\\":\\\"first\\\",\\\"100\\\":\\\"hundredth\\\",\\\"2\\\":\\\"second\\\"},\\\"key2\\\":\\\"value2\\\"}\"\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  dictResult: \"{\\\"key1\\\":{\\\"1\\\":\\\"first\\\",\\\"100\\\":\\\"hundredth\\\",\\\"2\\\":\\\"second\\\"},\\\"key2\\\":\\\"value2\\\"}\"\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n  dictResult: \"{\\\"key1\\\":{\\\"1\\\":\\\"first\\\",\\\"100\\\":\\\"hundredth\\\",\\\"2\\\":\\\"second\\\"},\\\"key2\\\":\\\"value2\\\"}\"\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  dictResult: \"{\\\"key1\\\":{\\\"1\\\":\\\"first\\\",\\\"100\\\":\\\"hundredth\\\",\\\"2\\\":\\\"second\\\"},\\\"key2\\\":\\\"value2\\\"}\"\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "dict",
      "dataType": "dictLargeFlat",
      "valuesOptions": {
        "values": [
          "testDict=true",
          "testDataType=dictLargeFlat"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n  dictResult: \"{\\\"key1\\\":{\\\"key1\\\":\\\"value1\\\",\\\"key10\\\":\\\"value10\\\",\\\"key2\\\":\\\"value2\\\",\\\"key3\\\":\\\"value3\\\",\\\"key4\\\":\\\"value4\\\",\\\"key5\\\":\\\"value5\\\",\\\"key6\\\":\\\"value6\\\",\\\"key7\\\":\\\"value7\\\",\\\"key8\\\":\\\"value8\\\",\\\"key9\\\":\\\"value9\\\"},\\\"key2\\\":\\\"value2\\\"}\"\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  dictResult: \"{\\\"key1\\\":{\\\"key1\\\":\\\"value1\\\",\\\"key10\\\":\\\"value10\\\",\\\"key2\\\":\\\"value2\\\",\\\"key3\\\":\\\"value3\\\",\\\"key4\\\":\\\"value4\\\",\\\"key5\\\":\\\"value5\\\",\\\"key6\\\":\\\"value6\\\",\\\"key7\\\":\\\"value7\\\",\\\"key8\\\":\\\"value8\\\",\\\"key9\\\":\\\"value9\\\"},\\\"key2\\\":\\\"value2\\\"}\"\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n  dictResult: \"{\\\"key1\\\":{\\\"key1\\\":\\\"value1\\\",\\\"key10\\\":\\\"value10\\\",\\\"key2\\\":\\\"value2\\\",\\\"key3\\\":\\\"value3\\\",\\\"key4\\\":\\\"value4\\\",\\\"key5\\\":\\\"value5\\\",\\\"key6\\\":\\\"value6\\\",\\\"key7\\\":\\\"value7\\\",\\\"key8\\\":\\\"value8\\\",\\\"key9\\\":\\\"value9\\\"},\\\"key2\\\":\\\"value2\\\"}\"\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  dictResult: \"{\\\"key1\\\":{\\\"key1\\\":\\\"value1\\\",\\\"key10\\\":\\\"value10\\\",\\\"key2\\\":\\\"value2\\\",\\\"key3\\\":\\\"value3\\\",\\\"key4\\\":\\\"value4\\\",\\\"key5\\\":\\\"value5\\\",\\\"key6\\\":\\\"value6\\\",\\\"key7\\\":\\\"value7\\\",\\\"key8\\\":\\\"value8\\\",\\\"key9\\\":\\\"value9\\\"},\\\"key2\\\":\\\"value2\\\"}\"\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "dict",
      "dataType": "dictForMerge",
      "valuesOptions": {
        "values": [
          "testDict=true",
          "testDataType=dictForMerge"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n  dictResult: \"{\\\"key1\\\":{\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"shared\\\":\\\"fromMergeDict\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"},\\\"key2\\\":\\\"value2\\\"}\"\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  dictResult: \"{\\\"key1\\\":{\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"shared\\\":\\\"fromMergeDict\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"},\\\"key2\\\":\\\"value2\\\"}\"\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n  dictResult: \"{\\\"key1\\\":{\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"shared\\\":\\\"fromMergeDict\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"},\\\"key2\\\":\\\"value2\\\"}\"\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  dictResult: \"{\\\"key1\\\":{\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"shared\\\":\\\"fromMergeDict\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"},\\\"key2\\\":\\\"value2\\\"}\"\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "dict",
      "dataType": "dictValue",
      "valuesOptions": {
        "values": [
          "testDict=true",
          "testDataType=dictValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n  dictResult: \"{\\\"key1\\\":{\\\"key\\\":\\\"value\\\",\\\"nested\\\":{\\\"level2\\\":\\\"deepValue\\\",\\\"sharedNested\\\":\\\"fromOriginal\\\"},\\\"shared\\\":\\\"fromOriginal\\\",\\\"unique1\\\":\\\"onlyInOriginal\\\"},\\\"key2\\\":\\\"value2\\\"}\"\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  dictResult: \"{\\\"key1\\\":{\\\"key\\\":\\\"value\\\",\\\"nested\\\":{\\\"level2\\\":\\\"deepValue\\\",\\\"sharedNested\\\":\\\"fromOriginal\\\"},\\\"shared\\\":\\\"fromOriginal\\\",\\\"unique1\\\":\\\"onlyInOriginal\\\"},\\\"key2\\\":\\\"value2\\\"}\"\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n  dictResult: \"{\\\"key1\\\":{\\\"key\\\":\\\"value\\\",\\\"nested\\\":{\\\"level2\\\":\\\"deepValue\\\",\\\"sharedNested\\\":\\\"fromOriginal\\\"},\\\"shared\\\":\\\"fromOriginal\\\",\\\"unique1\\\":\\\"onlyInOriginal\\\"},\\\"key2\\\":\\\"value2\\\"}\"\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  dictResult: \"{\\\"key1\\\":{\\\"key\\\":\\\"value\\\",\\\"nested\\\":{\\\"level2\\\":\\\"deepValue\\\",\\\"sharedNested\\\":\\\"fromOriginal\\\"},\\\"shared\\\":\\\"fromOriginal\\\",\\\"unique1\\\":\\\"onlyInOriginal\\\"},\\\"key2\\\":\\\"value2\\\"}\"\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "dict",
      "dataType": "listOfDicts",
      "valuesOptions": {
        "values": [
          "testDict=true",
          "testDataType=listOfDicts"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n  dictResult: \"{\\\"key1\\\":[{\\\"age\\\":30,\\\"city\\\":\\\"NYC\\\",\\\"name\\\":\\\"alice\\\"},{\\\"age\\\":25,\\\"city\\\":\\\"LA\\\",\\\"name\\\":\\\"bob\\\"},{\\\"age\\\":35,\\\"city\\\":\\\"Chicago\\\",\\\"name\\\":\\\"charlie\\\"}],\\\"key2\\\":\\\"value2\\\"}\"\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  dictResult: \"{\\\"key1\\\":[{\\\"age\\\":30,\\\"city\\\":\\\"NYC\\\",\\\"name\\\":\\\"alice\\\"},{\\\"age\\\":25,\\\"city\\\":\\\"LA\\\",\\\"name\\\":\\\"bob\\\"},{\\\"age\\\":35,\\\"city\\\":\\\"Chicago\\\",\\\"name\\\":\\\"charlie\\\"}],\\\"key2\\\":\\\"value2\\\"}\"\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n  dictResult: \"{\\\"key1\\\":[{\\\"age\\\":30,\\\"city\\\":\\\"NYC\\\",\\\"name\\\":\\\"alice\\\"},{\\\"age\\\":25,\\\"city\\\":\\\"LA\\\",\\\"name\\\":\\\"bob\\\"},{\\\"age\\\":35,\\\"city\\\":\\\"Chicago\\\",\\\"name\\\":\\\"charlie\\\"}],\\\"key2\\\":\\\"value2\\\"}\"\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  dictResult: \"{\\\"key1\\\":[{\\\"age\\\":30,\\\"city\\\":\\\"NYC\\\",\\\"name\\\":\\\"alice\\\"},{\\\"age\\\":25,\\\"city\\\":\\\"LA\\\",\\\"name\\\":\\\"bob\\\"},{\\\"age\\\":35,\\\"city\\\":\\\"Chicago\\\",\\\"name\\\":\\\"charlie\\\"}],\\\"key2\\\":\\\"value2\\\"}\"\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "dict",
      "dataType": "listEmpty",
      "valuesOptions": {
        "values": [
          "testDict=true",
          "testDataType=listEmpty"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n  dictResult: \"{\\\"key1\\\":[],\\\"key2\\\":\\\"value2\\\"}\"\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  dictResult: \"{\\\"key1\\\":[],\\\"key2\\\":\\\"value2\\\"}\"\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n  dictResult: \"{\\\"key1\\\":[],\\\"key2\\\":\\\"value2\\\"}\"\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  dictResult: \"{\\\"key1\\\":[],\\\"key2\\\":\\\"value2\\\"}\"\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "dict",
      "dataType": "listMixed",
      "valuesOptions": {
        "values": [
          "testDict=true",
          "testDataType=listMixed"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n  dictResult: \"{\\\"key1\\\":[\\\"string\\\",123,true,null,{\\\"key\\\":\\\"value\\\"}],\\\"key2\\\":\\\"value2\\\"}\"\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  dictResult: \"{\\\"key1\\\":[\\\"string\\\",123,true,null,{\\\"key\\\":\\\"value\\\"}],\\\"key2\\\":\\\"value2\\\"}\"\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n  dictResult: \"{\\\"key1\\\":[\\\"string\\\",123,true,null,{\\\"key\\\":\\\"value\\\"}],\\\"key2\\\":\\\"value2\\\"}\"\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  dictResult: \"{\\\"key1\\\":[\\\"string\\\",123,true,null,{\\\"key\\\":\\\"value\\\"}],\\\"key2\\\":\\\"value2\\\"}\"\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "get",
      "dataType": "nilValue",
      "valuesOptions": {
        "values": [
          "testGet=true",
          "testDataType=nilValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n  getResult: \"\"\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n  getResult: \"\"\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n  getResult: \"\"\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n  getResult: \"\"\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "get",
      "dataType": "stringValue",
      "valuesOptions": {
        "values": [
          "testGet=true",
          "testDataType=stringValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:19:20: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003c$testData\u003e: wrong type for value; expected map[string]interface {}; got string",
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      },
      "debug": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:19:20: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003c$testData\u003e: wrong type for value; expected map[string]interface {}; got string",
        "debugLogs": [],
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      }
    },
    {
      "toggle": "get",
      "dataType": "numberValue",
      "valuesOptions": {
        "values": [
          "testGet=true",
          "testDataType=numberValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:19:20: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003c$testData\u003e: wrong type for value; expected map[string]interface {}; got float64",
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      },
      "debug": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:19:20: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003c$testData\u003e: wrong type for value; expected map[string]interface {}; got float64",
        "debugLogs": [],
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      }
    },
    {
      "toggle": "get",
      "dataType": "boolValue",
      "valuesOptions": {
        "values": [
          "testGet=true",
          "testDataType=boolValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:19:20: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003c$testData\u003e: wrong type for value; expected map[string]interface {}; got bool",
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      },
      "debug": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:19:20: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003c$testData\u003e: wrong type for value; expected map[string]interface {}; got bool",
        "debugLogs": [],
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      }
    },
    {
      "toggle": "get",
      "dataType": "listValue",
      "valuesOptions": {
        "values": [
          "testGet=true",
          "testDataType=listValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:19:20: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003c$testData\u003e: wrong type for value; expected map[string]interface {}; got []interface {}",
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      },
      "debug": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:19:20: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003c$testData\u003e: wrong type for value; expected map[string]interface {}; got []interface {}",
        "debugLogs": [],
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      }
    },
    {
      "toggle": "get",
      "dataType": "dictSimple",
      "valuesOptions": {
        "values": [
          "testGet=true",
          "testDataType=dictSimple"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n  getResult: \"value\"\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n  getResult: \"value\"\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n  getResult: \"value\"\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n  getResult: \"value\"\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "get",
      "dataType": "dictEmpty",
      "valuesOptions": {
        "values": [
          "testGet=true",
          "testDataType=dictEmpty"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n  getResult: \"\"\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n  getResult: \"\"\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n  getResult: \"\"\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n  getResult: \"\"\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "get",
      "dataType": "dictNested",
      "valuesOptions": {
        "values": [
          "testGet=true",
          "testDataType=dictNested"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n  getResult: \"\"\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n  getResult: \"\"\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n  getResult: \"\"\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n  getResult: \"\"\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "get",
      "dataType": "dictDeepNested",
      "valuesOptions": {
        "values": [
          "testGet=true",
          "testDataType=dictDeepNested"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n  getResult: \"\"\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n  getResult: \"\"\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n  getResult: \"\"\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n  getResult: \"\"\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "get",
      "dataType": "dictMixedTypes",
      "valuesOptions": {
        "values": [
          "testGet=true",
          "testDataType=dictMixedTypes"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n  getResult: \"\"\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n  getResult: \"\"\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n  getResult: \"\"\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n  getResult: \"\"\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "get",
      "dataType": "dictWithListOfDicts",
      "valuesOptions": {
        "values": [
          "testGet=true",
          "testDataType=dictWithListOfDicts"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n  getResult: \"\"\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n  getResult: \"\"\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n  getResult: \"\"\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n  getResult: \"\"\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "get",
      "dataType": "dictSpecialKeys",
      "valuesOptions": {
        "values": [
          "testGet=true",
          "testDataType=dictSpecialKeys"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n  getResult: \"\"\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n  getResult: \"\"\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n  getResult: \"\"\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n  getResult: \"\"\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "get",
      "dataType": "dictNumericKeys",
      "valuesOptions": {
        "values": [
          "testGet=true",
          "testDataType=dictNumericKeys"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n  getResult: \"\"\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n  getResult: \"\"\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n  getResult: \"\"\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n  getResult: \"\"\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "get",
      "dataType": "dictLargeFlat",
      "valuesOptions": {
        "values": [
          "testGet=true",
          "testDataType=dictLargeFlat"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n  getResult: \"\"\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n  getResult: \"\"\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n  getResult: \"\"\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n  getResult: \"\"\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "get",
      "dataType": "dictForMerge",
      "valuesOptions": {
        "values": [
          "testGet=true",
          "testDataType=dictForMerge"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n  getResult: \"\"\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n  getResult: \"\"\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n  getResult: \"\"\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n  getResult: \"\"\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "get",
      "dataType": "dictValue",
      "valuesOptions": {
        "values": [
          "testGet=true",
          "testDataType=dictValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n  getResult: \"value\"\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n  getResult: \"value\"\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n  getResult: \"value\"\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n  getResult: \"value\"\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "get",
      "dataType": "listOfDicts",
      "valuesOptions": {
        "values": [
          "testGet=true",
          "testDataType=listOfDicts"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:19:20: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003c$testData\u003e: wrong type for value; expected map[string]interface {}; got []interface {}",
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      },
      "debug": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:19:20: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003c$testData\u003e: wrong type for value; expected map[string]interface {}; got []interface {}",
        "debugLogs": [],
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      }
    },
    {
      "toggle": "get",
      "dataType": "listEmpty",
      "valuesOptions": {
        "values": [
          "testGet=true",
          "testDataType=listEmpty"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:19:20: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003c$testData\u003e: wrong type for value; expected map[string]interface {}; got []interface {}",
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      },
      "debug": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:19:20: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003c$testData\u003e: wrong type for value; expected map[string]interface {}; got []interface {}",
        "debugLogs": [],
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      }
    },
    {
      "toggle": "get",
      "dataType": "listMixed",
      "valuesOptions": {
        "values": [
          "testGet=true",
          "testDataType=listMixed"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:19:20: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003c$testData\u003e: wrong type for value; expected map[string]interface {}; got []interface {}",
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      },
      "debug": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:19:20: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003c$testData\u003e: wrong type for value; expected map[string]interface {}; got []interface {}",
        "debugLogs": [],
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      }
    },
    {
      "toggle": "set",
      "dataType": "nilValue",
      "valuesOptions": {
        "values": [
          "testSet=true",
          "testDataType=nilValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:24:12: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003cset $testData \"newKey\" \"newValue\"\u003e: error calling set: assignment to entry in nil map",
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      },
      "debug": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:24:12: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003cset $testData \"newKey\" \"newValue\"\u003e: error calling set: assignment to entry in nil map",
        "debugLogs": [],
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      }
    },
    {
      "toggle": "set",
      "dataType": "stringValue",
      "valuesOptions": {
        "values": [
          "testSet=true",
          "testDataType=stringValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:24:16: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003c$testData\u003e: wrong type for value; expected map[string]interface {}; got string",
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      },
      "debug": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:24:16: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003c$testData\u003e: wrong type for value; expected map[string]interface {}; got string",
        "debugLogs": [],
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      }
    },
    {
      "toggle": "set",
      "dataType": "numberValue",
      "valuesOptions": {
        "values": [
          "testSet=true",
          "testDataType=numberValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:24:16: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003c$testData\u003e: wrong type for value; expected map[string]interface {}; got float64",
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      },
      "debug": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:24:16: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003c$testData\u003e: wrong type for value; expected map[string]interface {}; got float64",
        "debugLogs": [],
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      }
    },
    {
      "toggle": "set",
      "dataType": "boolValue",
      "valuesOptions": {
        "values": [
          "testSet=true",
          "testDataType=boolValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:24:16: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003c$testData\u003e: wrong type for value; expected map[string]interface {}; got bool",
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      },
      "debug": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:24:16: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003c$testData\u003e: wrong type for value; expected map[string]interface {}; got bool",
        "debugLogs": [],
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      }
    },
    {
      "toggle": "set",
      "dataType": "listValue",
      "valuesOptions": {
        "values": [
          "testSet=true",
          "testDataType=listValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:24:16: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003c$testData\u003e: wrong type for value; expected map[string]interface {}; got []interface {}",
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      },
      "debug": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:24:16: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003c$testData\u003e: wrong type for value; expected map[string]interface {}; got []interface {}",
        "debugLogs": [],
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      }
    },
    {
      "toggle": "set",
      "dataType": "dictSimple",
      "valuesOptions": {
        "values": [
          "testSet=true",
          "testDataType=dictSimple"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n  setResult: \"{\\\"key\\\":\\\"value\\\",\\\"name\\\":\\\"test\\\",\\\"newKey\\\":\\\"newValue\\\"}\"\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n  setResult: \"{\\\"key\\\":\\\"value\\\",\\\"name\\\":\\\"test\\\",\\\"newKey\\\":\\\"newValue\\\"}\"\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n  setResult: \"{\\\"key\\\":\\\"value\\\",\\\"name\\\":\\\"test\\\",\\\"newKey\\\":\\\"newValue\\\"}\"\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n  setResult: \"{\\\"key\\\":\\\"value\\\",\\\"name\\\":\\\"test\\\",\\\"newKey\\\":\\\"newValue\\\"}\"\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "set",
      "dataType": "dictEmpty",
      "valuesOptions": {
        "values": [
          "testSet=true",
          "testDataType=dictEmpty"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n  setResult: \"{\\\"newKey\\\":\\\"newValue\\\"}\"\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n  setResult: \"{\\\"newKey\\\":\\\"newValue\\\"}\"\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n  setResult: \"{\\\"newKey\\\":\\\"newValue\\\"}\"\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n  setResult: \"{\\\"newKey\\\":\\\"newValue\\\"}\"\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "set",
      "dataType": "dictNested",
      "valuesOptions": {
        "values": [
          "testSet=true",
          "testDataType=dictNested"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n  setResult: \"{\\\"level1\\\":{\\\"another\\\":\\\"data\\\",\\\"level2\\\":\\\"deepValue\\\"},\\\"newKey\\\":\\\"newValue\\\",\\\"topKey\\\":\\\"topValue\\\"}\"\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n  setResult: \"{\\\"level1\\\":{\\\"another\\\":\\\"data\\\",\\\"level2\\\":\\\"deepValue\\\"},\\\"newKey\\\":\\\"newValue\\\",\\\"topKey\\\":\\\"topValue\\\"}\"\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n  setResult: \"{\\\"level1\\\":{\\\"another\\\":\\\"data\\\",\\\"level2\\\":\\\"deepValue\\\"},\\\"newKey\\\":\\\"newValue\\\",\\\"topKey\\\":\\\"topValue\\\"}\"\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n  setResult: \"{\\\"level1\\\":{\\\"another\\\":\\\"data\\\",\\\"level2\\\":\\\"deepValue\\\"},\\\"newKey\\\":\\\"newValue\\\",\\\"topKey\\\":\\\"topValue\\\"}\"\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "set",
      "dataType": "dictDeepNested",
      "valuesOptions": {
        "values": [
          "testSet=true",
          "testDataType=dictDeepNested"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n  setResult: \"{\\\"a\\\":{\\\"b\\\":{\\\"c\\\":{\\\"d\\\":\\\"deepest\\\",\\\"value\\\":\\\"found\\\"},\\\"shallow\\\":\\\"here\\\"},\\\"mid\\\":\\\"level\\\"},\\\"newKey\\\":\\\"newValue\\\",\\\"root\\\":\\\"value\\\"}\"\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n  setResult: \"{\\\"a\\\":{\\\"b\\\":{\\\"c\\\":{\\\"d\\\":\\\"deepest\\\",\\\"value\\\":\\\"found\\\"},\\\"shallow\\\":\\\"here\\\"},\\\"mid\\\":\\\"level\\\"},\\\"newKey\\\":\\\"newValue\\\",\\\"root\\\":\\\"value\\\"}\"\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n  setResult: \"{\\\"a\\\":{\\\"b\\\":{\\\"c\\\":{\\\"d\\\":\\\"deepest\\\",\\\"value\\\":\\\"found\\\"},\\\"shallow\\\":\\\"here\\\"},\\\"mid\\\":\\\"level\\\"},\\\"newKey\\\":\\\"newValue\\\",\\\"root\\\":\\\"value\\\"}\"\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n  setResult: \"{\\\"a\\\":{\\\"b\\\":{\\\"c\\\":{\\\"d\\\":\\\"deepest\\\",\\\"value\\\":\\\"found\\\"},\\\"shallow\\\":\\\"here\\\"},\\\"mid\\\":\\\"level\\\"},\\\"newKey\\\":\\\"newValue\\\",\\\"root\\\":\\\"value\\\"}\"\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "set",
      "dataType": "dictMixedTypes",
      "valuesOptions": {
        "values": [
          "testSet=true",
          "testDataType=dictMixedTypes"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n  setResult: \"{\\\"boolKey\\\":true,\\\"listKey\\\":[1,2,3],\\\"nestedKey\\\":{\\\"inner\\\":\\\"value\\\"},\\\"newKey\\\":\\\"newValue\\\",\\\"nullKey\\\":null,\\\"numberKey\\\":123,\\\"stringKey\\\":\\\"hello\\\"}\"\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n  setResult: \"{\\\"boolKey\\\":true,\\\"listKey\\\":[1,2,3],\\\"nestedKey\\\":{\\\"inner\\\":\\\"value\\\"},\\\"newKey\\\":\\\"newValue\\\",\\\"nullKey\\\":null,\\\"numberKey\\\":123,\\\"stringKey\\\":\\\"hello\\\"}\"\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n  setResult: \"{\\\"boolKey\\\":true,\\\"listKey\\\":[1,2,3],\\\"nestedKey\\\":{\\\"inner\\\":\\\"value\\\"},\\\"newKey\\\":\\\"newValue\\\",\\\"nullKey\\\":null,\\\"numberKey\\\":123,\\\"stringKey\\\":\\\"hello\\\"}\"\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n  setResult: \"{\\\"boolKey\\\":true,\\\"listKey\\\":[1,2,3],\\\"nestedKey\\\":{\\\"inner\\\":\\\"value\\\"},\\\"newKey\\\":\\\"newValue\\\",\\\"nullKey\\\":null,\\\"numberKey\\\":123,\\\"stringKey\\\":\\\"hello\\\"}\"\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "set",
      "dataType": "dictWithListOfDicts",
      "valuesOptions": {
        "values": [
          "testSet=true",
          "testDataType=dictWithListOfDicts"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n  setResult: \"{\\\"items\\\":[{\\\"name\\\":\\\"item1\\\",\\\"value\\\":100},{\\\"name\\\":\\\"item2\\\",\\\"value\\\":200},{\\\"name\\\":\\\"item3\\\",\\\"value\\\":300}],\\\"newKey\\\":\\\"newValue\\\"}\"\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n  setResult: \"{\\\"items\\\":[{\\\"name\\\":\\\"item1\\\",\\\"value\\\":100},{\\\"name\\\":\\\"item2\\\",\\\"value\\\":200},{\\\"name\\\":\\\"item3\\\",\\\"value\\\":300}],\\\"newKey\\\":\\\"newValue\\\"}\"\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n  setResult: \"{\\\"items\\\":[{\\\"name\\\":\\\"item1\\\",\\\"value\\\":100},{\\\"name\\\":\\\"item2\\\",\\\"value\\\":200},{\\\"name\\\":\\\"item3\\\",\\\"value\\\":300}],\\\"newKey\\\":\\\"newValue\\\"}\"\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n  setResult: \"{\\\"items\\\":[{\\\"name\\\":\\\"item1\\\",\\\"value\\\":100},{\\\"name\\\":\\\"item2\\\",\\\"value\\\":200},{\\\"name\\\":\\\"item3\\\",\\\"value\\\":300}],\\\"newKey\\\":\\\"newValue\\\"}\"\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "set",
      "dataType": "dictSpecialKeys",
      "valuesOptions": {
        "values": [
          "testSet=true",
          "testDataType=dictSpecialKeys"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n  setResult: \"{\\\"key with spaces\\\":\\\"value4\\\",\\\"key-with-dash\\\":\\\"value1\\\",\\\"key.with.dots\\\":\\\"value2\\\",\\\"key_with_underscore\\\":\\\"value3\\\",\\\"newKey\\\":\\\"newValue\\\"}\"\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n  setResult: \"{\\\"key with spaces\\\":\\\"value4\\\",\\\"key-with-dash\\\":\\\"value1\\\",\\\"key.with.dots\\\":\\\"value2\\\",\\\"key_with_underscore\\\":\\\"value3\\\",\\\"newKey\\\":\\\"newValue\\\"}\"\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n  setResult: \"{\\\"key with spaces\\\":\\\"value4\\\",\\\"key-with-dash\\\":\\\"value1\\\",\\\"key.with.dots\\\":\\\"value2\\\",\\\"key_with_underscore\\\":\\\"value3\\\",\\\"newKey\\\":\\\"newValue\\\"}\"\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n  setResult: \"{\\\"key with spaces\\\":\\\"value4\\\",\\\"key-with-dash\\\":\\\"value1\\\",\\\"key.with.dots\\\":\\\"value2\\\",\\\"key_with_underscore\\\":\\\"value3\\\",\\\"newKey\\\":\\\"newValue\\\"}\"\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "set",
      "dataType": "dictNumericKeys",
      "valuesOptions": {
        "values": [
          "testSet=true",
          "testDataType=dictNumericKeys"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n  setResult: \"{\\\"1\\\":\\\"first\\\",\\\"100\\\":\\\"hundredth\\\",\\\"2\\\":\\\"second\\\",\\\"newKey\\\":\\\"newValue\\\"}\"\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n  setResult: \"{\\\"1\\\":\\\"first\\\",\\\"100\\\":\\\"hundredth\\\",\\\"2\\\":\\\"second\\\",\\\"newKey\\\":\\\"newValue\\\"}\"\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n  setResult: \"{\\\"1\\\":\\\"first\\\",\\\"100\\\":\\\"hundredth\\\",\\\"2\\\":\\\"second\\\",\\\"newKey\\\":\\\"newValue\\\"}\"\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n  setResult: \"{\\\"1\\\":\\\"first\\\",\\\"100\\\":\\\"hundredth\\\",\\\"2\\\":\\\"second\\\",\\\"newKey\\\":\\\"newValue\\\"}\"\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "set",
      "dataType": "dictLargeFlat",
      "valuesOptions": {
        "values": [
          "testSet=true",
          "testDataType=dictLargeFlat"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n  setResult: \"{\\\"key1\\\":\\\"value1\\\",\\\"key10\\\":\\\"value10\\\",\\\"key2\\\":\\\"value2\\\",\\\"key3\\\":\\\"value3\\\",\\\"key4\\\":\\\"value4\\\",\\\"key5\\\":\\\"value5\\\",\\\"key6\\\":\\\"value6\\\",\\\"key7\\\":\\\"value7\\\",\\\"key8\\\":\\\"value8\\\",\\\"key9\\\":\\\"value9\\\",\\\"newKey\\\":\\\"newValue\\\"}\"\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n  setResult: \"{\\\"key1\\\":\\\"value1\\\",\\\"key10\\\":\\\"value10\\\",\\\"key2\\\":\\\"value2\\\",\\\"key3\\\":\\\"value3\\\",\\\"key4\\\":\\\"value4\\\",\\\"key5\\\":\\\"value5\\\",\\\"key6\\\":\\\"value6\\\",\\\"key7\\\":\\\"value7\\\",\\\"key8\\\":\\\"value8\\\",\\\"key9\\\":\\\"value9\\\",\\\"newKey\\\":\\\"newValue\\\"}\"\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n  setResult: \"{\\\"key1\\\":\\\"value1\\\",\\\"key10\\\":\\\"value10\\\",\\\"key2\\\":\\\"value2\\\",\\\"key3\\\":\\\"value3\\\",\\\"key4\\\":\\\"value4\\\",\\\"key5\\\":\\\"value5\\\",\\\"key6\\\":\\\"value6\\\",\\\"key7\\\":\\\"value7\\\",\\\"key8\\\":\\\"value8\\\",\\\"key9\\\":\\\"value9\\\",\\\"newKey\\\":\\\"newValue\\\"}\"\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n  setResult: \"{\\\"key1\\\":\\\"value1\\\",\\\"key10\\\":\\\"value10\\\",\\\"key2\\\":\\\"value2\\\",\\\"key3\\\":\\\"value3\\\",\\\"key4\\\":\\\"value4\\\",\\\"key5\\\":\\\"value5\\\",\\\"key6\\\":\\\"value6\\\",\\\"key7\\\":\\\"value7\\\",\\\"key8\\\":\\\"value8\\\",\\\"key9\\\":\\\"value9\\\",\\\"newKey\\\":\\\"newValue\\\"}\"\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "set",
      "dataType": "dictForMerge",
      "valuesOptions": {
        "values": [
          "testSet=true",
          "testDataType=dictForMerge"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n  setResult: \"{\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"newKey\\\":\\\"newValue\\\",\\\"shared\\\":\\\"fromMergeDict\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n  setResult: \"{\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"newKey\\\":\\\"newValue\\\",\\\"shared\\\":\\\"fromMergeDict\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n  setResult: \"{\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"newKey\\\":\\\"newValue\\\",\\\"shared\\\":\\\"fromMergeDict\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n  setResult: \"{\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"newKey\\\":\\\"newValue\\\",\\\"shared\\\":\\\"fromMergeDict\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "set",
      "dataType": "dictValue",
      "valuesOptions": {
        "values": [
          "testSet=true",
          "testDataType=dictValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n  setResult: \"{\\\"key\\\":\\\"value\\\",\\\"nested\\\":{\\\"level2\\\":\\\"deepValue\\\",\\\"sharedNested\\\":\\\"fromOriginal\\\"},\\\"newKey\\\":\\\"newValue\\\",\\\"shared\\\":\\\"fromOriginal\\\",\\\"unique1\\\":\\\"onlyInOriginal\\\"}\"\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n  setResult: \"{\\\"key\\\":\\\"value\\\",\\\"nested\\\":{\\\"level2\\\":\\\"deepValue\\\",\\\"sharedNested\\\":\\\"fromOriginal\\\"},\\\"newKey\\\":\\\"newValue\\\",\\\"shared\\\":\\\"fromOriginal\\\",\\\"unique1\\\":\\\"onlyInOriginal\\\"}\"\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n  setResult: \"{\\\"key\\\":\\\"value\\\",\\\"nested\\\":{\\\"level2\\\":\\\"deepValue\\\",\\\"sharedNested\\\":\\\"fromOriginal\\\"},\\\"newKey\\\":\\\"newValue\\\",\\\"shared\\\":\\\"fromOriginal\\\",\\\"unique1\\\":\\\"onlyInOriginal\\\"}\"\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n  setResult: \"{\\\"key\\\":\\\"value\\\",\\\"nested\\\":{\\\"level2\\\":\\\"deepValue\\\",\\\"sharedNested\\\":\\\"fromOriginal\\\"},\\\"newKey\\\":\\\"newValue\\\",\\\"shared\\\":\\\"fromOriginal\\\",\\\"unique1\\\":\\\"onlyInOriginal\\\"}\"\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "set",
      "dataType": "listOfDicts",
      "valuesOptions": {
        "values": [
          "testSet=true",
          "testDataType=listOfDicts"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:24:16: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003c$testData\u003e: wrong type for value; expected map[string]interface {}; got []interface {}",
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      },
      "debug": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:24:16: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003c$testData\u003e: wrong type for value; expected map[string]interface {}; got []interface {}",
        "debugLogs": [],
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      }
    },
    {
      "toggle": "set",
      "dataType": "listEmpty",
      "valuesOptions": {
        "values": [
          "testSet=true",
          "testDataType=listEmpty"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:24:16: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003c$testData\u003e: wrong type for value; expected map[string]interface {}; got []interface {}",
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      },
      "debug": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:24:16: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003c$testData\u003e: wrong type for value; expected map[string]interface {}; got []interface {}",
        "debugLogs": [],
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      }
    },
    {
      "toggle": "set",
      "dataType": "listMixed",
      "valuesOptions": {
        "values": [
          "testSet=true",
          "testDataType=listMixed"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:24:16: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003c$testData\u003e: wrong type for value; expected map[string]interface {}; got []interface {}",
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      },
      "debug": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:24:16: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003c$testData\u003e: wrong type for value; expected map[string]interface {}; got []interface {}",
        "debugLogs": [],
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      }
    },
    {
      "toggle": "unset",
      "dataType": "nilValue",
      "valuesOptions": {
        "values": [
          "testUnset=true",
          "testDataType=nilValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n  unsetResult: \"null\"\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n  unsetResult: \"null\"\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n  unsetResult: \"null\"\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n  unsetResult: \"null\"\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "unset",
      "dataType": "stringValue",
      "valuesOptions": {
        "values": [
          "testUnset=true",
          "testDataType=stringValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:30:18: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003c$testData\u003e: wrong type for value; expected map[string]interface {}; got string",
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      },
      "debug": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:30:18: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003c$testData\u003e: wrong type for value; expected map[string]interface {}; got string",
        "debugLogs": [],
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      }
    },
    {
      "toggle": "unset",
      "dataType": "numberValue",
      "valuesOptions": {
        "values": [
          "testUnset=true",
          "testDataType=numberValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:30:18: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003c$testData\u003e: wrong type for value; expected map[string]interface {}; got float64",
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      },
      "debug": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:30:18: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003c$testData\u003e: wrong type for value; expected map[string]interface {}; got float64",
        "debugLogs": [],
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      }
    },
    {
      "toggle": "unset",
      "dataType": "boolValue",
      "valuesOptions": {
        "values": [
          "testUnset=true",
          "testDataType=boolValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:30:18: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003c$testData\u003e: wrong type for value; expected map[string]interface {}; got bool",
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      },
      "debug": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:30:18: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003c$testData\u003e: wrong type for value; expected map[string]interface {}; got bool",
        "debugLogs": [],
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      }
    },
    {
      "toggle": "unset",
      "dataType": "listValue",
      "valuesOptions": {
        "values": [
          "testUnset=true",
          "testDataType=listValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:30:18: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003c$testData\u003e: wrong type for value; expected map[string]interface {}; got []interface {}",
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      },
      "debug": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:30:18: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003c$testData\u003e: wrong type for value; expected map[string]interface {}; got []interface {}",
        "debugLogs": [],
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      }
    },
    {
      "toggle": "unset",
      "dataType": "dictSimple",
      "valuesOptions": {
        "values": [
          "testUnset=true",
          "testDataType=dictSimple"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n  unsetResult: \"{\\\"name\\\":\\\"test\\\"}\"\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n  unsetResult: \"{\\\"name\\\":\\\"test\\\"}\"\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n  unsetResult: \"{\\\"name\\\":\\\"test\\\"}\"\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n  unsetResult: \"{\\\"name\\\":\\\"test\\\"}\"\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "unset",
      "dataType": "dictEmpty",
      "valuesOptions": {
        "values": [
          "testUnset=true",
          "testDataType=dictEmpty"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n  unsetResult: \"{}\"\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n  unsetResult: \"{}\"\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n  unsetResult: \"{}\"\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n  unsetResult: \"{}\"\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "unset",
      "dataType": "dictNested",
      "valuesOptions": {
        "values": [
          "testUnset=true",
          "testDataType=dictNested"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n  unsetResult: \"{\\\"level1\\\":{\\\"another\\\":\\\"data\\\",\\\"level2\\\":\\\"deepValue\\\"},\\\"topKey\\\":\\\"topValue\\\"}\"\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n  unsetResult: \"{\\\"level1\\\":{\\\"another\\\":\\\"data\\\",\\\"level2\\\":\\\"deepValue\\\"},\\\"topKey\\\":\\\"topValue\\\"}\"\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n  unsetResult: \"{\\\"level1\\\":{\\\"another\\\":\\\"data\\\",\\\"level2\\\":\\\"deepValue\\\"},\\\"topKey\\\":\\\"topValue\\\"}\"\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n  unsetResult: \"{\\\"level1\\\":{\\\"another\\\":\\\"data\\\",\\\"level2\\\":\\\"deepValue\\\"},\\\"topKey\\\":\\\"topValue\\\"}\"\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "unset",
      "dataType": "dictDeepNested",
      "valuesOptions": {
        "values": [
          "testUnset=true",
          "testDataType=dictDeepNested"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n  unsetResult: \"{\\\"a\\\":{\\\"b\\\":{\\\"c\\\":{\\\"d\\\":\\\"deepest\\\",\\\"value\\\":\\\"found\\\"},\\\"shallow\\\":\\\"here\\\"},\\\"mid\\\":\\\"level\\\"},\\\"root\\\":\\\"value\\\"}\"\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n  unsetResult: \"{\\\"a\\\":{\\\"b\\\":{\\\"c\\\":{\\\"d\\\":\\\"deepest\\\",\\\"value\\\":\\\"found\\\"},\\\"shallow\\\":\\\"here\\\"},\\\"mid\\\":\\\"level\\\"},\\\"root\\\":\\\"value\\\"}\"\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n  unsetResult: \"{\\\"a\\\":{\\\"b\\\":{\\\"c\\\":{\\\"d\\\":\\\"deepest\\\",\\\"value\\\":\\\"found\\\"},\\\"shallow\\\":\\\"here\\\"},\\\"mid\\\":\\\"level\\\"},\\\"root\\\":\\\"value\\\"}\"\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n  unsetResult: \"{\\\"a\\\":{\\\"b\\\":{\\\"c\\\":{\\\"d\\\":\\\"deepest\\\",\\\"value\\\":\\\"found\\\"},\\\"shallow\\\":\\\"here\\\"},\\\"mid\\\":\\\"level\\\"},\\\"root\\\":\\\"value\\\"}\"\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "unset",
      "dataType": "dictMixedTypes",
      "valuesOptions": {
        "values": [
          "testUnset=true",
          "testDataType=dictMixedTypes"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n  unsetResult: \"{\\\"boolKey\\\":true,\\\"listKey\\\":[1,2,3],\\\"nestedKey\\\":{\\\"inner\\\":\\\"value\\\"},\\\"nullKey\\\":null,\\\"numberKey\\\":123,\\\"stringKey\\\":\\\"hello\\\"}\"\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n  unsetResult: \"{\\\"boolKey\\\":true,\\\"listKey\\\":[1,2,3],\\\"nestedKey\\\":{\\\"inner\\\":\\\"value\\\"},\\\"nullKey\\\":null,\\\"numberKey\\\":123,\\\"stringKey\\\":\\\"hello\\\"}\"\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n  unsetResult: \"{\\\"boolKey\\\":true,\\\"listKey\\\":[1,2,3],\\\"nestedKey\\\":{\\\"inner\\\":\\\"value\\\"},\\\"nullKey\\\":null,\\\"numberKey\\\":123,\\\"stringKey\\\":\\\"hello\\\"}\"\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n  unsetResult: \"{\\\"boolKey\\\":true,\\\"listKey\\\":[1,2,3],\\\"nestedKey\\\":{\\\"inner\\\":\\\"value\\\"},\\\"nullKey\\\":null,\\\"numberKey\\\":123,\\\"stringKey\\\":\\\"hello\\\"}\"\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "unset",
      "dataType": "dictWithListOfDicts",
      "valuesOptions": {
        "values": [
          "testUnset=true",
          "testDataType=dictWithListOfDicts"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n  unsetResult: \"{\\\"items\\\":[{\\\"name\\\":\\\"item1\\\",\\\"value\\\":100},{\\\"name\\\":\\\"item2\\\",\\\"value\\\":200},{\\\"name\\\":\\\"item3\\\",\\\"value\\\":300}]}\"\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n  unsetResult: \"{\\\"items\\\":[{\\\"name\\\":\\\"item1\\\",\\\"value\\\":100},{\\\"name\\\":\\\"item2\\\",\\\"value\\\":200},{\\\"name\\\":\\\"item3\\\",\\\"value\\\":300}]}\"\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n  unsetResult: \"{\\\"items\\\":[{\\\"name\\\":\\\"item1\\\",\\\"value\\\":100},{\\\"name\\\":\\\"item2\\\",\\\"value\\\":200},{\\\"name\\\":\\\"item3\\\",\\\"value\\\":300}]}\"\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n  unsetResult: \"{\\\"items\\\":[{\\\"name\\\":\\\"item1\\\",\\\"value\\\":100},{\\\"name\\\":\\\"item2\\\",\\\"value\\\":200},{\\\"name\\\":\\\"item3\\\",\\\"value\\\":300}]}\"\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "unset",
      "dataType": "dictSpecialKeys",
      "valuesOptions": {
        "values": [
          "testUnset=true",
          "testDataType=dictSpecialKeys"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n  unsetResult: \"{\\\"key with spaces\\\":\\\"value4\\\",\\\"key-with-dash\\\":\\\"value1\\\",\\\"key.with.dots\\\":\\\"value2\\\",\\\"key_with_underscore\\\":\\\"value3\\\"}\"\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n  unsetResult: \"{\\\"key with spaces\\\":\\\"value4\\\",\\\"key-with-dash\\\":\\\"value1\\\",\\\"key.with.dots\\\":\\\"value2\\\",\\\"key_with_underscore\\\":\\\"value3\\\"}\"\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n  unsetResult: \"{\\\"key with spaces\\\":\\\"value4\\\",\\\"key-with-dash\\\":\\\"value1\\\",\\\"key.with.dots\\\":\\\"value2\\\",\\\"key_with_underscore\\\":\\\"value3\\\"}\"\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n  unsetResult: \"{\\\"key with spaces\\\":\\\"value4\\\",\\\"key-with-dash\\\":\\\"value1\\\",\\\"key.with.dots\\\":\\\"value2\\\",\\\"key_with_underscore\\\":\\\"value3\\\"}\"\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "unset",
      "dataType": "dictNumericKeys",
      "valuesOptions": {
        "values": [
          "testUnset=true",
          "testDataType=dictNumericKeys"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n  unsetResult: \"{\\\"1\\\":\\\"first\\\",\\\"100\\\":\\\"hundredth\\\",\\\"2\\\":\\\"second\\\"}\"\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n  unsetResult: \"{\\\"1\\\":\\\"first\\\",\\\"100\\\":\\\"hundredth\\\",\\\"2\\\":\\\"second\\\"}\"\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n  unsetResult: \"{\\\"1\\\":\\\"first\\\",\\\"100\\\":\\\"hundredth\\\",\\\"2\\\":\\\"second\\\"}\"\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n  unsetResult: \"{\\\"1\\\":\\\"first\\\",\\\"100\\\":\\\"hundredth\\\",\\\"2\\\":\\\"second\\\"}\"\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "unset",
      "dataType": "dictLargeFlat",
      "valuesOptions": {
        "values": [
          "testUnset=true",
          "testDataType=dictLargeFlat"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n  unsetResult: \"{\\\"key1\\\":\\\"value1\\\",\\\"key10\\\":\\\"value10\\\",\\\"key2\\\":\\\"value2\\\",\\\"key3\\\":\\\"value3\\\",\\\"key4\\\":\\\"value4\\\",\\\"key5\\\":\\\"value5\\\",\\\"key6\\\":\\\"value6\\\",\\\"key7\\\":\\\"value7\\\",\\\"key8\\\":\\\"value8\\\",\\\"key9\\\":\\\"value9\\\"}\"\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n  unsetResult: \"{\\\"key1\\\":\\\"value1\\\",\\\"key10\\\":\\\"value10\\\",\\\"key2\\\":\\\"value2\\\",\\\"key3\\\":\\\"value3\\\",\\\"key4\\\":\\\"value4\\\",\\\"key5\\\":\\\"value5\\\",\\\"key6\\\":\\\"value6\\\",\\\"key7\\\":\\\"value7\\\",\\\"key8\\\":\\\"value8\\\",\\\"key9\\\":\\\"value9\\\"}\"\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n  unsetResult: \"{\\\"key1\\\":\\\"value1\\\",\\\"key10\\\":\\\"value10\\\",\\\"key2\\\":\\\"value2\\\",\\\"key3\\\":\\\"value3\\\",\\\"key4\\\":\\\"value4\\\",\\\"key5\\\":\\\"value5\\\",\\\"key6\\\":\\\"value6\\\",\\\"key7\\\":\\\"value7\\\",\\\"key8\\\":\\\"value8\\\",\\\"key9\\\":\\\"value9\\\"}\"\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n  unsetResult: \"{\\\"key1\\\":\\\"value1\\\",\\\"key10\\\":\\\"value10\\\",\\\"key2\\\":\\\"value2\\\",\\\"key3\\\":\\\"value3\\\",\\\"key4\\\":\\\"value4\\\",\\\"key5\\\":\\\"value5\\\",\\\"key6\\\":\\\"value6\\\",\\\"key7\\\":\\\"value7\\\",\\\"key8\\\":\\\"value8\\\",\\\"key9\\\":\\\"value9\\\"}\"\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "unset",
      "dataType": "dictForMerge",
      "valuesOptions": {
        "values": [
          "testUnset=true",
          "testDataType=dictForMerge"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n  unsetResult: \"{\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"shared\\\":\\\"fromMergeDict\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n  unsetResult: \"{\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"shared\\\":\\\"fromMergeDict\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n  unsetResult: \"{\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"shared\\\":\\\"fromMergeDict\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n  unsetResult: \"{\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"shared\\\":\\\"fromMergeDict\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "unset",
      "dataType": "dictValue",
      "valuesOptions": {
        "values": [
          "testUnset=true",
          "testDataType=dictValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n  unsetResult: \"{\\\"nested\\\":{\\\"level2\\\":\\\"deepValue\\\",\\\"sharedNested\\\":\\\"fromOriginal\\\"},\\\"shared\\\":\\\"fromOriginal\\\",\\\"unique1\\\":\\\"onlyInOriginal\\\"}\"\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n  unsetResult: \"{\\\"nested\\\":{\\\"level2\\\":\\\"deepValue\\\",\\\"sharedNested\\\":\\\"fromOriginal\\\"},\\\"shared\\\":\\\"fromOriginal\\\",\\\"unique1\\\":\\\"onlyInOriginal\\\"}\"\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n  unsetResult: \"{\\\"nested\\\":{\\\"level2\\\":\\\"deepValue\\\",\\\"sharedNested\\\":\\\"fromOriginal\\\"},\\\"shared\\\":\\\"fromOriginal\\\",\\\"unique1\\\":\\\"onlyInOriginal\\\"}\"\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n  unsetResult: \"{\\\"nested\\\":{\\\"level2\\\":\\\"deepValue\\\",\\\"sharedNested\\\":\\\"fromOriginal\\\"},\\\"shared\\\":\\\"fromOriginal\\\",\\\"unique1\\\":\\\"onlyInOriginal\\\"}\"\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "unset",
      "dataType": "listOfDicts",
      "valuesOptions": {
        "values": [
          "testUnset=true",
          "testDataType=listOfDicts"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:30:18: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003c$testData\u003e: wrong type for value; expected map[string]interface {}; got []interface {}",
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      },
      "debug": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:30:18: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003c$testData\u003e: wrong type for value; expected map[string]interface {}; got []interface {}",
        "debugLogs": [],
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      }
    },
    {
      "toggle": "unset",
      "dataType": "listEmpty",
      "valuesOptions": {
        "values": [
          "testUnset=true",
          "testDataType=listEmpty"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:30:18: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003c$testData\u003e: wrong type for value; expected map[string]interface {}; got []interface {}",
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      },
      "debug": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:30:18: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003c$testData\u003e: wrong type for value; expected map[string]interface {}; got []interface {}",
        "debugLogs": [],
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      }
    },
    {
      "toggle": "unset",
      "dataType": "listMixed",
      "valuesOptions": {
        "values": [
          "testUnset=true",
          "testDataType=listMixed"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:30:18: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003c$testData\u003e: wrong type for value; expected map[string]interface {}; got []interface {}",
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      },
      "debug": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:30:18: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003c$testData\u003e: wrong type for value; expected map[string]interface {}; got []interface {}",
        "debugLogs": [],
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      }
    },
    {
      "toggle": "hasKey",
      "dataType": "nilValue",
      "valuesOptions": {
        "values": [
          "testHasKey=true",
          "testDataType=nilValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n  hasKeyResult: \"false\"\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  hasKeyResult: \"false\"\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n  hasKeyResult: \"false\"\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  hasKeyResult: \"false\"\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "hasKey",
      "dataType": "stringValue",
      "valuesOptions": {
        "values": [
          "testHasKey=true",
          "testDataType=stringValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:39:26: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003c$testData\u003e: wrong type for value; expected map[string]interface {}; got string",
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      },
      "debug": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:39:26: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003c$testData\u003e: wrong type for value; expected map[string]interface {}; got string",
        "debugLogs": [],
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      }
    },
    {
      "toggle": "hasKey",
      "dataType": "numberValue",
      "valuesOptions": {
        "values": [
          "testHasKey=true",
          "testDataType=numberValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:39:26: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003c$testData\u003e: wrong type for value; expected map[string]interface {}; got float64",
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      },
      "debug": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:39:26: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003c$testData\u003e: wrong type for value; expected map[string]interface {}; got float64",
        "debugLogs": [],
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      }
    },
    {
      "toggle": "hasKey",
      "dataType": "boolValue",
      "valuesOptions": {
        "values": [
          "testHasKey=true",
          "testDataType=boolValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:39:26: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003c$testData\u003e: wrong type for value; expected map[string]interface {}; got bool",
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      },
      "debug": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:39:26: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003c$testData\u003e: wrong type for value; expected map[string]interface {}; got bool",
        "debugLogs": [],
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      }
    },
    {
      "toggle": "hasKey",
      "dataType": "listValue",
      "valuesOptions": {
        "values": [
          "testHasKey=true",
          "testDataType=listValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:39:26: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003c$testData\u003e: wrong type for value; expected map[string]interface {}; got []interface {}",
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      },
      "debug": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:39:26: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003c$testData\u003e: wrong type for value; expected map[string]interface {}; got []interface {}",
        "debugLogs": [],
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      }
    },
    {
      "toggle": "hasKey",
      "dataType": "dictSimple",
      "valuesOptions": {
        "values": [
          "testHasKey=true",
          "testDataType=dictSimple"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n  hasKeyResult: \"true\"\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  hasKeyResult: \"true\"\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n  hasKeyResult: \"true\"\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  hasKeyResult: \"true\"\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "hasKey",
      "dataType": "dictEmpty",
      "valuesOptions": {
        "values": [
          "testHasKey=true",
          "testDataType=dictEmpty"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n  hasKeyResult: \"false\"\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  hasKeyResult: \"false\"\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n  hasKeyResult: \"false\"\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  hasKeyResult: \"false\"\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "hasKey",
      "dataType": "dictNested",
      "valuesOptions": {
        "values": [
          "testHasKey=true",
          "testDataType=dictNested"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n  hasKeyResult: \"false\"\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  hasKeyResult: \"false\"\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n  hasKeyResult: \"false\"\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  hasKeyResult: \"false\"\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "hasKey",
      "dataType": "dictDeepNested",
      "valuesOptions": {
        "values": [
          "testHasKey=true",
          "testDataType=dictDeepNested"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n  hasKeyResult: \"false\"\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  hasKeyResult: \"false\"\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n  hasKeyResult: \"false\"\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  hasKeyResult: \"false\"\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "hasKey",
      "dataType": "dictMixedTypes",
      "valuesOptions": {
        "values": [
          "testHasKey=true",
          "testDataType=dictMixedTypes"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n  hasKeyResult: \"false\"\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  hasKeyResult: \"false\"\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n  hasKeyResult: \"false\"\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  hasKeyResult: \"false\"\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "hasKey",
      "dataType": "dictWithListOfDicts",
      "valuesOptions": {
        "values": [
          "testHasKey=true",
          "testDataType=dictWithListOfDicts"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n  hasKeyResult: \"false\"\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  hasKeyResult: \"false\"\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n  hasKeyResult: \"false\"\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  hasKeyResult: \"false\"\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "hasKey",
      "dataType": "dictSpecialKeys",
      "valuesOptions": {
        "values": [
          "testHasKey=true",
          "testDataType=dictSpecialKeys"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n  hasKeyResult: \"false\"\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  hasKeyResult: \"false\"\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n  hasKeyResult: \"false\"\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  hasKeyResult: \"false\"\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "hasKey",
      "dataType": "dictNumericKeys",
      "valuesOptions": {
        "values": [
          "testHasKey=true",
          "testDataType=dictNumericKeys"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n  hasKeyResult: \"false\"\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  hasKeyResult: \"false\"\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n  hasKeyResult: \"false\"\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  hasKeyResult: \"false\"\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "hasKey",
      "dataType": "dictLargeFlat",
      "valuesOptions": {
        "values": [
          "testHasKey=true",
          "testDataType=dictLargeFlat"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n  hasKeyResult: \"false\"\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  hasKeyResult: \"false\"\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n  hasKeyResult: \"false\"\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  hasKeyResult: \"false\"\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "hasKey",
      "dataType": "dictForMerge",
      "valuesOptions": {
        "values": [
          "testHasKey=true",
          "testDataType=dictForMerge"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n  hasKeyResult: \"false\"\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  hasKeyResult: \"false\"\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n  hasKeyResult: \"false\"\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  hasKeyResult: \"false\"\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "hasKey",
      "dataType": "dictValue",
      "valuesOptions": {
        "values": [
          "testHasKey=true",
          "testDataType=dictValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n  hasKeyResult: \"true\"\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  hasKeyResult: \"true\"\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n  hasKeyResult: \"true\"\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  hasKeyResult: \"true\"\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "hasKey",
      "dataType": "listOfDicts",
      "valuesOptions": {
        "values": [
          "testHasKey=true",
          "testDataType=listOfDicts"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:39:26: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003c$testData\u003e: wrong type for value; expected map[string]interface {}; got []interface {}",
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      },
      "debug": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:39:26: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003c$testData\u003e: wrong type for value; expected map[string]interface {}; got []interface {}",
        "debugLogs": [],
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      }
    },
    {
      "toggle": "hasKey",
      "dataType": "listEmpty",
      "valuesOptions": {
        "values": [
          "testHasKey=true",
          "testDataType=listEmpty"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:39:26: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003c$testData\u003e: wrong type for value; expected map[string]interface {}; got []interface {}",
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      },
      "debug": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:39:26: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003c$testData\u003e: wrong type for value; expected map[string]interface {}; got []interface {}",
        "debugLogs": [],
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      }
    },
    {
      "toggle": "hasKey",
      "dataType": "listMixed",
      "valuesOptions": {
        "values": [
          "testHasKey=true",
          "testDataType=listMixed"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:39:26: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003c$testData\u003e: wrong type for value; expected map[string]interface {}; got []interface {}",
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      },
      "debug": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:39:26: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003c$testData\u003e: wrong type for value; expected map[string]interface {}; got []interface {}",
        "debugLogs": [],
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      }
    },
    {
      "toggle": "pluck",
      "dataType": "nilValue",
      "valuesOptions": {
        "values": [
          "testPluck=true",
          "testDataType=nilValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n  pluckResult: \"[]\"\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  pluckResult: \"[]\"\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n  pluckResult: \"[]\"\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  pluckResult: \"[]\"\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "pluck",
      "dataType": "stringValue",
      "valuesOptions": {
        "values": [
          "testPluck=true",
          "testDataType=stringValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:45:31: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003c$testData\u003e: wrong type for value; expected map[string]interface {}; got string",
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      },
      "debug": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:45:31: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003c$testData\u003e: wrong type for value; expected map[string]interface {}; got string",
        "debugLogs": [],
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      }
    },
    {
      "toggle": "pluck",
      "dataType": "numberValue",
      "valuesOptions": {
        "values": [
          "testPluck=true",
          "testDataType=numberValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:45:31: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003c$testData\u003e: wrong type for value; expected map[string]interface {}; got float64",
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      },
      "debug": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:45:31: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003c$testData\u003e: wrong type for value; expected map[string]interface {}; got float64",
        "debugLogs": [],
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      }
    },
    {
      "toggle": "pluck",
      "dataType": "boolValue",
      "valuesOptions": {
        "values": [
          "testPluck=true",
          "testDataType=boolValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:45:31: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003c$testData\u003e: wrong type for value; expected map[string]interface {}; got bool",
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      },
      "debug": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:45:31: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003c$testData\u003e: wrong type for value; expected map[string]interface {}; got bool",
        "debugLogs": [],
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      }
    },
    {
      "toggle": "pluck",
      "dataType": "listValue",
      "valuesOptions": {
        "values": [
          "testPluck=true",
          "testDataType=listValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:45:31: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003c$testData\u003e: wrong type for value; expected map[string]interface {}; got []interface {}",
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      },
      "debug": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:45:31: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003c$testData\u003e: wrong type for value; expected map[string]interface {}; got []interface {}",
        "debugLogs": [],
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      }
    },
    {
      "toggle": "pluck",
      "dataType": "dictSimple",
      "valuesOptions": {
        "values": [
          "testPluck=true",
          "testDataType=dictSimple"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n  pluckResult: \"[\\\"test\\\"]\"\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  pluckResult: \"[\\\"test\\\"]\"\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n  pluckResult: \"[\\\"test\\\"]\"\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  pluckResult: \"[\\\"test\\\"]\"\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "pluck",
      "dataType": "dictEmpty",
      "valuesOptions": {
        "values": [
          "testPluck=true",
          "testDataType=dictEmpty"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n  pluckResult: \"[]\"\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  pluckResult: \"[]\"\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n  pluckResult: \"[]\"\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  pluckResult: \"[]\"\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "pluck",
      "dataType": "dictNested",
      "valuesOptions": {
        "values": [
          "testPluck=true",
          "testDataType=dictNested"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n  pluckResult: \"[]\"\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  pluckResult: \"[]\"\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n  pluckResult: \"[]\"\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  pluckResult: \"[]\"\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "pluck",
      "dataType": "dictDeepNested",
      "valuesOptions": {
        "values": [
          "testPluck=true",
          "testDataType=dictDeepNested"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n  pluckResult: \"[]\"\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  pluckResult: \"[]\"\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n  pluckResult: \"[]\"\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  pluckResult: \"[]\"\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "pluck",
      "dataType": "dictMixedTypes",
      "valuesOptions": {
        "values": [
          "testPluck=true",
          "testDataType=dictMixedTypes"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n  pluckResult: \"[]\"\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  pluckResult: \"[]\"\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n  pluckResult: \"[]\"\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  pluckResult: \"[]\"\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "pluck",
      "dataType": "dictWithListOfDicts",
      "valuesOptions": {
        "values": [
          "testPluck=true",
          "testDataType=dictWithListOfDicts"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n  pluckResult: \"[]\"\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  pluckResult: \"[]\"\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n  pluckResult: \"[]\"\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  pluckResult: \"[]\"\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "pluck",
      "dataType": "dictSpecialKeys",
      "valuesOptions": {
        "values": [
          "testPluck=true",
          "testDataType=dictSpecialKeys"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n  pluckResult: \"[]\"\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  pluckResult: \"[]\"\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n  pluckResult: \"[]\"\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  pluckResult: \"[]\"\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "pluck",
      "dataType": "dictNumericKeys",
      "valuesOptions": {
        "values": [
          "testPluck=true",
          "testDataType=dictNumericKeys"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n  pluckResult: \"[]\"\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  pluckResult: \"[]\"\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n  pluckResult: \"[]\"\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  pluckResult: \"[]\"\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "pluck",
      "dataType": "dictLargeFlat",
      "valuesOptions": {
        "values": [
          "testPluck=true",
          "testDataType=dictLargeFlat"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n  pluckResult: \"[]\"\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  pluckResult: \"[]\"\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n  pluckResult: \"[]\"\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  pluckResult: \"[]\"\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "pluck",
      "dataType": "dictForMerge",
      "valuesOptions": {
        "values": [
          "testPluck=true",
          "testDataType=dictForMerge"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n  pluckResult: \"[]\"\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  pluckResult: \"[]\"\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n  pluckResult: \"[]\"\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  pluckResult: \"[]\"\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "pluck",
      "dataType": "dictValue",
      "valuesOptions": {
        "values": [
          "testPluck=true",
          "testDataType=dictValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n  pluckResult: \"[]\"\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  pluckResult: \"[]\"\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n  pluckResult: \"[]\"\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  pluckResult: \"[]\"\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "pluck",
      "dataType": "listOfDicts",
      "valuesOptions": {
        "values": [
          "testPluck=true",
          "testDataType=listOfDicts"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:45:31: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003c$testData\u003e: wrong type for value; expected map[string]interface {}; got []interface {}",
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      },
      "debug": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:45:31: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003c$testData\u003e: wrong type for value; expected map[string]interface {}; got []interface {}",
        "debugLogs": [],
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      }
    },
    {
      "toggle": "pluck",
      "dataType": "listEmpty",
      "valuesOptions": {
        "values": [
          "testPluck=true",
          "testDataType=listEmpty"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:45:31: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003c$testData\u003e: wrong type for value; expected map[string]interface {}; got []interface {}",
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      },
      "debug": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:45:31: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003c$testData\u003e: wrong type for value; expected map[string]interface {}; got []interface {}",
        "debugLogs": [],
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      }
    },
    {
      "toggle": "pluck",
      "dataType": "listMixed",
      "valuesOptions": {
        "values": [
          "testPluck=true",
          "testDataType=listMixed"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:45:31: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003c$testData\u003e: wrong type for value; expected map[string]interface {}; got []interface {}",
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      },
      "debug": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:45:31: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003c$testData\u003e: wrong type for value; expected map[string]interface {}; got []interface {}",
        "debugLogs": [],
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      }
    },
    {
      "toggle": "dig",
      "dataType": "nilValue",
      "valuesOptions": {
        "values": [
          "testDig=true",
          "testDataType=nilValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:51:16: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003cdig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData\u003e: error calling dig: interface conversion: interface {} is nil, not map[string]interface {}",
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      },
      "debug": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:51:16: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003cdig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData\u003e: error calling dig: interface conversion: interface {} is nil, not map[string]interface {}",
        "debugLogs": [],
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      }
    },
    {
      "toggle": "dig",
      "dataType": "stringValue",
      "valuesOptions": {
        "values": [
          "testDig=true",
          "testDataType=stringValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:51:16: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003cdig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData\u003e: error calling dig: interface conversion: interface {} is string, not map[string]interface {}",
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      },
      "debug": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:51:16: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003cdig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData\u003e: error calling dig: interface conversion: interface {} is string, not map[string]interface {}",
        "debugLogs": [],
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      }
    },
    {
      "toggle": "dig",
      "dataType": "numberValue",
      "valuesOptions": {
        "values": [
          "testDig=true",
          "testDataType=numberValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:51:16: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003cdig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData\u003e: error calling dig: interface conversion: interface {} is float64, not map[string]interface {}",
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      },
      "debug": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:51:16: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003cdig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData\u003e: error calling dig: interface conversion: interface {} is float64, not map[string]interface {}",
        "debugLogs": [],
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      }
    },
    {
      "toggle": "dig",
      "dataType": "boolValue",
      "valuesOptions": {
        "values": [
          "testDig=true",
          "testDataType=boolValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:51:16: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003cdig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData\u003e: error calling dig: interface conversion: interface {} is bool, not map[string]interface {}",
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      },
      "debug": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:51:16: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003cdig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData\u003e: error calling dig: interface conversion: interface {} is bool, not map[string]interface {}",
        "debugLogs": [],
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      }
    },
    {
      "toggle": "dig",
      "dataType": "listValue",
      "valuesOptions": {
        "values": [
          "testDig=true",
          "testDataType=listValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:51:16: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003cdig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData\u003e: error calling dig: interface conversion: interface {} is []interface {}, not map[string]interface {}",
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      },
      "debug": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:51:16: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003cdig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData\u003e: error calling dig: interface conversion: interface {} is []interface {}, not map[string]interface {}",
        "debugLogs": [],
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      }
    },
    {
      "toggle": "dig",
      "dataType": "dictSimple",
      "valuesOptions": {
        "values": [
          "testDig=true",
          "testDataType=dictSimple"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n  digResult: \"notfound\"\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  digResult: \"notfound\"\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n  digResult: \"notfound\"\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  digResult: \"notfound\"\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "dig",
      "dataType": "dictEmpty",
      "valuesOptions": {
        "values": [
          "testDig=true",
          "testDataType=dictEmpty"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n  digResult: \"notfound\"\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  digResult: \"notfound\"\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n  digResult: \"notfound\"\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  digResult: \"notfound\"\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "dig",
      "dataType": "dictNested",
      "valuesOptions": {
        "values": [
          "testDig=true",
          "testDataType=dictNested"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n  digResult: \"notfound\"\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  digResult: \"notfound\"\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n  digResult: \"notfound\"\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  digResult: \"notfound\"\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "dig",
      "dataType": "dictDeepNested",
      "valuesOptions": {
        "values": [
          "testDig=true",
          "testDataType=dictDeepNested"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n  digResult: \"deepest\"\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  digResult: \"deepest\"\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n  digResult: \"deepest\"\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  digResult: \"deepest\"\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "dig",
      "dataType": "dictMixedTypes",
      "valuesOptions": {
        "values": [
          "testDig=true",
          "testDataType=dictMixedTypes"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n  digResult: \"notfound\"\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  digResult: \"notfound\"\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n  digResult: \"notfound\"\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  digResult: \"notfound\"\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "dig",
      "dataType": "dictWithListOfDicts",
      "valuesOptions": {
        "values": [
          "testDig=true",
          "testDataType=dictWithListOfDicts"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n  digResult: \"notfound\"\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  digResult: \"notfound\"\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n  digResult: \"notfound\"\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  digResult: \"notfound\"\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "dig",
      "dataType": "dictSpecialKeys",
      "valuesOptions": {
        "values": [
          "testDig=true",
          "testDataType=dictSpecialKeys"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n  digResult: \"notfound\"\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  digResult: \"notfound\"\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n  digResult: \"notfound\"\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  digResult: \"notfound\"\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "dig",
      "dataType": "dictNumericKeys",
      "valuesOptions": {
        "values": [
          "testDig=true",
          "testDataType=dictNumericKeys"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n  digResult: \"notfound\"\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  digResult: \"notfound\"\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n  digResult: \"notfound\"\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  digResult: \"notfound\"\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "dig",
      "dataType": "dictLargeFlat",
      "valuesOptions": {
        "values": [
          "testDig=true",
          "testDataType=dictLargeFlat"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n  digResult: \"notfound\"\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  digResult: \"notfound\"\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n  digResult: \"notfound\"\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  digResult: \"notfound\"\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "dig",
      "dataType": "dictForMerge",
      "valuesOptions": {
        "values": [
          "testDig=true",
          "testDataType=dictForMerge"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n  digResult: \"notfound\"\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  digResult: \"notfound\"\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n  digResult: \"notfound\"\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  digResult: \"notfound\"\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "dig",
      "dataType": "dictValue",
      "valuesOptions": {
        "values": [
          "testDig=true",
          "testDataType=dictValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n  digResult: \"notfound\"\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  digResult: \"notfound\"\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n  digResult: \"notfound\"\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  digResult: \"notfound\"\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "dig",
      "dataType": "listOfDicts",
      "valuesOptions": {
        "values": [
          "testDig=true",
          "testDataType=listOfDicts"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:51:16: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003cdig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData\u003e: error calling dig: interface conversion: interface {} is []interface {}, not map[string]interface {}",
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      },
      "debug": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:51:16: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003cdig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData\u003e: error calling dig: interface conversion: interface {} is []interface {}, not map[string]interface {}",
        "debugLogs": [],
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      }
    },
    {
      "toggle": "dig",
      "dataType": "listEmpty",
      "valuesOptions": {
        "values": [
          "testDig=true",
          "testDataType=listEmpty"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:51:16: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003cdig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData\u003e: error calling dig: interface conversion: interface {} is []interface {}, not map[string]interface {}",
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      },
      "debug": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:51:16: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003cdig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData\u003e: error calling dig: interface conversion: interface {} is []interface {}, not map[string]interface {}",
        "debugLogs": [],
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      }
    },
    {
      "toggle": "dig",
      "dataType": "listMixed",
      "valuesOptions": {
        "values": [
          "testDig=true",
          "testDataType=listMixed"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:51:16: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003cdig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData\u003e: error calling dig: interface conversion: interface {} is []interface {}, not map[string]interface {}",
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      },
      "debug": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:51:16: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003cdig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData\u003e: error calling dig: interface conversion: interface {} is []interface {}, not map[string]interface {}",
        "debugLogs": [],
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      }
    },
    {
      "toggle": "merge",
      "dataType": "nilValue",
      "valuesOptions": {
        "values": [
          "testMerge=true",
          "testDataType=nilValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n  mergeResult: \"{\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"shared\\\":\\\"fromMergeDict\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  mergeResult: \"{\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"shared\\\":\\\"fromMergeDict\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n  mergeResult: \"{\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"shared\\\":\\\"fromMergeDict\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  mergeResult: \"{\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"shared\\\":\\\"fromMergeDict\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "merge",
      "dataType": "stringValue",
      "valuesOptions": {
        "values": [
          "testMerge=true",
          "testDataType=stringValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:59:24: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003c$testData\u003e: wrong type for value; expected map[string]interface {}; got string",
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      },
      "debug": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:59:24: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003c$testData\u003e: wrong type for value; expected map[string]interface {}; got string",
        "debugLogs": [],
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      }
    },
    {
      "toggle": "merge",
      "dataType": "numberValue",
      "valuesOptions": {
        "values": [
          "testMerge=true",
          "testDataType=numberValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:59:24: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003c$testData\u003e: wrong type for value; expected map[string]interface {}; got float64",
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      },
      "debug": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:59:24: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003c$testData\u003e: wrong type for value; expected map[string]interface {}; got float64",
        "debugLogs": [],
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      }
    },
    {
      "toggle": "merge",
      "dataType": "boolValue",
      "valuesOptions": {
        "values": [
          "testMerge=true",
          "testDataType=boolValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:59:24: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003c$testData\u003e: wrong type for value; expected map[string]interface {}; got bool",
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      },
      "debug": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:59:24: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003c$testData\u003e: wrong type for value; expected map[string]interface {}; got bool",
        "debugLogs": [],
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      }
    },
    {
      "toggle": "merge",
      "dataType": "listValue",
      "valuesOptions": {
        "values": [
          "testMerge=true",
          "testDataType=listValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:59:24: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003c$testData\u003e: wrong type for value; expected map[string]interface {}; got []interface {}",
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      },
      "debug": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:59:24: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003c$testData\u003e: wrong type for value; expected map[string]interface {}; got []interface {}",
        "debugLogs": [],
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      }
    },
    {
      "toggle": "merge",
      "dataType": "dictSimple",
      "valuesOptions": {
        "values": [
          "testMerge=true",
          "testDataType=dictSimple"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n  mergeResult: \"{\\\"key\\\":\\\"value\\\",\\\"name\\\":\\\"test\\\",\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"shared\\\":\\\"fromMergeDict\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  mergeResult: \"{\\\"key\\\":\\\"value\\\",\\\"name\\\":\\\"test\\\",\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"shared\\\":\\\"fromMergeDict\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n  mergeResult: \"{\\\"key\\\":\\\"value\\\",\\\"name\\\":\\\"test\\\",\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"shared\\\":\\\"fromMergeDict\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  mergeResult: \"{\\\"key\\\":\\\"value\\\",\\\"name\\\":\\\"test\\\",\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"shared\\\":\\\"fromMergeDict\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "merge",
      "dataType": "dictEmpty",
      "valuesOptions": {
        "values": [
          "testMerge=true",
          "testDataType=dictEmpty"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n  mergeResult: \"{\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"shared\\\":\\\"fromMergeDict\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  mergeResult: \"{\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"shared\\\":\\\"fromMergeDict\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n  mergeResult: \"{\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"shared\\\":\\\"fromMergeDict\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  mergeResult: \"{\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"shared\\\":\\\"fromMergeDict\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "merge",
      "dataType": "dictNested",
      "valuesOptions": {
        "values": [
          "testMerge=true",
          "testDataType=dictNested"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n  mergeResult: \"{\\\"level1\\\":{\\\"another\\\":\\\"data\\\",\\\"level2\\\":\\\"deepValue\\\"},\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"shared\\\":\\\"fromMergeDict\\\",\\\"topKey\\\":\\\"topValue\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  mergeResult: \"{\\\"level1\\\":{\\\"another\\\":\\\"data\\\",\\\"level2\\\":\\\"deepValue\\\"},\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"shared\\\":\\\"fromMergeDict\\\",\\\"topKey\\\":\\\"topValue\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n  mergeResult: \"{\\\"level1\\\":{\\\"another\\\":\\\"data\\\",\\\"level2\\\":\\\"deepValue\\\"},\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"shared\\\":\\\"fromMergeDict\\\",\\\"topKey\\\":\\\"topValue\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  mergeResult: \"{\\\"level1\\\":{\\\"another\\\":\\\"data\\\",\\\"level2\\\":\\\"deepValue\\\"},\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"shared\\\":\\\"fromMergeDict\\\",\\\"topKey\\\":\\\"topValue\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "merge",
      "dataType": "dictDeepNested",
      "valuesOptions": {
        "values": [
          "testMerge=true",
          "testDataType=dictDeepNested"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n  mergeResult: \"{\\\"a\\\":{\\\"b\\\":{\\\"c\\\":{\\\"d\\\":\\\"deepest\\\",\\\"value\\\":\\\"found\\\"},\\\"shallow\\\":\\\"here\\\"},\\\"mid\\\":\\\"level\\\"},\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"root\\\":\\\"value\\\",\\\"shared\\\":\\\"fromMergeDict\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  mergeResult: \"{\\\"a\\\":{\\\"b\\\":{\\\"c\\\":{\\\"d\\\":\\\"deepest\\\",\\\"value\\\":\\\"found\\\"},\\\"shallow\\\":\\\"here\\\"},\\\"mid\\\":\\\"level\\\"},\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"root\\\":\\\"value\\\",\\\"shared\\\":\\\"fromMergeDict\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n  mergeResult: \"{\\\"a\\\":{\\\"b\\\":{\\\"c\\\":{\\\"d\\\":\\\"deepest\\\",\\\"value\\\":\\\"found\\\"},\\\"shallow\\\":\\\"here\\\"},\\\"mid\\\":\\\"level\\\"},\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"root\\\":\\\"value\\\",\\\"shared\\\":\\\"fromMergeDict\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  mergeResult: \"{\\\"a\\\":{\\\"b\\\":{\\\"c\\\":{\\\"d\\\":\\\"deepest\\\",\\\"value\\\":\\\"found\\\"},\\\"shallow\\\":\\\"here\\\"},\\\"mid\\\":\\\"level\\\"},\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"root\\\":\\\"value\\\",\\\"shared\\\":\\\"fromMergeDict\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "merge",
      "dataType": "dictMixedTypes",
      "valuesOptions": {
        "values": [
          "testMerge=true",
          "testDataType=dictMixedTypes"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n  mergeResult: \"{\\\"boolKey\\\":true,\\\"listKey\\\":[1,2,3],\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"nestedKey\\\":{\\\"inner\\\":\\\"value\\\"},\\\"nullKey\\\":null,\\\"numberKey\\\":123,\\\"shared\\\":\\\"fromMergeDict\\\",\\\"stringKey\\\":\\\"hello\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  mergeResult: \"{\\\"boolKey\\\":true,\\\"listKey\\\":[1,2,3],\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"nestedKey\\\":{\\\"inner\\\":\\\"value\\\"},\\\"nullKey\\\":null,\\\"numberKey\\\":123,\\\"shared\\\":\\\"fromMergeDict\\\",\\\"stringKey\\\":\\\"hello\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n  mergeResult: \"{\\\"boolKey\\\":true,\\\"listKey\\\":[1,2,3],\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"nestedKey\\\":{\\\"inner\\\":\\\"value\\\"},\\\"nullKey\\\":null,\\\"numberKey\\\":123,\\\"shared\\\":\\\"fromMergeDict\\\",\\\"stringKey\\\":\\\"hello\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  mergeResult: \"{\\\"boolKey\\\":true,\\\"listKey\\\":[1,2,3],\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"nestedKey\\\":{\\\"inner\\\":\\\"value\\\"},\\\"nullKey\\\":null,\\\"numberKey\\\":123,\\\"shared\\\":\\\"fromMergeDict\\\",\\\"stringKey\\\":\\\"hello\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "merge",
      "dataType": "dictWithListOfDicts",
      "valuesOptions": {
        "values": [
          "testMerge=true",
          "testDataType=dictWithListOfDicts"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n  mergeResult: \"{\\\"items\\\":[{\\\"name\\\":\\\"item1\\\",\\\"value\\\":100},{\\\"name\\\":\\\"item2\\\",\\\"value\\\":200},{\\\"name\\\":\\\"item3\\\",\\\"value\\\":300}],\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"shared\\\":\\\"fromMergeDict\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  mergeResult: \"{\\\"items\\\":[{\\\"name\\\":\\\"item1\\\",\\\"value\\\":100},{\\\"name\\\":\\\"item2\\\",\\\"value\\\":200},{\\\"name\\\":\\\"item3\\\",\\\"value\\\":300}],\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"shared\\\":\\\"fromMergeDict\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n  mergeResult: \"{\\\"items\\\":[{\\\"name\\\":\\\"item1\\\",\\\"value\\\":100},{\\\"name\\\":\\\"item2\\\",\\\"value\\\":200},{\\\"name\\\":\\\"item3\\\",\\\"value\\\":300}],\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"shared\\\":\\\"fromMergeDict\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  mergeResult: \"{\\\"items\\\":[{\\\"name\\\":\\\"item1\\\",\\\"value\\\":100},{\\\"name\\\":\\\"item2\\\",\\\"value\\\":200},{\\\"name\\\":\\\"item3\\\",\\\"value\\\":300}],\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"shared\\\":\\\"fromMergeDict\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "merge",
      "dataType": "dictSpecialKeys",
      "valuesOptions": {
        "values": [
          "testMerge=true",
          "testDataType=dictSpecialKeys"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n  mergeResult: \"{\\\"key with spaces\\\":\\\"value4\\\",\\\"key-with-dash\\\":\\\"value1\\\",\\\"key.with.dots\\\":\\\"value2\\\",\\\"key_with_underscore\\\":\\\"value3\\\",\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"shared\\\":\\\"fromMergeDict\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  mergeResult: \"{\\\"key with spaces\\\":\\\"value4\\\",\\\"key-with-dash\\\":\\\"value1\\\",\\\"key.with.dots\\\":\\\"value2\\\",\\\"key_with_underscore\\\":\\\"value3\\\",\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"shared\\\":\\\"fromMergeDict\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n  mergeResult: \"{\\\"key with spaces\\\":\\\"value4\\\",\\\"key-with-dash\\\":\\\"value1\\\",\\\"key.with.dots\\\":\\\"value2\\\",\\\"key_with_underscore\\\":\\\"value3\\\",\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"shared\\\":\\\"fromMergeDict\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  mergeResult: \"{\\\"key with spaces\\\":\\\"value4\\\",\\\"key-with-dash\\\":\\\"value1\\\",\\\"key.with.dots\\\":\\\"value2\\\",\\\"key_with_underscore\\\":\\\"value3\\\",\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"shared\\\":\\\"fromMergeDict\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "merge",
      "dataType": "dictNumericKeys",
      "valuesOptions": {
        "values": [
          "testMerge=true",
          "testDataType=dictNumericKeys"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n  mergeResult: \"{\\\"1\\\":\\\"first\\\",\\\"100\\\":\\\"hundredth\\\",\\\"2\\\":\\\"second\\\",\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"shared\\\":\\\"fromMergeDict\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  mergeResult: \"{\\\"1\\\":\\\"first\\\",\\\"100\\\":\\\"hundredth\\\",\\\"2\\\":\\\"second\\\",\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"shared\\\":\\\"fromMergeDict\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n  mergeResult: \"{\\\"1\\\":\\\"first\\\",\\\"100\\\":\\\"hundredth\\\",\\\"2\\\":\\\"second\\\",\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"shared\\\":\\\"fromMergeDict\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  mergeResult: \"{\\\"1\\\":\\\"first\\\",\\\"100\\\":\\\"hundredth\\\",\\\"2\\\":\\\"second\\\",\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"shared\\\":\\\"fromMergeDict\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "merge",
      "dataType": "dictLargeFlat",
      "valuesOptions": {
        "values": [
          "testMerge=true",
          "testDataType=dictLargeFlat"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n  mergeResult: \"{\\\"key1\\\":\\\"value1\\\",\\\"key10\\\":\\\"value10\\\",\\\"key2\\\":\\\"value2\\\",\\\"key3\\\":\\\"value3\\\",\\\"key4\\\":\\\"value4\\\",\\\"key5\\\":\\\"value5\\\",\\\"key6\\\":\\\"value6\\\",\\\"key7\\\":\\\"value7\\\",\\\"key8\\\":\\\"value8\\\",\\\"key9\\\":\\\"value9\\\",\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"shared\\\":\\\"fromMergeDict\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  mergeResult: \"{\\\"key1\\\":\\\"value1\\\",\\\"key10\\\":\\\"value10\\\",\\\"key2\\\":\\\"value2\\\",\\\"key3\\\":\\\"value3\\\",\\\"key4\\\":\\\"value4\\\",\\\"key5\\\":\\\"value5\\\",\\\"key6\\\":\\\"value6\\\",\\\"key7\\\":\\\"value7\\\",\\\"key8\\\":\\\"value8\\\",\\\"key9\\\":\\\"value9\\\",\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"shared\\\":\\\"fromMergeDict\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n  mergeResult: \"{\\\"key1\\\":\\\"value1\\\",\\\"key10\\\":\\\"value10\\\",\\\"key2\\\":\\\"value2\\\",\\\"key3\\\":\\\"value3\\\",\\\"key4\\\":\\\"value4\\\",\\\"key5\\\":\\\"value5\\\",\\\"key6\\\":\\\"value6\\\",\\\"key7\\\":\\\"value7\\\",\\\"key8\\\":\\\"value8\\\",\\\"key9\\\":\\\"value9\\\",\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"shared\\\":\\\"fromMergeDict\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  mergeResult: \"{\\\"key1\\\":\\\"value1\\\",\\\"key10\\\":\\\"value10\\\",\\\"key2\\\":\\\"value2\\\",\\\"key3\\\":\\\"value3\\\",\\\"key4\\\":\\\"value4\\\",\\\"key5\\\":\\\"value5\\\",\\\"key6\\\":\\\"value6\\\",\\\"key7\\\":\\\"value7\\\",\\\"key8\\\":\\\"value8\\\",\\\"key9\\\":\\\"value9\\\",\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"shared\\\":\\\"fromMergeDict\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "merge",
      "dataType": "dictForMerge",
      "valuesOptions": {
        "values": [
          "testMerge=true",
          "testDataType=dictForMerge"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n  mergeResult: \"{\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"shared\\\":\\\"fromMergeDict\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  mergeResult: \"{\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"shared\\\":\\\"fromMergeDict\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n  mergeResult: \"{\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"shared\\\":\\\"fromMergeDict\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  mergeResult: \"{\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"shared\\\":\\\"fromMergeDict\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "merge",
      "dataType": "dictValue",
      "valuesOptions": {
        "values": [
          "testMerge=true",
          "testDataType=dictValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n  mergeResult: \"{\\\"key\\\":\\\"value\\\",\\\"nested\\\":{\\\"level2\\\":\\\"deepValue\\\",\\\"sharedNested\\\":\\\"fromOriginal\\\"},\\\"shared\\\":\\\"fromOriginal\\\",\\\"unique1\\\":\\\"onlyInOriginal\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  mergeResult: \"{\\\"key\\\":\\\"value\\\",\\\"nested\\\":{\\\"level2\\\":\\\"deepValue\\\",\\\"sharedNested\\\":\\\"fromOriginal\\\"},\\\"shared\\\":\\\"fromOriginal\\\",\\\"unique1\\\":\\\"onlyInOriginal\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n  mergeResult: \"{\\\"key\\\":\\\"value\\\",\\\"nested\\\":{\\\"level2\\\":\\\"deepValue\\\",\\\"sharedNested\\\":\\\"fromOriginal\\\"},\\\"shared\\\":\\\"fromOriginal\\\",\\\"unique1\\\":\\\"onlyInOriginal\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  mergeResult: \"{\\\"key\\\":\\\"value\\\",\\\"nested\\\":{\\\"level2\\\":\\\"deepValue\\\",\\\"sharedNested\\\":\\\"fromOriginal\\\"},\\\"shared\\\":\\\"fromOriginal\\\",\\\"unique1\\\":\\\"onlyInOriginal\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "merge",
      "dataType": "listOfDicts",
      "valuesOptions": {
        "values": [
          "testMerge=true",
          "testDataType=listOfDicts"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:59:24: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003c$testData\u003e: wrong type for value; expected map[string]interface {}; got []interface {}",
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      },
      "debug": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:59:24: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003c$testData\u003e: wrong type for value; expected map[string]interface {}; got []interface {}",
        "debugLogs": [],
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      }
    },
    {
      "toggle": "merge",
      "dataType": "listEmpty",
      "valuesOptions": {
        "values": [
          "testMerge=true",
          "testDataType=listEmpty"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:59:24: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003c$testData\u003e: wrong type for value; expected map[string]interface {}; got []interface {}",
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      },
      "debug": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:59:24: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003c$testData\u003e: wrong type for value; expected map[string]interface {}; got []interface {}",
        "debugLogs": [],
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      }
    },
    {
      "toggle": "merge",
      "dataType": "listMixed",
      "valuesOptions": {
        "values": [
          "testMerge=true",
          "testDataType=listMixed"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:59:24: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003c$testData\u003e: wrong type for value; expected map[string]interface {}; got []interface {}",
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      },
      "debug": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:59:24: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003c$testData\u003e: wrong type for value; expected map[string]interface {}; got []interface {}",
        "debugLogs": [],
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      }
    },
    {
      "toggle": "mustMerge",
      "dataType": "nilValue",
      "valuesOptions": {
        "values": [
          "testMustMerge=true",
          "testDataType=nilValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n  mustMergeResult: \"{\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"shared\\\":\\\"fromMergeDict\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n  mustMergeResult: \"{\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"shared\\\":\\\"fromMergeDict\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n  mustMergeResult: \"{\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"shared\\\":\\\"fromMergeDict\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n  mustMergeResult: \"{\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"shared\\\":\\\"fromMergeDict\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "mustMerge",
      "dataType": "stringValue",
      "valuesOptions": {
        "values": [
          "testMustMerge=true",
          "testDataType=stringValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:64:32: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003c$testData\u003e: wrong type for value; expected map[string]interface {}; got string",
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      },
      "debug": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:64:32: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003c$testData\u003e: wrong type for value; expected map[string]interface {}; got string",
        "debugLogs": [],
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      }
    },
    {
      "toggle": "mustMerge",
      "dataType": "numberValue",
      "valuesOptions": {
        "values": [
          "testMustMerge=true",
          "testDataType=numberValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:64:32: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003c$testData\u003e: wrong type for value; expected map[string]interface {}; got float64",
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      },
      "debug": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:64:32: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003c$testData\u003e: wrong type for value; expected map[string]interface {}; got float64",
        "debugLogs": [],
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      }
    },
    {
      "toggle": "mustMerge",
      "dataType": "boolValue",
      "valuesOptions": {
        "values": [
          "testMustMerge=true",
          "testDataType=boolValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:64:32: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003c$testData\u003e: wrong type for value; expected map[string]interface {}; got bool",
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      },
      "debug": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:64:32: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003c$testData\u003e: wrong type for value; expected map[string]interface {}; got bool",
        "debugLogs": [],
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      }
    },
    {
      "toggle": "mustMerge",
      "dataType": "listValue",
      "valuesOptions": {
        "values": [
          "testMustMerge=true",
          "testDataType=listValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:64:32: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003c$testData\u003e: wrong type for value; expected map[string]interface {}; got []interface {}",
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      },
      "debug": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:64:32: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003c$testData\u003e: wrong type for value; expected map[string]interface {}; got []interface {}",
        "debugLogs": [],
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      }
    },
    {
      "toggle": "mustMerge",
      "dataType": "dictSimple",
      "valuesOptions": {
        "values": [
          "testMustMerge=true",
          "testDataType=dictSimple"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n  mustMergeResult: \"{\\\"key\\\":\\\"value\\\",\\\"name\\\":\\\"test\\\",\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"shared\\\":\\\"fromMergeDict\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n  mustMergeResult: \"{\\\"key\\\":\\\"value\\\",\\\"name\\\":\\\"test\\\",\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"shared\\\":\\\"fromMergeDict\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n  mustMergeResult: \"{\\\"key\\\":\\\"value\\\",\\\"name\\\":\\\"test\\\",\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"shared\\\":\\\"fromMergeDict\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n  mustMergeResult: \"{\\\"key\\\":\\\"value\\\",\\\"name\\\":\\\"test\\\",\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"shared\\\":\\\"fromMergeDict\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "mustMerge",
      "dataType": "dictEmpty",
      "valuesOptions": {
        "values": [
          "testMustMerge=true",
          "testDataType=dictEmpty"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n  mustMergeResult: \"{\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"shared\\\":\\\"fromMergeDict\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n  mustMergeResult: \"{\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"shared\\\":\\\"fromMergeDict\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n  mustMergeResult: \"{\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"shared\\\":\\\"fromMergeDict\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n  mustMergeResult: \"{\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"shared\\\":\\\"fromMergeDict\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "mustMerge",
      "dataType": "dictNested",
      "valuesOptions": {
        "values": [
          "testMustMerge=true",
          "testDataType=dictNested"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n  mustMergeResult: \"{\\\"level1\\\":{\\\"another\\\":\\\"data\\\",\\\"level2\\\":\\\"deepValue\\\"},\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"shared\\\":\\\"fromMergeDict\\\",\\\"topKey\\\":\\\"topValue\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n  mustMergeResult: \"{\\\"level1\\\":{\\\"another\\\":\\\"data\\\",\\\"level2\\\":\\\"deepValue\\\"},\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"shared\\\":\\\"fromMergeDict\\\",\\\"topKey\\\":\\\"topValue\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n  mustMergeResult: \"{\\\"level1\\\":{\\\"another\\\":\\\"data\\\",\\\"level2\\\":\\\"deepValue\\\"},\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"shared\\\":\\\"fromMergeDict\\\",\\\"topKey\\\":\\\"topValue\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n  mustMergeResult: \"{\\\"level1\\\":{\\\"another\\\":\\\"data\\\",\\\"level2\\\":\\\"deepValue\\\"},\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"shared\\\":\\\"fromMergeDict\\\",\\\"topKey\\\":\\\"topValue\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "mustMerge",
      "dataType": "dictDeepNested",
      "valuesOptions": {
        "values": [
          "testMustMerge=true",
          "testDataType=dictDeepNested"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n  mustMergeResult: \"{\\\"a\\\":{\\\"b\\\":{\\\"c\\\":{\\\"d\\\":\\\"deepest\\\",\\\"value\\\":\\\"found\\\"},\\\"shallow\\\":\\\"here\\\"},\\\"mid\\\":\\\"level\\\"},\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"root\\\":\\\"value\\\",\\\"shared\\\":\\\"fromMergeDict\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n  mustMergeResult: \"{\\\"a\\\":{\\\"b\\\":{\\\"c\\\":{\\\"d\\\":\\\"deepest\\\",\\\"value\\\":\\\"found\\\"},\\\"shallow\\\":\\\"here\\\"},\\\"mid\\\":\\\"level\\\"},\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"root\\\":\\\"value\\\",\\\"shared\\\":\\\"fromMergeDict\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n  mustMergeResult: \"{\\\"a\\\":{\\\"b\\\":{\\\"c\\\":{\\\"d\\\":\\\"deepest\\\",\\\"value\\\":\\\"found\\\"},\\\"shallow\\\":\\\"here\\\"},\\\"mid\\\":\\\"level\\\"},\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"root\\\":\\\"value\\\",\\\"shared\\\":\\\"fromMergeDict\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n  mustMergeResult: \"{\\\"a\\\":{\\\"b\\\":{\\\"c\\\":{\\\"d\\\":\\\"deepest\\\",\\\"value\\\":\\\"found\\\"},\\\"shallow\\\":\\\"here\\\"},\\\"mid\\\":\\\"level\\\"},\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"root\\\":\\\"value\\\",\\\"shared\\\":\\\"fromMergeDict\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "mustMerge",
      "dataType": "dictMixedTypes",
      "valuesOptions": {
        "values": [
          "testMustMerge=true",
          "testDataType=dictMixedTypes"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n  mustMergeResult: \"{\\\"boolKey\\\":true,\\\"listKey\\\":[1,2,3],\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"nestedKey\\\":{\\\"inner\\\":\\\"value\\\"},\\\"nullKey\\\":null,\\\"numberKey\\\":123,\\\"shared\\\":\\\"fromMergeDict\\\",\\\"stringKey\\\":\\\"hello\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n  mustMergeResult: \"{\\\"boolKey\\\":true,\\\"listKey\\\":[1,2,3],\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"nestedKey\\\":{\\\"inner\\\":\\\"value\\\"},\\\"nullKey\\\":null,\\\"numberKey\\\":123,\\\"shared\\\":\\\"fromMergeDict\\\",\\\"stringKey\\\":\\\"hello\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n  mustMergeResult: \"{\\\"boolKey\\\":true,\\\"listKey\\\":[1,2,3],\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"nestedKey\\\":{\\\"inner\\\":\\\"value\\\"},\\\"nullKey\\\":null,\\\"numberKey\\\":123,\\\"shared\\\":\\\"fromMergeDict\\\",\\\"stringKey\\\":\\\"hello\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n  mustMergeResult: \"{\\\"boolKey\\\":true,\\\"listKey\\\":[1,2,3],\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"nestedKey\\\":{\\\"inner\\\":\\\"value\\\"},\\\"nullKey\\\":null,\\\"numberKey\\\":123,\\\"shared\\\":\\\"fromMergeDict\\\",\\\"stringKey\\\":\\\"hello\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "mustMerge",
      "dataType": "dictWithListOfDicts",
      "valuesOptions": {
        "values": [
          "testMustMerge=true",
          "testDataType=dictWithListOfDicts"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n  mustMergeResult: \"{\\\"items\\\":[{\\\"name\\\":\\\"item1\\\",\\\"value\\\":100},{\\\"name\\\":\\\"item2\\\",\\\"value\\\":200},{\\\"name\\\":\\\"item3\\\",\\\"value\\\":300}],\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"shared\\\":\\\"fromMergeDict\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n  mustMergeResult: \"{\\\"items\\\":[{\\\"name\\\":\\\"item1\\\",\\\"value\\\":100},{\\\"name\\\":\\\"item2\\\",\\\"value\\\":200},{\\\"name\\\":\\\"item3\\\",\\\"value\\\":300}],\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"shared\\\":\\\"fromMergeDict\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n  mustMergeResult: \"{\\\"items\\\":[{\\\"name\\\":\\\"item1\\\",\\\"value\\\":100},{\\\"name\\\":\\\"item2\\\",\\\"value\\\":200},{\\\"name\\\":\\\"item3\\\",\\\"value\\\":300}],\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"shared\\\":\\\"fromMergeDict\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n  mustMergeResult: \"{\\\"items\\\":[{\\\"name\\\":\\\"item1\\\",\\\"value\\\":100},{\\\"name\\\":\\\"item2\\\",\\\"value\\\":200},{\\\"name\\\":\\\"item3\\\",\\\"value\\\":300}],\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"shared\\\":\\\"fromMergeDict\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "mustMerge",
      "dataType": "dictSpecialKeys",
      "valuesOptions": {
        "values": [
          "testMustMerge=true",
          "testDataType=dictSpecialKeys"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n  mustMergeResult: \"{\\\"key with spaces\\\":\\\"value4\\\",\\\"key-with-dash\\\":\\\"value1\\\",\\\"key.with.dots\\\":\\\"value2\\\",\\\"key_with_underscore\\\":\\\"value3\\\",\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"shared\\\":\\\"fromMergeDict\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n  mustMergeResult: \"{\\\"key with spaces\\\":\\\"value4\\\",\\\"key-with-dash\\\":\\\"value1\\\",\\\"key.with.dots\\\":\\\"value2\\\",\\\"key_with_underscore\\\":\\\"value3\\\",\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"shared\\\":\\\"fromMergeDict\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n  mustMergeResult: \"{\\\"key with spaces\\\":\\\"value4\\\",\\\"key-with-dash\\\":\\\"value1\\\",\\\"key.with.dots\\\":\\\"value2\\\",\\\"key_with_underscore\\\":\\\"value3\\\",\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"shared\\\":\\\"fromMergeDict\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n  mustMergeResult: \"{\\\"key with spaces\\\":\\\"value4\\\",\\\"key-with-dash\\\":\\\"value1\\\",\\\"key.with.dots\\\":\\\"value2\\\",\\\"key_with_underscore\\\":\\\"value3\\\",\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"shared\\\":\\\"fromMergeDict\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "mustMerge",
      "dataType": "dictNumericKeys",
      "valuesOptions": {
        "values": [
          "testMustMerge=true",
          "testDataType=dictNumericKeys"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n  mustMergeResult: \"{\\\"1\\\":\\\"first\\\",\\\"100\\\":\\\"hundredth\\\",\\\"2\\\":\\\"second\\\",\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"shared\\\":\\\"fromMergeDict\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n  mustMergeResult: \"{\\\"1\\\":\\\"first\\\",\\\"100\\\":\\\"hundredth\\\",\\\"2\\\":\\\"second\\\",\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"shared\\\":\\\"fromMergeDict\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n  mustMergeResult: \"{\\\"1\\\":\\\"first\\\",\\\"100\\\":\\\"hundredth\\\",\\\"2\\\":\\\"second\\\",\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"shared\\\":\\\"fromMergeDict\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n  mustMergeResult: \"{\\\"1\\\":\\\"first\\\",\\\"100\\\":\\\"hundredth\\\",\\\"2\\\":\\\"second\\\",\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"shared\\\":\\\"fromMergeDict\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "mustMerge",
      "dataType": "dictLargeFlat",
      "valuesOptions": {
        "values": [
          "testMustMerge=true",
          "testDataType=dictLargeFlat"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n  mustMergeResult: \"{\\\"key1\\\":\\\"value1\\\",\\\"key10\\\":\\\"value10\\\",\\\"key2\\\":\\\"value2\\\",\\\"key3\\\":\\\"value3\\\",\\\"key4\\\":\\\"value4\\\",\\\"key5\\\":\\\"value5\\\",\\\"key6\\\":\\\"value6\\\",\\\"key7\\\":\\\"value7\\\",\\\"key8\\\":\\\"value8\\\",\\\"key9\\\":\\\"value9\\\",\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"shared\\\":\\\"fromMergeDict\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n  mustMergeResult: \"{\\\"key1\\\":\\\"value1\\\",\\\"key10\\\":\\\"value10\\\",\\\"key2\\\":\\\"value2\\\",\\\"key3\\\":\\\"value3\\\",\\\"key4\\\":\\\"value4\\\",\\\"key5\\\":\\\"value5\\\",\\\"key6\\\":\\\"value6\\\",\\\"key7\\\":\\\"value7\\\",\\\"key8\\\":\\\"value8\\\",\\\"key9\\\":\\\"value9\\\",\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"shared\\\":\\\"fromMergeDict\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n  mustMergeResult: \"{\\\"key1\\\":\\\"value1\\\",\\\"key10\\\":\\\"value10\\\",\\\"key2\\\":\\\"value2\\\",\\\"key3\\\":\\\"value3\\\",\\\"key4\\\":\\\"value4\\\",\\\"key5\\\":\\\"value5\\\",\\\"key6\\\":\\\"value6\\\",\\\"key7\\\":\\\"value7\\\",\\\"key8\\\":\\\"value8\\\",\\\"key9\\\":\\\"value9\\\",\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"shared\\\":\\\"fromMergeDict\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n  mustMergeResult: \"{\\\"key1\\\":\\\"value1\\\",\\\"key10\\\":\\\"value10\\\",\\\"key2\\\":\\\"value2\\\",\\\"key3\\\":\\\"value3\\\",\\\"key4\\\":\\\"value4\\\",\\\"key5\\\":\\\"value5\\\",\\\"key6\\\":\\\"value6\\\",\\\"key7\\\":\\\"value7\\\",\\\"key8\\\":\\\"value8\\\",\\\"key9\\\":\\\"value9\\\",\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"shared\\\":\\\"fromMergeDict\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "mustMerge",
      "dataType": "dictForMerge",
      "valuesOptions": {
        "values": [
          "testMustMerge=true",
          "testDataType=dictForMerge"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n  mustMergeResult: \"{\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"shared\\\":\\\"fromMergeDict\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n  mustMergeResult: \"{\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"shared\\\":\\\"fromMergeDict\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n  mustMergeResult: \"{\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"shared\\\":\\\"fromMergeDict\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n  mustMergeResult: \"{\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"shared\\\":\\\"fromMergeDict\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "mustMerge",
      "dataType": "dictValue",
      "valuesOptions": {
        "values": [
          "testMustMerge=true",
          "testDataType=dictValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n  mustMergeResult: \"{\\\"key\\\":\\\"value\\\",\\\"nested\\\":{\\\"level2\\\":\\\"deepValue\\\",\\\"sharedNested\\\":\\\"fromOriginal\\\"},\\\"shared\\\":\\\"fromOriginal\\\",\\\"unique1\\\":\\\"onlyInOriginal\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n  mustMergeResult: \"{\\\"key\\\":\\\"value\\\",\\\"nested\\\":{\\\"level2\\\":\\\"deepValue\\\",\\\"sharedNested\\\":\\\"fromOriginal\\\"},\\\"shared\\\":\\\"fromOriginal\\\",\\\"unique1\\\":\\\"onlyInOriginal\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n  mustMergeResult: \"{\\\"key\\\":\\\"value\\\",\\\"nested\\\":{\\\"level2\\\":\\\"deepValue\\\",\\\"sharedNested\\\":\\\"fromOriginal\\\"},\\\"shared\\\":\\\"fromOriginal\\\",\\\"unique1\\\":\\\"onlyInOriginal\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n  mustMergeResult: \"{\\\"key\\\":\\\"value\\\",\\\"nested\\\":{\\\"level2\\\":\\\"deepValue\\\",\\\"sharedNested\\\":\\\"fromOriginal\\\"},\\\"shared\\\":\\\"fromOriginal\\\",\\\"unique1\\\":\\\"onlyInOriginal\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "mustMerge",
      "dataType": "listOfDicts",
      "valuesOptions": {
        "values": [
          "testMustMerge=true",
          "testDataType=listOfDicts"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:64:32: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003c$testData\u003e: wrong type for value; expected map[string]interface {}; got []interface {}",
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      },
      "debug": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:64:32: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003c$testData\u003e: wrong type for value; expected map[string]interface {}; got []interface {}",
        "debugLogs": [],
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      }
    },
    {
      "toggle": "mustMerge",
      "dataType": "listEmpty",
      "valuesOptions": {
        "values": [
          "testMustMerge=true",
          "testDataType=listEmpty"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:64:32: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003c$testData\u003e: wrong type for value; expected map[string]interface {}; got []interface {}",
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      },
      "debug": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:64:32: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003c$testData\u003e: wrong type for value; expected map[string]interface {}; got []interface {}",
        "debugLogs": [],
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      }
    },
    {
      "toggle": "mustMerge",
      "dataType": "listMixed",
      "valuesOptions": {
        "values": [
          "testMustMerge=true",
          "testDataType=listMixed"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:64:32: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003c$testData\u003e: wrong type for value; expected map[string]interface {}; got []interface {}",
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      },
      "debug": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:64:32: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003c$testData\u003e: wrong type for value; expected map[string]interface {}; got []interface {}",
        "debugLogs": [],
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      }
    },
    {
      "toggle": "mergeOverwrite",
      "dataType": "nilValue",
      "valuesOptions": {
        "values": [
          "testMergeOverwrite=true",
          "testDataType=nilValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n  mergeOverwriteResult: \"{\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"shared\\\":\\\"fromMergeDict\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  mergeOverwriteResult: \"{\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"shared\\\":\\\"fromMergeDict\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n  mergeOverwriteResult: \"{\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"shared\\\":\\\"fromMergeDict\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  mergeOverwriteResult: \"{\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"shared\\\":\\\"fromMergeDict\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "mergeOverwrite",
      "dataType": "stringValue",
      "valuesOptions": {
        "values": [
          "testMergeOverwrite=true",
          "testDataType=stringValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:69:42: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003c$testData\u003e: wrong type for value; expected map[string]interface {}; got string",
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      },
      "debug": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:69:42: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003c$testData\u003e: wrong type for value; expected map[string]interface {}; got string",
        "debugLogs": [],
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      }
    },
    {
      "toggle": "mergeOverwrite",
      "dataType": "numberValue",
      "valuesOptions": {
        "values": [
          "testMergeOverwrite=true",
          "testDataType=numberValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:69:42: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003c$testData\u003e: wrong type for value; expected map[string]interface {}; got float64",
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      },
      "debug": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:69:42: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003c$testData\u003e: wrong type for value; expected map[string]interface {}; got float64",
        "debugLogs": [],
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      }
    },
    {
      "toggle": "mergeOverwrite",
      "dataType": "boolValue",
      "valuesOptions": {
        "values": [
          "testMergeOverwrite=true",
          "testDataType=boolValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:69:42: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003c$testData\u003e: wrong type for value; expected map[string]interface {}; got bool",
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      },
      "debug": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:69:42: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003c$testData\u003e: wrong type for value; expected map[string]interface {}; got bool",
        "debugLogs": [],
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      }
    },
    {
      "toggle": "mergeOverwrite",
      "dataType": "listValue",
      "valuesOptions": {
        "values": [
          "testMergeOverwrite=true",
          "testDataType=listValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:69:42: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003c$testData\u003e: wrong type for value; expected map[string]interface {}; got []interface {}",
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      },
      "debug": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:69:42: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003c$testData\u003e: wrong type for value; expected map[string]interface {}; got []interface {}",
        "debugLogs": [],
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      }
    },
    {
      "toggle": "mergeOverwrite",
      "dataType": "dictSimple",
      "valuesOptions": {
        "values": [
          "testMergeOverwrite=true",
          "testDataType=dictSimple"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n  mergeOverwriteResult: \"{\\\"key\\\":\\\"value\\\",\\\"name\\\":\\\"test\\\",\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"shared\\\":\\\"fromMergeDict\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  mergeOverwriteResult: \"{\\\"key\\\":\\\"value\\\",\\\"name\\\":\\\"test\\\",\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"shared\\\":\\\"fromMergeDict\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n  mergeOverwriteResult: \"{\\\"key\\\":\\\"value\\\",\\\"name\\\":\\\"test\\\",\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"shared\\\":\\\"fromMergeDict\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  mergeOverwriteResult: \"{\\\"key\\\":\\\"value\\\",\\\"name\\\":\\\"test\\\",\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"shared\\\":\\\"fromMergeDict\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "mergeOverwrite",
      "dataType": "dictEmpty",
      "valuesOptions": {
        "values": [
          "testMergeOverwrite=true",
          "testDataType=dictEmpty"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n  mergeOverwriteResult: \"{\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"shared\\\":\\\"fromMergeDict\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  mergeOverwriteResult: \"{\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"shared\\\":\\\"fromMergeDict\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n  mergeOverwriteResult: \"{\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"shared\\\":\\\"fromMergeDict\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  mergeOverwriteResult: \"{\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"shared\\\":\\\"fromMergeDict\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "mergeOverwrite",
      "dataType": "dictNested",
      "valuesOptions": {
        "values": [
          "testMergeOverwrite=true",
          "testDataType=dictNested"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n  mergeOverwriteResult: \"{\\\"level1\\\":{\\\"another\\\":\\\"data\\\",\\\"level2\\\":\\\"deepValue\\\"},\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"shared\\\":\\\"fromMergeDict\\\",\\\"topKey\\\":\\\"topValue\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  mergeOverwriteResult: \"{\\\"level1\\\":{\\\"another\\\":\\\"data\\\",\\\"level2\\\":\\\"deepValue\\\"},\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"shared\\\":\\\"fromMergeDict\\\",\\\"topKey\\\":\\\"topValue\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n  mergeOverwriteResult: \"{\\\"level1\\\":{\\\"another\\\":\\\"data\\\",\\\"level2\\\":\\\"deepValue\\\"},\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"shared\\\":\\\"fromMergeDict\\\",\\\"topKey\\\":\\\"topValue\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  mergeOverwriteResult: \"{\\\"level1\\\":{\\\"another\\\":\\\"data\\\",\\\"level2\\\":\\\"deepValue\\\"},\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"shared\\\":\\\"fromMergeDict\\\",\\\"topKey\\\":\\\"topValue\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "mergeOverwrite",
      "dataType": "dictDeepNested",
      "valuesOptions": {
        "values": [
          "testMergeOverwrite=true",
          "testDataType=dictDeepNested"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n  mergeOverwriteResult: \"{\\\"a\\\":{\\\"b\\\":{\\\"c\\\":{\\\"d\\\":\\\"deepest\\\",\\\"value\\\":\\\"found\\\"},\\\"shallow\\\":\\\"here\\\"},\\\"mid\\\":\\\"level\\\"},\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"root\\\":\\\"value\\\",\\\"shared\\\":\\\"fromMergeDict\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  mergeOverwriteResult: \"{\\\"a\\\":{\\\"b\\\":{\\\"c\\\":{\\\"d\\\":\\\"deepest\\\",\\\"value\\\":\\\"found\\\"},\\\"shallow\\\":\\\"here\\\"},\\\"mid\\\":\\\"level\\\"},\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"root\\\":\\\"value\\\",\\\"shared\\\":\\\"fromMergeDict\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n  mergeOverwriteResult: \"{\\\"a\\\":{\\\"b\\\":{\\\"c\\\":{\\\"d\\\":\\\"deepest\\\",\\\"value\\\":\\\"found\\\"},\\\"shallow\\\":\\\"here\\\"},\\\"mid\\\":\\\"level\\\"},\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"root\\\":\\\"value\\\",\\\"shared\\\":\\\"fromMergeDict\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  mergeOverwriteResult: \"{\\\"a\\\":{\\\"b\\\":{\\\"c\\\":{\\\"d\\\":\\\"deepest\\\",\\\"value\\\":\\\"found\\\"},\\\"shallow\\\":\\\"here\\\"},\\\"mid\\\":\\\"level\\\"},\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"root\\\":\\\"value\\\",\\\"shared\\\":\\\"fromMergeDict\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "mergeOverwrite",
      "dataType": "dictMixedTypes",
      "valuesOptions": {
        "values": [
          "testMergeOverwrite=true",
          "testDataType=dictMixedTypes"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n  mergeOverwriteResult: \"{\\\"boolKey\\\":true,\\\"listKey\\\":[1,2,3],\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"nestedKey\\\":{\\\"inner\\\":\\\"value\\\"},\\\"nullKey\\\":null,\\\"numberKey\\\":123,\\\"shared\\\":\\\"fromMergeDict\\\",\\\"stringKey\\\":\\\"hello\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  mergeOverwriteResult: \"{\\\"boolKey\\\":true,\\\"listKey\\\":[1,2,3],\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"nestedKey\\\":{\\\"inner\\\":\\\"value\\\"},\\\"nullKey\\\":null,\\\"numberKey\\\":123,\\\"shared\\\":\\\"fromMergeDict\\\",\\\"stringKey\\\":\\\"hello\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n  mergeOverwriteResult: \"{\\\"boolKey\\\":true,\\\"listKey\\\":[1,2,3],\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"nestedKey\\\":{\\\"inner\\\":\\\"value\\\"},\\\"nullKey\\\":null,\\\"numberKey\\\":123,\\\"shared\\\":\\\"fromMergeDict\\\",\\\"stringKey\\\":\\\"hello\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  mergeOverwriteResult: \"{\\\"boolKey\\\":true,\\\"listKey\\\":[1,2,3],\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"nestedKey\\\":{\\\"inner\\\":\\\"value\\\"},\\\"nullKey\\\":null,\\\"numberKey\\\":123,\\\"shared\\\":\\\"fromMergeDict\\\",\\\"stringKey\\\":\\\"hello\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "mergeOverwrite",
      "dataType": "dictWithListOfDicts",
      "valuesOptions": {
        "values": [
          "testMergeOverwrite=true",
          "testDataType=dictWithListOfDicts"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n  mergeOverwriteResult: \"{\\\"items\\\":[{\\\"name\\\":\\\"item1\\\",\\\"value\\\":100},{\\\"name\\\":\\\"item2\\\",\\\"value\\\":200},{\\\"name\\\":\\\"item3\\\",\\\"value\\\":300}],\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"shared\\\":\\\"fromMergeDict\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  mergeOverwriteResult: \"{\\\"items\\\":[{\\\"name\\\":\\\"item1\\\",\\\"value\\\":100},{\\\"name\\\":\\\"item2\\\",\\\"value\\\":200},{\\\"name\\\":\\\"item3\\\",\\\"value\\\":300}],\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"shared\\\":\\\"fromMergeDict\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n  mergeOverwriteResult: \"{\\\"items\\\":[{\\\"name\\\":\\\"item1\\\",\\\"value\\\":100},{\\\"name\\\":\\\"item2\\\",\\\"value\\\":200},{\\\"name\\\":\\\"item3\\\",\\\"value\\\":300}],\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"shared\\\":\\\"fromMergeDict\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  mergeOverwriteResult: \"{\\\"items\\\":[{\\\"name\\\":\\\"item1\\\",\\\"value\\\":100},{\\\"name\\\":\\\"item2\\\",\\\"value\\\":200},{\\\"name\\\":\\\"item3\\\",\\\"value\\\":300}],\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"shared\\\":\\\"fromMergeDict\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "mergeOverwrite",
      "dataType": "dictSpecialKeys",
      "valuesOptions": {
        "values": [
          "testMergeOverwrite=true",
          "testDataType=dictSpecialKeys"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n  mergeOverwriteResult: \"{\\\"key with spaces\\\":\\\"value4\\\",\\\"key-with-dash\\\":\\\"value1\\\",\\\"key.with.dots\\\":\\\"value2\\\",\\\"key_with_underscore\\\":\\\"value3\\\",\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"shared\\\":\\\"fromMergeDict\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  mergeOverwriteResult: \"{\\\"key with spaces\\\":\\\"value4\\\",\\\"key-with-dash\\\":\\\"value1\\\",\\\"key.with.dots\\\":\\\"value2\\\",\\\"key_with_underscore\\\":\\\"value3\\\",\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"shared\\\":\\\"fromMergeDict\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n  mergeOverwriteResult: \"{\\\"key with spaces\\\":\\\"value4\\\",\\\"key-with-dash\\\":\\\"value1\\\",\\\"key.with.dots\\\":\\\"value2\\\",\\\"key_with_underscore\\\":\\\"value3\\\",\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"shared\\\":\\\"fromMergeDict\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  mergeOverwriteResult: \"{\\\"key with spaces\\\":\\\"value4\\\",\\\"key-with-dash\\\":\\\"value1\\\",\\\"key.with.dots\\\":\\\"value2\\\",\\\"key_with_underscore\\\":\\\"value3\\\",\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"shared\\\":\\\"fromMergeDict\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "mergeOverwrite",
      "dataType": "dictNumericKeys",
      "valuesOptions": {
        "values": [
          "testMergeOverwrite=true",
          "testDataType=dictNumericKeys"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n  mergeOverwriteResult: \"{\\\"1\\\":\\\"first\\\",\\\"100\\\":\\\"hundredth\\\",\\\"2\\\":\\\"second\\\",\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"shared\\\":\\\"fromMergeDict\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  mergeOverwriteResult: \"{\\\"1\\\":\\\"first\\\",\\\"100\\\":\\\"hundredth\\\",\\\"2\\\":\\\"second\\\",\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"shared\\\":\\\"fromMergeDict\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n  mergeOverwriteResult: \"{\\\"1\\\":\\\"first\\\",\\\"100\\\":\\\"hundredth\\\",\\\"2\\\":\\\"second\\\",\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"shared\\\":\\\"fromMergeDict\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  mergeOverwriteResult: \"{\\\"1\\\":\\\"first\\\",\\\"100\\\":\\\"hundredth\\\",\\\"2\\\":\\\"second\\\",\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"shared\\\":\\\"fromMergeDict\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "mergeOverwrite",
      "dataType": "dictLargeFlat",
      "valuesOptions": {
        "values": [
          "testMergeOverwrite=true",
          "testDataType=dictLargeFlat"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n  mergeOverwriteResult: \"{\\\"key1\\\":\\\"value1\\\",\\\"key10\\\":\\\"value10\\\",\\\"key2\\\":\\\"value2\\\",\\\"key3\\\":\\\"value3\\\",\\\"key4\\\":\\\"value4\\\",\\\"key5\\\":\\\"value5\\\",\\\"key6\\\":\\\"value6\\\",\\\"key7\\\":\\\"value7\\\",\\\"key8\\\":\\\"value8\\\",\\\"key9\\\":\\\"value9\\\",\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"shared\\\":\\\"fromMergeDict\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  mergeOverwriteResult: \"{\\\"key1\\\":\\\"value1\\\",\\\"key10\\\":\\\"value10\\\",\\\"key2\\\":\\\"value2\\\",\\\"key3\\\":\\\"value3\\\",\\\"key4\\\":\\\"value4\\\",\\\"key5\\\":\\\"value5\\\",\\\"key6\\\":\\\"value6\\\",\\\"key7\\\":\\\"value7\\\",\\\"key8\\\":\\\"value8\\\",\\\"key9\\\":\\\"value9\\\",\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"shared\\\":\\\"fromMergeDict\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n  mergeOverwriteResult: \"{\\\"key1\\\":\\\"value1\\\",\\\"key10\\\":\\\"value10\\\",\\\"key2\\\":\\\"value2\\\",\\\"key3\\\":\\\"value3\\\",\\\"key4\\\":\\\"value4\\\",\\\"key5\\\":\\\"value5\\\",\\\"key6\\\":\\\"value6\\\",\\\"key7\\\":\\\"value7\\\",\\\"key8\\\":\\\"value8\\\",\\\"key9\\\":\\\"value9\\\",\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"shared\\\":\\\"fromMergeDict\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  mergeOverwriteResult: \"{\\\"key1\\\":\\\"value1\\\",\\\"key10\\\":\\\"value10\\\",\\\"key2\\\":\\\"value2\\\",\\\"key3\\\":\\\"value3\\\",\\\"key4\\\":\\\"value4\\\",\\\"key5\\\":\\\"value5\\\",\\\"key6\\\":\\\"value6\\\",\\\"key7\\\":\\\"value7\\\",\\\"key8\\\":\\\"value8\\\",\\\"key9\\\":\\\"value9\\\",\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"shared\\\":\\\"fromMergeDict\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "mergeOverwrite",
      "dataType": "dictForMerge",
      "valuesOptions": {
        "values": [
          "testMergeOverwrite=true",
          "testDataType=dictForMerge"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n  mergeOverwriteResult: \"{\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"shared\\\":\\\"fromMergeDict\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  mergeOverwriteResult: \"{\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"shared\\\":\\\"fromMergeDict\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n  mergeOverwriteResult: \"{\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"shared\\\":\\\"fromMergeDict\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  mergeOverwriteResult: \"{\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"shared\\\":\\\"fromMergeDict\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "mergeOverwrite",
      "dataType": "dictValue",
      "valuesOptions": {
        "values": [
          "testMergeOverwrite=true",
          "testDataType=dictValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n  mergeOverwriteResult: \"{\\\"key\\\":\\\"value\\\",\\\"nested\\\":{\\\"level2\\\":\\\"deepValue\\\",\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"shared\\\":\\\"fromMergeDict\\\",\\\"unique1\\\":\\\"onlyInOriginal\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  mergeOverwriteResult: \"{\\\"key\\\":\\\"value\\\",\\\"nested\\\":{\\\"level2\\\":\\\"deepValue\\\",\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"shared\\\":\\\"fromMergeDict\\\",\\\"unique1\\\":\\\"onlyInOriginal\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n  mergeOverwriteResult: \"{\\\"key\\\":\\\"value\\\",\\\"nested\\\":{\\\"level2\\\":\\\"deepValue\\\",\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"shared\\\":\\\"fromMergeDict\\\",\\\"unique1\\\":\\\"onlyInOriginal\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  mergeOverwriteResult: \"{\\\"key\\\":\\\"value\\\",\\\"nested\\\":{\\\"level2\\\":\\\"deepValue\\\",\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"shared\\\":\\\"fromMergeDict\\\",\\\"unique1\\\":\\\"onlyInOriginal\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "mergeOverwrite",
      "dataType": "listOfDicts",
      "valuesOptions": {
        "values": [
          "testMergeOverwrite=true",
          "testDataType=listOfDicts"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:69:42: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003c$testData\u003e: wrong type for value; expected map[string]interface {}; got []interface {}",
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      },
      "debug": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:69:42: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003c$testData\u003e: wrong type for value; expected map[string]interface {}; got []interface {}",
        "debugLogs": [],
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      }
    },
    {
      "toggle": "mergeOverwrite",
      "dataType": "listEmpty",
      "valuesOptions": {
        "values": [
          "testMergeOverwrite=true",
          "testDataType=listEmpty"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:69:42: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003c$testData\u003e: wrong type for value; expected map[string]interface {}; got []interface {}",
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      },
      "debug": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:69:42: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003c$testData\u003e: wrong type for value; expected map[string]interface {}; got []interface {}",
        "debugLogs": [],
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      }
    },
    {
      "toggle": "mergeOverwrite",
      "dataType": "listMixed",
      "valuesOptions": {
        "values": [
          "testMergeOverwrite=true",
          "testDataType=listMixed"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:69:42: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003c$testData\u003e: wrong type for value; expected map[string]interface {}; got []interface {}",
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      },
      "debug": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:69:42: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003c$testData\u003e: wrong type for value; expected map[string]interface {}; got []interface {}",
        "debugLogs": [],
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      }
    },
    {
      "toggle": "mustMergeOverwrite",
      "dataType": "nilValue",
      "valuesOptions": {
        "values": [
          "testMustMergeOverwrite=true",
          "testDataType=nilValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n  mustMergeOverwriteResult: \"{\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"shared\\\":\\\"fromMergeDict\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  mustMergeOverwriteResult: \"{\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"shared\\\":\\\"fromMergeDict\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n  mustMergeOverwriteResult: \"{\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"shared\\\":\\\"fromMergeDict\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  mustMergeOverwriteResult: \"{\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"shared\\\":\\\"fromMergeDict\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "mustMergeOverwrite",
      "dataType": "stringValue",
      "valuesOptions": {
        "values": [
          "testMustMergeOverwrite=true",
          "testDataType=stringValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:74:50: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003c$testData\u003e: wrong type for value; expected map[string]interface {}; got string",
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      },
      "debug": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:74:50: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003c$testData\u003e: wrong type for value; expected map[string]interface {}; got string",
        "debugLogs": [],
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      }
    },
    {
      "toggle": "mustMergeOverwrite",
      "dataType": "numberValue",
      "valuesOptions": {
        "values": [
          "testMustMergeOverwrite=true",
          "testDataType=numberValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:74:50: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003c$testData\u003e: wrong type for value; expected map[string]interface {}; got float64",
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      },
      "debug": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:74:50: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003c$testData\u003e: wrong type for value; expected map[string]interface {}; got float64",
        "debugLogs": [],
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      }
    },
    {
      "toggle": "mustMergeOverwrite",
      "dataType": "boolValue",
      "valuesOptions": {
        "values": [
          "testMustMergeOverwrite=true",
          "testDataType=boolValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:74:50: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003c$testData\u003e: wrong type for value; expected map[string]interface {}; got bool",
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      },
      "debug": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:74:50: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003c$testData\u003e: wrong type for value; expected map[string]interface {}; got bool",
        "debugLogs": [],
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      }
    },
    {
      "toggle": "mustMergeOverwrite",
      "dataType": "listValue",
      "valuesOptions": {
        "values": [
          "testMustMergeOverwrite=true",
          "testDataType=listValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:74:50: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003c$testData\u003e: wrong type for value; expected map[string]interface {}; got []interface {}",
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      },
      "debug": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:74:50: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003c$testData\u003e: wrong type for value; expected map[string]interface {}; got []interface {}",
        "debugLogs": [],
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      }
    },
    {
      "toggle": "mustMergeOverwrite",
      "dataType": "dictSimple",
      "valuesOptions": {
        "values": [
          "testMustMergeOverwrite=true",
          "testDataType=dictSimple"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n  mustMergeOverwriteResult: \"{\\\"key\\\":\\\"value\\\",\\\"name\\\":\\\"test\\\",\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"shared\\\":\\\"fromMergeDict\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  mustMergeOverwriteResult: \"{\\\"key\\\":\\\"value\\\",\\\"name\\\":\\\"test\\\",\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"shared\\\":\\\"fromMergeDict\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n  mustMergeOverwriteResult: \"{\\\"key\\\":\\\"value\\\",\\\"name\\\":\\\"test\\\",\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"shared\\\":\\\"fromMergeDict\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  mustMergeOverwriteResult: \"{\\\"key\\\":\\\"value\\\",\\\"name\\\":\\\"test\\\",\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"shared\\\":\\\"fromMergeDict\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "mustMergeOverwrite",
      "dataType": "dictEmpty",
      "valuesOptions": {
        "values": [
          "testMustMergeOverwrite=true",
          "testDataType=dictEmpty"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n  mustMergeOverwriteResult: \"{\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"shared\\\":\\\"fromMergeDict\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  mustMergeOverwriteResult: \"{\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"shared\\\":\\\"fromMergeDict\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n  mustMergeOverwriteResult: \"{\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"shared\\\":\\\"fromMergeDict\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  mustMergeOverwriteResult: \"{\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"shared\\\":\\\"fromMergeDict\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "mustMergeOverwrite",
      "dataType": "dictNested",
      "valuesOptions": {
        "values": [
          "testMustMergeOverwrite=true",
          "testDataType=dictNested"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n  mustMergeOverwriteResult: \"{\\\"level1\\\":{\\\"another\\\":\\\"data\\\",\\\"level2\\\":\\\"deepValue\\\"},\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"shared\\\":\\\"fromMergeDict\\\",\\\"topKey\\\":\\\"topValue\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  mustMergeOverwriteResult: \"{\\\"level1\\\":{\\\"another\\\":\\\"data\\\",\\\"level2\\\":\\\"deepValue\\\"},\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"shared\\\":\\\"fromMergeDict\\\",\\\"topKey\\\":\\\"topValue\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n  mustMergeOverwriteResult: \"{\\\"level1\\\":{\\\"another\\\":\\\"data\\\",\\\"level2\\\":\\\"deepValue\\\"},\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"shared\\\":\\\"fromMergeDict\\\",\\\"topKey\\\":\\\"topValue\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  mustMergeOverwriteResult: \"{\\\"level1\\\":{\\\"another\\\":\\\"data\\\",\\\"level2\\\":\\\"deepValue\\\"},\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"shared\\\":\\\"fromMergeDict\\\",\\\"topKey\\\":\\\"topValue\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "mustMergeOverwrite",
      "dataType": "dictDeepNested",
      "valuesOptions": {
        "values": [
          "testMustMergeOverwrite=true",
          "testDataType=dictDeepNested"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n  mustMergeOverwriteResult: \"{\\\"a\\\":{\\\"b\\\":{\\\"c\\\":{\\\"d\\\":\\\"deepest\\\",\\\"value\\\":\\\"found\\\"},\\\"shallow\\\":\\\"here\\\"},\\\"mid\\\":\\\"level\\\"},\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"root\\\":\\\"value\\\",\\\"shared\\\":\\\"fromMergeDict\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  mustMergeOverwriteResult: \"{\\\"a\\\":{\\\"b\\\":{\\\"c\\\":{\\\"d\\\":\\\"deepest\\\",\\\"value\\\":\\\"found\\\"},\\\"shallow\\\":\\\"here\\\"},\\\"mid\\\":\\\"level\\\"},\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"root\\\":\\\"value\\\",\\\"shared\\\":\\\"fromMergeDict\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n  mustMergeOverwriteResult: \"{\\\"a\\\":{\\\"b\\\":{\\\"c\\\":{\\\"d\\\":\\\"deepest\\\",\\\"value\\\":\\\"found\\\"},\\\"shallow\\\":\\\"here\\\"},\\\"mid\\\":\\\"level\\\"},\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"root\\\":\\\"value\\\",\\\"shared\\\":\\\"fromMergeDict\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  mustMergeOverwriteResult: \"{\\\"a\\\":{\\\"b\\\":{\\\"c\\\":{\\\"d\\\":\\\"deepest\\\",\\\"value\\\":\\\"found\\\"},\\\"shallow\\\":\\\"here\\\"},\\\"mid\\\":\\\"level\\\"},\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"root\\\":\\\"value\\\",\\\"shared\\\":\\\"fromMergeDict\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "mustMergeOverwrite",
      "dataType": "dictMixedTypes",
      "valuesOptions": {
        "values": [
          "testMustMergeOverwrite=true",
          "testDataType=dictMixedTypes"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n  mustMergeOverwriteResult: \"{\\\"boolKey\\\":true,\\\"listKey\\\":[1,2,3],\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"nestedKey\\\":{\\\"inner\\\":\\\"value\\\"},\\\"nullKey\\\":null,\\\"numberKey\\\":123,\\\"shared\\\":\\\"fromMergeDict\\\",\\\"stringKey\\\":\\\"hello\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  mustMergeOverwriteResult: \"{\\\"boolKey\\\":true,\\\"listKey\\\":[1,2,3],\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"nestedKey\\\":{\\\"inner\\\":\\\"value\\\"},\\\"nullKey\\\":null,\\\"numberKey\\\":123,\\\"shared\\\":\\\"fromMergeDict\\\",\\\"stringKey\\\":\\\"hello\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n  mustMergeOverwriteResult: \"{\\\"boolKey\\\":true,\\\"listKey\\\":[1,2,3],\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"nestedKey\\\":{\\\"inner\\\":\\\"value\\\"},\\\"nullKey\\\":null,\\\"numberKey\\\":123,\\\"shared\\\":\\\"fromMergeDict\\\",\\\"stringKey\\\":\\\"hello\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  mustMergeOverwriteResult: \"{\\\"boolKey\\\":true,\\\"listKey\\\":[1,2,3],\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"nestedKey\\\":{\\\"inner\\\":\\\"value\\\"},\\\"nullKey\\\":null,\\\"numberKey\\\":123,\\\"shared\\\":\\\"fromMergeDict\\\",\\\"stringKey\\\":\\\"hello\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "mustMergeOverwrite",
      "dataType": "dictWithListOfDicts",
      "valuesOptions": {
        "values": [
          "testMustMergeOverwrite=true",
          "testDataType=dictWithListOfDicts"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n  mustMergeOverwriteResult: \"{\\\"items\\\":[{\\\"name\\\":\\\"item1\\\",\\\"value\\\":100},{\\\"name\\\":\\\"item2\\\",\\\"value\\\":200},{\\\"name\\\":\\\"item3\\\",\\\"value\\\":300}],\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"shared\\\":\\\"fromMergeDict\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  mustMergeOverwriteResult: \"{\\\"items\\\":[{\\\"name\\\":\\\"item1\\\",\\\"value\\\":100},{\\\"name\\\":\\\"item2\\\",\\\"value\\\":200},{\\\"name\\\":\\\"item3\\\",\\\"value\\\":300}],\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"shared\\\":\\\"fromMergeDict\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n  mustMergeOverwriteResult: \"{\\\"items\\\":[{\\\"name\\\":\\\"item1\\\",\\\"value\\\":100},{\\\"name\\\":\\\"item2\\\",\\\"value\\\":200},{\\\"name\\\":\\\"item3\\\",\\\"value\\\":300}],\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"shared\\\":\\\"fromMergeDict\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  mustMergeOverwriteResult: \"{\\\"items\\\":[{\\\"name\\\":\\\"item1\\\",\\\"value\\\":100},{\\\"name\\\":\\\"item2\\\",\\\"value\\\":200},{\\\"name\\\":\\\"item3\\\",\\\"value\\\":300}],\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"shared\\\":\\\"fromMergeDict\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "mustMergeOverwrite",
      "dataType": "dictSpecialKeys",
      "valuesOptions": {
        "values": [
          "testMustMergeOverwrite=true",
          "testDataType=dictSpecialKeys"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n  mustMergeOverwriteResult: \"{\\\"key with spaces\\\":\\\"value4\\\",\\\"key-with-dash\\\":\\\"value1\\\",\\\"key.with.dots\\\":\\\"value2\\\",\\\"key_with_underscore\\\":\\\"value3\\\",\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"shared\\\":\\\"fromMergeDict\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  mustMergeOverwriteResult: \"{\\\"key with spaces\\\":\\\"value4\\\",\\\"key-with-dash\\\":\\\"value1\\\",\\\"key.with.dots\\\":\\\"value2\\\",\\\"key_with_underscore\\\":\\\"value3\\\",\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"shared\\\":\\\"fromMergeDict\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n  mustMergeOverwriteResult: \"{\\\"key with spaces\\\":\\\"value4\\\",\\\"key-with-dash\\\":\\\"value1\\\",\\\"key.with.dots\\\":\\\"value2\\\",\\\"key_with_underscore\\\":\\\"value3\\\",\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"shared\\\":\\\"fromMergeDict\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  mustMergeOverwriteResult: \"{\\\"key with spaces\\\":\\\"value4\\\",\\\"key-with-dash\\\":\\\"value1\\\",\\\"key.with.dots\\\":\\\"value2\\\",\\\"key_with_underscore\\\":\\\"value3\\\",\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"shared\\\":\\\"fromMergeDict\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "mustMergeOverwrite",
      "dataType": "dictNumericKeys",
      "valuesOptions": {
        "values": [
          "testMustMergeOverwrite=true",
          "testDataType=dictNumericKeys"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n  mustMergeOverwriteResult: \"{\\\"1\\\":\\\"first\\\",\\\"100\\\":\\\"hundredth\\\",\\\"2\\\":\\\"second\\\",\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"shared\\\":\\\"fromMergeDict\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  mustMergeOverwriteResult: \"{\\\"1\\\":\\\"first\\\",\\\"100\\\":\\\"hundredth\\\",\\\"2\\\":\\\"second\\\",\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"shared\\\":\\\"fromMergeDict\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n  mustMergeOverwriteResult: \"{\\\"1\\\":\\\"first\\\",\\\"100\\\":\\\"hundredth\\\",\\\"2\\\":\\\"second\\\",\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"shared\\\":\\\"fromMergeDict\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  mustMergeOverwriteResult: \"{\\\"1\\\":\\\"first\\\",\\\"100\\\":\\\"hundredth\\\",\\\"2\\\":\\\"second\\\",\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"shared\\\":\\\"fromMergeDict\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "mustMergeOverwrite",
      "dataType": "dictLargeFlat",
      "valuesOptions": {
        "values": [
          "testMustMergeOverwrite=true",
          "testDataType=dictLargeFlat"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n  mustMergeOverwriteResult: \"{\\\"key1\\\":\\\"value1\\\",\\\"key10\\\":\\\"value10\\\",\\\"key2\\\":\\\"value2\\\",\\\"key3\\\":\\\"value3\\\",\\\"key4\\\":\\\"value4\\\",\\\"key5\\\":\\\"value5\\\",\\\"key6\\\":\\\"value6\\\",\\\"key7\\\":\\\"value7\\\",\\\"key8\\\":\\\"value8\\\",\\\"key9\\\":\\\"value9\\\",\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"shared\\\":\\\"fromMergeDict\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  mustMergeOverwriteResult: \"{\\\"key1\\\":\\\"value1\\\",\\\"key10\\\":\\\"value10\\\",\\\"key2\\\":\\\"value2\\\",\\\"key3\\\":\\\"value3\\\",\\\"key4\\\":\\\"value4\\\",\\\"key5\\\":\\\"value5\\\",\\\"key6\\\":\\\"value6\\\",\\\"key7\\\":\\\"value7\\\",\\\"key8\\\":\\\"value8\\\",\\\"key9\\\":\\\"value9\\\",\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"shared\\\":\\\"fromMergeDict\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n  mustMergeOverwriteResult: \"{\\\"key1\\\":\\\"value1\\\",\\\"key10\\\":\\\"value10\\\",\\\"key2\\\":\\\"value2\\\",\\\"key3\\\":\\\"value3\\\",\\\"key4\\\":\\\"value4\\\",\\\"key5\\\":\\\"value5\\\",\\\"key6\\\":\\\"value6\\\",\\\"key7\\\":\\\"value7\\\",\\\"key8\\\":\\\"value8\\\",\\\"key9\\\":\\\"value9\\\",\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"shared\\\":\\\"fromMergeDict\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  mustMergeOverwriteResult: \"{\\\"key1\\\":\\\"value1\\\",\\\"key10\\\":\\\"value10\\\",\\\"key2\\\":\\\"value2\\\",\\\"key3\\\":\\\"value3\\\",\\\"key4\\\":\\\"value4\\\",\\\"key5\\\":\\\"value5\\\",\\\"key6\\\":\\\"value6\\\",\\\"key7\\\":\\\"value7\\\",\\\"key8\\\":\\\"value8\\\",\\\"key9\\\":\\\"value9\\\",\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"shared\\\":\\\"fromMergeDict\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "mustMergeOverwrite",
      "dataType": "dictForMerge",
      "valuesOptions": {
        "values": [
          "testMustMergeOverwrite=true",
          "testDataType=dictForMerge"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n  mustMergeOverwriteResult: \"{\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"shared\\\":\\\"fromMergeDict\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  mustMergeOverwriteResult: \"{\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"shared\\\":\\\"fromMergeDict\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n  mustMergeOverwriteResult: \"{\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"shared\\\":\\\"fromMergeDict\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  mustMergeOverwriteResult: \"{\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"shared\\\":\\\"fromMergeDict\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "mustMergeOverwrite",
      "dataType": "dictValue",
      "valuesOptions": {
        "values": [
          "testMustMergeOverwrite=true",
          "testDataType=dictValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n  mustMergeOverwriteResult: \"{\\\"key\\\":\\\"value\\\",\\\"nested\\\":{\\\"level2\\\":\\\"deepValue\\\",\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"shared\\\":\\\"fromMergeDict\\\",\\\"unique1\\\":\\\"onlyInOriginal\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  mustMergeOverwriteResult: \"{\\\"key\\\":\\\"value\\\",\\\"nested\\\":{\\\"level2\\\":\\\"deepValue\\\",\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"shared\\\":\\\"fromMergeDict\\\",\\\"unique1\\\":\\\"onlyInOriginal\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n  mustMergeOverwriteResult: \"{\\\"key\\\":\\\"value\\\",\\\"nested\\\":{\\\"level2\\\":\\\"deepValue\\\",\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"shared\\\":\\\"fromMergeDict\\\",\\\"unique1\\\":\\\"onlyInOriginal\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  mustMergeOverwriteResult: \"{\\\"key\\\":\\\"value\\\",\\\"nested\\\":{\\\"level2\\\":\\\"deepValue\\\",\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"shared\\\":\\\"fromMergeDict\\\",\\\"unique1\\\":\\\"onlyInOriginal\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "mustMergeOverwrite",
      "dataType": "listOfDicts",
      "valuesOptions": {
        "values": [
          "testMustMergeOverwrite=true",
          "testDataType=listOfDicts"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:74:50: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003c$testData\u003e: wrong type for value; expected map[string]interface {}; got []interface {}",
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      },
      "debug": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:74:50: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003c$testData\u003e: wrong type for value; expected map[string]interface {}; got []interface {}",
        "debugLogs": [],
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      }
    },
    {
      "toggle": "mustMergeOverwrite",
      "dataType": "listEmpty",
      "valuesOptions": {
        "values": [
          "testMustMergeOverwrite=true",
          "testDataType=listEmpty"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:74:50: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003c$testData\u003e: wrong type for value; expected map[string]interface {}; got []interface {}",
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      },
      "debug": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:74:50: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003c$testData\u003e: wrong type for value; expected map[string]interface {}; got []interface {}",
        "debugLogs": [],
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      }
    },
    {
      "toggle": "mustMergeOverwrite",
      "dataType": "listMixed",
      "valuesOptions": {
        "values": [
          "testMustMergeOverwrite=true",
          "testDataType=listMixed"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:74:50: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003c$testData\u003e: wrong type for value; expected map[string]interface {}; got []interface {}",
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      },
      "debug": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:74:50: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003c$testData\u003e: wrong type for value; expected map[string]interface {}; got []interface {}",
        "debugLogs": [],
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      }
    },
    {
      "toggle": "keys",
      "dataType": "nilValue",
      "valuesOptions": {
        "values": [
          "testKeys=true",
          "testDataType=nilValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n  keysResult: \"[]\"\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  keysResult: \"[]\"\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n  keysResult: \"[]\"\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  keysResult: \"[]\"\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "keys",
      "dataType": "stringValue",
      "valuesOptions": {
        "values": [
          "testKeys=true",
          "testDataType=stringValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:82:22: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003c$testData\u003e: wrong type for value; expected map[string]interface {}; got string",
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      },
      "debug": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:82:22: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003c$testData\u003e: wrong type for value; expected map[string]interface {}; got string",
        "debugLogs": [],
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      }
    },
    {
      "toggle": "keys",
      "dataType": "numberValue",
      "valuesOptions": {
        "values": [
          "testKeys=true",
          "testDataType=numberValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:82:22: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003c$testData\u003e: wrong type for value; expected map[string]interface {}; got float64",
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      },
      "debug": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:82:22: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003c$testData\u003e: wrong type for value; expected map[string]interface {}; got float64",
        "debugLogs": [],
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      }
    },
    {
      "toggle": "keys",
      "dataType": "boolValue",
      "valuesOptions": {
        "values": [
          "testKeys=true",
          "testDataType=boolValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:82:22: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003c$testData\u003e: wrong type for value; expected map[string]interface {}; got bool",
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      },
      "debug": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:82:22: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003c$testData\u003e: wrong type for value; expected map[string]interface {}; got bool",
        "debugLogs": [],
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      }
    },
    {
      "toggle": "keys",
      "dataType": "listValue",
      "valuesOptions": {
        "values": [
          "testKeys=true",
          "testDataType=listValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:82:22: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003c$testData\u003e: wrong type for value; expected map[string]interface {}; got []interface {}",
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      },
      "debug": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:82:22: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003c$testData\u003e: wrong type for value; expected map[string]interface {}; got []interface {}",
        "debugLogs": [],
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      }
    },
    {
      "toggle": "keys",
      "dataType": "dictSimple",
      "valuesOptions": {
        "values": [
          "testKeys=true",
          "testDataType=dictSimple"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n  keysResult: \"[\\\"key\\\",\\\"name\\\"]\"\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  keysResult: \"[\\\"key\\\",\\\"name\\\"]\"\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n  keysResult: \"[\\\"key\\\",\\\"name\\\"]\"\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  keysResult: \"[\\\"key\\\",\\\"name\\\"]\"\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "keys",
      "dataType": "dictEmpty",
      "valuesOptions": {
        "values": [
          "testKeys=true",
          "testDataType=dictEmpty"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n  keysResult: \"[]\"\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  keysResult: \"[]\"\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n  keysResult: \"[]\"\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  keysResult: \"[]\"\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "keys",
      "dataType": "dictNested",
      "valuesOptions": {
        "values": [
          "testKeys=true",
          "testDataType=dictNested"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n  keysResult: \"[\\\"level1\\\",\\\"topKey\\\"]\"\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  keysResult: \"[\\\"level1\\\",\\\"topKey\\\"]\"\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n  keysResult: \"[\\\"level1\\\",\\\"topKey\\\"]\"\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  keysResult: \"[\\\"level1\\\",\\\"topKey\\\"]\"\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "keys",
      "dataType": "dictDeepNested",
      "valuesOptions": {
        "values": [
          "testKeys=true",
          "testDataType=dictDeepNested"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n  keysResult: \"[\\\"a\\\",\\\"root\\\"]\"\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  keysResult: \"[\\\"a\\\",\\\"root\\\"]\"\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n  keysResult: \"[\\\"a\\\",\\\"root\\\"]\"\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  keysResult: \"[\\\"a\\\",\\\"root\\\"]\"\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "keys",
      "dataType": "dictMixedTypes",
      "valuesOptions": {
        "values": [
          "testKeys=true",
          "testDataType=dictMixedTypes"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n  keysResult: \"[\\\"numberKey\\\",\\\"stringKey\\\",\\\"boolKey\\\",\\\"listKey\\\",\\\"nestedKey\\\",\\\"nullKey\\\"]\"\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  keysResult: \"[\\\"numberKey\\\",\\\"stringKey\\\",\\\"boolKey\\\",\\\"listKey\\\",\\\"nestedKey\\\",\\\"nullKey\\\"]\"\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n  keysResult: \"[\\\"listKey\\\",\\\"nestedKey\\\",\\\"nullKey\\\",\\\"numberKey\\\",\\\"stringKey\\\",\\\"boolKey\\\"]\"\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  keysResult: \"[\\\"listKey\\\",\\\"nestedKey\\\",\\\"nullKey\\\",\\\"numberKey\\\",\\\"stringKey\\\",\\\"boolKey\\\"]\"\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "keys",
      "dataType": "dictWithListOfDicts",
      "valuesOptions": {
        "values": [
          "testKeys=true",
          "testDataType=dictWithListOfDicts"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n  keysResult: \"[\\\"items\\\"]\"\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  keysResult: \"[\\\"items\\\"]\"\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n  keysResult: \"[\\\"items\\\"]\"\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  keysResult: \"[\\\"items\\\"]\"\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "keys",
      "dataType": "dictSpecialKeys",
      "valuesOptions": {
        "values": [
          "testKeys=true",
          "testDataType=dictSpecialKeys"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n  keysResult: \"[\\\"key with spaces\\\",\\\"key-with-dash\\\",\\\"key.with.dots\\\",\\\"key_with_underscore\\\"]\"\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  keysResult: \"[\\\"key with spaces\\\",\\\"key-with-dash\\\",\\\"key.with.dots\\\",\\\"key_with_underscore\\\"]\"\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n  keysResult: \"[\\\"key.with.dots\\\",\\\"key_with_underscore\\\",\\\"key with spaces\\\",\\\"key-with-dash\\\"]\"\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  keysResult: \"[\\\"key.with.dots\\\",\\\"key_with_underscore\\\",\\\"key with spaces\\\",\\\"key-with-dash\\\"]\"\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "keys",
      "dataType": "dictNumericKeys",
      "valuesOptions": {
        "values": [
          "testKeys=true",
          "testDataType=dictNumericKeys"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n  keysResult: \"[\\\"1\\\",\\\"100\\\",\\\"2\\\"]\"\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  keysResult: \"[\\\"1\\\",\\\"100\\\",\\\"2\\\"]\"\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n  keysResult: \"[\\\"1\\\",\\\"100\\\",\\\"2\\\"]\"\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  keysResult: \"[\\\"1\\\",\\\"100\\\",\\\"2\\\"]\"\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "keys",
      "dataType": "dictLargeFlat",
      "valuesOptions": {
        "values": [
          "testKeys=true",
          "testDataType=dictLargeFlat"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n  keysResult: \"[\\\"key5\\\",\\\"key6\\\",\\\"key9\\\",\\\"key1\\\",\\\"key10\\\",\\\"key7\\\",\\\"key3\\\",\\\"key8\\\",\\\"key2\\\",\\\"key4\\\"]\"\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  keysResult: \"[\\\"key5\\\",\\\"key6\\\",\\\"key9\\\",\\\"key1\\\",\\\"key10\\\",\\\"key7\\\",\\\"key3\\\",\\\"key8\\\",\\\"key2\\\",\\\"key4\\\"]\"\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n  keysResult: \"[\\\"key8\\\",\\\"key1\\\",\\\"key9\\\",\\\"key2\\\",\\\"key3\\\",\\\"key4\\\",\\\"key7\\\",\\\"key10\\\",\\\"key5\\\",\\\"key6\\\"]\"\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  keysResult: \"[\\\"key8\\\",\\\"key1\\\",\\\"key9\\\",\\\"key2\\\",\\\"key3\\\",\\\"key4\\\",\\\"key7\\\",\\\"key10\\\",\\\"key5\\\",\\\"key6\\\"]\"\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "keys",
      "dataType": "dictForMerge",
      "valuesOptions": {
        "values": [
          "testKeys=true",
          "testDataType=dictForMerge"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n  keysResult: \"[\\\"nested\\\",\\\"shared\\\",\\\"unique2\\\"]\"\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  keysResult: \"[\\\"nested\\\",\\\"shared\\\",\\\"unique2\\\"]\"\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n  keysResult: \"[\\\"shared\\\",\\\"unique2\\\",\\\"nested\\\"]\"\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  keysResult: \"[\\\"shared\\\",\\\"unique2\\\",\\\"nested\\\"]\"\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "keys",
      "dataType": "dictValue",
      "valuesOptions": {
        "values": [
          "testKeys=true",
          "testDataType=dictValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n  keysResult: \"[\\\"unique1\\\",\\\"key\\\",\\\"nested\\\",\\\"shared\\\"]\"\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  keysResult: \"[\\\"unique1\\\",\\\"key\\\",\\\"nested\\\",\\\"shared\\\"]\"\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n  keysResult: \"[\\\"key\\\",\\\"nested\\\",\\\"shared\\\",\\\"unique1\\\"]\"\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  keysResult: \"[\\\"key\\\",\\\"nested\\\",\\\"shared\\\",\\\"unique1\\\"]\"\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "keys",
      "dataType": "listOfDicts",
      "valuesOptions": {
        "values": [
          "testKeys=true",
          "testDataType=listOfDicts"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:82:22: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003c$testData\u003e: wrong type for value; expected map[string]interface {}; got []interface {}",
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      },
      "debug": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:82:22: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003c$testData\u003e: wrong type for value; expected map[string]interface {}; got []interface {}",
        "debugLogs": [],
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      }
    },
    {
      "toggle": "keys",
      "dataType": "listEmpty",
      "valuesOptions": {
        "values": [
          "testKeys=true",
          "testDataType=listEmpty"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:82:22: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003c$testData\u003e: wrong type for value; expected map[string]interface {}; got []interface {}",
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      },
      "debug": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:82:22: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003c$testData\u003e: wrong type for value; expected map[string]interface {}; got []interface {}",
        "debugLogs": [],
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      }
    },
    {
      "toggle": "keys",
      "dataType": "listMixed",
      "valuesOptions": {
        "values": [
          "testKeys=true",
          "testDataType=listMixed"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:82:22: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003c$testData\u003e: wrong type for value; expected map[string]interface {}; got []interface {}",
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      },
      "debug": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:82:22: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003c$testData\u003e: wrong type for value; expected map[string]interface {}; got []interface {}",
        "debugLogs": [],
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      }
    },
    {
      "toggle": "pick",
      "dataType": "nilValue",
      "valuesOptions": {
        "values": [
          "testPick=true",
          "testDataType=nilValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n  pickResult: \"{}\"\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n  pickResult: \"{}\"\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n  pickResult: \"{}\"\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n  pickResult: \"{}\"\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "pick",
      "dataType": "stringValue",
      "valuesOptions": {
        "values": [
          "testPick=true",
          "testDataType=stringValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:87:22: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003c$testData\u003e: wrong type for value; expected map[string]interface {}; got string",
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      },
      "debug": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:87:22: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003c$testData\u003e: wrong type for value; expected map[string]interface {}; got string",
        "debugLogs": [],
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      }
    },
    {
      "toggle": "pick",
      "dataType": "numberValue",
      "valuesOptions": {
        "values": [
          "testPick=true",
          "testDataType=numberValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:87:22: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003c$testData\u003e: wrong type for value; expected map[string]interface {}; got float64",
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      },
      "debug": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:87:22: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003c$testData\u003e: wrong type for value; expected map[string]interface {}; got float64",
        "debugLogs": [],
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      }
    },
    {
      "toggle": "pick",
      "dataType": "boolValue",
      "valuesOptions": {
        "values": [
          "testPick=true",
          "testDataType=boolValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:87:22: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003c$testData\u003e: wrong type for value; expected map[string]interface {}; got bool",
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      },
      "debug": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:87:22: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003c$testData\u003e: wrong type for value; expected map[string]interface {}; got bool",
        "debugLogs": [],
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      }
    },
    {
      "toggle": "pick",
      "dataType": "listValue",
      "valuesOptions": {
        "values": [
          "testPick=true",
          "testDataType=listValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:87:22: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003c$testData\u003e: wrong type for value; expected map[string]interface {}; got []interface {}",
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      },
      "debug": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:87:22: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003c$testData\u003e: wrong type for value; expected map[string]interface {}; got []interface {}",
        "debugLogs": [],
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      }
    },
    {
      "toggle": "pick",
      "dataType": "dictSimple",
      "valuesOptions": {
        "values": [
          "testPick=true",
          "testDataType=dictSimple"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n  pickResult: \"{\\\"key\\\":\\\"value\\\",\\\"name\\\":\\\"test\\\"}\"\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n  pickResult: \"{\\\"key\\\":\\\"value\\\",\\\"name\\\":\\\"test\\\"}\"\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n  pickResult: \"{\\\"key\\\":\\\"value\\\",\\\"name\\\":\\\"test\\\"}\"\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n  pickResult: \"{\\\"key\\\":\\\"value\\\",\\\"name\\\":\\\"test\\\"}\"\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "pick",
      "dataType": "dictEmpty",
      "valuesOptions": {
        "values": [
          "testPick=true",
          "testDataType=dictEmpty"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n  pickResult: \"{}\"\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n  pickResult: \"{}\"\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n  pickResult: \"{}\"\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n  pickResult: \"{}\"\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "pick",
      "dataType": "dictNested",
      "valuesOptions": {
        "values": [
          "testPick=true",
          "testDataType=dictNested"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n  pickResult: \"{}\"\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n  pickResult: \"{}\"\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n  pickResult: \"{}\"\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n  pickResult: \"{}\"\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "pick",
      "dataType": "dictDeepNested",
      "valuesOptions": {
        "values": [
          "testPick=true",
          "testDataType=dictDeepNested"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n  pickResult: \"{\\\"a\\\":{\\\"b\\\":{\\\"c\\\":{\\\"d\\\":\\\"deepest\\\",\\\"value\\\":\\\"found\\\"},\\\"shallow\\\":\\\"here\\\"},\\\"mid\\\":\\\"level\\\"}}\"\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n  pickResult: \"{\\\"a\\\":{\\\"b\\\":{\\\"c\\\":{\\\"d\\\":\\\"deepest\\\",\\\"value\\\":\\\"found\\\"},\\\"shallow\\\":\\\"here\\\"},\\\"mid\\\":\\\"level\\\"}}\"\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n  pickResult: \"{\\\"a\\\":{\\\"b\\\":{\\\"c\\\":{\\\"d\\\":\\\"deepest\\\",\\\"value\\\":\\\"found\\\"},\\\"shallow\\\":\\\"here\\\"},\\\"mid\\\":\\\"level\\\"}}\"\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n  pickResult: \"{\\\"a\\\":{\\\"b\\\":{\\\"c\\\":{\\\"d\\\":\\\"deepest\\\",\\\"value\\\":\\\"found\\\"},\\\"shallow\\\":\\\"here\\\"},\\\"mid\\\":\\\"level\\\"}}\"\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "pick",
      "dataType": "dictMixedTypes",
      "valuesOptions": {
        "values": [
          "testPick=true",
          "testDataType=dictMixedTypes"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n  pickResult: \"{\\\"stringKey\\\":\\\"hello\\\"}\"\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n  pickResult: \"{\\\"stringKey\\\":\\\"hello\\\"}\"\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n  pickResult: \"{\\\"stringKey\\\":\\\"hello\\\"}\"\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n  pickResult: \"{\\\"stringKey\\\":\\\"hello\\\"}\"\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "pick",
      "dataType": "dictWithListOfDicts",
      "valuesOptions": {
        "values": [
          "testPick=true",
          "testDataType=dictWithListOfDicts"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n  pickResult: \"{}\"\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n  pickResult: \"{}\"\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n  pickResult: \"{}\"\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n  pickResult: \"{}\"\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "pick",
      "dataType": "dictSpecialKeys",
      "valuesOptions": {
        "values": [
          "testPick=true",
          "testDataType=dictSpecialKeys"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n  pickResult: \"{}\"\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n  pickResult: \"{}\"\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n  pickResult: \"{}\"\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n  pickResult: \"{}\"\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "pick",
      "dataType": "dictNumericKeys",
      "valuesOptions": {
        "values": [
          "testPick=true",
          "testDataType=dictNumericKeys"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n  pickResult: \"{}\"\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n  pickResult: \"{}\"\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n  pickResult: \"{}\"\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n  pickResult: \"{}\"\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "pick",
      "dataType": "dictLargeFlat",
      "valuesOptions": {
        "values": [
          "testPick=true",
          "testDataType=dictLargeFlat"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n  pickResult: \"{}\"\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n  pickResult: \"{}\"\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n  pickResult: \"{}\"\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n  pickResult: \"{}\"\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "pick",
      "dataType": "dictForMerge",
      "valuesOptions": {
        "values": [
          "testPick=true",
          "testDataType=dictForMerge"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n  pickResult: \"{}\"\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n  pickResult: \"{}\"\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n  pickResult: \"{}\"\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n  pickResult: \"{}\"\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "pick",
      "dataType": "dictValue",
      "valuesOptions": {
        "values": [
          "testPick=true",
          "testDataType=dictValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n  pickResult: \"{\\\"key\\\":\\\"value\\\"}\"\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n  pickResult: \"{\\\"key\\\":\\\"value\\\"}\"\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n  pickResult: \"{\\\"key\\\":\\\"value\\\"}\"\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n  pickResult: \"{\\\"key\\\":\\\"value\\\"}\"\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "pick",
      "dataType": "listOfDicts",
      "valuesOptions": {
        "values": [
          "testPick=true",
          "testDataType=listOfDicts"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:87:22: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003c$testData\u003e: wrong type for value; expected map[string]interface {}; got []interface {}",
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      },
      "debug": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:87:22: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003c$testData\u003e: wrong type for value; expected map[string]interface {}; got []interface {}",
        "debugLogs": [],
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      }
    },
    {
      "toggle": "pick",
      "dataType": "listEmpty",
      "valuesOptions": {
        "values": [
          "testPick=true",
          "testDataType=listEmpty"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:87:22: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003c$testData\u003e: wrong type for value; expected map[string]interface {}; got []interface {}",
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      },
      "debug": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:87:22: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003c$testData\u003e: wrong type for value; expected map[string]interface {}; got []interface {}",
        "debugLogs": [],
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      }
    },
    {
      "toggle": "pick",
      "dataType": "listMixed",
      "valuesOptions": {
        "values": [
          "testPick=true",
          "testDataType=listMixed"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:87:22: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003c$testData\u003e: wrong type for value; expected map[string]interface {}; got []interface {}",
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      },
      "debug": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:87:22: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003c$testData\u003e: wrong type for value; expected map[string]interface {}; got []interface {}",
        "debugLogs": [],
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      }
    },
    {
      "toggle": "omit",
      "dataType": "nilValue",
      "valuesOptions": {
        "values": [
          "testOmit=true",
          "testDataType=nilValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n  omitResult: \"{}\"\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n  omitResult: \"{}\"\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n  omitResult: \"{}\"\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n  omitResult: \"{}\"\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "omit",
      "dataType": "stringValue",
      "valuesOptions": {
        "values": [
          "testOmit=true",
          "testDataType=stringValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:92:22: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003c$testData\u003e: wrong type for value; expected map[string]interface {}; got string",
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      },
      "debug": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:92:22: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003c$testData\u003e: wrong type for value; expected map[string]interface {}; got string",
        "debugLogs": [],
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      }
    },
    {
      "toggle": "omit",
      "dataType": "numberValue",
      "valuesOptions": {
        "values": [
          "testOmit=true",
          "testDataType=numberValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:92:22: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003c$testData\u003e: wrong type for value; expected map[string]interface {}; got float64",
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      },
      "debug": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:92:22: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003c$testData\u003e: wrong type for value; expected map[string]interface {}; got float64",
        "debugLogs": [],
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      }
    },
    {
      "toggle": "omit",
      "dataType": "boolValue",
      "valuesOptions": {
        "values": [
          "testOmit=true",
          "testDataType=boolValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:92:22: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003c$testData\u003e: wrong type for value; expected map[string]interface {}; got bool",
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      },
      "debug": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:92:22: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003c$testData\u003e: wrong type for value; expected map[string]interface {}; got bool",
        "debugLogs": [],
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      }
    },
    {
      "toggle": "omit",
      "dataType": "listValue",
      "valuesOptions": {
        "values": [
          "testOmit=true",
          "testDataType=listValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:92:22: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003c$testData\u003e: wrong type for value; expected map[string]interface {}; got []interface {}",
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      },
      "debug": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:92:22: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003c$testData\u003e: wrong type for value; expected map[string]interface {}; got []interface {}",
        "debugLogs": [],
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      }
    },
    {
      "toggle": "omit",
      "dataType": "dictSimple",
      "valuesOptions": {
        "values": [
          "testOmit=true",
          "testDataType=dictSimple"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n  omitResult: \"{}\"\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n  omitResult: \"{}\"\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n  omitResult: \"{}\"\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n  omitResult: \"{}\"\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "omit",
      "dataType": "dictEmpty",
      "valuesOptions": {
        "values": [
          "testOmit=true",
          "testDataType=dictEmpty"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n  omitResult: \"{}\"\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n  omitResult: \"{}\"\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n  omitResult: \"{}\"\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n  omitResult: \"{}\"\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "omit",
      "dataType": "dictNested",
      "valuesOptions": {
        "values": [
          "testOmit=true",
          "testDataType=dictNested"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n  omitResult: \"{\\\"level1\\\":{\\\"another\\\":\\\"data\\\",\\\"level2\\\":\\\"deepValue\\\"},\\\"topKey\\\":\\\"topValue\\\"}\"\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n  omitResult: \"{\\\"level1\\\":{\\\"another\\\":\\\"data\\\",\\\"level2\\\":\\\"deepValue\\\"},\\\"topKey\\\":\\\"topValue\\\"}\"\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n  omitResult: \"{\\\"level1\\\":{\\\"another\\\":\\\"data\\\",\\\"level2\\\":\\\"deepValue\\\"},\\\"topKey\\\":\\\"topValue\\\"}\"\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n  omitResult: \"{\\\"level1\\\":{\\\"another\\\":\\\"data\\\",\\\"level2\\\":\\\"deepValue\\\"},\\\"topKey\\\":\\\"topValue\\\"}\"\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "omit",
      "dataType": "dictDeepNested",
      "valuesOptions": {
        "values": [
          "testOmit=true",
          "testDataType=dictDeepNested"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n  omitResult: \"{\\\"a\\\":{\\\"b\\\":{\\\"c\\\":{\\\"d\\\":\\\"deepest\\\",\\\"value\\\":\\\"found\\\"},\\\"shallow\\\":\\\"here\\\"},\\\"mid\\\":\\\"level\\\"},\\\"root\\\":\\\"value\\\"}\"\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n  omitResult: \"{\\\"a\\\":{\\\"b\\\":{\\\"c\\\":{\\\"d\\\":\\\"deepest\\\",\\\"value\\\":\\\"found\\\"},\\\"shallow\\\":\\\"here\\\"},\\\"mid\\\":\\\"level\\\"},\\\"root\\\":\\\"value\\\"}\"\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n  omitResult: \"{\\\"a\\\":{\\\"b\\\":{\\\"c\\\":{\\\"d\\\":\\\"deepest\\\",\\\"value\\\":\\\"found\\\"},\\\"shallow\\\":\\\"here\\\"},\\\"mid\\\":\\\"level\\\"},\\\"root\\\":\\\"value\\\"}\"\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n  omitResult: \"{\\\"a\\\":{\\\"b\\\":{\\\"c\\\":{\\\"d\\\":\\\"deepest\\\",\\\"value\\\":\\\"found\\\"},\\\"shallow\\\":\\\"here\\\"},\\\"mid\\\":\\\"level\\\"},\\\"root\\\":\\\"value\\\"}\"\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "omit",
      "dataType": "dictMixedTypes",
      "valuesOptions": {
        "values": [
          "testOmit=true",
          "testDataType=dictMixedTypes"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n  omitResult: \"{\\\"boolKey\\\":true,\\\"listKey\\\":[1,2,3],\\\"nestedKey\\\":{\\\"inner\\\":\\\"value\\\"},\\\"nullKey\\\":null,\\\"numberKey\\\":123,\\\"stringKey\\\":\\\"hello\\\"}\"\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n  omitResult: \"{\\\"boolKey\\\":true,\\\"listKey\\\":[1,2,3],\\\"nestedKey\\\":{\\\"inner\\\":\\\"value\\\"},\\\"nullKey\\\":null,\\\"numberKey\\\":123,\\\"stringKey\\\":\\\"hello\\\"}\"\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n  omitResult: \"{\\\"boolKey\\\":true,\\\"listKey\\\":[1,2,3],\\\"nestedKey\\\":{\\\"inner\\\":\\\"value\\\"},\\\"nullKey\\\":null,\\\"numberKey\\\":123,\\\"stringKey\\\":\\\"hello\\\"}\"\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n  omitResult: \"{\\\"boolKey\\\":true,\\\"listKey\\\":[1,2,3],\\\"nestedKey\\\":{\\\"inner\\\":\\\"value\\\"},\\\"nullKey\\\":null,\\\"numberKey\\\":123,\\\"stringKey\\\":\\\"hello\\\"}\"\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "omit",
      "dataType": "dictWithListOfDicts",
      "valuesOptions": {
        "values": [
          "testOmit=true",
          "testDataType=dictWithListOfDicts"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n  omitResult: \"{\\\"items\\\":[{\\\"name\\\":\\\"item1\\\",\\\"value\\\":100},{\\\"name\\\":\\\"item2\\\",\\\"value\\\":200},{\\\"name\\\":\\\"item3\\\",\\\"value\\\":300}]}\"\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n  omitResult: \"{\\\"items\\\":[{\\\"name\\\":\\\"item1\\\",\\\"value\\\":100},{\\\"name\\\":\\\"item2\\\",\\\"value\\\":200},{\\\"name\\\":\\\"item3\\\",\\\"value\\\":300}]}\"\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n  omitResult: \"{\\\"items\\\":[{\\\"name\\\":\\\"item1\\\",\\\"value\\\":100},{\\\"name\\\":\\\"item2\\\",\\\"value\\\":200},{\\\"name\\\":\\\"item3\\\",\\\"value\\\":300}]}\"\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n  omitResult: \"{\\\"items\\\":[{\\\"name\\\":\\\"item1\\\",\\\"value\\\":100},{\\\"name\\\":\\\"item2\\\",\\\"value\\\":200},{\\\"name\\\":\\\"item3\\\",\\\"value\\\":300}]}\"\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "omit",
      "dataType": "dictSpecialKeys",
      "valuesOptions": {
        "values": [
          "testOmit=true",
          "testDataType=dictSpecialKeys"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n  omitResult: \"{\\\"key with spaces\\\":\\\"value4\\\",\\\"key-with-dash\\\":\\\"value1\\\",\\\"key.with.dots\\\":\\\"value2\\\",\\\"key_with_underscore\\\":\\\"value3\\\"}\"\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n  omitResult: \"{\\\"key with spaces\\\":\\\"value4\\\",\\\"key-with-dash\\\":\\\"value1\\\",\\\"key.with.dots\\\":\\\"value2\\\",\\\"key_with_underscore\\\":\\\"value3\\\"}\"\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n  omitResult: \"{\\\"key with spaces\\\":\\\"value4\\\",\\\"key-with-dash\\\":\\\"value1\\\",\\\"key.with.dots\\\":\\\"value2\\\",\\\"key_with_underscore\\\":\\\"value3\\\"}\"\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n  omitResult: \"{\\\"key with spaces\\\":\\\"value4\\\",\\\"key-with-dash\\\":\\\"value1\\\",\\\"key.with.dots\\\":\\\"value2\\\",\\\"key_with_underscore\\\":\\\"value3\\\"}\"\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "omit",
      "dataType": "dictNumericKeys",
      "valuesOptions": {
        "values": [
          "testOmit=true",
          "testDataType=dictNumericKeys"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n  omitResult: \"{\\\"1\\\":\\\"first\\\",\\\"100\\\":\\\"hundredth\\\",\\\"2\\\":\\\"second\\\"}\"\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n  omitResult: \"{\\\"1\\\":\\\"first\\\",\\\"100\\\":\\\"hundredth\\\",\\\"2\\\":\\\"second\\\"}\"\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n  omitResult: \"{\\\"1\\\":\\\"first\\\",\\\"100\\\":\\\"hundredth\\\",\\\"2\\\":\\\"second\\\"}\"\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n  omitResult: \"{\\\"1\\\":\\\"first\\\",\\\"100\\\":\\\"hundredth\\\",\\\"2\\\":\\\"second\\\"}\"\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "omit",
      "dataType": "dictLargeFlat",
      "valuesOptions": {
        "values": [
          "testOmit=true",
          "testDataType=dictLargeFlat"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n  omitResult: \"{\\\"key1\\\":\\\"value1\\\",\\\"key10\\\":\\\"value10\\\",\\\"key2\\\":\\\"value2\\\",\\\"key3\\\":\\\"value3\\\",\\\"key4\\\":\\\"value4\\\",\\\"key5\\\":\\\"value5\\\",\\\"key6\\\":\\\"value6\\\",\\\"key7\\\":\\\"value7\\\",\\\"key8\\\":\\\"value8\\\",\\\"key9\\\":\\\"value9\\\"}\"\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n  omitResult: \"{\\\"key1\\\":\\\"value1\\\",\\\"key10\\\":\\\"value10\\\",\\\"key2\\\":\\\"value2\\\",\\\"key3\\\":\\\"value3\\\",\\\"key4\\\":\\\"value4\\\",\\\"key5\\\":\\\"value5\\\",\\\"key6\\\":\\\"value6\\\",\\\"key7\\\":\\\"value7\\\",\\\"key8\\\":\\\"value8\\\",\\\"key9\\\":\\\"value9\\\"}\"\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n  omitResult: \"{\\\"key1\\\":\\\"value1\\\",\\\"key10\\\":\\\"value10\\\",\\\"key2\\\":\\\"value2\\\",\\\"key3\\\":\\\"value3\\\",\\\"key4\\\":\\\"value4\\\",\\\"key5\\\":\\\"value5\\\",\\\"key6\\\":\\\"value6\\\",\\\"key7\\\":\\\"value7\\\",\\\"key8\\\":\\\"value8\\\",\\\"key9\\\":\\\"value9\\\"}\"\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n  omitResult: \"{\\\"key1\\\":\\\"value1\\\",\\\"key10\\\":\\\"value10\\\",\\\"key2\\\":\\\"value2\\\",\\\"key3\\\":\\\"value3\\\",\\\"key4\\\":\\\"value4\\\",\\\"key5\\\":\\\"value5\\\",\\\"key6\\\":\\\"value6\\\",\\\"key7\\\":\\\"value7\\\",\\\"key8\\\":\\\"value8\\\",\\\"key9\\\":\\\"value9\\\"}\"\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "omit",
      "dataType": "dictForMerge",
      "valuesOptions": {
        "values": [
          "testOmit=true",
          "testDataType=dictForMerge"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n  omitResult: \"{\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"shared\\\":\\\"fromMergeDict\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n  omitResult: \"{\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"shared\\\":\\\"fromMergeDict\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n  omitResult: \"{\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"shared\\\":\\\"fromMergeDict\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n  omitResult: \"{\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"shared\\\":\\\"fromMergeDict\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "omit",
      "dataType": "dictValue",
      "valuesOptions": {
        "values": [
          "testOmit=true",
          "testDataType=dictValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n  omitResult: \"{\\\"nested\\\":{\\\"level2\\\":\\\"deepValue\\\",\\\"sharedNested\\\":\\\"fromOriginal\\\"},\\\"shared\\\":\\\"fromOriginal\\\",\\\"unique1\\\":\\\"onlyInOriginal\\\"}\"\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n  omitResult: \"{\\\"nested\\\":{\\\"level2\\\":\\\"deepValue\\\",\\\"sharedNested\\\":\\\"fromOriginal\\\"},\\\"shared\\\":\\\"fromOriginal\\\",\\\"unique1\\\":\\\"onlyInOriginal\\\"}\"\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n  omitResult: \"{\\\"nested\\\":{\\\"level2\\\":\\\"deepValue\\\",\\\"sharedNested\\\":\\\"fromOriginal\\\"},\\\"shared\\\":\\\"fromOriginal\\\",\\\"unique1\\\":\\\"onlyInOriginal\\\"}\"\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n  omitResult: \"{\\\"nested\\\":{\\\"level2\\\":\\\"deepValue\\\",\\\"sharedNested\\\":\\\"fromOriginal\\\"},\\\"shared\\\":\\\"fromOriginal\\\",\\\"unique1\\\":\\\"onlyInOriginal\\\"}\"\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "omit",
      "dataType": "listOfDicts",
      "valuesOptions": {
        "values": [
          "testOmit=true",
          "testDataType=listOfDicts"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:92:22: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003c$testData\u003e: wrong type for value; expected map[string]interface {}; got []interface {}",
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      },
      "debug": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:92:22: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003c$testData\u003e: wrong type for value; expected map[string]interface {}; got []interface {}",
        "debugLogs": [],
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      }
    },
    {
      "toggle": "omit",
      "dataType": "listEmpty",
      "valuesOptions": {
        "values": [
          "testOmit=true",
          "testDataType=listEmpty"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:92:22: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003c$testData\u003e: wrong type for value; expected map[string]interface {}; got []interface {}",
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      },
      "debug": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:92:22: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003c$testData\u003e: wrong type for value; expected map[string]interface {}; got []interface {}",
        "debugLogs": [],
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      }
    },
    {
      "toggle": "omit",
      "dataType": "listMixed",
      "valuesOptions": {
        "values": [
          "testOmit=true",
          "testDataType=listMixed"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:92:22: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003c$testData\u003e: wrong type for value; expected map[string]interface {}; got []interface {}",
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      },
      "debug": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:92:22: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003c$testData\u003e: wrong type for value; expected map[string]interface {}; got []interface {}",
        "debugLogs": [],
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      }
    },
    {
      "toggle": "values",
      "dataType": "nilValue",
      "valuesOptions": {
        "values": [
          "testValues=true",
          "testDataType=nilValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n  valuesResult: \"[]\"\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n  valuesResult: \"[]\"\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n  valuesResult: \"[]\"\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n  valuesResult: \"[]\"\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "values",
      "dataType": "stringValue",
      "valuesOptions": {
        "values": [
          "testValues=true",
          "testDataType=stringValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:97:26: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003c$testData\u003e: wrong type for value; expected map[string]interface {}; got string",
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      },
      "debug": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:97:26: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003c$testData\u003e: wrong type for value; expected map[string]interface {}; got string",
        "debugLogs": [],
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      }
    },
    {
      "toggle": "values",
      "dataType": "numberValue",
      "valuesOptions": {
        "values": [
          "testValues=true",
          "testDataType=numberValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:97:26: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003c$testData\u003e: wrong type for value; expected map[string]interface {}; got float64",
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      },
      "debug": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:97:26: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003c$testData\u003e: wrong type for value; expected map[string]interface {}; got float64",
        "debugLogs": [],
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      }
    },
    {
      "toggle": "values",
      "dataType": "boolValue",
      "valuesOptions": {
        "values": [
          "testValues=true",
          "testDataType=boolValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:97:26: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003c$testData\u003e: wrong type for value; expected map[string]interface {}; got bool",
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      },
      "debug": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:97:26: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003c$testData\u003e: wrong type for value; expected map[string]interface {}; got bool",
        "debugLogs": [],
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      }
    },
    {
      "toggle": "values",
      "dataType": "listValue",
      "valuesOptions": {
        "values": [
          "testValues=true",
          "testDataType=listValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:97:26: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003c$testData\u003e: wrong type for value; expected map[string]interface {}; got []interface {}",
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      },
      "debug": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:97:26: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003c$testData\u003e: wrong type for value; expected map[string]interface {}; got []interface {}",
        "debugLogs": [],
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      }
    },
    {
      "toggle": "values",
      "dataType": "dictSimple",
      "valuesOptions": {
        "values": [
          "testValues=true",
          "testDataType=dictSimple"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n  valuesResult: \"[\\\"value\\\",\\\"test\\\"]\"\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n  valuesResult: \"[\\\"value\\\",\\\"test\\\"]\"\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n  valuesResult: \"[\\\"value\\\",\\\"test\\\"]\"\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n  valuesResult: \"[\\\"value\\\",\\\"test\\\"]\"\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "values",
      "dataType": "dictEmpty",
      "valuesOptions": {
        "values": [
          "testValues=true",
          "testDataType=dictEmpty"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n  valuesResult: \"[]\"\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n  valuesResult: \"[]\"\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n  valuesResult: \"[]\"\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n  valuesResult: \"[]\"\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "values",
      "dataType": "dictNested",
      "valuesOptions": {
        "values": [
          "testValues=true",
          "testDataType=dictNested"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n  valuesResult: \"[{\\\"another\\\":\\\"data\\\",\\\"level2\\\":\\\"deepValue\\\"},\\\"topValue\\\"]\"\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n  valuesResult: \"[{\\\"another\\\":\\\"data\\\",\\\"level2\\\":\\\"deepValue\\\"},\\\"topValue\\\"]\"\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n  valuesResult: \"[{\\\"another\\\":\\\"data\\\",\\\"level2\\\":\\\"deepValue\\\"},\\\"topValue\\\"]\"\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n  valuesResult: \"[{\\\"another\\\":\\\"data\\\",\\\"level2\\\":\\\"deepValue\\\"},\\\"topValue\\\"]\"\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "values",
      "dataType": "dictDeepNested",
      "valuesOptions": {
        "values": [
          "testValues=true",
          "testDataType=dictDeepNested"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n  valuesResult: \"[\\\"value\\\",{\\\"b\\\":{\\\"c\\\":{\\\"d\\\":\\\"deepest\\\",\\\"value\\\":\\\"found\\\"},\\\"shallow\\\":\\\"here\\\"},\\\"mid\\\":\\\"level\\\"}]\"\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n  valuesResult: \"[\\\"value\\\",{\\\"b\\\":{\\\"c\\\":{\\\"d\\\":\\\"deepest\\\",\\\"value\\\":\\\"found\\\"},\\\"shallow\\\":\\\"here\\\"},\\\"mid\\\":\\\"level\\\"}]\"\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n  valuesResult: \"[{\\\"b\\\":{\\\"c\\\":{\\\"d\\\":\\\"deepest\\\",\\\"value\\\":\\\"found\\\"},\\\"shallow\\\":\\\"here\\\"},\\\"mid\\\":\\\"level\\\"},\\\"value\\\"]\"\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n  valuesResult: \"[{\\\"b\\\":{\\\"c\\\":{\\\"d\\\":\\\"deepest\\\",\\\"value\\\":\\\"found\\\"},\\\"shallow\\\":\\\"here\\\"},\\\"mid\\\":\\\"level\\\"},\\\"value\\\"]\"\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "values",
      "dataType": "dictMixedTypes",
      "valuesOptions": {
        "values": [
          "testValues=true",
          "testDataType=dictMixedTypes"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n  valuesResult: \"[123,\\\"hello\\\",true,[1,2,3],{\\\"inner\\\":\\\"value\\\"},null]\"\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n  valuesResult: \"[123,\\\"hello\\\",true,[1,2,3],{\\\"inner\\\":\\\"value\\\"},null]\"\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n  valuesResult: \"[true,[1,2,3],{\\\"inner\\\":\\\"value\\\"},null,123,\\\"hello\\\"]\"\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n  valuesResult: \"[true,[1,2,3],{\\\"inner\\\":\\\"value\\\"},null,123,\\\"hello\\\"]\"\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "values",
      "dataType": "dictWithListOfDicts",
      "valuesOptions": {
        "values": [
          "testValues=true",
          "testDataType=dictWithListOfDicts"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n  valuesResult: \"[[{\\\"name\\\":\\\"item1\\\",\\\"value\\\":100},{\\\"name\\\":\\\"item2\\\",\\\"value\\\":200},{\\\"name\\\":\\\"item3\\\",\\\"value\\\":300}]]\"\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n  valuesResult: \"[[{\\\"name\\\":\\\"item1\\\",\\\"value\\\":100},{\\\"name\\\":\\\"item2\\\",\\\"value\\\":200},{\\\"name\\\":\\\"item3\\\",\\\"value\\\":300}]]\"\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n  valuesResult: \"[[{\\\"name\\\":\\\"item1\\\",\\\"value\\\":100},{\\\"name\\\":\\\"item2\\\",\\\"value\\\":200},{\\\"name\\\":\\\"item3\\\",\\\"value\\\":300}]]\"\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n  valuesResult: \"[[{\\\"name\\\":\\\"item1\\\",\\\"value\\\":100},{\\\"name\\\":\\\"item2\\\",\\\"value\\\":200},{\\\"name\\\":\\\"item3\\\",\\\"value\\\":300}]]\"\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "values",
      "dataType": "dictSpecialKeys",
      "valuesOptions": {
        "values": [
          "testValues=true",
          "testDataType=dictSpecialKeys"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n  valuesResult: \"[\\\"value2\\\",\\\"value3\\\",\\\"value4\\\",\\\"value1\\\"]\"\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n  valuesResult: \"[\\\"value2\\\",\\\"value3\\\",\\\"value4\\\",\\\"value1\\\"]\"\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n  valuesResult: \"[\\\"value2\\\",\\\"value3\\\",\\\"value4\\\",\\\"value1\\\"]\"\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n  valuesResult: \"[\\\"value2\\\",\\\"value3\\\",\\\"value4\\\",\\\"value1\\\"]\"\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "values",
      "dataType": "dictNumericKeys",
      "valuesOptions": {
        "values": [
          "testValues=true",
          "testDataType=dictNumericKeys"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n  valuesResult: \"[\\\"first\\\",\\\"hundredth\\\",\\\"second\\\"]\"\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n  valuesResult: \"[\\\"first\\\",\\\"hundredth\\\",\\\"second\\\"]\"\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n  valuesResult: \"[\\\"second\\\",\\\"first\\\",\\\"hundredth\\\"]\"\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n  valuesResult: \"[\\\"second\\\",\\\"first\\\",\\\"hundredth\\\"]\"\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "values",
      "dataType": "dictLargeFlat",
      "valuesOptions": {
        "values": [
          "testValues=true",
          "testDataType=dictLargeFlat"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n  valuesResult: \"[\\\"value7\\\",\\\"value1\\\",\\\"value6\\\",\\\"value10\\\",\\\"value3\\\",\\\"value9\\\",\\\"value2\\\",\\\"value8\\\",\\\"value4\\\",\\\"value5\\\"]\"\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n  valuesResult: \"[\\\"value7\\\",\\\"value1\\\",\\\"value6\\\",\\\"value10\\\",\\\"value3\\\",\\\"value9\\\",\\\"value2\\\",\\\"value8\\\",\\\"value4\\\",\\\"value5\\\"]\"\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n  valuesResult: \"[\\\"value8\\\",\\\"value7\\\",\\\"value1\\\",\\\"value5\\\",\\\"value6\\\",\\\"value9\\\",\\\"value10\\\",\\\"value2\\\",\\\"value3\\\",\\\"value4\\\"]\"\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n  valuesResult: \"[\\\"value8\\\",\\\"value7\\\",\\\"value1\\\",\\\"value5\\\",\\\"value6\\\",\\\"value9\\\",\\\"value10\\\",\\\"value2\\\",\\\"value3\\\",\\\"value4\\\"]\"\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "values",
      "dataType": "dictForMerge",
      "valuesOptions": {
        "values": [
          "testValues=true",
          "testDataType=dictForMerge"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n  valuesResult: \"[\\\"onlyInMerge\\\",{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"fromMergeDict\\\"]\"\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n  valuesResult: \"[\\\"onlyInMerge\\\",{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"fromMergeDict\\\"]\"\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n  valuesResult: \"[{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"fromMergeDict\\\",\\\"onlyInMerge\\\"]\"\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n  valuesResult: \"[{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"fromMergeDict\\\",\\\"onlyInMerge\\\"]\"\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "values",
      "dataType": "dictValue",
      "valuesOptions": {
        "values": [
          "testValues=true",
          "testDataType=dictValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n  valuesResult: \"[\\\"value\\\",{\\\"level2\\\":\\\"deepValue\\\",\\\"sharedNested\\\":\\\"fromOriginal\\\"},\\\"fromOriginal\\\",\\\"onlyInOriginal\\\"]\"\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n  valuesResult: \"[\\\"value\\\",{\\\"level2\\\":\\\"deepValue\\\",\\\"sharedNested\\\":\\\"fromOriginal\\\"},\\\"fromOriginal\\\",\\\"onlyInOriginal\\\"]\"\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n  valuesResult: \"[\\\"fromOriginal\\\",\\\"onlyInOriginal\\\",\\\"value\\\",{\\\"level2\\\":\\\"deepValue\\\",\\\"sharedNested\\\":\\\"fromOriginal\\\"}]\"\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n  valuesResult: \"[\\\"fromOriginal\\\",\\\"onlyInOriginal\\\",\\\"value\\\",{\\\"level2\\\":\\\"deepValue\\\",\\\"sharedNested\\\":\\\"fromOriginal\\\"}]\"\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "values",
      "dataType": "listOfDicts",
      "valuesOptions": {
        "values": [
          "testValues=true",
          "testDataType=listOfDicts"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:97:26: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003c$testData\u003e: wrong type for value; expected map[string]interface {}; got []interface {}",
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      },
      "debug": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:97:26: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003c$testData\u003e: wrong type for value; expected map[string]interface {}; got []interface {}",
        "debugLogs": [],
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      }
    },
    {
      "toggle": "values",
      "dataType": "listEmpty",
      "valuesOptions": {
        "values": [
          "testValues=true",
          "testDataType=listEmpty"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:97:26: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003c$testData\u003e: wrong type for value; expected map[string]interface {}; got []interface {}",
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      },
      "debug": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:97:26: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003c$testData\u003e: wrong type for value; expected map[string]interface {}; got []interface {}",
        "debugLogs": [],
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      }
    },
    {
      "toggle": "values",
      "dataType": "listMixed",
      "valuesOptions": {
        "values": [
          "testValues=true",
          "testDataType=listMixed"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:97:26: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003c$testData\u003e: wrong type for value; expected map[string]interface {}; got []interface {}",
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      },
      "debug": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:97:26: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003c$testData\u003e: wrong type for value; expected map[string]interface {}; got []interface {}",
        "debugLogs": [],
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      }
    },
    {
      "toggle": "deepCopy",
      "dataType": "nilValue",
      "valuesOptions": {
        "values": [
          "testDeepCopy=true",
          "testDataType=nilValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:105:21: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003cdeepCopy $testData\u003e: error calling deepCopy: reflect: call of reflect.Value.Type on zero Value",
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      },
      "debug": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:105:21: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003cdeepCopy $testData\u003e: error calling deepCopy: reflect: call of reflect.Value.Type on zero Value",
        "debugLogs": [],
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      }
    },
    {
      "toggle": "deepCopy",
      "dataType": "stringValue",
      "valuesOptions": {
        "values": [
          "testDeepCopy=true",
          "testDataType=stringValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n  deepCopyResult: \"\\\"Hello World\\\"\"\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  deepCopyResult: \"\\\"Hello World\\\"\"\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n  deepCopyResult: \"\\\"Hello World\\\"\"\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  deepCopyResult: \"\\\"Hello World\\\"\"\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "deepCopy",
      "dataType": "numberValue",
      "valuesOptions": {
        "values": [
          "testDeepCopy=true",
          "testDataType=numberValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n  deepCopyResult: \"42\"\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  deepCopyResult: \"42\"\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n  deepCopyResult: \"42\"\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  deepCopyResult: \"42\"\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "deepCopy",
      "dataType": "boolValue",
      "valuesOptions": {
        "values": [
          "testDeepCopy=true",
          "testDataType=boolValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n  deepCopyResult: \"true\"\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  deepCopyResult: \"true\"\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n  deepCopyResult: \"true\"\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  deepCopyResult: \"true\"\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "deepCopy",
      "dataType": "listValue",
      "valuesOptions": {
        "values": [
          "testDeepCopy=true",
          "testDataType=listValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n  deepCopyResult: \"[1,2,3]\"\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  deepCopyResult: \"[1,2,3]\"\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n  deepCopyResult: \"[1,2,3]\"\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  deepCopyResult: \"[1,2,3]\"\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "deepCopy",
      "dataType": "dictSimple",
      "valuesOptions": {
        "values": [
          "testDeepCopy=true",
          "testDataType=dictSimple"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n  deepCopyResult: \"{\\\"key\\\":\\\"value\\\",\\\"name\\\":\\\"test\\\"}\"\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  deepCopyResult: \"{\\\"key\\\":\\\"value\\\",\\\"name\\\":\\\"test\\\"}\"\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n  deepCopyResult: \"{\\\"key\\\":\\\"value\\\",\\\"name\\\":\\\"test\\\"}\"\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  deepCopyResult: \"{\\\"key\\\":\\\"value\\\",\\\"name\\\":\\\"test\\\"}\"\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "deepCopy",
      "dataType": "dictEmpty",
      "valuesOptions": {
        "values": [
          "testDeepCopy=true",
          "testDataType=dictEmpty"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n  deepCopyResult: \"{}\"\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  deepCopyResult: \"{}\"\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n  deepCopyResult: \"{}\"\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  deepCopyResult: \"{}\"\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "deepCopy",
      "dataType": "dictNested",
      "valuesOptions": {
        "values": [
          "testDeepCopy=true",
          "testDataType=dictNested"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n  deepCopyResult: \"{\\\"level1\\\":{\\\"another\\\":\\\"data\\\",\\\"level2\\\":\\\"deepValue\\\"},\\\"topKey\\\":\\\"topValue\\\"}\"\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  deepCopyResult: \"{\\\"level1\\\":{\\\"another\\\":\\\"data\\\",\\\"level2\\\":\\\"deepValue\\\"},\\\"topKey\\\":\\\"topValue\\\"}\"\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n  deepCopyResult: \"{\\\"level1\\\":{\\\"another\\\":\\\"data\\\",\\\"level2\\\":\\\"deepValue\\\"},\\\"topKey\\\":\\\"topValue\\\"}\"\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  deepCopyResult: \"{\\\"level1\\\":{\\\"another\\\":\\\"data\\\",\\\"level2\\\":\\\"deepValue\\\"},\\\"topKey\\\":\\\"topValue\\\"}\"\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "deepCopy",
      "dataType": "dictDeepNested",
      "valuesOptions": {
        "values": [
          "testDeepCopy=true",
          "testDataType=dictDeepNested"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n  deepCopyResult: \"{\\\"a\\\":{\\\"b\\\":{\\\"c\\\":{\\\"d\\\":\\\"deepest\\\",\\\"value\\\":\\\"found\\\"},\\\"shallow\\\":\\\"here\\\"},\\\"mid\\\":\\\"level\\\"},\\\"root\\\":\\\"value\\\"}\"\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  deepCopyResult: \"{\\\"a\\\":{\\\"b\\\":{\\\"c\\\":{\\\"d\\\":\\\"deepest\\\",\\\"value\\\":\\\"found\\\"},\\\"shallow\\\":\\\"here\\\"},\\\"mid\\\":\\\"level\\\"},\\\"root\\\":\\\"value\\\"}\"\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n  deepCopyResult: \"{\\\"a\\\":{\\\"b\\\":{\\\"c\\\":{\\\"d\\\":\\\"deepest\\\",\\\"value\\\":\\\"found\\\"},\\\"shallow\\\":\\\"here\\\"},\\\"mid\\\":\\\"level\\\"},\\\"root\\\":\\\"value\\\"}\"\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  deepCopyResult: \"{\\\"a\\\":{\\\"b\\\":{\\\"c\\\":{\\\"d\\\":\\\"deepest\\\",\\\"value\\\":\\\"found\\\"},\\\"shallow\\\":\\\"here\\\"},\\\"mid\\\":\\\"level\\\"},\\\"root\\\":\\\"value\\\"}\"\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "deepCopy",
      "dataType": "dictMixedTypes",
      "valuesOptions": {
        "values": [
          "testDeepCopy=true",
          "testDataType=dictMixedTypes"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n  deepCopyResult: \"{\\\"boolKey\\\":true,\\\"listKey\\\":[1,2,3],\\\"nestedKey\\\":{\\\"inner\\\":\\\"value\\\"},\\\"nullKey\\\":null,\\\"numberKey\\\":123,\\\"stringKey\\\":\\\"hello\\\"}\"\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  deepCopyResult: \"{\\\"boolKey\\\":true,\\\"listKey\\\":[1,2,3],\\\"nestedKey\\\":{\\\"inner\\\":\\\"value\\\"},\\\"nullKey\\\":null,\\\"numberKey\\\":123,\\\"stringKey\\\":\\\"hello\\\"}\"\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n  deepCopyResult: \"{\\\"boolKey\\\":true,\\\"listKey\\\":[1,2,3],\\\"nestedKey\\\":{\\\"inner\\\":\\\"value\\\"},\\\"nullKey\\\":null,\\\"numberKey\\\":123,\\\"stringKey\\\":\\\"hello\\\"}\"\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  deepCopyResult: \"{\\\"boolKey\\\":true,\\\"listKey\\\":[1,2,3],\\\"nestedKey\\\":{\\\"inner\\\":\\\"value\\\"},\\\"nullKey\\\":null,\\\"numberKey\\\":123,\\\"stringKey\\\":\\\"hello\\\"}\"\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "deepCopy",
      "dataType": "dictWithListOfDicts",
      "valuesOptions": {
        "values": [
          "testDeepCopy=true",
          "testDataType=dictWithListOfDicts"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n  deepCopyResult: \"{\\\"items\\\":[{\\\"name\\\":\\\"item1\\\",\\\"value\\\":100},{\\\"name\\\":\\\"item2\\\",\\\"value\\\":200},{\\\"name\\\":\\\"item3\\\",\\\"value\\\":300}]}\"\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  deepCopyResult: \"{\\\"items\\\":[{\\\"name\\\":\\\"item1\\\",\\\"value\\\":100},{\\\"name\\\":\\\"item2\\\",\\\"value\\\":200},{\\\"name\\\":\\\"item3\\\",\\\"value\\\":300}]}\"\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n  deepCopyResult: \"{\\\"items\\\":[{\\\"name\\\":\\\"item1\\\",\\\"value\\\":100},{\\\"name\\\":\\\"item2\\\",\\\"value\\\":200},{\\\"name\\\":\\\"item3\\\",\\\"value\\\":300}]}\"\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  deepCopyResult: \"{\\\"items\\\":[{\\\"name\\\":\\\"item1\\\",\\\"value\\\":100},{\\\"name\\\":\\\"item2\\\",\\\"value\\\":200},{\\\"name\\\":\\\"item3\\\",\\\"value\\\":300}]}\"\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "deepCopy",
      "dataType": "dictSpecialKeys",
      "valuesOptions": {
        "values": [
          "testDeepCopy=true",
          "testDataType=dictSpecialKeys"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n  deepCopyResult: \"{\\\"key with spaces\\\":\\\"value4\\\",\\\"key-with-dash\\\":\\\"value1\\\",\\\"key.with.dots\\\":\\\"value2\\\",\\\"key_with_underscore\\\":\\\"value3\\\"}\"\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  deepCopyResult: \"{\\\"key with spaces\\\":\\\"value4\\\",\\\"key-with-dash\\\":\\\"value1\\\",\\\"key.with.dots\\\":\\\"value2\\\",\\\"key_with_underscore\\\":\\\"value3\\\"}\"\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n  deepCopyResult: \"{\\\"key with spaces\\\":\\\"value4\\\",\\\"key-with-dash\\\":\\\"value1\\\",\\\"key.with.dots\\\":\\\"value2\\\",\\\"key_with_underscore\\\":\\\"value3\\\"}\"\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  deepCopyResult: \"{\\\"key with spaces\\\":\\\"value4\\\",\\\"key-with-dash\\\":\\\"value1\\\",\\\"key.with.dots\\\":\\\"value2\\\",\\\"key_with_underscore\\\":\\\"value3\\\"}\"\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "deepCopy",
      "dataType": "dictNumericKeys",
      "valuesOptions": {
        "values": [
          "testDeepCopy=true",
          "testDataType=dictNumericKeys"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n  deepCopyResult: \"{\\\"1\\\":\\\"first\\\",\\\"100\\\":\\\"hundredth\\\",\\\"2\\\":\\\"second\\\"}\"\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  deepCopyResult: \"{\\\"1\\\":\\\"first\\\",\\\"100\\\":\\\"hundredth\\\",\\\"2\\\":\\\"second\\\"}\"\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n  deepCopyResult: \"{\\\"1\\\":\\\"first\\\",\\\"100\\\":\\\"hundredth\\\",\\\"2\\\":\\\"second\\\"}\"\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  deepCopyResult: \"{\\\"1\\\":\\\"first\\\",\\\"100\\\":\\\"hundredth\\\",\\\"2\\\":\\\"second\\\"}\"\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "deepCopy",
      "dataType": "dictLargeFlat",
      "valuesOptions": {
        "values": [
          "testDeepCopy=true",
          "testDataType=dictLargeFlat"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n  deepCopyResult: \"{\\\"key1\\\":\\\"value1\\\",\\\"key10\\\":\\\"value10\\\",\\\"key2\\\":\\\"value2\\\",\\\"key3\\\":\\\"value3\\\",\\\"key4\\\":\\\"value4\\\",\\\"key5\\\":\\\"value5\\\",\\\"key6\\\":\\\"value6\\\",\\\"key7\\\":\\\"value7\\\",\\\"key8\\\":\\\"value8\\\",\\\"key9\\\":\\\"value9\\\"}\"\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  deepCopyResult: \"{\\\"key1\\\":\\\"value1\\\",\\\"key10\\\":\\\"value10\\\",\\\"key2\\\":\\\"value2\\\",\\\"key3\\\":\\\"value3\\\",\\\"key4\\\":\\\"value4\\\",\\\"key5\\\":\\\"value5\\\",\\\"key6\\\":\\\"value6\\\",\\\"key7\\\":\\\"value7\\\",\\\"key8\\\":\\\"value8\\\",\\\"key9\\\":\\\"value9\\\"}\"\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n  deepCopyResult: \"{\\\"key1\\\":\\\"value1\\\",\\\"key10\\\":\\\"value10\\\",\\\"key2\\\":\\\"value2\\\",\\\"key3\\\":\\\"value3\\\",\\\"key4\\\":\\\"value4\\\",\\\"key5\\\":\\\"value5\\\",\\\"key6\\\":\\\"value6\\\",\\\"key7\\\":\\\"value7\\\",\\\"key8\\\":\\\"value8\\\",\\\"key9\\\":\\\"value9\\\"}\"\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  deepCopyResult: \"{\\\"key1\\\":\\\"value1\\\",\\\"key10\\\":\\\"value10\\\",\\\"key2\\\":\\\"value2\\\",\\\"key3\\\":\\\"value3\\\",\\\"key4\\\":\\\"value4\\\",\\\"key5\\\":\\\"value5\\\",\\\"key6\\\":\\\"value6\\\",\\\"key7\\\":\\\"value7\\\",\\\"key8\\\":\\\"value8\\\",\\\"key9\\\":\\\"value9\\\"}\"\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "deepCopy",
      "dataType": "dictForMerge",
      "valuesOptions": {
        "values": [
          "testDeepCopy=true",
          "testDataType=dictForMerge"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n  deepCopyResult: \"{\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"shared\\\":\\\"fromMergeDict\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  deepCopyResult: \"{\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"shared\\\":\\\"fromMergeDict\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n  deepCopyResult: \"{\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"shared\\\":\\\"fromMergeDict\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  deepCopyResult: \"{\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"shared\\\":\\\"fromMergeDict\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "deepCopy",
      "dataType": "dictValue",
      "valuesOptions": {
        "values": [
          "testDeepCopy=true",
          "testDataType=dictValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n  deepCopyResult: \"{\\\"key\\\":\\\"value\\\",\\\"nested\\\":{\\\"level2\\\":\\\"deepValue\\\",\\\"sharedNested\\\":\\\"fromOriginal\\\"},\\\"shared\\\":\\\"fromOriginal\\\",\\\"unique1\\\":\\\"onlyInOriginal\\\"}\"\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  deepCopyResult: \"{\\\"key\\\":\\\"value\\\",\\\"nested\\\":{\\\"level2\\\":\\\"deepValue\\\",\\\"sharedNested\\\":\\\"fromOriginal\\\"},\\\"shared\\\":\\\"fromOriginal\\\",\\\"unique1\\\":\\\"onlyInOriginal\\\"}\"\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n  deepCopyResult: \"{\\\"key\\\":\\\"value\\\",\\\"nested\\\":{\\\"level2\\\":\\\"deepValue\\\",\\\"sharedNested\\\":\\\"fromOriginal\\\"},\\\"shared\\\":\\\"fromOriginal\\\",\\\"unique1\\\":\\\"onlyInOriginal\\\"}\"\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  deepCopyResult: \"{\\\"key\\\":\\\"value\\\",\\\"nested\\\":{\\\"level2\\\":\\\"deepValue\\\",\\\"sharedNested\\\":\\\"fromOriginal\\\"},\\\"shared\\\":\\\"fromOriginal\\\",\\\"unique1\\\":\\\"onlyInOriginal\\\"}\"\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "deepCopy",
      "dataType": "listOfDicts",
      "valuesOptions": {
        "values": [
          "testDeepCopy=true",
          "testDataType=listOfDicts"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n  deepCopyResult: \"[{\\\"age\\\":30,\\\"city\\\":\\\"NYC\\\",\\\"name\\\":\\\"alice\\\"},{\\\"age\\\":25,\\\"city\\\":\\\"LA\\\",\\\"name\\\":\\\"bob\\\"},{\\\"age\\\":35,\\\"city\\\":\\\"Chicago\\\",\\\"name\\\":\\\"charlie\\\"}]\"\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  deepCopyResult: \"[{\\\"age\\\":30,\\\"city\\\":\\\"NYC\\\",\\\"name\\\":\\\"alice\\\"},{\\\"age\\\":25,\\\"city\\\":\\\"LA\\\",\\\"name\\\":\\\"bob\\\"},{\\\"age\\\":35,\\\"city\\\":\\\"Chicago\\\",\\\"name\\\":\\\"charlie\\\"}]\"\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n  deepCopyResult: \"[{\\\"age\\\":30,\\\"city\\\":\\\"NYC\\\",\\\"name\\\":\\\"alice\\\"},{\\\"age\\\":25,\\\"city\\\":\\\"LA\\\",\\\"name\\\":\\\"bob\\\"},{\\\"age\\\":35,\\\"city\\\":\\\"Chicago\\\",\\\"name\\\":\\\"charlie\\\"}]\"\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  deepCopyResult: \"[{\\\"age\\\":30,\\\"city\\\":\\\"NYC\\\",\\\"name\\\":\\\"alice\\\"},{\\\"age\\\":25,\\\"city\\\":\\\"LA\\\",\\\"name\\\":\\\"bob\\\"},{\\\"age\\\":35,\\\"city\\\":\\\"Chicago\\\",\\\"name\\\":\\\"charlie\\\"}]\"\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "deepCopy",
      "dataType": "listEmpty",
      "valuesOptions": {
        "values": [
          "testDeepCopy=true",
          "testDataType=listEmpty"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n  deepCopyResult: \"[]\"\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  deepCopyResult: \"[]\"\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n  deepCopyResult: \"[]\"\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  deepCopyResult: \"[]\"\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "deepCopy",
      "dataType": "listMixed",
      "valuesOptions": {
        "values": [
          "testDeepCopy=true",
          "testDataType=listMixed"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n  deepCopyResult: \"[\\\"string\\\",123,true,null,{\\\"key\\\":\\\"value\\\"}]\"\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  deepCopyResult: \"[\\\"string\\\",123,true,null,{\\\"key\\\":\\\"value\\\"}]\"\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n  deepCopyResult: \"[\\\"string\\\",123,true,null,{\\\"key\\\":\\\"value\\\"}]\"\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  deepCopyResult: \"[\\\"string\\\",123,true,null,{\\\"key\\\":\\\"value\\\"}]\"\n\n  # Test: mustDeepCopy - deep copy with error on failure\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "mustDeepCopy",
      "dataType": "nilValue",
      "valuesOptions": {
        "values": [
          "testMustDeepCopy=true",
          "testDataType=nilValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:110:25: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003cmustDeepCopy $testData\u003e: error calling mustDeepCopy: reflect: call of reflect.Value.Type on zero Value",
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      },
      "debug": {
        "error": "template: test-11-map-dict-error/templates/map-dict-error-test.yaml:110:25: executing \"test-11-map-dict-error/templates/map-dict-error-test.yaml\" at \u003cmustDeepCopy $testData\u003e: error calling mustDeepCopy: reflect: call of reflect.Value.Type on zero Value",
        "debugLogs": [],
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      }
    },
    {
      "toggle": "mustDeepCopy",
      "dataType": "stringValue",
      "valuesOptions": {
        "values": [
          "testMustDeepCopy=true",
          "testDataType=stringValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\r\n  mustDeepCopyResult: \"\\\"Hello World\\\"\"\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  mustDeepCopyResult: \"\\\"Hello World\\\"\"\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\r\n  mustDeepCopyResult: \"\\\"Hello World\\\"\"\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  mustDeepCopyResult: \"\\\"Hello World\\\"\"\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "mustDeepCopy",
      "dataType": "numberValue",
      "valuesOptions": {
        "values": [
          "testMustDeepCopy=true",
          "testDataType=numberValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\r\n  mustDeepCopyResult: \"42\"\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  mustDeepCopyResult: \"42\"\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\r\n  mustDeepCopyResult: \"42\"\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  mustDeepCopyResult: \"42\"\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "mustDeepCopy",
      "dataType": "boolValue",
      "valuesOptions": {
        "values": [
          "testMustDeepCopy=true",
          "testDataType=boolValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\r\n  mustDeepCopyResult: \"true\"\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  mustDeepCopyResult: \"true\"\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\r\n  mustDeepCopyResult: \"true\"\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  mustDeepCopyResult: \"true\"\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "mustDeepCopy",
      "dataType": "listValue",
      "valuesOptions": {
        "values": [
          "testMustDeepCopy=true",
          "testDataType=listValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\r\n  mustDeepCopyResult: \"[1,2,3]\"\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  mustDeepCopyResult: \"[1,2,3]\"\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\r\n  mustDeepCopyResult: \"[1,2,3]\"\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  mustDeepCopyResult: \"[1,2,3]\"\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "mustDeepCopy",
      "dataType": "dictSimple",
      "valuesOptions": {
        "values": [
          "testMustDeepCopy=true",
          "testDataType=dictSimple"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\r\n  mustDeepCopyResult: \"{\\\"key\\\":\\\"value\\\",\\\"name\\\":\\\"test\\\"}\"\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  mustDeepCopyResult: \"{\\\"key\\\":\\\"value\\\",\\\"name\\\":\\\"test\\\"}\"\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\r\n  mustDeepCopyResult: \"{\\\"key\\\":\\\"value\\\",\\\"name\\\":\\\"test\\\"}\"\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  mustDeepCopyResult: \"{\\\"key\\\":\\\"value\\\",\\\"name\\\":\\\"test\\\"}\"\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "mustDeepCopy",
      "dataType": "dictEmpty",
      "valuesOptions": {
        "values": [
          "testMustDeepCopy=true",
          "testDataType=dictEmpty"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\r\n  mustDeepCopyResult: \"{}\"\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  mustDeepCopyResult: \"{}\"\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\r\n  mustDeepCopyResult: \"{}\"\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  mustDeepCopyResult: \"{}\"\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "mustDeepCopy",
      "dataType": "dictNested",
      "valuesOptions": {
        "values": [
          "testMustDeepCopy=true",
          "testDataType=dictNested"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\r\n  mustDeepCopyResult: \"{\\\"level1\\\":{\\\"another\\\":\\\"data\\\",\\\"level2\\\":\\\"deepValue\\\"},\\\"topKey\\\":\\\"topValue\\\"}\"\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  mustDeepCopyResult: \"{\\\"level1\\\":{\\\"another\\\":\\\"data\\\",\\\"level2\\\":\\\"deepValue\\\"},\\\"topKey\\\":\\\"topValue\\\"}\"\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\r\n  mustDeepCopyResult: \"{\\\"level1\\\":{\\\"another\\\":\\\"data\\\",\\\"level2\\\":\\\"deepValue\\\"},\\\"topKey\\\":\\\"topValue\\\"}\"\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  mustDeepCopyResult: \"{\\\"level1\\\":{\\\"another\\\":\\\"data\\\",\\\"level2\\\":\\\"deepValue\\\"},\\\"topKey\\\":\\\"topValue\\\"}\"\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "mustDeepCopy",
      "dataType": "dictDeepNested",
      "valuesOptions": {
        "values": [
          "testMustDeepCopy=true",
          "testDataType=dictDeepNested"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\r\n  mustDeepCopyResult: \"{\\\"a\\\":{\\\"b\\\":{\\\"c\\\":{\\\"d\\\":\\\"deepest\\\",\\\"value\\\":\\\"found\\\"},\\\"shallow\\\":\\\"here\\\"},\\\"mid\\\":\\\"level\\\"},\\\"root\\\":\\\"value\\\"}\"\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  mustDeepCopyResult: \"{\\\"a\\\":{\\\"b\\\":{\\\"c\\\":{\\\"d\\\":\\\"deepest\\\",\\\"value\\\":\\\"found\\\"},\\\"shallow\\\":\\\"here\\\"},\\\"mid\\\":\\\"level\\\"},\\\"root\\\":\\\"value\\\"}\"\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\r\n  mustDeepCopyResult: \"{\\\"a\\\":{\\\"b\\\":{\\\"c\\\":{\\\"d\\\":\\\"deepest\\\",\\\"value\\\":\\\"found\\\"},\\\"shallow\\\":\\\"here\\\"},\\\"mid\\\":\\\"level\\\"},\\\"root\\\":\\\"value\\\"}\"\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  mustDeepCopyResult: \"{\\\"a\\\":{\\\"b\\\":{\\\"c\\\":{\\\"d\\\":\\\"deepest\\\",\\\"value\\\":\\\"found\\\"},\\\"shallow\\\":\\\"here\\\"},\\\"mid\\\":\\\"level\\\"},\\\"root\\\":\\\"value\\\"}\"\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "mustDeepCopy",
      "dataType": "dictMixedTypes",
      "valuesOptions": {
        "values": [
          "testMustDeepCopy=true",
          "testDataType=dictMixedTypes"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\r\n  mustDeepCopyResult: \"{\\\"boolKey\\\":true,\\\"listKey\\\":[1,2,3],\\\"nestedKey\\\":{\\\"inner\\\":\\\"value\\\"},\\\"nullKey\\\":null,\\\"numberKey\\\":123,\\\"stringKey\\\":\\\"hello\\\"}\"\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  mustDeepCopyResult: \"{\\\"boolKey\\\":true,\\\"listKey\\\":[1,2,3],\\\"nestedKey\\\":{\\\"inner\\\":\\\"value\\\"},\\\"nullKey\\\":null,\\\"numberKey\\\":123,\\\"stringKey\\\":\\\"hello\\\"}\"\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\r\n  mustDeepCopyResult: \"{\\\"boolKey\\\":true,\\\"listKey\\\":[1,2,3],\\\"nestedKey\\\":{\\\"inner\\\":\\\"value\\\"},\\\"nullKey\\\":null,\\\"numberKey\\\":123,\\\"stringKey\\\":\\\"hello\\\"}\"\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  mustDeepCopyResult: \"{\\\"boolKey\\\":true,\\\"listKey\\\":[1,2,3],\\\"nestedKey\\\":{\\\"inner\\\":\\\"value\\\"},\\\"nullKey\\\":null,\\\"numberKey\\\":123,\\\"stringKey\\\":\\\"hello\\\"}\"\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "mustDeepCopy",
      "dataType": "dictWithListOfDicts",
      "valuesOptions": {
        "values": [
          "testMustDeepCopy=true",
          "testDataType=dictWithListOfDicts"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\r\n  mustDeepCopyResult: \"{\\\"items\\\":[{\\\"name\\\":\\\"item1\\\",\\\"value\\\":100},{\\\"name\\\":\\\"item2\\\",\\\"value\\\":200},{\\\"name\\\":\\\"item3\\\",\\\"value\\\":300}]}\"\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  mustDeepCopyResult: \"{\\\"items\\\":[{\\\"name\\\":\\\"item1\\\",\\\"value\\\":100},{\\\"name\\\":\\\"item2\\\",\\\"value\\\":200},{\\\"name\\\":\\\"item3\\\",\\\"value\\\":300}]}\"\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\r\n  mustDeepCopyResult: \"{\\\"items\\\":[{\\\"name\\\":\\\"item1\\\",\\\"value\\\":100},{\\\"name\\\":\\\"item2\\\",\\\"value\\\":200},{\\\"name\\\":\\\"item3\\\",\\\"value\\\":300}]}\"\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  mustDeepCopyResult: \"{\\\"items\\\":[{\\\"name\\\":\\\"item1\\\",\\\"value\\\":100},{\\\"name\\\":\\\"item2\\\",\\\"value\\\":200},{\\\"name\\\":\\\"item3\\\",\\\"value\\\":300}]}\"\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "mustDeepCopy",
      "dataType": "dictSpecialKeys",
      "valuesOptions": {
        "values": [
          "testMustDeepCopy=true",
          "testDataType=dictSpecialKeys"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\r\n  mustDeepCopyResult: \"{\\\"key with spaces\\\":\\\"value4\\\",\\\"key-with-dash\\\":\\\"value1\\\",\\\"key.with.dots\\\":\\\"value2\\\",\\\"key_with_underscore\\\":\\\"value3\\\"}\"\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  mustDeepCopyResult: \"{\\\"key with spaces\\\":\\\"value4\\\",\\\"key-with-dash\\\":\\\"value1\\\",\\\"key.with.dots\\\":\\\"value2\\\",\\\"key_with_underscore\\\":\\\"value3\\\"}\"\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\r\n  mustDeepCopyResult: \"{\\\"key with spaces\\\":\\\"value4\\\",\\\"key-with-dash\\\":\\\"value1\\\",\\\"key.with.dots\\\":\\\"value2\\\",\\\"key_with_underscore\\\":\\\"value3\\\"}\"\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  mustDeepCopyResult: \"{\\\"key with spaces\\\":\\\"value4\\\",\\\"key-with-dash\\\":\\\"value1\\\",\\\"key.with.dots\\\":\\\"value2\\\",\\\"key_with_underscore\\\":\\\"value3\\\"}\"\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "mustDeepCopy",
      "dataType": "dictNumericKeys",
      "valuesOptions": {
        "values": [
          "testMustDeepCopy=true",
          "testDataType=dictNumericKeys"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\r\n  mustDeepCopyResult: \"{\\\"1\\\":\\\"first\\\",\\\"100\\\":\\\"hundredth\\\",\\\"2\\\":\\\"second\\\"}\"\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  mustDeepCopyResult: \"{\\\"1\\\":\\\"first\\\",\\\"100\\\":\\\"hundredth\\\",\\\"2\\\":\\\"second\\\"}\"\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\r\n  mustDeepCopyResult: \"{\\\"1\\\":\\\"first\\\",\\\"100\\\":\\\"hundredth\\\",\\\"2\\\":\\\"second\\\"}\"\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  mustDeepCopyResult: \"{\\\"1\\\":\\\"first\\\",\\\"100\\\":\\\"hundredth\\\",\\\"2\\\":\\\"second\\\"}\"\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "mustDeepCopy",
      "dataType": "dictLargeFlat",
      "valuesOptions": {
        "values": [
          "testMustDeepCopy=true",
          "testDataType=dictLargeFlat"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\r\n  mustDeepCopyResult: \"{\\\"key1\\\":\\\"value1\\\",\\\"key10\\\":\\\"value10\\\",\\\"key2\\\":\\\"value2\\\",\\\"key3\\\":\\\"value3\\\",\\\"key4\\\":\\\"value4\\\",\\\"key5\\\":\\\"value5\\\",\\\"key6\\\":\\\"value6\\\",\\\"key7\\\":\\\"value7\\\",\\\"key8\\\":\\\"value8\\\",\\\"key9\\\":\\\"value9\\\"}\"\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  mustDeepCopyResult: \"{\\\"key1\\\":\\\"value1\\\",\\\"key10\\\":\\\"value10\\\",\\\"key2\\\":\\\"value2\\\",\\\"key3\\\":\\\"value3\\\",\\\"key4\\\":\\\"value4\\\",\\\"key5\\\":\\\"value5\\\",\\\"key6\\\":\\\"value6\\\",\\\"key7\\\":\\\"value7\\\",\\\"key8\\\":\\\"value8\\\",\\\"key9\\\":\\\"value9\\\"}\"\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\r\n  mustDeepCopyResult: \"{\\\"key1\\\":\\\"value1\\\",\\\"key10\\\":\\\"value10\\\",\\\"key2\\\":\\\"value2\\\",\\\"key3\\\":\\\"value3\\\",\\\"key4\\\":\\\"value4\\\",\\\"key5\\\":\\\"value5\\\",\\\"key6\\\":\\\"value6\\\",\\\"key7\\\":\\\"value7\\\",\\\"key8\\\":\\\"value8\\\",\\\"key9\\\":\\\"value9\\\"}\"\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  mustDeepCopyResult: \"{\\\"key1\\\":\\\"value1\\\",\\\"key10\\\":\\\"value10\\\",\\\"key2\\\":\\\"value2\\\",\\\"key3\\\":\\\"value3\\\",\\\"key4\\\":\\\"value4\\\",\\\"key5\\\":\\\"value5\\\",\\\"key6\\\":\\\"value6\\\",\\\"key7\\\":\\\"value7\\\",\\\"key8\\\":\\\"value8\\\",\\\"key9\\\":\\\"value9\\\"}\"\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "mustDeepCopy",
      "dataType": "dictForMerge",
      "valuesOptions": {
        "values": [
          "testMustDeepCopy=true",
          "testDataType=dictForMerge"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\r\n  mustDeepCopyResult: \"{\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"shared\\\":\\\"fromMergeDict\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  mustDeepCopyResult: \"{\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"shared\\\":\\\"fromMergeDict\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\r\n  mustDeepCopyResult: \"{\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"shared\\\":\\\"fromMergeDict\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  mustDeepCopyResult: \"{\\\"nested\\\":{\\\"sharedNested\\\":\\\"fromMerge\\\"},\\\"shared\\\":\\\"fromMergeDict\\\",\\\"unique2\\\":\\\"onlyInMerge\\\"}\"\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "mustDeepCopy",
      "dataType": "dictValue",
      "valuesOptions": {
        "values": [
          "testMustDeepCopy=true",
          "testDataType=dictValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\r\n  mustDeepCopyResult: \"{\\\"key\\\":\\\"value\\\",\\\"nested\\\":{\\\"level2\\\":\\\"deepValue\\\",\\\"sharedNested\\\":\\\"fromOriginal\\\"},\\\"shared\\\":\\\"fromOriginal\\\",\\\"unique1\\\":\\\"onlyInOriginal\\\"}\"\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  mustDeepCopyResult: \"{\\\"key\\\":\\\"value\\\",\\\"nested\\\":{\\\"level2\\\":\\\"deepValue\\\",\\\"sharedNested\\\":\\\"fromOriginal\\\"},\\\"shared\\\":\\\"fromOriginal\\\",\\\"unique1\\\":\\\"onlyInOriginal\\\"}\"\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\r\n  mustDeepCopyResult: \"{\\\"key\\\":\\\"value\\\",\\\"nested\\\":{\\\"level2\\\":\\\"deepValue\\\",\\\"sharedNested\\\":\\\"fromOriginal\\\"},\\\"shared\\\":\\\"fromOriginal\\\",\\\"unique1\\\":\\\"onlyInOriginal\\\"}\"\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  mustDeepCopyResult: \"{\\\"key\\\":\\\"value\\\",\\\"nested\\\":{\\\"level2\\\":\\\"deepValue\\\",\\\"sharedNested\\\":\\\"fromOriginal\\\"},\\\"shared\\\":\\\"fromOriginal\\\",\\\"unique1\\\":\\\"onlyInOriginal\\\"}\"\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "mustDeepCopy",
      "dataType": "listOfDicts",
      "valuesOptions": {
        "values": [
          "testMustDeepCopy=true",
          "testDataType=listOfDicts"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\r\n  mustDeepCopyResult: \"[{\\\"age\\\":30,\\\"city\\\":\\\"NYC\\\",\\\"name\\\":\\\"alice\\\"},{\\\"age\\\":25,\\\"city\\\":\\\"LA\\\",\\\"name\\\":\\\"bob\\\"},{\\\"age\\\":35,\\\"city\\\":\\\"Chicago\\\",\\\"name\\\":\\\"charlie\\\"}]\"\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  mustDeepCopyResult: \"[{\\\"age\\\":30,\\\"city\\\":\\\"NYC\\\",\\\"name\\\":\\\"alice\\\"},{\\\"age\\\":25,\\\"city\\\":\\\"LA\\\",\\\"name\\\":\\\"bob\\\"},{\\\"age\\\":35,\\\"city\\\":\\\"Chicago\\\",\\\"name\\\":\\\"charlie\\\"}]\"\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\r\n  mustDeepCopyResult: \"[{\\\"age\\\":30,\\\"city\\\":\\\"NYC\\\",\\\"name\\\":\\\"alice\\\"},{\\\"age\\\":25,\\\"city\\\":\\\"LA\\\",\\\"name\\\":\\\"bob\\\"},{\\\"age\\\":35,\\\"city\\\":\\\"Chicago\\\",\\\"name\\\":\\\"charlie\\\"}]\"\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  mustDeepCopyResult: \"[{\\\"age\\\":30,\\\"city\\\":\\\"NYC\\\",\\\"name\\\":\\\"alice\\\"},{\\\"age\\\":25,\\\"city\\\":\\\"LA\\\",\\\"name\\\":\\\"bob\\\"},{\\\"age\\\":35,\\\"city\\\":\\\"Chicago\\\",\\\"name\\\":\\\"charlie\\\"}]\"\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "mustDeepCopy",
      "dataType": "listEmpty",
      "valuesOptions": {
        "values": [
          "testMustDeepCopy=true",
          "testDataType=listEmpty"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\r\n  mustDeepCopyResult: \"[]\"\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  mustDeepCopyResult: \"[]\"\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\r\n  mustDeepCopyResult: \"[]\"\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  mustDeepCopyResult: \"[]\"\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "mustDeepCopy",
      "dataType": "listMixed",
      "valuesOptions": {
        "values": [
          "testMustDeepCopy=true",
          "testDataType=listMixed"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\r\n  mustDeepCopyResult: \"[\\\"string\\\",123,true,null,{\\\"key\\\":\\\"value\\\"}]\"\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  mustDeepCopyResult: \"[\\\"string\\\",123,true,null,{\\\"key\\\":\\\"value\\\"}]\"\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-map-dict-test\r\ndata:\r\n  # ==========================================\r\n  # Dict creation and manipulation\r\n  # ==========================================\r\n  # Test: dict - create a dictionary\r\n\r\n  # Test: get - get a value from a dict\r\n\r\n  # Test: set - set a value in a dict\r\n\r\n  # Test: unset - remove a key from a dict\r\n\r\n  # ==========================================\r\n  # Dict query functions\r\n  # ==========================================\r\n  # Test: hasKey - check if key exists\r\n\r\n  # Test: pluck - extract values from list of dicts\r\n  # pluck expects variadic dict arguments, so we test with the data directly\r\n\r\n  # Test: dig - traverse nested dict (tests deep navigation)\r\n  # dig path... default dict - navigates nested structure\r\n\r\n  # ==========================================\r\n  # Merge functions\r\n  # ==========================================\r\n  # Test: merge - merge dicts (first dict wins)\r\n\r\n  # Test: mustMerge - merge dicts with error on failure\r\n\r\n  # Test: mergeOverwrite - merge dicts (last dict wins)\r\n\r\n  # Test: mustMergeOverwrite - merge dicts with error on failure\r\n\r\n  # ==========================================\r\n  # Dict extraction functions\r\n  # ==========================================\r\n  # Test: keys - get all keys from dict\r\n\r\n  # Test: pick - create dict with only specified keys\r\n\r\n  # Test: omit - create dict without specified keys\r\n\r\n  # Test: values - get all values from dict\r\n\r\n  # ==========================================\r\n  # Copy functions\r\n  # ==========================================\r\n  # Test: deepCopy - deep copy a dict\r\n\r\n  # Test: mustDeepCopy - deep copy with error on failure\r\n  mustDeepCopyResult: \"[\\\"string\\\",123,true,null,{\\\"key\\\":\\\"value\\\"}]\"\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-11-map-dict-error/templates/map-dict-error-test.yaml",
            "content": "\n# Source: test-11-map-dict-error/templates/map-dict-error-test.yaml\n# Test: Map/dict operation errors - comprehensive testing of all dict functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n\n  # Test: get - get a value from a dict\n\n  # Test: set - set a value in a dict\n\n  # Test: unset - remove a key from a dict\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n\n  # Test: mustMerge - merge dicts with error on failure\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n\n  # Test: pick - create dict with only specified keys\n\n  # Test: omit - create dict without specified keys\n\n  # Test: values - get all values from dict\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  mustDeepCopyResult: \"[\\\"string\\\",123,true,null,{\\\"key\\\":\\\"value\\\"}]\"\n",
            "originalContent": "# Test: Map/dict operation errors - comprehensive testing of all dict functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-map-dict-test\ndata:\n  # ==========================================\n  # Dict creation and manipulation\n  # ==========================================\n  # Test: dict - create a dictionary\n  {{- if .Values.testDict }}\n  dictResult: {{ dict \"key1\" $testData \"key2\" \"value2\" | toJson | quote }}\n  {{- end }}\n\n  # Test: get - get a value from a dict\n  {{- if .Values.testGet }}\n  getResult: {{ get $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: set - set a value in a dict\n  {{- if .Values.testSet }}\n  {{- $_ := set $testData \"newKey\" \"newValue\" }}\n  setResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: unset - remove a key from a dict\n  {{- if .Values.testUnset }}\n  {{- $_ := unset $testData \"key\" }}\n  unsetResult: {{ $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict query functions\n  # ==========================================\n  # Test: hasKey - check if key exists\n  {{- if .Values.testHasKey }}\n  hasKeyResult: {{ hasKey $testData \"key\" | quote }}\n  {{- end }}\n\n  # Test: pluck - extract values from list of dicts\n  # pluck expects variadic dict arguments, so we test with the data directly\n  {{- if .Values.testPluck }}\n  pluckResult: {{ pluck \"name\" $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: dig - traverse nested dict (tests deep navigation)\n  # dig path... default dict - navigates nested structure\n  {{- if .Values.testDig }}\n  digResult: {{ dig \"a\" \"b\" \"c\" \"d\" \"notfound\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Merge functions\n  # ==========================================\n  # Test: merge - merge dicts (first dict wins)\n  {{- if .Values.testMerge }}\n  mergeResult: {{ merge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMerge - merge dicts with error on failure\n  {{- if .Values.testMustMerge }}\n  mustMergeResult: {{ mustMerge $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mergeOverwrite - merge dicts (last dict wins)\n  {{- if .Values.testMergeOverwrite }}\n  mergeOverwriteResult: {{ mergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # Test: mustMergeOverwrite - merge dicts with error on failure\n  {{- if .Values.testMustMergeOverwrite }}\n  mustMergeOverwriteResult: {{ mustMergeOverwrite $testData .Values.dictForMerge | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Dict extraction functions\n  # ==========================================\n  # Test: keys - get all keys from dict\n  {{- if .Values.testKeys }}\n  keysResult: {{ keys $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: pick - create dict with only specified keys\n  {{- if .Values.testPick }}\n  pickResult: {{ pick $testData \"key\" \"name\" \"a\" \"stringKey\" | toJson | quote }}\n  {{- end }}\n\n  # Test: omit - create dict without specified keys\n  {{- if .Values.testOmit }}\n  omitResult: {{ omit $testData \"key\" \"name\" | toJson | quote }}\n  {{- end }}\n\n  # Test: values - get all values from dict\n  {{- if .Values.testValues }}\n  valuesResult: {{ values $testData | toJson | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Copy functions\n  # ==========================================\n  # Test: deepCopy - deep copy a dict\n  {{- if .Values.testDeepCopy }}\n  deepCopyResult: {{ deepCopy $testData | toJson | quote }}\n  {{- end }}\n\n  # Test: mustDeepCopy - deep copy with error on failure\n  {{- if .Values.testMustDeepCopy }}\n  mustDeepCopyResult: {{ mustDeepCopy $testData | toJson | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    }
  ]
}
{
  "suite": "render_problems",
  "testNumber": 1,
  "testName": "Logic and Default Functions",
  "chartPath": "..\\render_problems\\test-01-logic-default-functions",
  "renders": [
    {
      "toggle": "and",
      "dataType": "nilValue",
      "valuesOptions": {
        "values": [
          "testAnd=true",
          "testDataType=nilValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-logic-test\r\ndata:\r\n  # ==========================================\r\n  # Boolean Logic Functions\r\n  # ==========================================\r\n  # Test: and - returns boolean AND of arguments\r\n  andResult: \r\n\r\n  # Test: or - returns boolean OR of arguments\r\n\r\n  # Test: not - returns boolean negation\r\n\r\n  # ==========================================\r\n  # Comparison Functions\r\n  # ==========================================\r\n  # Test: eq - returns true if equal\r\n\r\n  # Test: ne - returns true if not equal\r\n\r\n  # Test: lt - returns true if less than\r\n\r\n  # Test: le - returns true if less than or equal\r\n\r\n  # Test: gt - returns true if greater than\r\n\r\n  # Test: ge - returns true if greater than or equal\r\n\r\n  # ==========================================\r\n  # Default/Required Functions\r\n  # ==========================================\r\n  # Test: default - returns default value if empty\r\n\r\n  # Test: required - fails if value is empty\r\n\r\n  # Test: empty - returns true if value is empty\r\n\r\n  # Test: fail - explicitly fails with message\r\n\r\n  # ==========================================\r\n  # Coalesce/Ternary Functions\r\n  # ==========================================\r\n  # Test: coalesce - returns first non-empty value\r\n\r\n  # Test: ternary - conditional expression\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-logic-default/templates/logic-default-test.yaml",
            "content": "\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n  andResult: \n\n  # Test: or - returns boolean OR of arguments\n\n  # Test: not - returns boolean negation\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n\n  # Test: ne - returns true if not equal\n\n  # Test: lt - returns true if less than\n\n  # Test: le - returns true if less than or equal\n\n  # Test: gt - returns true if greater than\n\n  # Test: ge - returns true if greater than or equal\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n\n  # Test: required - fails if value is empty\n\n  # Test: empty - returns true if value is empty\n\n  # Test: fail - explicitly fails with message\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n\n  # Test: ternary - conditional expression\n",
            "originalContent": "# Test: Logic and default functions - comprehensive testing\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n  {{- if .Values.testAnd }}\n  andResult: {{ and $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: or - returns boolean OR of arguments\n  {{- if .Values.testOr }}\n  orResult: {{ or $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: not - returns boolean negation\n  {{- if .Values.testNot }}\n  notResult: {{ not $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n  {{- if .Values.testEq }}\n  eqResult: {{ eq $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ne - returns true if not equal\n  {{- if .Values.testNe }}\n  neResult: {{ ne $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: lt - returns true if less than\n  {{- if .Values.testLt }}\n  ltResult: {{ lt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: le - returns true if less than or equal\n  {{- if .Values.testLe }}\n  leResult: {{ le $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: gt - returns true if greater than\n  {{- if .Values.testGt }}\n  gtResult: {{ gt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ge - returns true if greater than or equal\n  {{- if .Values.testGe }}\n  geResult: {{ ge $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n  {{- if .Values.testDefault }}\n  defaultResult: {{ default \"fallback\" $testData | quote }}\n  {{- end }}\n\n  # Test: required - fails if value is empty\n  {{- if .Values.testRequired }}\n  requiredResult: {{ required \"value is required\" $testData | quote }}\n  {{- end }}\n\n  # Test: empty - returns true if value is empty\n  {{- if .Values.testEmpty }}\n  emptyResult: {{ empty $testData | quote }}\n  {{- end }}\n\n  # Test: fail - explicitly fails with message\n  {{- if .Values.testFail }}\n  failResult: {{ fail (printf \"intentional fail with: %v\" $testData) }}\n  {{- end }}\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n  {{- if .Values.testCoalesce }}\n  coalesceResult: {{ coalesce $testData .Values.compareValue \"default\" | quote }}\n  {{- end }}\n\n  # Test: ternary - conditional expression\n  {{- if .Values.testTernary }}\n  ternaryResult: {{ ternary \"yes\" \"no\" $testData | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-logic-test\r\ndata:\r\n  # ==========================================\r\n  # Boolean Logic Functions\r\n  # ==========================================\r\n  # Test: and - returns boolean AND of arguments\r\n  andResult: \r\n\r\n  # Test: or - returns boolean OR of arguments\r\n\r\n  # Test: not - returns boolean negation\r\n\r\n  # ==========================================\r\n  # Comparison Functions\r\n  # ==========================================\r\n  # Test: eq - returns true if equal\r\n\r\n  # Test: ne - returns true if not equal\r\n\r\n  # Test: lt - returns true if less than\r\n\r\n  # Test: le - returns true if less than or equal\r\n\r\n  # Test: gt - returns true if greater than\r\n\r\n  # Test: ge - returns true if greater than or equal\r\n\r\n  # ==========================================\r\n  # Default/Required Functions\r\n  # ==========================================\r\n  # Test: default - returns default value if empty\r\n\r\n  # Test: required - fails if value is empty\r\n\r\n  # Test: empty - returns true if value is empty\r\n\r\n  # Test: fail - explicitly fails with message\r\n\r\n  # ==========================================\r\n  # Coalesce/Ternary Functions\r\n  # ==========================================\r\n  # Test: coalesce - returns first non-empty value\r\n\r\n  # Test: ternary - conditional expression\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-logic-default/templates/logic-default-test.yaml",
            "content": "\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n  andResult: \n\n  # Test: or - returns boolean OR of arguments\n\n  # Test: not - returns boolean negation\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n\n  # Test: ne - returns true if not equal\n\n  # Test: lt - returns true if less than\n\n  # Test: le - returns true if less than or equal\n\n  # Test: gt - returns true if greater than\n\n  # Test: ge - returns true if greater than or equal\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n\n  # Test: required - fails if value is empty\n\n  # Test: empty - returns true if value is empty\n\n  # Test: fail - explicitly fails with message\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n\n  # Test: ternary - conditional expression\n",
            "originalContent": "# Test: Logic and default functions - comprehensive testing\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n  {{- if .Values.testAnd }}\n  andResult: {{ and $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: or - returns boolean OR of arguments\n  {{- if .Values.testOr }}\n  orResult: {{ or $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: not - returns boolean negation\n  {{- if .Values.testNot }}\n  notResult: {{ not $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n  {{- if .Values.testEq }}\n  eqResult: {{ eq $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ne - returns true if not equal\n  {{- if .Values.testNe }}\n  neResult: {{ ne $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: lt - returns true if less than\n  {{- if .Values.testLt }}\n  ltResult: {{ lt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: le - returns true if less than or equal\n  {{- if .Values.testLe }}\n  leResult: {{ le $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: gt - returns true if greater than\n  {{- if .Values.testGt }}\n  gtResult: {{ gt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ge - returns true if greater than or equal\n  {{- if .Values.testGe }}\n  geResult: {{ ge $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n  {{- if .Values.testDefault }}\n  defaultResult: {{ default \"fallback\" $testData | quote }}\n  {{- end }}\n\n  # Test: required - fails if value is empty\n  {{- if .Values.testRequired }}\n  requiredResult: {{ required \"value is required\" $testData | quote }}\n  {{- end }}\n\n  # Test: empty - returns true if value is empty\n  {{- if .Values.testEmpty }}\n  emptyResult: {{ empty $testData | quote }}\n  {{- end }}\n\n  # Test: fail - explicitly fails with message\n  {{- if .Values.testFail }}\n  failResult: {{ fail (printf \"intentional fail with: %v\" $testData) }}\n  {{- end }}\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n  {{- if .Values.testCoalesce }}\n  coalesceResult: {{ coalesce $testData .Values.compareValue \"default\" | quote }}\n  {{- end }}\n\n  # Test: ternary - conditional expression\n  {{- if .Values.testTernary }}\n  ternaryResult: {{ ternary \"yes\" \"no\" $testData | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "and",
      "dataType": "stringValue",
      "valuesOptions": {
        "values": [
          "testAnd=true",
          "testDataType=stringValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-logic-test\r\ndata:\r\n  # ==========================================\r\n  # Boolean Logic Functions\r\n  # ==========================================\r\n  # Test: and - returns boolean AND of arguments\r\n  andResult: \"10\"\r\n\r\n  # Test: or - returns boolean OR of arguments\r\n\r\n  # Test: not - returns boolean negation\r\n\r\n  # ==========================================\r\n  # Comparison Functions\r\n  # ==========================================\r\n  # Test: eq - returns true if equal\r\n\r\n  # Test: ne - returns true if not equal\r\n\r\n  # Test: lt - returns true if less than\r\n\r\n  # Test: le - returns true if less than or equal\r\n\r\n  # Test: gt - returns true if greater than\r\n\r\n  # Test: ge - returns true if greater than or equal\r\n\r\n  # ==========================================\r\n  # Default/Required Functions\r\n  # ==========================================\r\n  # Test: default - returns default value if empty\r\n\r\n  # Test: required - fails if value is empty\r\n\r\n  # Test: empty - returns true if value is empty\r\n\r\n  # Test: fail - explicitly fails with message\r\n\r\n  # ==========================================\r\n  # Coalesce/Ternary Functions\r\n  # ==========================================\r\n  # Test: coalesce - returns first non-empty value\r\n\r\n  # Test: ternary - conditional expression\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-logic-default/templates/logic-default-test.yaml",
            "content": "\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n  andResult: \"10\"\n\n  # Test: or - returns boolean OR of arguments\n\n  # Test: not - returns boolean negation\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n\n  # Test: ne - returns true if not equal\n\n  # Test: lt - returns true if less than\n\n  # Test: le - returns true if less than or equal\n\n  # Test: gt - returns true if greater than\n\n  # Test: ge - returns true if greater than or equal\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n\n  # Test: required - fails if value is empty\n\n  # Test: empty - returns true if value is empty\n\n  # Test: fail - explicitly fails with message\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n\n  # Test: ternary - conditional expression\n",
            "originalContent": "# Test: Logic and default functions - comprehensive testing\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n  {{- if .Values.testAnd }}\n  andResult: {{ and $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: or - returns boolean OR of arguments\n  {{- if .Values.testOr }}\n  orResult: {{ or $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: not - returns boolean negation\n  {{- if .Values.testNot }}\n  notResult: {{ not $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n  {{- if .Values.testEq }}\n  eqResult: {{ eq $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ne - returns true if not equal\n  {{- if .Values.testNe }}\n  neResult: {{ ne $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: lt - returns true if less than\n  {{- if .Values.testLt }}\n  ltResult: {{ lt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: le - returns true if less than or equal\n  {{- if .Values.testLe }}\n  leResult: {{ le $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: gt - returns true if greater than\n  {{- if .Values.testGt }}\n  gtResult: {{ gt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ge - returns true if greater than or equal\n  {{- if .Values.testGe }}\n  geResult: {{ ge $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n  {{- if .Values.testDefault }}\n  defaultResult: {{ default \"fallback\" $testData | quote }}\n  {{- end }}\n\n  # Test: required - fails if value is empty\n  {{- if .Values.testRequired }}\n  requiredResult: {{ required \"value is required\" $testData | quote }}\n  {{- end }}\n\n  # Test: empty - returns true if value is empty\n  {{- if .Values.testEmpty }}\n  emptyResult: {{ empty $testData | quote }}\n  {{- end }}\n\n  # Test: fail - explicitly fails with message\n  {{- if .Values.testFail }}\n  failResult: {{ fail (printf \"intentional fail with: %v\" $testData) }}\n  {{- end }}\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n  {{- if .Values.testCoalesce }}\n  coalesceResult: {{ coalesce $testData .Values.compareValue \"default\" | quote }}\n  {{- end }}\n\n  # Test: ternary - conditional expression\n  {{- if .Values.testTernary }}\n  ternaryResult: {{ ternary \"yes\" \"no\" $testData | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-logic-test\r\ndata:\r\n  # ==========================================\r\n  # Boolean Logic Functions\r\n  # ==========================================\r\n  # Test: and - returns boolean AND of arguments\r\n  andResult: \"10\"\r\n\r\n  # Test: or - returns boolean OR of arguments\r\n\r\n  # Test: not - returns boolean negation\r\n\r\n  # ==========================================\r\n  # Comparison Functions\r\n  # ==========================================\r\n  # Test: eq - returns true if equal\r\n\r\n  # Test: ne - returns true if not equal\r\n\r\n  # Test: lt - returns true if less than\r\n\r\n  # Test: le - returns true if less than or equal\r\n\r\n  # Test: gt - returns true if greater than\r\n\r\n  # Test: ge - returns true if greater than or equal\r\n\r\n  # ==========================================\r\n  # Default/Required Functions\r\n  # ==========================================\r\n  # Test: default - returns default value if empty\r\n\r\n  # Test: required - fails if value is empty\r\n\r\n  # Test: empty - returns true if value is empty\r\n\r\n  # Test: fail - explicitly fails with message\r\n\r\n  # ==========================================\r\n  # Coalesce/Ternary Functions\r\n  # ==========================================\r\n  # Test: coalesce - returns first non-empty value\r\n\r\n  # Test: ternary - conditional expression\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-logic-default/templates/logic-default-test.yaml",
            "content": "\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n  andResult: \"10\"\n\n  # Test: or - returns boolean OR of arguments\n\n  # Test: not - returns boolean negation\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n\n  # Test: ne - returns true if not equal\n\n  # Test: lt - returns true if less than\n\n  # Test: le - returns true if less than or equal\n\n  # Test: gt - returns true if greater than\n\n  # Test: ge - returns true if greater than or equal\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n\n  # Test: required - fails if value is empty\n\n  # Test: empty - returns true if value is empty\n\n  # Test: fail - explicitly fails with message\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n\n  # Test: ternary - conditional expression\n",
            "originalContent": "# Test: Logic and default functions - comprehensive testing\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n  {{- if .Values.testAnd }}\n  andResult: {{ and $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: or - returns boolean OR of arguments\n  {{- if .Values.testOr }}\n  orResult: {{ or $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: not - returns boolean negation\n  {{- if .Values.testNot }}\n  notResult: {{ not $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n  {{- if .Values.testEq }}\n  eqResult: {{ eq $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ne - returns true if not equal\n  {{- if .Values.testNe }}\n  neResult: {{ ne $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: lt - returns true if less than\n  {{- if .Values.testLt }}\n  ltResult: {{ lt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: le - returns true if less than or equal\n  {{- if .Values.testLe }}\n  leResult: {{ le $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: gt - returns true if greater than\n  {{- if .Values.testGt }}\n  gtResult: {{ gt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ge - returns true if greater than or equal\n  {{- if .Values.testGe }}\n  geResult: {{ ge $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n  {{- if .Values.testDefault }}\n  defaultResult: {{ default \"fallback\" $testData | quote }}\n  {{- end }}\n\n  # Test: required - fails if value is empty\n  {{- if .Values.testRequired }}\n  requiredResult: {{ required \"value is required\" $testData | quote }}\n  {{- end }}\n\n  # Test: empty - returns true if value is empty\n  {{- if .Values.testEmpty }}\n  emptyResult: {{ empty $testData | quote }}\n  {{- end }}\n\n  # Test: fail - explicitly fails with message\n  {{- if .Values.testFail }}\n  failResult: {{ fail (printf \"intentional fail with: %v\" $testData) }}\n  {{- end }}\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n  {{- if .Values.testCoalesce }}\n  coalesceResult: {{ coalesce $testData .Values.compareValue \"default\" | quote }}\n  {{- end }}\n\n  # Test: ternary - conditional expression\n  {{- if .Values.testTernary }}\n  ternaryResult: {{ ternary \"yes\" \"no\" $testData | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "and",
      "dataType": "numberValue",
      "valuesOptions": {
        "values": [
          "testAnd=true",
          "testDataType=numberValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-logic-test\r\ndata:\r\n  # ==========================================\r\n  # Boolean Logic Functions\r\n  # ==========================================\r\n  # Test: and - returns boolean AND of arguments\r\n  andResult: \"10\"\r\n\r\n  # Test: or - returns boolean OR of arguments\r\n\r\n  # Test: not - returns boolean negation\r\n\r\n  # ==========================================\r\n  # Comparison Functions\r\n  # ==========================================\r\n  # Test: eq - returns true if equal\r\n\r\n  # Test: ne - returns true if not equal\r\n\r\n  # Test: lt - returns true if less than\r\n\r\n  # Test: le - returns true if less than or equal\r\n\r\n  # Test: gt - returns true if greater than\r\n\r\n  # Test: ge - returns true if greater than or equal\r\n\r\n  # ==========================================\r\n  # Default/Required Functions\r\n  # ==========================================\r\n  # Test: default - returns default value if empty\r\n\r\n  # Test: required - fails if value is empty\r\n\r\n  # Test: empty - returns true if value is empty\r\n\r\n  # Test: fail - explicitly fails with message\r\n\r\n  # ==========================================\r\n  # Coalesce/Ternary Functions\r\n  # ==========================================\r\n  # Test: coalesce - returns first non-empty value\r\n\r\n  # Test: ternary - conditional expression\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-logic-default/templates/logic-default-test.yaml",
            "content": "\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n  andResult: \"10\"\n\n  # Test: or - returns boolean OR of arguments\n\n  # Test: not - returns boolean negation\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n\n  # Test: ne - returns true if not equal\n\n  # Test: lt - returns true if less than\n\n  # Test: le - returns true if less than or equal\n\n  # Test: gt - returns true if greater than\n\n  # Test: ge - returns true if greater than or equal\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n\n  # Test: required - fails if value is empty\n\n  # Test: empty - returns true if value is empty\n\n  # Test: fail - explicitly fails with message\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n\n  # Test: ternary - conditional expression\n",
            "originalContent": "# Test: Logic and default functions - comprehensive testing\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n  {{- if .Values.testAnd }}\n  andResult: {{ and $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: or - returns boolean OR of arguments\n  {{- if .Values.testOr }}\n  orResult: {{ or $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: not - returns boolean negation\n  {{- if .Values.testNot }}\n  notResult: {{ not $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n  {{- if .Values.testEq }}\n  eqResult: {{ eq $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ne - returns true if not equal\n  {{- if .Values.testNe }}\n  neResult: {{ ne $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: lt - returns true if less than\n  {{- if .Values.testLt }}\n  ltResult: {{ lt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: le - returns true if less than or equal\n  {{- if .Values.testLe }}\n  leResult: {{ le $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: gt - returns true if greater than\n  {{- if .Values.testGt }}\n  gtResult: {{ gt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ge - returns true if greater than or equal\n  {{- if .Values.testGe }}\n  geResult: {{ ge $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n  {{- if .Values.testDefault }}\n  defaultResult: {{ default \"fallback\" $testData | quote }}\n  {{- end }}\n\n  # Test: required - fails if value is empty\n  {{- if .Values.testRequired }}\n  requiredResult: {{ required \"value is required\" $testData | quote }}\n  {{- end }}\n\n  # Test: empty - returns true if value is empty\n  {{- if .Values.testEmpty }}\n  emptyResult: {{ empty $testData | quote }}\n  {{- end }}\n\n  # Test: fail - explicitly fails with message\n  {{- if .Values.testFail }}\n  failResult: {{ fail (printf \"intentional fail with: %v\" $testData) }}\n  {{- end }}\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n  {{- if .Values.testCoalesce }}\n  coalesceResult: {{ coalesce $testData .Values.compareValue \"default\" | quote }}\n  {{- end }}\n\n  # Test: ternary - conditional expression\n  {{- if .Values.testTernary }}\n  ternaryResult: {{ ternary \"yes\" \"no\" $testData | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-logic-test\r\ndata:\r\n  # ==========================================\r\n  # Boolean Logic Functions\r\n  # ==========================================\r\n  # Test: and - returns boolean AND of arguments\r\n  andResult: \"10\"\r\n\r\n  # Test: or - returns boolean OR of arguments\r\n\r\n  # Test: not - returns boolean negation\r\n\r\n  # ==========================================\r\n  # Comparison Functions\r\n  # ==========================================\r\n  # Test: eq - returns true if equal\r\n\r\n  # Test: ne - returns true if not equal\r\n\r\n  # Test: lt - returns true if less than\r\n\r\n  # Test: le - returns true if less than or equal\r\n\r\n  # Test: gt - returns true if greater than\r\n\r\n  # Test: ge - returns true if greater than or equal\r\n\r\n  # ==========================================\r\n  # Default/Required Functions\r\n  # ==========================================\r\n  # Test: default - returns default value if empty\r\n\r\n  # Test: required - fails if value is empty\r\n\r\n  # Test: empty - returns true if value is empty\r\n\r\n  # Test: fail - explicitly fails with message\r\n\r\n  # ==========================================\r\n  # Coalesce/Ternary Functions\r\n  # ==========================================\r\n  # Test: coalesce - returns first non-empty value\r\n\r\n  # Test: ternary - conditional expression\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-logic-default/templates/logic-default-test.yaml",
            "content": "\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n  andResult: \"10\"\n\n  # Test: or - returns boolean OR of arguments\n\n  # Test: not - returns boolean negation\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n\n  # Test: ne - returns true if not equal\n\n  # Test: lt - returns true if less than\n\n  # Test: le - returns true if less than or equal\n\n  # Test: gt - returns true if greater than\n\n  # Test: ge - returns true if greater than or equal\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n\n  # Test: required - fails if value is empty\n\n  # Test: empty - returns true if value is empty\n\n  # Test: fail - explicitly fails with message\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n\n  # Test: ternary - conditional expression\n",
            "originalContent": "# Test: Logic and default functions - comprehensive testing\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n  {{- if .Values.testAnd }}\n  andResult: {{ and $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: or - returns boolean OR of arguments\n  {{- if .Values.testOr }}\n  orResult: {{ or $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: not - returns boolean negation\n  {{- if .Values.testNot }}\n  notResult: {{ not $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n  {{- if .Values.testEq }}\n  eqResult: {{ eq $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ne - returns true if not equal\n  {{- if .Values.testNe }}\n  neResult: {{ ne $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: lt - returns true if less than\n  {{- if .Values.testLt }}\n  ltResult: {{ lt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: le - returns true if less than or equal\n  {{- if .Values.testLe }}\n  leResult: {{ le $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: gt - returns true if greater than\n  {{- if .Values.testGt }}\n  gtResult: {{ gt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ge - returns true if greater than or equal\n  {{- if .Values.testGe }}\n  geResult: {{ ge $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n  {{- if .Values.testDefault }}\n  defaultResult: {{ default \"fallback\" $testData | quote }}\n  {{- end }}\n\n  # Test: required - fails if value is empty\n  {{- if .Values.testRequired }}\n  requiredResult: {{ required \"value is required\" $testData | quote }}\n  {{- end }}\n\n  # Test: empty - returns true if value is empty\n  {{- if .Values.testEmpty }}\n  emptyResult: {{ empty $testData | quote }}\n  {{- end }}\n\n  # Test: fail - explicitly fails with message\n  {{- if .Values.testFail }}\n  failResult: {{ fail (printf \"intentional fail with: %v\" $testData) }}\n  {{- end }}\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n  {{- if .Values.testCoalesce }}\n  coalesceResult: {{ coalesce $testData .Values.compareValue \"default\" | quote }}\n  {{- end }}\n\n  # Test: ternary - conditional expression\n  {{- if .Values.testTernary }}\n  ternaryResult: {{ ternary \"yes\" \"no\" $testData | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "and",
      "dataType": "boolValue",
      "valuesOptions": {
        "values": [
          "testAnd=true",
          "testDataType=boolValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-logic-test\r\ndata:\r\n  # ==========================================\r\n  # Boolean Logic Functions\r\n  # ==========================================\r\n  # Test: and - returns boolean AND of arguments\r\n  andResult: \"10\"\r\n\r\n  # Test: or - returns boolean OR of arguments\r\n\r\n  # Test: not - returns boolean negation\r\n\r\n  # ==========================================\r\n  # Comparison Functions\r\n  # ==========================================\r\n  # Test: eq - returns true if equal\r\n\r\n  # Test: ne - returns true if not equal\r\n\r\n  # Test: lt - returns true if less than\r\n\r\n  # Test: le - returns true if less than or equal\r\n\r\n  # Test: gt - returns true if greater than\r\n\r\n  # Test: ge - returns true if greater than or equal\r\n\r\n  # ==========================================\r\n  # Default/Required Functions\r\n  # ==========================================\r\n  # Test: default - returns default value if empty\r\n\r\n  # Test: required - fails if value is empty\r\n\r\n  # Test: empty - returns true if value is empty\r\n\r\n  # Test: fail - explicitly fails with message\r\n\r\n  # ==========================================\r\n  # Coalesce/Ternary Functions\r\n  # ==========================================\r\n  # Test: coalesce - returns first non-empty value\r\n\r\n  # Test: ternary - conditional expression\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-logic-default/templates/logic-default-test.yaml",
            "content": "\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n  andResult: \"10\"\n\n  # Test: or - returns boolean OR of arguments\n\n  # Test: not - returns boolean negation\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n\n  # Test: ne - returns true if not equal\n\n  # Test: lt - returns true if less than\n\n  # Test: le - returns true if less than or equal\n\n  # Test: gt - returns true if greater than\n\n  # Test: ge - returns true if greater than or equal\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n\n  # Test: required - fails if value is empty\n\n  # Test: empty - returns true if value is empty\n\n  # Test: fail - explicitly fails with message\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n\n  # Test: ternary - conditional expression\n",
            "originalContent": "# Test: Logic and default functions - comprehensive testing\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n  {{- if .Values.testAnd }}\n  andResult: {{ and $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: or - returns boolean OR of arguments\n  {{- if .Values.testOr }}\n  orResult: {{ or $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: not - returns boolean negation\n  {{- if .Values.testNot }}\n  notResult: {{ not $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n  {{- if .Values.testEq }}\n  eqResult: {{ eq $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ne - returns true if not equal\n  {{- if .Values.testNe }}\n  neResult: {{ ne $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: lt - returns true if less than\n  {{- if .Values.testLt }}\n  ltResult: {{ lt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: le - returns true if less than or equal\n  {{- if .Values.testLe }}\n  leResult: {{ le $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: gt - returns true if greater than\n  {{- if .Values.testGt }}\n  gtResult: {{ gt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ge - returns true if greater than or equal\n  {{- if .Values.testGe }}\n  geResult: {{ ge $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n  {{- if .Values.testDefault }}\n  defaultResult: {{ default \"fallback\" $testData | quote }}\n  {{- end }}\n\n  # Test: required - fails if value is empty\n  {{- if .Values.testRequired }}\n  requiredResult: {{ required \"value is required\" $testData | quote }}\n  {{- end }}\n\n  # Test: empty - returns true if value is empty\n  {{- if .Values.testEmpty }}\n  emptyResult: {{ empty $testData | quote }}\n  {{- end }}\n\n  # Test: fail - explicitly fails with message\n  {{- if .Values.testFail }}\n  failResult: {{ fail (printf \"intentional fail with: %v\" $testData) }}\n  {{- end }}\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n  {{- if .Values.testCoalesce }}\n  coalesceResult: {{ coalesce $testData .Values.compareValue \"default\" | quote }}\n  {{- end }}\n\n  # Test: ternary - conditional expression\n  {{- if .Values.testTernary }}\n  ternaryResult: {{ ternary \"yes\" \"no\" $testData | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-logic-test\r\ndata:\r\n  # ==========================================\r\n  # Boolean Logic Functions\r\n  # ==========================================\r\n  # Test: and - returns boolean AND of arguments\r\n  andResult: \"10\"\r\n\r\n  # Test: or - returns boolean OR of arguments\r\n\r\n  # Test: not - returns boolean negation\r\n\r\n  # ==========================================\r\n  # Comparison Functions\r\n  # ==========================================\r\n  # Test: eq - returns true if equal\r\n\r\n  # Test: ne - returns true if not equal\r\n\r\n  # Test: lt - returns true if less than\r\n\r\n  # Test: le - returns true if less than or equal\r\n\r\n  # Test: gt - returns true if greater than\r\n\r\n  # Test: ge - returns true if greater than or equal\r\n\r\n  # ==========================================\r\n  # Default/Required Functions\r\n  # ==========================================\r\n  # Test: default - returns default value if empty\r\n\r\n  # Test: required - fails if value is empty\r\n\r\n  # Test: empty - returns true if value is empty\r\n\r\n  # Test: fail - explicitly fails with message\r\n\r\n  # ==========================================\r\n  # Coalesce/Ternary Functions\r\n  # ==========================================\r\n  # Test: coalesce - returns first non-empty value\r\n\r\n  # Test: ternary - conditional expression\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-logic-default/templates/logic-default-test.yaml",
            "content": "\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n  andResult: \"10\"\n\n  # Test: or - returns boolean OR of arguments\n\n  # Test: not - returns boolean negation\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n\n  # Test: ne - returns true if not equal\n\n  # Test: lt - returns true if less than\n\n  # Test: le - returns true if less than or equal\n\n  # Test: gt - returns true if greater than\n\n  # Test: ge - returns true if greater than or equal\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n\n  # Test: required - fails if value is empty\n\n  # Test: empty - returns true if value is empty\n\n  # Test: fail - explicitly fails with message\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n\n  # Test: ternary - conditional expression\n",
            "originalContent": "# Test: Logic and default functions - comprehensive testing\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n  {{- if .Values.testAnd }}\n  andResult: {{ and $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: or - returns boolean OR of arguments\n  {{- if .Values.testOr }}\n  orResult: {{ or $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: not - returns boolean negation\n  {{- if .Values.testNot }}\n  notResult: {{ not $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n  {{- if .Values.testEq }}\n  eqResult: {{ eq $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ne - returns true if not equal\n  {{- if .Values.testNe }}\n  neResult: {{ ne $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: lt - returns true if less than\n  {{- if .Values.testLt }}\n  ltResult: {{ lt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: le - returns true if less than or equal\n  {{- if .Values.testLe }}\n  leResult: {{ le $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: gt - returns true if greater than\n  {{- if .Values.testGt }}\n  gtResult: {{ gt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ge - returns true if greater than or equal\n  {{- if .Values.testGe }}\n  geResult: {{ ge $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n  {{- if .Values.testDefault }}\n  defaultResult: {{ default \"fallback\" $testData | quote }}\n  {{- end }}\n\n  # Test: required - fails if value is empty\n  {{- if .Values.testRequired }}\n  requiredResult: {{ required \"value is required\" $testData | quote }}\n  {{- end }}\n\n  # Test: empty - returns true if value is empty\n  {{- if .Values.testEmpty }}\n  emptyResult: {{ empty $testData | quote }}\n  {{- end }}\n\n  # Test: fail - explicitly fails with message\n  {{- if .Values.testFail }}\n  failResult: {{ fail (printf \"intentional fail with: %v\" $testData) }}\n  {{- end }}\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n  {{- if .Values.testCoalesce }}\n  coalesceResult: {{ coalesce $testData .Values.compareValue \"default\" | quote }}\n  {{- end }}\n\n  # Test: ternary - conditional expression\n  {{- if .Values.testTernary }}\n  ternaryResult: {{ ternary \"yes\" \"no\" $testData | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "and",
      "dataType": "listValue",
      "valuesOptions": {
        "values": [
          "testAnd=true",
          "testDataType=listValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-logic-test\r\ndata:\r\n  # ==========================================\r\n  # Boolean Logic Functions\r\n  # ==========================================\r\n  # Test: and - returns boolean AND of arguments\r\n  andResult: \"10\"\r\n\r\n  # Test: or - returns boolean OR of arguments\r\n\r\n  # Test: not - returns boolean negation\r\n\r\n  # ==========================================\r\n  # Comparison Functions\r\n  # ==========================================\r\n  # Test: eq - returns true if equal\r\n\r\n  # Test: ne - returns true if not equal\r\n\r\n  # Test: lt - returns true if less than\r\n\r\n  # Test: le - returns true if less than or equal\r\n\r\n  # Test: gt - returns true if greater than\r\n\r\n  # Test: ge - returns true if greater than or equal\r\n\r\n  # ==========================================\r\n  # Default/Required Functions\r\n  # ==========================================\r\n  # Test: default - returns default value if empty\r\n\r\n  # Test: required - fails if value is empty\r\n\r\n  # Test: empty - returns true if value is empty\r\n\r\n  # Test: fail - explicitly fails with message\r\n\r\n  # ==========================================\r\n  # Coalesce/Ternary Functions\r\n  # ==========================================\r\n  # Test: coalesce - returns first non-empty value\r\n\r\n  # Test: ternary - conditional expression\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-logic-default/templates/logic-default-test.yaml",
            "content": "\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n  andResult: \"10\"\n\n  # Test: or - returns boolean OR of arguments\n\n  # Test: not - returns boolean negation\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n\n  # Test: ne - returns true if not equal\n\n  # Test: lt - returns true if less than\n\n  # Test: le - returns true if less than or equal\n\n  # Test: gt - returns true if greater than\n\n  # Test: ge - returns true if greater than or equal\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n\n  # Test: required - fails if value is empty\n\n  # Test: empty - returns true if value is empty\n\n  # Test: fail - explicitly fails with message\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n\n  # Test: ternary - conditional expression\n",
            "originalContent": "# Test: Logic and default functions - comprehensive testing\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n  {{- if .Values.testAnd }}\n  andResult: {{ and $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: or - returns boolean OR of arguments\n  {{- if .Values.testOr }}\n  orResult: {{ or $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: not - returns boolean negation\n  {{- if .Values.testNot }}\n  notResult: {{ not $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n  {{- if .Values.testEq }}\n  eqResult: {{ eq $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ne - returns true if not equal\n  {{- if .Values.testNe }}\n  neResult: {{ ne $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: lt - returns true if less than\n  {{- if .Values.testLt }}\n  ltResult: {{ lt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: le - returns true if less than or equal\n  {{- if .Values.testLe }}\n  leResult: {{ le $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: gt - returns true if greater than\n  {{- if .Values.testGt }}\n  gtResult: {{ gt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ge - returns true if greater than or equal\n  {{- if .Values.testGe }}\n  geResult: {{ ge $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n  {{- if .Values.testDefault }}\n  defaultResult: {{ default \"fallback\" $testData | quote }}\n  {{- end }}\n\n  # Test: required - fails if value is empty\n  {{- if .Values.testRequired }}\n  requiredResult: {{ required \"value is required\" $testData | quote }}\n  {{- end }}\n\n  # Test: empty - returns true if value is empty\n  {{- if .Values.testEmpty }}\n  emptyResult: {{ empty $testData | quote }}\n  {{- end }}\n\n  # Test: fail - explicitly fails with message\n  {{- if .Values.testFail }}\n  failResult: {{ fail (printf \"intentional fail with: %v\" $testData) }}\n  {{- end }}\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n  {{- if .Values.testCoalesce }}\n  coalesceResult: {{ coalesce $testData .Values.compareValue \"default\" | quote }}\n  {{- end }}\n\n  # Test: ternary - conditional expression\n  {{- if .Values.testTernary }}\n  ternaryResult: {{ ternary \"yes\" \"no\" $testData | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-logic-test\r\ndata:\r\n  # ==========================================\r\n  # Boolean Logic Functions\r\n  # ==========================================\r\n  # Test: and - returns boolean AND of arguments\r\n  andResult: \"10\"\r\n\r\n  # Test: or - returns boolean OR of arguments\r\n\r\n  # Test: not - returns boolean negation\r\n\r\n  # ==========================================\r\n  # Comparison Functions\r\n  # ==========================================\r\n  # Test: eq - returns true if equal\r\n\r\n  # Test: ne - returns true if not equal\r\n\r\n  # Test: lt - returns true if less than\r\n\r\n  # Test: le - returns true if less than or equal\r\n\r\n  # Test: gt - returns true if greater than\r\n\r\n  # Test: ge - returns true if greater than or equal\r\n\r\n  # ==========================================\r\n  # Default/Required Functions\r\n  # ==========================================\r\n  # Test: default - returns default value if empty\r\n\r\n  # Test: required - fails if value is empty\r\n\r\n  # Test: empty - returns true if value is empty\r\n\r\n  # Test: fail - explicitly fails with message\r\n\r\n  # ==========================================\r\n  # Coalesce/Ternary Functions\r\n  # ==========================================\r\n  # Test: coalesce - returns first non-empty value\r\n\r\n  # Test: ternary - conditional expression\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-logic-default/templates/logic-default-test.yaml",
            "content": "\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n  andResult: \"10\"\n\n  # Test: or - returns boolean OR of arguments\n\n  # Test: not - returns boolean negation\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n\n  # Test: ne - returns true if not equal\n\n  # Test: lt - returns true if less than\n\n  # Test: le - returns true if less than or equal\n\n  # Test: gt - returns true if greater than\n\n  # Test: ge - returns true if greater than or equal\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n\n  # Test: required - fails if value is empty\n\n  # Test: empty - returns true if value is empty\n\n  # Test: fail - explicitly fails with message\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n\n  # Test: ternary - conditional expression\n",
            "originalContent": "# Test: Logic and default functions - comprehensive testing\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n  {{- if .Values.testAnd }}\n  andResult: {{ and $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: or - returns boolean OR of arguments\n  {{- if .Values.testOr }}\n  orResult: {{ or $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: not - returns boolean negation\n  {{- if .Values.testNot }}\n  notResult: {{ not $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n  {{- if .Values.testEq }}\n  eqResult: {{ eq $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ne - returns true if not equal\n  {{- if .Values.testNe }}\n  neResult: {{ ne $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: lt - returns true if less than\n  {{- if .Values.testLt }}\n  ltResult: {{ lt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: le - returns true if less than or equal\n  {{- if .Values.testLe }}\n  leResult: {{ le $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: gt - returns true if greater than\n  {{- if .Values.testGt }}\n  gtResult: {{ gt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ge - returns true if greater than or equal\n  {{- if .Values.testGe }}\n  geResult: {{ ge $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n  {{- if .Values.testDefault }}\n  defaultResult: {{ default \"fallback\" $testData | quote }}\n  {{- end }}\n\n  # Test: required - fails if value is empty\n  {{- if .Values.testRequired }}\n  requiredResult: {{ required \"value is required\" $testData | quote }}\n  {{- end }}\n\n  # Test: empty - returns true if value is empty\n  {{- if .Values.testEmpty }}\n  emptyResult: {{ empty $testData | quote }}\n  {{- end }}\n\n  # Test: fail - explicitly fails with message\n  {{- if .Values.testFail }}\n  failResult: {{ fail (printf \"intentional fail with: %v\" $testData) }}\n  {{- end }}\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n  {{- if .Values.testCoalesce }}\n  coalesceResult: {{ coalesce $testData .Values.compareValue \"default\" | quote }}\n  {{- end }}\n\n  # Test: ternary - conditional expression\n  {{- if .Values.testTernary }}\n  ternaryResult: {{ ternary \"yes\" \"no\" $testData | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "and",
      "dataType": "dictValue",
      "valuesOptions": {
        "values": [
          "testAnd=true",
          "testDataType=dictValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-logic-test\r\ndata:\r\n  # ==========================================\r\n  # Boolean Logic Functions\r\n  # ==========================================\r\n  # Test: and - returns boolean AND of arguments\r\n  andResult: \"10\"\r\n\r\n  # Test: or - returns boolean OR of arguments\r\n\r\n  # Test: not - returns boolean negation\r\n\r\n  # ==========================================\r\n  # Comparison Functions\r\n  # ==========================================\r\n  # Test: eq - returns true if equal\r\n\r\n  # Test: ne - returns true if not equal\r\n\r\n  # Test: lt - returns true if less than\r\n\r\n  # Test: le - returns true if less than or equal\r\n\r\n  # Test: gt - returns true if greater than\r\n\r\n  # Test: ge - returns true if greater than or equal\r\n\r\n  # ==========================================\r\n  # Default/Required Functions\r\n  # ==========================================\r\n  # Test: default - returns default value if empty\r\n\r\n  # Test: required - fails if value is empty\r\n\r\n  # Test: empty - returns true if value is empty\r\n\r\n  # Test: fail - explicitly fails with message\r\n\r\n  # ==========================================\r\n  # Coalesce/Ternary Functions\r\n  # ==========================================\r\n  # Test: coalesce - returns first non-empty value\r\n\r\n  # Test: ternary - conditional expression\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-logic-default/templates/logic-default-test.yaml",
            "content": "\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n  andResult: \"10\"\n\n  # Test: or - returns boolean OR of arguments\n\n  # Test: not - returns boolean negation\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n\n  # Test: ne - returns true if not equal\n\n  # Test: lt - returns true if less than\n\n  # Test: le - returns true if less than or equal\n\n  # Test: gt - returns true if greater than\n\n  # Test: ge - returns true if greater than or equal\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n\n  # Test: required - fails if value is empty\n\n  # Test: empty - returns true if value is empty\n\n  # Test: fail - explicitly fails with message\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n\n  # Test: ternary - conditional expression\n",
            "originalContent": "# Test: Logic and default functions - comprehensive testing\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n  {{- if .Values.testAnd }}\n  andResult: {{ and $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: or - returns boolean OR of arguments\n  {{- if .Values.testOr }}\n  orResult: {{ or $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: not - returns boolean negation\n  {{- if .Values.testNot }}\n  notResult: {{ not $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n  {{- if .Values.testEq }}\n  eqResult: {{ eq $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ne - returns true if not equal\n  {{- if .Values.testNe }}\n  neResult: {{ ne $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: lt - returns true if less than\n  {{- if .Values.testLt }}\n  ltResult: {{ lt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: le - returns true if less than or equal\n  {{- if .Values.testLe }}\n  leResult: {{ le $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: gt - returns true if greater than\n  {{- if .Values.testGt }}\n  gtResult: {{ gt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ge - returns true if greater than or equal\n  {{- if .Values.testGe }}\n  geResult: {{ ge $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n  {{- if .Values.testDefault }}\n  defaultResult: {{ default \"fallback\" $testData | quote }}\n  {{- end }}\n\n  # Test: required - fails if value is empty\n  {{- if .Values.testRequired }}\n  requiredResult: {{ required \"value is required\" $testData | quote }}\n  {{- end }}\n\n  # Test: empty - returns true if value is empty\n  {{- if .Values.testEmpty }}\n  emptyResult: {{ empty $testData | quote }}\n  {{- end }}\n\n  # Test: fail - explicitly fails with message\n  {{- if .Values.testFail }}\n  failResult: {{ fail (printf \"intentional fail with: %v\" $testData) }}\n  {{- end }}\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n  {{- if .Values.testCoalesce }}\n  coalesceResult: {{ coalesce $testData .Values.compareValue \"default\" | quote }}\n  {{- end }}\n\n  # Test: ternary - conditional expression\n  {{- if .Values.testTernary }}\n  ternaryResult: {{ ternary \"yes\" \"no\" $testData | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-logic-test\r\ndata:\r\n  # ==========================================\r\n  # Boolean Logic Functions\r\n  # ==========================================\r\n  # Test: and - returns boolean AND of arguments\r\n  andResult: \"10\"\r\n\r\n  # Test: or - returns boolean OR of arguments\r\n\r\n  # Test: not - returns boolean negation\r\n\r\n  # ==========================================\r\n  # Comparison Functions\r\n  # ==========================================\r\n  # Test: eq - returns true if equal\r\n\r\n  # Test: ne - returns true if not equal\r\n\r\n  # Test: lt - returns true if less than\r\n\r\n  # Test: le - returns true if less than or equal\r\n\r\n  # Test: gt - returns true if greater than\r\n\r\n  # Test: ge - returns true if greater than or equal\r\n\r\n  # ==========================================\r\n  # Default/Required Functions\r\n  # ==========================================\r\n  # Test: default - returns default value if empty\r\n\r\n  # Test: required - fails if value is empty\r\n\r\n  # Test: empty - returns true if value is empty\r\n\r\n  # Test: fail - explicitly fails with message\r\n\r\n  # ==========================================\r\n  # Coalesce/Ternary Functions\r\n  # ==========================================\r\n  # Test: coalesce - returns first non-empty value\r\n\r\n  # Test: ternary - conditional expression\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-logic-default/templates/logic-default-test.yaml",
            "content": "\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n  andResult: \"10\"\n\n  # Test: or - returns boolean OR of arguments\n\n  # Test: not - returns boolean negation\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n\n  # Test: ne - returns true if not equal\n\n  # Test: lt - returns true if less than\n\n  # Test: le - returns true if less than or equal\n\n  # Test: gt - returns true if greater than\n\n  # Test: ge - returns true if greater than or equal\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n\n  # Test: required - fails if value is empty\n\n  # Test: empty - returns true if value is empty\n\n  # Test: fail - explicitly fails with message\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n\n  # Test: ternary - conditional expression\n",
            "originalContent": "# Test: Logic and default functions - comprehensive testing\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n  {{- if .Values.testAnd }}\n  andResult: {{ and $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: or - returns boolean OR of arguments\n  {{- if .Values.testOr }}\n  orResult: {{ or $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: not - returns boolean negation\n  {{- if .Values.testNot }}\n  notResult: {{ not $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n  {{- if .Values.testEq }}\n  eqResult: {{ eq $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ne - returns true if not equal\n  {{- if .Values.testNe }}\n  neResult: {{ ne $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: lt - returns true if less than\n  {{- if .Values.testLt }}\n  ltResult: {{ lt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: le - returns true if less than or equal\n  {{- if .Values.testLe }}\n  leResult: {{ le $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: gt - returns true if greater than\n  {{- if .Values.testGt }}\n  gtResult: {{ gt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ge - returns true if greater than or equal\n  {{- if .Values.testGe }}\n  geResult: {{ ge $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n  {{- if .Values.testDefault }}\n  defaultResult: {{ default \"fallback\" $testData | quote }}\n  {{- end }}\n\n  # Test: required - fails if value is empty\n  {{- if .Values.testRequired }}\n  requiredResult: {{ required \"value is required\" $testData | quote }}\n  {{- end }}\n\n  # Test: empty - returns true if value is empty\n  {{- if .Values.testEmpty }}\n  emptyResult: {{ empty $testData | quote }}\n  {{- end }}\n\n  # Test: fail - explicitly fails with message\n  {{- if .Values.testFail }}\n  failResult: {{ fail (printf \"intentional fail with: %v\" $testData) }}\n  {{- end }}\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n  {{- if .Values.testCoalesce }}\n  coalesceResult: {{ coalesce $testData .Values.compareValue \"default\" | quote }}\n  {{- end }}\n\n  # Test: ternary - conditional expression\n  {{- if .Values.testTernary }}\n  ternaryResult: {{ ternary \"yes\" \"no\" $testData | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "or",
      "dataType": "nilValue",
      "valuesOptions": {
        "values": [
          "testOr=true",
          "testDataType=nilValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-logic-test\r\ndata:\r\n  # ==========================================\r\n  # Boolean Logic Functions\r\n  # ==========================================\r\n  # Test: and - returns boolean AND of arguments\r\n\r\n  # Test: or - returns boolean OR of arguments\r\n  orResult: \"10\"\r\n\r\n  # Test: not - returns boolean negation\r\n\r\n  # ==========================================\r\n  # Comparison Functions\r\n  # ==========================================\r\n  # Test: eq - returns true if equal\r\n\r\n  # Test: ne - returns true if not equal\r\n\r\n  # Test: lt - returns true if less than\r\n\r\n  # Test: le - returns true if less than or equal\r\n\r\n  # Test: gt - returns true if greater than\r\n\r\n  # Test: ge - returns true if greater than or equal\r\n\r\n  # ==========================================\r\n  # Default/Required Functions\r\n  # ==========================================\r\n  # Test: default - returns default value if empty\r\n\r\n  # Test: required - fails if value is empty\r\n\r\n  # Test: empty - returns true if value is empty\r\n\r\n  # Test: fail - explicitly fails with message\r\n\r\n  # ==========================================\r\n  # Coalesce/Ternary Functions\r\n  # ==========================================\r\n  # Test: coalesce - returns first non-empty value\r\n\r\n  # Test: ternary - conditional expression\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-logic-default/templates/logic-default-test.yaml",
            "content": "\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n\n  # Test: or - returns boolean OR of arguments\n  orResult: \"10\"\n\n  # Test: not - returns boolean negation\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n\n  # Test: ne - returns true if not equal\n\n  # Test: lt - returns true if less than\n\n  # Test: le - returns true if less than or equal\n\n  # Test: gt - returns true if greater than\n\n  # Test: ge - returns true if greater than or equal\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n\n  # Test: required - fails if value is empty\n\n  # Test: empty - returns true if value is empty\n\n  # Test: fail - explicitly fails with message\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n\n  # Test: ternary - conditional expression\n",
            "originalContent": "# Test: Logic and default functions - comprehensive testing\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n  {{- if .Values.testAnd }}\n  andResult: {{ and $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: or - returns boolean OR of arguments\n  {{- if .Values.testOr }}\n  orResult: {{ or $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: not - returns boolean negation\n  {{- if .Values.testNot }}\n  notResult: {{ not $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n  {{- if .Values.testEq }}\n  eqResult: {{ eq $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ne - returns true if not equal\n  {{- if .Values.testNe }}\n  neResult: {{ ne $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: lt - returns true if less than\n  {{- if .Values.testLt }}\n  ltResult: {{ lt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: le - returns true if less than or equal\n  {{- if .Values.testLe }}\n  leResult: {{ le $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: gt - returns true if greater than\n  {{- if .Values.testGt }}\n  gtResult: {{ gt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ge - returns true if greater than or equal\n  {{- if .Values.testGe }}\n  geResult: {{ ge $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n  {{- if .Values.testDefault }}\n  defaultResult: {{ default \"fallback\" $testData | quote }}\n  {{- end }}\n\n  # Test: required - fails if value is empty\n  {{- if .Values.testRequired }}\n  requiredResult: {{ required \"value is required\" $testData | quote }}\n  {{- end }}\n\n  # Test: empty - returns true if value is empty\n  {{- if .Values.testEmpty }}\n  emptyResult: {{ empty $testData | quote }}\n  {{- end }}\n\n  # Test: fail - explicitly fails with message\n  {{- if .Values.testFail }}\n  failResult: {{ fail (printf \"intentional fail with: %v\" $testData) }}\n  {{- end }}\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n  {{- if .Values.testCoalesce }}\n  coalesceResult: {{ coalesce $testData .Values.compareValue \"default\" | quote }}\n  {{- end }}\n\n  # Test: ternary - conditional expression\n  {{- if .Values.testTernary }}\n  ternaryResult: {{ ternary \"yes\" \"no\" $testData | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-logic-test\r\ndata:\r\n  # ==========================================\r\n  # Boolean Logic Functions\r\n  # ==========================================\r\n  # Test: and - returns boolean AND of arguments\r\n\r\n  # Test: or - returns boolean OR of arguments\r\n  orResult: \"10\"\r\n\r\n  # Test: not - returns boolean negation\r\n\r\n  # ==========================================\r\n  # Comparison Functions\r\n  # ==========================================\r\n  # Test: eq - returns true if equal\r\n\r\n  # Test: ne - returns true if not equal\r\n\r\n  # Test: lt - returns true if less than\r\n\r\n  # Test: le - returns true if less than or equal\r\n\r\n  # Test: gt - returns true if greater than\r\n\r\n  # Test: ge - returns true if greater than or equal\r\n\r\n  # ==========================================\r\n  # Default/Required Functions\r\n  # ==========================================\r\n  # Test: default - returns default value if empty\r\n\r\n  # Test: required - fails if value is empty\r\n\r\n  # Test: empty - returns true if value is empty\r\n\r\n  # Test: fail - explicitly fails with message\r\n\r\n  # ==========================================\r\n  # Coalesce/Ternary Functions\r\n  # ==========================================\r\n  # Test: coalesce - returns first non-empty value\r\n\r\n  # Test: ternary - conditional expression\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-logic-default/templates/logic-default-test.yaml",
            "content": "\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n\n  # Test: or - returns boolean OR of arguments\n  orResult: \"10\"\n\n  # Test: not - returns boolean negation\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n\n  # Test: ne - returns true if not equal\n\n  # Test: lt - returns true if less than\n\n  # Test: le - returns true if less than or equal\n\n  # Test: gt - returns true if greater than\n\n  # Test: ge - returns true if greater than or equal\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n\n  # Test: required - fails if value is empty\n\n  # Test: empty - returns true if value is empty\n\n  # Test: fail - explicitly fails with message\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n\n  # Test: ternary - conditional expression\n",
            "originalContent": "# Test: Logic and default functions - comprehensive testing\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n  {{- if .Values.testAnd }}\n  andResult: {{ and $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: or - returns boolean OR of arguments\n  {{- if .Values.testOr }}\n  orResult: {{ or $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: not - returns boolean negation\n  {{- if .Values.testNot }}\n  notResult: {{ not $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n  {{- if .Values.testEq }}\n  eqResult: {{ eq $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ne - returns true if not equal\n  {{- if .Values.testNe }}\n  neResult: {{ ne $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: lt - returns true if less than\n  {{- if .Values.testLt }}\n  ltResult: {{ lt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: le - returns true if less than or equal\n  {{- if .Values.testLe }}\n  leResult: {{ le $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: gt - returns true if greater than\n  {{- if .Values.testGt }}\n  gtResult: {{ gt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ge - returns true if greater than or equal\n  {{- if .Values.testGe }}\n  geResult: {{ ge $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n  {{- if .Values.testDefault }}\n  defaultResult: {{ default \"fallback\" $testData | quote }}\n  {{- end }}\n\n  # Test: required - fails if value is empty\n  {{- if .Values.testRequired }}\n  requiredResult: {{ required \"value is required\" $testData | quote }}\n  {{- end }}\n\n  # Test: empty - returns true if value is empty\n  {{- if .Values.testEmpty }}\n  emptyResult: {{ empty $testData | quote }}\n  {{- end }}\n\n  # Test: fail - explicitly fails with message\n  {{- if .Values.testFail }}\n  failResult: {{ fail (printf \"intentional fail with: %v\" $testData) }}\n  {{- end }}\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n  {{- if .Values.testCoalesce }}\n  coalesceResult: {{ coalesce $testData .Values.compareValue \"default\" | quote }}\n  {{- end }}\n\n  # Test: ternary - conditional expression\n  {{- if .Values.testTernary }}\n  ternaryResult: {{ ternary \"yes\" \"no\" $testData | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "or",
      "dataType": "stringValue",
      "valuesOptions": {
        "values": [
          "testOr=true",
          "testDataType=stringValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-logic-test\r\ndata:\r\n  # ==========================================\r\n  # Boolean Logic Functions\r\n  # ==========================================\r\n  # Test: and - returns boolean AND of arguments\r\n\r\n  # Test: or - returns boolean OR of arguments\r\n  orResult: \"test string\"\r\n\r\n  # Test: not - returns boolean negation\r\n\r\n  # ==========================================\r\n  # Comparison Functions\r\n  # ==========================================\r\n  # Test: eq - returns true if equal\r\n\r\n  # Test: ne - returns true if not equal\r\n\r\n  # Test: lt - returns true if less than\r\n\r\n  # Test: le - returns true if less than or equal\r\n\r\n  # Test: gt - returns true if greater than\r\n\r\n  # Test: ge - returns true if greater than or equal\r\n\r\n  # ==========================================\r\n  # Default/Required Functions\r\n  # ==========================================\r\n  # Test: default - returns default value if empty\r\n\r\n  # Test: required - fails if value is empty\r\n\r\n  # Test: empty - returns true if value is empty\r\n\r\n  # Test: fail - explicitly fails with message\r\n\r\n  # ==========================================\r\n  # Coalesce/Ternary Functions\r\n  # ==========================================\r\n  # Test: coalesce - returns first non-empty value\r\n\r\n  # Test: ternary - conditional expression\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-logic-default/templates/logic-default-test.yaml",
            "content": "\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n\n  # Test: or - returns boolean OR of arguments\n  orResult: \"test string\"\n\n  # Test: not - returns boolean negation\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n\n  # Test: ne - returns true if not equal\n\n  # Test: lt - returns true if less than\n\n  # Test: le - returns true if less than or equal\n\n  # Test: gt - returns true if greater than\n\n  # Test: ge - returns true if greater than or equal\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n\n  # Test: required - fails if value is empty\n\n  # Test: empty - returns true if value is empty\n\n  # Test: fail - explicitly fails with message\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n\n  # Test: ternary - conditional expression\n",
            "originalContent": "# Test: Logic and default functions - comprehensive testing\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n  {{- if .Values.testAnd }}\n  andResult: {{ and $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: or - returns boolean OR of arguments\n  {{- if .Values.testOr }}\n  orResult: {{ or $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: not - returns boolean negation\n  {{- if .Values.testNot }}\n  notResult: {{ not $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n  {{- if .Values.testEq }}\n  eqResult: {{ eq $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ne - returns true if not equal\n  {{- if .Values.testNe }}\n  neResult: {{ ne $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: lt - returns true if less than\n  {{- if .Values.testLt }}\n  ltResult: {{ lt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: le - returns true if less than or equal\n  {{- if .Values.testLe }}\n  leResult: {{ le $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: gt - returns true if greater than\n  {{- if .Values.testGt }}\n  gtResult: {{ gt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ge - returns true if greater than or equal\n  {{- if .Values.testGe }}\n  geResult: {{ ge $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n  {{- if .Values.testDefault }}\n  defaultResult: {{ default \"fallback\" $testData | quote }}\n  {{- end }}\n\n  # Test: required - fails if value is empty\n  {{- if .Values.testRequired }}\n  requiredResult: {{ required \"value is required\" $testData | quote }}\n  {{- end }}\n\n  # Test: empty - returns true if value is empty\n  {{- if .Values.testEmpty }}\n  emptyResult: {{ empty $testData | quote }}\n  {{- end }}\n\n  # Test: fail - explicitly fails with message\n  {{- if .Values.testFail }}\n  failResult: {{ fail (printf \"intentional fail with: %v\" $testData) }}\n  {{- end }}\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n  {{- if .Values.testCoalesce }}\n  coalesceResult: {{ coalesce $testData .Values.compareValue \"default\" | quote }}\n  {{- end }}\n\n  # Test: ternary - conditional expression\n  {{- if .Values.testTernary }}\n  ternaryResult: {{ ternary \"yes\" \"no\" $testData | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-logic-test\r\ndata:\r\n  # ==========================================\r\n  # Boolean Logic Functions\r\n  # ==========================================\r\n  # Test: and - returns boolean AND of arguments\r\n\r\n  # Test: or - returns boolean OR of arguments\r\n  orResult: \"test string\"\r\n\r\n  # Test: not - returns boolean negation\r\n\r\n  # ==========================================\r\n  # Comparison Functions\r\n  # ==========================================\r\n  # Test: eq - returns true if equal\r\n\r\n  # Test: ne - returns true if not equal\r\n\r\n  # Test: lt - returns true if less than\r\n\r\n  # Test: le - returns true if less than or equal\r\n\r\n  # Test: gt - returns true if greater than\r\n\r\n  # Test: ge - returns true if greater than or equal\r\n\r\n  # ==========================================\r\n  # Default/Required Functions\r\n  # ==========================================\r\n  # Test: default - returns default value if empty\r\n\r\n  # Test: required - fails if value is empty\r\n\r\n  # Test: empty - returns true if value is empty\r\n\r\n  # Test: fail - explicitly fails with message\r\n\r\n  # ==========================================\r\n  # Coalesce/Ternary Functions\r\n  # ==========================================\r\n  # Test: coalesce - returns first non-empty value\r\n\r\n  # Test: ternary - conditional expression\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-logic-default/templates/logic-default-test.yaml",
            "content": "\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n\n  # Test: or - returns boolean OR of arguments\n  orResult: \"test string\"\n\n  # Test: not - returns boolean negation\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n\n  # Test: ne - returns true if not equal\n\n  # Test: lt - returns true if less than\n\n  # Test: le - returns true if less than or equal\n\n  # Test: gt - returns true if greater than\n\n  # Test: ge - returns true if greater than or equal\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n\n  # Test: required - fails if value is empty\n\n  # Test: empty - returns true if value is empty\n\n  # Test: fail - explicitly fails with message\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n\n  # Test: ternary - conditional expression\n",
            "originalContent": "# Test: Logic and default functions - comprehensive testing\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n  {{- if .Values.testAnd }}\n  andResult: {{ and $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: or - returns boolean OR of arguments\n  {{- if .Values.testOr }}\n  orResult: {{ or $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: not - returns boolean negation\n  {{- if .Values.testNot }}\n  notResult: {{ not $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n  {{- if .Values.testEq }}\n  eqResult: {{ eq $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ne - returns true if not equal\n  {{- if .Values.testNe }}\n  neResult: {{ ne $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: lt - returns true if less than\n  {{- if .Values.testLt }}\n  ltResult: {{ lt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: le - returns true if less than or equal\n  {{- if .Values.testLe }}\n  leResult: {{ le $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: gt - returns true if greater than\n  {{- if .Values.testGt }}\n  gtResult: {{ gt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ge - returns true if greater than or equal\n  {{- if .Values.testGe }}\n  geResult: {{ ge $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n  {{- if .Values.testDefault }}\n  defaultResult: {{ default \"fallback\" $testData | quote }}\n  {{- end }}\n\n  # Test: required - fails if value is empty\n  {{- if .Values.testRequired }}\n  requiredResult: {{ required \"value is required\" $testData | quote }}\n  {{- end }}\n\n  # Test: empty - returns true if value is empty\n  {{- if .Values.testEmpty }}\n  emptyResult: {{ empty $testData | quote }}\n  {{- end }}\n\n  # Test: fail - explicitly fails with message\n  {{- if .Values.testFail }}\n  failResult: {{ fail (printf \"intentional fail with: %v\" $testData) }}\n  {{- end }}\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n  {{- if .Values.testCoalesce }}\n  coalesceResult: {{ coalesce $testData .Values.compareValue \"default\" | quote }}\n  {{- end }}\n\n  # Test: ternary - conditional expression\n  {{- if .Values.testTernary }}\n  ternaryResult: {{ ternary \"yes\" \"no\" $testData | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "or",
      "dataType": "numberValue",
      "valuesOptions": {
        "values": [
          "testOr=true",
          "testDataType=numberValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-logic-test\r\ndata:\r\n  # ==========================================\r\n  # Boolean Logic Functions\r\n  # ==========================================\r\n  # Test: and - returns boolean AND of arguments\r\n\r\n  # Test: or - returns boolean OR of arguments\r\n  orResult: \"42\"\r\n\r\n  # Test: not - returns boolean negation\r\n\r\n  # ==========================================\r\n  # Comparison Functions\r\n  # ==========================================\r\n  # Test: eq - returns true if equal\r\n\r\n  # Test: ne - returns true if not equal\r\n\r\n  # Test: lt - returns true if less than\r\n\r\n  # Test: le - returns true if less than or equal\r\n\r\n  # Test: gt - returns true if greater than\r\n\r\n  # Test: ge - returns true if greater than or equal\r\n\r\n  # ==========================================\r\n  # Default/Required Functions\r\n  # ==========================================\r\n  # Test: default - returns default value if empty\r\n\r\n  # Test: required - fails if value is empty\r\n\r\n  # Test: empty - returns true if value is empty\r\n\r\n  # Test: fail - explicitly fails with message\r\n\r\n  # ==========================================\r\n  # Coalesce/Ternary Functions\r\n  # ==========================================\r\n  # Test: coalesce - returns first non-empty value\r\n\r\n  # Test: ternary - conditional expression\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-logic-default/templates/logic-default-test.yaml",
            "content": "\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n\n  # Test: or - returns boolean OR of arguments\n  orResult: \"42\"\n\n  # Test: not - returns boolean negation\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n\n  # Test: ne - returns true if not equal\n\n  # Test: lt - returns true if less than\n\n  # Test: le - returns true if less than or equal\n\n  # Test: gt - returns true if greater than\n\n  # Test: ge - returns true if greater than or equal\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n\n  # Test: required - fails if value is empty\n\n  # Test: empty - returns true if value is empty\n\n  # Test: fail - explicitly fails with message\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n\n  # Test: ternary - conditional expression\n",
            "originalContent": "# Test: Logic and default functions - comprehensive testing\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n  {{- if .Values.testAnd }}\n  andResult: {{ and $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: or - returns boolean OR of arguments\n  {{- if .Values.testOr }}\n  orResult: {{ or $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: not - returns boolean negation\n  {{- if .Values.testNot }}\n  notResult: {{ not $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n  {{- if .Values.testEq }}\n  eqResult: {{ eq $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ne - returns true if not equal\n  {{- if .Values.testNe }}\n  neResult: {{ ne $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: lt - returns true if less than\n  {{- if .Values.testLt }}\n  ltResult: {{ lt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: le - returns true if less than or equal\n  {{- if .Values.testLe }}\n  leResult: {{ le $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: gt - returns true if greater than\n  {{- if .Values.testGt }}\n  gtResult: {{ gt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ge - returns true if greater than or equal\n  {{- if .Values.testGe }}\n  geResult: {{ ge $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n  {{- if .Values.testDefault }}\n  defaultResult: {{ default \"fallback\" $testData | quote }}\n  {{- end }}\n\n  # Test: required - fails if value is empty\n  {{- if .Values.testRequired }}\n  requiredResult: {{ required \"value is required\" $testData | quote }}\n  {{- end }}\n\n  # Test: empty - returns true if value is empty\n  {{- if .Values.testEmpty }}\n  emptyResult: {{ empty $testData | quote }}\n  {{- end }}\n\n  # Test: fail - explicitly fails with message\n  {{- if .Values.testFail }}\n  failResult: {{ fail (printf \"intentional fail with: %v\" $testData) }}\n  {{- end }}\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n  {{- if .Values.testCoalesce }}\n  coalesceResult: {{ coalesce $testData .Values.compareValue \"default\" | quote }}\n  {{- end }}\n\n  # Test: ternary - conditional expression\n  {{- if .Values.testTernary }}\n  ternaryResult: {{ ternary \"yes\" \"no\" $testData | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-logic-test\r\ndata:\r\n  # ==========================================\r\n  # Boolean Logic Functions\r\n  # ==========================================\r\n  # Test: and - returns boolean AND of arguments\r\n\r\n  # Test: or - returns boolean OR of arguments\r\n  orResult: \"42\"\r\n\r\n  # Test: not - returns boolean negation\r\n\r\n  # ==========================================\r\n  # Comparison Functions\r\n  # ==========================================\r\n  # Test: eq - returns true if equal\r\n\r\n  # Test: ne - returns true if not equal\r\n\r\n  # Test: lt - returns true if less than\r\n\r\n  # Test: le - returns true if less than or equal\r\n\r\n  # Test: gt - returns true if greater than\r\n\r\n  # Test: ge - returns true if greater than or equal\r\n\r\n  # ==========================================\r\n  # Default/Required Functions\r\n  # ==========================================\r\n  # Test: default - returns default value if empty\r\n\r\n  # Test: required - fails if value is empty\r\n\r\n  # Test: empty - returns true if value is empty\r\n\r\n  # Test: fail - explicitly fails with message\r\n\r\n  # ==========================================\r\n  # Coalesce/Ternary Functions\r\n  # ==========================================\r\n  # Test: coalesce - returns first non-empty value\r\n\r\n  # Test: ternary - conditional expression\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-logic-default/templates/logic-default-test.yaml",
            "content": "\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n\n  # Test: or - returns boolean OR of arguments\n  orResult: \"42\"\n\n  # Test: not - returns boolean negation\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n\n  # Test: ne - returns true if not equal\n\n  # Test: lt - returns true if less than\n\n  # Test: le - returns true if less than or equal\n\n  # Test: gt - returns true if greater than\n\n  # Test: ge - returns true if greater than or equal\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n\n  # Test: required - fails if value is empty\n\n  # Test: empty - returns true if value is empty\n\n  # Test: fail - explicitly fails with message\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n\n  # Test: ternary - conditional expression\n",
            "originalContent": "# Test: Logic and default functions - comprehensive testing\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n  {{- if .Values.testAnd }}\n  andResult: {{ and $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: or - returns boolean OR of arguments\n  {{- if .Values.testOr }}\n  orResult: {{ or $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: not - returns boolean negation\n  {{- if .Values.testNot }}\n  notResult: {{ not $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n  {{- if .Values.testEq }}\n  eqResult: {{ eq $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ne - returns true if not equal\n  {{- if .Values.testNe }}\n  neResult: {{ ne $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: lt - returns true if less than\n  {{- if .Values.testLt }}\n  ltResult: {{ lt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: le - returns true if less than or equal\n  {{- if .Values.testLe }}\n  leResult: {{ le $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: gt - returns true if greater than\n  {{- if .Values.testGt }}\n  gtResult: {{ gt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ge - returns true if greater than or equal\n  {{- if .Values.testGe }}\n  geResult: {{ ge $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n  {{- if .Values.testDefault }}\n  defaultResult: {{ default \"fallback\" $testData | quote }}\n  {{- end }}\n\n  # Test: required - fails if value is empty\n  {{- if .Values.testRequired }}\n  requiredResult: {{ required \"value is required\" $testData | quote }}\n  {{- end }}\n\n  # Test: empty - returns true if value is empty\n  {{- if .Values.testEmpty }}\n  emptyResult: {{ empty $testData | quote }}\n  {{- end }}\n\n  # Test: fail - explicitly fails with message\n  {{- if .Values.testFail }}\n  failResult: {{ fail (printf \"intentional fail with: %v\" $testData) }}\n  {{- end }}\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n  {{- if .Values.testCoalesce }}\n  coalesceResult: {{ coalesce $testData .Values.compareValue \"default\" | quote }}\n  {{- end }}\n\n  # Test: ternary - conditional expression\n  {{- if .Values.testTernary }}\n  ternaryResult: {{ ternary \"yes\" \"no\" $testData | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "or",
      "dataType": "boolValue",
      "valuesOptions": {
        "values": [
          "testOr=true",
          "testDataType=boolValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-logic-test\r\ndata:\r\n  # ==========================================\r\n  # Boolean Logic Functions\r\n  # ==========================================\r\n  # Test: and - returns boolean AND of arguments\r\n\r\n  # Test: or - returns boolean OR of arguments\r\n  orResult: \"true\"\r\n\r\n  # Test: not - returns boolean negation\r\n\r\n  # ==========================================\r\n  # Comparison Functions\r\n  # ==========================================\r\n  # Test: eq - returns true if equal\r\n\r\n  # Test: ne - returns true if not equal\r\n\r\n  # Test: lt - returns true if less than\r\n\r\n  # Test: le - returns true if less than or equal\r\n\r\n  # Test: gt - returns true if greater than\r\n\r\n  # Test: ge - returns true if greater than or equal\r\n\r\n  # ==========================================\r\n  # Default/Required Functions\r\n  # ==========================================\r\n  # Test: default - returns default value if empty\r\n\r\n  # Test: required - fails if value is empty\r\n\r\n  # Test: empty - returns true if value is empty\r\n\r\n  # Test: fail - explicitly fails with message\r\n\r\n  # ==========================================\r\n  # Coalesce/Ternary Functions\r\n  # ==========================================\r\n  # Test: coalesce - returns first non-empty value\r\n\r\n  # Test: ternary - conditional expression\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-logic-default/templates/logic-default-test.yaml",
            "content": "\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n\n  # Test: or - returns boolean OR of arguments\n  orResult: \"true\"\n\n  # Test: not - returns boolean negation\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n\n  # Test: ne - returns true if not equal\n\n  # Test: lt - returns true if less than\n\n  # Test: le - returns true if less than or equal\n\n  # Test: gt - returns true if greater than\n\n  # Test: ge - returns true if greater than or equal\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n\n  # Test: required - fails if value is empty\n\n  # Test: empty - returns true if value is empty\n\n  # Test: fail - explicitly fails with message\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n\n  # Test: ternary - conditional expression\n",
            "originalContent": "# Test: Logic and default functions - comprehensive testing\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n  {{- if .Values.testAnd }}\n  andResult: {{ and $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: or - returns boolean OR of arguments\n  {{- if .Values.testOr }}\n  orResult: {{ or $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: not - returns boolean negation\n  {{- if .Values.testNot }}\n  notResult: {{ not $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n  {{- if .Values.testEq }}\n  eqResult: {{ eq $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ne - returns true if not equal\n  {{- if .Values.testNe }}\n  neResult: {{ ne $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: lt - returns true if less than\n  {{- if .Values.testLt }}\n  ltResult: {{ lt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: le - returns true if less than or equal\n  {{- if .Values.testLe }}\n  leResult: {{ le $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: gt - returns true if greater than\n  {{- if .Values.testGt }}\n  gtResult: {{ gt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ge - returns true if greater than or equal\n  {{- if .Values.testGe }}\n  geResult: {{ ge $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n  {{- if .Values.testDefault }}\n  defaultResult: {{ default \"fallback\" $testData | quote }}\n  {{- end }}\n\n  # Test: required - fails if value is empty\n  {{- if .Values.testRequired }}\n  requiredResult: {{ required \"value is required\" $testData | quote }}\n  {{- end }}\n\n  # Test: empty - returns true if value is empty\n  {{- if .Values.testEmpty }}\n  emptyResult: {{ empty $testData | quote }}\n  {{- end }}\n\n  # Test: fail - explicitly fails with message\n  {{- if .Values.testFail }}\n  failResult: {{ fail (printf \"intentional fail with: %v\" $testData) }}\n  {{- end }}\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n  {{- if .Values.testCoalesce }}\n  coalesceResult: {{ coalesce $testData .Values.compareValue \"default\" | quote }}\n  {{- end }}\n\n  # Test: ternary - conditional expression\n  {{- if .Values.testTernary }}\n  ternaryResult: {{ ternary \"yes\" \"no\" $testData | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-logic-test\r\ndata:\r\n  # ==========================================\r\n  # Boolean Logic Functions\r\n  # ==========================================\r\n  # Test: and - returns boolean AND of arguments\r\n\r\n  # Test: or - returns boolean OR of arguments\r\n  orResult: \"true\"\r\n\r\n  # Test: not - returns boolean negation\r\n\r\n  # ==========================================\r\n  # Comparison Functions\r\n  # ==========================================\r\n  # Test: eq - returns true if equal\r\n\r\n  # Test: ne - returns true if not equal\r\n\r\n  # Test: lt - returns true if less than\r\n\r\n  # Test: le - returns true if less than or equal\r\n\r\n  # Test: gt - returns true if greater than\r\n\r\n  # Test: ge - returns true if greater than or equal\r\n\r\n  # ==========================================\r\n  # Default/Required Functions\r\n  # ==========================================\r\n  # Test: default - returns default value if empty\r\n\r\n  # Test: required - fails if value is empty\r\n\r\n  # Test: empty - returns true if value is empty\r\n\r\n  # Test: fail - explicitly fails with message\r\n\r\n  # ==========================================\r\n  # Coalesce/Ternary Functions\r\n  # ==========================================\r\n  # Test: coalesce - returns first non-empty value\r\n\r\n  # Test: ternary - conditional expression\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-logic-default/templates/logic-default-test.yaml",
            "content": "\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n\n  # Test: or - returns boolean OR of arguments\n  orResult: \"true\"\n\n  # Test: not - returns boolean negation\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n\n  # Test: ne - returns true if not equal\n\n  # Test: lt - returns true if less than\n\n  # Test: le - returns true if less than or equal\n\n  # Test: gt - returns true if greater than\n\n  # Test: ge - returns true if greater than or equal\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n\n  # Test: required - fails if value is empty\n\n  # Test: empty - returns true if value is empty\n\n  # Test: fail - explicitly fails with message\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n\n  # Test: ternary - conditional expression\n",
            "originalContent": "# Test: Logic and default functions - comprehensive testing\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n  {{- if .Values.testAnd }}\n  andResult: {{ and $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: or - returns boolean OR of arguments\n  {{- if .Values.testOr }}\n  orResult: {{ or $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: not - returns boolean negation\n  {{- if .Values.testNot }}\n  notResult: {{ not $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n  {{- if .Values.testEq }}\n  eqResult: {{ eq $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ne - returns true if not equal\n  {{- if .Values.testNe }}\n  neResult: {{ ne $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: lt - returns true if less than\n  {{- if .Values.testLt }}\n  ltResult: {{ lt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: le - returns true if less than or equal\n  {{- if .Values.testLe }}\n  leResult: {{ le $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: gt - returns true if greater than\n  {{- if .Values.testGt }}\n  gtResult: {{ gt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ge - returns true if greater than or equal\n  {{- if .Values.testGe }}\n  geResult: {{ ge $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n  {{- if .Values.testDefault }}\n  defaultResult: {{ default \"fallback\" $testData | quote }}\n  {{- end }}\n\n  # Test: required - fails if value is empty\n  {{- if .Values.testRequired }}\n  requiredResult: {{ required \"value is required\" $testData | quote }}\n  {{- end }}\n\n  # Test: empty - returns true if value is empty\n  {{- if .Values.testEmpty }}\n  emptyResult: {{ empty $testData | quote }}\n  {{- end }}\n\n  # Test: fail - explicitly fails with message\n  {{- if .Values.testFail }}\n  failResult: {{ fail (printf \"intentional fail with: %v\" $testData) }}\n  {{- end }}\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n  {{- if .Values.testCoalesce }}\n  coalesceResult: {{ coalesce $testData .Values.compareValue \"default\" | quote }}\n  {{- end }}\n\n  # Test: ternary - conditional expression\n  {{- if .Values.testTernary }}\n  ternaryResult: {{ ternary \"yes\" \"no\" $testData | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "or",
      "dataType": "listValue",
      "valuesOptions": {
        "values": [
          "testOr=true",
          "testDataType=listValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-logic-test\r\ndata:\r\n  # ==========================================\r\n  # Boolean Logic Functions\r\n  # ==========================================\r\n  # Test: and - returns boolean AND of arguments\r\n\r\n  # Test: or - returns boolean OR of arguments\r\n  orResult: \"[1 2 3]\"\r\n\r\n  # Test: not - returns boolean negation\r\n\r\n  # ==========================================\r\n  # Comparison Functions\r\n  # ==========================================\r\n  # Test: eq - returns true if equal\r\n\r\n  # Test: ne - returns true if not equal\r\n\r\n  # Test: lt - returns true if less than\r\n\r\n  # Test: le - returns true if less than or equal\r\n\r\n  # Test: gt - returns true if greater than\r\n\r\n  # Test: ge - returns true if greater than or equal\r\n\r\n  # ==========================================\r\n  # Default/Required Functions\r\n  # ==========================================\r\n  # Test: default - returns default value if empty\r\n\r\n  # Test: required - fails if value is empty\r\n\r\n  # Test: empty - returns true if value is empty\r\n\r\n  # Test: fail - explicitly fails with message\r\n\r\n  # ==========================================\r\n  # Coalesce/Ternary Functions\r\n  # ==========================================\r\n  # Test: coalesce - returns first non-empty value\r\n\r\n  # Test: ternary - conditional expression\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-logic-default/templates/logic-default-test.yaml",
            "content": "\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n\n  # Test: or - returns boolean OR of arguments\n  orResult: \"[1 2 3]\"\n\n  # Test: not - returns boolean negation\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n\n  # Test: ne - returns true if not equal\n\n  # Test: lt - returns true if less than\n\n  # Test: le - returns true if less than or equal\n\n  # Test: gt - returns true if greater than\n\n  # Test: ge - returns true if greater than or equal\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n\n  # Test: required - fails if value is empty\n\n  # Test: empty - returns true if value is empty\n\n  # Test: fail - explicitly fails with message\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n\n  # Test: ternary - conditional expression\n",
            "originalContent": "# Test: Logic and default functions - comprehensive testing\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n  {{- if .Values.testAnd }}\n  andResult: {{ and $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: or - returns boolean OR of arguments\n  {{- if .Values.testOr }}\n  orResult: {{ or $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: not - returns boolean negation\n  {{- if .Values.testNot }}\n  notResult: {{ not $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n  {{- if .Values.testEq }}\n  eqResult: {{ eq $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ne - returns true if not equal\n  {{- if .Values.testNe }}\n  neResult: {{ ne $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: lt - returns true if less than\n  {{- if .Values.testLt }}\n  ltResult: {{ lt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: le - returns true if less than or equal\n  {{- if .Values.testLe }}\n  leResult: {{ le $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: gt - returns true if greater than\n  {{- if .Values.testGt }}\n  gtResult: {{ gt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ge - returns true if greater than or equal\n  {{- if .Values.testGe }}\n  geResult: {{ ge $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n  {{- if .Values.testDefault }}\n  defaultResult: {{ default \"fallback\" $testData | quote }}\n  {{- end }}\n\n  # Test: required - fails if value is empty\n  {{- if .Values.testRequired }}\n  requiredResult: {{ required \"value is required\" $testData | quote }}\n  {{- end }}\n\n  # Test: empty - returns true if value is empty\n  {{- if .Values.testEmpty }}\n  emptyResult: {{ empty $testData | quote }}\n  {{- end }}\n\n  # Test: fail - explicitly fails with message\n  {{- if .Values.testFail }}\n  failResult: {{ fail (printf \"intentional fail with: %v\" $testData) }}\n  {{- end }}\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n  {{- if .Values.testCoalesce }}\n  coalesceResult: {{ coalesce $testData .Values.compareValue \"default\" | quote }}\n  {{- end }}\n\n  # Test: ternary - conditional expression\n  {{- if .Values.testTernary }}\n  ternaryResult: {{ ternary \"yes\" \"no\" $testData | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-logic-test\r\ndata:\r\n  # ==========================================\r\n  # Boolean Logic Functions\r\n  # ==========================================\r\n  # Test: and - returns boolean AND of arguments\r\n\r\n  # Test: or - returns boolean OR of arguments\r\n  orResult: \"[1 2 3]\"\r\n\r\n  # Test: not - returns boolean negation\r\n\r\n  # ==========================================\r\n  # Comparison Functions\r\n  # ==========================================\r\n  # Test: eq - returns true if equal\r\n\r\n  # Test: ne - returns true if not equal\r\n\r\n  # Test: lt - returns true if less than\r\n\r\n  # Test: le - returns true if less than or equal\r\n\r\n  # Test: gt - returns true if greater than\r\n\r\n  # Test: ge - returns true if greater than or equal\r\n\r\n  # ==========================================\r\n  # Default/Required Functions\r\n  # ==========================================\r\n  # Test: default - returns default value if empty\r\n\r\n  # Test: required - fails if value is empty\r\n\r\n  # Test: empty - returns true if value is empty\r\n\r\n  # Test: fail - explicitly fails with message\r\n\r\n  # ==========================================\r\n  # Coalesce/Ternary Functions\r\n  # ==========================================\r\n  # Test: coalesce - returns first non-empty value\r\n\r\n  # Test: ternary - conditional expression\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-logic-default/templates/logic-default-test.yaml",
            "content": "\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n\n  # Test: or - returns boolean OR of arguments\n  orResult: \"[1 2 3]\"\n\n  # Test: not - returns boolean negation\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n\n  # Test: ne - returns true if not equal\n\n  # Test: lt - returns true if less than\n\n  # Test: le - returns true if less than or equal\n\n  # Test: gt - returns true if greater than\n\n  # Test: ge - returns true if greater than or equal\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n\n  # Test: required - fails if value is empty\n\n  # Test: empty - returns true if value is empty\n\n  # Test: fail - explicitly fails with message\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n\n  # Test: ternary - conditional expression\n",
            "originalContent": "# Test: Logic and default functions - comprehensive testing\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n  {{- if .Values.testAnd }}\n  andResult: {{ and $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: or - returns boolean OR of arguments\n  {{- if .Values.testOr }}\n  orResult: {{ or $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: not - returns boolean negation\n  {{- if .Values.testNot }}\n  notResult: {{ not $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n  {{- if .Values.testEq }}\n  eqResult: {{ eq $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ne - returns true if not equal\n  {{- if .Values.testNe }}\n  neResult: {{ ne $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: lt - returns true if less than\n  {{- if .Values.testLt }}\n  ltResult: {{ lt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: le - returns true if less than or equal\n  {{- if .Values.testLe }}\n  leResult: {{ le $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: gt - returns true if greater than\n  {{- if .Values.testGt }}\n  gtResult: {{ gt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ge - returns true if greater than or equal\n  {{- if .Values.testGe }}\n  geResult: {{ ge $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n  {{- if .Values.testDefault }}\n  defaultResult: {{ default \"fallback\" $testData | quote }}\n  {{- end }}\n\n  # Test: required - fails if value is empty\n  {{- if .Values.testRequired }}\n  requiredResult: {{ required \"value is required\" $testData | quote }}\n  {{- end }}\n\n  # Test: empty - returns true if value is empty\n  {{- if .Values.testEmpty }}\n  emptyResult: {{ empty $testData | quote }}\n  {{- end }}\n\n  # Test: fail - explicitly fails with message\n  {{- if .Values.testFail }}\n  failResult: {{ fail (printf \"intentional fail with: %v\" $testData) }}\n  {{- end }}\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n  {{- if .Values.testCoalesce }}\n  coalesceResult: {{ coalesce $testData .Values.compareValue \"default\" | quote }}\n  {{- end }}\n\n  # Test: ternary - conditional expression\n  {{- if .Values.testTernary }}\n  ternaryResult: {{ ternary \"yes\" \"no\" $testData | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "or",
      "dataType": "dictValue",
      "valuesOptions": {
        "values": [
          "testOr=true",
          "testDataType=dictValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-logic-test\r\ndata:\r\n  # ==========================================\r\n  # Boolean Logic Functions\r\n  # ==========================================\r\n  # Test: and - returns boolean AND of arguments\r\n\r\n  # Test: or - returns boolean OR of arguments\r\n  orResult: \"map[key:value]\"\r\n\r\n  # Test: not - returns boolean negation\r\n\r\n  # ==========================================\r\n  # Comparison Functions\r\n  # ==========================================\r\n  # Test: eq - returns true if equal\r\n\r\n  # Test: ne - returns true if not equal\r\n\r\n  # Test: lt - returns true if less than\r\n\r\n  # Test: le - returns true if less than or equal\r\n\r\n  # Test: gt - returns true if greater than\r\n\r\n  # Test: ge - returns true if greater than or equal\r\n\r\n  # ==========================================\r\n  # Default/Required Functions\r\n  # ==========================================\r\n  # Test: default - returns default value if empty\r\n\r\n  # Test: required - fails if value is empty\r\n\r\n  # Test: empty - returns true if value is empty\r\n\r\n  # Test: fail - explicitly fails with message\r\n\r\n  # ==========================================\r\n  # Coalesce/Ternary Functions\r\n  # ==========================================\r\n  # Test: coalesce - returns first non-empty value\r\n\r\n  # Test: ternary - conditional expression\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-logic-default/templates/logic-default-test.yaml",
            "content": "\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n\n  # Test: or - returns boolean OR of arguments\n  orResult: \"map[key:value]\"\n\n  # Test: not - returns boolean negation\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n\n  # Test: ne - returns true if not equal\n\n  # Test: lt - returns true if less than\n\n  # Test: le - returns true if less than or equal\n\n  # Test: gt - returns true if greater than\n\n  # Test: ge - returns true if greater than or equal\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n\n  # Test: required - fails if value is empty\n\n  # Test: empty - returns true if value is empty\n\n  # Test: fail - explicitly fails with message\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n\n  # Test: ternary - conditional expression\n",
            "originalContent": "# Test: Logic and default functions - comprehensive testing\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n  {{- if .Values.testAnd }}\n  andResult: {{ and $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: or - returns boolean OR of arguments\n  {{- if .Values.testOr }}\n  orResult: {{ or $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: not - returns boolean negation\n  {{- if .Values.testNot }}\n  notResult: {{ not $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n  {{- if .Values.testEq }}\n  eqResult: {{ eq $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ne - returns true if not equal\n  {{- if .Values.testNe }}\n  neResult: {{ ne $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: lt - returns true if less than\n  {{- if .Values.testLt }}\n  ltResult: {{ lt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: le - returns true if less than or equal\n  {{- if .Values.testLe }}\n  leResult: {{ le $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: gt - returns true if greater than\n  {{- if .Values.testGt }}\n  gtResult: {{ gt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ge - returns true if greater than or equal\n  {{- if .Values.testGe }}\n  geResult: {{ ge $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n  {{- if .Values.testDefault }}\n  defaultResult: {{ default \"fallback\" $testData | quote }}\n  {{- end }}\n\n  # Test: required - fails if value is empty\n  {{- if .Values.testRequired }}\n  requiredResult: {{ required \"value is required\" $testData | quote }}\n  {{- end }}\n\n  # Test: empty - returns true if value is empty\n  {{- if .Values.testEmpty }}\n  emptyResult: {{ empty $testData | quote }}\n  {{- end }}\n\n  # Test: fail - explicitly fails with message\n  {{- if .Values.testFail }}\n  failResult: {{ fail (printf \"intentional fail with: %v\" $testData) }}\n  {{- end }}\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n  {{- if .Values.testCoalesce }}\n  coalesceResult: {{ coalesce $testData .Values.compareValue \"default\" | quote }}\n  {{- end }}\n\n  # Test: ternary - conditional expression\n  {{- if .Values.testTernary }}\n  ternaryResult: {{ ternary \"yes\" \"no\" $testData | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-logic-test\r\ndata:\r\n  # ==========================================\r\n  # Boolean Logic Functions\r\n  # ==========================================\r\n  # Test: and - returns boolean AND of arguments\r\n\r\n  # Test: or - returns boolean OR of arguments\r\n  orResult: \"map[key:value]\"\r\n\r\n  # Test: not - returns boolean negation\r\n\r\n  # ==========================================\r\n  # Comparison Functions\r\n  # ==========================================\r\n  # Test: eq - returns true if equal\r\n\r\n  # Test: ne - returns true if not equal\r\n\r\n  # Test: lt - returns true if less than\r\n\r\n  # Test: le - returns true if less than or equal\r\n\r\n  # Test: gt - returns true if greater than\r\n\r\n  # Test: ge - returns true if greater than or equal\r\n\r\n  # ==========================================\r\n  # Default/Required Functions\r\n  # ==========================================\r\n  # Test: default - returns default value if empty\r\n\r\n  # Test: required - fails if value is empty\r\n\r\n  # Test: empty - returns true if value is empty\r\n\r\n  # Test: fail - explicitly fails with message\r\n\r\n  # ==========================================\r\n  # Coalesce/Ternary Functions\r\n  # ==========================================\r\n  # Test: coalesce - returns first non-empty value\r\n\r\n  # Test: ternary - conditional expression\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-logic-default/templates/logic-default-test.yaml",
            "content": "\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n\n  # Test: or - returns boolean OR of arguments\n  orResult: \"map[key:value]\"\n\n  # Test: not - returns boolean negation\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n\n  # Test: ne - returns true if not equal\n\n  # Test: lt - returns true if less than\n\n  # Test: le - returns true if less than or equal\n\n  # Test: gt - returns true if greater than\n\n  # Test: ge - returns true if greater than or equal\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n\n  # Test: required - fails if value is empty\n\n  # Test: empty - returns true if value is empty\n\n  # Test: fail - explicitly fails with message\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n\n  # Test: ternary - conditional expression\n",
            "originalContent": "# Test: Logic and default functions - comprehensive testing\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n  {{- if .Values.testAnd }}\n  andResult: {{ and $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: or - returns boolean OR of arguments\n  {{- if .Values.testOr }}\n  orResult: {{ or $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: not - returns boolean negation\n  {{- if .Values.testNot }}\n  notResult: {{ not $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n  {{- if .Values.testEq }}\n  eqResult: {{ eq $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ne - returns true if not equal\n  {{- if .Values.testNe }}\n  neResult: {{ ne $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: lt - returns true if less than\n  {{- if .Values.testLt }}\n  ltResult: {{ lt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: le - returns true if less than or equal\n  {{- if .Values.testLe }}\n  leResult: {{ le $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: gt - returns true if greater than\n  {{- if .Values.testGt }}\n  gtResult: {{ gt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ge - returns true if greater than or equal\n  {{- if .Values.testGe }}\n  geResult: {{ ge $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n  {{- if .Values.testDefault }}\n  defaultResult: {{ default \"fallback\" $testData | quote }}\n  {{- end }}\n\n  # Test: required - fails if value is empty\n  {{- if .Values.testRequired }}\n  requiredResult: {{ required \"value is required\" $testData | quote }}\n  {{- end }}\n\n  # Test: empty - returns true if value is empty\n  {{- if .Values.testEmpty }}\n  emptyResult: {{ empty $testData | quote }}\n  {{- end }}\n\n  # Test: fail - explicitly fails with message\n  {{- if .Values.testFail }}\n  failResult: {{ fail (printf \"intentional fail with: %v\" $testData) }}\n  {{- end }}\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n  {{- if .Values.testCoalesce }}\n  coalesceResult: {{ coalesce $testData .Values.compareValue \"default\" | quote }}\n  {{- end }}\n\n  # Test: ternary - conditional expression\n  {{- if .Values.testTernary }}\n  ternaryResult: {{ ternary \"yes\" \"no\" $testData | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "not",
      "dataType": "nilValue",
      "valuesOptions": {
        "values": [
          "testNot=true",
          "testDataType=nilValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-logic-test\r\ndata:\r\n  # ==========================================\r\n  # Boolean Logic Functions\r\n  # ==========================================\r\n  # Test: and - returns boolean AND of arguments\r\n\r\n  # Test: or - returns boolean OR of arguments\r\n\r\n  # Test: not - returns boolean negation\r\n  notResult: \"true\"\r\n\r\n  # ==========================================\r\n  # Comparison Functions\r\n  # ==========================================\r\n  # Test: eq - returns true if equal\r\n\r\n  # Test: ne - returns true if not equal\r\n\r\n  # Test: lt - returns true if less than\r\n\r\n  # Test: le - returns true if less than or equal\r\n\r\n  # Test: gt - returns true if greater than\r\n\r\n  # Test: ge - returns true if greater than or equal\r\n\r\n  # ==========================================\r\n  # Default/Required Functions\r\n  # ==========================================\r\n  # Test: default - returns default value if empty\r\n\r\n  # Test: required - fails if value is empty\r\n\r\n  # Test: empty - returns true if value is empty\r\n\r\n  # Test: fail - explicitly fails with message\r\n\r\n  # ==========================================\r\n  # Coalesce/Ternary Functions\r\n  # ==========================================\r\n  # Test: coalesce - returns first non-empty value\r\n\r\n  # Test: ternary - conditional expression\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-logic-default/templates/logic-default-test.yaml",
            "content": "\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n\n  # Test: or - returns boolean OR of arguments\n\n  # Test: not - returns boolean negation\n  notResult: \"true\"\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n\n  # Test: ne - returns true if not equal\n\n  # Test: lt - returns true if less than\n\n  # Test: le - returns true if less than or equal\n\n  # Test: gt - returns true if greater than\n\n  # Test: ge - returns true if greater than or equal\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n\n  # Test: required - fails if value is empty\n\n  # Test: empty - returns true if value is empty\n\n  # Test: fail - explicitly fails with message\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n\n  # Test: ternary - conditional expression\n",
            "originalContent": "# Test: Logic and default functions - comprehensive testing\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n  {{- if .Values.testAnd }}\n  andResult: {{ and $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: or - returns boolean OR of arguments\n  {{- if .Values.testOr }}\n  orResult: {{ or $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: not - returns boolean negation\n  {{- if .Values.testNot }}\n  notResult: {{ not $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n  {{- if .Values.testEq }}\n  eqResult: {{ eq $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ne - returns true if not equal\n  {{- if .Values.testNe }}\n  neResult: {{ ne $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: lt - returns true if less than\n  {{- if .Values.testLt }}\n  ltResult: {{ lt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: le - returns true if less than or equal\n  {{- if .Values.testLe }}\n  leResult: {{ le $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: gt - returns true if greater than\n  {{- if .Values.testGt }}\n  gtResult: {{ gt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ge - returns true if greater than or equal\n  {{- if .Values.testGe }}\n  geResult: {{ ge $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n  {{- if .Values.testDefault }}\n  defaultResult: {{ default \"fallback\" $testData | quote }}\n  {{- end }}\n\n  # Test: required - fails if value is empty\n  {{- if .Values.testRequired }}\n  requiredResult: {{ required \"value is required\" $testData | quote }}\n  {{- end }}\n\n  # Test: empty - returns true if value is empty\n  {{- if .Values.testEmpty }}\n  emptyResult: {{ empty $testData | quote }}\n  {{- end }}\n\n  # Test: fail - explicitly fails with message\n  {{- if .Values.testFail }}\n  failResult: {{ fail (printf \"intentional fail with: %v\" $testData) }}\n  {{- end }}\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n  {{- if .Values.testCoalesce }}\n  coalesceResult: {{ coalesce $testData .Values.compareValue \"default\" | quote }}\n  {{- end }}\n\n  # Test: ternary - conditional expression\n  {{- if .Values.testTernary }}\n  ternaryResult: {{ ternary \"yes\" \"no\" $testData | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-logic-test\r\ndata:\r\n  # ==========================================\r\n  # Boolean Logic Functions\r\n  # ==========================================\r\n  # Test: and - returns boolean AND of arguments\r\n\r\n  # Test: or - returns boolean OR of arguments\r\n\r\n  # Test: not - returns boolean negation\r\n  notResult: \"true\"\r\n\r\n  # ==========================================\r\n  # Comparison Functions\r\n  # ==========================================\r\n  # Test: eq - returns true if equal\r\n\r\n  # Test: ne - returns true if not equal\r\n\r\n  # Test: lt - returns true if less than\r\n\r\n  # Test: le - returns true if less than or equal\r\n\r\n  # Test: gt - returns true if greater than\r\n\r\n  # Test: ge - returns true if greater than or equal\r\n\r\n  # ==========================================\r\n  # Default/Required Functions\r\n  # ==========================================\r\n  # Test: default - returns default value if empty\r\n\r\n  # Test: required - fails if value is empty\r\n\r\n  # Test: empty - returns true if value is empty\r\n\r\n  # Test: fail - explicitly fails with message\r\n\r\n  # ==========================================\r\n  # Coalesce/Ternary Functions\r\n  # ==========================================\r\n  # Test: coalesce - returns first non-empty value\r\n\r\n  # Test: ternary - conditional expression\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-logic-default/templates/logic-default-test.yaml",
            "content": "\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n\n  # Test: or - returns boolean OR of arguments\n\n  # Test: not - returns boolean negation\n  notResult: \"true\"\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n\n  # Test: ne - returns true if not equal\n\n  # Test: lt - returns true if less than\n\n  # Test: le - returns true if less than or equal\n\n  # Test: gt - returns true if greater than\n\n  # Test: ge - returns true if greater than or equal\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n\n  # Test: required - fails if value is empty\n\n  # Test: empty - returns true if value is empty\n\n  # Test: fail - explicitly fails with message\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n\n  # Test: ternary - conditional expression\n",
            "originalContent": "# Test: Logic and default functions - comprehensive testing\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n  {{- if .Values.testAnd }}\n  andResult: {{ and $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: or - returns boolean OR of arguments\n  {{- if .Values.testOr }}\n  orResult: {{ or $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: not - returns boolean negation\n  {{- if .Values.testNot }}\n  notResult: {{ not $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n  {{- if .Values.testEq }}\n  eqResult: {{ eq $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ne - returns true if not equal\n  {{- if .Values.testNe }}\n  neResult: {{ ne $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: lt - returns true if less than\n  {{- if .Values.testLt }}\n  ltResult: {{ lt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: le - returns true if less than or equal\n  {{- if .Values.testLe }}\n  leResult: {{ le $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: gt - returns true if greater than\n  {{- if .Values.testGt }}\n  gtResult: {{ gt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ge - returns true if greater than or equal\n  {{- if .Values.testGe }}\n  geResult: {{ ge $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n  {{- if .Values.testDefault }}\n  defaultResult: {{ default \"fallback\" $testData | quote }}\n  {{- end }}\n\n  # Test: required - fails if value is empty\n  {{- if .Values.testRequired }}\n  requiredResult: {{ required \"value is required\" $testData | quote }}\n  {{- end }}\n\n  # Test: empty - returns true if value is empty\n  {{- if .Values.testEmpty }}\n  emptyResult: {{ empty $testData | quote }}\n  {{- end }}\n\n  # Test: fail - explicitly fails with message\n  {{- if .Values.testFail }}\n  failResult: {{ fail (printf \"intentional fail with: %v\" $testData) }}\n  {{- end }}\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n  {{- if .Values.testCoalesce }}\n  coalesceResult: {{ coalesce $testData .Values.compareValue \"default\" | quote }}\n  {{- end }}\n\n  # Test: ternary - conditional expression\n  {{- if .Values.testTernary }}\n  ternaryResult: {{ ternary \"yes\" \"no\" $testData | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "not",
      "dataType": "stringValue",
      "valuesOptions": {
        "values": [
          "testNot=true",
          "testDataType=stringValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-logic-test\r\ndata:\r\n  # ==========================================\r\n  # Boolean Logic Functions\r\n  # ==========================================\r\n  # Test: and - returns boolean AND of arguments\r\n\r\n  # Test: or - returns boolean OR of arguments\r\n\r\n  # Test: not - returns boolean negation\r\n  notResult: \"false\"\r\n\r\n  # ==========================================\r\n  # Comparison Functions\r\n  # ==========================================\r\n  # Test: eq - returns true if equal\r\n\r\n  # Test: ne - returns true if not equal\r\n\r\n  # Test: lt - returns true if less than\r\n\r\n  # Test: le - returns true if less than or equal\r\n\r\n  # Test: gt - returns true if greater than\r\n\r\n  # Test: ge - returns true if greater than or equal\r\n\r\n  # ==========================================\r\n  # Default/Required Functions\r\n  # ==========================================\r\n  # Test: default - returns default value if empty\r\n\r\n  # Test: required - fails if value is empty\r\n\r\n  # Test: empty - returns true if value is empty\r\n\r\n  # Test: fail - explicitly fails with message\r\n\r\n  # ==========================================\r\n  # Coalesce/Ternary Functions\r\n  # ==========================================\r\n  # Test: coalesce - returns first non-empty value\r\n\r\n  # Test: ternary - conditional expression\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-logic-default/templates/logic-default-test.yaml",
            "content": "\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n\n  # Test: or - returns boolean OR of arguments\n\n  # Test: not - returns boolean negation\n  notResult: \"false\"\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n\n  # Test: ne - returns true if not equal\n\n  # Test: lt - returns true if less than\n\n  # Test: le - returns true if less than or equal\n\n  # Test: gt - returns true if greater than\n\n  # Test: ge - returns true if greater than or equal\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n\n  # Test: required - fails if value is empty\n\n  # Test: empty - returns true if value is empty\n\n  # Test: fail - explicitly fails with message\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n\n  # Test: ternary - conditional expression\n",
            "originalContent": "# Test: Logic and default functions - comprehensive testing\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n  {{- if .Values.testAnd }}\n  andResult: {{ and $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: or - returns boolean OR of arguments\n  {{- if .Values.testOr }}\n  orResult: {{ or $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: not - returns boolean negation\n  {{- if .Values.testNot }}\n  notResult: {{ not $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n  {{- if .Values.testEq }}\n  eqResult: {{ eq $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ne - returns true if not equal\n  {{- if .Values.testNe }}\n  neResult: {{ ne $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: lt - returns true if less than\n  {{- if .Values.testLt }}\n  ltResult: {{ lt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: le - returns true if less than or equal\n  {{- if .Values.testLe }}\n  leResult: {{ le $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: gt - returns true if greater than\n  {{- if .Values.testGt }}\n  gtResult: {{ gt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ge - returns true if greater than or equal\n  {{- if .Values.testGe }}\n  geResult: {{ ge $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n  {{- if .Values.testDefault }}\n  defaultResult: {{ default \"fallback\" $testData | quote }}\n  {{- end }}\n\n  # Test: required - fails if value is empty\n  {{- if .Values.testRequired }}\n  requiredResult: {{ required \"value is required\" $testData | quote }}\n  {{- end }}\n\n  # Test: empty - returns true if value is empty\n  {{- if .Values.testEmpty }}\n  emptyResult: {{ empty $testData | quote }}\n  {{- end }}\n\n  # Test: fail - explicitly fails with message\n  {{- if .Values.testFail }}\n  failResult: {{ fail (printf \"intentional fail with: %v\" $testData) }}\n  {{- end }}\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n  {{- if .Values.testCoalesce }}\n  coalesceResult: {{ coalesce $testData .Values.compareValue \"default\" | quote }}\n  {{- end }}\n\n  # Test: ternary - conditional expression\n  {{- if .Values.testTernary }}\n  ternaryResult: {{ ternary \"yes\" \"no\" $testData | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-logic-test\r\ndata:\r\n  # ==========================================\r\n  # Boolean Logic Functions\r\n  # ==========================================\r\n  # Test: and - returns boolean AND of arguments\r\n\r\n  # Test: or - returns boolean OR of arguments\r\n\r\n  # Test: not - returns boolean negation\r\n  notResult: \"false\"\r\n\r\n  # ==========================================\r\n  # Comparison Functions\r\n  # ==========================================\r\n  # Test: eq - returns true if equal\r\n\r\n  # Test: ne - returns true if not equal\r\n\r\n  # Test: lt - returns true if less than\r\n\r\n  # Test: le - returns true if less than or equal\r\n\r\n  # Test: gt - returns true if greater than\r\n\r\n  # Test: ge - returns true if greater than or equal\r\n\r\n  # ==========================================\r\n  # Default/Required Functions\r\n  # ==========================================\r\n  # Test: default - returns default value if empty\r\n\r\n  # Test: required - fails if value is empty\r\n\r\n  # Test: empty - returns true if value is empty\r\n\r\n  # Test: fail - explicitly fails with message\r\n\r\n  # ==========================================\r\n  # Coalesce/Ternary Functions\r\n  # ==========================================\r\n  # Test: coalesce - returns first non-empty value\r\n\r\n  # Test: ternary - conditional expression\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-logic-default/templates/logic-default-test.yaml",
            "content": "\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n\n  # Test: or - returns boolean OR of arguments\n\n  # Test: not - returns boolean negation\n  notResult: \"false\"\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n\n  # Test: ne - returns true if not equal\n\n  # Test: lt - returns true if less than\n\n  # Test: le - returns true if less than or equal\n\n  # Test: gt - returns true if greater than\n\n  # Test: ge - returns true if greater than or equal\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n\n  # Test: required - fails if value is empty\n\n  # Test: empty - returns true if value is empty\n\n  # Test: fail - explicitly fails with message\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n\n  # Test: ternary - conditional expression\n",
            "originalContent": "# Test: Logic and default functions - comprehensive testing\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n  {{- if .Values.testAnd }}\n  andResult: {{ and $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: or - returns boolean OR of arguments\n  {{- if .Values.testOr }}\n  orResult: {{ or $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: not - returns boolean negation\n  {{- if .Values.testNot }}\n  notResult: {{ not $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n  {{- if .Values.testEq }}\n  eqResult: {{ eq $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ne - returns true if not equal\n  {{- if .Values.testNe }}\n  neResult: {{ ne $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: lt - returns true if less than\n  {{- if .Values.testLt }}\n  ltResult: {{ lt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: le - returns true if less than or equal\n  {{- if .Values.testLe }}\n  leResult: {{ le $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: gt - returns true if greater than\n  {{- if .Values.testGt }}\n  gtResult: {{ gt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ge - returns true if greater than or equal\n  {{- if .Values.testGe }}\n  geResult: {{ ge $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n  {{- if .Values.testDefault }}\n  defaultResult: {{ default \"fallback\" $testData | quote }}\n  {{- end }}\n\n  # Test: required - fails if value is empty\n  {{- if .Values.testRequired }}\n  requiredResult: {{ required \"value is required\" $testData | quote }}\n  {{- end }}\n\n  # Test: empty - returns true if value is empty\n  {{- if .Values.testEmpty }}\n  emptyResult: {{ empty $testData | quote }}\n  {{- end }}\n\n  # Test: fail - explicitly fails with message\n  {{- if .Values.testFail }}\n  failResult: {{ fail (printf \"intentional fail with: %v\" $testData) }}\n  {{- end }}\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n  {{- if .Values.testCoalesce }}\n  coalesceResult: {{ coalesce $testData .Values.compareValue \"default\" | quote }}\n  {{- end }}\n\n  # Test: ternary - conditional expression\n  {{- if .Values.testTernary }}\n  ternaryResult: {{ ternary \"yes\" \"no\" $testData | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "not",
      "dataType": "numberValue",
      "valuesOptions": {
        "values": [
          "testNot=true",
          "testDataType=numberValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-logic-test\r\ndata:\r\n  # ==========================================\r\n  # Boolean Logic Functions\r\n  # ==========================================\r\n  # Test: and - returns boolean AND of arguments\r\n\r\n  # Test: or - returns boolean OR of arguments\r\n\r\n  # Test: not - returns boolean negation\r\n  notResult: \"false\"\r\n\r\n  # ==========================================\r\n  # Comparison Functions\r\n  # ==========================================\r\n  # Test: eq - returns true if equal\r\n\r\n  # Test: ne - returns true if not equal\r\n\r\n  # Test: lt - returns true if less than\r\n\r\n  # Test: le - returns true if less than or equal\r\n\r\n  # Test: gt - returns true if greater than\r\n\r\n  # Test: ge - returns true if greater than or equal\r\n\r\n  # ==========================================\r\n  # Default/Required Functions\r\n  # ==========================================\r\n  # Test: default - returns default value if empty\r\n\r\n  # Test: required - fails if value is empty\r\n\r\n  # Test: empty - returns true if value is empty\r\n\r\n  # Test: fail - explicitly fails with message\r\n\r\n  # ==========================================\r\n  # Coalesce/Ternary Functions\r\n  # ==========================================\r\n  # Test: coalesce - returns first non-empty value\r\n\r\n  # Test: ternary - conditional expression\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-logic-default/templates/logic-default-test.yaml",
            "content": "\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n\n  # Test: or - returns boolean OR of arguments\n\n  # Test: not - returns boolean negation\n  notResult: \"false\"\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n\n  # Test: ne - returns true if not equal\n\n  # Test: lt - returns true if less than\n\n  # Test: le - returns true if less than or equal\n\n  # Test: gt - returns true if greater than\n\n  # Test: ge - returns true if greater than or equal\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n\n  # Test: required - fails if value is empty\n\n  # Test: empty - returns true if value is empty\n\n  # Test: fail - explicitly fails with message\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n\n  # Test: ternary - conditional expression\n",
            "originalContent": "# Test: Logic and default functions - comprehensive testing\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n  {{- if .Values.testAnd }}\n  andResult: {{ and $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: or - returns boolean OR of arguments\n  {{- if .Values.testOr }}\n  orResult: {{ or $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: not - returns boolean negation\n  {{- if .Values.testNot }}\n  notResult: {{ not $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n  {{- if .Values.testEq }}\n  eqResult: {{ eq $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ne - returns true if not equal\n  {{- if .Values.testNe }}\n  neResult: {{ ne $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: lt - returns true if less than\n  {{- if .Values.testLt }}\n  ltResult: {{ lt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: le - returns true if less than or equal\n  {{- if .Values.testLe }}\n  leResult: {{ le $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: gt - returns true if greater than\n  {{- if .Values.testGt }}\n  gtResult: {{ gt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ge - returns true if greater than or equal\n  {{- if .Values.testGe }}\n  geResult: {{ ge $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n  {{- if .Values.testDefault }}\n  defaultResult: {{ default \"fallback\" $testData | quote }}\n  {{- end }}\n\n  # Test: required - fails if value is empty\n  {{- if .Values.testRequired }}\n  requiredResult: {{ required \"value is required\" $testData | quote }}\n  {{- end }}\n\n  # Test: empty - returns true if value is empty\n  {{- if .Values.testEmpty }}\n  emptyResult: {{ empty $testData | quote }}\n  {{- end }}\n\n  # Test: fail - explicitly fails with message\n  {{- if .Values.testFail }}\n  failResult: {{ fail (printf \"intentional fail with: %v\" $testData) }}\n  {{- end }}\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n  {{- if .Values.testCoalesce }}\n  coalesceResult: {{ coalesce $testData .Values.compareValue \"default\" | quote }}\n  {{- end }}\n\n  # Test: ternary - conditional expression\n  {{- if .Values.testTernary }}\n  ternaryResult: {{ ternary \"yes\" \"no\" $testData | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-logic-test\r\ndata:\r\n  # ==========================================\r\n  # Boolean Logic Functions\r\n  # ==========================================\r\n  # Test: and - returns boolean AND of arguments\r\n\r\n  # Test: or - returns boolean OR of arguments\r\n\r\n  # Test: not - returns boolean negation\r\n  notResult: \"false\"\r\n\r\n  # ==========================================\r\n  # Comparison Functions\r\n  # ==========================================\r\n  # Test: eq - returns true if equal\r\n\r\n  # Test: ne - returns true if not equal\r\n\r\n  # Test: lt - returns true if less than\r\n\r\n  # Test: le - returns true if less than or equal\r\n\r\n  # Test: gt - returns true if greater than\r\n\r\n  # Test: ge - returns true if greater than or equal\r\n\r\n  # ==========================================\r\n  # Default/Required Functions\r\n  # ==========================================\r\n  # Test: default - returns default value if empty\r\n\r\n  # Test: required - fails if value is empty\r\n\r\n  # Test: empty - returns true if value is empty\r\n\r\n  # Test: fail - explicitly fails with message\r\n\r\n  # ==========================================\r\n  # Coalesce/Ternary Functions\r\n  # ==========================================\r\n  # Test: coalesce - returns first non-empty value\r\n\r\n  # Test: ternary - conditional expression\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-logic-default/templates/logic-default-test.yaml",
            "content": "\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n\n  # Test: or - returns boolean OR of arguments\n\n  # Test: not - returns boolean negation\n  notResult: \"false\"\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n\n  # Test: ne - returns true if not equal\n\n  # Test: lt - returns true if less than\n\n  # Test: le - returns true if less than or equal\n\n  # Test: gt - returns true if greater than\n\n  # Test: ge - returns true if greater than or equal\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n\n  # Test: required - fails if value is empty\n\n  # Test: empty - returns true if value is empty\n\n  # Test: fail - explicitly fails with message\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n\n  # Test: ternary - conditional expression\n",
            "originalContent": "# Test: Logic and default functions - comprehensive testing\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n  {{- if .Values.testAnd }}\n  andResult: {{ and $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: or - returns boolean OR of arguments\n  {{- if .Values.testOr }}\n  orResult: {{ or $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: not - returns boolean negation\n  {{- if .Values.testNot }}\n  notResult: {{ not $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n  {{- if .Values.testEq }}\n  eqResult: {{ eq $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ne - returns true if not equal\n  {{- if .Values.testNe }}\n  neResult: {{ ne $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: lt - returns true if less than\n  {{- if .Values.testLt }}\n  ltResult: {{ lt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: le - returns true if less than or equal\n  {{- if .Values.testLe }}\n  leResult: {{ le $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: gt - returns true if greater than\n  {{- if .Values.testGt }}\n  gtResult: {{ gt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ge - returns true if greater than or equal\n  {{- if .Values.testGe }}\n  geResult: {{ ge $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n  {{- if .Values.testDefault }}\n  defaultResult: {{ default \"fallback\" $testData | quote }}\n  {{- end }}\n\n  # Test: required - fails if value is empty\n  {{- if .Values.testRequired }}\n  requiredResult: {{ required \"value is required\" $testData | quote }}\n  {{- end }}\n\n  # Test: empty - returns true if value is empty\n  {{- if .Values.testEmpty }}\n  emptyResult: {{ empty $testData | quote }}\n  {{- end }}\n\n  # Test: fail - explicitly fails with message\n  {{- if .Values.testFail }}\n  failResult: {{ fail (printf \"intentional fail with: %v\" $testData) }}\n  {{- end }}\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n  {{- if .Values.testCoalesce }}\n  coalesceResult: {{ coalesce $testData .Values.compareValue \"default\" | quote }}\n  {{- end }}\n\n  # Test: ternary - conditional expression\n  {{- if .Values.testTernary }}\n  ternaryResult: {{ ternary \"yes\" \"no\" $testData | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "not",
      "dataType": "boolValue",
      "valuesOptions": {
        "values": [
          "testNot=true",
          "testDataType=boolValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-logic-test\r\ndata:\r\n  # ==========================================\r\n  # Boolean Logic Functions\r\n  # ==========================================\r\n  # Test: and - returns boolean AND of arguments\r\n\r\n  # Test: or - returns boolean OR of arguments\r\n\r\n  # Test: not - returns boolean negation\r\n  notResult: \"false\"\r\n\r\n  # ==========================================\r\n  # Comparison Functions\r\n  # ==========================================\r\n  # Test: eq - returns true if equal\r\n\r\n  # Test: ne - returns true if not equal\r\n\r\n  # Test: lt - returns true if less than\r\n\r\n  # Test: le - returns true if less than or equal\r\n\r\n  # Test: gt - returns true if greater than\r\n\r\n  # Test: ge - returns true if greater than or equal\r\n\r\n  # ==========================================\r\n  # Default/Required Functions\r\n  # ==========================================\r\n  # Test: default - returns default value if empty\r\n\r\n  # Test: required - fails if value is empty\r\n\r\n  # Test: empty - returns true if value is empty\r\n\r\n  # Test: fail - explicitly fails with message\r\n\r\n  # ==========================================\r\n  # Coalesce/Ternary Functions\r\n  # ==========================================\r\n  # Test: coalesce - returns first non-empty value\r\n\r\n  # Test: ternary - conditional expression\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-logic-default/templates/logic-default-test.yaml",
            "content": "\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n\n  # Test: or - returns boolean OR of arguments\n\n  # Test: not - returns boolean negation\n  notResult: \"false\"\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n\n  # Test: ne - returns true if not equal\n\n  # Test: lt - returns true if less than\n\n  # Test: le - returns true if less than or equal\n\n  # Test: gt - returns true if greater than\n\n  # Test: ge - returns true if greater than or equal\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n\n  # Test: required - fails if value is empty\n\n  # Test: empty - returns true if value is empty\n\n  # Test: fail - explicitly fails with message\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n\n  # Test: ternary - conditional expression\n",
            "originalContent": "# Test: Logic and default functions - comprehensive testing\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n  {{- if .Values.testAnd }}\n  andResult: {{ and $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: or - returns boolean OR of arguments\n  {{- if .Values.testOr }}\n  orResult: {{ or $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: not - returns boolean negation\n  {{- if .Values.testNot }}\n  notResult: {{ not $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n  {{- if .Values.testEq }}\n  eqResult: {{ eq $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ne - returns true if not equal\n  {{- if .Values.testNe }}\n  neResult: {{ ne $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: lt - returns true if less than\n  {{- if .Values.testLt }}\n  ltResult: {{ lt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: le - returns true if less than or equal\n  {{- if .Values.testLe }}\n  leResult: {{ le $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: gt - returns true if greater than\n  {{- if .Values.testGt }}\n  gtResult: {{ gt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ge - returns true if greater than or equal\n  {{- if .Values.testGe }}\n  geResult: {{ ge $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n  {{- if .Values.testDefault }}\n  defaultResult: {{ default \"fallback\" $testData | quote }}\n  {{- end }}\n\n  # Test: required - fails if value is empty\n  {{- if .Values.testRequired }}\n  requiredResult: {{ required \"value is required\" $testData | quote }}\n  {{- end }}\n\n  # Test: empty - returns true if value is empty\n  {{- if .Values.testEmpty }}\n  emptyResult: {{ empty $testData | quote }}\n  {{- end }}\n\n  # Test: fail - explicitly fails with message\n  {{- if .Values.testFail }}\n  failResult: {{ fail (printf \"intentional fail with: %v\" $testData) }}\n  {{- end }}\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n  {{- if .Values.testCoalesce }}\n  coalesceResult: {{ coalesce $testData .Values.compareValue \"default\" | quote }}\n  {{- end }}\n\n  # Test: ternary - conditional expression\n  {{- if .Values.testTernary }}\n  ternaryResult: {{ ternary \"yes\" \"no\" $testData | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-logic-test\r\ndata:\r\n  # ==========================================\r\n  # Boolean Logic Functions\r\n  # ==========================================\r\n  # Test: and - returns boolean AND of arguments\r\n\r\n  # Test: or - returns boolean OR of arguments\r\n\r\n  # Test: not - returns boolean negation\r\n  notResult: \"false\"\r\n\r\n  # ==========================================\r\n  # Comparison Functions\r\n  # ==========================================\r\n  # Test: eq - returns true if equal\r\n\r\n  # Test: ne - returns true if not equal\r\n\r\n  # Test: lt - returns true if less than\r\n\r\n  # Test: le - returns true if less than or equal\r\n\r\n  # Test: gt - returns true if greater than\r\n\r\n  # Test: ge - returns true if greater than or equal\r\n\r\n  # ==========================================\r\n  # Default/Required Functions\r\n  # ==========================================\r\n  # Test: default - returns default value if empty\r\n\r\n  # Test: required - fails if value is empty\r\n\r\n  # Test: empty - returns true if value is empty\r\n\r\n  # Test: fail - explicitly fails with message\r\n\r\n  # ==========================================\r\n  # Coalesce/Ternary Functions\r\n  # ==========================================\r\n  # Test: coalesce - returns first non-empty value\r\n\r\n  # Test: ternary - conditional expression\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-logic-default/templates/logic-default-test.yaml",
            "content": "\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n\n  # Test: or - returns boolean OR of arguments\n\n  # Test: not - returns boolean negation\n  notResult: \"false\"\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n\n  # Test: ne - returns true if not equal\n\n  # Test: lt - returns true if less than\n\n  # Test: le - returns true if less than or equal\n\n  # Test: gt - returns true if greater than\n\n  # Test: ge - returns true if greater than or equal\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n\n  # Test: required - fails if value is empty\n\n  # Test: empty - returns true if value is empty\n\n  # Test: fail - explicitly fails with message\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n\n  # Test: ternary - conditional expression\n",
            "originalContent": "# Test: Logic and default functions - comprehensive testing\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n  {{- if .Values.testAnd }}\n  andResult: {{ and $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: or - returns boolean OR of arguments\n  {{- if .Values.testOr }}\n  orResult: {{ or $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: not - returns boolean negation\n  {{- if .Values.testNot }}\n  notResult: {{ not $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n  {{- if .Values.testEq }}\n  eqResult: {{ eq $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ne - returns true if not equal\n  {{- if .Values.testNe }}\n  neResult: {{ ne $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: lt - returns true if less than\n  {{- if .Values.testLt }}\n  ltResult: {{ lt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: le - returns true if less than or equal\n  {{- if .Values.testLe }}\n  leResult: {{ le $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: gt - returns true if greater than\n  {{- if .Values.testGt }}\n  gtResult: {{ gt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ge - returns true if greater than or equal\n  {{- if .Values.testGe }}\n  geResult: {{ ge $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n  {{- if .Values.testDefault }}\n  defaultResult: {{ default \"fallback\" $testData | quote }}\n  {{- end }}\n\n  # Test: required - fails if value is empty\n  {{- if .Values.testRequired }}\n  requiredResult: {{ required \"value is required\" $testData | quote }}\n  {{- end }}\n\n  # Test: empty - returns true if value is empty\n  {{- if .Values.testEmpty }}\n  emptyResult: {{ empty $testData | quote }}\n  {{- end }}\n\n  # Test: fail - explicitly fails with message\n  {{- if .Values.testFail }}\n  failResult: {{ fail (printf \"intentional fail with: %v\" $testData) }}\n  {{- end }}\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n  {{- if .Values.testCoalesce }}\n  coalesceResult: {{ coalesce $testData .Values.compareValue \"default\" | quote }}\n  {{- end }}\n\n  # Test: ternary - conditional expression\n  {{- if .Values.testTernary }}\n  ternaryResult: {{ ternary \"yes\" \"no\" $testData | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "not",
      "dataType": "listValue",
      "valuesOptions": {
        "values": [
          "testNot=true",
          "testDataType=listValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-logic-test\r\ndata:\r\n  # ==========================================\r\n  # Boolean Logic Functions\r\n  # ==========================================\r\n  # Test: and - returns boolean AND of arguments\r\n\r\n  # Test: or - returns boolean OR of arguments\r\n\r\n  # Test: not - returns boolean negation\r\n  notResult: \"false\"\r\n\r\n  # ==========================================\r\n  # Comparison Functions\r\n  # ==========================================\r\n  # Test: eq - returns true if equal\r\n\r\n  # Test: ne - returns true if not equal\r\n\r\n  # Test: lt - returns true if less than\r\n\r\n  # Test: le - returns true if less than or equal\r\n\r\n  # Test: gt - returns true if greater than\r\n\r\n  # Test: ge - returns true if greater than or equal\r\n\r\n  # ==========================================\r\n  # Default/Required Functions\r\n  # ==========================================\r\n  # Test: default - returns default value if empty\r\n\r\n  # Test: required - fails if value is empty\r\n\r\n  # Test: empty - returns true if value is empty\r\n\r\n  # Test: fail - explicitly fails with message\r\n\r\n  # ==========================================\r\n  # Coalesce/Ternary Functions\r\n  # ==========================================\r\n  # Test: coalesce - returns first non-empty value\r\n\r\n  # Test: ternary - conditional expression\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-logic-default/templates/logic-default-test.yaml",
            "content": "\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n\n  # Test: or - returns boolean OR of arguments\n\n  # Test: not - returns boolean negation\n  notResult: \"false\"\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n\n  # Test: ne - returns true if not equal\n\n  # Test: lt - returns true if less than\n\n  # Test: le - returns true if less than or equal\n\n  # Test: gt - returns true if greater than\n\n  # Test: ge - returns true if greater than or equal\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n\n  # Test: required - fails if value is empty\n\n  # Test: empty - returns true if value is empty\n\n  # Test: fail - explicitly fails with message\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n\n  # Test: ternary - conditional expression\n",
            "originalContent": "# Test: Logic and default functions - comprehensive testing\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n  {{- if .Values.testAnd }}\n  andResult: {{ and $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: or - returns boolean OR of arguments\n  {{- if .Values.testOr }}\n  orResult: {{ or $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: not - returns boolean negation\n  {{- if .Values.testNot }}\n  notResult: {{ not $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n  {{- if .Values.testEq }}\n  eqResult: {{ eq $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ne - returns true if not equal\n  {{- if .Values.testNe }}\n  neResult: {{ ne $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: lt - returns true if less than\n  {{- if .Values.testLt }}\n  ltResult: {{ lt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: le - returns true if less than or equal\n  {{- if .Values.testLe }}\n  leResult: {{ le $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: gt - returns true if greater than\n  {{- if .Values.testGt }}\n  gtResult: {{ gt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ge - returns true if greater than or equal\n  {{- if .Values.testGe }}\n  geResult: {{ ge $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n  {{- if .Values.testDefault }}\n  defaultResult: {{ default \"fallback\" $testData | quote }}\n  {{- end }}\n\n  # Test: required - fails if value is empty\n  {{- if .Values.testRequired }}\n  requiredResult: {{ required \"value is required\" $testData | quote }}\n  {{- end }}\n\n  # Test: empty - returns true if value is empty\n  {{- if .Values.testEmpty }}\n  emptyResult: {{ empty $testData | quote }}\n  {{- end }}\n\n  # Test: fail - explicitly fails with message\n  {{- if .Values.testFail }}\n  failResult: {{ fail (printf \"intentional fail with: %v\" $testData) }}\n  {{- end }}\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n  {{- if .Values.testCoalesce }}\n  coalesceResult: {{ coalesce $testData .Values.compareValue \"default\" | quote }}\n  {{- end }}\n\n  # Test: ternary - conditional expression\n  {{- if .Values.testTernary }}\n  ternaryResult: {{ ternary \"yes\" \"no\" $testData | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-logic-test\r\ndata:\r\n  # ==========================================\r\n  # Boolean Logic Functions\r\n  # ==========================================\r\n  # Test: and - returns boolean AND of arguments\r\n\r\n  # Test: or - returns boolean OR of arguments\r\n\r\n  # Test: not - returns boolean negation\r\n  notResult: \"false\"\r\n\r\n  # ==========================================\r\n  # Comparison Functions\r\n  # ==========================================\r\n  # Test: eq - returns true if equal\r\n\r\n  # Test: ne - returns true if not equal\r\n\r\n  # Test: lt - returns true if less than\r\n\r\n  # Test: le - returns true if less than or equal\r\n\r\n  # Test: gt - returns true if greater than\r\n\r\n  # Test: ge - returns true if greater than or equal\r\n\r\n  # ==========================================\r\n  # Default/Required Functions\r\n  # ==========================================\r\n  # Test: default - returns default value if empty\r\n\r\n  # Test: required - fails if value is empty\r\n\r\n  # Test: empty - returns true if value is empty\r\n\r\n  # Test: fail - explicitly fails with message\r\n\r\n  # ==========================================\r\n  # Coalesce/Ternary Functions\r\n  # ==========================================\r\n  # Test: coalesce - returns first non-empty value\r\n\r\n  # Test: ternary - conditional expression\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-logic-default/templates/logic-default-test.yaml",
            "content": "\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n\n  # Test: or - returns boolean OR of arguments\n\n  # Test: not - returns boolean negation\n  notResult: \"false\"\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n\n  # Test: ne - returns true if not equal\n\n  # Test: lt - returns true if less than\n\n  # Test: le - returns true if less than or equal\n\n  # Test: gt - returns true if greater than\n\n  # Test: ge - returns true if greater than or equal\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n\n  # Test: required - fails if value is empty\n\n  # Test: empty - returns true if value is empty\n\n  # Test: fail - explicitly fails with message\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n\n  # Test: ternary - conditional expression\n",
            "originalContent": "# Test: Logic and default functions - comprehensive testing\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n  {{- if .Values.testAnd }}\n  andResult: {{ and $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: or - returns boolean OR of arguments\n  {{- if .Values.testOr }}\n  orResult: {{ or $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: not - returns boolean negation\n  {{- if .Values.testNot }}\n  notResult: {{ not $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n  {{- if .Values.testEq }}\n  eqResult: {{ eq $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ne - returns true if not equal\n  {{- if .Values.testNe }}\n  neResult: {{ ne $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: lt - returns true if less than\n  {{- if .Values.testLt }}\n  ltResult: {{ lt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: le - returns true if less than or equal\n  {{- if .Values.testLe }}\n  leResult: {{ le $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: gt - returns true if greater than\n  {{- if .Values.testGt }}\n  gtResult: {{ gt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ge - returns true if greater than or equal\n  {{- if .Values.testGe }}\n  geResult: {{ ge $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n  {{- if .Values.testDefault }}\n  defaultResult: {{ default \"fallback\" $testData | quote }}\n  {{- end }}\n\n  # Test: required - fails if value is empty\n  {{- if .Values.testRequired }}\n  requiredResult: {{ required \"value is required\" $testData | quote }}\n  {{- end }}\n\n  # Test: empty - returns true if value is empty\n  {{- if .Values.testEmpty }}\n  emptyResult: {{ empty $testData | quote }}\n  {{- end }}\n\n  # Test: fail - explicitly fails with message\n  {{- if .Values.testFail }}\n  failResult: {{ fail (printf \"intentional fail with: %v\" $testData) }}\n  {{- end }}\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n  {{- if .Values.testCoalesce }}\n  coalesceResult: {{ coalesce $testData .Values.compareValue \"default\" | quote }}\n  {{- end }}\n\n  # Test: ternary - conditional expression\n  {{- if .Values.testTernary }}\n  ternaryResult: {{ ternary \"yes\" \"no\" $testData | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "not",
      "dataType": "dictValue",
      "valuesOptions": {
        "values": [
          "testNot=true",
          "testDataType=dictValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-logic-test\r\ndata:\r\n  # ==========================================\r\n  # Boolean Logic Functions\r\n  # ==========================================\r\n  # Test: and - returns boolean AND of arguments\r\n\r\n  # Test: or - returns boolean OR of arguments\r\n\r\n  # Test: not - returns boolean negation\r\n  notResult: \"false\"\r\n\r\n  # ==========================================\r\n  # Comparison Functions\r\n  # ==========================================\r\n  # Test: eq - returns true if equal\r\n\r\n  # Test: ne - returns true if not equal\r\n\r\n  # Test: lt - returns true if less than\r\n\r\n  # Test: le - returns true if less than or equal\r\n\r\n  # Test: gt - returns true if greater than\r\n\r\n  # Test: ge - returns true if greater than or equal\r\n\r\n  # ==========================================\r\n  # Default/Required Functions\r\n  # ==========================================\r\n  # Test: default - returns default value if empty\r\n\r\n  # Test: required - fails if value is empty\r\n\r\n  # Test: empty - returns true if value is empty\r\n\r\n  # Test: fail - explicitly fails with message\r\n\r\n  # ==========================================\r\n  # Coalesce/Ternary Functions\r\n  # ==========================================\r\n  # Test: coalesce - returns first non-empty value\r\n\r\n  # Test: ternary - conditional expression\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-logic-default/templates/logic-default-test.yaml",
            "content": "\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n\n  # Test: or - returns boolean OR of arguments\n\n  # Test: not - returns boolean negation\n  notResult: \"false\"\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n\n  # Test: ne - returns true if not equal\n\n  # Test: lt - returns true if less than\n\n  # Test: le - returns true if less than or equal\n\n  # Test: gt - returns true if greater than\n\n  # Test: ge - returns true if greater than or equal\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n\n  # Test: required - fails if value is empty\n\n  # Test: empty - returns true if value is empty\n\n  # Test: fail - explicitly fails with message\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n\n  # Test: ternary - conditional expression\n",
            "originalContent": "# Test: Logic and default functions - comprehensive testing\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n  {{- if .Values.testAnd }}\n  andResult: {{ and $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: or - returns boolean OR of arguments\n  {{- if .Values.testOr }}\n  orResult: {{ or $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: not - returns boolean negation\n  {{- if .Values.testNot }}\n  notResult: {{ not $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n  {{- if .Values.testEq }}\n  eqResult: {{ eq $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ne - returns true if not equal\n  {{- if .Values.testNe }}\n  neResult: {{ ne $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: lt - returns true if less than\n  {{- if .Values.testLt }}\n  ltResult: {{ lt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: le - returns true if less than or equal\n  {{- if .Values.testLe }}\n  leResult: {{ le $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: gt - returns true if greater than\n  {{- if .Values.testGt }}\n  gtResult: {{ gt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ge - returns true if greater than or equal\n  {{- if .Values.testGe }}\n  geResult: {{ ge $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n  {{- if .Values.testDefault }}\n  defaultResult: {{ default \"fallback\" $testData | quote }}\n  {{- end }}\n\n  # Test: required - fails if value is empty\n  {{- if .Values.testRequired }}\n  requiredResult: {{ required \"value is required\" $testData | quote }}\n  {{- end }}\n\n  # Test: empty - returns true if value is empty\n  {{- if .Values.testEmpty }}\n  emptyResult: {{ empty $testData | quote }}\n  {{- end }}\n\n  # Test: fail - explicitly fails with message\n  {{- if .Values.testFail }}\n  failResult: {{ fail (printf \"intentional fail with: %v\" $testData) }}\n  {{- end }}\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n  {{- if .Values.testCoalesce }}\n  coalesceResult: {{ coalesce $testData .Values.compareValue \"default\" | quote }}\n  {{- end }}\n\n  # Test: ternary - conditional expression\n  {{- if .Values.testTernary }}\n  ternaryResult: {{ ternary \"yes\" \"no\" $testData | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-logic-test\r\ndata:\r\n  # ==========================================\r\n  # Boolean Logic Functions\r\n  # ==========================================\r\n  # Test: and - returns boolean AND of arguments\r\n\r\n  # Test: or - returns boolean OR of arguments\r\n\r\n  # Test: not - returns boolean negation\r\n  notResult: \"false\"\r\n\r\n  # ==========================================\r\n  # Comparison Functions\r\n  # ==========================================\r\n  # Test: eq - returns true if equal\r\n\r\n  # Test: ne - returns true if not equal\r\n\r\n  # Test: lt - returns true if less than\r\n\r\n  # Test: le - returns true if less than or equal\r\n\r\n  # Test: gt - returns true if greater than\r\n\r\n  # Test: ge - returns true if greater than or equal\r\n\r\n  # ==========================================\r\n  # Default/Required Functions\r\n  # ==========================================\r\n  # Test: default - returns default value if empty\r\n\r\n  # Test: required - fails if value is empty\r\n\r\n  # Test: empty - returns true if value is empty\r\n\r\n  # Test: fail - explicitly fails with message\r\n\r\n  # ==========================================\r\n  # Coalesce/Ternary Functions\r\n  # ==========================================\r\n  # Test: coalesce - returns first non-empty value\r\n\r\n  # Test: ternary - conditional expression\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-logic-default/templates/logic-default-test.yaml",
            "content": "\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n\n  # Test: or - returns boolean OR of arguments\n\n  # Test: not - returns boolean negation\n  notResult: \"false\"\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n\n  # Test: ne - returns true if not equal\n\n  # Test: lt - returns true if less than\n\n  # Test: le - returns true if less than or equal\n\n  # Test: gt - returns true if greater than\n\n  # Test: ge - returns true if greater than or equal\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n\n  # Test: required - fails if value is empty\n\n  # Test: empty - returns true if value is empty\n\n  # Test: fail - explicitly fails with message\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n\n  # Test: ternary - conditional expression\n",
            "originalContent": "# Test: Logic and default functions - comprehensive testing\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n  {{- if .Values.testAnd }}\n  andResult: {{ and $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: or - returns boolean OR of arguments\n  {{- if .Values.testOr }}\n  orResult: {{ or $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: not - returns boolean negation\n  {{- if .Values.testNot }}\n  notResult: {{ not $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n  {{- if .Values.testEq }}\n  eqResult: {{ eq $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ne - returns true if not equal\n  {{- if .Values.testNe }}\n  neResult: {{ ne $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: lt - returns true if less than\n  {{- if .Values.testLt }}\n  ltResult: {{ lt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: le - returns true if less than or equal\n  {{- if .Values.testLe }}\n  leResult: {{ le $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: gt - returns true if greater than\n  {{- if .Values.testGt }}\n  gtResult: {{ gt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ge - returns true if greater than or equal\n  {{- if .Values.testGe }}\n  geResult: {{ ge $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n  {{- if .Values.testDefault }}\n  defaultResult: {{ default \"fallback\" $testData | quote }}\n  {{- end }}\n\n  # Test: required - fails if value is empty\n  {{- if .Values.testRequired }}\n  requiredResult: {{ required \"value is required\" $testData | quote }}\n  {{- end }}\n\n  # Test: empty - returns true if value is empty\n  {{- if .Values.testEmpty }}\n  emptyResult: {{ empty $testData | quote }}\n  {{- end }}\n\n  # Test: fail - explicitly fails with message\n  {{- if .Values.testFail }}\n  failResult: {{ fail (printf \"intentional fail with: %v\" $testData) }}\n  {{- end }}\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n  {{- if .Values.testCoalesce }}\n  coalesceResult: {{ coalesce $testData .Values.compareValue \"default\" | quote }}\n  {{- end }}\n\n  # Test: ternary - conditional expression\n  {{- if .Values.testTernary }}\n  ternaryResult: {{ ternary \"yes\" \"no\" $testData | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "eq",
      "dataType": "nilValue",
      "valuesOptions": {
        "values": [
          "testEq=true",
          "testDataType=nilValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-logic-test\r\ndata:\r\n  # ==========================================\r\n  # Boolean Logic Functions\r\n  # ==========================================\r\n  # Test: and - returns boolean AND of arguments\r\n\r\n  # Test: or - returns boolean OR of arguments\r\n\r\n  # Test: not - returns boolean negation\r\n\r\n  # ==========================================\r\n  # Comparison Functions\r\n  # ==========================================\r\n  # Test: eq - returns true if equal\r\n  eqResult: \"false\"\r\n\r\n  # Test: ne - returns true if not equal\r\n\r\n  # Test: lt - returns true if less than\r\n\r\n  # Test: le - returns true if less than or equal\r\n\r\n  # Test: gt - returns true if greater than\r\n\r\n  # Test: ge - returns true if greater than or equal\r\n\r\n  # ==========================================\r\n  # Default/Required Functions\r\n  # ==========================================\r\n  # Test: default - returns default value if empty\r\n\r\n  # Test: required - fails if value is empty\r\n\r\n  # Test: empty - returns true if value is empty\r\n\r\n  # Test: fail - explicitly fails with message\r\n\r\n  # ==========================================\r\n  # Coalesce/Ternary Functions\r\n  # ==========================================\r\n  # Test: coalesce - returns first non-empty value\r\n\r\n  # Test: ternary - conditional expression\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-logic-default/templates/logic-default-test.yaml",
            "content": "\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n\n  # Test: or - returns boolean OR of arguments\n\n  # Test: not - returns boolean negation\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n  eqResult: \"false\"\n\n  # Test: ne - returns true if not equal\n\n  # Test: lt - returns true if less than\n\n  # Test: le - returns true if less than or equal\n\n  # Test: gt - returns true if greater than\n\n  # Test: ge - returns true if greater than or equal\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n\n  # Test: required - fails if value is empty\n\n  # Test: empty - returns true if value is empty\n\n  # Test: fail - explicitly fails with message\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n\n  # Test: ternary - conditional expression\n",
            "originalContent": "# Test: Logic and default functions - comprehensive testing\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n  {{- if .Values.testAnd }}\n  andResult: {{ and $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: or - returns boolean OR of arguments\n  {{- if .Values.testOr }}\n  orResult: {{ or $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: not - returns boolean negation\n  {{- if .Values.testNot }}\n  notResult: {{ not $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n  {{- if .Values.testEq }}\n  eqResult: {{ eq $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ne - returns true if not equal\n  {{- if .Values.testNe }}\n  neResult: {{ ne $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: lt - returns true if less than\n  {{- if .Values.testLt }}\n  ltResult: {{ lt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: le - returns true if less than or equal\n  {{- if .Values.testLe }}\n  leResult: {{ le $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: gt - returns true if greater than\n  {{- if .Values.testGt }}\n  gtResult: {{ gt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ge - returns true if greater than or equal\n  {{- if .Values.testGe }}\n  geResult: {{ ge $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n  {{- if .Values.testDefault }}\n  defaultResult: {{ default \"fallback\" $testData | quote }}\n  {{- end }}\n\n  # Test: required - fails if value is empty\n  {{- if .Values.testRequired }}\n  requiredResult: {{ required \"value is required\" $testData | quote }}\n  {{- end }}\n\n  # Test: empty - returns true if value is empty\n  {{- if .Values.testEmpty }}\n  emptyResult: {{ empty $testData | quote }}\n  {{- end }}\n\n  # Test: fail - explicitly fails with message\n  {{- if .Values.testFail }}\n  failResult: {{ fail (printf \"intentional fail with: %v\" $testData) }}\n  {{- end }}\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n  {{- if .Values.testCoalesce }}\n  coalesceResult: {{ coalesce $testData .Values.compareValue \"default\" | quote }}\n  {{- end }}\n\n  # Test: ternary - conditional expression\n  {{- if .Values.testTernary }}\n  ternaryResult: {{ ternary \"yes\" \"no\" $testData | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-logic-test\r\ndata:\r\n  # ==========================================\r\n  # Boolean Logic Functions\r\n  # ==========================================\r\n  # Test: and - returns boolean AND of arguments\r\n\r\n  # Test: or - returns boolean OR of arguments\r\n\r\n  # Test: not - returns boolean negation\r\n\r\n  # ==========================================\r\n  # Comparison Functions\r\n  # ==========================================\r\n  # Test: eq - returns true if equal\r\n  eqResult: \"false\"\r\n\r\n  # Test: ne - returns true if not equal\r\n\r\n  # Test: lt - returns true if less than\r\n\r\n  # Test: le - returns true if less than or equal\r\n\r\n  # Test: gt - returns true if greater than\r\n\r\n  # Test: ge - returns true if greater than or equal\r\n\r\n  # ==========================================\r\n  # Default/Required Functions\r\n  # ==========================================\r\n  # Test: default - returns default value if empty\r\n\r\n  # Test: required - fails if value is empty\r\n\r\n  # Test: empty - returns true if value is empty\r\n\r\n  # Test: fail - explicitly fails with message\r\n\r\n  # ==========================================\r\n  # Coalesce/Ternary Functions\r\n  # ==========================================\r\n  # Test: coalesce - returns first non-empty value\r\n\r\n  # Test: ternary - conditional expression\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-logic-default/templates/logic-default-test.yaml",
            "content": "\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n\n  # Test: or - returns boolean OR of arguments\n\n  # Test: not - returns boolean negation\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n  eqResult: \"false\"\n\n  # Test: ne - returns true if not equal\n\n  # Test: lt - returns true if less than\n\n  # Test: le - returns true if less than or equal\n\n  # Test: gt - returns true if greater than\n\n  # Test: ge - returns true if greater than or equal\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n\n  # Test: required - fails if value is empty\n\n  # Test: empty - returns true if value is empty\n\n  # Test: fail - explicitly fails with message\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n\n  # Test: ternary - conditional expression\n",
            "originalContent": "# Test: Logic and default functions - comprehensive testing\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n  {{- if .Values.testAnd }}\n  andResult: {{ and $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: or - returns boolean OR of arguments\n  {{- if .Values.testOr }}\n  orResult: {{ or $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: not - returns boolean negation\n  {{- if .Values.testNot }}\n  notResult: {{ not $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n  {{- if .Values.testEq }}\n  eqResult: {{ eq $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ne - returns true if not equal\n  {{- if .Values.testNe }}\n  neResult: {{ ne $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: lt - returns true if less than\n  {{- if .Values.testLt }}\n  ltResult: {{ lt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: le - returns true if less than or equal\n  {{- if .Values.testLe }}\n  leResult: {{ le $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: gt - returns true if greater than\n  {{- if .Values.testGt }}\n  gtResult: {{ gt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ge - returns true if greater than or equal\n  {{- if .Values.testGe }}\n  geResult: {{ ge $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n  {{- if .Values.testDefault }}\n  defaultResult: {{ default \"fallback\" $testData | quote }}\n  {{- end }}\n\n  # Test: required - fails if value is empty\n  {{- if .Values.testRequired }}\n  requiredResult: {{ required \"value is required\" $testData | quote }}\n  {{- end }}\n\n  # Test: empty - returns true if value is empty\n  {{- if .Values.testEmpty }}\n  emptyResult: {{ empty $testData | quote }}\n  {{- end }}\n\n  # Test: fail - explicitly fails with message\n  {{- if .Values.testFail }}\n  failResult: {{ fail (printf \"intentional fail with: %v\" $testData) }}\n  {{- end }}\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n  {{- if .Values.testCoalesce }}\n  coalesceResult: {{ coalesce $testData .Values.compareValue \"default\" | quote }}\n  {{- end }}\n\n  # Test: ternary - conditional expression\n  {{- if .Values.testTernary }}\n  ternaryResult: {{ ternary \"yes\" \"no\" $testData | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "eq",
      "dataType": "stringValue",
      "valuesOptions": {
        "values": [
          "testEq=true",
          "testDataType=stringValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": "template: test-logic-default/templates/logic-default-test.yaml:32:15: executing \"test-logic-default/templates/logic-default-test.yaml\" at \u003ceq $testData .Values.compareValue\u003e: error calling eq: incompatible types for comparison: string and float64",
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      },
      "debug": {
        "error": "template: test-logic-default/templates/logic-default-test.yaml:32:15: executing \"test-logic-default/templates/logic-default-test.yaml\" at \u003ceq $testData .Values.compareValue\u003e: error calling eq: incompatible types for comparison: string and float64",
        "debugLogs": [],
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      }
    },
    {
      "toggle": "eq",
      "dataType": "numberValue",
      "valuesOptions": {
        "values": [
          "testEq=true",
          "testDataType=numberValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-logic-test\r\ndata:\r\n  # ==========================================\r\n  # Boolean Logic Functions\r\n  # ==========================================\r\n  # Test: and - returns boolean AND of arguments\r\n\r\n  # Test: or - returns boolean OR of arguments\r\n\r\n  # Test: not - returns boolean negation\r\n\r\n  # ==========================================\r\n  # Comparison Functions\r\n  # ==========================================\r\n  # Test: eq - returns true if equal\r\n  eqResult: \"false\"\r\n\r\n  # Test: ne - returns true if not equal\r\n\r\n  # Test: lt - returns true if less than\r\n\r\n  # Test: le - returns true if less than or equal\r\n\r\n  # Test: gt - returns true if greater than\r\n\r\n  # Test: ge - returns true if greater than or equal\r\n\r\n  # ==========================================\r\n  # Default/Required Functions\r\n  # ==========================================\r\n  # Test: default - returns default value if empty\r\n\r\n  # Test: required - fails if value is empty\r\n\r\n  # Test: empty - returns true if value is empty\r\n\r\n  # Test: fail - explicitly fails with message\r\n\r\n  # ==========================================\r\n  # Coalesce/Ternary Functions\r\n  # ==========================================\r\n  # Test: coalesce - returns first non-empty value\r\n\r\n  # Test: ternary - conditional expression\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-logic-default/templates/logic-default-test.yaml",
            "content": "\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n\n  # Test: or - returns boolean OR of arguments\n\n  # Test: not - returns boolean negation\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n  eqResult: \"false\"\n\n  # Test: ne - returns true if not equal\n\n  # Test: lt - returns true if less than\n\n  # Test: le - returns true if less than or equal\n\n  # Test: gt - returns true if greater than\n\n  # Test: ge - returns true if greater than or equal\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n\n  # Test: required - fails if value is empty\n\n  # Test: empty - returns true if value is empty\n\n  # Test: fail - explicitly fails with message\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n\n  # Test: ternary - conditional expression\n",
            "originalContent": "# Test: Logic and default functions - comprehensive testing\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n  {{- if .Values.testAnd }}\n  andResult: {{ and $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: or - returns boolean OR of arguments\n  {{- if .Values.testOr }}\n  orResult: {{ or $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: not - returns boolean negation\n  {{- if .Values.testNot }}\n  notResult: {{ not $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n  {{- if .Values.testEq }}\n  eqResult: {{ eq $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ne - returns true if not equal\n  {{- if .Values.testNe }}\n  neResult: {{ ne $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: lt - returns true if less than\n  {{- if .Values.testLt }}\n  ltResult: {{ lt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: le - returns true if less than or equal\n  {{- if .Values.testLe }}\n  leResult: {{ le $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: gt - returns true if greater than\n  {{- if .Values.testGt }}\n  gtResult: {{ gt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ge - returns true if greater than or equal\n  {{- if .Values.testGe }}\n  geResult: {{ ge $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n  {{- if .Values.testDefault }}\n  defaultResult: {{ default \"fallback\" $testData | quote }}\n  {{- end }}\n\n  # Test: required - fails if value is empty\n  {{- if .Values.testRequired }}\n  requiredResult: {{ required \"value is required\" $testData | quote }}\n  {{- end }}\n\n  # Test: empty - returns true if value is empty\n  {{- if .Values.testEmpty }}\n  emptyResult: {{ empty $testData | quote }}\n  {{- end }}\n\n  # Test: fail - explicitly fails with message\n  {{- if .Values.testFail }}\n  failResult: {{ fail (printf \"intentional fail with: %v\" $testData) }}\n  {{- end }}\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n  {{- if .Values.testCoalesce }}\n  coalesceResult: {{ coalesce $testData .Values.compareValue \"default\" | quote }}\n  {{- end }}\n\n  # Test: ternary - conditional expression\n  {{- if .Values.testTernary }}\n  ternaryResult: {{ ternary \"yes\" \"no\" $testData | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-logic-test\r\ndata:\r\n  # ==========================================\r\n  # Boolean Logic Functions\r\n  # ==========================================\r\n  # Test: and - returns boolean AND of arguments\r\n\r\n  # Test: or - returns boolean OR of arguments\r\n\r\n  # Test: not - returns boolean negation\r\n\r\n  # ==========================================\r\n  # Comparison Functions\r\n  # ==========================================\r\n  # Test: eq - returns true if equal\r\n  eqResult: \"false\"\r\n\r\n  # Test: ne - returns true if not equal\r\n\r\n  # Test: lt - returns true if less than\r\n\r\n  # Test: le - returns true if less than or equal\r\n\r\n  # Test: gt - returns true if greater than\r\n\r\n  # Test: ge - returns true if greater than or equal\r\n\r\n  # ==========================================\r\n  # Default/Required Functions\r\n  # ==========================================\r\n  # Test: default - returns default value if empty\r\n\r\n  # Test: required - fails if value is empty\r\n\r\n  # Test: empty - returns true if value is empty\r\n\r\n  # Test: fail - explicitly fails with message\r\n\r\n  # ==========================================\r\n  # Coalesce/Ternary Functions\r\n  # ==========================================\r\n  # Test: coalesce - returns first non-empty value\r\n\r\n  # Test: ternary - conditional expression\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-logic-default/templates/logic-default-test.yaml",
            "content": "\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n\n  # Test: or - returns boolean OR of arguments\n\n  # Test: not - returns boolean negation\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n  eqResult: \"false\"\n\n  # Test: ne - returns true if not equal\n\n  # Test: lt - returns true if less than\n\n  # Test: le - returns true if less than or equal\n\n  # Test: gt - returns true if greater than\n\n  # Test: ge - returns true if greater than or equal\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n\n  # Test: required - fails if value is empty\n\n  # Test: empty - returns true if value is empty\n\n  # Test: fail - explicitly fails with message\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n\n  # Test: ternary - conditional expression\n",
            "originalContent": "# Test: Logic and default functions - comprehensive testing\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n  {{- if .Values.testAnd }}\n  andResult: {{ and $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: or - returns boolean OR of arguments\n  {{- if .Values.testOr }}\n  orResult: {{ or $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: not - returns boolean negation\n  {{- if .Values.testNot }}\n  notResult: {{ not $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n  {{- if .Values.testEq }}\n  eqResult: {{ eq $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ne - returns true if not equal\n  {{- if .Values.testNe }}\n  neResult: {{ ne $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: lt - returns true if less than\n  {{- if .Values.testLt }}\n  ltResult: {{ lt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: le - returns true if less than or equal\n  {{- if .Values.testLe }}\n  leResult: {{ le $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: gt - returns true if greater than\n  {{- if .Values.testGt }}\n  gtResult: {{ gt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ge - returns true if greater than or equal\n  {{- if .Values.testGe }}\n  geResult: {{ ge $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n  {{- if .Values.testDefault }}\n  defaultResult: {{ default \"fallback\" $testData | quote }}\n  {{- end }}\n\n  # Test: required - fails if value is empty\n  {{- if .Values.testRequired }}\n  requiredResult: {{ required \"value is required\" $testData | quote }}\n  {{- end }}\n\n  # Test: empty - returns true if value is empty\n  {{- if .Values.testEmpty }}\n  emptyResult: {{ empty $testData | quote }}\n  {{- end }}\n\n  # Test: fail - explicitly fails with message\n  {{- if .Values.testFail }}\n  failResult: {{ fail (printf \"intentional fail with: %v\" $testData) }}\n  {{- end }}\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n  {{- if .Values.testCoalesce }}\n  coalesceResult: {{ coalesce $testData .Values.compareValue \"default\" | quote }}\n  {{- end }}\n\n  # Test: ternary - conditional expression\n  {{- if .Values.testTernary }}\n  ternaryResult: {{ ternary \"yes\" \"no\" $testData | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "eq",
      "dataType": "boolValue",
      "valuesOptions": {
        "values": [
          "testEq=true",
          "testDataType=boolValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": "template: test-logic-default/templates/logic-default-test.yaml:32:15: executing \"test-logic-default/templates/logic-default-test.yaml\" at \u003ceq $testData .Values.compareValue\u003e: error calling eq: incompatible types for comparison: bool and float64",
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      },
      "debug": {
        "error": "template: test-logic-default/templates/logic-default-test.yaml:32:15: executing \"test-logic-default/templates/logic-default-test.yaml\" at \u003ceq $testData .Values.compareValue\u003e: error calling eq: incompatible types for comparison: bool and float64",
        "debugLogs": [],
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      }
    },
    {
      "toggle": "eq",
      "dataType": "listValue",
      "valuesOptions": {
        "values": [
          "testEq=true",
          "testDataType=listValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": "template: test-logic-default/templates/logic-default-test.yaml:32:15: executing \"test-logic-default/templates/logic-default-test.yaml\" at \u003ceq $testData .Values.compareValue\u003e: error calling eq: incompatible types for comparison: []interface {} and float64",
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      },
      "debug": {
        "error": "template: test-logic-default/templates/logic-default-test.yaml:32:15: executing \"test-logic-default/templates/logic-default-test.yaml\" at \u003ceq $testData .Values.compareValue\u003e: error calling eq: incompatible types for comparison: []interface {} and float64",
        "debugLogs": [],
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      }
    },
    {
      "toggle": "eq",
      "dataType": "dictValue",
      "valuesOptions": {
        "values": [
          "testEq=true",
          "testDataType=dictValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": "template: test-logic-default/templates/logic-default-test.yaml:32:15: executing \"test-logic-default/templates/logic-default-test.yaml\" at \u003ceq $testData .Values.compareValue\u003e: error calling eq: incompatible types for comparison: map[string]interface {} and float64",
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      },
      "debug": {
        "error": "template: test-logic-default/templates/logic-default-test.yaml:32:15: executing \"test-logic-default/templates/logic-default-test.yaml\" at \u003ceq $testData .Values.compareValue\u003e: error calling eq: incompatible types for comparison: map[string]interface {} and float64",
        "debugLogs": [],
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      }
    },
    {
      "toggle": "ne",
      "dataType": "nilValue",
      "valuesOptions": {
        "values": [
          "testNe=true",
          "testDataType=nilValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-logic-test\r\ndata:\r\n  # ==========================================\r\n  # Boolean Logic Functions\r\n  # ==========================================\r\n  # Test: and - returns boolean AND of arguments\r\n\r\n  # Test: or - returns boolean OR of arguments\r\n\r\n  # Test: not - returns boolean negation\r\n\r\n  # ==========================================\r\n  # Comparison Functions\r\n  # ==========================================\r\n  # Test: eq - returns true if equal\r\n\r\n  # Test: ne - returns true if not equal\r\n  neResult: \"true\"\r\n\r\n  # Test: lt - returns true if less than\r\n\r\n  # Test: le - returns true if less than or equal\r\n\r\n  # Test: gt - returns true if greater than\r\n\r\n  # Test: ge - returns true if greater than or equal\r\n\r\n  # ==========================================\r\n  # Default/Required Functions\r\n  # ==========================================\r\n  # Test: default - returns default value if empty\r\n\r\n  # Test: required - fails if value is empty\r\n\r\n  # Test: empty - returns true if value is empty\r\n\r\n  # Test: fail - explicitly fails with message\r\n\r\n  # ==========================================\r\n  # Coalesce/Ternary Functions\r\n  # ==========================================\r\n  # Test: coalesce - returns first non-empty value\r\n\r\n  # Test: ternary - conditional expression\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-logic-default/templates/logic-default-test.yaml",
            "content": "\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n\n  # Test: or - returns boolean OR of arguments\n\n  # Test: not - returns boolean negation\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n\n  # Test: ne - returns true if not equal\n  neResult: \"true\"\n\n  # Test: lt - returns true if less than\n\n  # Test: le - returns true if less than or equal\n\n  # Test: gt - returns true if greater than\n\n  # Test: ge - returns true if greater than or equal\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n\n  # Test: required - fails if value is empty\n\n  # Test: empty - returns true if value is empty\n\n  # Test: fail - explicitly fails with message\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n\n  # Test: ternary - conditional expression\n",
            "originalContent": "# Test: Logic and default functions - comprehensive testing\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n  {{- if .Values.testAnd }}\n  andResult: {{ and $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: or - returns boolean OR of arguments\n  {{- if .Values.testOr }}\n  orResult: {{ or $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: not - returns boolean negation\n  {{- if .Values.testNot }}\n  notResult: {{ not $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n  {{- if .Values.testEq }}\n  eqResult: {{ eq $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ne - returns true if not equal\n  {{- if .Values.testNe }}\n  neResult: {{ ne $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: lt - returns true if less than\n  {{- if .Values.testLt }}\n  ltResult: {{ lt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: le - returns true if less than or equal\n  {{- if .Values.testLe }}\n  leResult: {{ le $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: gt - returns true if greater than\n  {{- if .Values.testGt }}\n  gtResult: {{ gt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ge - returns true if greater than or equal\n  {{- if .Values.testGe }}\n  geResult: {{ ge $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n  {{- if .Values.testDefault }}\n  defaultResult: {{ default \"fallback\" $testData | quote }}\n  {{- end }}\n\n  # Test: required - fails if value is empty\n  {{- if .Values.testRequired }}\n  requiredResult: {{ required \"value is required\" $testData | quote }}\n  {{- end }}\n\n  # Test: empty - returns true if value is empty\n  {{- if .Values.testEmpty }}\n  emptyResult: {{ empty $testData | quote }}\n  {{- end }}\n\n  # Test: fail - explicitly fails with message\n  {{- if .Values.testFail }}\n  failResult: {{ fail (printf \"intentional fail with: %v\" $testData) }}\n  {{- end }}\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n  {{- if .Values.testCoalesce }}\n  coalesceResult: {{ coalesce $testData .Values.compareValue \"default\" | quote }}\n  {{- end }}\n\n  # Test: ternary - conditional expression\n  {{- if .Values.testTernary }}\n  ternaryResult: {{ ternary \"yes\" \"no\" $testData | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-logic-test\r\ndata:\r\n  # ==========================================\r\n  # Boolean Logic Functions\r\n  # ==========================================\r\n  # Test: and - returns boolean AND of arguments\r\n\r\n  # Test: or - returns boolean OR of arguments\r\n\r\n  # Test: not - returns boolean negation\r\n\r\n  # ==========================================\r\n  # Comparison Functions\r\n  # ==========================================\r\n  # Test: eq - returns true if equal\r\n\r\n  # Test: ne - returns true if not equal\r\n  neResult: \"true\"\r\n\r\n  # Test: lt - returns true if less than\r\n\r\n  # Test: le - returns true if less than or equal\r\n\r\n  # Test: gt - returns true if greater than\r\n\r\n  # Test: ge - returns true if greater than or equal\r\n\r\n  # ==========================================\r\n  # Default/Required Functions\r\n  # ==========================================\r\n  # Test: default - returns default value if empty\r\n\r\n  # Test: required - fails if value is empty\r\n\r\n  # Test: empty - returns true if value is empty\r\n\r\n  # Test: fail - explicitly fails with message\r\n\r\n  # ==========================================\r\n  # Coalesce/Ternary Functions\r\n  # ==========================================\r\n  # Test: coalesce - returns first non-empty value\r\n\r\n  # Test: ternary - conditional expression\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-logic-default/templates/logic-default-test.yaml",
            "content": "\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n\n  # Test: or - returns boolean OR of arguments\n\n  # Test: not - returns boolean negation\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n\n  # Test: ne - returns true if not equal\n  neResult: \"true\"\n\n  # Test: lt - returns true if less than\n\n  # Test: le - returns true if less than or equal\n\n  # Test: gt - returns true if greater than\n\n  # Test: ge - returns true if greater than or equal\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n\n  # Test: required - fails if value is empty\n\n  # Test: empty - returns true if value is empty\n\n  # Test: fail - explicitly fails with message\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n\n  # Test: ternary - conditional expression\n",
            "originalContent": "# Test: Logic and default functions - comprehensive testing\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n  {{- if .Values.testAnd }}\n  andResult: {{ and $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: or - returns boolean OR of arguments\n  {{- if .Values.testOr }}\n  orResult: {{ or $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: not - returns boolean negation\n  {{- if .Values.testNot }}\n  notResult: {{ not $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n  {{- if .Values.testEq }}\n  eqResult: {{ eq $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ne - returns true if not equal\n  {{- if .Values.testNe }}\n  neResult: {{ ne $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: lt - returns true if less than\n  {{- if .Values.testLt }}\n  ltResult: {{ lt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: le - returns true if less than or equal\n  {{- if .Values.testLe }}\n  leResult: {{ le $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: gt - returns true if greater than\n  {{- if .Values.testGt }}\n  gtResult: {{ gt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ge - returns true if greater than or equal\n  {{- if .Values.testGe }}\n  geResult: {{ ge $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n  {{- if .Values.testDefault }}\n  defaultResult: {{ default \"fallback\" $testData | quote }}\n  {{- end }}\n\n  # Test: required - fails if value is empty\n  {{- if .Values.testRequired }}\n  requiredResult: {{ required \"value is required\" $testData | quote }}\n  {{- end }}\n\n  # Test: empty - returns true if value is empty\n  {{- if .Values.testEmpty }}\n  emptyResult: {{ empty $testData | quote }}\n  {{- end }}\n\n  # Test: fail - explicitly fails with message\n  {{- if .Values.testFail }}\n  failResult: {{ fail (printf \"intentional fail with: %v\" $testData) }}\n  {{- end }}\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n  {{- if .Values.testCoalesce }}\n  coalesceResult: {{ coalesce $testData .Values.compareValue \"default\" | quote }}\n  {{- end }}\n\n  # Test: ternary - conditional expression\n  {{- if .Values.testTernary }}\n  ternaryResult: {{ ternary \"yes\" \"no\" $testData | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "ne",
      "dataType": "stringValue",
      "valuesOptions": {
        "values": [
          "testNe=true",
          "testDataType=stringValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": "template: test-logic-default/templates/logic-default-test.yaml:37:15: executing \"test-logic-default/templates/logic-default-test.yaml\" at \u003cne $testData .Values.compareValue\u003e: error calling ne: incompatible types for comparison: string and float64",
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      },
      "debug": {
        "error": "template: test-logic-default/templates/logic-default-test.yaml:37:15: executing \"test-logic-default/templates/logic-default-test.yaml\" at \u003cne $testData .Values.compareValue\u003e: error calling ne: incompatible types for comparison: string and float64",
        "debugLogs": [],
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      }
    },
    {
      "toggle": "ne",
      "dataType": "numberValue",
      "valuesOptions": {
        "values": [
          "testNe=true",
          "testDataType=numberValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-logic-test\r\ndata:\r\n  # ==========================================\r\n  # Boolean Logic Functions\r\n  # ==========================================\r\n  # Test: and - returns boolean AND of arguments\r\n\r\n  # Test: or - returns boolean OR of arguments\r\n\r\n  # Test: not - returns boolean negation\r\n\r\n  # ==========================================\r\n  # Comparison Functions\r\n  # ==========================================\r\n  # Test: eq - returns true if equal\r\n\r\n  # Test: ne - returns true if not equal\r\n  neResult: \"true\"\r\n\r\n  # Test: lt - returns true if less than\r\n\r\n  # Test: le - returns true if less than or equal\r\n\r\n  # Test: gt - returns true if greater than\r\n\r\n  # Test: ge - returns true if greater than or equal\r\n\r\n  # ==========================================\r\n  # Default/Required Functions\r\n  # ==========================================\r\n  # Test: default - returns default value if empty\r\n\r\n  # Test: required - fails if value is empty\r\n\r\n  # Test: empty - returns true if value is empty\r\n\r\n  # Test: fail - explicitly fails with message\r\n\r\n  # ==========================================\r\n  # Coalesce/Ternary Functions\r\n  # ==========================================\r\n  # Test: coalesce - returns first non-empty value\r\n\r\n  # Test: ternary - conditional expression\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-logic-default/templates/logic-default-test.yaml",
            "content": "\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n\n  # Test: or - returns boolean OR of arguments\n\n  # Test: not - returns boolean negation\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n\n  # Test: ne - returns true if not equal\n  neResult: \"true\"\n\n  # Test: lt - returns true if less than\n\n  # Test: le - returns true if less than or equal\n\n  # Test: gt - returns true if greater than\n\n  # Test: ge - returns true if greater than or equal\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n\n  # Test: required - fails if value is empty\n\n  # Test: empty - returns true if value is empty\n\n  # Test: fail - explicitly fails with message\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n\n  # Test: ternary - conditional expression\n",
            "originalContent": "# Test: Logic and default functions - comprehensive testing\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n  {{- if .Values.testAnd }}\n  andResult: {{ and $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: or - returns boolean OR of arguments\n  {{- if .Values.testOr }}\n  orResult: {{ or $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: not - returns boolean negation\n  {{- if .Values.testNot }}\n  notResult: {{ not $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n  {{- if .Values.testEq }}\n  eqResult: {{ eq $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ne - returns true if not equal\n  {{- if .Values.testNe }}\n  neResult: {{ ne $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: lt - returns true if less than\n  {{- if .Values.testLt }}\n  ltResult: {{ lt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: le - returns true if less than or equal\n  {{- if .Values.testLe }}\n  leResult: {{ le $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: gt - returns true if greater than\n  {{- if .Values.testGt }}\n  gtResult: {{ gt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ge - returns true if greater than or equal\n  {{- if .Values.testGe }}\n  geResult: {{ ge $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n  {{- if .Values.testDefault }}\n  defaultResult: {{ default \"fallback\" $testData | quote }}\n  {{- end }}\n\n  # Test: required - fails if value is empty\n  {{- if .Values.testRequired }}\n  requiredResult: {{ required \"value is required\" $testData | quote }}\n  {{- end }}\n\n  # Test: empty - returns true if value is empty\n  {{- if .Values.testEmpty }}\n  emptyResult: {{ empty $testData | quote }}\n  {{- end }}\n\n  # Test: fail - explicitly fails with message\n  {{- if .Values.testFail }}\n  failResult: {{ fail (printf \"intentional fail with: %v\" $testData) }}\n  {{- end }}\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n  {{- if .Values.testCoalesce }}\n  coalesceResult: {{ coalesce $testData .Values.compareValue \"default\" | quote }}\n  {{- end }}\n\n  # Test: ternary - conditional expression\n  {{- if .Values.testTernary }}\n  ternaryResult: {{ ternary \"yes\" \"no\" $testData | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-logic-test\r\ndata:\r\n  # ==========================================\r\n  # Boolean Logic Functions\r\n  # ==========================================\r\n  # Test: and - returns boolean AND of arguments\r\n\r\n  # Test: or - returns boolean OR of arguments\r\n\r\n  # Test: not - returns boolean negation\r\n\r\n  # ==========================================\r\n  # Comparison Functions\r\n  # ==========================================\r\n  # Test: eq - returns true if equal\r\n\r\n  # Test: ne - returns true if not equal\r\n  neResult: \"true\"\r\n\r\n  # Test: lt - returns true if less than\r\n\r\n  # Test: le - returns true if less than or equal\r\n\r\n  # Test: gt - returns true if greater than\r\n\r\n  # Test: ge - returns true if greater than or equal\r\n\r\n  # ==========================================\r\n  # Default/Required Functions\r\n  # ==========================================\r\n  # Test: default - returns default value if empty\r\n\r\n  # Test: required - fails if value is empty\r\n\r\n  # Test: empty - returns true if value is empty\r\n\r\n  # Test: fail - explicitly fails with message\r\n\r\n  # ==========================================\r\n  # Coalesce/Ternary Functions\r\n  # ==========================================\r\n  # Test: coalesce - returns first non-empty value\r\n\r\n  # Test: ternary - conditional expression\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-logic-default/templates/logic-default-test.yaml",
            "content": "\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n\n  # Test: or - returns boolean OR of arguments\n\n  # Test: not - returns boolean negation\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n\n  # Test: ne - returns true if not equal\n  neResult: \"true\"\n\n  # Test: lt - returns true if less than\n\n  # Test: le - returns true if less than or equal\n\n  # Test: gt - returns true if greater than\n\n  # Test: ge - returns true if greater than or equal\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n\n  # Test: required - fails if value is empty\n\n  # Test: empty - returns true if value is empty\n\n  # Test: fail - explicitly fails with message\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n\n  # Test: ternary - conditional expression\n",
            "originalContent": "# Test: Logic and default functions - comprehensive testing\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n  {{- if .Values.testAnd }}\n  andResult: {{ and $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: or - returns boolean OR of arguments\n  {{- if .Values.testOr }}\n  orResult: {{ or $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: not - returns boolean negation\n  {{- if .Values.testNot }}\n  notResult: {{ not $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n  {{- if .Values.testEq }}\n  eqResult: {{ eq $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ne - returns true if not equal\n  {{- if .Values.testNe }}\n  neResult: {{ ne $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: lt - returns true if less than\n  {{- if .Values.testLt }}\n  ltResult: {{ lt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: le - returns true if less than or equal\n  {{- if .Values.testLe }}\n  leResult: {{ le $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: gt - returns true if greater than\n  {{- if .Values.testGt }}\n  gtResult: {{ gt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ge - returns true if greater than or equal\n  {{- if .Values.testGe }}\n  geResult: {{ ge $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n  {{- if .Values.testDefault }}\n  defaultResult: {{ default \"fallback\" $testData | quote }}\n  {{- end }}\n\n  # Test: required - fails if value is empty\n  {{- if .Values.testRequired }}\n  requiredResult: {{ required \"value is required\" $testData | quote }}\n  {{- end }}\n\n  # Test: empty - returns true if value is empty\n  {{- if .Values.testEmpty }}\n  emptyResult: {{ empty $testData | quote }}\n  {{- end }}\n\n  # Test: fail - explicitly fails with message\n  {{- if .Values.testFail }}\n  failResult: {{ fail (printf \"intentional fail with: %v\" $testData) }}\n  {{- end }}\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n  {{- if .Values.testCoalesce }}\n  coalesceResult: {{ coalesce $testData .Values.compareValue \"default\" | quote }}\n  {{- end }}\n\n  # Test: ternary - conditional expression\n  {{- if .Values.testTernary }}\n  ternaryResult: {{ ternary \"yes\" \"no\" $testData | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "ne",
      "dataType": "boolValue",
      "valuesOptions": {
        "values": [
          "testNe=true",
          "testDataType=boolValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": "template: test-logic-default/templates/logic-default-test.yaml:37:15: executing \"test-logic-default/templates/logic-default-test.yaml\" at \u003cne $testData .Values.compareValue\u003e: error calling ne: incompatible types for comparison: bool and float64",
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      },
      "debug": {
        "error": "template: test-logic-default/templates/logic-default-test.yaml:37:15: executing \"test-logic-default/templates/logic-default-test.yaml\" at \u003cne $testData .Values.compareValue\u003e: error calling ne: incompatible types for comparison: bool and float64",
        "debugLogs": [],
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      }
    },
    {
      "toggle": "ne",
      "dataType": "listValue",
      "valuesOptions": {
        "values": [
          "testNe=true",
          "testDataType=listValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": "template: test-logic-default/templates/logic-default-test.yaml:37:15: executing \"test-logic-default/templates/logic-default-test.yaml\" at \u003cne $testData .Values.compareValue\u003e: error calling ne: incompatible types for comparison: []interface {} and float64",
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      },
      "debug": {
        "error": "template: test-logic-default/templates/logic-default-test.yaml:37:15: executing \"test-logic-default/templates/logic-default-test.yaml\" at \u003cne $testData .Values.compareValue\u003e: error calling ne: incompatible types for comparison: []interface {} and float64",
        "debugLogs": [],
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      }
    },
    {
      "toggle": "ne",
      "dataType": "dictValue",
      "valuesOptions": {
        "values": [
          "testNe=true",
          "testDataType=dictValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": "template: test-logic-default/templates/logic-default-test.yaml:37:15: executing \"test-logic-default/templates/logic-default-test.yaml\" at \u003cne $testData .Values.compareValue\u003e: error calling ne: incompatible types for comparison: map[string]interface {} and float64",
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      },
      "debug": {
        "error": "template: test-logic-default/templates/logic-default-test.yaml:37:15: executing \"test-logic-default/templates/logic-default-test.yaml\" at \u003cne $testData .Values.compareValue\u003e: error calling ne: incompatible types for comparison: map[string]interface {} and float64",
        "debugLogs": [],
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      }
    },
    {
      "toggle": "lt",
      "dataType": "nilValue",
      "valuesOptions": {
        "values": [
          "testLt=true",
          "testDataType=nilValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": "template: test-logic-default/templates/logic-default-test.yaml:42:15: executing \"test-logic-default/templates/logic-default-test.yaml\" at \u003clt $testData .Values.compareValue\u003e: error calling lt: invalid type for comparison",
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      },
      "debug": {
        "error": "template: test-logic-default/templates/logic-default-test.yaml:42:15: executing \"test-logic-default/templates/logic-default-test.yaml\" at \u003clt $testData .Values.compareValue\u003e: error calling lt: invalid type for comparison",
        "debugLogs": [],
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      }
    },
    {
      "toggle": "lt",
      "dataType": "stringValue",
      "valuesOptions": {
        "values": [
          "testLt=true",
          "testDataType=stringValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": "template: test-logic-default/templates/logic-default-test.yaml:42:15: executing \"test-logic-default/templates/logic-default-test.yaml\" at \u003clt $testData .Values.compareValue\u003e: error calling lt: incompatible types for comparison: string and float64",
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      },
      "debug": {
        "error": "template: test-logic-default/templates/logic-default-test.yaml:42:15: executing \"test-logic-default/templates/logic-default-test.yaml\" at \u003clt $testData .Values.compareValue\u003e: error calling lt: incompatible types for comparison: string and float64",
        "debugLogs": [],
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      }
    },
    {
      "toggle": "lt",
      "dataType": "numberValue",
      "valuesOptions": {
        "values": [
          "testLt=true",
          "testDataType=numberValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-logic-test\r\ndata:\r\n  # ==========================================\r\n  # Boolean Logic Functions\r\n  # ==========================================\r\n  # Test: and - returns boolean AND of arguments\r\n\r\n  # Test: or - returns boolean OR of arguments\r\n\r\n  # Test: not - returns boolean negation\r\n\r\n  # ==========================================\r\n  # Comparison Functions\r\n  # ==========================================\r\n  # Test: eq - returns true if equal\r\n\r\n  # Test: ne - returns true if not equal\r\n\r\n  # Test: lt - returns true if less than\r\n  ltResult: \"false\"\r\n\r\n  # Test: le - returns true if less than or equal\r\n\r\n  # Test: gt - returns true if greater than\r\n\r\n  # Test: ge - returns true if greater than or equal\r\n\r\n  # ==========================================\r\n  # Default/Required Functions\r\n  # ==========================================\r\n  # Test: default - returns default value if empty\r\n\r\n  # Test: required - fails if value is empty\r\n\r\n  # Test: empty - returns true if value is empty\r\n\r\n  # Test: fail - explicitly fails with message\r\n\r\n  # ==========================================\r\n  # Coalesce/Ternary Functions\r\n  # ==========================================\r\n  # Test: coalesce - returns first non-empty value\r\n\r\n  # Test: ternary - conditional expression\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-logic-default/templates/logic-default-test.yaml",
            "content": "\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n\n  # Test: or - returns boolean OR of arguments\n\n  # Test: not - returns boolean negation\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n\n  # Test: ne - returns true if not equal\n\n  # Test: lt - returns true if less than\n  ltResult: \"false\"\n\n  # Test: le - returns true if less than or equal\n\n  # Test: gt - returns true if greater than\n\n  # Test: ge - returns true if greater than or equal\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n\n  # Test: required - fails if value is empty\n\n  # Test: empty - returns true if value is empty\n\n  # Test: fail - explicitly fails with message\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n\n  # Test: ternary - conditional expression\n",
            "originalContent": "# Test: Logic and default functions - comprehensive testing\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n  {{- if .Values.testAnd }}\n  andResult: {{ and $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: or - returns boolean OR of arguments\n  {{- if .Values.testOr }}\n  orResult: {{ or $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: not - returns boolean negation\n  {{- if .Values.testNot }}\n  notResult: {{ not $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n  {{- if .Values.testEq }}\n  eqResult: {{ eq $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ne - returns true if not equal\n  {{- if .Values.testNe }}\n  neResult: {{ ne $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: lt - returns true if less than\n  {{- if .Values.testLt }}\n  ltResult: {{ lt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: le - returns true if less than or equal\n  {{- if .Values.testLe }}\n  leResult: {{ le $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: gt - returns true if greater than\n  {{- if .Values.testGt }}\n  gtResult: {{ gt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ge - returns true if greater than or equal\n  {{- if .Values.testGe }}\n  geResult: {{ ge $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n  {{- if .Values.testDefault }}\n  defaultResult: {{ default \"fallback\" $testData | quote }}\n  {{- end }}\n\n  # Test: required - fails if value is empty\n  {{- if .Values.testRequired }}\n  requiredResult: {{ required \"value is required\" $testData | quote }}\n  {{- end }}\n\n  # Test: empty - returns true if value is empty\n  {{- if .Values.testEmpty }}\n  emptyResult: {{ empty $testData | quote }}\n  {{- end }}\n\n  # Test: fail - explicitly fails with message\n  {{- if .Values.testFail }}\n  failResult: {{ fail (printf \"intentional fail with: %v\" $testData) }}\n  {{- end }}\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n  {{- if .Values.testCoalesce }}\n  coalesceResult: {{ coalesce $testData .Values.compareValue \"default\" | quote }}\n  {{- end }}\n\n  # Test: ternary - conditional expression\n  {{- if .Values.testTernary }}\n  ternaryResult: {{ ternary \"yes\" \"no\" $testData | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-logic-test\r\ndata:\r\n  # ==========================================\r\n  # Boolean Logic Functions\r\n  # ==========================================\r\n  # Test: and - returns boolean AND of arguments\r\n\r\n  # Test: or - returns boolean OR of arguments\r\n\r\n  # Test: not - returns boolean negation\r\n\r\n  # ==========================================\r\n  # Comparison Functions\r\n  # ==========================================\r\n  # Test: eq - returns true if equal\r\n\r\n  # Test: ne - returns true if not equal\r\n\r\n  # Test: lt - returns true if less than\r\n  ltResult: \"false\"\r\n\r\n  # Test: le - returns true if less than or equal\r\n\r\n  # Test: gt - returns true if greater than\r\n\r\n  # Test: ge - returns true if greater than or equal\r\n\r\n  # ==========================================\r\n  # Default/Required Functions\r\n  # ==========================================\r\n  # Test: default - returns default value if empty\r\n\r\n  # Test: required - fails if value is empty\r\n\r\n  # Test: empty - returns true if value is empty\r\n\r\n  # Test: fail - explicitly fails with message\r\n\r\n  # ==========================================\r\n  # Coalesce/Ternary Functions\r\n  # ==========================================\r\n  # Test: coalesce - returns first non-empty value\r\n\r\n  # Test: ternary - conditional expression\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-logic-default/templates/logic-default-test.yaml",
            "content": "\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n\n  # Test: or - returns boolean OR of arguments\n\n  # Test: not - returns boolean negation\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n\n  # Test: ne - returns true if not equal\n\n  # Test: lt - returns true if less than\n  ltResult: \"false\"\n\n  # Test: le - returns true if less than or equal\n\n  # Test: gt - returns true if greater than\n\n  # Test: ge - returns true if greater than or equal\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n\n  # Test: required - fails if value is empty\n\n  # Test: empty - returns true if value is empty\n\n  # Test: fail - explicitly fails with message\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n\n  # Test: ternary - conditional expression\n",
            "originalContent": "# Test: Logic and default functions - comprehensive testing\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n  {{- if .Values.testAnd }}\n  andResult: {{ and $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: or - returns boolean OR of arguments\n  {{- if .Values.testOr }}\n  orResult: {{ or $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: not - returns boolean negation\n  {{- if .Values.testNot }}\n  notResult: {{ not $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n  {{- if .Values.testEq }}\n  eqResult: {{ eq $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ne - returns true if not equal\n  {{- if .Values.testNe }}\n  neResult: {{ ne $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: lt - returns true if less than\n  {{- if .Values.testLt }}\n  ltResult: {{ lt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: le - returns true if less than or equal\n  {{- if .Values.testLe }}\n  leResult: {{ le $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: gt - returns true if greater than\n  {{- if .Values.testGt }}\n  gtResult: {{ gt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ge - returns true if greater than or equal\n  {{- if .Values.testGe }}\n  geResult: {{ ge $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n  {{- if .Values.testDefault }}\n  defaultResult: {{ default \"fallback\" $testData | quote }}\n  {{- end }}\n\n  # Test: required - fails if value is empty\n  {{- if .Values.testRequired }}\n  requiredResult: {{ required \"value is required\" $testData | quote }}\n  {{- end }}\n\n  # Test: empty - returns true if value is empty\n  {{- if .Values.testEmpty }}\n  emptyResult: {{ empty $testData | quote }}\n  {{- end }}\n\n  # Test: fail - explicitly fails with message\n  {{- if .Values.testFail }}\n  failResult: {{ fail (printf \"intentional fail with: %v\" $testData) }}\n  {{- end }}\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n  {{- if .Values.testCoalesce }}\n  coalesceResult: {{ coalesce $testData .Values.compareValue \"default\" | quote }}\n  {{- end }}\n\n  # Test: ternary - conditional expression\n  {{- if .Values.testTernary }}\n  ternaryResult: {{ ternary \"yes\" \"no\" $testData | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "lt",
      "dataType": "boolValue",
      "valuesOptions": {
        "values": [
          "testLt=true",
          "testDataType=boolValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": "template: test-logic-default/templates/logic-default-test.yaml:42:15: executing \"test-logic-default/templates/logic-default-test.yaml\" at \u003clt $testData .Values.compareValue\u003e: error calling lt: incompatible types for comparison: bool and float64",
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      },
      "debug": {
        "error": "template: test-logic-default/templates/logic-default-test.yaml:42:15: executing \"test-logic-default/templates/logic-default-test.yaml\" at \u003clt $testData .Values.compareValue\u003e: error calling lt: incompatible types for comparison: bool and float64",
        "debugLogs": [],
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      }
    },
    {
      "toggle": "lt",
      "dataType": "listValue",
      "valuesOptions": {
        "values": [
          "testLt=true",
          "testDataType=listValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": "template: test-logic-default/templates/logic-default-test.yaml:42:15: executing \"test-logic-default/templates/logic-default-test.yaml\" at \u003clt $testData .Values.compareValue\u003e: error calling lt: invalid type for comparison",
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      },
      "debug": {
        "error": "template: test-logic-default/templates/logic-default-test.yaml:42:15: executing \"test-logic-default/templates/logic-default-test.yaml\" at \u003clt $testData .Values.compareValue\u003e: error calling lt: invalid type for comparison",
        "debugLogs": [],
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      }
    },
    {
      "toggle": "lt",
      "dataType": "dictValue",
      "valuesOptions": {
        "values": [
          "testLt=true",
          "testDataType=dictValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": "template: test-logic-default/templates/logic-default-test.yaml:42:15: executing \"test-logic-default/templates/logic-default-test.yaml\" at \u003clt $testData .Values.compareValue\u003e: error calling lt: invalid type for comparison",
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      },
      "debug": {
        "error": "template: test-logic-default/templates/logic-default-test.yaml:42:15: executing \"test-logic-default/templates/logic-default-test.yaml\" at \u003clt $testData .Values.compareValue\u003e: error calling lt: invalid type for comparison",
        "debugLogs": [],
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      }
    },
    {
      "toggle": "le",
      "dataType": "nilValue",
      "valuesOptions": {
        "values": [
          "testLe=true",
          "testDataType=nilValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": "template: test-logic-default/templates/logic-default-test.yaml:47:15: executing \"test-logic-default/templates/logic-default-test.yaml\" at \u003cle $testData .Values.compareValue\u003e: error calling le: invalid type for comparison",
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      },
      "debug": {
        "error": "template: test-logic-default/templates/logic-default-test.yaml:47:15: executing \"test-logic-default/templates/logic-default-test.yaml\" at \u003cle $testData .Values.compareValue\u003e: error calling le: invalid type for comparison",
        "debugLogs": [],
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      }
    },
    {
      "toggle": "le",
      "dataType": "stringValue",
      "valuesOptions": {
        "values": [
          "testLe=true",
          "testDataType=stringValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": "template: test-logic-default/templates/logic-default-test.yaml:47:15: executing \"test-logic-default/templates/logic-default-test.yaml\" at \u003cle $testData .Values.compareValue\u003e: error calling le: incompatible types for comparison: string and float64",
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      },
      "debug": {
        "error": "template: test-logic-default/templates/logic-default-test.yaml:47:15: executing \"test-logic-default/templates/logic-default-test.yaml\" at \u003cle $testData .Values.compareValue\u003e: error calling le: incompatible types for comparison: string and float64",
        "debugLogs": [],
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      }
    },
    {
      "toggle": "le",
      "dataType": "numberValue",
      "valuesOptions": {
        "values": [
          "testLe=true",
          "testDataType=numberValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-logic-test\r\ndata:\r\n  # ==========================================\r\n  # Boolean Logic Functions\r\n  # ==========================================\r\n  # Test: and - returns boolean AND of arguments\r\n\r\n  # Test: or - returns boolean OR of arguments\r\n\r\n  # Test: not - returns boolean negation\r\n\r\n  # ==========================================\r\n  # Comparison Functions\r\n  # ==========================================\r\n  # Test: eq - returns true if equal\r\n\r\n  # Test: ne - returns true if not equal\r\n\r\n  # Test: lt - returns true if less than\r\n\r\n  # Test: le - returns true if less than or equal\r\n  leResult: \"false\"\r\n\r\n  # Test: gt - returns true if greater than\r\n\r\n  # Test: ge - returns true if greater than or equal\r\n\r\n  # ==========================================\r\n  # Default/Required Functions\r\n  # ==========================================\r\n  # Test: default - returns default value if empty\r\n\r\n  # Test: required - fails if value is empty\r\n\r\n  # Test: empty - returns true if value is empty\r\n\r\n  # Test: fail - explicitly fails with message\r\n\r\n  # ==========================================\r\n  # Coalesce/Ternary Functions\r\n  # ==========================================\r\n  # Test: coalesce - returns first non-empty value\r\n\r\n  # Test: ternary - conditional expression\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-logic-default/templates/logic-default-test.yaml",
            "content": "\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n\n  # Test: or - returns boolean OR of arguments\n\n  # Test: not - returns boolean negation\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n\n  # Test: ne - returns true if not equal\n\n  # Test: lt - returns true if less than\n\n  # Test: le - returns true if less than or equal\n  leResult: \"false\"\n\n  # Test: gt - returns true if greater than\n\n  # Test: ge - returns true if greater than or equal\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n\n  # Test: required - fails if value is empty\n\n  # Test: empty - returns true if value is empty\n\n  # Test: fail - explicitly fails with message\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n\n  # Test: ternary - conditional expression\n",
            "originalContent": "# Test: Logic and default functions - comprehensive testing\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n  {{- if .Values.testAnd }}\n  andResult: {{ and $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: or - returns boolean OR of arguments\n  {{- if .Values.testOr }}\n  orResult: {{ or $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: not - returns boolean negation\n  {{- if .Values.testNot }}\n  notResult: {{ not $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n  {{- if .Values.testEq }}\n  eqResult: {{ eq $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ne - returns true if not equal\n  {{- if .Values.testNe }}\n  neResult: {{ ne $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: lt - returns true if less than\n  {{- if .Values.testLt }}\n  ltResult: {{ lt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: le - returns true if less than or equal\n  {{- if .Values.testLe }}\n  leResult: {{ le $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: gt - returns true if greater than\n  {{- if .Values.testGt }}\n  gtResult: {{ gt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ge - returns true if greater than or equal\n  {{- if .Values.testGe }}\n  geResult: {{ ge $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n  {{- if .Values.testDefault }}\n  defaultResult: {{ default \"fallback\" $testData | quote }}\n  {{- end }}\n\n  # Test: required - fails if value is empty\n  {{- if .Values.testRequired }}\n  requiredResult: {{ required \"value is required\" $testData | quote }}\n  {{- end }}\n\n  # Test: empty - returns true if value is empty\n  {{- if .Values.testEmpty }}\n  emptyResult: {{ empty $testData | quote }}\n  {{- end }}\n\n  # Test: fail - explicitly fails with message\n  {{- if .Values.testFail }}\n  failResult: {{ fail (printf \"intentional fail with: %v\" $testData) }}\n  {{- end }}\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n  {{- if .Values.testCoalesce }}\n  coalesceResult: {{ coalesce $testData .Values.compareValue \"default\" | quote }}\n  {{- end }}\n\n  # Test: ternary - conditional expression\n  {{- if .Values.testTernary }}\n  ternaryResult: {{ ternary \"yes\" \"no\" $testData | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-logic-test\r\ndata:\r\n  # ==========================================\r\n  # Boolean Logic Functions\r\n  # ==========================================\r\n  # Test: and - returns boolean AND of arguments\r\n\r\n  # Test: or - returns boolean OR of arguments\r\n\r\n  # Test: not - returns boolean negation\r\n\r\n  # ==========================================\r\n  # Comparison Functions\r\n  # ==========================================\r\n  # Test: eq - returns true if equal\r\n\r\n  # Test: ne - returns true if not equal\r\n\r\n  # Test: lt - returns true if less than\r\n\r\n  # Test: le - returns true if less than or equal\r\n  leResult: \"false\"\r\n\r\n  # Test: gt - returns true if greater than\r\n\r\n  # Test: ge - returns true if greater than or equal\r\n\r\n  # ==========================================\r\n  # Default/Required Functions\r\n  # ==========================================\r\n  # Test: default - returns default value if empty\r\n\r\n  # Test: required - fails if value is empty\r\n\r\n  # Test: empty - returns true if value is empty\r\n\r\n  # Test: fail - explicitly fails with message\r\n\r\n  # ==========================================\r\n  # Coalesce/Ternary Functions\r\n  # ==========================================\r\n  # Test: coalesce - returns first non-empty value\r\n\r\n  # Test: ternary - conditional expression\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-logic-default/templates/logic-default-test.yaml",
            "content": "\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n\n  # Test: or - returns boolean OR of arguments\n\n  # Test: not - returns boolean negation\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n\n  # Test: ne - returns true if not equal\n\n  # Test: lt - returns true if less than\n\n  # Test: le - returns true if less than or equal\n  leResult: \"false\"\n\n  # Test: gt - returns true if greater than\n\n  # Test: ge - returns true if greater than or equal\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n\n  # Test: required - fails if value is empty\n\n  # Test: empty - returns true if value is empty\n\n  # Test: fail - explicitly fails with message\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n\n  # Test: ternary - conditional expression\n",
            "originalContent": "# Test: Logic and default functions - comprehensive testing\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n  {{- if .Values.testAnd }}\n  andResult: {{ and $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: or - returns boolean OR of arguments\n  {{- if .Values.testOr }}\n  orResult: {{ or $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: not - returns boolean negation\n  {{- if .Values.testNot }}\n  notResult: {{ not $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n  {{- if .Values.testEq }}\n  eqResult: {{ eq $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ne - returns true if not equal\n  {{- if .Values.testNe }}\n  neResult: {{ ne $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: lt - returns true if less than\n  {{- if .Values.testLt }}\n  ltResult: {{ lt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: le - returns true if less than or equal\n  {{- if .Values.testLe }}\n  leResult: {{ le $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: gt - returns true if greater than\n  {{- if .Values.testGt }}\n  gtResult: {{ gt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ge - returns true if greater than or equal\n  {{- if .Values.testGe }}\n  geResult: {{ ge $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n  {{- if .Values.testDefault }}\n  defaultResult: {{ default \"fallback\" $testData | quote }}\n  {{- end }}\n\n  # Test: required - fails if value is empty\n  {{- if .Values.testRequired }}\n  requiredResult: {{ required \"value is required\" $testData | quote }}\n  {{- end }}\n\n  # Test: empty - returns true if value is empty\n  {{- if .Values.testEmpty }}\n  emptyResult: {{ empty $testData | quote }}\n  {{- end }}\n\n  # Test: fail - explicitly fails with message\n  {{- if .Values.testFail }}\n  failResult: {{ fail (printf \"intentional fail with: %v\" $testData) }}\n  {{- end }}\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n  {{- if .Values.testCoalesce }}\n  coalesceResult: {{ coalesce $testData .Values.compareValue \"default\" | quote }}\n  {{- end }}\n\n  # Test: ternary - conditional expression\n  {{- if .Values.testTernary }}\n  ternaryResult: {{ ternary \"yes\" \"no\" $testData | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "le",
      "dataType": "boolValue",
      "valuesOptions": {
        "values": [
          "testLe=true",
          "testDataType=boolValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": "template: test-logic-default/templates/logic-default-test.yaml:47:15: executing \"test-logic-default/templates/logic-default-test.yaml\" at \u003cle $testData .Values.compareValue\u003e: error calling le: incompatible types for comparison: bool and float64",
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      },
      "debug": {
        "error": "template: test-logic-default/templates/logic-default-test.yaml:47:15: executing \"test-logic-default/templates/logic-default-test.yaml\" at \u003cle $testData .Values.compareValue\u003e: error calling le: incompatible types for comparison: bool and float64",
        "debugLogs": [],
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      }
    },
    {
      "toggle": "le",
      "dataType": "listValue",
      "valuesOptions": {
        "values": [
          "testLe=true",
          "testDataType=listValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": "template: test-logic-default/templates/logic-default-test.yaml:47:15: executing \"test-logic-default/templates/logic-default-test.yaml\" at \u003cle $testData .Values.compareValue\u003e: error calling le: invalid type for comparison",
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      },
      "debug": {
        "error": "template: test-logic-default/templates/logic-default-test.yaml:47:15: executing \"test-logic-default/templates/logic-default-test.yaml\" at \u003cle $testData .Values.compareValue\u003e: error calling le: invalid type for comparison",
        "debugLogs": [],
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      }
    },
    {
      "toggle": "le",
      "dataType": "dictValue",
      "valuesOptions": {
        "values": [
          "testLe=true",
          "testDataType=dictValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": "template: test-logic-default/templates/logic-default-test.yaml:47:15: executing \"test-logic-default/templates/logic-default-test.yaml\" at \u003cle $testData .Values.compareValue\u003e: error calling le: invalid type for comparison",
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      },
      "debug": {
        "error": "template: test-logic-default/templates/logic-default-test.yaml:47:15: executing \"test-logic-default/templates/logic-default-test.yaml\" at \u003cle $testData .Values.compareValue\u003e: error calling le: invalid type for comparison",
        "debugLogs": [],
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      }
    },
    {
      "toggle": "gt",
      "dataType": "nilValue",
      "valuesOptions": {
        "values": [
          "testGt=true",
          "testDataType=nilValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": "template: test-logic-default/templates/logic-default-test.yaml:52:15: executing \"test-logic-default/templates/logic-default-test.yaml\" at \u003cgt $testData .Values.compareValue\u003e: error calling gt: invalid type for comparison",
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      },
      "debug": {
        "error": "template: test-logic-default/templates/logic-default-test.yaml:52:15: executing \"test-logic-default/templates/logic-default-test.yaml\" at \u003cgt $testData .Values.compareValue\u003e: error calling gt: invalid type for comparison",
        "debugLogs": [],
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      }
    },
    {
      "toggle": "gt",
      "dataType": "stringValue",
      "valuesOptions": {
        "values": [
          "testGt=true",
          "testDataType=stringValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": "template: test-logic-default/templates/logic-default-test.yaml:52:15: executing \"test-logic-default/templates/logic-default-test.yaml\" at \u003cgt $testData .Values.compareValue\u003e: error calling gt: incompatible types for comparison: string and float64",
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      },
      "debug": {
        "error": "template: test-logic-default/templates/logic-default-test.yaml:52:15: executing \"test-logic-default/templates/logic-default-test.yaml\" at \u003cgt $testData .Values.compareValue\u003e: error calling gt: incompatible types for comparison: string and float64",
        "debugLogs": [],
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      }
    },
    {
      "toggle": "gt",
      "dataType": "numberValue",
      "valuesOptions": {
        "values": [
          "testGt=true",
          "testDataType=numberValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-logic-test\r\ndata:\r\n  # ==========================================\r\n  # Boolean Logic Functions\r\n  # ==========================================\r\n  # Test: and - returns boolean AND of arguments\r\n\r\n  # Test: or - returns boolean OR of arguments\r\n\r\n  # Test: not - returns boolean negation\r\n\r\n  # ==========================================\r\n  # Comparison Functions\r\n  # ==========================================\r\n  # Test: eq - returns true if equal\r\n\r\n  # Test: ne - returns true if not equal\r\n\r\n  # Test: lt - returns true if less than\r\n\r\n  # Test: le - returns true if less than or equal\r\n\r\n  # Test: gt - returns true if greater than\r\n  gtResult: \"true\"\r\n\r\n  # Test: ge - returns true if greater than or equal\r\n\r\n  # ==========================================\r\n  # Default/Required Functions\r\n  # ==========================================\r\n  # Test: default - returns default value if empty\r\n\r\n  # Test: required - fails if value is empty\r\n\r\n  # Test: empty - returns true if value is empty\r\n\r\n  # Test: fail - explicitly fails with message\r\n\r\n  # ==========================================\r\n  # Coalesce/Ternary Functions\r\n  # ==========================================\r\n  # Test: coalesce - returns first non-empty value\r\n\r\n  # Test: ternary - conditional expression\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-logic-default/templates/logic-default-test.yaml",
            "content": "\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n\n  # Test: or - returns boolean OR of arguments\n\n  # Test: not - returns boolean negation\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n\n  # Test: ne - returns true if not equal\n\n  # Test: lt - returns true if less than\n\n  # Test: le - returns true if less than or equal\n\n  # Test: gt - returns true if greater than\n  gtResult: \"true\"\n\n  # Test: ge - returns true if greater than or equal\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n\n  # Test: required - fails if value is empty\n\n  # Test: empty - returns true if value is empty\n\n  # Test: fail - explicitly fails with message\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n\n  # Test: ternary - conditional expression\n",
            "originalContent": "# Test: Logic and default functions - comprehensive testing\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n  {{- if .Values.testAnd }}\n  andResult: {{ and $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: or - returns boolean OR of arguments\n  {{- if .Values.testOr }}\n  orResult: {{ or $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: not - returns boolean negation\n  {{- if .Values.testNot }}\n  notResult: {{ not $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n  {{- if .Values.testEq }}\n  eqResult: {{ eq $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ne - returns true if not equal\n  {{- if .Values.testNe }}\n  neResult: {{ ne $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: lt - returns true if less than\n  {{- if .Values.testLt }}\n  ltResult: {{ lt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: le - returns true if less than or equal\n  {{- if .Values.testLe }}\n  leResult: {{ le $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: gt - returns true if greater than\n  {{- if .Values.testGt }}\n  gtResult: {{ gt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ge - returns true if greater than or equal\n  {{- if .Values.testGe }}\n  geResult: {{ ge $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n  {{- if .Values.testDefault }}\n  defaultResult: {{ default \"fallback\" $testData | quote }}\n  {{- end }}\n\n  # Test: required - fails if value is empty\n  {{- if .Values.testRequired }}\n  requiredResult: {{ required \"value is required\" $testData | quote }}\n  {{- end }}\n\n  # Test: empty - returns true if value is empty\n  {{- if .Values.testEmpty }}\n  emptyResult: {{ empty $testData | quote }}\n  {{- end }}\n\n  # Test: fail - explicitly fails with message\n  {{- if .Values.testFail }}\n  failResult: {{ fail (printf \"intentional fail with: %v\" $testData) }}\n  {{- end }}\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n  {{- if .Values.testCoalesce }}\n  coalesceResult: {{ coalesce $testData .Values.compareValue \"default\" | quote }}\n  {{- end }}\n\n  # Test: ternary - conditional expression\n  {{- if .Values.testTernary }}\n  ternaryResult: {{ ternary \"yes\" \"no\" $testData | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-logic-test\r\ndata:\r\n  # ==========================================\r\n  # Boolean Logic Functions\r\n  # ==========================================\r\n  # Test: and - returns boolean AND of arguments\r\n\r\n  # Test: or - returns boolean OR of arguments\r\n\r\n  # Test: not - returns boolean negation\r\n\r\n  # ==========================================\r\n  # Comparison Functions\r\n  # ==========================================\r\n  # Test: eq - returns true if equal\r\n\r\n  # Test: ne - returns true if not equal\r\n\r\n  # Test: lt - returns true if less than\r\n\r\n  # Test: le - returns true if less than or equal\r\n\r\n  # Test: gt - returns true if greater than\r\n  gtResult: \"true\"\r\n\r\n  # Test: ge - returns true if greater than or equal\r\n\r\n  # ==========================================\r\n  # Default/Required Functions\r\n  # ==========================================\r\n  # Test: default - returns default value if empty\r\n\r\n  # Test: required - fails if value is empty\r\n\r\n  # Test: empty - returns true if value is empty\r\n\r\n  # Test: fail - explicitly fails with message\r\n\r\n  # ==========================================\r\n  # Coalesce/Ternary Functions\r\n  # ==========================================\r\n  # Test: coalesce - returns first non-empty value\r\n\r\n  # Test: ternary - conditional expression\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-logic-default/templates/logic-default-test.yaml",
            "content": "\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n\n  # Test: or - returns boolean OR of arguments\n\n  # Test: not - returns boolean negation\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n\n  # Test: ne - returns true if not equal\n\n  # Test: lt - returns true if less than\n\n  # Test: le - returns true if less than or equal\n\n  # Test: gt - returns true if greater than\n  gtResult: \"true\"\n\n  # Test: ge - returns true if greater than or equal\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n\n  # Test: required - fails if value is empty\n\n  # Test: empty - returns true if value is empty\n\n  # Test: fail - explicitly fails with message\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n\n  # Test: ternary - conditional expression\n",
            "originalContent": "# Test: Logic and default functions - comprehensive testing\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n  {{- if .Values.testAnd }}\n  andResult: {{ and $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: or - returns boolean OR of arguments\n  {{- if .Values.testOr }}\n  orResult: {{ or $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: not - returns boolean negation\n  {{- if .Values.testNot }}\n  notResult: {{ not $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n  {{- if .Values.testEq }}\n  eqResult: {{ eq $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ne - returns true if not equal\n  {{- if .Values.testNe }}\n  neResult: {{ ne $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: lt - returns true if less than\n  {{- if .Values.testLt }}\n  ltResult: {{ lt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: le - returns true if less than or equal\n  {{- if .Values.testLe }}\n  leResult: {{ le $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: gt - returns true if greater than\n  {{- if .Values.testGt }}\n  gtResult: {{ gt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ge - returns true if greater than or equal\n  {{- if .Values.testGe }}\n  geResult: {{ ge $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n  {{- if .Values.testDefault }}\n  defaultResult: {{ default \"fallback\" $testData | quote }}\n  {{- end }}\n\n  # Test: required - fails if value is empty\n  {{- if .Values.testRequired }}\n  requiredResult: {{ required \"value is required\" $testData | quote }}\n  {{- end }}\n\n  # Test: empty - returns true if value is empty\n  {{- if .Values.testEmpty }}\n  emptyResult: {{ empty $testData | quote }}\n  {{- end }}\n\n  # Test: fail - explicitly fails with message\n  {{- if .Values.testFail }}\n  failResult: {{ fail (printf \"intentional fail with: %v\" $testData) }}\n  {{- end }}\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n  {{- if .Values.testCoalesce }}\n  coalesceResult: {{ coalesce $testData .Values.compareValue \"default\" | quote }}\n  {{- end }}\n\n  # Test: ternary - conditional expression\n  {{- if .Values.testTernary }}\n  ternaryResult: {{ ternary \"yes\" \"no\" $testData | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "gt",
      "dataType": "boolValue",
      "valuesOptions": {
        "values": [
          "testGt=true",
          "testDataType=boolValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": "template: test-logic-default/templates/logic-default-test.yaml:52:15: executing \"test-logic-default/templates/logic-default-test.yaml\" at \u003cgt $testData .Values.compareValue\u003e: error calling gt: incompatible types for comparison: bool and float64",
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      },
      "debug": {
        "error": "template: test-logic-default/templates/logic-default-test.yaml:52:15: executing \"test-logic-default/templates/logic-default-test.yaml\" at \u003cgt $testData .Values.compareValue\u003e: error calling gt: incompatible types for comparison: bool and float64",
        "debugLogs": [],
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      }
    },
    {
      "toggle": "gt",
      "dataType": "listValue",
      "valuesOptions": {
        "values": [
          "testGt=true",
          "testDataType=listValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": "template: test-logic-default/templates/logic-default-test.yaml:52:15: executing \"test-logic-default/templates/logic-default-test.yaml\" at \u003cgt $testData .Values.compareValue\u003e: error calling gt: invalid type for comparison",
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      },
      "debug": {
        "error": "template: test-logic-default/templates/logic-default-test.yaml:52:15: executing \"test-logic-default/templates/logic-default-test.yaml\" at \u003cgt $testData .Values.compareValue\u003e: error calling gt: invalid type for comparison",
        "debugLogs": [],
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      }
    },
    {
      "toggle": "gt",
      "dataType": "dictValue",
      "valuesOptions": {
        "values": [
          "testGt=true",
          "testDataType=dictValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": "template: test-logic-default/templates/logic-default-test.yaml:52:15: executing \"test-logic-default/templates/logic-default-test.yaml\" at \u003cgt $testData .Values.compareValue\u003e: error calling gt: invalid type for comparison",
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      },
      "debug": {
        "error": "template: test-logic-default/templates/logic-default-test.yaml:52:15: executing \"test-logic-default/templates/logic-default-test.yaml\" at \u003cgt $testData .Values.compareValue\u003e: error calling gt: invalid type for comparison",
        "debugLogs": [],
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      }
    },
    {
      "toggle": "ge",
      "dataType": "nilValue",
      "valuesOptions": {
        "values": [
          "testGe=true",
          "testDataType=nilValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": "template: test-logic-default/templates/logic-default-test.yaml:57:15: executing \"test-logic-default/templates/logic-default-test.yaml\" at \u003cge $testData .Values.compareValue\u003e: error calling ge: invalid type for comparison",
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      },
      "debug": {
        "error": "template: test-logic-default/templates/logic-default-test.yaml:57:15: executing \"test-logic-default/templates/logic-default-test.yaml\" at \u003cge $testData .Values.compareValue\u003e: error calling ge: invalid type for comparison",
        "debugLogs": [],
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      }
    },
    {
      "toggle": "ge",
      "dataType": "stringValue",
      "valuesOptions": {
        "values": [
          "testGe=true",
          "testDataType=stringValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": "template: test-logic-default/templates/logic-default-test.yaml:57:15: executing \"test-logic-default/templates/logic-default-test.yaml\" at \u003cge $testData .Values.compareValue\u003e: error calling ge: incompatible types for comparison: string and float64",
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      },
      "debug": {
        "error": "template: test-logic-default/templates/logic-default-test.yaml:57:15: executing \"test-logic-default/templates/logic-default-test.yaml\" at \u003cge $testData .Values.compareValue\u003e: error calling ge: incompatible types for comparison: string and float64",
        "debugLogs": [],
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      }
    },
    {
      "toggle": "ge",
      "dataType": "numberValue",
      "valuesOptions": {
        "values": [
          "testGe=true",
          "testDataType=numberValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-logic-test\r\ndata:\r\n  # ==========================================\r\n  # Boolean Logic Functions\r\n  # ==========================================\r\n  # Test: and - returns boolean AND of arguments\r\n\r\n  # Test: or - returns boolean OR of arguments\r\n\r\n  # Test: not - returns boolean negation\r\n\r\n  # ==========================================\r\n  # Comparison Functions\r\n  # ==========================================\r\n  # Test: eq - returns true if equal\r\n\r\n  # Test: ne - returns true if not equal\r\n\r\n  # Test: lt - returns true if less than\r\n\r\n  # Test: le - returns true if less than or equal\r\n\r\n  # Test: gt - returns true if greater than\r\n\r\n  # Test: ge - returns true if greater than or equal\r\n  geResult: \"true\"\r\n\r\n  # ==========================================\r\n  # Default/Required Functions\r\n  # ==========================================\r\n  # Test: default - returns default value if empty\r\n\r\n  # Test: required - fails if value is empty\r\n\r\n  # Test: empty - returns true if value is empty\r\n\r\n  # Test: fail - explicitly fails with message\r\n\r\n  # ==========================================\r\n  # Coalesce/Ternary Functions\r\n  # ==========================================\r\n  # Test: coalesce - returns first non-empty value\r\n\r\n  # Test: ternary - conditional expression\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-logic-default/templates/logic-default-test.yaml",
            "content": "\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n\n  # Test: or - returns boolean OR of arguments\n\n  # Test: not - returns boolean negation\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n\n  # Test: ne - returns true if not equal\n\n  # Test: lt - returns true if less than\n\n  # Test: le - returns true if less than or equal\n\n  # Test: gt - returns true if greater than\n\n  # Test: ge - returns true if greater than or equal\n  geResult: \"true\"\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n\n  # Test: required - fails if value is empty\n\n  # Test: empty - returns true if value is empty\n\n  # Test: fail - explicitly fails with message\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n\n  # Test: ternary - conditional expression\n",
            "originalContent": "# Test: Logic and default functions - comprehensive testing\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n  {{- if .Values.testAnd }}\n  andResult: {{ and $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: or - returns boolean OR of arguments\n  {{- if .Values.testOr }}\n  orResult: {{ or $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: not - returns boolean negation\n  {{- if .Values.testNot }}\n  notResult: {{ not $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n  {{- if .Values.testEq }}\n  eqResult: {{ eq $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ne - returns true if not equal\n  {{- if .Values.testNe }}\n  neResult: {{ ne $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: lt - returns true if less than\n  {{- if .Values.testLt }}\n  ltResult: {{ lt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: le - returns true if less than or equal\n  {{- if .Values.testLe }}\n  leResult: {{ le $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: gt - returns true if greater than\n  {{- if .Values.testGt }}\n  gtResult: {{ gt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ge - returns true if greater than or equal\n  {{- if .Values.testGe }}\n  geResult: {{ ge $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n  {{- if .Values.testDefault }}\n  defaultResult: {{ default \"fallback\" $testData | quote }}\n  {{- end }}\n\n  # Test: required - fails if value is empty\n  {{- if .Values.testRequired }}\n  requiredResult: {{ required \"value is required\" $testData | quote }}\n  {{- end }}\n\n  # Test: empty - returns true if value is empty\n  {{- if .Values.testEmpty }}\n  emptyResult: {{ empty $testData | quote }}\n  {{- end }}\n\n  # Test: fail - explicitly fails with message\n  {{- if .Values.testFail }}\n  failResult: {{ fail (printf \"intentional fail with: %v\" $testData) }}\n  {{- end }}\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n  {{- if .Values.testCoalesce }}\n  coalesceResult: {{ coalesce $testData .Values.compareValue \"default\" | quote }}\n  {{- end }}\n\n  # Test: ternary - conditional expression\n  {{- if .Values.testTernary }}\n  ternaryResult: {{ ternary \"yes\" \"no\" $testData | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-logic-test\r\ndata:\r\n  # ==========================================\r\n  # Boolean Logic Functions\r\n  # ==========================================\r\n  # Test: and - returns boolean AND of arguments\r\n\r\n  # Test: or - returns boolean OR of arguments\r\n\r\n  # Test: not - returns boolean negation\r\n\r\n  # ==========================================\r\n  # Comparison Functions\r\n  # ==========================================\r\n  # Test: eq - returns true if equal\r\n\r\n  # Test: ne - returns true if not equal\r\n\r\n  # Test: lt - returns true if less than\r\n\r\n  # Test: le - returns true if less than or equal\r\n\r\n  # Test: gt - returns true if greater than\r\n\r\n  # Test: ge - returns true if greater than or equal\r\n  geResult: \"true\"\r\n\r\n  # ==========================================\r\n  # Default/Required Functions\r\n  # ==========================================\r\n  # Test: default - returns default value if empty\r\n\r\n  # Test: required - fails if value is empty\r\n\r\n  # Test: empty - returns true if value is empty\r\n\r\n  # Test: fail - explicitly fails with message\r\n\r\n  # ==========================================\r\n  # Coalesce/Ternary Functions\r\n  # ==========================================\r\n  # Test: coalesce - returns first non-empty value\r\n\r\n  # Test: ternary - conditional expression\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-logic-default/templates/logic-default-test.yaml",
            "content": "\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n\n  # Test: or - returns boolean OR of arguments\n\n  # Test: not - returns boolean negation\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n\n  # Test: ne - returns true if not equal\n\n  # Test: lt - returns true if less than\n\n  # Test: le - returns true if less than or equal\n\n  # Test: gt - returns true if greater than\n\n  # Test: ge - returns true if greater than or equal\n  geResult: \"true\"\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n\n  # Test: required - fails if value is empty\n\n  # Test: empty - returns true if value is empty\n\n  # Test: fail - explicitly fails with message\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n\n  # Test: ternary - conditional expression\n",
            "originalContent": "# Test: Logic and default functions - comprehensive testing\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n  {{- if .Values.testAnd }}\n  andResult: {{ and $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: or - returns boolean OR of arguments\n  {{- if .Values.testOr }}\n  orResult: {{ or $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: not - returns boolean negation\n  {{- if .Values.testNot }}\n  notResult: {{ not $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n  {{- if .Values.testEq }}\n  eqResult: {{ eq $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ne - returns true if not equal\n  {{- if .Values.testNe }}\n  neResult: {{ ne $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: lt - returns true if less than\n  {{- if .Values.testLt }}\n  ltResult: {{ lt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: le - returns true if less than or equal\n  {{- if .Values.testLe }}\n  leResult: {{ le $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: gt - returns true if greater than\n  {{- if .Values.testGt }}\n  gtResult: {{ gt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ge - returns true if greater than or equal\n  {{- if .Values.testGe }}\n  geResult: {{ ge $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n  {{- if .Values.testDefault }}\n  defaultResult: {{ default \"fallback\" $testData | quote }}\n  {{- end }}\n\n  # Test: required - fails if value is empty\n  {{- if .Values.testRequired }}\n  requiredResult: {{ required \"value is required\" $testData | quote }}\n  {{- end }}\n\n  # Test: empty - returns true if value is empty\n  {{- if .Values.testEmpty }}\n  emptyResult: {{ empty $testData | quote }}\n  {{- end }}\n\n  # Test: fail - explicitly fails with message\n  {{- if .Values.testFail }}\n  failResult: {{ fail (printf \"intentional fail with: %v\" $testData) }}\n  {{- end }}\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n  {{- if .Values.testCoalesce }}\n  coalesceResult: {{ coalesce $testData .Values.compareValue \"default\" | quote }}\n  {{- end }}\n\n  # Test: ternary - conditional expression\n  {{- if .Values.testTernary }}\n  ternaryResult: {{ ternary \"yes\" \"no\" $testData | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "ge",
      "dataType": "boolValue",
      "valuesOptions": {
        "values": [
          "testGe=true",
          "testDataType=boolValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": "template: test-logic-default/templates/logic-default-test.yaml:57:15: executing \"test-logic-default/templates/logic-default-test.yaml\" at \u003cge $testData .Values.compareValue\u003e: error calling ge: incompatible types for comparison: bool and float64",
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      },
      "debug": {
        "error": "template: test-logic-default/templates/logic-default-test.yaml:57:15: executing \"test-logic-default/templates/logic-default-test.yaml\" at \u003cge $testData .Values.compareValue\u003e: error calling ge: incompatible types for comparison: bool and float64",
        "debugLogs": [],
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      }
    },
    {
      "toggle": "ge",
      "dataType": "listValue",
      "valuesOptions": {
        "values": [
          "testGe=true",
          "testDataType=listValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": "template: test-logic-default/templates/logic-default-test.yaml:57:15: executing \"test-logic-default/templates/logic-default-test.yaml\" at \u003cge $testData .Values.compareValue\u003e: error calling ge: invalid type for comparison",
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      },
      "debug": {
        "error": "template: test-logic-default/templates/logic-default-test.yaml:57:15: executing \"test-logic-default/templates/logic-default-test.yaml\" at \u003cge $testData .Values.compareValue\u003e: error calling ge: invalid type for comparison",
        "debugLogs": [],
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      }
    },
    {
      "toggle": "ge",
      "dataType": "dictValue",
      "valuesOptions": {
        "values": [
          "testGe=true",
          "testDataType=dictValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": "template: test-logic-default/templates/logic-default-test.yaml:57:15: executing \"test-logic-default/templates/logic-default-test.yaml\" at \u003cge $testData .Values.compareValue\u003e: error calling ge: invalid type for comparison",
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      },
      "debug": {
        "error": "template: test-logic-default/templates/logic-default-test.yaml:57:15: executing \"test-logic-default/templates/logic-default-test.yaml\" at \u003cge $testData .Values.compareValue\u003e: error calling ge: invalid type for comparison",
        "debugLogs": [],
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      }
    },
    {
      "toggle": "default",
      "dataType": "nilValue",
      "valuesOptions": {
        "values": [
          "testDefault=true",
          "testDataType=nilValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-logic-test\r\ndata:\r\n  # ==========================================\r\n  # Boolean Logic Functions\r\n  # ==========================================\r\n  # Test: and - returns boolean AND of arguments\r\n\r\n  # Test: or - returns boolean OR of arguments\r\n\r\n  # Test: not - returns boolean negation\r\n\r\n  # ==========================================\r\n  # Comparison Functions\r\n  # ==========================================\r\n  # Test: eq - returns true if equal\r\n\r\n  # Test: ne - returns true if not equal\r\n\r\n  # Test: lt - returns true if less than\r\n\r\n  # Test: le - returns true if less than or equal\r\n\r\n  # Test: gt - returns true if greater than\r\n\r\n  # Test: ge - returns true if greater than or equal\r\n\r\n  # ==========================================\r\n  # Default/Required Functions\r\n  # ==========================================\r\n  # Test: default - returns default value if empty\r\n  defaultResult: \"fallback\"\r\n\r\n  # Test: required - fails if value is empty\r\n\r\n  # Test: empty - returns true if value is empty\r\n\r\n  # Test: fail - explicitly fails with message\r\n\r\n  # ==========================================\r\n  # Coalesce/Ternary Functions\r\n  # ==========================================\r\n  # Test: coalesce - returns first non-empty value\r\n\r\n  # Test: ternary - conditional expression\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-logic-default/templates/logic-default-test.yaml",
            "content": "\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n\n  # Test: or - returns boolean OR of arguments\n\n  # Test: not - returns boolean negation\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n\n  # Test: ne - returns true if not equal\n\n  # Test: lt - returns true if less than\n\n  # Test: le - returns true if less than or equal\n\n  # Test: gt - returns true if greater than\n\n  # Test: ge - returns true if greater than or equal\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n  defaultResult: \"fallback\"\n\n  # Test: required - fails if value is empty\n\n  # Test: empty - returns true if value is empty\n\n  # Test: fail - explicitly fails with message\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n\n  # Test: ternary - conditional expression\n",
            "originalContent": "# Test: Logic and default functions - comprehensive testing\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n  {{- if .Values.testAnd }}\n  andResult: {{ and $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: or - returns boolean OR of arguments\n  {{- if .Values.testOr }}\n  orResult: {{ or $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: not - returns boolean negation\n  {{- if .Values.testNot }}\n  notResult: {{ not $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n  {{- if .Values.testEq }}\n  eqResult: {{ eq $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ne - returns true if not equal\n  {{- if .Values.testNe }}\n  neResult: {{ ne $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: lt - returns true if less than\n  {{- if .Values.testLt }}\n  ltResult: {{ lt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: le - returns true if less than or equal\n  {{- if .Values.testLe }}\n  leResult: {{ le $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: gt - returns true if greater than\n  {{- if .Values.testGt }}\n  gtResult: {{ gt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ge - returns true if greater than or equal\n  {{- if .Values.testGe }}\n  geResult: {{ ge $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n  {{- if .Values.testDefault }}\n  defaultResult: {{ default \"fallback\" $testData | quote }}\n  {{- end }}\n\n  # Test: required - fails if value is empty\n  {{- if .Values.testRequired }}\n  requiredResult: {{ required \"value is required\" $testData | quote }}\n  {{- end }}\n\n  # Test: empty - returns true if value is empty\n  {{- if .Values.testEmpty }}\n  emptyResult: {{ empty $testData | quote }}\n  {{- end }}\n\n  # Test: fail - explicitly fails with message\n  {{- if .Values.testFail }}\n  failResult: {{ fail (printf \"intentional fail with: %v\" $testData) }}\n  {{- end }}\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n  {{- if .Values.testCoalesce }}\n  coalesceResult: {{ coalesce $testData .Values.compareValue \"default\" | quote }}\n  {{- end }}\n\n  # Test: ternary - conditional expression\n  {{- if .Values.testTernary }}\n  ternaryResult: {{ ternary \"yes\" \"no\" $testData | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-logic-test\r\ndata:\r\n  # ==========================================\r\n  # Boolean Logic Functions\r\n  # ==========================================\r\n  # Test: and - returns boolean AND of arguments\r\n\r\n  # Test: or - returns boolean OR of arguments\r\n\r\n  # Test: not - returns boolean negation\r\n\r\n  # ==========================================\r\n  # Comparison Functions\r\n  # ==========================================\r\n  # Test: eq - returns true if equal\r\n\r\n  # Test: ne - returns true if not equal\r\n\r\n  # Test: lt - returns true if less than\r\n\r\n  # Test: le - returns true if less than or equal\r\n\r\n  # Test: gt - returns true if greater than\r\n\r\n  # Test: ge - returns true if greater than or equal\r\n\r\n  # ==========================================\r\n  # Default/Required Functions\r\n  # ==========================================\r\n  # Test: default - returns default value if empty\r\n  defaultResult: \"fallback\"\r\n\r\n  # Test: required - fails if value is empty\r\n\r\n  # Test: empty - returns true if value is empty\r\n\r\n  # Test: fail - explicitly fails with message\r\n\r\n  # ==========================================\r\n  # Coalesce/Ternary Functions\r\n  # ==========================================\r\n  # Test: coalesce - returns first non-empty value\r\n\r\n  # Test: ternary - conditional expression\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-logic-default/templates/logic-default-test.yaml",
            "content": "\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n\n  # Test: or - returns boolean OR of arguments\n\n  # Test: not - returns boolean negation\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n\n  # Test: ne - returns true if not equal\n\n  # Test: lt - returns true if less than\n\n  # Test: le - returns true if less than or equal\n\n  # Test: gt - returns true if greater than\n\n  # Test: ge - returns true if greater than or equal\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n  defaultResult: \"fallback\"\n\n  # Test: required - fails if value is empty\n\n  # Test: empty - returns true if value is empty\n\n  # Test: fail - explicitly fails with message\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n\n  # Test: ternary - conditional expression\n",
            "originalContent": "# Test: Logic and default functions - comprehensive testing\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n  {{- if .Values.testAnd }}\n  andResult: {{ and $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: or - returns boolean OR of arguments\n  {{- if .Values.testOr }}\n  orResult: {{ or $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: not - returns boolean negation\n  {{- if .Values.testNot }}\n  notResult: {{ not $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n  {{- if .Values.testEq }}\n  eqResult: {{ eq $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ne - returns true if not equal\n  {{- if .Values.testNe }}\n  neResult: {{ ne $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: lt - returns true if less than\n  {{- if .Values.testLt }}\n  ltResult: {{ lt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: le - returns true if less than or equal\n  {{- if .Values.testLe }}\n  leResult: {{ le $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: gt - returns true if greater than\n  {{- if .Values.testGt }}\n  gtResult: {{ gt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ge - returns true if greater than or equal\n  {{- if .Values.testGe }}\n  geResult: {{ ge $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n  {{- if .Values.testDefault }}\n  defaultResult: {{ default \"fallback\" $testData | quote }}\n  {{- end }}\n\n  # Test: required - fails if value is empty\n  {{- if .Values.testRequired }}\n  requiredResult: {{ required \"value is required\" $testData | quote }}\n  {{- end }}\n\n  # Test: empty - returns true if value is empty\n  {{- if .Values.testEmpty }}\n  emptyResult: {{ empty $testData | quote }}\n  {{- end }}\n\n  # Test: fail - explicitly fails with message\n  {{- if .Values.testFail }}\n  failResult: {{ fail (printf \"intentional fail with: %v\" $testData) }}\n  {{- end }}\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n  {{- if .Values.testCoalesce }}\n  coalesceResult: {{ coalesce $testData .Values.compareValue \"default\" | quote }}\n  {{- end }}\n\n  # Test: ternary - conditional expression\n  {{- if .Values.testTernary }}\n  ternaryResult: {{ ternary \"yes\" \"no\" $testData | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "default",
      "dataType": "stringValue",
      "valuesOptions": {
        "values": [
          "testDefault=true",
          "testDataType=stringValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-logic-test\r\ndata:\r\n  # ==========================================\r\n  # Boolean Logic Functions\r\n  # ==========================================\r\n  # Test: and - returns boolean AND of arguments\r\n\r\n  # Test: or - returns boolean OR of arguments\r\n\r\n  # Test: not - returns boolean negation\r\n\r\n  # ==========================================\r\n  # Comparison Functions\r\n  # ==========================================\r\n  # Test: eq - returns true if equal\r\n\r\n  # Test: ne - returns true if not equal\r\n\r\n  # Test: lt - returns true if less than\r\n\r\n  # Test: le - returns true if less than or equal\r\n\r\n  # Test: gt - returns true if greater than\r\n\r\n  # Test: ge - returns true if greater than or equal\r\n\r\n  # ==========================================\r\n  # Default/Required Functions\r\n  # ==========================================\r\n  # Test: default - returns default value if empty\r\n  defaultResult: \"test string\"\r\n\r\n  # Test: required - fails if value is empty\r\n\r\n  # Test: empty - returns true if value is empty\r\n\r\n  # Test: fail - explicitly fails with message\r\n\r\n  # ==========================================\r\n  # Coalesce/Ternary Functions\r\n  # ==========================================\r\n  # Test: coalesce - returns first non-empty value\r\n\r\n  # Test: ternary - conditional expression\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-logic-default/templates/logic-default-test.yaml",
            "content": "\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n\n  # Test: or - returns boolean OR of arguments\n\n  # Test: not - returns boolean negation\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n\n  # Test: ne - returns true if not equal\n\n  # Test: lt - returns true if less than\n\n  # Test: le - returns true if less than or equal\n\n  # Test: gt - returns true if greater than\n\n  # Test: ge - returns true if greater than or equal\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n  defaultResult: \"test string\"\n\n  # Test: required - fails if value is empty\n\n  # Test: empty - returns true if value is empty\n\n  # Test: fail - explicitly fails with message\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n\n  # Test: ternary - conditional expression\n",
            "originalContent": "# Test: Logic and default functions - comprehensive testing\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n  {{- if .Values.testAnd }}\n  andResult: {{ and $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: or - returns boolean OR of arguments\n  {{- if .Values.testOr }}\n  orResult: {{ or $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: not - returns boolean negation\n  {{- if .Values.testNot }}\n  notResult: {{ not $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n  {{- if .Values.testEq }}\n  eqResult: {{ eq $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ne - returns true if not equal\n  {{- if .Values.testNe }}\n  neResult: {{ ne $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: lt - returns true if less than\n  {{- if .Values.testLt }}\n  ltResult: {{ lt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: le - returns true if less than or equal\n  {{- if .Values.testLe }}\n  leResult: {{ le $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: gt - returns true if greater than\n  {{- if .Values.testGt }}\n  gtResult: {{ gt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ge - returns true if greater than or equal\n  {{- if .Values.testGe }}\n  geResult: {{ ge $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n  {{- if .Values.testDefault }}\n  defaultResult: {{ default \"fallback\" $testData | quote }}\n  {{- end }}\n\n  # Test: required - fails if value is empty\n  {{- if .Values.testRequired }}\n  requiredResult: {{ required \"value is required\" $testData | quote }}\n  {{- end }}\n\n  # Test: empty - returns true if value is empty\n  {{- if .Values.testEmpty }}\n  emptyResult: {{ empty $testData | quote }}\n  {{- end }}\n\n  # Test: fail - explicitly fails with message\n  {{- if .Values.testFail }}\n  failResult: {{ fail (printf \"intentional fail with: %v\" $testData) }}\n  {{- end }}\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n  {{- if .Values.testCoalesce }}\n  coalesceResult: {{ coalesce $testData .Values.compareValue \"default\" | quote }}\n  {{- end }}\n\n  # Test: ternary - conditional expression\n  {{- if .Values.testTernary }}\n  ternaryResult: {{ ternary \"yes\" \"no\" $testData | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-logic-test\r\ndata:\r\n  # ==========================================\r\n  # Boolean Logic Functions\r\n  # ==========================================\r\n  # Test: and - returns boolean AND of arguments\r\n\r\n  # Test: or - returns boolean OR of arguments\r\n\r\n  # Test: not - returns boolean negation\r\n\r\n  # ==========================================\r\n  # Comparison Functions\r\n  # ==========================================\r\n  # Test: eq - returns true if equal\r\n\r\n  # Test: ne - returns true if not equal\r\n\r\n  # Test: lt - returns true if less than\r\n\r\n  # Test: le - returns true if less than or equal\r\n\r\n  # Test: gt - returns true if greater than\r\n\r\n  # Test: ge - returns true if greater than or equal\r\n\r\n  # ==========================================\r\n  # Default/Required Functions\r\n  # ==========================================\r\n  # Test: default - returns default value if empty\r\n  defaultResult: \"test string\"\r\n\r\n  # Test: required - fails if value is empty\r\n\r\n  # Test: empty - returns true if value is empty\r\n\r\n  # Test: fail - explicitly fails with message\r\n\r\n  # ==========================================\r\n  # Coalesce/Ternary Functions\r\n  # ==========================================\r\n  # Test: coalesce - returns first non-empty value\r\n\r\n  # Test: ternary - conditional expression\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-logic-default/templates/logic-default-test.yaml",
            "content": "\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n\n  # Test: or - returns boolean OR of arguments\n\n  # Test: not - returns boolean negation\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n\n  # Test: ne - returns true if not equal\n\n  # Test: lt - returns true if less than\n\n  # Test: le - returns true if less than or equal\n\n  # Test: gt - returns true if greater than\n\n  # Test: ge - returns true if greater than or equal\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n  defaultResult: \"test string\"\n\n  # Test: required - fails if value is empty\n\n  # Test: empty - returns true if value is empty\n\n  # Test: fail - explicitly fails with message\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n\n  # Test: ternary - conditional expression\n",
            "originalContent": "# Test: Logic and default functions - comprehensive testing\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n  {{- if .Values.testAnd }}\n  andResult: {{ and $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: or - returns boolean OR of arguments\n  {{- if .Values.testOr }}\n  orResult: {{ or $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: not - returns boolean negation\n  {{- if .Values.testNot }}\n  notResult: {{ not $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n  {{- if .Values.testEq }}\n  eqResult: {{ eq $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ne - returns true if not equal\n  {{- if .Values.testNe }}\n  neResult: {{ ne $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: lt - returns true if less than\n  {{- if .Values.testLt }}\n  ltResult: {{ lt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: le - returns true if less than or equal\n  {{- if .Values.testLe }}\n  leResult: {{ le $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: gt - returns true if greater than\n  {{- if .Values.testGt }}\n  gtResult: {{ gt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ge - returns true if greater than or equal\n  {{- if .Values.testGe }}\n  geResult: {{ ge $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n  {{- if .Values.testDefault }}\n  defaultResult: {{ default \"fallback\" $testData | quote }}\n  {{- end }}\n\n  # Test: required - fails if value is empty\n  {{- if .Values.testRequired }}\n  requiredResult: {{ required \"value is required\" $testData | quote }}\n  {{- end }}\n\n  # Test: empty - returns true if value is empty\n  {{- if .Values.testEmpty }}\n  emptyResult: {{ empty $testData | quote }}\n  {{- end }}\n\n  # Test: fail - explicitly fails with message\n  {{- if .Values.testFail }}\n  failResult: {{ fail (printf \"intentional fail with: %v\" $testData) }}\n  {{- end }}\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n  {{- if .Values.testCoalesce }}\n  coalesceResult: {{ coalesce $testData .Values.compareValue \"default\" | quote }}\n  {{- end }}\n\n  # Test: ternary - conditional expression\n  {{- if .Values.testTernary }}\n  ternaryResult: {{ ternary \"yes\" \"no\" $testData | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "default",
      "dataType": "numberValue",
      "valuesOptions": {
        "values": [
          "testDefault=true",
          "testDataType=numberValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-logic-test\r\ndata:\r\n  # ==========================================\r\n  # Boolean Logic Functions\r\n  # ==========================================\r\n  # Test: and - returns boolean AND of arguments\r\n\r\n  # Test: or - returns boolean OR of arguments\r\n\r\n  # Test: not - returns boolean negation\r\n\r\n  # ==========================================\r\n  # Comparison Functions\r\n  # ==========================================\r\n  # Test: eq - returns true if equal\r\n\r\n  # Test: ne - returns true if not equal\r\n\r\n  # Test: lt - returns true if less than\r\n\r\n  # Test: le - returns true if less than or equal\r\n\r\n  # Test: gt - returns true if greater than\r\n\r\n  # Test: ge - returns true if greater than or equal\r\n\r\n  # ==========================================\r\n  # Default/Required Functions\r\n  # ==========================================\r\n  # Test: default - returns default value if empty\r\n  defaultResult: \"42\"\r\n\r\n  # Test: required - fails if value is empty\r\n\r\n  # Test: empty - returns true if value is empty\r\n\r\n  # Test: fail - explicitly fails with message\r\n\r\n  # ==========================================\r\n  # Coalesce/Ternary Functions\r\n  # ==========================================\r\n  # Test: coalesce - returns first non-empty value\r\n\r\n  # Test: ternary - conditional expression\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-logic-default/templates/logic-default-test.yaml",
            "content": "\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n\n  # Test: or - returns boolean OR of arguments\n\n  # Test: not - returns boolean negation\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n\n  # Test: ne - returns true if not equal\n\n  # Test: lt - returns true if less than\n\n  # Test: le - returns true if less than or equal\n\n  # Test: gt - returns true if greater than\n\n  # Test: ge - returns true if greater than or equal\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n  defaultResult: \"42\"\n\n  # Test: required - fails if value is empty\n\n  # Test: empty - returns true if value is empty\n\n  # Test: fail - explicitly fails with message\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n\n  # Test: ternary - conditional expression\n",
            "originalContent": "# Test: Logic and default functions - comprehensive testing\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n  {{- if .Values.testAnd }}\n  andResult: {{ and $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: or - returns boolean OR of arguments\n  {{- if .Values.testOr }}\n  orResult: {{ or $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: not - returns boolean negation\n  {{- if .Values.testNot }}\n  notResult: {{ not $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n  {{- if .Values.testEq }}\n  eqResult: {{ eq $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ne - returns true if not equal\n  {{- if .Values.testNe }}\n  neResult: {{ ne $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: lt - returns true if less than\n  {{- if .Values.testLt }}\n  ltResult: {{ lt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: le - returns true if less than or equal\n  {{- if .Values.testLe }}\n  leResult: {{ le $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: gt - returns true if greater than\n  {{- if .Values.testGt }}\n  gtResult: {{ gt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ge - returns true if greater than or equal\n  {{- if .Values.testGe }}\n  geResult: {{ ge $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n  {{- if .Values.testDefault }}\n  defaultResult: {{ default \"fallback\" $testData | quote }}\n  {{- end }}\n\n  # Test: required - fails if value is empty\n  {{- if .Values.testRequired }}\n  requiredResult: {{ required \"value is required\" $testData | quote }}\n  {{- end }}\n\n  # Test: empty - returns true if value is empty\n  {{- if .Values.testEmpty }}\n  emptyResult: {{ empty $testData | quote }}\n  {{- end }}\n\n  # Test: fail - explicitly fails with message\n  {{- if .Values.testFail }}\n  failResult: {{ fail (printf \"intentional fail with: %v\" $testData) }}\n  {{- end }}\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n  {{- if .Values.testCoalesce }}\n  coalesceResult: {{ coalesce $testData .Values.compareValue \"default\" | quote }}\n  {{- end }}\n\n  # Test: ternary - conditional expression\n  {{- if .Values.testTernary }}\n  ternaryResult: {{ ternary \"yes\" \"no\" $testData | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-logic-test\r\ndata:\r\n  # ==========================================\r\n  # Boolean Logic Functions\r\n  # ==========================================\r\n  # Test: and - returns boolean AND of arguments\r\n\r\n  # Test: or - returns boolean OR of arguments\r\n\r\n  # Test: not - returns boolean negation\r\n\r\n  # ==========================================\r\n  # Comparison Functions\r\n  # ==========================================\r\n  # Test: eq - returns true if equal\r\n\r\n  # Test: ne - returns true if not equal\r\n\r\n  # Test: lt - returns true if less than\r\n\r\n  # Test: le - returns true if less than or equal\r\n\r\n  # Test: gt - returns true if greater than\r\n\r\n  # Test: ge - returns true if greater than or equal\r\n\r\n  # ==========================================\r\n  # Default/Required Functions\r\n  # ==========================================\r\n  # Test: default - returns default value if empty\r\n  defaultResult: \"42\"\r\n\r\n  # Test: required - fails if value is empty\r\n\r\n  # Test: empty - returns true if value is empty\r\n\r\n  # Test: fail - explicitly fails with message\r\n\r\n  # ==========================================\r\n  # Coalesce/Ternary Functions\r\n  # ==========================================\r\n  # Test: coalesce - returns first non-empty value\r\n\r\n  # Test: ternary - conditional expression\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-logic-default/templates/logic-default-test.yaml",
            "content": "\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n\n  # Test: or - returns boolean OR of arguments\n\n  # Test: not - returns boolean negation\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n\n  # Test: ne - returns true if not equal\n\n  # Test: lt - returns true if less than\n\n  # Test: le - returns true if less than or equal\n\n  # Test: gt - returns true if greater than\n\n  # Test: ge - returns true if greater than or equal\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n  defaultResult: \"42\"\n\n  # Test: required - fails if value is empty\n\n  # Test: empty - returns true if value is empty\n\n  # Test: fail - explicitly fails with message\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n\n  # Test: ternary - conditional expression\n",
            "originalContent": "# Test: Logic and default functions - comprehensive testing\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n  {{- if .Values.testAnd }}\n  andResult: {{ and $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: or - returns boolean OR of arguments\n  {{- if .Values.testOr }}\n  orResult: {{ or $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: not - returns boolean negation\n  {{- if .Values.testNot }}\n  notResult: {{ not $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n  {{- if .Values.testEq }}\n  eqResult: {{ eq $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ne - returns true if not equal\n  {{- if .Values.testNe }}\n  neResult: {{ ne $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: lt - returns true if less than\n  {{- if .Values.testLt }}\n  ltResult: {{ lt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: le - returns true if less than or equal\n  {{- if .Values.testLe }}\n  leResult: {{ le $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: gt - returns true if greater than\n  {{- if .Values.testGt }}\n  gtResult: {{ gt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ge - returns true if greater than or equal\n  {{- if .Values.testGe }}\n  geResult: {{ ge $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n  {{- if .Values.testDefault }}\n  defaultResult: {{ default \"fallback\" $testData | quote }}\n  {{- end }}\n\n  # Test: required - fails if value is empty\n  {{- if .Values.testRequired }}\n  requiredResult: {{ required \"value is required\" $testData | quote }}\n  {{- end }}\n\n  # Test: empty - returns true if value is empty\n  {{- if .Values.testEmpty }}\n  emptyResult: {{ empty $testData | quote }}\n  {{- end }}\n\n  # Test: fail - explicitly fails with message\n  {{- if .Values.testFail }}\n  failResult: {{ fail (printf \"intentional fail with: %v\" $testData) }}\n  {{- end }}\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n  {{- if .Values.testCoalesce }}\n  coalesceResult: {{ coalesce $testData .Values.compareValue \"default\" | quote }}\n  {{- end }}\n\n  # Test: ternary - conditional expression\n  {{- if .Values.testTernary }}\n  ternaryResult: {{ ternary \"yes\" \"no\" $testData | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "default",
      "dataType": "boolValue",
      "valuesOptions": {
        "values": [
          "testDefault=true",
          "testDataType=boolValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-logic-test\r\ndata:\r\n  # ==========================================\r\n  # Boolean Logic Functions\r\n  # ==========================================\r\n  # Test: and - returns boolean AND of arguments\r\n\r\n  # Test: or - returns boolean OR of arguments\r\n\r\n  # Test: not - returns boolean negation\r\n\r\n  # ==========================================\r\n  # Comparison Functions\r\n  # ==========================================\r\n  # Test: eq - returns true if equal\r\n\r\n  # Test: ne - returns true if not equal\r\n\r\n  # Test: lt - returns true if less than\r\n\r\n  # Test: le - returns true if less than or equal\r\n\r\n  # Test: gt - returns true if greater than\r\n\r\n  # Test: ge - returns true if greater than or equal\r\n\r\n  # ==========================================\r\n  # Default/Required Functions\r\n  # ==========================================\r\n  # Test: default - returns default value if empty\r\n  defaultResult: \"true\"\r\n\r\n  # Test: required - fails if value is empty\r\n\r\n  # Test: empty - returns true if value is empty\r\n\r\n  # Test: fail - explicitly fails with message\r\n\r\n  # ==========================================\r\n  # Coalesce/Ternary Functions\r\n  # ==========================================\r\n  # Test: coalesce - returns first non-empty value\r\n\r\n  # Test: ternary - conditional expression\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-logic-default/templates/logic-default-test.yaml",
            "content": "\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n\n  # Test: or - returns boolean OR of arguments\n\n  # Test: not - returns boolean negation\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n\n  # Test: ne - returns true if not equal\n\n  # Test: lt - returns true if less than\n\n  # Test: le - returns true if less than or equal\n\n  # Test: gt - returns true if greater than\n\n  # Test: ge - returns true if greater than or equal\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n  defaultResult: \"true\"\n\n  # Test: required - fails if value is empty\n\n  # Test: empty - returns true if value is empty\n\n  # Test: fail - explicitly fails with message\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n\n  # Test: ternary - conditional expression\n",
            "originalContent": "# Test: Logic and default functions - comprehensive testing\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n  {{- if .Values.testAnd }}\n  andResult: {{ and $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: or - returns boolean OR of arguments\n  {{- if .Values.testOr }}\n  orResult: {{ or $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: not - returns boolean negation\n  {{- if .Values.testNot }}\n  notResult: {{ not $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n  {{- if .Values.testEq }}\n  eqResult: {{ eq $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ne - returns true if not equal\n  {{- if .Values.testNe }}\n  neResult: {{ ne $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: lt - returns true if less than\n  {{- if .Values.testLt }}\n  ltResult: {{ lt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: le - returns true if less than or equal\n  {{- if .Values.testLe }}\n  leResult: {{ le $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: gt - returns true if greater than\n  {{- if .Values.testGt }}\n  gtResult: {{ gt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ge - returns true if greater than or equal\n  {{- if .Values.testGe }}\n  geResult: {{ ge $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n  {{- if .Values.testDefault }}\n  defaultResult: {{ default \"fallback\" $testData | quote }}\n  {{- end }}\n\n  # Test: required - fails if value is empty\n  {{- if .Values.testRequired }}\n  requiredResult: {{ required \"value is required\" $testData | quote }}\n  {{- end }}\n\n  # Test: empty - returns true if value is empty\n  {{- if .Values.testEmpty }}\n  emptyResult: {{ empty $testData | quote }}\n  {{- end }}\n\n  # Test: fail - explicitly fails with message\n  {{- if .Values.testFail }}\n  failResult: {{ fail (printf \"intentional fail with: %v\" $testData) }}\n  {{- end }}\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n  {{- if .Values.testCoalesce }}\n  coalesceResult: {{ coalesce $testData .Values.compareValue \"default\" | quote }}\n  {{- end }}\n\n  # Test: ternary - conditional expression\n  {{- if .Values.testTernary }}\n  ternaryResult: {{ ternary \"yes\" \"no\" $testData | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-logic-test\r\ndata:\r\n  # ==========================================\r\n  # Boolean Logic Functions\r\n  # ==========================================\r\n  # Test: and - returns boolean AND of arguments\r\n\r\n  # Test: or - returns boolean OR of arguments\r\n\r\n  # Test: not - returns boolean negation\r\n\r\n  # ==========================================\r\n  # Comparison Functions\r\n  # ==========================================\r\n  # Test: eq - returns true if equal\r\n\r\n  # Test: ne - returns true if not equal\r\n\r\n  # Test: lt - returns true if less than\r\n\r\n  # Test: le - returns true if less than or equal\r\n\r\n  # Test: gt - returns true if greater than\r\n\r\n  # Test: ge - returns true if greater than or equal\r\n\r\n  # ==========================================\r\n  # Default/Required Functions\r\n  # ==========================================\r\n  # Test: default - returns default value if empty\r\n  defaultResult: \"true\"\r\n\r\n  # Test: required - fails if value is empty\r\n\r\n  # Test: empty - returns true if value is empty\r\n\r\n  # Test: fail - explicitly fails with message\r\n\r\n  # ==========================================\r\n  # Coalesce/Ternary Functions\r\n  # ==========================================\r\n  # Test: coalesce - returns first non-empty value\r\n\r\n  # Test: ternary - conditional expression\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-logic-default/templates/logic-default-test.yaml",
            "content": "\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n\n  # Test: or - returns boolean OR of arguments\n\n  # Test: not - returns boolean negation\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n\n  # Test: ne - returns true if not equal\n\n  # Test: lt - returns true if less than\n\n  # Test: le - returns true if less than or equal\n\n  # Test: gt - returns true if greater than\n\n  # Test: ge - returns true if greater than or equal\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n  defaultResult: \"true\"\n\n  # Test: required - fails if value is empty\n\n  # Test: empty - returns true if value is empty\n\n  # Test: fail - explicitly fails with message\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n\n  # Test: ternary - conditional expression\n",
            "originalContent": "# Test: Logic and default functions - comprehensive testing\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n  {{- if .Values.testAnd }}\n  andResult: {{ and $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: or - returns boolean OR of arguments\n  {{- if .Values.testOr }}\n  orResult: {{ or $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: not - returns boolean negation\n  {{- if .Values.testNot }}\n  notResult: {{ not $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n  {{- if .Values.testEq }}\n  eqResult: {{ eq $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ne - returns true if not equal\n  {{- if .Values.testNe }}\n  neResult: {{ ne $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: lt - returns true if less than\n  {{- if .Values.testLt }}\n  ltResult: {{ lt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: le - returns true if less than or equal\n  {{- if .Values.testLe }}\n  leResult: {{ le $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: gt - returns true if greater than\n  {{- if .Values.testGt }}\n  gtResult: {{ gt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ge - returns true if greater than or equal\n  {{- if .Values.testGe }}\n  geResult: {{ ge $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n  {{- if .Values.testDefault }}\n  defaultResult: {{ default \"fallback\" $testData | quote }}\n  {{- end }}\n\n  # Test: required - fails if value is empty\n  {{- if .Values.testRequired }}\n  requiredResult: {{ required \"value is required\" $testData | quote }}\n  {{- end }}\n\n  # Test: empty - returns true if value is empty\n  {{- if .Values.testEmpty }}\n  emptyResult: {{ empty $testData | quote }}\n  {{- end }}\n\n  # Test: fail - explicitly fails with message\n  {{- if .Values.testFail }}\n  failResult: {{ fail (printf \"intentional fail with: %v\" $testData) }}\n  {{- end }}\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n  {{- if .Values.testCoalesce }}\n  coalesceResult: {{ coalesce $testData .Values.compareValue \"default\" | quote }}\n  {{- end }}\n\n  # Test: ternary - conditional expression\n  {{- if .Values.testTernary }}\n  ternaryResult: {{ ternary \"yes\" \"no\" $testData | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "default",
      "dataType": "listValue",
      "valuesOptions": {
        "values": [
          "testDefault=true",
          "testDataType=listValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-logic-test\r\ndata:\r\n  # ==========================================\r\n  # Boolean Logic Functions\r\n  # ==========================================\r\n  # Test: and - returns boolean AND of arguments\r\n\r\n  # Test: or - returns boolean OR of arguments\r\n\r\n  # Test: not - returns boolean negation\r\n\r\n  # ==========================================\r\n  # Comparison Functions\r\n  # ==========================================\r\n  # Test: eq - returns true if equal\r\n\r\n  # Test: ne - returns true if not equal\r\n\r\n  # Test: lt - returns true if less than\r\n\r\n  # Test: le - returns true if less than or equal\r\n\r\n  # Test: gt - returns true if greater than\r\n\r\n  # Test: ge - returns true if greater than or equal\r\n\r\n  # ==========================================\r\n  # Default/Required Functions\r\n  # ==========================================\r\n  # Test: default - returns default value if empty\r\n  defaultResult: \"[1 2 3]\"\r\n\r\n  # Test: required - fails if value is empty\r\n\r\n  # Test: empty - returns true if value is empty\r\n\r\n  # Test: fail - explicitly fails with message\r\n\r\n  # ==========================================\r\n  # Coalesce/Ternary Functions\r\n  # ==========================================\r\n  # Test: coalesce - returns first non-empty value\r\n\r\n  # Test: ternary - conditional expression\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-logic-default/templates/logic-default-test.yaml",
            "content": "\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n\n  # Test: or - returns boolean OR of arguments\n\n  # Test: not - returns boolean negation\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n\n  # Test: ne - returns true if not equal\n\n  # Test: lt - returns true if less than\n\n  # Test: le - returns true if less than or equal\n\n  # Test: gt - returns true if greater than\n\n  # Test: ge - returns true if greater than or equal\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n  defaultResult: \"[1 2 3]\"\n\n  # Test: required - fails if value is empty\n\n  # Test: empty - returns true if value is empty\n\n  # Test: fail - explicitly fails with message\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n\n  # Test: ternary - conditional expression\n",
            "originalContent": "# Test: Logic and default functions - comprehensive testing\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n  {{- if .Values.testAnd }}\n  andResult: {{ and $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: or - returns boolean OR of arguments\n  {{- if .Values.testOr }}\n  orResult: {{ or $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: not - returns boolean negation\n  {{- if .Values.testNot }}\n  notResult: {{ not $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n  {{- if .Values.testEq }}\n  eqResult: {{ eq $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ne - returns true if not equal\n  {{- if .Values.testNe }}\n  neResult: {{ ne $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: lt - returns true if less than\n  {{- if .Values.testLt }}\n  ltResult: {{ lt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: le - returns true if less than or equal\n  {{- if .Values.testLe }}\n  leResult: {{ le $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: gt - returns true if greater than\n  {{- if .Values.testGt }}\n  gtResult: {{ gt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ge - returns true if greater than or equal\n  {{- if .Values.testGe }}\n  geResult: {{ ge $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n  {{- if .Values.testDefault }}\n  defaultResult: {{ default \"fallback\" $testData | quote }}\n  {{- end }}\n\n  # Test: required - fails if value is empty\n  {{- if .Values.testRequired }}\n  requiredResult: {{ required \"value is required\" $testData | quote }}\n  {{- end }}\n\n  # Test: empty - returns true if value is empty\n  {{- if .Values.testEmpty }}\n  emptyResult: {{ empty $testData | quote }}\n  {{- end }}\n\n  # Test: fail - explicitly fails with message\n  {{- if .Values.testFail }}\n  failResult: {{ fail (printf \"intentional fail with: %v\" $testData) }}\n  {{- end }}\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n  {{- if .Values.testCoalesce }}\n  coalesceResult: {{ coalesce $testData .Values.compareValue \"default\" | quote }}\n  {{- end }}\n\n  # Test: ternary - conditional expression\n  {{- if .Values.testTernary }}\n  ternaryResult: {{ ternary \"yes\" \"no\" $testData | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-logic-test\r\ndata:\r\n  # ==========================================\r\n  # Boolean Logic Functions\r\n  # ==========================================\r\n  # Test: and - returns boolean AND of arguments\r\n\r\n  # Test: or - returns boolean OR of arguments\r\n\r\n  # Test: not - returns boolean negation\r\n\r\n  # ==========================================\r\n  # Comparison Functions\r\n  # ==========================================\r\n  # Test: eq - returns true if equal\r\n\r\n  # Test: ne - returns true if not equal\r\n\r\n  # Test: lt - returns true if less than\r\n\r\n  # Test: le - returns true if less than or equal\r\n\r\n  # Test: gt - returns true if greater than\r\n\r\n  # Test: ge - returns true if greater than or equal\r\n\r\n  # ==========================================\r\n  # Default/Required Functions\r\n  # ==========================================\r\n  # Test: default - returns default value if empty\r\n  defaultResult: \"[1 2 3]\"\r\n\r\n  # Test: required - fails if value is empty\r\n\r\n  # Test: empty - returns true if value is empty\r\n\r\n  # Test: fail - explicitly fails with message\r\n\r\n  # ==========================================\r\n  # Coalesce/Ternary Functions\r\n  # ==========================================\r\n  # Test: coalesce - returns first non-empty value\r\n\r\n  # Test: ternary - conditional expression\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-logic-default/templates/logic-default-test.yaml",
            "content": "\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n\n  # Test: or - returns boolean OR of arguments\n\n  # Test: not - returns boolean negation\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n\n  # Test: ne - returns true if not equal\n\n  # Test: lt - returns true if less than\n\n  # Test: le - returns true if less than or equal\n\n  # Test: gt - returns true if greater than\n\n  # Test: ge - returns true if greater than or equal\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n  defaultResult: \"[1 2 3]\"\n\n  # Test: required - fails if value is empty\n\n  # Test: empty - returns true if value is empty\n\n  # Test: fail - explicitly fails with message\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n\n  # Test: ternary - conditional expression\n",
            "originalContent": "# Test: Logic and default functions - comprehensive testing\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n  {{- if .Values.testAnd }}\n  andResult: {{ and $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: or - returns boolean OR of arguments\n  {{- if .Values.testOr }}\n  orResult: {{ or $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: not - returns boolean negation\n  {{- if .Values.testNot }}\n  notResult: {{ not $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n  {{- if .Values.testEq }}\n  eqResult: {{ eq $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ne - returns true if not equal\n  {{- if .Values.testNe }}\n  neResult: {{ ne $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: lt - returns true if less than\n  {{- if .Values.testLt }}\n  ltResult: {{ lt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: le - returns true if less than or equal\n  {{- if .Values.testLe }}\n  leResult: {{ le $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: gt - returns true if greater than\n  {{- if .Values.testGt }}\n  gtResult: {{ gt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ge - returns true if greater than or equal\n  {{- if .Values.testGe }}\n  geResult: {{ ge $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n  {{- if .Values.testDefault }}\n  defaultResult: {{ default \"fallback\" $testData | quote }}\n  {{- end }}\n\n  # Test: required - fails if value is empty\n  {{- if .Values.testRequired }}\n  requiredResult: {{ required \"value is required\" $testData | quote }}\n  {{- end }}\n\n  # Test: empty - returns true if value is empty\n  {{- if .Values.testEmpty }}\n  emptyResult: {{ empty $testData | quote }}\n  {{- end }}\n\n  # Test: fail - explicitly fails with message\n  {{- if .Values.testFail }}\n  failResult: {{ fail (printf \"intentional fail with: %v\" $testData) }}\n  {{- end }}\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n  {{- if .Values.testCoalesce }}\n  coalesceResult: {{ coalesce $testData .Values.compareValue \"default\" | quote }}\n  {{- end }}\n\n  # Test: ternary - conditional expression\n  {{- if .Values.testTernary }}\n  ternaryResult: {{ ternary \"yes\" \"no\" $testData | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "default",
      "dataType": "dictValue",
      "valuesOptions": {
        "values": [
          "testDefault=true",
          "testDataType=dictValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-logic-test\r\ndata:\r\n  # ==========================================\r\n  # Boolean Logic Functions\r\n  # ==========================================\r\n  # Test: and - returns boolean AND of arguments\r\n\r\n  # Test: or - returns boolean OR of arguments\r\n\r\n  # Test: not - returns boolean negation\r\n\r\n  # ==========================================\r\n  # Comparison Functions\r\n  # ==========================================\r\n  # Test: eq - returns true if equal\r\n\r\n  # Test: ne - returns true if not equal\r\n\r\n  # Test: lt - returns true if less than\r\n\r\n  # Test: le - returns true if less than or equal\r\n\r\n  # Test: gt - returns true if greater than\r\n\r\n  # Test: ge - returns true if greater than or equal\r\n\r\n  # ==========================================\r\n  # Default/Required Functions\r\n  # ==========================================\r\n  # Test: default - returns default value if empty\r\n  defaultResult: \"map[key:value]\"\r\n\r\n  # Test: required - fails if value is empty\r\n\r\n  # Test: empty - returns true if value is empty\r\n\r\n  # Test: fail - explicitly fails with message\r\n\r\n  # ==========================================\r\n  # Coalesce/Ternary Functions\r\n  # ==========================================\r\n  # Test: coalesce - returns first non-empty value\r\n\r\n  # Test: ternary - conditional expression\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-logic-default/templates/logic-default-test.yaml",
            "content": "\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n\n  # Test: or - returns boolean OR of arguments\n\n  # Test: not - returns boolean negation\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n\n  # Test: ne - returns true if not equal\n\n  # Test: lt - returns true if less than\n\n  # Test: le - returns true if less than or equal\n\n  # Test: gt - returns true if greater than\n\n  # Test: ge - returns true if greater than or equal\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n  defaultResult: \"map[key:value]\"\n\n  # Test: required - fails if value is empty\n\n  # Test: empty - returns true if value is empty\n\n  # Test: fail - explicitly fails with message\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n\n  # Test: ternary - conditional expression\n",
            "originalContent": "# Test: Logic and default functions - comprehensive testing\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n  {{- if .Values.testAnd }}\n  andResult: {{ and $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: or - returns boolean OR of arguments\n  {{- if .Values.testOr }}\n  orResult: {{ or $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: not - returns boolean negation\n  {{- if .Values.testNot }}\n  notResult: {{ not $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n  {{- if .Values.testEq }}\n  eqResult: {{ eq $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ne - returns true if not equal\n  {{- if .Values.testNe }}\n  neResult: {{ ne $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: lt - returns true if less than\n  {{- if .Values.testLt }}\n  ltResult: {{ lt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: le - returns true if less than or equal\n  {{- if .Values.testLe }}\n  leResult: {{ le $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: gt - returns true if greater than\n  {{- if .Values.testGt }}\n  gtResult: {{ gt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ge - returns true if greater than or equal\n  {{- if .Values.testGe }}\n  geResult: {{ ge $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n  {{- if .Values.testDefault }}\n  defaultResult: {{ default \"fallback\" $testData | quote }}\n  {{- end }}\n\n  # Test: required - fails if value is empty\n  {{- if .Values.testRequired }}\n  requiredResult: {{ required \"value is required\" $testData | quote }}\n  {{- end }}\n\n  # Test: empty - returns true if value is empty\n  {{- if .Values.testEmpty }}\n  emptyResult: {{ empty $testData | quote }}\n  {{- end }}\n\n  # Test: fail - explicitly fails with message\n  {{- if .Values.testFail }}\n  failResult: {{ fail (printf \"intentional fail with: %v\" $testData) }}\n  {{- end }}\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n  {{- if .Values.testCoalesce }}\n  coalesceResult: {{ coalesce $testData .Values.compareValue \"default\" | quote }}\n  {{- end }}\n\n  # Test: ternary - conditional expression\n  {{- if .Values.testTernary }}\n  ternaryResult: {{ ternary \"yes\" \"no\" $testData | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-logic-test\r\ndata:\r\n  # ==========================================\r\n  # Boolean Logic Functions\r\n  # ==========================================\r\n  # Test: and - returns boolean AND of arguments\r\n\r\n  # Test: or - returns boolean OR of arguments\r\n\r\n  # Test: not - returns boolean negation\r\n\r\n  # ==========================================\r\n  # Comparison Functions\r\n  # ==========================================\r\n  # Test: eq - returns true if equal\r\n\r\n  # Test: ne - returns true if not equal\r\n\r\n  # Test: lt - returns true if less than\r\n\r\n  # Test: le - returns true if less than or equal\r\n\r\n  # Test: gt - returns true if greater than\r\n\r\n  # Test: ge - returns true if greater than or equal\r\n\r\n  # ==========================================\r\n  # Default/Required Functions\r\n  # ==========================================\r\n  # Test: default - returns default value if empty\r\n  defaultResult: \"map[key:value]\"\r\n\r\n  # Test: required - fails if value is empty\r\n\r\n  # Test: empty - returns true if value is empty\r\n\r\n  # Test: fail - explicitly fails with message\r\n\r\n  # ==========================================\r\n  # Coalesce/Ternary Functions\r\n  # ==========================================\r\n  # Test: coalesce - returns first non-empty value\r\n\r\n  # Test: ternary - conditional expression\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-logic-default/templates/logic-default-test.yaml",
            "content": "\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n\n  # Test: or - returns boolean OR of arguments\n\n  # Test: not - returns boolean negation\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n\n  # Test: ne - returns true if not equal\n\n  # Test: lt - returns true if less than\n\n  # Test: le - returns true if less than or equal\n\n  # Test: gt - returns true if greater than\n\n  # Test: ge - returns true if greater than or equal\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n  defaultResult: \"map[key:value]\"\n\n  # Test: required - fails if value is empty\n\n  # Test: empty - returns true if value is empty\n\n  # Test: fail - explicitly fails with message\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n\n  # Test: ternary - conditional expression\n",
            "originalContent": "# Test: Logic and default functions - comprehensive testing\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n  {{- if .Values.testAnd }}\n  andResult: {{ and $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: or - returns boolean OR of arguments\n  {{- if .Values.testOr }}\n  orResult: {{ or $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: not - returns boolean negation\n  {{- if .Values.testNot }}\n  notResult: {{ not $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n  {{- if .Values.testEq }}\n  eqResult: {{ eq $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ne - returns true if not equal\n  {{- if .Values.testNe }}\n  neResult: {{ ne $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: lt - returns true if less than\n  {{- if .Values.testLt }}\n  ltResult: {{ lt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: le - returns true if less than or equal\n  {{- if .Values.testLe }}\n  leResult: {{ le $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: gt - returns true if greater than\n  {{- if .Values.testGt }}\n  gtResult: {{ gt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ge - returns true if greater than or equal\n  {{- if .Values.testGe }}\n  geResult: {{ ge $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n  {{- if .Values.testDefault }}\n  defaultResult: {{ default \"fallback\" $testData | quote }}\n  {{- end }}\n\n  # Test: required - fails if value is empty\n  {{- if .Values.testRequired }}\n  requiredResult: {{ required \"value is required\" $testData | quote }}\n  {{- end }}\n\n  # Test: empty - returns true if value is empty\n  {{- if .Values.testEmpty }}\n  emptyResult: {{ empty $testData | quote }}\n  {{- end }}\n\n  # Test: fail - explicitly fails with message\n  {{- if .Values.testFail }}\n  failResult: {{ fail (printf \"intentional fail with: %v\" $testData) }}\n  {{- end }}\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n  {{- if .Values.testCoalesce }}\n  coalesceResult: {{ coalesce $testData .Values.compareValue \"default\" | quote }}\n  {{- end }}\n\n  # Test: ternary - conditional expression\n  {{- if .Values.testTernary }}\n  ternaryResult: {{ ternary \"yes\" \"no\" $testData | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "required",
      "dataType": "nilValue",
      "valuesOptions": {
        "values": [
          "testRequired=true",
          "testDataType=nilValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": "execution error at (test-logic-default/templates/logic-default-test.yaml:70:21): value is required",
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      },
      "debug": {
        "error": "execution error at (test-logic-default/templates/logic-default-test.yaml:70:21): value is required",
        "debugLogs": [],
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      }
    },
    {
      "toggle": "required",
      "dataType": "stringValue",
      "valuesOptions": {
        "values": [
          "testRequired=true",
          "testDataType=stringValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-logic-test\r\ndata:\r\n  # ==========================================\r\n  # Boolean Logic Functions\r\n  # ==========================================\r\n  # Test: and - returns boolean AND of arguments\r\n\r\n  # Test: or - returns boolean OR of arguments\r\n\r\n  # Test: not - returns boolean negation\r\n\r\n  # ==========================================\r\n  # Comparison Functions\r\n  # ==========================================\r\n  # Test: eq - returns true if equal\r\n\r\n  # Test: ne - returns true if not equal\r\n\r\n  # Test: lt - returns true if less than\r\n\r\n  # Test: le - returns true if less than or equal\r\n\r\n  # Test: gt - returns true if greater than\r\n\r\n  # Test: ge - returns true if greater than or equal\r\n\r\n  # ==========================================\r\n  # Default/Required Functions\r\n  # ==========================================\r\n  # Test: default - returns default value if empty\r\n\r\n  # Test: required - fails if value is empty\r\n  requiredResult: \"test string\"\r\n\r\n  # Test: empty - returns true if value is empty\r\n\r\n  # Test: fail - explicitly fails with message\r\n\r\n  # ==========================================\r\n  # Coalesce/Ternary Functions\r\n  # ==========================================\r\n  # Test: coalesce - returns first non-empty value\r\n\r\n  # Test: ternary - conditional expression\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-logic-default/templates/logic-default-test.yaml",
            "content": "\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n\n  # Test: or - returns boolean OR of arguments\n\n  # Test: not - returns boolean negation\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n\n  # Test: ne - returns true if not equal\n\n  # Test: lt - returns true if less than\n\n  # Test: le - returns true if less than or equal\n\n  # Test: gt - returns true if greater than\n\n  # Test: ge - returns true if greater than or equal\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n\n  # Test: required - fails if value is empty\n  requiredResult: \"test string\"\n\n  # Test: empty - returns true if value is empty\n\n  # Test: fail - explicitly fails with message\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n\n  # Test: ternary - conditional expression\n",
            "originalContent": "# Test: Logic and default functions - comprehensive testing\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n  {{- if .Values.testAnd }}\n  andResult: {{ and $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: or - returns boolean OR of arguments\n  {{- if .Values.testOr }}\n  orResult: {{ or $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: not - returns boolean negation\n  {{- if .Values.testNot }}\n  notResult: {{ not $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n  {{- if .Values.testEq }}\n  eqResult: {{ eq $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ne - returns true if not equal\n  {{- if .Values.testNe }}\n  neResult: {{ ne $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: lt - returns true if less than\n  {{- if .Values.testLt }}\n  ltResult: {{ lt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: le - returns true if less than or equal\n  {{- if .Values.testLe }}\n  leResult: {{ le $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: gt - returns true if greater than\n  {{- if .Values.testGt }}\n  gtResult: {{ gt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ge - returns true if greater than or equal\n  {{- if .Values.testGe }}\n  geResult: {{ ge $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n  {{- if .Values.testDefault }}\n  defaultResult: {{ default \"fallback\" $testData | quote }}\n  {{- end }}\n\n  # Test: required - fails if value is empty\n  {{- if .Values.testRequired }}\n  requiredResult: {{ required \"value is required\" $testData | quote }}\n  {{- end }}\n\n  # Test: empty - returns true if value is empty\n  {{- if .Values.testEmpty }}\n  emptyResult: {{ empty $testData | quote }}\n  {{- end }}\n\n  # Test: fail - explicitly fails with message\n  {{- if .Values.testFail }}\n  failResult: {{ fail (printf \"intentional fail with: %v\" $testData) }}\n  {{- end }}\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n  {{- if .Values.testCoalesce }}\n  coalesceResult: {{ coalesce $testData .Values.compareValue \"default\" | quote }}\n  {{- end }}\n\n  # Test: ternary - conditional expression\n  {{- if .Values.testTernary }}\n  ternaryResult: {{ ternary \"yes\" \"no\" $testData | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-logic-test\r\ndata:\r\n  # ==========================================\r\n  # Boolean Logic Functions\r\n  # ==========================================\r\n  # Test: and - returns boolean AND of arguments\r\n\r\n  # Test: or - returns boolean OR of arguments\r\n\r\n  # Test: not - returns boolean negation\r\n\r\n  # ==========================================\r\n  # Comparison Functions\r\n  # ==========================================\r\n  # Test: eq - returns true if equal\r\n\r\n  # Test: ne - returns true if not equal\r\n\r\n  # Test: lt - returns true if less than\r\n\r\n  # Test: le - returns true if less than or equal\r\n\r\n  # Test: gt - returns true if greater than\r\n\r\n  # Test: ge - returns true if greater than or equal\r\n\r\n  # ==========================================\r\n  # Default/Required Functions\r\n  # ==========================================\r\n  # Test: default - returns default value if empty\r\n\r\n  # Test: required - fails if value is empty\r\n  requiredResult: \"test string\"\r\n\r\n  # Test: empty - returns true if value is empty\r\n\r\n  # Test: fail - explicitly fails with message\r\n\r\n  # ==========================================\r\n  # Coalesce/Ternary Functions\r\n  # ==========================================\r\n  # Test: coalesce - returns first non-empty value\r\n\r\n  # Test: ternary - conditional expression\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-logic-default/templates/logic-default-test.yaml",
            "content": "\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n\n  # Test: or - returns boolean OR of arguments\n\n  # Test: not - returns boolean negation\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n\n  # Test: ne - returns true if not equal\n\n  # Test: lt - returns true if less than\n\n  # Test: le - returns true if less than or equal\n\n  # Test: gt - returns true if greater than\n\n  # Test: ge - returns true if greater than or equal\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n\n  # Test: required - fails if value is empty\n  requiredResult: \"test string\"\n\n  # Test: empty - returns true if value is empty\n\n  # Test: fail - explicitly fails with message\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n\n  # Test: ternary - conditional expression\n",
            "originalContent": "# Test: Logic and default functions - comprehensive testing\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n  {{- if .Values.testAnd }}\n  andResult: {{ and $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: or - returns boolean OR of arguments\n  {{- if .Values.testOr }}\n  orResult: {{ or $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: not - returns boolean negation\n  {{- if .Values.testNot }}\n  notResult: {{ not $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n  {{- if .Values.testEq }}\n  eqResult: {{ eq $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ne - returns true if not equal\n  {{- if .Values.testNe }}\n  neResult: {{ ne $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: lt - returns true if less than\n  {{- if .Values.testLt }}\n  ltResult: {{ lt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: le - returns true if less than or equal\n  {{- if .Values.testLe }}\n  leResult: {{ le $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: gt - returns true if greater than\n  {{- if .Values.testGt }}\n  gtResult: {{ gt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ge - returns true if greater than or equal\n  {{- if .Values.testGe }}\n  geResult: {{ ge $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n  {{- if .Values.testDefault }}\n  defaultResult: {{ default \"fallback\" $testData | quote }}\n  {{- end }}\n\n  # Test: required - fails if value is empty\n  {{- if .Values.testRequired }}\n  requiredResult: {{ required \"value is required\" $testData | quote }}\n  {{- end }}\n\n  # Test: empty - returns true if value is empty\n  {{- if .Values.testEmpty }}\n  emptyResult: {{ empty $testData | quote }}\n  {{- end }}\n\n  # Test: fail - explicitly fails with message\n  {{- if .Values.testFail }}\n  failResult: {{ fail (printf \"intentional fail with: %v\" $testData) }}\n  {{- end }}\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n  {{- if .Values.testCoalesce }}\n  coalesceResult: {{ coalesce $testData .Values.compareValue \"default\" | quote }}\n  {{- end }}\n\n  # Test: ternary - conditional expression\n  {{- if .Values.testTernary }}\n  ternaryResult: {{ ternary \"yes\" \"no\" $testData | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "required",
      "dataType": "numberValue",
      "valuesOptions": {
        "values": [
          "testRequired=true",
          "testDataType=numberValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-logic-test\r\ndata:\r\n  # ==========================================\r\n  # Boolean Logic Functions\r\n  # ==========================================\r\n  # Test: and - returns boolean AND of arguments\r\n\r\n  # Test: or - returns boolean OR of arguments\r\n\r\n  # Test: not - returns boolean negation\r\n\r\n  # ==========================================\r\n  # Comparison Functions\r\n  # ==========================================\r\n  # Test: eq - returns true if equal\r\n\r\n  # Test: ne - returns true if not equal\r\n\r\n  # Test: lt - returns true if less than\r\n\r\n  # Test: le - returns true if less than or equal\r\n\r\n  # Test: gt - returns true if greater than\r\n\r\n  # Test: ge - returns true if greater than or equal\r\n\r\n  # ==========================================\r\n  # Default/Required Functions\r\n  # ==========================================\r\n  # Test: default - returns default value if empty\r\n\r\n  # Test: required - fails if value is empty\r\n  requiredResult: \"42\"\r\n\r\n  # Test: empty - returns true if value is empty\r\n\r\n  # Test: fail - explicitly fails with message\r\n\r\n  # ==========================================\r\n  # Coalesce/Ternary Functions\r\n  # ==========================================\r\n  # Test: coalesce - returns first non-empty value\r\n\r\n  # Test: ternary - conditional expression\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-logic-default/templates/logic-default-test.yaml",
            "content": "\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n\n  # Test: or - returns boolean OR of arguments\n\n  # Test: not - returns boolean negation\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n\n  # Test: ne - returns true if not equal\n\n  # Test: lt - returns true if less than\n\n  # Test: le - returns true if less than or equal\n\n  # Test: gt - returns true if greater than\n\n  # Test: ge - returns true if greater than or equal\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n\n  # Test: required - fails if value is empty\n  requiredResult: \"42\"\n\n  # Test: empty - returns true if value is empty\n\n  # Test: fail - explicitly fails with message\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n\n  # Test: ternary - conditional expression\n",
            "originalContent": "# Test: Logic and default functions - comprehensive testing\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n  {{- if .Values.testAnd }}\n  andResult: {{ and $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: or - returns boolean OR of arguments\n  {{- if .Values.testOr }}\n  orResult: {{ or $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: not - returns boolean negation\n  {{- if .Values.testNot }}\n  notResult: {{ not $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n  {{- if .Values.testEq }}\n  eqResult: {{ eq $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ne - returns true if not equal\n  {{- if .Values.testNe }}\n  neResult: {{ ne $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: lt - returns true if less than\n  {{- if .Values.testLt }}\n  ltResult: {{ lt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: le - returns true if less than or equal\n  {{- if .Values.testLe }}\n  leResult: {{ le $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: gt - returns true if greater than\n  {{- if .Values.testGt }}\n  gtResult: {{ gt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ge - returns true if greater than or equal\n  {{- if .Values.testGe }}\n  geResult: {{ ge $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n  {{- if .Values.testDefault }}\n  defaultResult: {{ default \"fallback\" $testData | quote }}\n  {{- end }}\n\n  # Test: required - fails if value is empty\n  {{- if .Values.testRequired }}\n  requiredResult: {{ required \"value is required\" $testData | quote }}\n  {{- end }}\n\n  # Test: empty - returns true if value is empty\n  {{- if .Values.testEmpty }}\n  emptyResult: {{ empty $testData | quote }}\n  {{- end }}\n\n  # Test: fail - explicitly fails with message\n  {{- if .Values.testFail }}\n  failResult: {{ fail (printf \"intentional fail with: %v\" $testData) }}\n  {{- end }}\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n  {{- if .Values.testCoalesce }}\n  coalesceResult: {{ coalesce $testData .Values.compareValue \"default\" | quote }}\n  {{- end }}\n\n  # Test: ternary - conditional expression\n  {{- if .Values.testTernary }}\n  ternaryResult: {{ ternary \"yes\" \"no\" $testData | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-logic-test\r\ndata:\r\n  # ==========================================\r\n  # Boolean Logic Functions\r\n  # ==========================================\r\n  # Test: and - returns boolean AND of arguments\r\n\r\n  # Test: or - returns boolean OR of arguments\r\n\r\n  # Test: not - returns boolean negation\r\n\r\n  # ==========================================\r\n  # Comparison Functions\r\n  # ==========================================\r\n  # Test: eq - returns true if equal\r\n\r\n  # Test: ne - returns true if not equal\r\n\r\n  # Test: lt - returns true if less than\r\n\r\n  # Test: le - returns true if less than or equal\r\n\r\n  # Test: gt - returns true if greater than\r\n\r\n  # Test: ge - returns true if greater than or equal\r\n\r\n  # ==========================================\r\n  # Default/Required Functions\r\n  # ==========================================\r\n  # Test: default - returns default value if empty\r\n\r\n  # Test: required - fails if value is empty\r\n  requiredResult: \"42\"\r\n\r\n  # Test: empty - returns true if value is empty\r\n\r\n  # Test: fail - explicitly fails with message\r\n\r\n  # ==========================================\r\n  # Coalesce/Ternary Functions\r\n  # ==========================================\r\n  # Test: coalesce - returns first non-empty value\r\n\r\n  # Test: ternary - conditional expression\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-logic-default/templates/logic-default-test.yaml",
            "content": "\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n\n  # Test: or - returns boolean OR of arguments\n\n  # Test: not - returns boolean negation\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n\n  # Test: ne - returns true if not equal\n\n  # Test: lt - returns true if less than\n\n  # Test: le - returns true if less than or equal\n\n  # Test: gt - returns true if greater than\n\n  # Test: ge - returns true if greater than or equal\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n\n  # Test: required - fails if value is empty\n  requiredResult: \"42\"\n\n  # Test: empty - returns true if value is empty\n\n  # Test: fail - explicitly fails with message\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n\n  # Test: ternary - conditional expression\n",
            "originalContent": "# Test: Logic and default functions - comprehensive testing\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n  {{- if .Values.testAnd }}\n  andResult: {{ and $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: or - returns boolean OR of arguments\n  {{- if .Values.testOr }}\n  orResult: {{ or $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: not - returns boolean negation\n  {{- if .Values.testNot }}\n  notResult: {{ not $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n  {{- if .Values.testEq }}\n  eqResult: {{ eq $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ne - returns true if not equal\n  {{- if .Values.testNe }}\n  neResult: {{ ne $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: lt - returns true if less than\n  {{- if .Values.testLt }}\n  ltResult: {{ lt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: le - returns true if less than or equal\n  {{- if .Values.testLe }}\n  leResult: {{ le $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: gt - returns true if greater than\n  {{- if .Values.testGt }}\n  gtResult: {{ gt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ge - returns true if greater than or equal\n  {{- if .Values.testGe }}\n  geResult: {{ ge $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n  {{- if .Values.testDefault }}\n  defaultResult: {{ default \"fallback\" $testData | quote }}\n  {{- end }}\n\n  # Test: required - fails if value is empty\n  {{- if .Values.testRequired }}\n  requiredResult: {{ required \"value is required\" $testData | quote }}\n  {{- end }}\n\n  # Test: empty - returns true if value is empty\n  {{- if .Values.testEmpty }}\n  emptyResult: {{ empty $testData | quote }}\n  {{- end }}\n\n  # Test: fail - explicitly fails with message\n  {{- if .Values.testFail }}\n  failResult: {{ fail (printf \"intentional fail with: %v\" $testData) }}\n  {{- end }}\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n  {{- if .Values.testCoalesce }}\n  coalesceResult: {{ coalesce $testData .Values.compareValue \"default\" | quote }}\n  {{- end }}\n\n  # Test: ternary - conditional expression\n  {{- if .Values.testTernary }}\n  ternaryResult: {{ ternary \"yes\" \"no\" $testData | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "required",
      "dataType": "boolValue",
      "valuesOptions": {
        "values": [
          "testRequired=true",
          "testDataType=boolValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-logic-test\r\ndata:\r\n  # ==========================================\r\n  # Boolean Logic Functions\r\n  # ==========================================\r\n  # Test: and - returns boolean AND of arguments\r\n\r\n  # Test: or - returns boolean OR of arguments\r\n\r\n  # Test: not - returns boolean negation\r\n\r\n  # ==========================================\r\n  # Comparison Functions\r\n  # ==========================================\r\n  # Test: eq - returns true if equal\r\n\r\n  # Test: ne - returns true if not equal\r\n\r\n  # Test: lt - returns true if less than\r\n\r\n  # Test: le - returns true if less than or equal\r\n\r\n  # Test: gt - returns true if greater than\r\n\r\n  # Test: ge - returns true if greater than or equal\r\n\r\n  # ==========================================\r\n  # Default/Required Functions\r\n  # ==========================================\r\n  # Test: default - returns default value if empty\r\n\r\n  # Test: required - fails if value is empty\r\n  requiredResult: \"true\"\r\n\r\n  # Test: empty - returns true if value is empty\r\n\r\n  # Test: fail - explicitly fails with message\r\n\r\n  # ==========================================\r\n  # Coalesce/Ternary Functions\r\n  # ==========================================\r\n  # Test: coalesce - returns first non-empty value\r\n\r\n  # Test: ternary - conditional expression\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-logic-default/templates/logic-default-test.yaml",
            "content": "\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n\n  # Test: or - returns boolean OR of arguments\n\n  # Test: not - returns boolean negation\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n\n  # Test: ne - returns true if not equal\n\n  # Test: lt - returns true if less than\n\n  # Test: le - returns true if less than or equal\n\n  # Test: gt - returns true if greater than\n\n  # Test: ge - returns true if greater than or equal\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n\n  # Test: required - fails if value is empty\n  requiredResult: \"true\"\n\n  # Test: empty - returns true if value is empty\n\n  # Test: fail - explicitly fails with message\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n\n  # Test: ternary - conditional expression\n",
            "originalContent": "# Test: Logic and default functions - comprehensive testing\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n  {{- if .Values.testAnd }}\n  andResult: {{ and $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: or - returns boolean OR of arguments\n  {{- if .Values.testOr }}\n  orResult: {{ or $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: not - returns boolean negation\n  {{- if .Values.testNot }}\n  notResult: {{ not $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n  {{- if .Values.testEq }}\n  eqResult: {{ eq $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ne - returns true if not equal\n  {{- if .Values.testNe }}\n  neResult: {{ ne $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: lt - returns true if less than\n  {{- if .Values.testLt }}\n  ltResult: {{ lt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: le - returns true if less than or equal\n  {{- if .Values.testLe }}\n  leResult: {{ le $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: gt - returns true if greater than\n  {{- if .Values.testGt }}\n  gtResult: {{ gt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ge - returns true if greater than or equal\n  {{- if .Values.testGe }}\n  geResult: {{ ge $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n  {{- if .Values.testDefault }}\n  defaultResult: {{ default \"fallback\" $testData | quote }}\n  {{- end }}\n\n  # Test: required - fails if value is empty\n  {{- if .Values.testRequired }}\n  requiredResult: {{ required \"value is required\" $testData | quote }}\n  {{- end }}\n\n  # Test: empty - returns true if value is empty\n  {{- if .Values.testEmpty }}\n  emptyResult: {{ empty $testData | quote }}\n  {{- end }}\n\n  # Test: fail - explicitly fails with message\n  {{- if .Values.testFail }}\n  failResult: {{ fail (printf \"intentional fail with: %v\" $testData) }}\n  {{- end }}\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n  {{- if .Values.testCoalesce }}\n  coalesceResult: {{ coalesce $testData .Values.compareValue \"default\" | quote }}\n  {{- end }}\n\n  # Test: ternary - conditional expression\n  {{- if .Values.testTernary }}\n  ternaryResult: {{ ternary \"yes\" \"no\" $testData | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-logic-test\r\ndata:\r\n  # ==========================================\r\n  # Boolean Logic Functions\r\n  # ==========================================\r\n  # Test: and - returns boolean AND of arguments\r\n\r\n  # Test: or - returns boolean OR of arguments\r\n\r\n  # Test: not - returns boolean negation\r\n\r\n  # ==========================================\r\n  # Comparison Functions\r\n  # ==========================================\r\n  # Test: eq - returns true if equal\r\n\r\n  # Test: ne - returns true if not equal\r\n\r\n  # Test: lt - returns true if less than\r\n\r\n  # Test: le - returns true if less than or equal\r\n\r\n  # Test: gt - returns true if greater than\r\n\r\n  # Test: ge - returns true if greater than or equal\r\n\r\n  # ==========================================\r\n  # Default/Required Functions\r\n  # ==========================================\r\n  # Test: default - returns default value if empty\r\n\r\n  # Test: required - fails if value is empty\r\n  requiredResult: \"true\"\r\n\r\n  # Test: empty - returns true if value is empty\r\n\r\n  # Test: fail - explicitly fails with message\r\n\r\n  # ==========================================\r\n  # Coalesce/Ternary Functions\r\n  # ==========================================\r\n  # Test: coalesce - returns first non-empty value\r\n\r\n  # Test: ternary - conditional expression\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-logic-default/templates/logic-default-test.yaml",
            "content": "\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n\n  # Test: or - returns boolean OR of arguments\n\n  # Test: not - returns boolean negation\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n\n  # Test: ne - returns true if not equal\n\n  # Test: lt - returns true if less than\n\n  # Test: le - returns true if less than or equal\n\n  # Test: gt - returns true if greater than\n\n  # Test: ge - returns true if greater than or equal\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n\n  # Test: required - fails if value is empty\n  requiredResult: \"true\"\n\n  # Test: empty - returns true if value is empty\n\n  # Test: fail - explicitly fails with message\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n\n  # Test: ternary - conditional expression\n",
            "originalContent": "# Test: Logic and default functions - comprehensive testing\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n  {{- if .Values.testAnd }}\n  andResult: {{ and $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: or - returns boolean OR of arguments\n  {{- if .Values.testOr }}\n  orResult: {{ or $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: not - returns boolean negation\n  {{- if .Values.testNot }}\n  notResult: {{ not $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n  {{- if .Values.testEq }}\n  eqResult: {{ eq $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ne - returns true if not equal\n  {{- if .Values.testNe }}\n  neResult: {{ ne $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: lt - returns true if less than\n  {{- if .Values.testLt }}\n  ltResult: {{ lt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: le - returns true if less than or equal\n  {{- if .Values.testLe }}\n  leResult: {{ le $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: gt - returns true if greater than\n  {{- if .Values.testGt }}\n  gtResult: {{ gt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ge - returns true if greater than or equal\n  {{- if .Values.testGe }}\n  geResult: {{ ge $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n  {{- if .Values.testDefault }}\n  defaultResult: {{ default \"fallback\" $testData | quote }}\n  {{- end }}\n\n  # Test: required - fails if value is empty\n  {{- if .Values.testRequired }}\n  requiredResult: {{ required \"value is required\" $testData | quote }}\n  {{- end }}\n\n  # Test: empty - returns true if value is empty\n  {{- if .Values.testEmpty }}\n  emptyResult: {{ empty $testData | quote }}\n  {{- end }}\n\n  # Test: fail - explicitly fails with message\n  {{- if .Values.testFail }}\n  failResult: {{ fail (printf \"intentional fail with: %v\" $testData) }}\n  {{- end }}\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n  {{- if .Values.testCoalesce }}\n  coalesceResult: {{ coalesce $testData .Values.compareValue \"default\" | quote }}\n  {{- end }}\n\n  # Test: ternary - conditional expression\n  {{- if .Values.testTernary }}\n  ternaryResult: {{ ternary \"yes\" \"no\" $testData | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "required",
      "dataType": "listValue",
      "valuesOptions": {
        "values": [
          "testRequired=true",
          "testDataType=listValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-logic-test\r\ndata:\r\n  # ==========================================\r\n  # Boolean Logic Functions\r\n  # ==========================================\r\n  # Test: and - returns boolean AND of arguments\r\n\r\n  # Test: or - returns boolean OR of arguments\r\n\r\n  # Test: not - returns boolean negation\r\n\r\n  # ==========================================\r\n  # Comparison Functions\r\n  # ==========================================\r\n  # Test: eq - returns true if equal\r\n\r\n  # Test: ne - returns true if not equal\r\n\r\n  # Test: lt - returns true if less than\r\n\r\n  # Test: le - returns true if less than or equal\r\n\r\n  # Test: gt - returns true if greater than\r\n\r\n  # Test: ge - returns true if greater than or equal\r\n\r\n  # ==========================================\r\n  # Default/Required Functions\r\n  # ==========================================\r\n  # Test: default - returns default value if empty\r\n\r\n  # Test: required - fails if value is empty\r\n  requiredResult: \"[1 2 3]\"\r\n\r\n  # Test: empty - returns true if value is empty\r\n\r\n  # Test: fail - explicitly fails with message\r\n\r\n  # ==========================================\r\n  # Coalesce/Ternary Functions\r\n  # ==========================================\r\n  # Test: coalesce - returns first non-empty value\r\n\r\n  # Test: ternary - conditional expression\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-logic-default/templates/logic-default-test.yaml",
            "content": "\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n\n  # Test: or - returns boolean OR of arguments\n\n  # Test: not - returns boolean negation\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n\n  # Test: ne - returns true if not equal\n\n  # Test: lt - returns true if less than\n\n  # Test: le - returns true if less than or equal\n\n  # Test: gt - returns true if greater than\n\n  # Test: ge - returns true if greater than or equal\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n\n  # Test: required - fails if value is empty\n  requiredResult: \"[1 2 3]\"\n\n  # Test: empty - returns true if value is empty\n\n  # Test: fail - explicitly fails with message\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n\n  # Test: ternary - conditional expression\n",
            "originalContent": "# Test: Logic and default functions - comprehensive testing\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n  {{- if .Values.testAnd }}\n  andResult: {{ and $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: or - returns boolean OR of arguments\n  {{- if .Values.testOr }}\n  orResult: {{ or $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: not - returns boolean negation\n  {{- if .Values.testNot }}\n  notResult: {{ not $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n  {{- if .Values.testEq }}\n  eqResult: {{ eq $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ne - returns true if not equal\n  {{- if .Values.testNe }}\n  neResult: {{ ne $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: lt - returns true if less than\n  {{- if .Values.testLt }}\n  ltResult: {{ lt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: le - returns true if less than or equal\n  {{- if .Values.testLe }}\n  leResult: {{ le $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: gt - returns true if greater than\n  {{- if .Values.testGt }}\n  gtResult: {{ gt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ge - returns true if greater than or equal\n  {{- if .Values.testGe }}\n  geResult: {{ ge $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n  {{- if .Values.testDefault }}\n  defaultResult: {{ default \"fallback\" $testData | quote }}\n  {{- end }}\n\n  # Test: required - fails if value is empty\n  {{- if .Values.testRequired }}\n  requiredResult: {{ required \"value is required\" $testData | quote }}\n  {{- end }}\n\n  # Test: empty - returns true if value is empty\n  {{- if .Values.testEmpty }}\n  emptyResult: {{ empty $testData | quote }}\n  {{- end }}\n\n  # Test: fail - explicitly fails with message\n  {{- if .Values.testFail }}\n  failResult: {{ fail (printf \"intentional fail with: %v\" $testData) }}\n  {{- end }}\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n  {{- if .Values.testCoalesce }}\n  coalesceResult: {{ coalesce $testData .Values.compareValue \"default\" | quote }}\n  {{- end }}\n\n  # Test: ternary - conditional expression\n  {{- if .Values.testTernary }}\n  ternaryResult: {{ ternary \"yes\" \"no\" $testData | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-logic-test\r\ndata:\r\n  # ==========================================\r\n  # Boolean Logic Functions\r\n  # ==========================================\r\n  # Test: and - returns boolean AND of arguments\r\n\r\n  # Test: or - returns boolean OR of arguments\r\n\r\n  # Test: not - returns boolean negation\r\n\r\n  # ==========================================\r\n  # Comparison Functions\r\n  # ==========================================\r\n  # Test: eq - returns true if equal\r\n\r\n  # Test: ne - returns true if not equal\r\n\r\n  # Test: lt - returns true if less than\r\n\r\n  # Test: le - returns true if less than or equal\r\n\r\n  # Test: gt - returns true if greater than\r\n\r\n  # Test: ge - returns true if greater than or equal\r\n\r\n  # ==========================================\r\n  # Default/Required Functions\r\n  # ==========================================\r\n  # Test: default - returns default value if empty\r\n\r\n  # Test: required - fails if value is empty\r\n  requiredResult: \"[1 2 3]\"\r\n\r\n  # Test: empty - returns true if value is empty\r\n\r\n  # Test: fail - explicitly fails with message\r\n\r\n  # ==========================================\r\n  # Coalesce/Ternary Functions\r\n  # ==========================================\r\n  # Test: coalesce - returns first non-empty value\r\n\r\n  # Test: ternary - conditional expression\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-logic-default/templates/logic-default-test.yaml",
            "content": "\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n\n  # Test: or - returns boolean OR of arguments\n\n  # Test: not - returns boolean negation\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n\n  # Test: ne - returns true if not equal\n\n  # Test: lt - returns true if less than\n\n  # Test: le - returns true if less than or equal\n\n  # Test: gt - returns true if greater than\n\n  # Test: ge - returns true if greater than or equal\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n\n  # Test: required - fails if value is empty\n  requiredResult: \"[1 2 3]\"\n\n  # Test: empty - returns true if value is empty\n\n  # Test: fail - explicitly fails with message\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n\n  # Test: ternary - conditional expression\n",
            "originalContent": "# Test: Logic and default functions - comprehensive testing\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n  {{- if .Values.testAnd }}\n  andResult: {{ and $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: or - returns boolean OR of arguments\n  {{- if .Values.testOr }}\n  orResult: {{ or $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: not - returns boolean negation\n  {{- if .Values.testNot }}\n  notResult: {{ not $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n  {{- if .Values.testEq }}\n  eqResult: {{ eq $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ne - returns true if not equal\n  {{- if .Values.testNe }}\n  neResult: {{ ne $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: lt - returns true if less than\n  {{- if .Values.testLt }}\n  ltResult: {{ lt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: le - returns true if less than or equal\n  {{- if .Values.testLe }}\n  leResult: {{ le $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: gt - returns true if greater than\n  {{- if .Values.testGt }}\n  gtResult: {{ gt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ge - returns true if greater than or equal\n  {{- if .Values.testGe }}\n  geResult: {{ ge $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n  {{- if .Values.testDefault }}\n  defaultResult: {{ default \"fallback\" $testData | quote }}\n  {{- end }}\n\n  # Test: required - fails if value is empty\n  {{- if .Values.testRequired }}\n  requiredResult: {{ required \"value is required\" $testData | quote }}\n  {{- end }}\n\n  # Test: empty - returns true if value is empty\n  {{- if .Values.testEmpty }}\n  emptyResult: {{ empty $testData | quote }}\n  {{- end }}\n\n  # Test: fail - explicitly fails with message\n  {{- if .Values.testFail }}\n  failResult: {{ fail (printf \"intentional fail with: %v\" $testData) }}\n  {{- end }}\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n  {{- if .Values.testCoalesce }}\n  coalesceResult: {{ coalesce $testData .Values.compareValue \"default\" | quote }}\n  {{- end }}\n\n  # Test: ternary - conditional expression\n  {{- if .Values.testTernary }}\n  ternaryResult: {{ ternary \"yes\" \"no\" $testData | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "required",
      "dataType": "dictValue",
      "valuesOptions": {
        "values": [
          "testRequired=true",
          "testDataType=dictValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-logic-test\r\ndata:\r\n  # ==========================================\r\n  # Boolean Logic Functions\r\n  # ==========================================\r\n  # Test: and - returns boolean AND of arguments\r\n\r\n  # Test: or - returns boolean OR of arguments\r\n\r\n  # Test: not - returns boolean negation\r\n\r\n  # ==========================================\r\n  # Comparison Functions\r\n  # ==========================================\r\n  # Test: eq - returns true if equal\r\n\r\n  # Test: ne - returns true if not equal\r\n\r\n  # Test: lt - returns true if less than\r\n\r\n  # Test: le - returns true if less than or equal\r\n\r\n  # Test: gt - returns true if greater than\r\n\r\n  # Test: ge - returns true if greater than or equal\r\n\r\n  # ==========================================\r\n  # Default/Required Functions\r\n  # ==========================================\r\n  # Test: default - returns default value if empty\r\n\r\n  # Test: required - fails if value is empty\r\n  requiredResult: \"map[key:value]\"\r\n\r\n  # Test: empty - returns true if value is empty\r\n\r\n  # Test: fail - explicitly fails with message\r\n\r\n  # ==========================================\r\n  # Coalesce/Ternary Functions\r\n  # ==========================================\r\n  # Test: coalesce - returns first non-empty value\r\n\r\n  # Test: ternary - conditional expression\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-logic-default/templates/logic-default-test.yaml",
            "content": "\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n\n  # Test: or - returns boolean OR of arguments\n\n  # Test: not - returns boolean negation\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n\n  # Test: ne - returns true if not equal\n\n  # Test: lt - returns true if less than\n\n  # Test: le - returns true if less than or equal\n\n  # Test: gt - returns true if greater than\n\n  # Test: ge - returns true if greater than or equal\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n\n  # Test: required - fails if value is empty\n  requiredResult: \"map[key:value]\"\n\n  # Test: empty - returns true if value is empty\n\n  # Test: fail - explicitly fails with message\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n\n  # Test: ternary - conditional expression\n",
            "originalContent": "# Test: Logic and default functions - comprehensive testing\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n  {{- if .Values.testAnd }}\n  andResult: {{ and $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: or - returns boolean OR of arguments\n  {{- if .Values.testOr }}\n  orResult: {{ or $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: not - returns boolean negation\n  {{- if .Values.testNot }}\n  notResult: {{ not $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n  {{- if .Values.testEq }}\n  eqResult: {{ eq $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ne - returns true if not equal\n  {{- if .Values.testNe }}\n  neResult: {{ ne $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: lt - returns true if less than\n  {{- if .Values.testLt }}\n  ltResult: {{ lt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: le - returns true if less than or equal\n  {{- if .Values.testLe }}\n  leResult: {{ le $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: gt - returns true if greater than\n  {{- if .Values.testGt }}\n  gtResult: {{ gt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ge - returns true if greater than or equal\n  {{- if .Values.testGe }}\n  geResult: {{ ge $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n  {{- if .Values.testDefault }}\n  defaultResult: {{ default \"fallback\" $testData | quote }}\n  {{- end }}\n\n  # Test: required - fails if value is empty\n  {{- if .Values.testRequired }}\n  requiredResult: {{ required \"value is required\" $testData | quote }}\n  {{- end }}\n\n  # Test: empty - returns true if value is empty\n  {{- if .Values.testEmpty }}\n  emptyResult: {{ empty $testData | quote }}\n  {{- end }}\n\n  # Test: fail - explicitly fails with message\n  {{- if .Values.testFail }}\n  failResult: {{ fail (printf \"intentional fail with: %v\" $testData) }}\n  {{- end }}\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n  {{- if .Values.testCoalesce }}\n  coalesceResult: {{ coalesce $testData .Values.compareValue \"default\" | quote }}\n  {{- end }}\n\n  # Test: ternary - conditional expression\n  {{- if .Values.testTernary }}\n  ternaryResult: {{ ternary \"yes\" \"no\" $testData | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-logic-test\r\ndata:\r\n  # ==========================================\r\n  # Boolean Logic Functions\r\n  # ==========================================\r\n  # Test: and - returns boolean AND of arguments\r\n\r\n  # Test: or - returns boolean OR of arguments\r\n\r\n  # Test: not - returns boolean negation\r\n\r\n  # ==========================================\r\n  # Comparison Functions\r\n  # ==========================================\r\n  # Test: eq - returns true if equal\r\n\r\n  # Test: ne - returns true if not equal\r\n\r\n  # Test: lt - returns true if less than\r\n\r\n  # Test: le - returns true if less than or equal\r\n\r\n  # Test: gt - returns true if greater than\r\n\r\n  # Test: ge - returns true if greater than or equal\r\n\r\n  # ==========================================\r\n  # Default/Required Functions\r\n  # ==========================================\r\n  # Test: default - returns default value if empty\r\n\r\n  # Test: required - fails if value is empty\r\n  requiredResult: \"map[key:value]\"\r\n\r\n  # Test: empty - returns true if value is empty\r\n\r\n  # Test: fail - explicitly fails with message\r\n\r\n  # ==========================================\r\n  # Coalesce/Ternary Functions\r\n  # ==========================================\r\n  # Test: coalesce - returns first non-empty value\r\n\r\n  # Test: ternary - conditional expression\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-logic-default/templates/logic-default-test.yaml",
            "content": "\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n\n  # Test: or - returns boolean OR of arguments\n\n  # Test: not - returns boolean negation\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n\n  # Test: ne - returns true if not equal\n\n  # Test: lt - returns true if less than\n\n  # Test: le - returns true if less than or equal\n\n  # Test: gt - returns true if greater than\n\n  # Test: ge - returns true if greater than or equal\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n\n  # Test: required - fails if value is empty\n  requiredResult: \"map[key:value]\"\n\n  # Test: empty - returns true if value is empty\n\n  # Test: fail - explicitly fails with message\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n\n  # Test: ternary - conditional expression\n",
            "originalContent": "# Test: Logic and default functions - comprehensive testing\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n  {{- if .Values.testAnd }}\n  andResult: {{ and $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: or - returns boolean OR of arguments\n  {{- if .Values.testOr }}\n  orResult: {{ or $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: not - returns boolean negation\n  {{- if .Values.testNot }}\n  notResult: {{ not $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n  {{- if .Values.testEq }}\n  eqResult: {{ eq $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ne - returns true if not equal\n  {{- if .Values.testNe }}\n  neResult: {{ ne $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: lt - returns true if less than\n  {{- if .Values.testLt }}\n  ltResult: {{ lt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: le - returns true if less than or equal\n  {{- if .Values.testLe }}\n  leResult: {{ le $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: gt - returns true if greater than\n  {{- if .Values.testGt }}\n  gtResult: {{ gt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ge - returns true if greater than or equal\n  {{- if .Values.testGe }}\n  geResult: {{ ge $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n  {{- if .Values.testDefault }}\n  defaultResult: {{ default \"fallback\" $testData | quote }}\n  {{- end }}\n\n  # Test: required - fails if value is empty\n  {{- if .Values.testRequired }}\n  requiredResult: {{ required \"value is required\" $testData | quote }}\n  {{- end }}\n\n  # Test: empty - returns true if value is empty\n  {{- if .Values.testEmpty }}\n  emptyResult: {{ empty $testData | quote }}\n  {{- end }}\n\n  # Test: fail - explicitly fails with message\n  {{- if .Values.testFail }}\n  failResult: {{ fail (printf \"intentional fail with: %v\" $testData) }}\n  {{- end }}\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n  {{- if .Values.testCoalesce }}\n  coalesceResult: {{ coalesce $testData .Values.compareValue \"default\" | quote }}\n  {{- end }}\n\n  # Test: ternary - conditional expression\n  {{- if .Values.testTernary }}\n  ternaryResult: {{ ternary \"yes\" \"no\" $testData | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "empty",
      "dataType": "nilValue",
      "valuesOptions": {
        "values": [
          "testEmpty=true",
          "testDataType=nilValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-logic-test\r\ndata:\r\n  # ==========================================\r\n  # Boolean Logic Functions\r\n  # ==========================================\r\n  # Test: and - returns boolean AND of arguments\r\n\r\n  # Test: or - returns boolean OR of arguments\r\n\r\n  # Test: not - returns boolean negation\r\n\r\n  # ==========================================\r\n  # Comparison Functions\r\n  # ==========================================\r\n  # Test: eq - returns true if equal\r\n\r\n  # Test: ne - returns true if not equal\r\n\r\n  # Test: lt - returns true if less than\r\n\r\n  # Test: le - returns true if less than or equal\r\n\r\n  # Test: gt - returns true if greater than\r\n\r\n  # Test: ge - returns true if greater than or equal\r\n\r\n  # ==========================================\r\n  # Default/Required Functions\r\n  # ==========================================\r\n  # Test: default - returns default value if empty\r\n\r\n  # Test: required - fails if value is empty\r\n\r\n  # Test: empty - returns true if value is empty\r\n  emptyResult: \"true\"\r\n\r\n  # Test: fail - explicitly fails with message\r\n\r\n  # ==========================================\r\n  # Coalesce/Ternary Functions\r\n  # ==========================================\r\n  # Test: coalesce - returns first non-empty value\r\n\r\n  # Test: ternary - conditional expression\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-logic-default/templates/logic-default-test.yaml",
            "content": "\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n\n  # Test: or - returns boolean OR of arguments\n\n  # Test: not - returns boolean negation\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n\n  # Test: ne - returns true if not equal\n\n  # Test: lt - returns true if less than\n\n  # Test: le - returns true if less than or equal\n\n  # Test: gt - returns true if greater than\n\n  # Test: ge - returns true if greater than or equal\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n\n  # Test: required - fails if value is empty\n\n  # Test: empty - returns true if value is empty\n  emptyResult: \"true\"\n\n  # Test: fail - explicitly fails with message\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n\n  # Test: ternary - conditional expression\n",
            "originalContent": "# Test: Logic and default functions - comprehensive testing\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n  {{- if .Values.testAnd }}\n  andResult: {{ and $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: or - returns boolean OR of arguments\n  {{- if .Values.testOr }}\n  orResult: {{ or $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: not - returns boolean negation\n  {{- if .Values.testNot }}\n  notResult: {{ not $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n  {{- if .Values.testEq }}\n  eqResult: {{ eq $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ne - returns true if not equal\n  {{- if .Values.testNe }}\n  neResult: {{ ne $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: lt - returns true if less than\n  {{- if .Values.testLt }}\n  ltResult: {{ lt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: le - returns true if less than or equal\n  {{- if .Values.testLe }}\n  leResult: {{ le $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: gt - returns true if greater than\n  {{- if .Values.testGt }}\n  gtResult: {{ gt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ge - returns true if greater than or equal\n  {{- if .Values.testGe }}\n  geResult: {{ ge $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n  {{- if .Values.testDefault }}\n  defaultResult: {{ default \"fallback\" $testData | quote }}\n  {{- end }}\n\n  # Test: required - fails if value is empty\n  {{- if .Values.testRequired }}\n  requiredResult: {{ required \"value is required\" $testData | quote }}\n  {{- end }}\n\n  # Test: empty - returns true if value is empty\n  {{- if .Values.testEmpty }}\n  emptyResult: {{ empty $testData | quote }}\n  {{- end }}\n\n  # Test: fail - explicitly fails with message\n  {{- if .Values.testFail }}\n  failResult: {{ fail (printf \"intentional fail with: %v\" $testData) }}\n  {{- end }}\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n  {{- if .Values.testCoalesce }}\n  coalesceResult: {{ coalesce $testData .Values.compareValue \"default\" | quote }}\n  {{- end }}\n\n  # Test: ternary - conditional expression\n  {{- if .Values.testTernary }}\n  ternaryResult: {{ ternary \"yes\" \"no\" $testData | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-logic-test\r\ndata:\r\n  # ==========================================\r\n  # Boolean Logic Functions\r\n  # ==========================================\r\n  # Test: and - returns boolean AND of arguments\r\n\r\n  # Test: or - returns boolean OR of arguments\r\n\r\n  # Test: not - returns boolean negation\r\n\r\n  # ==========================================\r\n  # Comparison Functions\r\n  # ==========================================\r\n  # Test: eq - returns true if equal\r\n\r\n  # Test: ne - returns true if not equal\r\n\r\n  # Test: lt - returns true if less than\r\n\r\n  # Test: le - returns true if less than or equal\r\n\r\n  # Test: gt - returns true if greater than\r\n\r\n  # Test: ge - returns true if greater than or equal\r\n\r\n  # ==========================================\r\n  # Default/Required Functions\r\n  # ==========================================\r\n  # Test: default - returns default value if empty\r\n\r\n  # Test: required - fails if value is empty\r\n\r\n  # Test: empty - returns true if value is empty\r\n  emptyResult: \"true\"\r\n\r\n  # Test: fail - explicitly fails with message\r\n\r\n  # ==========================================\r\n  # Coalesce/Ternary Functions\r\n  # ==========================================\r\n  # Test: coalesce - returns first non-empty value\r\n\r\n  # Test: ternary - conditional expression\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-logic-default/templates/logic-default-test.yaml",
            "content": "\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n\n  # Test: or - returns boolean OR of arguments\n\n  # Test: not - returns boolean negation\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n\n  # Test: ne - returns true if not equal\n\n  # Test: lt - returns true if less than\n\n  # Test: le - returns true if less than or equal\n\n  # Test: gt - returns true if greater than\n\n  # Test: ge - returns true if greater than or equal\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n\n  # Test: required - fails if value is empty\n\n  # Test: empty - returns true if value is empty\n  emptyResult: \"true\"\n\n  # Test: fail - explicitly fails with message\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n\n  # Test: ternary - conditional expression\n",
            "originalContent": "# Test: Logic and default functions - comprehensive testing\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n  {{- if .Values.testAnd }}\n  andResult: {{ and $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: or - returns boolean OR of arguments\n  {{- if .Values.testOr }}\n  orResult: {{ or $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: not - returns boolean negation\n  {{- if .Values.testNot }}\n  notResult: {{ not $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n  {{- if .Values.testEq }}\n  eqResult: {{ eq $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ne - returns true if not equal\n  {{- if .Values.testNe }}\n  neResult: {{ ne $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: lt - returns true if less than\n  {{- if .Values.testLt }}\n  ltResult: {{ lt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: le - returns true if less than or equal\n  {{- if .Values.testLe }}\n  leResult: {{ le $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: gt - returns true if greater than\n  {{- if .Values.testGt }}\n  gtResult: {{ gt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ge - returns true if greater than or equal\n  {{- if .Values.testGe }}\n  geResult: {{ ge $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n  {{- if .Values.testDefault }}\n  defaultResult: {{ default \"fallback\" $testData | quote }}\n  {{- end }}\n\n  # Test: required - fails if value is empty\n  {{- if .Values.testRequired }}\n  requiredResult: {{ required \"value is required\" $testData | quote }}\n  {{- end }}\n\n  # Test: empty - returns true if value is empty\n  {{- if .Values.testEmpty }}\n  emptyResult: {{ empty $testData | quote }}\n  {{- end }}\n\n  # Test: fail - explicitly fails with message\n  {{- if .Values.testFail }}\n  failResult: {{ fail (printf \"intentional fail with: %v\" $testData) }}\n  {{- end }}\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n  {{- if .Values.testCoalesce }}\n  coalesceResult: {{ coalesce $testData .Values.compareValue \"default\" | quote }}\n  {{- end }}\n\n  # Test: ternary - conditional expression\n  {{- if .Values.testTernary }}\n  ternaryResult: {{ ternary \"yes\" \"no\" $testData | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "empty",
      "dataType": "stringValue",
      "valuesOptions": {
        "values": [
          "testEmpty=true",
          "testDataType=stringValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-logic-test\r\ndata:\r\n  # ==========================================\r\n  # Boolean Logic Functions\r\n  # ==========================================\r\n  # Test: and - returns boolean AND of arguments\r\n\r\n  # Test: or - returns boolean OR of arguments\r\n\r\n  # Test: not - returns boolean negation\r\n\r\n  # ==========================================\r\n  # Comparison Functions\r\n  # ==========================================\r\n  # Test: eq - returns true if equal\r\n\r\n  # Test: ne - returns true if not equal\r\n\r\n  # Test: lt - returns true if less than\r\n\r\n  # Test: le - returns true if less than or equal\r\n\r\n  # Test: gt - returns true if greater than\r\n\r\n  # Test: ge - returns true if greater than or equal\r\n\r\n  # ==========================================\r\n  # Default/Required Functions\r\n  # ==========================================\r\n  # Test: default - returns default value if empty\r\n\r\n  # Test: required - fails if value is empty\r\n\r\n  # Test: empty - returns true if value is empty\r\n  emptyResult: \"false\"\r\n\r\n  # Test: fail - explicitly fails with message\r\n\r\n  # ==========================================\r\n  # Coalesce/Ternary Functions\r\n  # ==========================================\r\n  # Test: coalesce - returns first non-empty value\r\n\r\n  # Test: ternary - conditional expression\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-logic-default/templates/logic-default-test.yaml",
            "content": "\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n\n  # Test: or - returns boolean OR of arguments\n\n  # Test: not - returns boolean negation\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n\n  # Test: ne - returns true if not equal\n\n  # Test: lt - returns true if less than\n\n  # Test: le - returns true if less than or equal\n\n  # Test: gt - returns true if greater than\n\n  # Test: ge - returns true if greater than or equal\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n\n  # Test: required - fails if value is empty\n\n  # Test: empty - returns true if value is empty\n  emptyResult: \"false\"\n\n  # Test: fail - explicitly fails with message\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n\n  # Test: ternary - conditional expression\n",
            "originalContent": "# Test: Logic and default functions - comprehensive testing\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n  {{- if .Values.testAnd }}\n  andResult: {{ and $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: or - returns boolean OR of arguments\n  {{- if .Values.testOr }}\n  orResult: {{ or $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: not - returns boolean negation\n  {{- if .Values.testNot }}\n  notResult: {{ not $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n  {{- if .Values.testEq }}\n  eqResult: {{ eq $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ne - returns true if not equal\n  {{- if .Values.testNe }}\n  neResult: {{ ne $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: lt - returns true if less than\n  {{- if .Values.testLt }}\n  ltResult: {{ lt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: le - returns true if less than or equal\n  {{- if .Values.testLe }}\n  leResult: {{ le $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: gt - returns true if greater than\n  {{- if .Values.testGt }}\n  gtResult: {{ gt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ge - returns true if greater than or equal\n  {{- if .Values.testGe }}\n  geResult: {{ ge $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n  {{- if .Values.testDefault }}\n  defaultResult: {{ default \"fallback\" $testData | quote }}\n  {{- end }}\n\n  # Test: required - fails if value is empty\n  {{- if .Values.testRequired }}\n  requiredResult: {{ required \"value is required\" $testData | quote }}\n  {{- end }}\n\n  # Test: empty - returns true if value is empty\n  {{- if .Values.testEmpty }}\n  emptyResult: {{ empty $testData | quote }}\n  {{- end }}\n\n  # Test: fail - explicitly fails with message\n  {{- if .Values.testFail }}\n  failResult: {{ fail (printf \"intentional fail with: %v\" $testData) }}\n  {{- end }}\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n  {{- if .Values.testCoalesce }}\n  coalesceResult: {{ coalesce $testData .Values.compareValue \"default\" | quote }}\n  {{- end }}\n\n  # Test: ternary - conditional expression\n  {{- if .Values.testTernary }}\n  ternaryResult: {{ ternary \"yes\" \"no\" $testData | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-logic-test\r\ndata:\r\n  # ==========================================\r\n  # Boolean Logic Functions\r\n  # ==========================================\r\n  # Test: and - returns boolean AND of arguments\r\n\r\n  # Test: or - returns boolean OR of arguments\r\n\r\n  # Test: not - returns boolean negation\r\n\r\n  # ==========================================\r\n  # Comparison Functions\r\n  # ==========================================\r\n  # Test: eq - returns true if equal\r\n\r\n  # Test: ne - returns true if not equal\r\n\r\n  # Test: lt - returns true if less than\r\n\r\n  # Test: le - returns true if less than or equal\r\n\r\n  # Test: gt - returns true if greater than\r\n\r\n  # Test: ge - returns true if greater than or equal\r\n\r\n  # ==========================================\r\n  # Default/Required Functions\r\n  # ==========================================\r\n  # Test: default - returns default value if empty\r\n\r\n  # Test: required - fails if value is empty\r\n\r\n  # Test: empty - returns true if value is empty\r\n  emptyResult: \"false\"\r\n\r\n  # Test: fail - explicitly fails with message\r\n\r\n  # ==========================================\r\n  # Coalesce/Ternary Functions\r\n  # ==========================================\r\n  # Test: coalesce - returns first non-empty value\r\n\r\n  # Test: ternary - conditional expression\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-logic-default/templates/logic-default-test.yaml",
            "content": "\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n\n  # Test: or - returns boolean OR of arguments\n\n  # Test: not - returns boolean negation\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n\n  # Test: ne - returns true if not equal\n\n  # Test: lt - returns true if less than\n\n  # Test: le - returns true if less than or equal\n\n  # Test: gt - returns true if greater than\n\n  # Test: ge - returns true if greater than or equal\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n\n  # Test: required - fails if value is empty\n\n  # Test: empty - returns true if value is empty\n  emptyResult: \"false\"\n\n  # Test: fail - explicitly fails with message\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n\n  # Test: ternary - conditional expression\n",
            "originalContent": "# Test: Logic and default functions - comprehensive testing\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n  {{- if .Values.testAnd }}\n  andResult: {{ and $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: or - returns boolean OR of arguments\n  {{- if .Values.testOr }}\n  orResult: {{ or $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: not - returns boolean negation\n  {{- if .Values.testNot }}\n  notResult: {{ not $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n  {{- if .Values.testEq }}\n  eqResult: {{ eq $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ne - returns true if not equal\n  {{- if .Values.testNe }}\n  neResult: {{ ne $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: lt - returns true if less than\n  {{- if .Values.testLt }}\n  ltResult: {{ lt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: le - returns true if less than or equal\n  {{- if .Values.testLe }}\n  leResult: {{ le $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: gt - returns true if greater than\n  {{- if .Values.testGt }}\n  gtResult: {{ gt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ge - returns true if greater than or equal\n  {{- if .Values.testGe }}\n  geResult: {{ ge $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n  {{- if .Values.testDefault }}\n  defaultResult: {{ default \"fallback\" $testData | quote }}\n  {{- end }}\n\n  # Test: required - fails if value is empty\n  {{- if .Values.testRequired }}\n  requiredResult: {{ required \"value is required\" $testData | quote }}\n  {{- end }}\n\n  # Test: empty - returns true if value is empty\n  {{- if .Values.testEmpty }}\n  emptyResult: {{ empty $testData | quote }}\n  {{- end }}\n\n  # Test: fail - explicitly fails with message\n  {{- if .Values.testFail }}\n  failResult: {{ fail (printf \"intentional fail with: %v\" $testData) }}\n  {{- end }}\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n  {{- if .Values.testCoalesce }}\n  coalesceResult: {{ coalesce $testData .Values.compareValue \"default\" | quote }}\n  {{- end }}\n\n  # Test: ternary - conditional expression\n  {{- if .Values.testTernary }}\n  ternaryResult: {{ ternary \"yes\" \"no\" $testData | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "empty",
      "dataType": "numberValue",
      "valuesOptions": {
        "values": [
          "testEmpty=true",
          "testDataType=numberValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-logic-test\r\ndata:\r\n  # ==========================================\r\n  # Boolean Logic Functions\r\n  # ==========================================\r\n  # Test: and - returns boolean AND of arguments\r\n\r\n  # Test: or - returns boolean OR of arguments\r\n\r\n  # Test: not - returns boolean negation\r\n\r\n  # ==========================================\r\n  # Comparison Functions\r\n  # ==========================================\r\n  # Test: eq - returns true if equal\r\n\r\n  # Test: ne - returns true if not equal\r\n\r\n  # Test: lt - returns true if less than\r\n\r\n  # Test: le - returns true if less than or equal\r\n\r\n  # Test: gt - returns true if greater than\r\n\r\n  # Test: ge - returns true if greater than or equal\r\n\r\n  # ==========================================\r\n  # Default/Required Functions\r\n  # ==========================================\r\n  # Test: default - returns default value if empty\r\n\r\n  # Test: required - fails if value is empty\r\n\r\n  # Test: empty - returns true if value is empty\r\n  emptyResult: \"false\"\r\n\r\n  # Test: fail - explicitly fails with message\r\n\r\n  # ==========================================\r\n  # Coalesce/Ternary Functions\r\n  # ==========================================\r\n  # Test: coalesce - returns first non-empty value\r\n\r\n  # Test: ternary - conditional expression\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-logic-default/templates/logic-default-test.yaml",
            "content": "\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n\n  # Test: or - returns boolean OR of arguments\n\n  # Test: not - returns boolean negation\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n\n  # Test: ne - returns true if not equal\n\n  # Test: lt - returns true if less than\n\n  # Test: le - returns true if less than or equal\n\n  # Test: gt - returns true if greater than\n\n  # Test: ge - returns true if greater than or equal\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n\n  # Test: required - fails if value is empty\n\n  # Test: empty - returns true if value is empty\n  emptyResult: \"false\"\n\n  # Test: fail - explicitly fails with message\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n\n  # Test: ternary - conditional expression\n",
            "originalContent": "# Test: Logic and default functions - comprehensive testing\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n  {{- if .Values.testAnd }}\n  andResult: {{ and $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: or - returns boolean OR of arguments\n  {{- if .Values.testOr }}\n  orResult: {{ or $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: not - returns boolean negation\n  {{- if .Values.testNot }}\n  notResult: {{ not $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n  {{- if .Values.testEq }}\n  eqResult: {{ eq $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ne - returns true if not equal\n  {{- if .Values.testNe }}\n  neResult: {{ ne $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: lt - returns true if less than\n  {{- if .Values.testLt }}\n  ltResult: {{ lt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: le - returns true if less than or equal\n  {{- if .Values.testLe }}\n  leResult: {{ le $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: gt - returns true if greater than\n  {{- if .Values.testGt }}\n  gtResult: {{ gt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ge - returns true if greater than or equal\n  {{- if .Values.testGe }}\n  geResult: {{ ge $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n  {{- if .Values.testDefault }}\n  defaultResult: {{ default \"fallback\" $testData | quote }}\n  {{- end }}\n\n  # Test: required - fails if value is empty\n  {{- if .Values.testRequired }}\n  requiredResult: {{ required \"value is required\" $testData | quote }}\n  {{- end }}\n\n  # Test: empty - returns true if value is empty\n  {{- if .Values.testEmpty }}\n  emptyResult: {{ empty $testData | quote }}\n  {{- end }}\n\n  # Test: fail - explicitly fails with message\n  {{- if .Values.testFail }}\n  failResult: {{ fail (printf \"intentional fail with: %v\" $testData) }}\n  {{- end }}\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n  {{- if .Values.testCoalesce }}\n  coalesceResult: {{ coalesce $testData .Values.compareValue \"default\" | quote }}\n  {{- end }}\n\n  # Test: ternary - conditional expression\n  {{- if .Values.testTernary }}\n  ternaryResult: {{ ternary \"yes\" \"no\" $testData | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-logic-test\r\ndata:\r\n  # ==========================================\r\n  # Boolean Logic Functions\r\n  # ==========================================\r\n  # Test: and - returns boolean AND of arguments\r\n\r\n  # Test: or - returns boolean OR of arguments\r\n\r\n  # Test: not - returns boolean negation\r\n\r\n  # ==========================================\r\n  # Comparison Functions\r\n  # ==========================================\r\n  # Test: eq - returns true if equal\r\n\r\n  # Test: ne - returns true if not equal\r\n\r\n  # Test: lt - returns true if less than\r\n\r\n  # Test: le - returns true if less than or equal\r\n\r\n  # Test: gt - returns true if greater than\r\n\r\n  # Test: ge - returns true if greater than or equal\r\n\r\n  # ==========================================\r\n  # Default/Required Functions\r\n  # ==========================================\r\n  # Test: default - returns default value if empty\r\n\r\n  # Test: required - fails if value is empty\r\n\r\n  # Test: empty - returns true if value is empty\r\n  emptyResult: \"false\"\r\n\r\n  # Test: fail - explicitly fails with message\r\n\r\n  # ==========================================\r\n  # Coalesce/Ternary Functions\r\n  # ==========================================\r\n  # Test: coalesce - returns first non-empty value\r\n\r\n  # Test: ternary - conditional expression\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-logic-default/templates/logic-default-test.yaml",
            "content": "\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n\n  # Test: or - returns boolean OR of arguments\n\n  # Test: not - returns boolean negation\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n\n  # Test: ne - returns true if not equal\n\n  # Test: lt - returns true if less than\n\n  # Test: le - returns true if less than or equal\n\n  # Test: gt - returns true if greater than\n\n  # Test: ge - returns true if greater than or equal\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n\n  # Test: required - fails if value is empty\n\n  # Test: empty - returns true if value is empty\n  emptyResult: \"false\"\n\n  # Test: fail - explicitly fails with message\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n\n  # Test: ternary - conditional expression\n",
            "originalContent": "# Test: Logic and default functions - comprehensive testing\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n  {{- if .Values.testAnd }}\n  andResult: {{ and $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: or - returns boolean OR of arguments\n  {{- if .Values.testOr }}\n  orResult: {{ or $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: not - returns boolean negation\n  {{- if .Values.testNot }}\n  notResult: {{ not $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n  {{- if .Values.testEq }}\n  eqResult: {{ eq $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ne - returns true if not equal\n  {{- if .Values.testNe }}\n  neResult: {{ ne $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: lt - returns true if less than\n  {{- if .Values.testLt }}\n  ltResult: {{ lt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: le - returns true if less than or equal\n  {{- if .Values.testLe }}\n  leResult: {{ le $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: gt - returns true if greater than\n  {{- if .Values.testGt }}\n  gtResult: {{ gt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ge - returns true if greater than or equal\n  {{- if .Values.testGe }}\n  geResult: {{ ge $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n  {{- if .Values.testDefault }}\n  defaultResult: {{ default \"fallback\" $testData | quote }}\n  {{- end }}\n\n  # Test: required - fails if value is empty\n  {{- if .Values.testRequired }}\n  requiredResult: {{ required \"value is required\" $testData | quote }}\n  {{- end }}\n\n  # Test: empty - returns true if value is empty\n  {{- if .Values.testEmpty }}\n  emptyResult: {{ empty $testData | quote }}\n  {{- end }}\n\n  # Test: fail - explicitly fails with message\n  {{- if .Values.testFail }}\n  failResult: {{ fail (printf \"intentional fail with: %v\" $testData) }}\n  {{- end }}\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n  {{- if .Values.testCoalesce }}\n  coalesceResult: {{ coalesce $testData .Values.compareValue \"default\" | quote }}\n  {{- end }}\n\n  # Test: ternary - conditional expression\n  {{- if .Values.testTernary }}\n  ternaryResult: {{ ternary \"yes\" \"no\" $testData | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "empty",
      "dataType": "boolValue",
      "valuesOptions": {
        "values": [
          "testEmpty=true",
          "testDataType=boolValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-logic-test\r\ndata:\r\n  # ==========================================\r\n  # Boolean Logic Functions\r\n  # ==========================================\r\n  # Test: and - returns boolean AND of arguments\r\n\r\n  # Test: or - returns boolean OR of arguments\r\n\r\n  # Test: not - returns boolean negation\r\n\r\n  # ==========================================\r\n  # Comparison Functions\r\n  # ==========================================\r\n  # Test: eq - returns true if equal\r\n\r\n  # Test: ne - returns true if not equal\r\n\r\n  # Test: lt - returns true if less than\r\n\r\n  # Test: le - returns true if less than or equal\r\n\r\n  # Test: gt - returns true if greater than\r\n\r\n  # Test: ge - returns true if greater than or equal\r\n\r\n  # ==========================================\r\n  # Default/Required Functions\r\n  # ==========================================\r\n  # Test: default - returns default value if empty\r\n\r\n  # Test: required - fails if value is empty\r\n\r\n  # Test: empty - returns true if value is empty\r\n  emptyResult: \"false\"\r\n\r\n  # Test: fail - explicitly fails with message\r\n\r\n  # ==========================================\r\n  # Coalesce/Ternary Functions\r\n  # ==========================================\r\n  # Test: coalesce - returns first non-empty value\r\n\r\n  # Test: ternary - conditional expression\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-logic-default/templates/logic-default-test.yaml",
            "content": "\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n\n  # Test: or - returns boolean OR of arguments\n\n  # Test: not - returns boolean negation\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n\n  # Test: ne - returns true if not equal\n\n  # Test: lt - returns true if less than\n\n  # Test: le - returns true if less than or equal\n\n  # Test: gt - returns true if greater than\n\n  # Test: ge - returns true if greater than or equal\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n\n  # Test: required - fails if value is empty\n\n  # Test: empty - returns true if value is empty\n  emptyResult: \"false\"\n\n  # Test: fail - explicitly fails with message\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n\n  # Test: ternary - conditional expression\n",
            "originalContent": "# Test: Logic and default functions - comprehensive testing\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n  {{- if .Values.testAnd }}\n  andResult: {{ and $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: or - returns boolean OR of arguments\n  {{- if .Values.testOr }}\n  orResult: {{ or $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: not - returns boolean negation\n  {{- if .Values.testNot }}\n  notResult: {{ not $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n  {{- if .Values.testEq }}\n  eqResult: {{ eq $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ne - returns true if not equal\n  {{- if .Values.testNe }}\n  neResult: {{ ne $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: lt - returns true if less than\n  {{- if .Values.testLt }}\n  ltResult: {{ lt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: le - returns true if less than or equal\n  {{- if .Values.testLe }}\n  leResult: {{ le $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: gt - returns true if greater than\n  {{- if .Values.testGt }}\n  gtResult: {{ gt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ge - returns true if greater than or equal\n  {{- if .Values.testGe }}\n  geResult: {{ ge $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n  {{- if .Values.testDefault }}\n  defaultResult: {{ default \"fallback\" $testData | quote }}\n  {{- end }}\n\n  # Test: required - fails if value is empty\n  {{- if .Values.testRequired }}\n  requiredResult: {{ required \"value is required\" $testData | quote }}\n  {{- end }}\n\n  # Test: empty - returns true if value is empty\n  {{- if .Values.testEmpty }}\n  emptyResult: {{ empty $testData | quote }}\n  {{- end }}\n\n  # Test: fail - explicitly fails with message\n  {{- if .Values.testFail }}\n  failResult: {{ fail (printf \"intentional fail with: %v\" $testData) }}\n  {{- end }}\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n  {{- if .Values.testCoalesce }}\n  coalesceResult: {{ coalesce $testData .Values.compareValue \"default\" | quote }}\n  {{- end }}\n\n  # Test: ternary - conditional expression\n  {{- if .Values.testTernary }}\n  ternaryResult: {{ ternary \"yes\" \"no\" $testData | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-logic-test\r\ndata:\r\n  # ==========================================\r\n  # Boolean Logic Functions\r\n  # ==========================================\r\n  # Test: and - returns boolean AND of arguments\r\n\r\n  # Test: or - returns boolean OR of arguments\r\n\r\n  # Test: not - returns boolean negation\r\n\r\n  # ==========================================\r\n  # Comparison Functions\r\n  # ==========================================\r\n  # Test: eq - returns true if equal\r\n\r\n  # Test: ne - returns true if not equal\r\n\r\n  # Test: lt - returns true if less than\r\n\r\n  # Test: le - returns true if less than or equal\r\n\r\n  # Test: gt - returns true if greater than\r\n\r\n  # Test: ge - returns true if greater than or equal\r\n\r\n  # ==========================================\r\n  # Default/Required Functions\r\n  # ==========================================\r\n  # Test: default - returns default value if empty\r\n\r\n  # Test: required - fails if value is empty\r\n\r\n  # Test: empty - returns true if value is empty\r\n  emptyResult: \"false\"\r\n\r\n  # Test: fail - explicitly fails with message\r\n\r\n  # ==========================================\r\n  # Coalesce/Ternary Functions\r\n  # ==========================================\r\n  # Test: coalesce - returns first non-empty value\r\n\r\n  # Test: ternary - conditional expression\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-logic-default/templates/logic-default-test.yaml",
            "content": "\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n\n  # Test: or - returns boolean OR of arguments\n\n  # Test: not - returns boolean negation\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n\n  # Test: ne - returns true if not equal\n\n  # Test: lt - returns true if less than\n\n  # Test: le - returns true if less than or equal\n\n  # Test: gt - returns true if greater than\n\n  # Test: ge - returns true if greater than or equal\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n\n  # Test: required - fails if value is empty\n\n  # Test: empty - returns true if value is empty\n  emptyResult: \"false\"\n\n  # Test: fail - explicitly fails with message\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n\n  # Test: ternary - conditional expression\n",
            "originalContent": "# Test: Logic and default functions - comprehensive testing\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n  {{- if .Values.testAnd }}\n  andResult: {{ and $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: or - returns boolean OR of arguments\n  {{- if .Values.testOr }}\n  orResult: {{ or $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: not - returns boolean negation\n  {{- if .Values.testNot }}\n  notResult: {{ not $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n  {{- if .Values.testEq }}\n  eqResult: {{ eq $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ne - returns true if not equal\n  {{- if .Values.testNe }}\n  neResult: {{ ne $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: lt - returns true if less than\n  {{- if .Values.testLt }}\n  ltResult: {{ lt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: le - returns true if less than or equal\n  {{- if .Values.testLe }}\n  leResult: {{ le $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: gt - returns true if greater than\n  {{- if .Values.testGt }}\n  gtResult: {{ gt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ge - returns true if greater than or equal\n  {{- if .Values.testGe }}\n  geResult: {{ ge $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n  {{- if .Values.testDefault }}\n  defaultResult: {{ default \"fallback\" $testData | quote }}\n  {{- end }}\n\n  # Test: required - fails if value is empty\n  {{- if .Values.testRequired }}\n  requiredResult: {{ required \"value is required\" $testData | quote }}\n  {{- end }}\n\n  # Test: empty - returns true if value is empty\n  {{- if .Values.testEmpty }}\n  emptyResult: {{ empty $testData | quote }}\n  {{- end }}\n\n  # Test: fail - explicitly fails with message\n  {{- if .Values.testFail }}\n  failResult: {{ fail (printf \"intentional fail with: %v\" $testData) }}\n  {{- end }}\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n  {{- if .Values.testCoalesce }}\n  coalesceResult: {{ coalesce $testData .Values.compareValue \"default\" | quote }}\n  {{- end }}\n\n  # Test: ternary - conditional expression\n  {{- if .Values.testTernary }}\n  ternaryResult: {{ ternary \"yes\" \"no\" $testData | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "empty",
      "dataType": "listValue",
      "valuesOptions": {
        "values": [
          "testEmpty=true",
          "testDataType=listValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-logic-test\r\ndata:\r\n  # ==========================================\r\n  # Boolean Logic Functions\r\n  # ==========================================\r\n  # Test: and - returns boolean AND of arguments\r\n\r\n  # Test: or - returns boolean OR of arguments\r\n\r\n  # Test: not - returns boolean negation\r\n\r\n  # ==========================================\r\n  # Comparison Functions\r\n  # ==========================================\r\n  # Test: eq - returns true if equal\r\n\r\n  # Test: ne - returns true if not equal\r\n\r\n  # Test: lt - returns true if less than\r\n\r\n  # Test: le - returns true if less than or equal\r\n\r\n  # Test: gt - returns true if greater than\r\n\r\n  # Test: ge - returns true if greater than or equal\r\n\r\n  # ==========================================\r\n  # Default/Required Functions\r\n  # ==========================================\r\n  # Test: default - returns default value if empty\r\n\r\n  # Test: required - fails if value is empty\r\n\r\n  # Test: empty - returns true if value is empty\r\n  emptyResult: \"false\"\r\n\r\n  # Test: fail - explicitly fails with message\r\n\r\n  # ==========================================\r\n  # Coalesce/Ternary Functions\r\n  # ==========================================\r\n  # Test: coalesce - returns first non-empty value\r\n\r\n  # Test: ternary - conditional expression\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-logic-default/templates/logic-default-test.yaml",
            "content": "\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n\n  # Test: or - returns boolean OR of arguments\n\n  # Test: not - returns boolean negation\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n\n  # Test: ne - returns true if not equal\n\n  # Test: lt - returns true if less than\n\n  # Test: le - returns true if less than or equal\n\n  # Test: gt - returns true if greater than\n\n  # Test: ge - returns true if greater than or equal\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n\n  # Test: required - fails if value is empty\n\n  # Test: empty - returns true if value is empty\n  emptyResult: \"false\"\n\n  # Test: fail - explicitly fails with message\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n\n  # Test: ternary - conditional expression\n",
            "originalContent": "# Test: Logic and default functions - comprehensive testing\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n  {{- if .Values.testAnd }}\n  andResult: {{ and $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: or - returns boolean OR of arguments\n  {{- if .Values.testOr }}\n  orResult: {{ or $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: not - returns boolean negation\n  {{- if .Values.testNot }}\n  notResult: {{ not $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n  {{- if .Values.testEq }}\n  eqResult: {{ eq $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ne - returns true if not equal\n  {{- if .Values.testNe }}\n  neResult: {{ ne $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: lt - returns true if less than\n  {{- if .Values.testLt }}\n  ltResult: {{ lt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: le - returns true if less than or equal\n  {{- if .Values.testLe }}\n  leResult: {{ le $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: gt - returns true if greater than\n  {{- if .Values.testGt }}\n  gtResult: {{ gt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ge - returns true if greater than or equal\n  {{- if .Values.testGe }}\n  geResult: {{ ge $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n  {{- if .Values.testDefault }}\n  defaultResult: {{ default \"fallback\" $testData | quote }}\n  {{- end }}\n\n  # Test: required - fails if value is empty\n  {{- if .Values.testRequired }}\n  requiredResult: {{ required \"value is required\" $testData | quote }}\n  {{- end }}\n\n  # Test: empty - returns true if value is empty\n  {{- if .Values.testEmpty }}\n  emptyResult: {{ empty $testData | quote }}\n  {{- end }}\n\n  # Test: fail - explicitly fails with message\n  {{- if .Values.testFail }}\n  failResult: {{ fail (printf \"intentional fail with: %v\" $testData) }}\n  {{- end }}\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n  {{- if .Values.testCoalesce }}\n  coalesceResult: {{ coalesce $testData .Values.compareValue \"default\" | quote }}\n  {{- end }}\n\n  # Test: ternary - conditional expression\n  {{- if .Values.testTernary }}\n  ternaryResult: {{ ternary \"yes\" \"no\" $testData | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-logic-test\r\ndata:\r\n  # ==========================================\r\n  # Boolean Logic Functions\r\n  # ==========================================\r\n  # Test: and - returns boolean AND of arguments\r\n\r\n  # Test: or - returns boolean OR of arguments\r\n\r\n  # Test: not - returns boolean negation\r\n\r\n  # ==========================================\r\n  # Comparison Functions\r\n  # ==========================================\r\n  # Test: eq - returns true if equal\r\n\r\n  # Test: ne - returns true if not equal\r\n\r\n  # Test: lt - returns true if less than\r\n\r\n  # Test: le - returns true if less than or equal\r\n\r\n  # Test: gt - returns true if greater than\r\n\r\n  # Test: ge - returns true if greater than or equal\r\n\r\n  # ==========================================\r\n  # Default/Required Functions\r\n  # ==========================================\r\n  # Test: default - returns default value if empty\r\n\r\n  # Test: required - fails if value is empty\r\n\r\n  # Test: empty - returns true if value is empty\r\n  emptyResult: \"false\"\r\n\r\n  # Test: fail - explicitly fails with message\r\n\r\n  # ==========================================\r\n  # Coalesce/Ternary Functions\r\n  # ==========================================\r\n  # Test: coalesce - returns first non-empty value\r\n\r\n  # Test: ternary - conditional expression\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-logic-default/templates/logic-default-test.yaml",
            "content": "\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n\n  # Test: or - returns boolean OR of arguments\n\n  # Test: not - returns boolean negation\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n\n  # Test: ne - returns true if not equal\n\n  # Test: lt - returns true if less than\n\n  # Test: le - returns true if less than or equal\n\n  # Test: gt - returns true if greater than\n\n  # Test: ge - returns true if greater than or equal\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n\n  # Test: required - fails if value is empty\n\n  # Test: empty - returns true if value is empty\n  emptyResult: \"false\"\n\n  # Test: fail - explicitly fails with message\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n\n  # Test: ternary - conditional expression\n",
            "originalContent": "# Test: Logic and default functions - comprehensive testing\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n  {{- if .Values.testAnd }}\n  andResult: {{ and $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: or - returns boolean OR of arguments\n  {{- if .Values.testOr }}\n  orResult: {{ or $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: not - returns boolean negation\n  {{- if .Values.testNot }}\n  notResult: {{ not $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n  {{- if .Values.testEq }}\n  eqResult: {{ eq $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ne - returns true if not equal\n  {{- if .Values.testNe }}\n  neResult: {{ ne $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: lt - returns true if less than\n  {{- if .Values.testLt }}\n  ltResult: {{ lt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: le - returns true if less than or equal\n  {{- if .Values.testLe }}\n  leResult: {{ le $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: gt - returns true if greater than\n  {{- if .Values.testGt }}\n  gtResult: {{ gt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ge - returns true if greater than or equal\n  {{- if .Values.testGe }}\n  geResult: {{ ge $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n  {{- if .Values.testDefault }}\n  defaultResult: {{ default \"fallback\" $testData | quote }}\n  {{- end }}\n\n  # Test: required - fails if value is empty\n  {{- if .Values.testRequired }}\n  requiredResult: {{ required \"value is required\" $testData | quote }}\n  {{- end }}\n\n  # Test: empty - returns true if value is empty\n  {{- if .Values.testEmpty }}\n  emptyResult: {{ empty $testData | quote }}\n  {{- end }}\n\n  # Test: fail - explicitly fails with message\n  {{- if .Values.testFail }}\n  failResult: {{ fail (printf \"intentional fail with: %v\" $testData) }}\n  {{- end }}\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n  {{- if .Values.testCoalesce }}\n  coalesceResult: {{ coalesce $testData .Values.compareValue \"default\" | quote }}\n  {{- end }}\n\n  # Test: ternary - conditional expression\n  {{- if .Values.testTernary }}\n  ternaryResult: {{ ternary \"yes\" \"no\" $testData | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "empty",
      "dataType": "dictValue",
      "valuesOptions": {
        "values": [
          "testEmpty=true",
          "testDataType=dictValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-logic-test\r\ndata:\r\n  # ==========================================\r\n  # Boolean Logic Functions\r\n  # ==========================================\r\n  # Test: and - returns boolean AND of arguments\r\n\r\n  # Test: or - returns boolean OR of arguments\r\n\r\n  # Test: not - returns boolean negation\r\n\r\n  # ==========================================\r\n  # Comparison Functions\r\n  # ==========================================\r\n  # Test: eq - returns true if equal\r\n\r\n  # Test: ne - returns true if not equal\r\n\r\n  # Test: lt - returns true if less than\r\n\r\n  # Test: le - returns true if less than or equal\r\n\r\n  # Test: gt - returns true if greater than\r\n\r\n  # Test: ge - returns true if greater than or equal\r\n\r\n  # ==========================================\r\n  # Default/Required Functions\r\n  # ==========================================\r\n  # Test: default - returns default value if empty\r\n\r\n  # Test: required - fails if value is empty\r\n\r\n  # Test: empty - returns true if value is empty\r\n  emptyResult: \"false\"\r\n\r\n  # Test: fail - explicitly fails with message\r\n\r\n  # ==========================================\r\n  # Coalesce/Ternary Functions\r\n  # ==========================================\r\n  # Test: coalesce - returns first non-empty value\r\n\r\n  # Test: ternary - conditional expression\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-logic-default/templates/logic-default-test.yaml",
            "content": "\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n\n  # Test: or - returns boolean OR of arguments\n\n  # Test: not - returns boolean negation\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n\n  # Test: ne - returns true if not equal\n\n  # Test: lt - returns true if less than\n\n  # Test: le - returns true if less than or equal\n\n  # Test: gt - returns true if greater than\n\n  # Test: ge - returns true if greater than or equal\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n\n  # Test: required - fails if value is empty\n\n  # Test: empty - returns true if value is empty\n  emptyResult: \"false\"\n\n  # Test: fail - explicitly fails with message\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n\n  # Test: ternary - conditional expression\n",
            "originalContent": "# Test: Logic and default functions - comprehensive testing\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n  {{- if .Values.testAnd }}\n  andResult: {{ and $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: or - returns boolean OR of arguments\n  {{- if .Values.testOr }}\n  orResult: {{ or $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: not - returns boolean negation\n  {{- if .Values.testNot }}\n  notResult: {{ not $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n  {{- if .Values.testEq }}\n  eqResult: {{ eq $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ne - returns true if not equal\n  {{- if .Values.testNe }}\n  neResult: {{ ne $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: lt - returns true if less than\n  {{- if .Values.testLt }}\n  ltResult: {{ lt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: le - returns true if less than or equal\n  {{- if .Values.testLe }}\n  leResult: {{ le $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: gt - returns true if greater than\n  {{- if .Values.testGt }}\n  gtResult: {{ gt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ge - returns true if greater than or equal\n  {{- if .Values.testGe }}\n  geResult: {{ ge $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n  {{- if .Values.testDefault }}\n  defaultResult: {{ default \"fallback\" $testData | quote }}\n  {{- end }}\n\n  # Test: required - fails if value is empty\n  {{- if .Values.testRequired }}\n  requiredResult: {{ required \"value is required\" $testData | quote }}\n  {{- end }}\n\n  # Test: empty - returns true if value is empty\n  {{- if .Values.testEmpty }}\n  emptyResult: {{ empty $testData | quote }}\n  {{- end }}\n\n  # Test: fail - explicitly fails with message\n  {{- if .Values.testFail }}\n  failResult: {{ fail (printf \"intentional fail with: %v\" $testData) }}\n  {{- end }}\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n  {{- if .Values.testCoalesce }}\n  coalesceResult: {{ coalesce $testData .Values.compareValue \"default\" | quote }}\n  {{- end }}\n\n  # Test: ternary - conditional expression\n  {{- if .Values.testTernary }}\n  ternaryResult: {{ ternary \"yes\" \"no\" $testData | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-logic-test\r\ndata:\r\n  # ==========================================\r\n  # Boolean Logic Functions\r\n  # ==========================================\r\n  # Test: and - returns boolean AND of arguments\r\n\r\n  # Test: or - returns boolean OR of arguments\r\n\r\n  # Test: not - returns boolean negation\r\n\r\n  # ==========================================\r\n  # Comparison Functions\r\n  # ==========================================\r\n  # Test: eq - returns true if equal\r\n\r\n  # Test: ne - returns true if not equal\r\n\r\n  # Test: lt - returns true if less than\r\n\r\n  # Test: le - returns true if less than or equal\r\n\r\n  # Test: gt - returns true if greater than\r\n\r\n  # Test: ge - returns true if greater than or equal\r\n\r\n  # ==========================================\r\n  # Default/Required Functions\r\n  # ==========================================\r\n  # Test: default - returns default value if empty\r\n\r\n  # Test: required - fails if value is empty\r\n\r\n  # Test: empty - returns true if value is empty\r\n  emptyResult: \"false\"\r\n\r\n  # Test: fail - explicitly fails with message\r\n\r\n  # ==========================================\r\n  # Coalesce/Ternary Functions\r\n  # ==========================================\r\n  # Test: coalesce - returns first non-empty value\r\n\r\n  # Test: ternary - conditional expression\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-logic-default/templates/logic-default-test.yaml",
            "content": "\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n\n  # Test: or - returns boolean OR of arguments\n\n  # Test: not - returns boolean negation\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n\n  # Test: ne - returns true if not equal\n\n  # Test: lt - returns true if less than\n\n  # Test: le - returns true if less than or equal\n\n  # Test: gt - returns true if greater than\n\n  # Test: ge - returns true if greater than or equal\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n\n  # Test: required - fails if value is empty\n\n  # Test: empty - returns true if value is empty\n  emptyResult: \"false\"\n\n  # Test: fail - explicitly fails with message\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n\n  # Test: ternary - conditional expression\n",
            "originalContent": "# Test: Logic and default functions - comprehensive testing\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n  {{- if .Values.testAnd }}\n  andResult: {{ and $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: or - returns boolean OR of arguments\n  {{- if .Values.testOr }}\n  orResult: {{ or $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: not - returns boolean negation\n  {{- if .Values.testNot }}\n  notResult: {{ not $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n  {{- if .Values.testEq }}\n  eqResult: {{ eq $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ne - returns true if not equal\n  {{- if .Values.testNe }}\n  neResult: {{ ne $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: lt - returns true if less than\n  {{- if .Values.testLt }}\n  ltResult: {{ lt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: le - returns true if less than or equal\n  {{- if .Values.testLe }}\n  leResult: {{ le $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: gt - returns true if greater than\n  {{- if .Values.testGt }}\n  gtResult: {{ gt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ge - returns true if greater than or equal\n  {{- if .Values.testGe }}\n  geResult: {{ ge $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n  {{- if .Values.testDefault }}\n  defaultResult: {{ default \"fallback\" $testData | quote }}\n  {{- end }}\n\n  # Test: required - fails if value is empty\n  {{- if .Values.testRequired }}\n  requiredResult: {{ required \"value is required\" $testData | quote }}\n  {{- end }}\n\n  # Test: empty - returns true if value is empty\n  {{- if .Values.testEmpty }}\n  emptyResult: {{ empty $testData | quote }}\n  {{- end }}\n\n  # Test: fail - explicitly fails with message\n  {{- if .Values.testFail }}\n  failResult: {{ fail (printf \"intentional fail with: %v\" $testData) }}\n  {{- end }}\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n  {{- if .Values.testCoalesce }}\n  coalesceResult: {{ coalesce $testData .Values.compareValue \"default\" | quote }}\n  {{- end }}\n\n  # Test: ternary - conditional expression\n  {{- if .Values.testTernary }}\n  ternaryResult: {{ ternary \"yes\" \"no\" $testData | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "fail",
      "dataType": "nilValue",
      "valuesOptions": {
        "values": [
          "testFail=true",
          "testDataType=nilValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": "execution error at (test-logic-default/templates/logic-default-test.yaml:80:17): intentional fail with: \u003cnil\u003e",
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      },
      "debug": {
        "error": "execution error at (test-logic-default/templates/logic-default-test.yaml:80:17): intentional fail with: \u003cnil\u003e",
        "debugLogs": [],
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      }
    },
    {
      "toggle": "fail",
      "dataType": "stringValue",
      "valuesOptions": {
        "values": [
          "testFail=true",
          "testDataType=stringValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": "execution error at (test-logic-default/templates/logic-default-test.yaml:80:17): intentional fail with: test string",
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      },
      "debug": {
        "error": "execution error at (test-logic-default/templates/logic-default-test.yaml:80:17): intentional fail with: test string",
        "debugLogs": [],
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      }
    },
    {
      "toggle": "fail",
      "dataType": "numberValue",
      "valuesOptions": {
        "values": [
          "testFail=true",
          "testDataType=numberValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": "execution error at (test-logic-default/templates/logic-default-test.yaml:80:17): intentional fail with: 42",
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      },
      "debug": {
        "error": "execution error at (test-logic-default/templates/logic-default-test.yaml:80:17): intentional fail with: 42",
        "debugLogs": [],
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      }
    },
    {
      "toggle": "fail",
      "dataType": "boolValue",
      "valuesOptions": {
        "values": [
          "testFail=true",
          "testDataType=boolValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": "execution error at (test-logic-default/templates/logic-default-test.yaml:80:17): intentional fail with: true",
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      },
      "debug": {
        "error": "execution error at (test-logic-default/templates/logic-default-test.yaml:80:17): intentional fail with: true",
        "debugLogs": [],
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      }
    },
    {
      "toggle": "fail",
      "dataType": "listValue",
      "valuesOptions": {
        "values": [
          "testFail=true",
          "testDataType=listValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": "execution error at (test-logic-default/templates/logic-default-test.yaml:80:17): intentional fail with: [1 2 3]",
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      },
      "debug": {
        "error": "execution error at (test-logic-default/templates/logic-default-test.yaml:80:17): intentional fail with: [1 2 3]",
        "debugLogs": [],
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      }
    },
    {
      "toggle": "fail",
      "dataType": "dictValue",
      "valuesOptions": {
        "values": [
          "testFail=true",
          "testDataType=dictValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": "execution error at (test-logic-default/templates/logic-default-test.yaml:80:17): intentional fail with: map[key:value]",
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      },
      "debug": {
        "error": "execution error at (test-logic-default/templates/logic-default-test.yaml:80:17): intentional fail with: map[key:value]",
        "debugLogs": [],
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      }
    },
    {
      "toggle": "coalesce",
      "dataType": "nilValue",
      "valuesOptions": {
        "values": [
          "testCoalesce=true",
          "testDataType=nilValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-logic-test\r\ndata:\r\n  # ==========================================\r\n  # Boolean Logic Functions\r\n  # ==========================================\r\n  # Test: and - returns boolean AND of arguments\r\n\r\n  # Test: or - returns boolean OR of arguments\r\n\r\n  # Test: not - returns boolean negation\r\n\r\n  # ==========================================\r\n  # Comparison Functions\r\n  # ==========================================\r\n  # Test: eq - returns true if equal\r\n\r\n  # Test: ne - returns true if not equal\r\n\r\n  # Test: lt - returns true if less than\r\n\r\n  # Test: le - returns true if less than or equal\r\n\r\n  # Test: gt - returns true if greater than\r\n\r\n  # Test: ge - returns true if greater than or equal\r\n\r\n  # ==========================================\r\n  # Default/Required Functions\r\n  # ==========================================\r\n  # Test: default - returns default value if empty\r\n\r\n  # Test: required - fails if value is empty\r\n\r\n  # Test: empty - returns true if value is empty\r\n\r\n  # Test: fail - explicitly fails with message\r\n\r\n  # ==========================================\r\n  # Coalesce/Ternary Functions\r\n  # ==========================================\r\n  # Test: coalesce - returns first non-empty value\r\n  coalesceResult: \"10\"\r\n\r\n  # Test: ternary - conditional expression\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-logic-default/templates/logic-default-test.yaml",
            "content": "\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n\n  # Test: or - returns boolean OR of arguments\n\n  # Test: not - returns boolean negation\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n\n  # Test: ne - returns true if not equal\n\n  # Test: lt - returns true if less than\n\n  # Test: le - returns true if less than or equal\n\n  # Test: gt - returns true if greater than\n\n  # Test: ge - returns true if greater than or equal\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n\n  # Test: required - fails if value is empty\n\n  # Test: empty - returns true if value is empty\n\n  # Test: fail - explicitly fails with message\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n  coalesceResult: \"10\"\n\n  # Test: ternary - conditional expression\n",
            "originalContent": "# Test: Logic and default functions - comprehensive testing\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n  {{- if .Values.testAnd }}\n  andResult: {{ and $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: or - returns boolean OR of arguments\n  {{- if .Values.testOr }}\n  orResult: {{ or $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: not - returns boolean negation\n  {{- if .Values.testNot }}\n  notResult: {{ not $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n  {{- if .Values.testEq }}\n  eqResult: {{ eq $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ne - returns true if not equal\n  {{- if .Values.testNe }}\n  neResult: {{ ne $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: lt - returns true if less than\n  {{- if .Values.testLt }}\n  ltResult: {{ lt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: le - returns true if less than or equal\n  {{- if .Values.testLe }}\n  leResult: {{ le $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: gt - returns true if greater than\n  {{- if .Values.testGt }}\n  gtResult: {{ gt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ge - returns true if greater than or equal\n  {{- if .Values.testGe }}\n  geResult: {{ ge $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n  {{- if .Values.testDefault }}\n  defaultResult: {{ default \"fallback\" $testData | quote }}\n  {{- end }}\n\n  # Test: required - fails if value is empty\n  {{- if .Values.testRequired }}\n  requiredResult: {{ required \"value is required\" $testData | quote }}\n  {{- end }}\n\n  # Test: empty - returns true if value is empty\n  {{- if .Values.testEmpty }}\n  emptyResult: {{ empty $testData | quote }}\n  {{- end }}\n\n  # Test: fail - explicitly fails with message\n  {{- if .Values.testFail }}\n  failResult: {{ fail (printf \"intentional fail with: %v\" $testData) }}\n  {{- end }}\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n  {{- if .Values.testCoalesce }}\n  coalesceResult: {{ coalesce $testData .Values.compareValue \"default\" | quote }}\n  {{- end }}\n\n  # Test: ternary - conditional expression\n  {{- if .Values.testTernary }}\n  ternaryResult: {{ ternary \"yes\" \"no\" $testData | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-logic-test\r\ndata:\r\n  # ==========================================\r\n  # Boolean Logic Functions\r\n  # ==========================================\r\n  # Test: and - returns boolean AND of arguments\r\n\r\n  # Test: or - returns boolean OR of arguments\r\n\r\n  # Test: not - returns boolean negation\r\n\r\n  # ==========================================\r\n  # Comparison Functions\r\n  # ==========================================\r\n  # Test: eq - returns true if equal\r\n\r\n  # Test: ne - returns true if not equal\r\n\r\n  # Test: lt - returns true if less than\r\n\r\n  # Test: le - returns true if less than or equal\r\n\r\n  # Test: gt - returns true if greater than\r\n\r\n  # Test: ge - returns true if greater than or equal\r\n\r\n  # ==========================================\r\n  # Default/Required Functions\r\n  # ==========================================\r\n  # Test: default - returns default value if empty\r\n\r\n  # Test: required - fails if value is empty\r\n\r\n  # Test: empty - returns true if value is empty\r\n\r\n  # Test: fail - explicitly fails with message\r\n\r\n  # ==========================================\r\n  # Coalesce/Ternary Functions\r\n  # ==========================================\r\n  # Test: coalesce - returns first non-empty value\r\n  coalesceResult: \"10\"\r\n\r\n  # Test: ternary - conditional expression\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-logic-default/templates/logic-default-test.yaml",
            "content": "\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n\n  # Test: or - returns boolean OR of arguments\n\n  # Test: not - returns boolean negation\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n\n  # Test: ne - returns true if not equal\n\n  # Test: lt - returns true if less than\n\n  # Test: le - returns true if less than or equal\n\n  # Test: gt - returns true if greater than\n\n  # Test: ge - returns true if greater than or equal\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n\n  # Test: required - fails if value is empty\n\n  # Test: empty - returns true if value is empty\n\n  # Test: fail - explicitly fails with message\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n  coalesceResult: \"10\"\n\n  # Test: ternary - conditional expression\n",
            "originalContent": "# Test: Logic and default functions - comprehensive testing\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n  {{- if .Values.testAnd }}\n  andResult: {{ and $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: or - returns boolean OR of arguments\n  {{- if .Values.testOr }}\n  orResult: {{ or $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: not - returns boolean negation\n  {{- if .Values.testNot }}\n  notResult: {{ not $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n  {{- if .Values.testEq }}\n  eqResult: {{ eq $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ne - returns true if not equal\n  {{- if .Values.testNe }}\n  neResult: {{ ne $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: lt - returns true if less than\n  {{- if .Values.testLt }}\n  ltResult: {{ lt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: le - returns true if less than or equal\n  {{- if .Values.testLe }}\n  leResult: {{ le $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: gt - returns true if greater than\n  {{- if .Values.testGt }}\n  gtResult: {{ gt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ge - returns true if greater than or equal\n  {{- if .Values.testGe }}\n  geResult: {{ ge $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n  {{- if .Values.testDefault }}\n  defaultResult: {{ default \"fallback\" $testData | quote }}\n  {{- end }}\n\n  # Test: required - fails if value is empty\n  {{- if .Values.testRequired }}\n  requiredResult: {{ required \"value is required\" $testData | quote }}\n  {{- end }}\n\n  # Test: empty - returns true if value is empty\n  {{- if .Values.testEmpty }}\n  emptyResult: {{ empty $testData | quote }}\n  {{- end }}\n\n  # Test: fail - explicitly fails with message\n  {{- if .Values.testFail }}\n  failResult: {{ fail (printf \"intentional fail with: %v\" $testData) }}\n  {{- end }}\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n  {{- if .Values.testCoalesce }}\n  coalesceResult: {{ coalesce $testData .Values.compareValue \"default\" | quote }}\n  {{- end }}\n\n  # Test: ternary - conditional expression\n  {{- if .Values.testTernary }}\n  ternaryResult: {{ ternary \"yes\" \"no\" $testData | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "coalesce",
      "dataType": "stringValue",
      "valuesOptions": {
        "values": [
          "testCoalesce=true",
          "testDataType=stringValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-logic-test\r\ndata:\r\n  # ==========================================\r\n  # Boolean Logic Functions\r\n  # ==========================================\r\n  # Test: and - returns boolean AND of arguments\r\n\r\n  # Test: or - returns boolean OR of arguments\r\n\r\n  # Test: not - returns boolean negation\r\n\r\n  # ==========================================\r\n  # Comparison Functions\r\n  # ==========================================\r\n  # Test: eq - returns true if equal\r\n\r\n  # Test: ne - returns true if not equal\r\n\r\n  # Test: lt - returns true if less than\r\n\r\n  # Test: le - returns true if less than or equal\r\n\r\n  # Test: gt - returns true if greater than\r\n\r\n  # Test: ge - returns true if greater than or equal\r\n\r\n  # ==========================================\r\n  # Default/Required Functions\r\n  # ==========================================\r\n  # Test: default - returns default value if empty\r\n\r\n  # Test: required - fails if value is empty\r\n\r\n  # Test: empty - returns true if value is empty\r\n\r\n  # Test: fail - explicitly fails with message\r\n\r\n  # ==========================================\r\n  # Coalesce/Ternary Functions\r\n  # ==========================================\r\n  # Test: coalesce - returns first non-empty value\r\n  coalesceResult: \"test string\"\r\n\r\n  # Test: ternary - conditional expression\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-logic-default/templates/logic-default-test.yaml",
            "content": "\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n\n  # Test: or - returns boolean OR of arguments\n\n  # Test: not - returns boolean negation\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n\n  # Test: ne - returns true if not equal\n\n  # Test: lt - returns true if less than\n\n  # Test: le - returns true if less than or equal\n\n  # Test: gt - returns true if greater than\n\n  # Test: ge - returns true if greater than or equal\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n\n  # Test: required - fails if value is empty\n\n  # Test: empty - returns true if value is empty\n\n  # Test: fail - explicitly fails with message\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n  coalesceResult: \"test string\"\n\n  # Test: ternary - conditional expression\n",
            "originalContent": "# Test: Logic and default functions - comprehensive testing\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n  {{- if .Values.testAnd }}\n  andResult: {{ and $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: or - returns boolean OR of arguments\n  {{- if .Values.testOr }}\n  orResult: {{ or $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: not - returns boolean negation\n  {{- if .Values.testNot }}\n  notResult: {{ not $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n  {{- if .Values.testEq }}\n  eqResult: {{ eq $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ne - returns true if not equal\n  {{- if .Values.testNe }}\n  neResult: {{ ne $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: lt - returns true if less than\n  {{- if .Values.testLt }}\n  ltResult: {{ lt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: le - returns true if less than or equal\n  {{- if .Values.testLe }}\n  leResult: {{ le $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: gt - returns true if greater than\n  {{- if .Values.testGt }}\n  gtResult: {{ gt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ge - returns true if greater than or equal\n  {{- if .Values.testGe }}\n  geResult: {{ ge $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n  {{- if .Values.testDefault }}\n  defaultResult: {{ default \"fallback\" $testData | quote }}\n  {{- end }}\n\n  # Test: required - fails if value is empty\n  {{- if .Values.testRequired }}\n  requiredResult: {{ required \"value is required\" $testData | quote }}\n  {{- end }}\n\n  # Test: empty - returns true if value is empty\n  {{- if .Values.testEmpty }}\n  emptyResult: {{ empty $testData | quote }}\n  {{- end }}\n\n  # Test: fail - explicitly fails with message\n  {{- if .Values.testFail }}\n  failResult: {{ fail (printf \"intentional fail with: %v\" $testData) }}\n  {{- end }}\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n  {{- if .Values.testCoalesce }}\n  coalesceResult: {{ coalesce $testData .Values.compareValue \"default\" | quote }}\n  {{- end }}\n\n  # Test: ternary - conditional expression\n  {{- if .Values.testTernary }}\n  ternaryResult: {{ ternary \"yes\" \"no\" $testData | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-logic-test\r\ndata:\r\n  # ==========================================\r\n  # Boolean Logic Functions\r\n  # ==========================================\r\n  # Test: and - returns boolean AND of arguments\r\n\r\n  # Test: or - returns boolean OR of arguments\r\n\r\n  # Test: not - returns boolean negation\r\n\r\n  # ==========================================\r\n  # Comparison Functions\r\n  # ==========================================\r\n  # Test: eq - returns true if equal\r\n\r\n  # Test: ne - returns true if not equal\r\n\r\n  # Test: lt - returns true if less than\r\n\r\n  # Test: le - returns true if less than or equal\r\n\r\n  # Test: gt - returns true if greater than\r\n\r\n  # Test: ge - returns true if greater than or equal\r\n\r\n  # ==========================================\r\n  # Default/Required Functions\r\n  # ==========================================\r\n  # Test: default - returns default value if empty\r\n\r\n  # Test: required - fails if value is empty\r\n\r\n  # Test: empty - returns true if value is empty\r\n\r\n  # Test: fail - explicitly fails with message\r\n\r\n  # ==========================================\r\n  # Coalesce/Ternary Functions\r\n  # ==========================================\r\n  # Test: coalesce - returns first non-empty value\r\n  coalesceResult: \"test string\"\r\n\r\n  # Test: ternary - conditional expression\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-logic-default/templates/logic-default-test.yaml",
            "content": "\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n\n  # Test: or - returns boolean OR of arguments\n\n  # Test: not - returns boolean negation\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n\n  # Test: ne - returns true if not equal\n\n  # Test: lt - returns true if less than\n\n  # Test: le - returns true if less than or equal\n\n  # Test: gt - returns true if greater than\n\n  # Test: ge - returns true if greater than or equal\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n\n  # Test: required - fails if value is empty\n\n  # Test: empty - returns true if value is empty\n\n  # Test: fail - explicitly fails with message\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n  coalesceResult: \"test string\"\n\n  # Test: ternary - conditional expression\n",
            "originalContent": "# Test: Logic and default functions - comprehensive testing\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n  {{- if .Values.testAnd }}\n  andResult: {{ and $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: or - returns boolean OR of arguments\n  {{- if .Values.testOr }}\n  orResult: {{ or $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: not - returns boolean negation\n  {{- if .Values.testNot }}\n  notResult: {{ not $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n  {{- if .Values.testEq }}\n  eqResult: {{ eq $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ne - returns true if not equal\n  {{- if .Values.testNe }}\n  neResult: {{ ne $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: lt - returns true if less than\n  {{- if .Values.testLt }}\n  ltResult: {{ lt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: le - returns true if less than or equal\n  {{- if .Values.testLe }}\n  leResult: {{ le $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: gt - returns true if greater than\n  {{- if .Values.testGt }}\n  gtResult: {{ gt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ge - returns true if greater than or equal\n  {{- if .Values.testGe }}\n  geResult: {{ ge $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n  {{- if .Values.testDefault }}\n  defaultResult: {{ default \"fallback\" $testData | quote }}\n  {{- end }}\n\n  # Test: required - fails if value is empty\n  {{- if .Values.testRequired }}\n  requiredResult: {{ required \"value is required\" $testData | quote }}\n  {{- end }}\n\n  # Test: empty - returns true if value is empty\n  {{- if .Values.testEmpty }}\n  emptyResult: {{ empty $testData | quote }}\n  {{- end }}\n\n  # Test: fail - explicitly fails with message\n  {{- if .Values.testFail }}\n  failResult: {{ fail (printf \"intentional fail with: %v\" $testData) }}\n  {{- end }}\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n  {{- if .Values.testCoalesce }}\n  coalesceResult: {{ coalesce $testData .Values.compareValue \"default\" | quote }}\n  {{- end }}\n\n  # Test: ternary - conditional expression\n  {{- if .Values.testTernary }}\n  ternaryResult: {{ ternary \"yes\" \"no\" $testData | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "coalesce",
      "dataType": "numberValue",
      "valuesOptions": {
        "values": [
          "testCoalesce=true",
          "testDataType=numberValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-logic-test\r\ndata:\r\n  # ==========================================\r\n  # Boolean Logic Functions\r\n  # ==========================================\r\n  # Test: and - returns boolean AND of arguments\r\n\r\n  # Test: or - returns boolean OR of arguments\r\n\r\n  # Test: not - returns boolean negation\r\n\r\n  # ==========================================\r\n  # Comparison Functions\r\n  # ==========================================\r\n  # Test: eq - returns true if equal\r\n\r\n  # Test: ne - returns true if not equal\r\n\r\n  # Test: lt - returns true if less than\r\n\r\n  # Test: le - returns true if less than or equal\r\n\r\n  # Test: gt - returns true if greater than\r\n\r\n  # Test: ge - returns true if greater than or equal\r\n\r\n  # ==========================================\r\n  # Default/Required Functions\r\n  # ==========================================\r\n  # Test: default - returns default value if empty\r\n\r\n  # Test: required - fails if value is empty\r\n\r\n  # Test: empty - returns true if value is empty\r\n\r\n  # Test: fail - explicitly fails with message\r\n\r\n  # ==========================================\r\n  # Coalesce/Ternary Functions\r\n  # ==========================================\r\n  # Test: coalesce - returns first non-empty value\r\n  coalesceResult: \"42\"\r\n\r\n  # Test: ternary - conditional expression\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-logic-default/templates/logic-default-test.yaml",
            "content": "\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n\n  # Test: or - returns boolean OR of arguments\n\n  # Test: not - returns boolean negation\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n\n  # Test: ne - returns true if not equal\n\n  # Test: lt - returns true if less than\n\n  # Test: le - returns true if less than or equal\n\n  # Test: gt - returns true if greater than\n\n  # Test: ge - returns true if greater than or equal\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n\n  # Test: required - fails if value is empty\n\n  # Test: empty - returns true if value is empty\n\n  # Test: fail - explicitly fails with message\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n  coalesceResult: \"42\"\n\n  # Test: ternary - conditional expression\n",
            "originalContent": "# Test: Logic and default functions - comprehensive testing\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n  {{- if .Values.testAnd }}\n  andResult: {{ and $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: or - returns boolean OR of arguments\n  {{- if .Values.testOr }}\n  orResult: {{ or $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: not - returns boolean negation\n  {{- if .Values.testNot }}\n  notResult: {{ not $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n  {{- if .Values.testEq }}\n  eqResult: {{ eq $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ne - returns true if not equal\n  {{- if .Values.testNe }}\n  neResult: {{ ne $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: lt - returns true if less than\n  {{- if .Values.testLt }}\n  ltResult: {{ lt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: le - returns true if less than or equal\n  {{- if .Values.testLe }}\n  leResult: {{ le $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: gt - returns true if greater than\n  {{- if .Values.testGt }}\n  gtResult: {{ gt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ge - returns true if greater than or equal\n  {{- if .Values.testGe }}\n  geResult: {{ ge $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n  {{- if .Values.testDefault }}\n  defaultResult: {{ default \"fallback\" $testData | quote }}\n  {{- end }}\n\n  # Test: required - fails if value is empty\n  {{- if .Values.testRequired }}\n  requiredResult: {{ required \"value is required\" $testData | quote }}\n  {{- end }}\n\n  # Test: empty - returns true if value is empty\n  {{- if .Values.testEmpty }}\n  emptyResult: {{ empty $testData | quote }}\n  {{- end }}\n\n  # Test: fail - explicitly fails with message\n  {{- if .Values.testFail }}\n  failResult: {{ fail (printf \"intentional fail with: %v\" $testData) }}\n  {{- end }}\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n  {{- if .Values.testCoalesce }}\n  coalesceResult: {{ coalesce $testData .Values.compareValue \"default\" | quote }}\n  {{- end }}\n\n  # Test: ternary - conditional expression\n  {{- if .Values.testTernary }}\n  ternaryResult: {{ ternary \"yes\" \"no\" $testData | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-logic-test\r\ndata:\r\n  # ==========================================\r\n  # Boolean Logic Functions\r\n  # ==========================================\r\n  # Test: and - returns boolean AND of arguments\r\n\r\n  # Test: or - returns boolean OR of arguments\r\n\r\n  # Test: not - returns boolean negation\r\n\r\n  # ==========================================\r\n  # Comparison Functions\r\n  # ==========================================\r\n  # Test: eq - returns true if equal\r\n\r\n  # Test: ne - returns true if not equal\r\n\r\n  # Test: lt - returns true if less than\r\n\r\n  # Test: le - returns true if less than or equal\r\n\r\n  # Test: gt - returns true if greater than\r\n\r\n  # Test: ge - returns true if greater than or equal\r\n\r\n  # ==========================================\r\n  # Default/Required Functions\r\n  # ==========================================\r\n  # Test: default - returns default value if empty\r\n\r\n  # Test: required - fails if value is empty\r\n\r\n  # Test: empty - returns true if value is empty\r\n\r\n  # Test: fail - explicitly fails with message\r\n\r\n  # ==========================================\r\n  # Coalesce/Ternary Functions\r\n  # ==========================================\r\n  # Test: coalesce - returns first non-empty value\r\n  coalesceResult: \"42\"\r\n\r\n  # Test: ternary - conditional expression\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-logic-default/templates/logic-default-test.yaml",
            "content": "\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n\n  # Test: or - returns boolean OR of arguments\n\n  # Test: not - returns boolean negation\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n\n  # Test: ne - returns true if not equal\n\n  # Test: lt - returns true if less than\n\n  # Test: le - returns true if less than or equal\n\n  # Test: gt - returns true if greater than\n\n  # Test: ge - returns true if greater than or equal\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n\n  # Test: required - fails if value is empty\n\n  # Test: empty - returns true if value is empty\n\n  # Test: fail - explicitly fails with message\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n  coalesceResult: \"42\"\n\n  # Test: ternary - conditional expression\n",
            "originalContent": "# Test: Logic and default functions - comprehensive testing\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n  {{- if .Values.testAnd }}\n  andResult: {{ and $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: or - returns boolean OR of arguments\n  {{- if .Values.testOr }}\n  orResult: {{ or $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: not - returns boolean negation\n  {{- if .Values.testNot }}\n  notResult: {{ not $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n  {{- if .Values.testEq }}\n  eqResult: {{ eq $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ne - returns true if not equal\n  {{- if .Values.testNe }}\n  neResult: {{ ne $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: lt - returns true if less than\n  {{- if .Values.testLt }}\n  ltResult: {{ lt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: le - returns true if less than or equal\n  {{- if .Values.testLe }}\n  leResult: {{ le $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: gt - returns true if greater than\n  {{- if .Values.testGt }}\n  gtResult: {{ gt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ge - returns true if greater than or equal\n  {{- if .Values.testGe }}\n  geResult: {{ ge $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n  {{- if .Values.testDefault }}\n  defaultResult: {{ default \"fallback\" $testData | quote }}\n  {{- end }}\n\n  # Test: required - fails if value is empty\n  {{- if .Values.testRequired }}\n  requiredResult: {{ required \"value is required\" $testData | quote }}\n  {{- end }}\n\n  # Test: empty - returns true if value is empty\n  {{- if .Values.testEmpty }}\n  emptyResult: {{ empty $testData | quote }}\n  {{- end }}\n\n  # Test: fail - explicitly fails with message\n  {{- if .Values.testFail }}\n  failResult: {{ fail (printf \"intentional fail with: %v\" $testData) }}\n  {{- end }}\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n  {{- if .Values.testCoalesce }}\n  coalesceResult: {{ coalesce $testData .Values.compareValue \"default\" | quote }}\n  {{- end }}\n\n  # Test: ternary - conditional expression\n  {{- if .Values.testTernary }}\n  ternaryResult: {{ ternary \"yes\" \"no\" $testData | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "coalesce",
      "dataType": "boolValue",
      "valuesOptions": {
        "values": [
          "testCoalesce=true",
          "testDataType=boolValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-logic-test\r\ndata:\r\n  # ==========================================\r\n  # Boolean Logic Functions\r\n  # ==========================================\r\n  # Test: and - returns boolean AND of arguments\r\n\r\n  # Test: or - returns boolean OR of arguments\r\n\r\n  # Test: not - returns boolean negation\r\n\r\n  # ==========================================\r\n  # Comparison Functions\r\n  # ==========================================\r\n  # Test: eq - returns true if equal\r\n\r\n  # Test: ne - returns true if not equal\r\n\r\n  # Test: lt - returns true if less than\r\n\r\n  # Test: le - returns true if less than or equal\r\n\r\n  # Test: gt - returns true if greater than\r\n\r\n  # Test: ge - returns true if greater than or equal\r\n\r\n  # ==========================================\r\n  # Default/Required Functions\r\n  # ==========================================\r\n  # Test: default - returns default value if empty\r\n\r\n  # Test: required - fails if value is empty\r\n\r\n  # Test: empty - returns true if value is empty\r\n\r\n  # Test: fail - explicitly fails with message\r\n\r\n  # ==========================================\r\n  # Coalesce/Ternary Functions\r\n  # ==========================================\r\n  # Test: coalesce - returns first non-empty value\r\n  coalesceResult: \"true\"\r\n\r\n  # Test: ternary - conditional expression\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-logic-default/templates/logic-default-test.yaml",
            "content": "\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n\n  # Test: or - returns boolean OR of arguments\n\n  # Test: not - returns boolean negation\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n\n  # Test: ne - returns true if not equal\n\n  # Test: lt - returns true if less than\n\n  # Test: le - returns true if less than or equal\n\n  # Test: gt - returns true if greater than\n\n  # Test: ge - returns true if greater than or equal\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n\n  # Test: required - fails if value is empty\n\n  # Test: empty - returns true if value is empty\n\n  # Test: fail - explicitly fails with message\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n  coalesceResult: \"true\"\n\n  # Test: ternary - conditional expression\n",
            "originalContent": "# Test: Logic and default functions - comprehensive testing\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n  {{- if .Values.testAnd }}\n  andResult: {{ and $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: or - returns boolean OR of arguments\n  {{- if .Values.testOr }}\n  orResult: {{ or $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: not - returns boolean negation\n  {{- if .Values.testNot }}\n  notResult: {{ not $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n  {{- if .Values.testEq }}\n  eqResult: {{ eq $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ne - returns true if not equal\n  {{- if .Values.testNe }}\n  neResult: {{ ne $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: lt - returns true if less than\n  {{- if .Values.testLt }}\n  ltResult: {{ lt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: le - returns true if less than or equal\n  {{- if .Values.testLe }}\n  leResult: {{ le $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: gt - returns true if greater than\n  {{- if .Values.testGt }}\n  gtResult: {{ gt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ge - returns true if greater than or equal\n  {{- if .Values.testGe }}\n  geResult: {{ ge $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n  {{- if .Values.testDefault }}\n  defaultResult: {{ default \"fallback\" $testData | quote }}\n  {{- end }}\n\n  # Test: required - fails if value is empty\n  {{- if .Values.testRequired }}\n  requiredResult: {{ required \"value is required\" $testData | quote }}\n  {{- end }}\n\n  # Test: empty - returns true if value is empty\n  {{- if .Values.testEmpty }}\n  emptyResult: {{ empty $testData | quote }}\n  {{- end }}\n\n  # Test: fail - explicitly fails with message\n  {{- if .Values.testFail }}\n  failResult: {{ fail (printf \"intentional fail with: %v\" $testData) }}\n  {{- end }}\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n  {{- if .Values.testCoalesce }}\n  coalesceResult: {{ coalesce $testData .Values.compareValue \"default\" | quote }}\n  {{- end }}\n\n  # Test: ternary - conditional expression\n  {{- if .Values.testTernary }}\n  ternaryResult: {{ ternary \"yes\" \"no\" $testData | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-logic-test\r\ndata:\r\n  # ==========================================\r\n  # Boolean Logic Functions\r\n  # ==========================================\r\n  # Test: and - returns boolean AND of arguments\r\n\r\n  # Test: or - returns boolean OR of arguments\r\n\r\n  # Test: not - returns boolean negation\r\n\r\n  # ==========================================\r\n  # Comparison Functions\r\n  # ==========================================\r\n  # Test: eq - returns true if equal\r\n\r\n  # Test: ne - returns true if not equal\r\n\r\n  # Test: lt - returns true if less than\r\n\r\n  # Test: le - returns true if less than or equal\r\n\r\n  # Test: gt - returns true if greater than\r\n\r\n  # Test: ge - returns true if greater than or equal\r\n\r\n  # ==========================================\r\n  # Default/Required Functions\r\n  # ==========================================\r\n  # Test: default - returns default value if empty\r\n\r\n  # Test: required - fails if value is empty\r\n\r\n  # Test: empty - returns true if value is empty\r\n\r\n  # Test: fail - explicitly fails with message\r\n\r\n  # ==========================================\r\n  # Coalesce/Ternary Functions\r\n  # ==========================================\r\n  # Test: coalesce - returns first non-empty value\r\n  coalesceResult: \"true\"\r\n\r\n  # Test: ternary - conditional expression\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-logic-default/templates/logic-default-test.yaml",
            "content": "\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n\n  # Test: or - returns boolean OR of arguments\n\n  # Test: not - returns boolean negation\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n\n  # Test: ne - returns true if not equal\n\n  # Test: lt - returns true if less than\n\n  # Test: le - returns true if less than or equal\n\n  # Test: gt - returns true if greater than\n\n  # Test: ge - returns true if greater than or equal\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n\n  # Test: required - fails if value is empty\n\n  # Test: empty - returns true if value is empty\n\n  # Test: fail - explicitly fails with message\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n  coalesceResult: \"true\"\n\n  # Test: ternary - conditional expression\n",
            "originalContent": "# Test: Logic and default functions - comprehensive testing\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n  {{- if .Values.testAnd }}\n  andResult: {{ and $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: or - returns boolean OR of arguments\n  {{- if .Values.testOr }}\n  orResult: {{ or $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: not - returns boolean negation\n  {{- if .Values.testNot }}\n  notResult: {{ not $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n  {{- if .Values.testEq }}\n  eqResult: {{ eq $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ne - returns true if not equal\n  {{- if .Values.testNe }}\n  neResult: {{ ne $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: lt - returns true if less than\n  {{- if .Values.testLt }}\n  ltResult: {{ lt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: le - returns true if less than or equal\n  {{- if .Values.testLe }}\n  leResult: {{ le $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: gt - returns true if greater than\n  {{- if .Values.testGt }}\n  gtResult: {{ gt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ge - returns true if greater than or equal\n  {{- if .Values.testGe }}\n  geResult: {{ ge $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n  {{- if .Values.testDefault }}\n  defaultResult: {{ default \"fallback\" $testData | quote }}\n  {{- end }}\n\n  # Test: required - fails if value is empty\n  {{- if .Values.testRequired }}\n  requiredResult: {{ required \"value is required\" $testData | quote }}\n  {{- end }}\n\n  # Test: empty - returns true if value is empty\n  {{- if .Values.testEmpty }}\n  emptyResult: {{ empty $testData | quote }}\n  {{- end }}\n\n  # Test: fail - explicitly fails with message\n  {{- if .Values.testFail }}\n  failResult: {{ fail (printf \"intentional fail with: %v\" $testData) }}\n  {{- end }}\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n  {{- if .Values.testCoalesce }}\n  coalesceResult: {{ coalesce $testData .Values.compareValue \"default\" | quote }}\n  {{- end }}\n\n  # Test: ternary - conditional expression\n  {{- if .Values.testTernary }}\n  ternaryResult: {{ ternary \"yes\" \"no\" $testData | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "coalesce",
      "dataType": "listValue",
      "valuesOptions": {
        "values": [
          "testCoalesce=true",
          "testDataType=listValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-logic-test\r\ndata:\r\n  # ==========================================\r\n  # Boolean Logic Functions\r\n  # ==========================================\r\n  # Test: and - returns boolean AND of arguments\r\n\r\n  # Test: or - returns boolean OR of arguments\r\n\r\n  # Test: not - returns boolean negation\r\n\r\n  # ==========================================\r\n  # Comparison Functions\r\n  # ==========================================\r\n  # Test: eq - returns true if equal\r\n\r\n  # Test: ne - returns true if not equal\r\n\r\n  # Test: lt - returns true if less than\r\n\r\n  # Test: le - returns true if less than or equal\r\n\r\n  # Test: gt - returns true if greater than\r\n\r\n  # Test: ge - returns true if greater than or equal\r\n\r\n  # ==========================================\r\n  # Default/Required Functions\r\n  # ==========================================\r\n  # Test: default - returns default value if empty\r\n\r\n  # Test: required - fails if value is empty\r\n\r\n  # Test: empty - returns true if value is empty\r\n\r\n  # Test: fail - explicitly fails with message\r\n\r\n  # ==========================================\r\n  # Coalesce/Ternary Functions\r\n  # ==========================================\r\n  # Test: coalesce - returns first non-empty value\r\n  coalesceResult: \"[1 2 3]\"\r\n\r\n  # Test: ternary - conditional expression\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-logic-default/templates/logic-default-test.yaml",
            "content": "\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n\n  # Test: or - returns boolean OR of arguments\n\n  # Test: not - returns boolean negation\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n\n  # Test: ne - returns true if not equal\n\n  # Test: lt - returns true if less than\n\n  # Test: le - returns true if less than or equal\n\n  # Test: gt - returns true if greater than\n\n  # Test: ge - returns true if greater than or equal\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n\n  # Test: required - fails if value is empty\n\n  # Test: empty - returns true if value is empty\n\n  # Test: fail - explicitly fails with message\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n  coalesceResult: \"[1 2 3]\"\n\n  # Test: ternary - conditional expression\n",
            "originalContent": "# Test: Logic and default functions - comprehensive testing\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n  {{- if .Values.testAnd }}\n  andResult: {{ and $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: or - returns boolean OR of arguments\n  {{- if .Values.testOr }}\n  orResult: {{ or $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: not - returns boolean negation\n  {{- if .Values.testNot }}\n  notResult: {{ not $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n  {{- if .Values.testEq }}\n  eqResult: {{ eq $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ne - returns true if not equal\n  {{- if .Values.testNe }}\n  neResult: {{ ne $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: lt - returns true if less than\n  {{- if .Values.testLt }}\n  ltResult: {{ lt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: le - returns true if less than or equal\n  {{- if .Values.testLe }}\n  leResult: {{ le $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: gt - returns true if greater than\n  {{- if .Values.testGt }}\n  gtResult: {{ gt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ge - returns true if greater than or equal\n  {{- if .Values.testGe }}\n  geResult: {{ ge $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n  {{- if .Values.testDefault }}\n  defaultResult: {{ default \"fallback\" $testData | quote }}\n  {{- end }}\n\n  # Test: required - fails if value is empty\n  {{- if .Values.testRequired }}\n  requiredResult: {{ required \"value is required\" $testData | quote }}\n  {{- end }}\n\n  # Test: empty - returns true if value is empty\n  {{- if .Values.testEmpty }}\n  emptyResult: {{ empty $testData | quote }}\n  {{- end }}\n\n  # Test: fail - explicitly fails with message\n  {{- if .Values.testFail }}\n  failResult: {{ fail (printf \"intentional fail with: %v\" $testData) }}\n  {{- end }}\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n  {{- if .Values.testCoalesce }}\n  coalesceResult: {{ coalesce $testData .Values.compareValue \"default\" | quote }}\n  {{- end }}\n\n  # Test: ternary - conditional expression\n  {{- if .Values.testTernary }}\n  ternaryResult: {{ ternary \"yes\" \"no\" $testData | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-logic-test\r\ndata:\r\n  # ==========================================\r\n  # Boolean Logic Functions\r\n  # ==========================================\r\n  # Test: and - returns boolean AND of arguments\r\n\r\n  # Test: or - returns boolean OR of arguments\r\n\r\n  # Test: not - returns boolean negation\r\n\r\n  # ==========================================\r\n  # Comparison Functions\r\n  # ==========================================\r\n  # Test: eq - returns true if equal\r\n\r\n  # Test: ne - returns true if not equal\r\n\r\n  # Test: lt - returns true if less than\r\n\r\n  # Test: le - returns true if less than or equal\r\n\r\n  # Test: gt - returns true if greater than\r\n\r\n  # Test: ge - returns true if greater than or equal\r\n\r\n  # ==========================================\r\n  # Default/Required Functions\r\n  # ==========================================\r\n  # Test: default - returns default value if empty\r\n\r\n  # Test: required - fails if value is empty\r\n\r\n  # Test: empty - returns true if value is empty\r\n\r\n  # Test: fail - explicitly fails with message\r\n\r\n  # ==========================================\r\n  # Coalesce/Ternary Functions\r\n  # ==========================================\r\n  # Test: coalesce - returns first non-empty value\r\n  coalesceResult: \"[1 2 3]\"\r\n\r\n  # Test: ternary - conditional expression\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-logic-default/templates/logic-default-test.yaml",
            "content": "\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n\n  # Test: or - returns boolean OR of arguments\n\n  # Test: not - returns boolean negation\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n\n  # Test: ne - returns true if not equal\n\n  # Test: lt - returns true if less than\n\n  # Test: le - returns true if less than or equal\n\n  # Test: gt - returns true if greater than\n\n  # Test: ge - returns true if greater than or equal\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n\n  # Test: required - fails if value is empty\n\n  # Test: empty - returns true if value is empty\n\n  # Test: fail - explicitly fails with message\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n  coalesceResult: \"[1 2 3]\"\n\n  # Test: ternary - conditional expression\n",
            "originalContent": "# Test: Logic and default functions - comprehensive testing\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n  {{- if .Values.testAnd }}\n  andResult: {{ and $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: or - returns boolean OR of arguments\n  {{- if .Values.testOr }}\n  orResult: {{ or $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: not - returns boolean negation\n  {{- if .Values.testNot }}\n  notResult: {{ not $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n  {{- if .Values.testEq }}\n  eqResult: {{ eq $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ne - returns true if not equal\n  {{- if .Values.testNe }}\n  neResult: {{ ne $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: lt - returns true if less than\n  {{- if .Values.testLt }}\n  ltResult: {{ lt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: le - returns true if less than or equal\n  {{- if .Values.testLe }}\n  leResult: {{ le $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: gt - returns true if greater than\n  {{- if .Values.testGt }}\n  gtResult: {{ gt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ge - returns true if greater than or equal\n  {{- if .Values.testGe }}\n  geResult: {{ ge $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n  {{- if .Values.testDefault }}\n  defaultResult: {{ default \"fallback\" $testData | quote }}\n  {{- end }}\n\n  # Test: required - fails if value is empty\n  {{- if .Values.testRequired }}\n  requiredResult: {{ required \"value is required\" $testData | quote }}\n  {{- end }}\n\n  # Test: empty - returns true if value is empty\n  {{- if .Values.testEmpty }}\n  emptyResult: {{ empty $testData | quote }}\n  {{- end }}\n\n  # Test: fail - explicitly fails with message\n  {{- if .Values.testFail }}\n  failResult: {{ fail (printf \"intentional fail with: %v\" $testData) }}\n  {{- end }}\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n  {{- if .Values.testCoalesce }}\n  coalesceResult: {{ coalesce $testData .Values.compareValue \"default\" | quote }}\n  {{- end }}\n\n  # Test: ternary - conditional expression\n  {{- if .Values.testTernary }}\n  ternaryResult: {{ ternary \"yes\" \"no\" $testData | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "coalesce",
      "dataType": "dictValue",
      "valuesOptions": {
        "values": [
          "testCoalesce=true",
          "testDataType=dictValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-logic-test\r\ndata:\r\n  # ==========================================\r\n  # Boolean Logic Functions\r\n  # ==========================================\r\n  # Test: and - returns boolean AND of arguments\r\n\r\n  # Test: or - returns boolean OR of arguments\r\n\r\n  # Test: not - returns boolean negation\r\n\r\n  # ==========================================\r\n  # Comparison Functions\r\n  # ==========================================\r\n  # Test: eq - returns true if equal\r\n\r\n  # Test: ne - returns true if not equal\r\n\r\n  # Test: lt - returns true if less than\r\n\r\n  # Test: le - returns true if less than or equal\r\n\r\n  # Test: gt - returns true if greater than\r\n\r\n  # Test: ge - returns true if greater than or equal\r\n\r\n  # ==========================================\r\n  # Default/Required Functions\r\n  # ==========================================\r\n  # Test: default - returns default value if empty\r\n\r\n  # Test: required - fails if value is empty\r\n\r\n  # Test: empty - returns true if value is empty\r\n\r\n  # Test: fail - explicitly fails with message\r\n\r\n  # ==========================================\r\n  # Coalesce/Ternary Functions\r\n  # ==========================================\r\n  # Test: coalesce - returns first non-empty value\r\n  coalesceResult: \"map[key:value]\"\r\n\r\n  # Test: ternary - conditional expression\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-logic-default/templates/logic-default-test.yaml",
            "content": "\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n\n  # Test: or - returns boolean OR of arguments\n\n  # Test: not - returns boolean negation\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n\n  # Test: ne - returns true if not equal\n\n  # Test: lt - returns true if less than\n\n  # Test: le - returns true if less than or equal\n\n  # Test: gt - returns true if greater than\n\n  # Test: ge - returns true if greater than or equal\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n\n  # Test: required - fails if value is empty\n\n  # Test: empty - returns true if value is empty\n\n  # Test: fail - explicitly fails with message\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n  coalesceResult: \"map[key:value]\"\n\n  # Test: ternary - conditional expression\n",
            "originalContent": "# Test: Logic and default functions - comprehensive testing\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n  {{- if .Values.testAnd }}\n  andResult: {{ and $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: or - returns boolean OR of arguments\n  {{- if .Values.testOr }}\n  orResult: {{ or $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: not - returns boolean negation\n  {{- if .Values.testNot }}\n  notResult: {{ not $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n  {{- if .Values.testEq }}\n  eqResult: {{ eq $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ne - returns true if not equal\n  {{- if .Values.testNe }}\n  neResult: {{ ne $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: lt - returns true if less than\n  {{- if .Values.testLt }}\n  ltResult: {{ lt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: le - returns true if less than or equal\n  {{- if .Values.testLe }}\n  leResult: {{ le $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: gt - returns true if greater than\n  {{- if .Values.testGt }}\n  gtResult: {{ gt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ge - returns true if greater than or equal\n  {{- if .Values.testGe }}\n  geResult: {{ ge $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n  {{- if .Values.testDefault }}\n  defaultResult: {{ default \"fallback\" $testData | quote }}\n  {{- end }}\n\n  # Test: required - fails if value is empty\n  {{- if .Values.testRequired }}\n  requiredResult: {{ required \"value is required\" $testData | quote }}\n  {{- end }}\n\n  # Test: empty - returns true if value is empty\n  {{- if .Values.testEmpty }}\n  emptyResult: {{ empty $testData | quote }}\n  {{- end }}\n\n  # Test: fail - explicitly fails with message\n  {{- if .Values.testFail }}\n  failResult: {{ fail (printf \"intentional fail with: %v\" $testData) }}\n  {{- end }}\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n  {{- if .Values.testCoalesce }}\n  coalesceResult: {{ coalesce $testData .Values.compareValue \"default\" | quote }}\n  {{- end }}\n\n  # Test: ternary - conditional expression\n  {{- if .Values.testTernary }}\n  ternaryResult: {{ ternary \"yes\" \"no\" $testData | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-logic-test\r\ndata:\r\n  # ==========================================\r\n  # Boolean Logic Functions\r\n  # ==========================================\r\n  # Test: and - returns boolean AND of arguments\r\n\r\n  # Test: or - returns boolean OR of arguments\r\n\r\n  # Test: not - returns boolean negation\r\n\r\n  # ==========================================\r\n  # Comparison Functions\r\n  # ==========================================\r\n  # Test: eq - returns true if equal\r\n\r\n  # Test: ne - returns true if not equal\r\n\r\n  # Test: lt - returns true if less than\r\n\r\n  # Test: le - returns true if less than or equal\r\n\r\n  # Test: gt - returns true if greater than\r\n\r\n  # Test: ge - returns true if greater than or equal\r\n\r\n  # ==========================================\r\n  # Default/Required Functions\r\n  # ==========================================\r\n  # Test: default - returns default value if empty\r\n\r\n  # Test: required - fails if value is empty\r\n\r\n  # Test: empty - returns true if value is empty\r\n\r\n  # Test: fail - explicitly fails with message\r\n\r\n  # ==========================================\r\n  # Coalesce/Ternary Functions\r\n  # ==========================================\r\n  # Test: coalesce - returns first non-empty value\r\n  coalesceResult: \"map[key:value]\"\r\n\r\n  # Test: ternary - conditional expression\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-logic-default/templates/logic-default-test.yaml",
            "content": "\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n\n  # Test: or - returns boolean OR of arguments\n\n  # Test: not - returns boolean negation\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n\n  # Test: ne - returns true if not equal\n\n  # Test: lt - returns true if less than\n\n  # Test: le - returns true if less than or equal\n\n  # Test: gt - returns true if greater than\n\n  # Test: ge - returns true if greater than or equal\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n\n  # Test: required - fails if value is empty\n\n  # Test: empty - returns true if value is empty\n\n  # Test: fail - explicitly fails with message\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n  coalesceResult: \"map[key:value]\"\n\n  # Test: ternary - conditional expression\n",
            "originalContent": "# Test: Logic and default functions - comprehensive testing\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n  {{- if .Values.testAnd }}\n  andResult: {{ and $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: or - returns boolean OR of arguments\n  {{- if .Values.testOr }}\n  orResult: {{ or $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: not - returns boolean negation\n  {{- if .Values.testNot }}\n  notResult: {{ not $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n  {{- if .Values.testEq }}\n  eqResult: {{ eq $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ne - returns true if not equal\n  {{- if .Values.testNe }}\n  neResult: {{ ne $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: lt - returns true if less than\n  {{- if .Values.testLt }}\n  ltResult: {{ lt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: le - returns true if less than or equal\n  {{- if .Values.testLe }}\n  leResult: {{ le $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: gt - returns true if greater than\n  {{- if .Values.testGt }}\n  gtResult: {{ gt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ge - returns true if greater than or equal\n  {{- if .Values.testGe }}\n  geResult: {{ ge $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n  {{- if .Values.testDefault }}\n  defaultResult: {{ default \"fallback\" $testData | quote }}\n  {{- end }}\n\n  # Test: required - fails if value is empty\n  {{- if .Values.testRequired }}\n  requiredResult: {{ required \"value is required\" $testData | quote }}\n  {{- end }}\n\n  # Test: empty - returns true if value is empty\n  {{- if .Values.testEmpty }}\n  emptyResult: {{ empty $testData | quote }}\n  {{- end }}\n\n  # Test: fail - explicitly fails with message\n  {{- if .Values.testFail }}\n  failResult: {{ fail (printf \"intentional fail with: %v\" $testData) }}\n  {{- end }}\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n  {{- if .Values.testCoalesce }}\n  coalesceResult: {{ coalesce $testData .Values.compareValue \"default\" | quote }}\n  {{- end }}\n\n  # Test: ternary - conditional expression\n  {{- if .Values.testTernary }}\n  ternaryResult: {{ ternary \"yes\" \"no\" $testData | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "ternary",
      "dataType": "nilValue",
      "valuesOptions": {
        "values": [
          "testTernary=true",
          "testDataType=nilValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": "template: test-logic-default/templates/logic-default-test.yaml:93:39: executing \"test-logic-default/templates/logic-default-test.yaml\" at \u003c$testData\u003e: invalid value; expected bool",
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      },
      "debug": {
        "error": "template: test-logic-default/templates/logic-default-test.yaml:93:39: executing \"test-logic-default/templates/logic-default-test.yaml\" at \u003c$testData\u003e: invalid value; expected bool",
        "debugLogs": [],
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      }
    },
    {
      "toggle": "ternary",
      "dataType": "stringValue",
      "valuesOptions": {
        "values": [
          "testTernary=true",
          "testDataType=stringValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": "template: test-logic-default/templates/logic-default-test.yaml:93:39: executing \"test-logic-default/templates/logic-default-test.yaml\" at \u003c$testData\u003e: wrong type for value; expected bool; got string",
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      },
      "debug": {
        "error": "template: test-logic-default/templates/logic-default-test.yaml:93:39: executing \"test-logic-default/templates/logic-default-test.yaml\" at \u003c$testData\u003e: wrong type for value; expected bool; got string",
        "debugLogs": [],
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      }
    },
    {
      "toggle": "ternary",
      "dataType": "numberValue",
      "valuesOptions": {
        "values": [
          "testTernary=true",
          "testDataType=numberValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": "template: test-logic-default/templates/logic-default-test.yaml:93:39: executing \"test-logic-default/templates/logic-default-test.yaml\" at \u003c$testData\u003e: wrong type for value; expected bool; got float64",
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      },
      "debug": {
        "error": "template: test-logic-default/templates/logic-default-test.yaml:93:39: executing \"test-logic-default/templates/logic-default-test.yaml\" at \u003c$testData\u003e: wrong type for value; expected bool; got float64",
        "debugLogs": [],
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      }
    },
    {
      "toggle": "ternary",
      "dataType": "boolValue",
      "valuesOptions": {
        "values": [
          "testTernary=true",
          "testDataType=boolValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-logic-test\r\ndata:\r\n  # ==========================================\r\n  # Boolean Logic Functions\r\n  # ==========================================\r\n  # Test: and - returns boolean AND of arguments\r\n\r\n  # Test: or - returns boolean OR of arguments\r\n\r\n  # Test: not - returns boolean negation\r\n\r\n  # ==========================================\r\n  # Comparison Functions\r\n  # ==========================================\r\n  # Test: eq - returns true if equal\r\n\r\n  # Test: ne - returns true if not equal\r\n\r\n  # Test: lt - returns true if less than\r\n\r\n  # Test: le - returns true if less than or equal\r\n\r\n  # Test: gt - returns true if greater than\r\n\r\n  # Test: ge - returns true if greater than or equal\r\n\r\n  # ==========================================\r\n  # Default/Required Functions\r\n  # ==========================================\r\n  # Test: default - returns default value if empty\r\n\r\n  # Test: required - fails if value is empty\r\n\r\n  # Test: empty - returns true if value is empty\r\n\r\n  # Test: fail - explicitly fails with message\r\n\r\n  # ==========================================\r\n  # Coalesce/Ternary Functions\r\n  # ==========================================\r\n  # Test: coalesce - returns first non-empty value\r\n\r\n  # Test: ternary - conditional expression\r\n  ternaryResult: \"yes\"\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-logic-default/templates/logic-default-test.yaml",
            "content": "\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n\n  # Test: or - returns boolean OR of arguments\n\n  # Test: not - returns boolean negation\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n\n  # Test: ne - returns true if not equal\n\n  # Test: lt - returns true if less than\n\n  # Test: le - returns true if less than or equal\n\n  # Test: gt - returns true if greater than\n\n  # Test: ge - returns true if greater than or equal\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n\n  # Test: required - fails if value is empty\n\n  # Test: empty - returns true if value is empty\n\n  # Test: fail - explicitly fails with message\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n\n  # Test: ternary - conditional expression\n  ternaryResult: \"yes\"\n",
            "originalContent": "# Test: Logic and default functions - comprehensive testing\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n  {{- if .Values.testAnd }}\n  andResult: {{ and $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: or - returns boolean OR of arguments\n  {{- if .Values.testOr }}\n  orResult: {{ or $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: not - returns boolean negation\n  {{- if .Values.testNot }}\n  notResult: {{ not $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n  {{- if .Values.testEq }}\n  eqResult: {{ eq $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ne - returns true if not equal\n  {{- if .Values.testNe }}\n  neResult: {{ ne $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: lt - returns true if less than\n  {{- if .Values.testLt }}\n  ltResult: {{ lt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: le - returns true if less than or equal\n  {{- if .Values.testLe }}\n  leResult: {{ le $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: gt - returns true if greater than\n  {{- if .Values.testGt }}\n  gtResult: {{ gt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ge - returns true if greater than or equal\n  {{- if .Values.testGe }}\n  geResult: {{ ge $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n  {{- if .Values.testDefault }}\n  defaultResult: {{ default \"fallback\" $testData | quote }}\n  {{- end }}\n\n  # Test: required - fails if value is empty\n  {{- if .Values.testRequired }}\n  requiredResult: {{ required \"value is required\" $testData | quote }}\n  {{- end }}\n\n  # Test: empty - returns true if value is empty\n  {{- if .Values.testEmpty }}\n  emptyResult: {{ empty $testData | quote }}\n  {{- end }}\n\n  # Test: fail - explicitly fails with message\n  {{- if .Values.testFail }}\n  failResult: {{ fail (printf \"intentional fail with: %v\" $testData) }}\n  {{- end }}\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n  {{- if .Values.testCoalesce }}\n  coalesceResult: {{ coalesce $testData .Values.compareValue \"default\" | quote }}\n  {{- end }}\n\n  # Test: ternary - conditional expression\n  {{- if .Values.testTernary }}\n  ternaryResult: {{ ternary \"yes\" \"no\" $testData | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-logic-test\r\ndata:\r\n  # ==========================================\r\n  # Boolean Logic Functions\r\n  # ==========================================\r\n  # Test: and - returns boolean AND of arguments\r\n\r\n  # Test: or - returns boolean OR of arguments\r\n\r\n  # Test: not - returns boolean negation\r\n\r\n  # ==========================================\r\n  # Comparison Functions\r\n  # ==========================================\r\n  # Test: eq - returns true if equal\r\n\r\n  # Test: ne - returns true if not equal\r\n\r\n  # Test: lt - returns true if less than\r\n\r\n  # Test: le - returns true if less than or equal\r\n\r\n  # Test: gt - returns true if greater than\r\n\r\n  # Test: ge - returns true if greater than or equal\r\n\r\n  # ==========================================\r\n  # Default/Required Functions\r\n  # ==========================================\r\n  # Test: default - returns default value if empty\r\n\r\n  # Test: required - fails if value is empty\r\n\r\n  # Test: empty - returns true if value is empty\r\n\r\n  # Test: fail - explicitly fails with message\r\n\r\n  # ==========================================\r\n  # Coalesce/Ternary Functions\r\n  # ==========================================\r\n  # Test: coalesce - returns first non-empty value\r\n\r\n  # Test: ternary - conditional expression\r\n  ternaryResult: \"yes\"\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-logic-default/templates/logic-default-test.yaml",
            "content": "\n# Source: test-logic-default/templates/logic-default-test.yaml\n# Test: Logic and default functions - comprehensive testing\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n\n  # Test: or - returns boolean OR of arguments\n\n  # Test: not - returns boolean negation\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n\n  # Test: ne - returns true if not equal\n\n  # Test: lt - returns true if less than\n\n  # Test: le - returns true if less than or equal\n\n  # Test: gt - returns true if greater than\n\n  # Test: ge - returns true if greater than or equal\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n\n  # Test: required - fails if value is empty\n\n  # Test: empty - returns true if value is empty\n\n  # Test: fail - explicitly fails with message\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n\n  # Test: ternary - conditional expression\n  ternaryResult: \"yes\"\n",
            "originalContent": "# Test: Logic and default functions - comprehensive testing\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-logic-test\ndata:\n  # ==========================================\n  # Boolean Logic Functions\n  # ==========================================\n  # Test: and - returns boolean AND of arguments\n  {{- if .Values.testAnd }}\n  andResult: {{ and $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: or - returns boolean OR of arguments\n  {{- if .Values.testOr }}\n  orResult: {{ or $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: not - returns boolean negation\n  {{- if .Values.testNot }}\n  notResult: {{ not $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Comparison Functions\n  # ==========================================\n  # Test: eq - returns true if equal\n  {{- if .Values.testEq }}\n  eqResult: {{ eq $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ne - returns true if not equal\n  {{- if .Values.testNe }}\n  neResult: {{ ne $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: lt - returns true if less than\n  {{- if .Values.testLt }}\n  ltResult: {{ lt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: le - returns true if less than or equal\n  {{- if .Values.testLe }}\n  leResult: {{ le $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: gt - returns true if greater than\n  {{- if .Values.testGt }}\n  gtResult: {{ gt $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # Test: ge - returns true if greater than or equal\n  {{- if .Values.testGe }}\n  geResult: {{ ge $testData .Values.compareValue | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Default/Required Functions\n  # ==========================================\n  # Test: default - returns default value if empty\n  {{- if .Values.testDefault }}\n  defaultResult: {{ default \"fallback\" $testData | quote }}\n  {{- end }}\n\n  # Test: required - fails if value is empty\n  {{- if .Values.testRequired }}\n  requiredResult: {{ required \"value is required\" $testData | quote }}\n  {{- end }}\n\n  # Test: empty - returns true if value is empty\n  {{- if .Values.testEmpty }}\n  emptyResult: {{ empty $testData | quote }}\n  {{- end }}\n\n  # Test: fail - explicitly fails with message\n  {{- if .Values.testFail }}\n  failResult: {{ fail (printf \"intentional fail with: %v\" $testData) }}\n  {{- end }}\n\n  # ==========================================\n  # Coalesce/Ternary Functions\n  # ==========================================\n  # Test: coalesce - returns first non-empty value\n  {{- if .Values.testCoalesce }}\n  coalesceResult: {{ coalesce $testData .Values.compareValue \"default\" | quote }}\n  {{- end }}\n\n  # Test: ternary - conditional expression\n  {{- if .Values.testTernary }}\n  ternaryResult: {{ ternary \"yes\" \"no\" $testData | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      }
    },
    {
      "toggle": "ternary",
      "dataType": "listValue",
      "valuesOptions": {
        "values": [
          "testTernary=true",
          "testDataType=listValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": "template: test-logic-default/templates/logic-default-test.yaml:93:39: executing \"test-logic-default/templates/logic-default-test.yaml\" at \u003c$testData\u003e: wrong type for value; expected bool; got []interface {}",
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      },
      "debug": {
        "error": "template: test-logic-default/templates/logic-default-test.yaml:93:39: executing \"test-logic-default/templates/logic-default-test.yaml\" at \u003c$testData\u003e: wrong type for value; expected bool; got []interface {}",
        "debugLogs": [],
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      }
    },
    {
      "toggle": "ternary",
      "dataType": "dictValue",
      "valuesOptions": {
        "values": [
          "testTernary=true",
          "testDataType=dictValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": "template: test-logic-default/templates/logic-default-test.yaml:93:39: executing \"test-logic-default/templates/logic-default-test.yaml\" at \u003c$testData\u003e: wrong type for value; expected bool; got map[string]interface {}",
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      },
      "debug": {
        "error": "template: test-logic-default/templates/logic-default-test.yaml:93:39: executing \"test-logic-default/templates/logic-default-test.yaml\" at \u003c$testData\u003e: wrong type for value; expected bool; got map[string]interface {}",
        "debugLogs": [],
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      }
    }
  ]
}
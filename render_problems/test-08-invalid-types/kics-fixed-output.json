{
  "suite": "render_problems",
  "testNumber": 11,
  "testName": "Type Conversion Errors",
  "chartPath": "..\\render_problems\\test-08-invalid-types",
  "renders": [
    {
      "toggle": "toStrings",
      "dataType": "nilValue",
      "valuesOptions": {
        "values": [
          "testToStrings=true",
          "testDataType=nilValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-type-conversion-test\r\ndata:\r\n  # ==========================================\r\n  # String conversion functions\r\n  # ==========================================\r\n  # Test: toStrings\r\n  toStringsValue: []\r\n\r\n  # ==========================================\r\n  # Decimal conversion functions\r\n  # ==========================================\r\n  # Test: toDecimal\r\n\r\n  # ==========================================\r\n  # JSON conversion functions\r\n  # ==========================================\r\n  # Test: toJson\r\n  # Test: mustToJson\r\n  # Test: toPrettyJson\r\n  # Test: mustToPrettyJson\r\n  # Test: toRawJson\r\n  # Test: mustToRawJson\r\n  # Test: fromJson\r\n  # Test: fromJsonArray\r\n\r\n  # ==========================================\r\n  # YAML conversion functions\r\n  # ==========================================\r\n  # Test: fromYaml\r\n  # Test: toYaml\r\n  # Test: toYamlPretty\r\n  # Test: fromYamlArray\r\n\r\n  # ==========================================\r\n  # TOML conversion functions\r\n  # ==========================================\r\n  # Test: toToml\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-type-conversion/templates/type-conversion-test.yaml",
            "content": "\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n  toStringsValue: []\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  # Test: mustToJson\n  # Test: toPrettyJson\n  # Test: mustToPrettyJson\n  # Test: toRawJson\n  # Test: mustToRawJson\n  # Test: fromJson\n  # Test: fromJsonArray\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  # Test: toYaml\n  # Test: toYamlPretty\n  # Test: fromYamlArray\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n",
            "originalContent": "# Test: Type conversion errors - comprehensive testing of all type conversion functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n  {{- if .Values.testToStrings }}\n  toStringsValue: {{ toStrings $testData | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n  {{- if .Values.testToDecimal }}\n  toDecimalValue: {{ toDecimal $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  {{- if .Values.testToJson }}\n  toJsonValue: {{ toJson $testData | quote }}\n  {{- end }}\n  # Test: mustToJson\n  {{- if .Values.testMustToJson }}\n  mustToJsonValue: {{ mustToJson $testData | quote }}\n  {{- end }}\n  # Test: toPrettyJson\n  {{- if .Values.testToPrettyJson }}\n  toPrettyJsonValue: {{ toPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: mustToPrettyJson\n  {{- if .Values.testMustToPrettyJson }}\n  mustToPrettyJsonValue: {{ mustToPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: toRawJson\n  {{- if .Values.testToRawJson }}\n  toRawJsonValue: {{ toRawJson $testData | quote }}\n  {{- end }}\n  # Test: mustToRawJson\n  {{- if .Values.testMustToRawJson }}\n  mustToRawJsonValue: {{ mustToRawJson $testData | quote }}\n  {{- end }}\n  # Test: fromJson\n  {{- if .Values.testFromJson }}\n  {{- $parsed := $testData | fromJson }}\n  fromJsonValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: fromJsonArray\n  {{- if .Values.testFromJsonArray }}\n  {{- $arr := $testData | fromJsonArray }}\n  fromJsonArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  {{- if .Values.testFromYaml }}\n  {{- $parsed := $testData | fromYaml }}\n  fromYamlValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: toYaml\n  {{- if .Values.testToYaml }}\n  toYamlValue: {{ toYaml $testData | quote }}\n  {{- end }}\n  # Test: toYamlPretty\n  {{- if .Values.testToYamlPretty }}\n  toYamlPrettyValue: {{ toYamlPretty $testData | quote }}\n  {{- end }}\n  # Test: fromYamlArray\n  {{- if .Values.testFromYamlArray }}\n  {{- $arr := $testData | fromYamlArray }}\n  fromYamlArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n  {{- if .Values.testToToml }}\n  toTomlValue: {{ toToml $testData | quote }}\n  {{- end }}\n\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-type-conversion-test\r\ndata:\r\n  # ==========================================\r\n  # String conversion functions\r\n  # ==========================================\r\n  # Test: toStrings\r\n  toStringsValue: []\r\n\r\n  # ==========================================\r\n  # Decimal conversion functions\r\n  # ==========================================\r\n  # Test: toDecimal\r\n\r\n  # ==========================================\r\n  # JSON conversion functions\r\n  # ==========================================\r\n  # Test: toJson\r\n  # Test: mustToJson\r\n  # Test: toPrettyJson\r\n  # Test: mustToPrettyJson\r\n  # Test: toRawJson\r\n  # Test: mustToRawJson\r\n  # Test: fromJson\r\n  # Test: fromJsonArray\r\n\r\n  # ==========================================\r\n  # YAML conversion functions\r\n  # ==========================================\r\n  # Test: fromYaml\r\n  # Test: toYaml\r\n  # Test: toYamlPretty\r\n  # Test: fromYamlArray\r\n\r\n  # ==========================================\r\n  # TOML conversion functions\r\n  # ==========================================\r\n  # Test: toToml\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-type-conversion/templates/type-conversion-test.yaml",
            "content": "\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n  toStringsValue: []\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  # Test: mustToJson\n  # Test: toPrettyJson\n  # Test: mustToPrettyJson\n  # Test: toRawJson\n  # Test: mustToRawJson\n  # Test: fromJson\n  # Test: fromJsonArray\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  # Test: toYaml\n  # Test: toYamlPretty\n  # Test: fromYamlArray\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n",
            "originalContent": "# Test: Type conversion errors - comprehensive testing of all type conversion functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n  {{- if .Values.testToStrings }}\n  toStringsValue: {{ toStrings $testData | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n  {{- if .Values.testToDecimal }}\n  toDecimalValue: {{ toDecimal $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  {{- if .Values.testToJson }}\n  toJsonValue: {{ toJson $testData | quote }}\n  {{- end }}\n  # Test: mustToJson\n  {{- if .Values.testMustToJson }}\n  mustToJsonValue: {{ mustToJson $testData | quote }}\n  {{- end }}\n  # Test: toPrettyJson\n  {{- if .Values.testToPrettyJson }}\n  toPrettyJsonValue: {{ toPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: mustToPrettyJson\n  {{- if .Values.testMustToPrettyJson }}\n  mustToPrettyJsonValue: {{ mustToPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: toRawJson\n  {{- if .Values.testToRawJson }}\n  toRawJsonValue: {{ toRawJson $testData | quote }}\n  {{- end }}\n  # Test: mustToRawJson\n  {{- if .Values.testMustToRawJson }}\n  mustToRawJsonValue: {{ mustToRawJson $testData | quote }}\n  {{- end }}\n  # Test: fromJson\n  {{- if .Values.testFromJson }}\n  {{- $parsed := $testData | fromJson }}\n  fromJsonValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: fromJsonArray\n  {{- if .Values.testFromJsonArray }}\n  {{- $arr := $testData | fromJsonArray }}\n  fromJsonArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  {{- if .Values.testFromYaml }}\n  {{- $parsed := $testData | fromYaml }}\n  fromYamlValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: toYaml\n  {{- if .Values.testToYaml }}\n  toYamlValue: {{ toYaml $testData | quote }}\n  {{- end }}\n  # Test: toYamlPretty\n  {{- if .Values.testToYamlPretty }}\n  toYamlPrettyValue: {{ toYamlPretty $testData | quote }}\n  {{- end }}\n  # Test: fromYamlArray\n  {{- if .Values.testFromYamlArray }}\n  {{- $arr := $testData | fromYamlArray }}\n  fromYamlArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n  {{- if .Values.testToToml }}\n  toTomlValue: {{ toToml $testData | quote }}\n  {{- end }}\n\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "resolved": true,
      "stopReason": "",
      "fixChain": []
    },
    {
      "toggle": "toStrings",
      "dataType": "stringValue",
      "valuesOptions": {
        "values": [
          "testToStrings=true",
          "testDataType=stringValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-type-conversion-test\r\ndata:\r\n  # ==========================================\r\n  # String conversion functions\r\n  # ==========================================\r\n  # Test: toStrings\r\n  toStringsValue: [\"test string\"]\r\n\r\n  # ==========================================\r\n  # Decimal conversion functions\r\n  # ==========================================\r\n  # Test: toDecimal\r\n\r\n  # ==========================================\r\n  # JSON conversion functions\r\n  # ==========================================\r\n  # Test: toJson\r\n  # Test: mustToJson\r\n  # Test: toPrettyJson\r\n  # Test: mustToPrettyJson\r\n  # Test: toRawJson\r\n  # Test: mustToRawJson\r\n  # Test: fromJson\r\n  # Test: fromJsonArray\r\n\r\n  # ==========================================\r\n  # YAML conversion functions\r\n  # ==========================================\r\n  # Test: fromYaml\r\n  # Test: toYaml\r\n  # Test: toYamlPretty\r\n  # Test: fromYamlArray\r\n\r\n  # ==========================================\r\n  # TOML conversion functions\r\n  # ==========================================\r\n  # Test: toToml\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-type-conversion/templates/type-conversion-test.yaml",
            "content": "\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n  toStringsValue: [\"test string\"]\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  # Test: mustToJson\n  # Test: toPrettyJson\n  # Test: mustToPrettyJson\n  # Test: toRawJson\n  # Test: mustToRawJson\n  # Test: fromJson\n  # Test: fromJsonArray\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  # Test: toYaml\n  # Test: toYamlPretty\n  # Test: fromYamlArray\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n",
            "originalContent": "# Test: Type conversion errors - comprehensive testing of all type conversion functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n  {{- if .Values.testToStrings }}\n  toStringsValue: {{ toStrings $testData | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n  {{- if .Values.testToDecimal }}\n  toDecimalValue: {{ toDecimal $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  {{- if .Values.testToJson }}\n  toJsonValue: {{ toJson $testData | quote }}\n  {{- end }}\n  # Test: mustToJson\n  {{- if .Values.testMustToJson }}\n  mustToJsonValue: {{ mustToJson $testData | quote }}\n  {{- end }}\n  # Test: toPrettyJson\n  {{- if .Values.testToPrettyJson }}\n  toPrettyJsonValue: {{ toPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: mustToPrettyJson\n  {{- if .Values.testMustToPrettyJson }}\n  mustToPrettyJsonValue: {{ mustToPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: toRawJson\n  {{- if .Values.testToRawJson }}\n  toRawJsonValue: {{ toRawJson $testData | quote }}\n  {{- end }}\n  # Test: mustToRawJson\n  {{- if .Values.testMustToRawJson }}\n  mustToRawJsonValue: {{ mustToRawJson $testData | quote }}\n  {{- end }}\n  # Test: fromJson\n  {{- if .Values.testFromJson }}\n  {{- $parsed := $testData | fromJson }}\n  fromJsonValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: fromJsonArray\n  {{- if .Values.testFromJsonArray }}\n  {{- $arr := $testData | fromJsonArray }}\n  fromJsonArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  {{- if .Values.testFromYaml }}\n  {{- $parsed := $testData | fromYaml }}\n  fromYamlValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: toYaml\n  {{- if .Values.testToYaml }}\n  toYamlValue: {{ toYaml $testData | quote }}\n  {{- end }}\n  # Test: toYamlPretty\n  {{- if .Values.testToYamlPretty }}\n  toYamlPrettyValue: {{ toYamlPretty $testData | quote }}\n  {{- end }}\n  # Test: fromYamlArray\n  {{- if .Values.testFromYamlArray }}\n  {{- $arr := $testData | fromYamlArray }}\n  fromYamlArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n  {{- if .Values.testToToml }}\n  toTomlValue: {{ toToml $testData | quote }}\n  {{- end }}\n\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-type-conversion-test\r\ndata:\r\n  # ==========================================\r\n  # String conversion functions\r\n  # ==========================================\r\n  # Test: toStrings\r\n  toStringsValue: [\"test string\"]\r\n\r\n  # ==========================================\r\n  # Decimal conversion functions\r\n  # ==========================================\r\n  # Test: toDecimal\r\n\r\n  # ==========================================\r\n  # JSON conversion functions\r\n  # ==========================================\r\n  # Test: toJson\r\n  # Test: mustToJson\r\n  # Test: toPrettyJson\r\n  # Test: mustToPrettyJson\r\n  # Test: toRawJson\r\n  # Test: mustToRawJson\r\n  # Test: fromJson\r\n  # Test: fromJsonArray\r\n\r\n  # ==========================================\r\n  # YAML conversion functions\r\n  # ==========================================\r\n  # Test: fromYaml\r\n  # Test: toYaml\r\n  # Test: toYamlPretty\r\n  # Test: fromYamlArray\r\n\r\n  # ==========================================\r\n  # TOML conversion functions\r\n  # ==========================================\r\n  # Test: toToml\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-type-conversion/templates/type-conversion-test.yaml",
            "content": "\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n  toStringsValue: [\"test string\"]\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  # Test: mustToJson\n  # Test: toPrettyJson\n  # Test: mustToPrettyJson\n  # Test: toRawJson\n  # Test: mustToRawJson\n  # Test: fromJson\n  # Test: fromJsonArray\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  # Test: toYaml\n  # Test: toYamlPretty\n  # Test: fromYamlArray\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n",
            "originalContent": "# Test: Type conversion errors - comprehensive testing of all type conversion functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n  {{- if .Values.testToStrings }}\n  toStringsValue: {{ toStrings $testData | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n  {{- if .Values.testToDecimal }}\n  toDecimalValue: {{ toDecimal $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  {{- if .Values.testToJson }}\n  toJsonValue: {{ toJson $testData | quote }}\n  {{- end }}\n  # Test: mustToJson\n  {{- if .Values.testMustToJson }}\n  mustToJsonValue: {{ mustToJson $testData | quote }}\n  {{- end }}\n  # Test: toPrettyJson\n  {{- if .Values.testToPrettyJson }}\n  toPrettyJsonValue: {{ toPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: mustToPrettyJson\n  {{- if .Values.testMustToPrettyJson }}\n  mustToPrettyJsonValue: {{ mustToPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: toRawJson\n  {{- if .Values.testToRawJson }}\n  toRawJsonValue: {{ toRawJson $testData | quote }}\n  {{- end }}\n  # Test: mustToRawJson\n  {{- if .Values.testMustToRawJson }}\n  mustToRawJsonValue: {{ mustToRawJson $testData | quote }}\n  {{- end }}\n  # Test: fromJson\n  {{- if .Values.testFromJson }}\n  {{- $parsed := $testData | fromJson }}\n  fromJsonValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: fromJsonArray\n  {{- if .Values.testFromJsonArray }}\n  {{- $arr := $testData | fromJsonArray }}\n  fromJsonArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  {{- if .Values.testFromYaml }}\n  {{- $parsed := $testData | fromYaml }}\n  fromYamlValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: toYaml\n  {{- if .Values.testToYaml }}\n  toYamlValue: {{ toYaml $testData | quote }}\n  {{- end }}\n  # Test: toYamlPretty\n  {{- if .Values.testToYamlPretty }}\n  toYamlPrettyValue: {{ toYamlPretty $testData | quote }}\n  {{- end }}\n  # Test: fromYamlArray\n  {{- if .Values.testFromYamlArray }}\n  {{- $arr := $testData | fromYamlArray }}\n  fromYamlArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n  {{- if .Values.testToToml }}\n  toTomlValue: {{ toToml $testData | quote }}\n  {{- end }}\n\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "resolved": true,
      "stopReason": "",
      "fixChain": []
    },
    {
      "toggle": "toStrings",
      "dataType": "numberValue",
      "valuesOptions": {
        "values": [
          "testToStrings=true",
          "testDataType=numberValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-type-conversion-test\r\ndata:\r\n  # ==========================================\r\n  # String conversion functions\r\n  # ==========================================\r\n  # Test: toStrings\r\n  toStringsValue: [\"42\"]\r\n\r\n  # ==========================================\r\n  # Decimal conversion functions\r\n  # ==========================================\r\n  # Test: toDecimal\r\n\r\n  # ==========================================\r\n  # JSON conversion functions\r\n  # ==========================================\r\n  # Test: toJson\r\n  # Test: mustToJson\r\n  # Test: toPrettyJson\r\n  # Test: mustToPrettyJson\r\n  # Test: toRawJson\r\n  # Test: mustToRawJson\r\n  # Test: fromJson\r\n  # Test: fromJsonArray\r\n\r\n  # ==========================================\r\n  # YAML conversion functions\r\n  # ==========================================\r\n  # Test: fromYaml\r\n  # Test: toYaml\r\n  # Test: toYamlPretty\r\n  # Test: fromYamlArray\r\n\r\n  # ==========================================\r\n  # TOML conversion functions\r\n  # ==========================================\r\n  # Test: toToml\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-type-conversion/templates/type-conversion-test.yaml",
            "content": "\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n  toStringsValue: [\"42\"]\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  # Test: mustToJson\n  # Test: toPrettyJson\n  # Test: mustToPrettyJson\n  # Test: toRawJson\n  # Test: mustToRawJson\n  # Test: fromJson\n  # Test: fromJsonArray\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  # Test: toYaml\n  # Test: toYamlPretty\n  # Test: fromYamlArray\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n",
            "originalContent": "# Test: Type conversion errors - comprehensive testing of all type conversion functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n  {{- if .Values.testToStrings }}\n  toStringsValue: {{ toStrings $testData | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n  {{- if .Values.testToDecimal }}\n  toDecimalValue: {{ toDecimal $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  {{- if .Values.testToJson }}\n  toJsonValue: {{ toJson $testData | quote }}\n  {{- end }}\n  # Test: mustToJson\n  {{- if .Values.testMustToJson }}\n  mustToJsonValue: {{ mustToJson $testData | quote }}\n  {{- end }}\n  # Test: toPrettyJson\n  {{- if .Values.testToPrettyJson }}\n  toPrettyJsonValue: {{ toPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: mustToPrettyJson\n  {{- if .Values.testMustToPrettyJson }}\n  mustToPrettyJsonValue: {{ mustToPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: toRawJson\n  {{- if .Values.testToRawJson }}\n  toRawJsonValue: {{ toRawJson $testData | quote }}\n  {{- end }}\n  # Test: mustToRawJson\n  {{- if .Values.testMustToRawJson }}\n  mustToRawJsonValue: {{ mustToRawJson $testData | quote }}\n  {{- end }}\n  # Test: fromJson\n  {{- if .Values.testFromJson }}\n  {{- $parsed := $testData | fromJson }}\n  fromJsonValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: fromJsonArray\n  {{- if .Values.testFromJsonArray }}\n  {{- $arr := $testData | fromJsonArray }}\n  fromJsonArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  {{- if .Values.testFromYaml }}\n  {{- $parsed := $testData | fromYaml }}\n  fromYamlValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: toYaml\n  {{- if .Values.testToYaml }}\n  toYamlValue: {{ toYaml $testData | quote }}\n  {{- end }}\n  # Test: toYamlPretty\n  {{- if .Values.testToYamlPretty }}\n  toYamlPrettyValue: {{ toYamlPretty $testData | quote }}\n  {{- end }}\n  # Test: fromYamlArray\n  {{- if .Values.testFromYamlArray }}\n  {{- $arr := $testData | fromYamlArray }}\n  fromYamlArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n  {{- if .Values.testToToml }}\n  toTomlValue: {{ toToml $testData | quote }}\n  {{- end }}\n\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-type-conversion-test\r\ndata:\r\n  # ==========================================\r\n  # String conversion functions\r\n  # ==========================================\r\n  # Test: toStrings\r\n  toStringsValue: [\"42\"]\r\n\r\n  # ==========================================\r\n  # Decimal conversion functions\r\n  # ==========================================\r\n  # Test: toDecimal\r\n\r\n  # ==========================================\r\n  # JSON conversion functions\r\n  # ==========================================\r\n  # Test: toJson\r\n  # Test: mustToJson\r\n  # Test: toPrettyJson\r\n  # Test: mustToPrettyJson\r\n  # Test: toRawJson\r\n  # Test: mustToRawJson\r\n  # Test: fromJson\r\n  # Test: fromJsonArray\r\n\r\n  # ==========================================\r\n  # YAML conversion functions\r\n  # ==========================================\r\n  # Test: fromYaml\r\n  # Test: toYaml\r\n  # Test: toYamlPretty\r\n  # Test: fromYamlArray\r\n\r\n  # ==========================================\r\n  # TOML conversion functions\r\n  # ==========================================\r\n  # Test: toToml\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-type-conversion/templates/type-conversion-test.yaml",
            "content": "\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n  toStringsValue: [\"42\"]\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  # Test: mustToJson\n  # Test: toPrettyJson\n  # Test: mustToPrettyJson\n  # Test: toRawJson\n  # Test: mustToRawJson\n  # Test: fromJson\n  # Test: fromJsonArray\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  # Test: toYaml\n  # Test: toYamlPretty\n  # Test: fromYamlArray\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n",
            "originalContent": "# Test: Type conversion errors - comprehensive testing of all type conversion functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n  {{- if .Values.testToStrings }}\n  toStringsValue: {{ toStrings $testData | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n  {{- if .Values.testToDecimal }}\n  toDecimalValue: {{ toDecimal $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  {{- if .Values.testToJson }}\n  toJsonValue: {{ toJson $testData | quote }}\n  {{- end }}\n  # Test: mustToJson\n  {{- if .Values.testMustToJson }}\n  mustToJsonValue: {{ mustToJson $testData | quote }}\n  {{- end }}\n  # Test: toPrettyJson\n  {{- if .Values.testToPrettyJson }}\n  toPrettyJsonValue: {{ toPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: mustToPrettyJson\n  {{- if .Values.testMustToPrettyJson }}\n  mustToPrettyJsonValue: {{ mustToPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: toRawJson\n  {{- if .Values.testToRawJson }}\n  toRawJsonValue: {{ toRawJson $testData | quote }}\n  {{- end }}\n  # Test: mustToRawJson\n  {{- if .Values.testMustToRawJson }}\n  mustToRawJsonValue: {{ mustToRawJson $testData | quote }}\n  {{- end }}\n  # Test: fromJson\n  {{- if .Values.testFromJson }}\n  {{- $parsed := $testData | fromJson }}\n  fromJsonValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: fromJsonArray\n  {{- if .Values.testFromJsonArray }}\n  {{- $arr := $testData | fromJsonArray }}\n  fromJsonArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  {{- if .Values.testFromYaml }}\n  {{- $parsed := $testData | fromYaml }}\n  fromYamlValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: toYaml\n  {{- if .Values.testToYaml }}\n  toYamlValue: {{ toYaml $testData | quote }}\n  {{- end }}\n  # Test: toYamlPretty\n  {{- if .Values.testToYamlPretty }}\n  toYamlPrettyValue: {{ toYamlPretty $testData | quote }}\n  {{- end }}\n  # Test: fromYamlArray\n  {{- if .Values.testFromYamlArray }}\n  {{- $arr := $testData | fromYamlArray }}\n  fromYamlArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n  {{- if .Values.testToToml }}\n  toTomlValue: {{ toToml $testData | quote }}\n  {{- end }}\n\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "resolved": true,
      "stopReason": "",
      "fixChain": []
    },
    {
      "toggle": "toStrings",
      "dataType": "boolValue",
      "valuesOptions": {
        "values": [
          "testToStrings=true",
          "testDataType=boolValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-type-conversion-test\r\ndata:\r\n  # ==========================================\r\n  # String conversion functions\r\n  # ==========================================\r\n  # Test: toStrings\r\n  toStringsValue: [\"true\"]\r\n\r\n  # ==========================================\r\n  # Decimal conversion functions\r\n  # ==========================================\r\n  # Test: toDecimal\r\n\r\n  # ==========================================\r\n  # JSON conversion functions\r\n  # ==========================================\r\n  # Test: toJson\r\n  # Test: mustToJson\r\n  # Test: toPrettyJson\r\n  # Test: mustToPrettyJson\r\n  # Test: toRawJson\r\n  # Test: mustToRawJson\r\n  # Test: fromJson\r\n  # Test: fromJsonArray\r\n\r\n  # ==========================================\r\n  # YAML conversion functions\r\n  # ==========================================\r\n  # Test: fromYaml\r\n  # Test: toYaml\r\n  # Test: toYamlPretty\r\n  # Test: fromYamlArray\r\n\r\n  # ==========================================\r\n  # TOML conversion functions\r\n  # ==========================================\r\n  # Test: toToml\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-type-conversion/templates/type-conversion-test.yaml",
            "content": "\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n  toStringsValue: [\"true\"]\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  # Test: mustToJson\n  # Test: toPrettyJson\n  # Test: mustToPrettyJson\n  # Test: toRawJson\n  # Test: mustToRawJson\n  # Test: fromJson\n  # Test: fromJsonArray\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  # Test: toYaml\n  # Test: toYamlPretty\n  # Test: fromYamlArray\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n",
            "originalContent": "# Test: Type conversion errors - comprehensive testing of all type conversion functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n  {{- if .Values.testToStrings }}\n  toStringsValue: {{ toStrings $testData | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n  {{- if .Values.testToDecimal }}\n  toDecimalValue: {{ toDecimal $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  {{- if .Values.testToJson }}\n  toJsonValue: {{ toJson $testData | quote }}\n  {{- end }}\n  # Test: mustToJson\n  {{- if .Values.testMustToJson }}\n  mustToJsonValue: {{ mustToJson $testData | quote }}\n  {{- end }}\n  # Test: toPrettyJson\n  {{- if .Values.testToPrettyJson }}\n  toPrettyJsonValue: {{ toPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: mustToPrettyJson\n  {{- if .Values.testMustToPrettyJson }}\n  mustToPrettyJsonValue: {{ mustToPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: toRawJson\n  {{- if .Values.testToRawJson }}\n  toRawJsonValue: {{ toRawJson $testData | quote }}\n  {{- end }}\n  # Test: mustToRawJson\n  {{- if .Values.testMustToRawJson }}\n  mustToRawJsonValue: {{ mustToRawJson $testData | quote }}\n  {{- end }}\n  # Test: fromJson\n  {{- if .Values.testFromJson }}\n  {{- $parsed := $testData | fromJson }}\n  fromJsonValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: fromJsonArray\n  {{- if .Values.testFromJsonArray }}\n  {{- $arr := $testData | fromJsonArray }}\n  fromJsonArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  {{- if .Values.testFromYaml }}\n  {{- $parsed := $testData | fromYaml }}\n  fromYamlValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: toYaml\n  {{- if .Values.testToYaml }}\n  toYamlValue: {{ toYaml $testData | quote }}\n  {{- end }}\n  # Test: toYamlPretty\n  {{- if .Values.testToYamlPretty }}\n  toYamlPrettyValue: {{ toYamlPretty $testData | quote }}\n  {{- end }}\n  # Test: fromYamlArray\n  {{- if .Values.testFromYamlArray }}\n  {{- $arr := $testData | fromYamlArray }}\n  fromYamlArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n  {{- if .Values.testToToml }}\n  toTomlValue: {{ toToml $testData | quote }}\n  {{- end }}\n\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-type-conversion-test\r\ndata:\r\n  # ==========================================\r\n  # String conversion functions\r\n  # ==========================================\r\n  # Test: toStrings\r\n  toStringsValue: [\"true\"]\r\n\r\n  # ==========================================\r\n  # Decimal conversion functions\r\n  # ==========================================\r\n  # Test: toDecimal\r\n\r\n  # ==========================================\r\n  # JSON conversion functions\r\n  # ==========================================\r\n  # Test: toJson\r\n  # Test: mustToJson\r\n  # Test: toPrettyJson\r\n  # Test: mustToPrettyJson\r\n  # Test: toRawJson\r\n  # Test: mustToRawJson\r\n  # Test: fromJson\r\n  # Test: fromJsonArray\r\n\r\n  # ==========================================\r\n  # YAML conversion functions\r\n  # ==========================================\r\n  # Test: fromYaml\r\n  # Test: toYaml\r\n  # Test: toYamlPretty\r\n  # Test: fromYamlArray\r\n\r\n  # ==========================================\r\n  # TOML conversion functions\r\n  # ==========================================\r\n  # Test: toToml\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-type-conversion/templates/type-conversion-test.yaml",
            "content": "\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n  toStringsValue: [\"true\"]\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  # Test: mustToJson\n  # Test: toPrettyJson\n  # Test: mustToPrettyJson\n  # Test: toRawJson\n  # Test: mustToRawJson\n  # Test: fromJson\n  # Test: fromJsonArray\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  # Test: toYaml\n  # Test: toYamlPretty\n  # Test: fromYamlArray\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n",
            "originalContent": "# Test: Type conversion errors - comprehensive testing of all type conversion functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n  {{- if .Values.testToStrings }}\n  toStringsValue: {{ toStrings $testData | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n  {{- if .Values.testToDecimal }}\n  toDecimalValue: {{ toDecimal $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  {{- if .Values.testToJson }}\n  toJsonValue: {{ toJson $testData | quote }}\n  {{- end }}\n  # Test: mustToJson\n  {{- if .Values.testMustToJson }}\n  mustToJsonValue: {{ mustToJson $testData | quote }}\n  {{- end }}\n  # Test: toPrettyJson\n  {{- if .Values.testToPrettyJson }}\n  toPrettyJsonValue: {{ toPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: mustToPrettyJson\n  {{- if .Values.testMustToPrettyJson }}\n  mustToPrettyJsonValue: {{ mustToPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: toRawJson\n  {{- if .Values.testToRawJson }}\n  toRawJsonValue: {{ toRawJson $testData | quote }}\n  {{- end }}\n  # Test: mustToRawJson\n  {{- if .Values.testMustToRawJson }}\n  mustToRawJsonValue: {{ mustToRawJson $testData | quote }}\n  {{- end }}\n  # Test: fromJson\n  {{- if .Values.testFromJson }}\n  {{- $parsed := $testData | fromJson }}\n  fromJsonValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: fromJsonArray\n  {{- if .Values.testFromJsonArray }}\n  {{- $arr := $testData | fromJsonArray }}\n  fromJsonArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  {{- if .Values.testFromYaml }}\n  {{- $parsed := $testData | fromYaml }}\n  fromYamlValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: toYaml\n  {{- if .Values.testToYaml }}\n  toYamlValue: {{ toYaml $testData | quote }}\n  {{- end }}\n  # Test: toYamlPretty\n  {{- if .Values.testToYamlPretty }}\n  toYamlPrettyValue: {{ toYamlPretty $testData | quote }}\n  {{- end }}\n  # Test: fromYamlArray\n  {{- if .Values.testFromYamlArray }}\n  {{- $arr := $testData | fromYamlArray }}\n  fromYamlArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n  {{- if .Values.testToToml }}\n  toTomlValue: {{ toToml $testData | quote }}\n  {{- end }}\n\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "resolved": true,
      "stopReason": "",
      "fixChain": []
    },
    {
      "toggle": "toStrings",
      "dataType": "listValue",
      "valuesOptions": {
        "values": [
          "testToStrings=true",
          "testDataType=listValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-type-conversion-test\r\ndata:\r\n  # ==========================================\r\n  # String conversion functions\r\n  # ==========================================\r\n  # Test: toStrings\r\n  toStringsValue: [\"1\",\"2\",\"3\"]\r\n\r\n  # ==========================================\r\n  # Decimal conversion functions\r\n  # ==========================================\r\n  # Test: toDecimal\r\n\r\n  # ==========================================\r\n  # JSON conversion functions\r\n  # ==========================================\r\n  # Test: toJson\r\n  # Test: mustToJson\r\n  # Test: toPrettyJson\r\n  # Test: mustToPrettyJson\r\n  # Test: toRawJson\r\n  # Test: mustToRawJson\r\n  # Test: fromJson\r\n  # Test: fromJsonArray\r\n\r\n  # ==========================================\r\n  # YAML conversion functions\r\n  # ==========================================\r\n  # Test: fromYaml\r\n  # Test: toYaml\r\n  # Test: toYamlPretty\r\n  # Test: fromYamlArray\r\n\r\n  # ==========================================\r\n  # TOML conversion functions\r\n  # ==========================================\r\n  # Test: toToml\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-type-conversion/templates/type-conversion-test.yaml",
            "content": "\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n  toStringsValue: [\"1\",\"2\",\"3\"]\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  # Test: mustToJson\n  # Test: toPrettyJson\n  # Test: mustToPrettyJson\n  # Test: toRawJson\n  # Test: mustToRawJson\n  # Test: fromJson\n  # Test: fromJsonArray\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  # Test: toYaml\n  # Test: toYamlPretty\n  # Test: fromYamlArray\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n",
            "originalContent": "# Test: Type conversion errors - comprehensive testing of all type conversion functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n  {{- if .Values.testToStrings }}\n  toStringsValue: {{ toStrings $testData | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n  {{- if .Values.testToDecimal }}\n  toDecimalValue: {{ toDecimal $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  {{- if .Values.testToJson }}\n  toJsonValue: {{ toJson $testData | quote }}\n  {{- end }}\n  # Test: mustToJson\n  {{- if .Values.testMustToJson }}\n  mustToJsonValue: {{ mustToJson $testData | quote }}\n  {{- end }}\n  # Test: toPrettyJson\n  {{- if .Values.testToPrettyJson }}\n  toPrettyJsonValue: {{ toPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: mustToPrettyJson\n  {{- if .Values.testMustToPrettyJson }}\n  mustToPrettyJsonValue: {{ mustToPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: toRawJson\n  {{- if .Values.testToRawJson }}\n  toRawJsonValue: {{ toRawJson $testData | quote }}\n  {{- end }}\n  # Test: mustToRawJson\n  {{- if .Values.testMustToRawJson }}\n  mustToRawJsonValue: {{ mustToRawJson $testData | quote }}\n  {{- end }}\n  # Test: fromJson\n  {{- if .Values.testFromJson }}\n  {{- $parsed := $testData | fromJson }}\n  fromJsonValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: fromJsonArray\n  {{- if .Values.testFromJsonArray }}\n  {{- $arr := $testData | fromJsonArray }}\n  fromJsonArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  {{- if .Values.testFromYaml }}\n  {{- $parsed := $testData | fromYaml }}\n  fromYamlValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: toYaml\n  {{- if .Values.testToYaml }}\n  toYamlValue: {{ toYaml $testData | quote }}\n  {{- end }}\n  # Test: toYamlPretty\n  {{- if .Values.testToYamlPretty }}\n  toYamlPrettyValue: {{ toYamlPretty $testData | quote }}\n  {{- end }}\n  # Test: fromYamlArray\n  {{- if .Values.testFromYamlArray }}\n  {{- $arr := $testData | fromYamlArray }}\n  fromYamlArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n  {{- if .Values.testToToml }}\n  toTomlValue: {{ toToml $testData | quote }}\n  {{- end }}\n\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-type-conversion-test\r\ndata:\r\n  # ==========================================\r\n  # String conversion functions\r\n  # ==========================================\r\n  # Test: toStrings\r\n  toStringsValue: [\"1\",\"2\",\"3\"]\r\n\r\n  # ==========================================\r\n  # Decimal conversion functions\r\n  # ==========================================\r\n  # Test: toDecimal\r\n\r\n  # ==========================================\r\n  # JSON conversion functions\r\n  # ==========================================\r\n  # Test: toJson\r\n  # Test: mustToJson\r\n  # Test: toPrettyJson\r\n  # Test: mustToPrettyJson\r\n  # Test: toRawJson\r\n  # Test: mustToRawJson\r\n  # Test: fromJson\r\n  # Test: fromJsonArray\r\n\r\n  # ==========================================\r\n  # YAML conversion functions\r\n  # ==========================================\r\n  # Test: fromYaml\r\n  # Test: toYaml\r\n  # Test: toYamlPretty\r\n  # Test: fromYamlArray\r\n\r\n  # ==========================================\r\n  # TOML conversion functions\r\n  # ==========================================\r\n  # Test: toToml\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-type-conversion/templates/type-conversion-test.yaml",
            "content": "\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n  toStringsValue: [\"1\",\"2\",\"3\"]\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  # Test: mustToJson\n  # Test: toPrettyJson\n  # Test: mustToPrettyJson\n  # Test: toRawJson\n  # Test: mustToRawJson\n  # Test: fromJson\n  # Test: fromJsonArray\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  # Test: toYaml\n  # Test: toYamlPretty\n  # Test: fromYamlArray\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n",
            "originalContent": "# Test: Type conversion errors - comprehensive testing of all type conversion functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n  {{- if .Values.testToStrings }}\n  toStringsValue: {{ toStrings $testData | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n  {{- if .Values.testToDecimal }}\n  toDecimalValue: {{ toDecimal $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  {{- if .Values.testToJson }}\n  toJsonValue: {{ toJson $testData | quote }}\n  {{- end }}\n  # Test: mustToJson\n  {{- if .Values.testMustToJson }}\n  mustToJsonValue: {{ mustToJson $testData | quote }}\n  {{- end }}\n  # Test: toPrettyJson\n  {{- if .Values.testToPrettyJson }}\n  toPrettyJsonValue: {{ toPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: mustToPrettyJson\n  {{- if .Values.testMustToPrettyJson }}\n  mustToPrettyJsonValue: {{ mustToPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: toRawJson\n  {{- if .Values.testToRawJson }}\n  toRawJsonValue: {{ toRawJson $testData | quote }}\n  {{- end }}\n  # Test: mustToRawJson\n  {{- if .Values.testMustToRawJson }}\n  mustToRawJsonValue: {{ mustToRawJson $testData | quote }}\n  {{- end }}\n  # Test: fromJson\n  {{- if .Values.testFromJson }}\n  {{- $parsed := $testData | fromJson }}\n  fromJsonValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: fromJsonArray\n  {{- if .Values.testFromJsonArray }}\n  {{- $arr := $testData | fromJsonArray }}\n  fromJsonArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  {{- if .Values.testFromYaml }}\n  {{- $parsed := $testData | fromYaml }}\n  fromYamlValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: toYaml\n  {{- if .Values.testToYaml }}\n  toYamlValue: {{ toYaml $testData | quote }}\n  {{- end }}\n  # Test: toYamlPretty\n  {{- if .Values.testToYamlPretty }}\n  toYamlPrettyValue: {{ toYamlPretty $testData | quote }}\n  {{- end }}\n  # Test: fromYamlArray\n  {{- if .Values.testFromYamlArray }}\n  {{- $arr := $testData | fromYamlArray }}\n  fromYamlArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n  {{- if .Values.testToToml }}\n  toTomlValue: {{ toToml $testData | quote }}\n  {{- end }}\n\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "resolved": true,
      "stopReason": "",
      "fixChain": []
    },
    {
      "toggle": "toStrings",
      "dataType": "dictValue",
      "valuesOptions": {
        "values": [
          "testToStrings=true",
          "testDataType=dictValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-type-conversion-test\r\ndata:\r\n  # ==========================================\r\n  # String conversion functions\r\n  # ==========================================\r\n  # Test: toStrings\r\n  toStringsValue: [\"map[key:value]\"]\r\n\r\n  # ==========================================\r\n  # Decimal conversion functions\r\n  # ==========================================\r\n  # Test: toDecimal\r\n\r\n  # ==========================================\r\n  # JSON conversion functions\r\n  # ==========================================\r\n  # Test: toJson\r\n  # Test: mustToJson\r\n  # Test: toPrettyJson\r\n  # Test: mustToPrettyJson\r\n  # Test: toRawJson\r\n  # Test: mustToRawJson\r\n  # Test: fromJson\r\n  # Test: fromJsonArray\r\n\r\n  # ==========================================\r\n  # YAML conversion functions\r\n  # ==========================================\r\n  # Test: fromYaml\r\n  # Test: toYaml\r\n  # Test: toYamlPretty\r\n  # Test: fromYamlArray\r\n\r\n  # ==========================================\r\n  # TOML conversion functions\r\n  # ==========================================\r\n  # Test: toToml\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-type-conversion/templates/type-conversion-test.yaml",
            "content": "\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n  toStringsValue: [\"map[key:value]\"]\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  # Test: mustToJson\n  # Test: toPrettyJson\n  # Test: mustToPrettyJson\n  # Test: toRawJson\n  # Test: mustToRawJson\n  # Test: fromJson\n  # Test: fromJsonArray\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  # Test: toYaml\n  # Test: toYamlPretty\n  # Test: fromYamlArray\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n",
            "originalContent": "# Test: Type conversion errors - comprehensive testing of all type conversion functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n  {{- if .Values.testToStrings }}\n  toStringsValue: {{ toStrings $testData | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n  {{- if .Values.testToDecimal }}\n  toDecimalValue: {{ toDecimal $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  {{- if .Values.testToJson }}\n  toJsonValue: {{ toJson $testData | quote }}\n  {{- end }}\n  # Test: mustToJson\n  {{- if .Values.testMustToJson }}\n  mustToJsonValue: {{ mustToJson $testData | quote }}\n  {{- end }}\n  # Test: toPrettyJson\n  {{- if .Values.testToPrettyJson }}\n  toPrettyJsonValue: {{ toPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: mustToPrettyJson\n  {{- if .Values.testMustToPrettyJson }}\n  mustToPrettyJsonValue: {{ mustToPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: toRawJson\n  {{- if .Values.testToRawJson }}\n  toRawJsonValue: {{ toRawJson $testData | quote }}\n  {{- end }}\n  # Test: mustToRawJson\n  {{- if .Values.testMustToRawJson }}\n  mustToRawJsonValue: {{ mustToRawJson $testData | quote }}\n  {{- end }}\n  # Test: fromJson\n  {{- if .Values.testFromJson }}\n  {{- $parsed := $testData | fromJson }}\n  fromJsonValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: fromJsonArray\n  {{- if .Values.testFromJsonArray }}\n  {{- $arr := $testData | fromJsonArray }}\n  fromJsonArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  {{- if .Values.testFromYaml }}\n  {{- $parsed := $testData | fromYaml }}\n  fromYamlValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: toYaml\n  {{- if .Values.testToYaml }}\n  toYamlValue: {{ toYaml $testData | quote }}\n  {{- end }}\n  # Test: toYamlPretty\n  {{- if .Values.testToYamlPretty }}\n  toYamlPrettyValue: {{ toYamlPretty $testData | quote }}\n  {{- end }}\n  # Test: fromYamlArray\n  {{- if .Values.testFromYamlArray }}\n  {{- $arr := $testData | fromYamlArray }}\n  fromYamlArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n  {{- if .Values.testToToml }}\n  toTomlValue: {{ toToml $testData | quote }}\n  {{- end }}\n\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-type-conversion-test\r\ndata:\r\n  # ==========================================\r\n  # String conversion functions\r\n  # ==========================================\r\n  # Test: toStrings\r\n  toStringsValue: [\"map[key:value]\"]\r\n\r\n  # ==========================================\r\n  # Decimal conversion functions\r\n  # ==========================================\r\n  # Test: toDecimal\r\n\r\n  # ==========================================\r\n  # JSON conversion functions\r\n  # ==========================================\r\n  # Test: toJson\r\n  # Test: mustToJson\r\n  # Test: toPrettyJson\r\n  # Test: mustToPrettyJson\r\n  # Test: toRawJson\r\n  # Test: mustToRawJson\r\n  # Test: fromJson\r\n  # Test: fromJsonArray\r\n\r\n  # ==========================================\r\n  # YAML conversion functions\r\n  # ==========================================\r\n  # Test: fromYaml\r\n  # Test: toYaml\r\n  # Test: toYamlPretty\r\n  # Test: fromYamlArray\r\n\r\n  # ==========================================\r\n  # TOML conversion functions\r\n  # ==========================================\r\n  # Test: toToml\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-type-conversion/templates/type-conversion-test.yaml",
            "content": "\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n  toStringsValue: [\"map[key:value]\"]\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  # Test: mustToJson\n  # Test: toPrettyJson\n  # Test: mustToPrettyJson\n  # Test: toRawJson\n  # Test: mustToRawJson\n  # Test: fromJson\n  # Test: fromJsonArray\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  # Test: toYaml\n  # Test: toYamlPretty\n  # Test: fromYamlArray\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n",
            "originalContent": "# Test: Type conversion errors - comprehensive testing of all type conversion functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n  {{- if .Values.testToStrings }}\n  toStringsValue: {{ toStrings $testData | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n  {{- if .Values.testToDecimal }}\n  toDecimalValue: {{ toDecimal $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  {{- if .Values.testToJson }}\n  toJsonValue: {{ toJson $testData | quote }}\n  {{- end }}\n  # Test: mustToJson\n  {{- if .Values.testMustToJson }}\n  mustToJsonValue: {{ mustToJson $testData | quote }}\n  {{- end }}\n  # Test: toPrettyJson\n  {{- if .Values.testToPrettyJson }}\n  toPrettyJsonValue: {{ toPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: mustToPrettyJson\n  {{- if .Values.testMustToPrettyJson }}\n  mustToPrettyJsonValue: {{ mustToPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: toRawJson\n  {{- if .Values.testToRawJson }}\n  toRawJsonValue: {{ toRawJson $testData | quote }}\n  {{- end }}\n  # Test: mustToRawJson\n  {{- if .Values.testMustToRawJson }}\n  mustToRawJsonValue: {{ mustToRawJson $testData | quote }}\n  {{- end }}\n  # Test: fromJson\n  {{- if .Values.testFromJson }}\n  {{- $parsed := $testData | fromJson }}\n  fromJsonValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: fromJsonArray\n  {{- if .Values.testFromJsonArray }}\n  {{- $arr := $testData | fromJsonArray }}\n  fromJsonArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  {{- if .Values.testFromYaml }}\n  {{- $parsed := $testData | fromYaml }}\n  fromYamlValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: toYaml\n  {{- if .Values.testToYaml }}\n  toYamlValue: {{ toYaml $testData | quote }}\n  {{- end }}\n  # Test: toYamlPretty\n  {{- if .Values.testToYamlPretty }}\n  toYamlPrettyValue: {{ toYamlPretty $testData | quote }}\n  {{- end }}\n  # Test: fromYamlArray\n  {{- if .Values.testFromYamlArray }}\n  {{- $arr := $testData | fromYamlArray }}\n  fromYamlArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n  {{- if .Values.testToToml }}\n  toTomlValue: {{ toToml $testData | quote }}\n  {{- end }}\n\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "resolved": true,
      "stopReason": "",
      "fixChain": []
    },
    {
      "toggle": "toDecimal",
      "dataType": "nilValue",
      "valuesOptions": {
        "values": [
          "testToDecimal=true",
          "testDataType=nilValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-type-conversion-test\r\ndata:\r\n  # ==========================================\r\n  # String conversion functions\r\n  # ==========================================\r\n  # Test: toStrings\r\n\r\n  # ==========================================\r\n  # Decimal conversion functions\r\n  # ==========================================\r\n  # Test: toDecimal\r\n  toDecimalValue: \"0\"\r\n\r\n  # ==========================================\r\n  # JSON conversion functions\r\n  # ==========================================\r\n  # Test: toJson\r\n  # Test: mustToJson\r\n  # Test: toPrettyJson\r\n  # Test: mustToPrettyJson\r\n  # Test: toRawJson\r\n  # Test: mustToRawJson\r\n  # Test: fromJson\r\n  # Test: fromJsonArray\r\n\r\n  # ==========================================\r\n  # YAML conversion functions\r\n  # ==========================================\r\n  # Test: fromYaml\r\n  # Test: toYaml\r\n  # Test: toYamlPretty\r\n  # Test: fromYamlArray\r\n\r\n  # ==========================================\r\n  # TOML conversion functions\r\n  # ==========================================\r\n  # Test: toToml\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-type-conversion/templates/type-conversion-test.yaml",
            "content": "\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n  toDecimalValue: \"0\"\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  # Test: mustToJson\n  # Test: toPrettyJson\n  # Test: mustToPrettyJson\n  # Test: toRawJson\n  # Test: mustToRawJson\n  # Test: fromJson\n  # Test: fromJsonArray\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  # Test: toYaml\n  # Test: toYamlPretty\n  # Test: fromYamlArray\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n",
            "originalContent": "# Test: Type conversion errors - comprehensive testing of all type conversion functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n  {{- if .Values.testToStrings }}\n  toStringsValue: {{ toStrings $testData | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n  {{- if .Values.testToDecimal }}\n  toDecimalValue: {{ toDecimal $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  {{- if .Values.testToJson }}\n  toJsonValue: {{ toJson $testData | quote }}\n  {{- end }}\n  # Test: mustToJson\n  {{- if .Values.testMustToJson }}\n  mustToJsonValue: {{ mustToJson $testData | quote }}\n  {{- end }}\n  # Test: toPrettyJson\n  {{- if .Values.testToPrettyJson }}\n  toPrettyJsonValue: {{ toPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: mustToPrettyJson\n  {{- if .Values.testMustToPrettyJson }}\n  mustToPrettyJsonValue: {{ mustToPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: toRawJson\n  {{- if .Values.testToRawJson }}\n  toRawJsonValue: {{ toRawJson $testData | quote }}\n  {{- end }}\n  # Test: mustToRawJson\n  {{- if .Values.testMustToRawJson }}\n  mustToRawJsonValue: {{ mustToRawJson $testData | quote }}\n  {{- end }}\n  # Test: fromJson\n  {{- if .Values.testFromJson }}\n  {{- $parsed := $testData | fromJson }}\n  fromJsonValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: fromJsonArray\n  {{- if .Values.testFromJsonArray }}\n  {{- $arr := $testData | fromJsonArray }}\n  fromJsonArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  {{- if .Values.testFromYaml }}\n  {{- $parsed := $testData | fromYaml }}\n  fromYamlValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: toYaml\n  {{- if .Values.testToYaml }}\n  toYamlValue: {{ toYaml $testData | quote }}\n  {{- end }}\n  # Test: toYamlPretty\n  {{- if .Values.testToYamlPretty }}\n  toYamlPrettyValue: {{ toYamlPretty $testData | quote }}\n  {{- end }}\n  # Test: fromYamlArray\n  {{- if .Values.testFromYamlArray }}\n  {{- $arr := $testData | fromYamlArray }}\n  fromYamlArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n  {{- if .Values.testToToml }}\n  toTomlValue: {{ toToml $testData | quote }}\n  {{- end }}\n\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-type-conversion-test\r\ndata:\r\n  # ==========================================\r\n  # String conversion functions\r\n  # ==========================================\r\n  # Test: toStrings\r\n\r\n  # ==========================================\r\n  # Decimal conversion functions\r\n  # ==========================================\r\n  # Test: toDecimal\r\n  toDecimalValue: \"0\"\r\n\r\n  # ==========================================\r\n  # JSON conversion functions\r\n  # ==========================================\r\n  # Test: toJson\r\n  # Test: mustToJson\r\n  # Test: toPrettyJson\r\n  # Test: mustToPrettyJson\r\n  # Test: toRawJson\r\n  # Test: mustToRawJson\r\n  # Test: fromJson\r\n  # Test: fromJsonArray\r\n\r\n  # ==========================================\r\n  # YAML conversion functions\r\n  # ==========================================\r\n  # Test: fromYaml\r\n  # Test: toYaml\r\n  # Test: toYamlPretty\r\n  # Test: fromYamlArray\r\n\r\n  # ==========================================\r\n  # TOML conversion functions\r\n  # ==========================================\r\n  # Test: toToml\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-type-conversion/templates/type-conversion-test.yaml",
            "content": "\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n  toDecimalValue: \"0\"\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  # Test: mustToJson\n  # Test: toPrettyJson\n  # Test: mustToPrettyJson\n  # Test: toRawJson\n  # Test: mustToRawJson\n  # Test: fromJson\n  # Test: fromJsonArray\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  # Test: toYaml\n  # Test: toYamlPretty\n  # Test: fromYamlArray\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n",
            "originalContent": "# Test: Type conversion errors - comprehensive testing of all type conversion functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n  {{- if .Values.testToStrings }}\n  toStringsValue: {{ toStrings $testData | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n  {{- if .Values.testToDecimal }}\n  toDecimalValue: {{ toDecimal $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  {{- if .Values.testToJson }}\n  toJsonValue: {{ toJson $testData | quote }}\n  {{- end }}\n  # Test: mustToJson\n  {{- if .Values.testMustToJson }}\n  mustToJsonValue: {{ mustToJson $testData | quote }}\n  {{- end }}\n  # Test: toPrettyJson\n  {{- if .Values.testToPrettyJson }}\n  toPrettyJsonValue: {{ toPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: mustToPrettyJson\n  {{- if .Values.testMustToPrettyJson }}\n  mustToPrettyJsonValue: {{ mustToPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: toRawJson\n  {{- if .Values.testToRawJson }}\n  toRawJsonValue: {{ toRawJson $testData | quote }}\n  {{- end }}\n  # Test: mustToRawJson\n  {{- if .Values.testMustToRawJson }}\n  mustToRawJsonValue: {{ mustToRawJson $testData | quote }}\n  {{- end }}\n  # Test: fromJson\n  {{- if .Values.testFromJson }}\n  {{- $parsed := $testData | fromJson }}\n  fromJsonValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: fromJsonArray\n  {{- if .Values.testFromJsonArray }}\n  {{- $arr := $testData | fromJsonArray }}\n  fromJsonArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  {{- if .Values.testFromYaml }}\n  {{- $parsed := $testData | fromYaml }}\n  fromYamlValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: toYaml\n  {{- if .Values.testToYaml }}\n  toYamlValue: {{ toYaml $testData | quote }}\n  {{- end }}\n  # Test: toYamlPretty\n  {{- if .Values.testToYamlPretty }}\n  toYamlPrettyValue: {{ toYamlPretty $testData | quote }}\n  {{- end }}\n  # Test: fromYamlArray\n  {{- if .Values.testFromYamlArray }}\n  {{- $arr := $testData | fromYamlArray }}\n  fromYamlArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n  {{- if .Values.testToToml }}\n  toTomlValue: {{ toToml $testData | quote }}\n  {{- end }}\n\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "resolved": true,
      "stopReason": "",
      "fixChain": []
    },
    {
      "toggle": "toDecimal",
      "dataType": "stringValue",
      "valuesOptions": {
        "values": [
          "testToDecimal=true",
          "testDataType=stringValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-type-conversion-test\r\ndata:\r\n  # ==========================================\r\n  # String conversion functions\r\n  # ==========================================\r\n  # Test: toStrings\r\n\r\n  # ==========================================\r\n  # Decimal conversion functions\r\n  # ==========================================\r\n  # Test: toDecimal\r\n  toDecimalValue: \"0\"\r\n\r\n  # ==========================================\r\n  # JSON conversion functions\r\n  # ==========================================\r\n  # Test: toJson\r\n  # Test: mustToJson\r\n  # Test: toPrettyJson\r\n  # Test: mustToPrettyJson\r\n  # Test: toRawJson\r\n  # Test: mustToRawJson\r\n  # Test: fromJson\r\n  # Test: fromJsonArray\r\n\r\n  # ==========================================\r\n  # YAML conversion functions\r\n  # ==========================================\r\n  # Test: fromYaml\r\n  # Test: toYaml\r\n  # Test: toYamlPretty\r\n  # Test: fromYamlArray\r\n\r\n  # ==========================================\r\n  # TOML conversion functions\r\n  # ==========================================\r\n  # Test: toToml\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-type-conversion/templates/type-conversion-test.yaml",
            "content": "\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n  toDecimalValue: \"0\"\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  # Test: mustToJson\n  # Test: toPrettyJson\n  # Test: mustToPrettyJson\n  # Test: toRawJson\n  # Test: mustToRawJson\n  # Test: fromJson\n  # Test: fromJsonArray\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  # Test: toYaml\n  # Test: toYamlPretty\n  # Test: fromYamlArray\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n",
            "originalContent": "# Test: Type conversion errors - comprehensive testing of all type conversion functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n  {{- if .Values.testToStrings }}\n  toStringsValue: {{ toStrings $testData | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n  {{- if .Values.testToDecimal }}\n  toDecimalValue: {{ toDecimal $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  {{- if .Values.testToJson }}\n  toJsonValue: {{ toJson $testData | quote }}\n  {{- end }}\n  # Test: mustToJson\n  {{- if .Values.testMustToJson }}\n  mustToJsonValue: {{ mustToJson $testData | quote }}\n  {{- end }}\n  # Test: toPrettyJson\n  {{- if .Values.testToPrettyJson }}\n  toPrettyJsonValue: {{ toPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: mustToPrettyJson\n  {{- if .Values.testMustToPrettyJson }}\n  mustToPrettyJsonValue: {{ mustToPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: toRawJson\n  {{- if .Values.testToRawJson }}\n  toRawJsonValue: {{ toRawJson $testData | quote }}\n  {{- end }}\n  # Test: mustToRawJson\n  {{- if .Values.testMustToRawJson }}\n  mustToRawJsonValue: {{ mustToRawJson $testData | quote }}\n  {{- end }}\n  # Test: fromJson\n  {{- if .Values.testFromJson }}\n  {{- $parsed := $testData | fromJson }}\n  fromJsonValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: fromJsonArray\n  {{- if .Values.testFromJsonArray }}\n  {{- $arr := $testData | fromJsonArray }}\n  fromJsonArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  {{- if .Values.testFromYaml }}\n  {{- $parsed := $testData | fromYaml }}\n  fromYamlValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: toYaml\n  {{- if .Values.testToYaml }}\n  toYamlValue: {{ toYaml $testData | quote }}\n  {{- end }}\n  # Test: toYamlPretty\n  {{- if .Values.testToYamlPretty }}\n  toYamlPrettyValue: {{ toYamlPretty $testData | quote }}\n  {{- end }}\n  # Test: fromYamlArray\n  {{- if .Values.testFromYamlArray }}\n  {{- $arr := $testData | fromYamlArray }}\n  fromYamlArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n  {{- if .Values.testToToml }}\n  toTomlValue: {{ toToml $testData | quote }}\n  {{- end }}\n\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-type-conversion-test\r\ndata:\r\n  # ==========================================\r\n  # String conversion functions\r\n  # ==========================================\r\n  # Test: toStrings\r\n\r\n  # ==========================================\r\n  # Decimal conversion functions\r\n  # ==========================================\r\n  # Test: toDecimal\r\n  toDecimalValue: \"0\"\r\n\r\n  # ==========================================\r\n  # JSON conversion functions\r\n  # ==========================================\r\n  # Test: toJson\r\n  # Test: mustToJson\r\n  # Test: toPrettyJson\r\n  # Test: mustToPrettyJson\r\n  # Test: toRawJson\r\n  # Test: mustToRawJson\r\n  # Test: fromJson\r\n  # Test: fromJsonArray\r\n\r\n  # ==========================================\r\n  # YAML conversion functions\r\n  # ==========================================\r\n  # Test: fromYaml\r\n  # Test: toYaml\r\n  # Test: toYamlPretty\r\n  # Test: fromYamlArray\r\n\r\n  # ==========================================\r\n  # TOML conversion functions\r\n  # ==========================================\r\n  # Test: toToml\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-type-conversion/templates/type-conversion-test.yaml",
            "content": "\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n  toDecimalValue: \"0\"\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  # Test: mustToJson\n  # Test: toPrettyJson\n  # Test: mustToPrettyJson\n  # Test: toRawJson\n  # Test: mustToRawJson\n  # Test: fromJson\n  # Test: fromJsonArray\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  # Test: toYaml\n  # Test: toYamlPretty\n  # Test: fromYamlArray\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n",
            "originalContent": "# Test: Type conversion errors - comprehensive testing of all type conversion functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n  {{- if .Values.testToStrings }}\n  toStringsValue: {{ toStrings $testData | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n  {{- if .Values.testToDecimal }}\n  toDecimalValue: {{ toDecimal $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  {{- if .Values.testToJson }}\n  toJsonValue: {{ toJson $testData | quote }}\n  {{- end }}\n  # Test: mustToJson\n  {{- if .Values.testMustToJson }}\n  mustToJsonValue: {{ mustToJson $testData | quote }}\n  {{- end }}\n  # Test: toPrettyJson\n  {{- if .Values.testToPrettyJson }}\n  toPrettyJsonValue: {{ toPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: mustToPrettyJson\n  {{- if .Values.testMustToPrettyJson }}\n  mustToPrettyJsonValue: {{ mustToPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: toRawJson\n  {{- if .Values.testToRawJson }}\n  toRawJsonValue: {{ toRawJson $testData | quote }}\n  {{- end }}\n  # Test: mustToRawJson\n  {{- if .Values.testMustToRawJson }}\n  mustToRawJsonValue: {{ mustToRawJson $testData | quote }}\n  {{- end }}\n  # Test: fromJson\n  {{- if .Values.testFromJson }}\n  {{- $parsed := $testData | fromJson }}\n  fromJsonValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: fromJsonArray\n  {{- if .Values.testFromJsonArray }}\n  {{- $arr := $testData | fromJsonArray }}\n  fromJsonArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  {{- if .Values.testFromYaml }}\n  {{- $parsed := $testData | fromYaml }}\n  fromYamlValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: toYaml\n  {{- if .Values.testToYaml }}\n  toYamlValue: {{ toYaml $testData | quote }}\n  {{- end }}\n  # Test: toYamlPretty\n  {{- if .Values.testToYamlPretty }}\n  toYamlPrettyValue: {{ toYamlPretty $testData | quote }}\n  {{- end }}\n  # Test: fromYamlArray\n  {{- if .Values.testFromYamlArray }}\n  {{- $arr := $testData | fromYamlArray }}\n  fromYamlArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n  {{- if .Values.testToToml }}\n  toTomlValue: {{ toToml $testData | quote }}\n  {{- end }}\n\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "resolved": true,
      "stopReason": "",
      "fixChain": []
    },
    {
      "toggle": "toDecimal",
      "dataType": "numberValue",
      "valuesOptions": {
        "values": [
          "testToDecimal=true",
          "testDataType=numberValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-type-conversion-test\r\ndata:\r\n  # ==========================================\r\n  # String conversion functions\r\n  # ==========================================\r\n  # Test: toStrings\r\n\r\n  # ==========================================\r\n  # Decimal conversion functions\r\n  # ==========================================\r\n  # Test: toDecimal\r\n  toDecimalValue: \"34\"\r\n\r\n  # ==========================================\r\n  # JSON conversion functions\r\n  # ==========================================\r\n  # Test: toJson\r\n  # Test: mustToJson\r\n  # Test: toPrettyJson\r\n  # Test: mustToPrettyJson\r\n  # Test: toRawJson\r\n  # Test: mustToRawJson\r\n  # Test: fromJson\r\n  # Test: fromJsonArray\r\n\r\n  # ==========================================\r\n  # YAML conversion functions\r\n  # ==========================================\r\n  # Test: fromYaml\r\n  # Test: toYaml\r\n  # Test: toYamlPretty\r\n  # Test: fromYamlArray\r\n\r\n  # ==========================================\r\n  # TOML conversion functions\r\n  # ==========================================\r\n  # Test: toToml\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-type-conversion/templates/type-conversion-test.yaml",
            "content": "\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n  toDecimalValue: \"34\"\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  # Test: mustToJson\n  # Test: toPrettyJson\n  # Test: mustToPrettyJson\n  # Test: toRawJson\n  # Test: mustToRawJson\n  # Test: fromJson\n  # Test: fromJsonArray\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  # Test: toYaml\n  # Test: toYamlPretty\n  # Test: fromYamlArray\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n",
            "originalContent": "# Test: Type conversion errors - comprehensive testing of all type conversion functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n  {{- if .Values.testToStrings }}\n  toStringsValue: {{ toStrings $testData | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n  {{- if .Values.testToDecimal }}\n  toDecimalValue: {{ toDecimal $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  {{- if .Values.testToJson }}\n  toJsonValue: {{ toJson $testData | quote }}\n  {{- end }}\n  # Test: mustToJson\n  {{- if .Values.testMustToJson }}\n  mustToJsonValue: {{ mustToJson $testData | quote }}\n  {{- end }}\n  # Test: toPrettyJson\n  {{- if .Values.testToPrettyJson }}\n  toPrettyJsonValue: {{ toPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: mustToPrettyJson\n  {{- if .Values.testMustToPrettyJson }}\n  mustToPrettyJsonValue: {{ mustToPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: toRawJson\n  {{- if .Values.testToRawJson }}\n  toRawJsonValue: {{ toRawJson $testData | quote }}\n  {{- end }}\n  # Test: mustToRawJson\n  {{- if .Values.testMustToRawJson }}\n  mustToRawJsonValue: {{ mustToRawJson $testData | quote }}\n  {{- end }}\n  # Test: fromJson\n  {{- if .Values.testFromJson }}\n  {{- $parsed := $testData | fromJson }}\n  fromJsonValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: fromJsonArray\n  {{- if .Values.testFromJsonArray }}\n  {{- $arr := $testData | fromJsonArray }}\n  fromJsonArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  {{- if .Values.testFromYaml }}\n  {{- $parsed := $testData | fromYaml }}\n  fromYamlValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: toYaml\n  {{- if .Values.testToYaml }}\n  toYamlValue: {{ toYaml $testData | quote }}\n  {{- end }}\n  # Test: toYamlPretty\n  {{- if .Values.testToYamlPretty }}\n  toYamlPrettyValue: {{ toYamlPretty $testData | quote }}\n  {{- end }}\n  # Test: fromYamlArray\n  {{- if .Values.testFromYamlArray }}\n  {{- $arr := $testData | fromYamlArray }}\n  fromYamlArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n  {{- if .Values.testToToml }}\n  toTomlValue: {{ toToml $testData | quote }}\n  {{- end }}\n\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-type-conversion-test\r\ndata:\r\n  # ==========================================\r\n  # String conversion functions\r\n  # ==========================================\r\n  # Test: toStrings\r\n\r\n  # ==========================================\r\n  # Decimal conversion functions\r\n  # ==========================================\r\n  # Test: toDecimal\r\n  toDecimalValue: \"34\"\r\n\r\n  # ==========================================\r\n  # JSON conversion functions\r\n  # ==========================================\r\n  # Test: toJson\r\n  # Test: mustToJson\r\n  # Test: toPrettyJson\r\n  # Test: mustToPrettyJson\r\n  # Test: toRawJson\r\n  # Test: mustToRawJson\r\n  # Test: fromJson\r\n  # Test: fromJsonArray\r\n\r\n  # ==========================================\r\n  # YAML conversion functions\r\n  # ==========================================\r\n  # Test: fromYaml\r\n  # Test: toYaml\r\n  # Test: toYamlPretty\r\n  # Test: fromYamlArray\r\n\r\n  # ==========================================\r\n  # TOML conversion functions\r\n  # ==========================================\r\n  # Test: toToml\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-type-conversion/templates/type-conversion-test.yaml",
            "content": "\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n  toDecimalValue: \"34\"\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  # Test: mustToJson\n  # Test: toPrettyJson\n  # Test: mustToPrettyJson\n  # Test: toRawJson\n  # Test: mustToRawJson\n  # Test: fromJson\n  # Test: fromJsonArray\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  # Test: toYaml\n  # Test: toYamlPretty\n  # Test: fromYamlArray\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n",
            "originalContent": "# Test: Type conversion errors - comprehensive testing of all type conversion functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n  {{- if .Values.testToStrings }}\n  toStringsValue: {{ toStrings $testData | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n  {{- if .Values.testToDecimal }}\n  toDecimalValue: {{ toDecimal $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  {{- if .Values.testToJson }}\n  toJsonValue: {{ toJson $testData | quote }}\n  {{- end }}\n  # Test: mustToJson\n  {{- if .Values.testMustToJson }}\n  mustToJsonValue: {{ mustToJson $testData | quote }}\n  {{- end }}\n  # Test: toPrettyJson\n  {{- if .Values.testToPrettyJson }}\n  toPrettyJsonValue: {{ toPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: mustToPrettyJson\n  {{- if .Values.testMustToPrettyJson }}\n  mustToPrettyJsonValue: {{ mustToPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: toRawJson\n  {{- if .Values.testToRawJson }}\n  toRawJsonValue: {{ toRawJson $testData | quote }}\n  {{- end }}\n  # Test: mustToRawJson\n  {{- if .Values.testMustToRawJson }}\n  mustToRawJsonValue: {{ mustToRawJson $testData | quote }}\n  {{- end }}\n  # Test: fromJson\n  {{- if .Values.testFromJson }}\n  {{- $parsed := $testData | fromJson }}\n  fromJsonValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: fromJsonArray\n  {{- if .Values.testFromJsonArray }}\n  {{- $arr := $testData | fromJsonArray }}\n  fromJsonArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  {{- if .Values.testFromYaml }}\n  {{- $parsed := $testData | fromYaml }}\n  fromYamlValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: toYaml\n  {{- if .Values.testToYaml }}\n  toYamlValue: {{ toYaml $testData | quote }}\n  {{- end }}\n  # Test: toYamlPretty\n  {{- if .Values.testToYamlPretty }}\n  toYamlPrettyValue: {{ toYamlPretty $testData | quote }}\n  {{- end }}\n  # Test: fromYamlArray\n  {{- if .Values.testFromYamlArray }}\n  {{- $arr := $testData | fromYamlArray }}\n  fromYamlArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n  {{- if .Values.testToToml }}\n  toTomlValue: {{ toToml $testData | quote }}\n  {{- end }}\n\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "resolved": true,
      "stopReason": "",
      "fixChain": []
    },
    {
      "toggle": "toDecimal",
      "dataType": "boolValue",
      "valuesOptions": {
        "values": [
          "testToDecimal=true",
          "testDataType=boolValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-type-conversion-test\r\ndata:\r\n  # ==========================================\r\n  # String conversion functions\r\n  # ==========================================\r\n  # Test: toStrings\r\n\r\n  # ==========================================\r\n  # Decimal conversion functions\r\n  # ==========================================\r\n  # Test: toDecimal\r\n  toDecimalValue: \"0\"\r\n\r\n  # ==========================================\r\n  # JSON conversion functions\r\n  # ==========================================\r\n  # Test: toJson\r\n  # Test: mustToJson\r\n  # Test: toPrettyJson\r\n  # Test: mustToPrettyJson\r\n  # Test: toRawJson\r\n  # Test: mustToRawJson\r\n  # Test: fromJson\r\n  # Test: fromJsonArray\r\n\r\n  # ==========================================\r\n  # YAML conversion functions\r\n  # ==========================================\r\n  # Test: fromYaml\r\n  # Test: toYaml\r\n  # Test: toYamlPretty\r\n  # Test: fromYamlArray\r\n\r\n  # ==========================================\r\n  # TOML conversion functions\r\n  # ==========================================\r\n  # Test: toToml\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-type-conversion/templates/type-conversion-test.yaml",
            "content": "\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n  toDecimalValue: \"0\"\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  # Test: mustToJson\n  # Test: toPrettyJson\n  # Test: mustToPrettyJson\n  # Test: toRawJson\n  # Test: mustToRawJson\n  # Test: fromJson\n  # Test: fromJsonArray\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  # Test: toYaml\n  # Test: toYamlPretty\n  # Test: fromYamlArray\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n",
            "originalContent": "# Test: Type conversion errors - comprehensive testing of all type conversion functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n  {{- if .Values.testToStrings }}\n  toStringsValue: {{ toStrings $testData | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n  {{- if .Values.testToDecimal }}\n  toDecimalValue: {{ toDecimal $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  {{- if .Values.testToJson }}\n  toJsonValue: {{ toJson $testData | quote }}\n  {{- end }}\n  # Test: mustToJson\n  {{- if .Values.testMustToJson }}\n  mustToJsonValue: {{ mustToJson $testData | quote }}\n  {{- end }}\n  # Test: toPrettyJson\n  {{- if .Values.testToPrettyJson }}\n  toPrettyJsonValue: {{ toPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: mustToPrettyJson\n  {{- if .Values.testMustToPrettyJson }}\n  mustToPrettyJsonValue: {{ mustToPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: toRawJson\n  {{- if .Values.testToRawJson }}\n  toRawJsonValue: {{ toRawJson $testData | quote }}\n  {{- end }}\n  # Test: mustToRawJson\n  {{- if .Values.testMustToRawJson }}\n  mustToRawJsonValue: {{ mustToRawJson $testData | quote }}\n  {{- end }}\n  # Test: fromJson\n  {{- if .Values.testFromJson }}\n  {{- $parsed := $testData | fromJson }}\n  fromJsonValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: fromJsonArray\n  {{- if .Values.testFromJsonArray }}\n  {{- $arr := $testData | fromJsonArray }}\n  fromJsonArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  {{- if .Values.testFromYaml }}\n  {{- $parsed := $testData | fromYaml }}\n  fromYamlValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: toYaml\n  {{- if .Values.testToYaml }}\n  toYamlValue: {{ toYaml $testData | quote }}\n  {{- end }}\n  # Test: toYamlPretty\n  {{- if .Values.testToYamlPretty }}\n  toYamlPrettyValue: {{ toYamlPretty $testData | quote }}\n  {{- end }}\n  # Test: fromYamlArray\n  {{- if .Values.testFromYamlArray }}\n  {{- $arr := $testData | fromYamlArray }}\n  fromYamlArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n  {{- if .Values.testToToml }}\n  toTomlValue: {{ toToml $testData | quote }}\n  {{- end }}\n\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-type-conversion-test\r\ndata:\r\n  # ==========================================\r\n  # String conversion functions\r\n  # ==========================================\r\n  # Test: toStrings\r\n\r\n  # ==========================================\r\n  # Decimal conversion functions\r\n  # ==========================================\r\n  # Test: toDecimal\r\n  toDecimalValue: \"0\"\r\n\r\n  # ==========================================\r\n  # JSON conversion functions\r\n  # ==========================================\r\n  # Test: toJson\r\n  # Test: mustToJson\r\n  # Test: toPrettyJson\r\n  # Test: mustToPrettyJson\r\n  # Test: toRawJson\r\n  # Test: mustToRawJson\r\n  # Test: fromJson\r\n  # Test: fromJsonArray\r\n\r\n  # ==========================================\r\n  # YAML conversion functions\r\n  # ==========================================\r\n  # Test: fromYaml\r\n  # Test: toYaml\r\n  # Test: toYamlPretty\r\n  # Test: fromYamlArray\r\n\r\n  # ==========================================\r\n  # TOML conversion functions\r\n  # ==========================================\r\n  # Test: toToml\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-type-conversion/templates/type-conversion-test.yaml",
            "content": "\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n  toDecimalValue: \"0\"\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  # Test: mustToJson\n  # Test: toPrettyJson\n  # Test: mustToPrettyJson\n  # Test: toRawJson\n  # Test: mustToRawJson\n  # Test: fromJson\n  # Test: fromJsonArray\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  # Test: toYaml\n  # Test: toYamlPretty\n  # Test: fromYamlArray\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n",
            "originalContent": "# Test: Type conversion errors - comprehensive testing of all type conversion functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n  {{- if .Values.testToStrings }}\n  toStringsValue: {{ toStrings $testData | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n  {{- if .Values.testToDecimal }}\n  toDecimalValue: {{ toDecimal $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  {{- if .Values.testToJson }}\n  toJsonValue: {{ toJson $testData | quote }}\n  {{- end }}\n  # Test: mustToJson\n  {{- if .Values.testMustToJson }}\n  mustToJsonValue: {{ mustToJson $testData | quote }}\n  {{- end }}\n  # Test: toPrettyJson\n  {{- if .Values.testToPrettyJson }}\n  toPrettyJsonValue: {{ toPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: mustToPrettyJson\n  {{- if .Values.testMustToPrettyJson }}\n  mustToPrettyJsonValue: {{ mustToPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: toRawJson\n  {{- if .Values.testToRawJson }}\n  toRawJsonValue: {{ toRawJson $testData | quote }}\n  {{- end }}\n  # Test: mustToRawJson\n  {{- if .Values.testMustToRawJson }}\n  mustToRawJsonValue: {{ mustToRawJson $testData | quote }}\n  {{- end }}\n  # Test: fromJson\n  {{- if .Values.testFromJson }}\n  {{- $parsed := $testData | fromJson }}\n  fromJsonValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: fromJsonArray\n  {{- if .Values.testFromJsonArray }}\n  {{- $arr := $testData | fromJsonArray }}\n  fromJsonArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  {{- if .Values.testFromYaml }}\n  {{- $parsed := $testData | fromYaml }}\n  fromYamlValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: toYaml\n  {{- if .Values.testToYaml }}\n  toYamlValue: {{ toYaml $testData | quote }}\n  {{- end }}\n  # Test: toYamlPretty\n  {{- if .Values.testToYamlPretty }}\n  toYamlPrettyValue: {{ toYamlPretty $testData | quote }}\n  {{- end }}\n  # Test: fromYamlArray\n  {{- if .Values.testFromYamlArray }}\n  {{- $arr := $testData | fromYamlArray }}\n  fromYamlArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n  {{- if .Values.testToToml }}\n  toTomlValue: {{ toToml $testData | quote }}\n  {{- end }}\n\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "resolved": true,
      "stopReason": "",
      "fixChain": []
    },
    {
      "toggle": "toDecimal",
      "dataType": "listValue",
      "valuesOptions": {
        "values": [
          "testToDecimal=true",
          "testDataType=listValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-type-conversion-test\r\ndata:\r\n  # ==========================================\r\n  # String conversion functions\r\n  # ==========================================\r\n  # Test: toStrings\r\n\r\n  # ==========================================\r\n  # Decimal conversion functions\r\n  # ==========================================\r\n  # Test: toDecimal\r\n  toDecimalValue: \"0\"\r\n\r\n  # ==========================================\r\n  # JSON conversion functions\r\n  # ==========================================\r\n  # Test: toJson\r\n  # Test: mustToJson\r\n  # Test: toPrettyJson\r\n  # Test: mustToPrettyJson\r\n  # Test: toRawJson\r\n  # Test: mustToRawJson\r\n  # Test: fromJson\r\n  # Test: fromJsonArray\r\n\r\n  # ==========================================\r\n  # YAML conversion functions\r\n  # ==========================================\r\n  # Test: fromYaml\r\n  # Test: toYaml\r\n  # Test: toYamlPretty\r\n  # Test: fromYamlArray\r\n\r\n  # ==========================================\r\n  # TOML conversion functions\r\n  # ==========================================\r\n  # Test: toToml\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-type-conversion/templates/type-conversion-test.yaml",
            "content": "\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n  toDecimalValue: \"0\"\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  # Test: mustToJson\n  # Test: toPrettyJson\n  # Test: mustToPrettyJson\n  # Test: toRawJson\n  # Test: mustToRawJson\n  # Test: fromJson\n  # Test: fromJsonArray\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  # Test: toYaml\n  # Test: toYamlPretty\n  # Test: fromYamlArray\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n",
            "originalContent": "# Test: Type conversion errors - comprehensive testing of all type conversion functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n  {{- if .Values.testToStrings }}\n  toStringsValue: {{ toStrings $testData | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n  {{- if .Values.testToDecimal }}\n  toDecimalValue: {{ toDecimal $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  {{- if .Values.testToJson }}\n  toJsonValue: {{ toJson $testData | quote }}\n  {{- end }}\n  # Test: mustToJson\n  {{- if .Values.testMustToJson }}\n  mustToJsonValue: {{ mustToJson $testData | quote }}\n  {{- end }}\n  # Test: toPrettyJson\n  {{- if .Values.testToPrettyJson }}\n  toPrettyJsonValue: {{ toPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: mustToPrettyJson\n  {{- if .Values.testMustToPrettyJson }}\n  mustToPrettyJsonValue: {{ mustToPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: toRawJson\n  {{- if .Values.testToRawJson }}\n  toRawJsonValue: {{ toRawJson $testData | quote }}\n  {{- end }}\n  # Test: mustToRawJson\n  {{- if .Values.testMustToRawJson }}\n  mustToRawJsonValue: {{ mustToRawJson $testData | quote }}\n  {{- end }}\n  # Test: fromJson\n  {{- if .Values.testFromJson }}\n  {{- $parsed := $testData | fromJson }}\n  fromJsonValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: fromJsonArray\n  {{- if .Values.testFromJsonArray }}\n  {{- $arr := $testData | fromJsonArray }}\n  fromJsonArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  {{- if .Values.testFromYaml }}\n  {{- $parsed := $testData | fromYaml }}\n  fromYamlValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: toYaml\n  {{- if .Values.testToYaml }}\n  toYamlValue: {{ toYaml $testData | quote }}\n  {{- end }}\n  # Test: toYamlPretty\n  {{- if .Values.testToYamlPretty }}\n  toYamlPrettyValue: {{ toYamlPretty $testData | quote }}\n  {{- end }}\n  # Test: fromYamlArray\n  {{- if .Values.testFromYamlArray }}\n  {{- $arr := $testData | fromYamlArray }}\n  fromYamlArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n  {{- if .Values.testToToml }}\n  toTomlValue: {{ toToml $testData | quote }}\n  {{- end }}\n\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-type-conversion-test\r\ndata:\r\n  # ==========================================\r\n  # String conversion functions\r\n  # ==========================================\r\n  # Test: toStrings\r\n\r\n  # ==========================================\r\n  # Decimal conversion functions\r\n  # ==========================================\r\n  # Test: toDecimal\r\n  toDecimalValue: \"0\"\r\n\r\n  # ==========================================\r\n  # JSON conversion functions\r\n  # ==========================================\r\n  # Test: toJson\r\n  # Test: mustToJson\r\n  # Test: toPrettyJson\r\n  # Test: mustToPrettyJson\r\n  # Test: toRawJson\r\n  # Test: mustToRawJson\r\n  # Test: fromJson\r\n  # Test: fromJsonArray\r\n\r\n  # ==========================================\r\n  # YAML conversion functions\r\n  # ==========================================\r\n  # Test: fromYaml\r\n  # Test: toYaml\r\n  # Test: toYamlPretty\r\n  # Test: fromYamlArray\r\n\r\n  # ==========================================\r\n  # TOML conversion functions\r\n  # ==========================================\r\n  # Test: toToml\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-type-conversion/templates/type-conversion-test.yaml",
            "content": "\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n  toDecimalValue: \"0\"\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  # Test: mustToJson\n  # Test: toPrettyJson\n  # Test: mustToPrettyJson\n  # Test: toRawJson\n  # Test: mustToRawJson\n  # Test: fromJson\n  # Test: fromJsonArray\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  # Test: toYaml\n  # Test: toYamlPretty\n  # Test: fromYamlArray\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n",
            "originalContent": "# Test: Type conversion errors - comprehensive testing of all type conversion functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n  {{- if .Values.testToStrings }}\n  toStringsValue: {{ toStrings $testData | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n  {{- if .Values.testToDecimal }}\n  toDecimalValue: {{ toDecimal $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  {{- if .Values.testToJson }}\n  toJsonValue: {{ toJson $testData | quote }}\n  {{- end }}\n  # Test: mustToJson\n  {{- if .Values.testMustToJson }}\n  mustToJsonValue: {{ mustToJson $testData | quote }}\n  {{- end }}\n  # Test: toPrettyJson\n  {{- if .Values.testToPrettyJson }}\n  toPrettyJsonValue: {{ toPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: mustToPrettyJson\n  {{- if .Values.testMustToPrettyJson }}\n  mustToPrettyJsonValue: {{ mustToPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: toRawJson\n  {{- if .Values.testToRawJson }}\n  toRawJsonValue: {{ toRawJson $testData | quote }}\n  {{- end }}\n  # Test: mustToRawJson\n  {{- if .Values.testMustToRawJson }}\n  mustToRawJsonValue: {{ mustToRawJson $testData | quote }}\n  {{- end }}\n  # Test: fromJson\n  {{- if .Values.testFromJson }}\n  {{- $parsed := $testData | fromJson }}\n  fromJsonValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: fromJsonArray\n  {{- if .Values.testFromJsonArray }}\n  {{- $arr := $testData | fromJsonArray }}\n  fromJsonArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  {{- if .Values.testFromYaml }}\n  {{- $parsed := $testData | fromYaml }}\n  fromYamlValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: toYaml\n  {{- if .Values.testToYaml }}\n  toYamlValue: {{ toYaml $testData | quote }}\n  {{- end }}\n  # Test: toYamlPretty\n  {{- if .Values.testToYamlPretty }}\n  toYamlPrettyValue: {{ toYamlPretty $testData | quote }}\n  {{- end }}\n  # Test: fromYamlArray\n  {{- if .Values.testFromYamlArray }}\n  {{- $arr := $testData | fromYamlArray }}\n  fromYamlArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n  {{- if .Values.testToToml }}\n  toTomlValue: {{ toToml $testData | quote }}\n  {{- end }}\n\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "resolved": true,
      "stopReason": "",
      "fixChain": []
    },
    {
      "toggle": "toDecimal",
      "dataType": "dictValue",
      "valuesOptions": {
        "values": [
          "testToDecimal=true",
          "testDataType=dictValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-type-conversion-test\r\ndata:\r\n  # ==========================================\r\n  # String conversion functions\r\n  # ==========================================\r\n  # Test: toStrings\r\n\r\n  # ==========================================\r\n  # Decimal conversion functions\r\n  # ==========================================\r\n  # Test: toDecimal\r\n  toDecimalValue: \"0\"\r\n\r\n  # ==========================================\r\n  # JSON conversion functions\r\n  # ==========================================\r\n  # Test: toJson\r\n  # Test: mustToJson\r\n  # Test: toPrettyJson\r\n  # Test: mustToPrettyJson\r\n  # Test: toRawJson\r\n  # Test: mustToRawJson\r\n  # Test: fromJson\r\n  # Test: fromJsonArray\r\n\r\n  # ==========================================\r\n  # YAML conversion functions\r\n  # ==========================================\r\n  # Test: fromYaml\r\n  # Test: toYaml\r\n  # Test: toYamlPretty\r\n  # Test: fromYamlArray\r\n\r\n  # ==========================================\r\n  # TOML conversion functions\r\n  # ==========================================\r\n  # Test: toToml\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-type-conversion/templates/type-conversion-test.yaml",
            "content": "\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n  toDecimalValue: \"0\"\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  # Test: mustToJson\n  # Test: toPrettyJson\n  # Test: mustToPrettyJson\n  # Test: toRawJson\n  # Test: mustToRawJson\n  # Test: fromJson\n  # Test: fromJsonArray\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  # Test: toYaml\n  # Test: toYamlPretty\n  # Test: fromYamlArray\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n",
            "originalContent": "# Test: Type conversion errors - comprehensive testing of all type conversion functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n  {{- if .Values.testToStrings }}\n  toStringsValue: {{ toStrings $testData | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n  {{- if .Values.testToDecimal }}\n  toDecimalValue: {{ toDecimal $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  {{- if .Values.testToJson }}\n  toJsonValue: {{ toJson $testData | quote }}\n  {{- end }}\n  # Test: mustToJson\n  {{- if .Values.testMustToJson }}\n  mustToJsonValue: {{ mustToJson $testData | quote }}\n  {{- end }}\n  # Test: toPrettyJson\n  {{- if .Values.testToPrettyJson }}\n  toPrettyJsonValue: {{ toPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: mustToPrettyJson\n  {{- if .Values.testMustToPrettyJson }}\n  mustToPrettyJsonValue: {{ mustToPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: toRawJson\n  {{- if .Values.testToRawJson }}\n  toRawJsonValue: {{ toRawJson $testData | quote }}\n  {{- end }}\n  # Test: mustToRawJson\n  {{- if .Values.testMustToRawJson }}\n  mustToRawJsonValue: {{ mustToRawJson $testData | quote }}\n  {{- end }}\n  # Test: fromJson\n  {{- if .Values.testFromJson }}\n  {{- $parsed := $testData | fromJson }}\n  fromJsonValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: fromJsonArray\n  {{- if .Values.testFromJsonArray }}\n  {{- $arr := $testData | fromJsonArray }}\n  fromJsonArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  {{- if .Values.testFromYaml }}\n  {{- $parsed := $testData | fromYaml }}\n  fromYamlValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: toYaml\n  {{- if .Values.testToYaml }}\n  toYamlValue: {{ toYaml $testData | quote }}\n  {{- end }}\n  # Test: toYamlPretty\n  {{- if .Values.testToYamlPretty }}\n  toYamlPrettyValue: {{ toYamlPretty $testData | quote }}\n  {{- end }}\n  # Test: fromYamlArray\n  {{- if .Values.testFromYamlArray }}\n  {{- $arr := $testData | fromYamlArray }}\n  fromYamlArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n  {{- if .Values.testToToml }}\n  toTomlValue: {{ toToml $testData | quote }}\n  {{- end }}\n\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-type-conversion-test\r\ndata:\r\n  # ==========================================\r\n  # String conversion functions\r\n  # ==========================================\r\n  # Test: toStrings\r\n\r\n  # ==========================================\r\n  # Decimal conversion functions\r\n  # ==========================================\r\n  # Test: toDecimal\r\n  toDecimalValue: \"0\"\r\n\r\n  # ==========================================\r\n  # JSON conversion functions\r\n  # ==========================================\r\n  # Test: toJson\r\n  # Test: mustToJson\r\n  # Test: toPrettyJson\r\n  # Test: mustToPrettyJson\r\n  # Test: toRawJson\r\n  # Test: mustToRawJson\r\n  # Test: fromJson\r\n  # Test: fromJsonArray\r\n\r\n  # ==========================================\r\n  # YAML conversion functions\r\n  # ==========================================\r\n  # Test: fromYaml\r\n  # Test: toYaml\r\n  # Test: toYamlPretty\r\n  # Test: fromYamlArray\r\n\r\n  # ==========================================\r\n  # TOML conversion functions\r\n  # ==========================================\r\n  # Test: toToml\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-type-conversion/templates/type-conversion-test.yaml",
            "content": "\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n  toDecimalValue: \"0\"\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  # Test: mustToJson\n  # Test: toPrettyJson\n  # Test: mustToPrettyJson\n  # Test: toRawJson\n  # Test: mustToRawJson\n  # Test: fromJson\n  # Test: fromJsonArray\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  # Test: toYaml\n  # Test: toYamlPretty\n  # Test: fromYamlArray\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n",
            "originalContent": "# Test: Type conversion errors - comprehensive testing of all type conversion functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n  {{- if .Values.testToStrings }}\n  toStringsValue: {{ toStrings $testData | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n  {{- if .Values.testToDecimal }}\n  toDecimalValue: {{ toDecimal $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  {{- if .Values.testToJson }}\n  toJsonValue: {{ toJson $testData | quote }}\n  {{- end }}\n  # Test: mustToJson\n  {{- if .Values.testMustToJson }}\n  mustToJsonValue: {{ mustToJson $testData | quote }}\n  {{- end }}\n  # Test: toPrettyJson\n  {{- if .Values.testToPrettyJson }}\n  toPrettyJsonValue: {{ toPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: mustToPrettyJson\n  {{- if .Values.testMustToPrettyJson }}\n  mustToPrettyJsonValue: {{ mustToPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: toRawJson\n  {{- if .Values.testToRawJson }}\n  toRawJsonValue: {{ toRawJson $testData | quote }}\n  {{- end }}\n  # Test: mustToRawJson\n  {{- if .Values.testMustToRawJson }}\n  mustToRawJsonValue: {{ mustToRawJson $testData | quote }}\n  {{- end }}\n  # Test: fromJson\n  {{- if .Values.testFromJson }}\n  {{- $parsed := $testData | fromJson }}\n  fromJsonValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: fromJsonArray\n  {{- if .Values.testFromJsonArray }}\n  {{- $arr := $testData | fromJsonArray }}\n  fromJsonArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  {{- if .Values.testFromYaml }}\n  {{- $parsed := $testData | fromYaml }}\n  fromYamlValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: toYaml\n  {{- if .Values.testToYaml }}\n  toYamlValue: {{ toYaml $testData | quote }}\n  {{- end }}\n  # Test: toYamlPretty\n  {{- if .Values.testToYamlPretty }}\n  toYamlPrettyValue: {{ toYamlPretty $testData | quote }}\n  {{- end }}\n  # Test: fromYamlArray\n  {{- if .Values.testFromYamlArray }}\n  {{- $arr := $testData | fromYamlArray }}\n  fromYamlArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n  {{- if .Values.testToToml }}\n  toTomlValue: {{ toToml $testData | quote }}\n  {{- end }}\n\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "resolved": true,
      "stopReason": "",
      "fixChain": []
    },
    {
      "toggle": "toJson",
      "dataType": "nilValue",
      "valuesOptions": {
        "values": [
          "testToJson=true",
          "testDataType=nilValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-type-conversion-test\r\ndata:\r\n  # ==========================================\r\n  # String conversion functions\r\n  # ==========================================\r\n  # Test: toStrings\r\n\r\n  # ==========================================\r\n  # Decimal conversion functions\r\n  # ==========================================\r\n  # Test: toDecimal\r\n\r\n  # ==========================================\r\n  # JSON conversion functions\r\n  # ==========================================\r\n  # Test: toJson\r\n  toJsonValue: \"null\"\r\n  # Test: mustToJson\r\n  # Test: toPrettyJson\r\n  # Test: mustToPrettyJson\r\n  # Test: toRawJson\r\n  # Test: mustToRawJson\r\n  # Test: fromJson\r\n  # Test: fromJsonArray\r\n\r\n  # ==========================================\r\n  # YAML conversion functions\r\n  # ==========================================\r\n  # Test: fromYaml\r\n  # Test: toYaml\r\n  # Test: toYamlPretty\r\n  # Test: fromYamlArray\r\n\r\n  # ==========================================\r\n  # TOML conversion functions\r\n  # ==========================================\r\n  # Test: toToml\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-type-conversion/templates/type-conversion-test.yaml",
            "content": "\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  toJsonValue: \"null\"\n  # Test: mustToJson\n  # Test: toPrettyJson\n  # Test: mustToPrettyJson\n  # Test: toRawJson\n  # Test: mustToRawJson\n  # Test: fromJson\n  # Test: fromJsonArray\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  # Test: toYaml\n  # Test: toYamlPretty\n  # Test: fromYamlArray\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n",
            "originalContent": "# Test: Type conversion errors - comprehensive testing of all type conversion functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n  {{- if .Values.testToStrings }}\n  toStringsValue: {{ toStrings $testData | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n  {{- if .Values.testToDecimal }}\n  toDecimalValue: {{ toDecimal $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  {{- if .Values.testToJson }}\n  toJsonValue: {{ toJson $testData | quote }}\n  {{- end }}\n  # Test: mustToJson\n  {{- if .Values.testMustToJson }}\n  mustToJsonValue: {{ mustToJson $testData | quote }}\n  {{- end }}\n  # Test: toPrettyJson\n  {{- if .Values.testToPrettyJson }}\n  toPrettyJsonValue: {{ toPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: mustToPrettyJson\n  {{- if .Values.testMustToPrettyJson }}\n  mustToPrettyJsonValue: {{ mustToPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: toRawJson\n  {{- if .Values.testToRawJson }}\n  toRawJsonValue: {{ toRawJson $testData | quote }}\n  {{- end }}\n  # Test: mustToRawJson\n  {{- if .Values.testMustToRawJson }}\n  mustToRawJsonValue: {{ mustToRawJson $testData | quote }}\n  {{- end }}\n  # Test: fromJson\n  {{- if .Values.testFromJson }}\n  {{- $parsed := $testData | fromJson }}\n  fromJsonValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: fromJsonArray\n  {{- if .Values.testFromJsonArray }}\n  {{- $arr := $testData | fromJsonArray }}\n  fromJsonArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  {{- if .Values.testFromYaml }}\n  {{- $parsed := $testData | fromYaml }}\n  fromYamlValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: toYaml\n  {{- if .Values.testToYaml }}\n  toYamlValue: {{ toYaml $testData | quote }}\n  {{- end }}\n  # Test: toYamlPretty\n  {{- if .Values.testToYamlPretty }}\n  toYamlPrettyValue: {{ toYamlPretty $testData | quote }}\n  {{- end }}\n  # Test: fromYamlArray\n  {{- if .Values.testFromYamlArray }}\n  {{- $arr := $testData | fromYamlArray }}\n  fromYamlArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n  {{- if .Values.testToToml }}\n  toTomlValue: {{ toToml $testData | quote }}\n  {{- end }}\n\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-type-conversion-test\r\ndata:\r\n  # ==========================================\r\n  # String conversion functions\r\n  # ==========================================\r\n  # Test: toStrings\r\n\r\n  # ==========================================\r\n  # Decimal conversion functions\r\n  # ==========================================\r\n  # Test: toDecimal\r\n\r\n  # ==========================================\r\n  # JSON conversion functions\r\n  # ==========================================\r\n  # Test: toJson\r\n  toJsonValue: \"null\"\r\n  # Test: mustToJson\r\n  # Test: toPrettyJson\r\n  # Test: mustToPrettyJson\r\n  # Test: toRawJson\r\n  # Test: mustToRawJson\r\n  # Test: fromJson\r\n  # Test: fromJsonArray\r\n\r\n  # ==========================================\r\n  # YAML conversion functions\r\n  # ==========================================\r\n  # Test: fromYaml\r\n  # Test: toYaml\r\n  # Test: toYamlPretty\r\n  # Test: fromYamlArray\r\n\r\n  # ==========================================\r\n  # TOML conversion functions\r\n  # ==========================================\r\n  # Test: toToml\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-type-conversion/templates/type-conversion-test.yaml",
            "content": "\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  toJsonValue: \"null\"\n  # Test: mustToJson\n  # Test: toPrettyJson\n  # Test: mustToPrettyJson\n  # Test: toRawJson\n  # Test: mustToRawJson\n  # Test: fromJson\n  # Test: fromJsonArray\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  # Test: toYaml\n  # Test: toYamlPretty\n  # Test: fromYamlArray\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n",
            "originalContent": "# Test: Type conversion errors - comprehensive testing of all type conversion functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n  {{- if .Values.testToStrings }}\n  toStringsValue: {{ toStrings $testData | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n  {{- if .Values.testToDecimal }}\n  toDecimalValue: {{ toDecimal $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  {{- if .Values.testToJson }}\n  toJsonValue: {{ toJson $testData | quote }}\n  {{- end }}\n  # Test: mustToJson\n  {{- if .Values.testMustToJson }}\n  mustToJsonValue: {{ mustToJson $testData | quote }}\n  {{- end }}\n  # Test: toPrettyJson\n  {{- if .Values.testToPrettyJson }}\n  toPrettyJsonValue: {{ toPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: mustToPrettyJson\n  {{- if .Values.testMustToPrettyJson }}\n  mustToPrettyJsonValue: {{ mustToPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: toRawJson\n  {{- if .Values.testToRawJson }}\n  toRawJsonValue: {{ toRawJson $testData | quote }}\n  {{- end }}\n  # Test: mustToRawJson\n  {{- if .Values.testMustToRawJson }}\n  mustToRawJsonValue: {{ mustToRawJson $testData | quote }}\n  {{- end }}\n  # Test: fromJson\n  {{- if .Values.testFromJson }}\n  {{- $parsed := $testData | fromJson }}\n  fromJsonValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: fromJsonArray\n  {{- if .Values.testFromJsonArray }}\n  {{- $arr := $testData | fromJsonArray }}\n  fromJsonArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  {{- if .Values.testFromYaml }}\n  {{- $parsed := $testData | fromYaml }}\n  fromYamlValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: toYaml\n  {{- if .Values.testToYaml }}\n  toYamlValue: {{ toYaml $testData | quote }}\n  {{- end }}\n  # Test: toYamlPretty\n  {{- if .Values.testToYamlPretty }}\n  toYamlPrettyValue: {{ toYamlPretty $testData | quote }}\n  {{- end }}\n  # Test: fromYamlArray\n  {{- if .Values.testFromYamlArray }}\n  {{- $arr := $testData | fromYamlArray }}\n  fromYamlArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n  {{- if .Values.testToToml }}\n  toTomlValue: {{ toToml $testData | quote }}\n  {{- end }}\n\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "resolved": true,
      "stopReason": "",
      "fixChain": []
    },
    {
      "toggle": "toJson",
      "dataType": "stringValue",
      "valuesOptions": {
        "values": [
          "testToJson=true",
          "testDataType=stringValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-type-conversion-test\r\ndata:\r\n  # ==========================================\r\n  # String conversion functions\r\n  # ==========================================\r\n  # Test: toStrings\r\n\r\n  # ==========================================\r\n  # Decimal conversion functions\r\n  # ==========================================\r\n  # Test: toDecimal\r\n\r\n  # ==========================================\r\n  # JSON conversion functions\r\n  # ==========================================\r\n  # Test: toJson\r\n  toJsonValue: \"\\\"test string\\\"\"\r\n  # Test: mustToJson\r\n  # Test: toPrettyJson\r\n  # Test: mustToPrettyJson\r\n  # Test: toRawJson\r\n  # Test: mustToRawJson\r\n  # Test: fromJson\r\n  # Test: fromJsonArray\r\n\r\n  # ==========================================\r\n  # YAML conversion functions\r\n  # ==========================================\r\n  # Test: fromYaml\r\n  # Test: toYaml\r\n  # Test: toYamlPretty\r\n  # Test: fromYamlArray\r\n\r\n  # ==========================================\r\n  # TOML conversion functions\r\n  # ==========================================\r\n  # Test: toToml\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-type-conversion/templates/type-conversion-test.yaml",
            "content": "\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  toJsonValue: \"\\\"test string\\\"\"\n  # Test: mustToJson\n  # Test: toPrettyJson\n  # Test: mustToPrettyJson\n  # Test: toRawJson\n  # Test: mustToRawJson\n  # Test: fromJson\n  # Test: fromJsonArray\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  # Test: toYaml\n  # Test: toYamlPretty\n  # Test: fromYamlArray\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n",
            "originalContent": "# Test: Type conversion errors - comprehensive testing of all type conversion functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n  {{- if .Values.testToStrings }}\n  toStringsValue: {{ toStrings $testData | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n  {{- if .Values.testToDecimal }}\n  toDecimalValue: {{ toDecimal $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  {{- if .Values.testToJson }}\n  toJsonValue: {{ toJson $testData | quote }}\n  {{- end }}\n  # Test: mustToJson\n  {{- if .Values.testMustToJson }}\n  mustToJsonValue: {{ mustToJson $testData | quote }}\n  {{- end }}\n  # Test: toPrettyJson\n  {{- if .Values.testToPrettyJson }}\n  toPrettyJsonValue: {{ toPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: mustToPrettyJson\n  {{- if .Values.testMustToPrettyJson }}\n  mustToPrettyJsonValue: {{ mustToPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: toRawJson\n  {{- if .Values.testToRawJson }}\n  toRawJsonValue: {{ toRawJson $testData | quote }}\n  {{- end }}\n  # Test: mustToRawJson\n  {{- if .Values.testMustToRawJson }}\n  mustToRawJsonValue: {{ mustToRawJson $testData | quote }}\n  {{- end }}\n  # Test: fromJson\n  {{- if .Values.testFromJson }}\n  {{- $parsed := $testData | fromJson }}\n  fromJsonValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: fromJsonArray\n  {{- if .Values.testFromJsonArray }}\n  {{- $arr := $testData | fromJsonArray }}\n  fromJsonArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  {{- if .Values.testFromYaml }}\n  {{- $parsed := $testData | fromYaml }}\n  fromYamlValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: toYaml\n  {{- if .Values.testToYaml }}\n  toYamlValue: {{ toYaml $testData | quote }}\n  {{- end }}\n  # Test: toYamlPretty\n  {{- if .Values.testToYamlPretty }}\n  toYamlPrettyValue: {{ toYamlPretty $testData | quote }}\n  {{- end }}\n  # Test: fromYamlArray\n  {{- if .Values.testFromYamlArray }}\n  {{- $arr := $testData | fromYamlArray }}\n  fromYamlArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n  {{- if .Values.testToToml }}\n  toTomlValue: {{ toToml $testData | quote }}\n  {{- end }}\n\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-type-conversion-test\r\ndata:\r\n  # ==========================================\r\n  # String conversion functions\r\n  # ==========================================\r\n  # Test: toStrings\r\n\r\n  # ==========================================\r\n  # Decimal conversion functions\r\n  # ==========================================\r\n  # Test: toDecimal\r\n\r\n  # ==========================================\r\n  # JSON conversion functions\r\n  # ==========================================\r\n  # Test: toJson\r\n  toJsonValue: \"\\\"test string\\\"\"\r\n  # Test: mustToJson\r\n  # Test: toPrettyJson\r\n  # Test: mustToPrettyJson\r\n  # Test: toRawJson\r\n  # Test: mustToRawJson\r\n  # Test: fromJson\r\n  # Test: fromJsonArray\r\n\r\n  # ==========================================\r\n  # YAML conversion functions\r\n  # ==========================================\r\n  # Test: fromYaml\r\n  # Test: toYaml\r\n  # Test: toYamlPretty\r\n  # Test: fromYamlArray\r\n\r\n  # ==========================================\r\n  # TOML conversion functions\r\n  # ==========================================\r\n  # Test: toToml\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-type-conversion/templates/type-conversion-test.yaml",
            "content": "\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  toJsonValue: \"\\\"test string\\\"\"\n  # Test: mustToJson\n  # Test: toPrettyJson\n  # Test: mustToPrettyJson\n  # Test: toRawJson\n  # Test: mustToRawJson\n  # Test: fromJson\n  # Test: fromJsonArray\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  # Test: toYaml\n  # Test: toYamlPretty\n  # Test: fromYamlArray\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n",
            "originalContent": "# Test: Type conversion errors - comprehensive testing of all type conversion functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n  {{- if .Values.testToStrings }}\n  toStringsValue: {{ toStrings $testData | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n  {{- if .Values.testToDecimal }}\n  toDecimalValue: {{ toDecimal $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  {{- if .Values.testToJson }}\n  toJsonValue: {{ toJson $testData | quote }}\n  {{- end }}\n  # Test: mustToJson\n  {{- if .Values.testMustToJson }}\n  mustToJsonValue: {{ mustToJson $testData | quote }}\n  {{- end }}\n  # Test: toPrettyJson\n  {{- if .Values.testToPrettyJson }}\n  toPrettyJsonValue: {{ toPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: mustToPrettyJson\n  {{- if .Values.testMustToPrettyJson }}\n  mustToPrettyJsonValue: {{ mustToPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: toRawJson\n  {{- if .Values.testToRawJson }}\n  toRawJsonValue: {{ toRawJson $testData | quote }}\n  {{- end }}\n  # Test: mustToRawJson\n  {{- if .Values.testMustToRawJson }}\n  mustToRawJsonValue: {{ mustToRawJson $testData | quote }}\n  {{- end }}\n  # Test: fromJson\n  {{- if .Values.testFromJson }}\n  {{- $parsed := $testData | fromJson }}\n  fromJsonValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: fromJsonArray\n  {{- if .Values.testFromJsonArray }}\n  {{- $arr := $testData | fromJsonArray }}\n  fromJsonArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  {{- if .Values.testFromYaml }}\n  {{- $parsed := $testData | fromYaml }}\n  fromYamlValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: toYaml\n  {{- if .Values.testToYaml }}\n  toYamlValue: {{ toYaml $testData | quote }}\n  {{- end }}\n  # Test: toYamlPretty\n  {{- if .Values.testToYamlPretty }}\n  toYamlPrettyValue: {{ toYamlPretty $testData | quote }}\n  {{- end }}\n  # Test: fromYamlArray\n  {{- if .Values.testFromYamlArray }}\n  {{- $arr := $testData | fromYamlArray }}\n  fromYamlArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n  {{- if .Values.testToToml }}\n  toTomlValue: {{ toToml $testData | quote }}\n  {{- end }}\n\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "resolved": true,
      "stopReason": "",
      "fixChain": []
    },
    {
      "toggle": "toJson",
      "dataType": "numberValue",
      "valuesOptions": {
        "values": [
          "testToJson=true",
          "testDataType=numberValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-type-conversion-test\r\ndata:\r\n  # ==========================================\r\n  # String conversion functions\r\n  # ==========================================\r\n  # Test: toStrings\r\n\r\n  # ==========================================\r\n  # Decimal conversion functions\r\n  # ==========================================\r\n  # Test: toDecimal\r\n\r\n  # ==========================================\r\n  # JSON conversion functions\r\n  # ==========================================\r\n  # Test: toJson\r\n  toJsonValue: \"42\"\r\n  # Test: mustToJson\r\n  # Test: toPrettyJson\r\n  # Test: mustToPrettyJson\r\n  # Test: toRawJson\r\n  # Test: mustToRawJson\r\n  # Test: fromJson\r\n  # Test: fromJsonArray\r\n\r\n  # ==========================================\r\n  # YAML conversion functions\r\n  # ==========================================\r\n  # Test: fromYaml\r\n  # Test: toYaml\r\n  # Test: toYamlPretty\r\n  # Test: fromYamlArray\r\n\r\n  # ==========================================\r\n  # TOML conversion functions\r\n  # ==========================================\r\n  # Test: toToml\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-type-conversion/templates/type-conversion-test.yaml",
            "content": "\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  toJsonValue: \"42\"\n  # Test: mustToJson\n  # Test: toPrettyJson\n  # Test: mustToPrettyJson\n  # Test: toRawJson\n  # Test: mustToRawJson\n  # Test: fromJson\n  # Test: fromJsonArray\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  # Test: toYaml\n  # Test: toYamlPretty\n  # Test: fromYamlArray\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n",
            "originalContent": "# Test: Type conversion errors - comprehensive testing of all type conversion functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n  {{- if .Values.testToStrings }}\n  toStringsValue: {{ toStrings $testData | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n  {{- if .Values.testToDecimal }}\n  toDecimalValue: {{ toDecimal $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  {{- if .Values.testToJson }}\n  toJsonValue: {{ toJson $testData | quote }}\n  {{- end }}\n  # Test: mustToJson\n  {{- if .Values.testMustToJson }}\n  mustToJsonValue: {{ mustToJson $testData | quote }}\n  {{- end }}\n  # Test: toPrettyJson\n  {{- if .Values.testToPrettyJson }}\n  toPrettyJsonValue: {{ toPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: mustToPrettyJson\n  {{- if .Values.testMustToPrettyJson }}\n  mustToPrettyJsonValue: {{ mustToPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: toRawJson\n  {{- if .Values.testToRawJson }}\n  toRawJsonValue: {{ toRawJson $testData | quote }}\n  {{- end }}\n  # Test: mustToRawJson\n  {{- if .Values.testMustToRawJson }}\n  mustToRawJsonValue: {{ mustToRawJson $testData | quote }}\n  {{- end }}\n  # Test: fromJson\n  {{- if .Values.testFromJson }}\n  {{- $parsed := $testData | fromJson }}\n  fromJsonValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: fromJsonArray\n  {{- if .Values.testFromJsonArray }}\n  {{- $arr := $testData | fromJsonArray }}\n  fromJsonArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  {{- if .Values.testFromYaml }}\n  {{- $parsed := $testData | fromYaml }}\n  fromYamlValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: toYaml\n  {{- if .Values.testToYaml }}\n  toYamlValue: {{ toYaml $testData | quote }}\n  {{- end }}\n  # Test: toYamlPretty\n  {{- if .Values.testToYamlPretty }}\n  toYamlPrettyValue: {{ toYamlPretty $testData | quote }}\n  {{- end }}\n  # Test: fromYamlArray\n  {{- if .Values.testFromYamlArray }}\n  {{- $arr := $testData | fromYamlArray }}\n  fromYamlArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n  {{- if .Values.testToToml }}\n  toTomlValue: {{ toToml $testData | quote }}\n  {{- end }}\n\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-type-conversion-test\r\ndata:\r\n  # ==========================================\r\n  # String conversion functions\r\n  # ==========================================\r\n  # Test: toStrings\r\n\r\n  # ==========================================\r\n  # Decimal conversion functions\r\n  # ==========================================\r\n  # Test: toDecimal\r\n\r\n  # ==========================================\r\n  # JSON conversion functions\r\n  # ==========================================\r\n  # Test: toJson\r\n  toJsonValue: \"42\"\r\n  # Test: mustToJson\r\n  # Test: toPrettyJson\r\n  # Test: mustToPrettyJson\r\n  # Test: toRawJson\r\n  # Test: mustToRawJson\r\n  # Test: fromJson\r\n  # Test: fromJsonArray\r\n\r\n  # ==========================================\r\n  # YAML conversion functions\r\n  # ==========================================\r\n  # Test: fromYaml\r\n  # Test: toYaml\r\n  # Test: toYamlPretty\r\n  # Test: fromYamlArray\r\n\r\n  # ==========================================\r\n  # TOML conversion functions\r\n  # ==========================================\r\n  # Test: toToml\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-type-conversion/templates/type-conversion-test.yaml",
            "content": "\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  toJsonValue: \"42\"\n  # Test: mustToJson\n  # Test: toPrettyJson\n  # Test: mustToPrettyJson\n  # Test: toRawJson\n  # Test: mustToRawJson\n  # Test: fromJson\n  # Test: fromJsonArray\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  # Test: toYaml\n  # Test: toYamlPretty\n  # Test: fromYamlArray\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n",
            "originalContent": "# Test: Type conversion errors - comprehensive testing of all type conversion functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n  {{- if .Values.testToStrings }}\n  toStringsValue: {{ toStrings $testData | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n  {{- if .Values.testToDecimal }}\n  toDecimalValue: {{ toDecimal $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  {{- if .Values.testToJson }}\n  toJsonValue: {{ toJson $testData | quote }}\n  {{- end }}\n  # Test: mustToJson\n  {{- if .Values.testMustToJson }}\n  mustToJsonValue: {{ mustToJson $testData | quote }}\n  {{- end }}\n  # Test: toPrettyJson\n  {{- if .Values.testToPrettyJson }}\n  toPrettyJsonValue: {{ toPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: mustToPrettyJson\n  {{- if .Values.testMustToPrettyJson }}\n  mustToPrettyJsonValue: {{ mustToPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: toRawJson\n  {{- if .Values.testToRawJson }}\n  toRawJsonValue: {{ toRawJson $testData | quote }}\n  {{- end }}\n  # Test: mustToRawJson\n  {{- if .Values.testMustToRawJson }}\n  mustToRawJsonValue: {{ mustToRawJson $testData | quote }}\n  {{- end }}\n  # Test: fromJson\n  {{- if .Values.testFromJson }}\n  {{- $parsed := $testData | fromJson }}\n  fromJsonValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: fromJsonArray\n  {{- if .Values.testFromJsonArray }}\n  {{- $arr := $testData | fromJsonArray }}\n  fromJsonArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  {{- if .Values.testFromYaml }}\n  {{- $parsed := $testData | fromYaml }}\n  fromYamlValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: toYaml\n  {{- if .Values.testToYaml }}\n  toYamlValue: {{ toYaml $testData | quote }}\n  {{- end }}\n  # Test: toYamlPretty\n  {{- if .Values.testToYamlPretty }}\n  toYamlPrettyValue: {{ toYamlPretty $testData | quote }}\n  {{- end }}\n  # Test: fromYamlArray\n  {{- if .Values.testFromYamlArray }}\n  {{- $arr := $testData | fromYamlArray }}\n  fromYamlArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n  {{- if .Values.testToToml }}\n  toTomlValue: {{ toToml $testData | quote }}\n  {{- end }}\n\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "resolved": true,
      "stopReason": "",
      "fixChain": []
    },
    {
      "toggle": "toJson",
      "dataType": "boolValue",
      "valuesOptions": {
        "values": [
          "testToJson=true",
          "testDataType=boolValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-type-conversion-test\r\ndata:\r\n  # ==========================================\r\n  # String conversion functions\r\n  # ==========================================\r\n  # Test: toStrings\r\n\r\n  # ==========================================\r\n  # Decimal conversion functions\r\n  # ==========================================\r\n  # Test: toDecimal\r\n\r\n  # ==========================================\r\n  # JSON conversion functions\r\n  # ==========================================\r\n  # Test: toJson\r\n  toJsonValue: \"true\"\r\n  # Test: mustToJson\r\n  # Test: toPrettyJson\r\n  # Test: mustToPrettyJson\r\n  # Test: toRawJson\r\n  # Test: mustToRawJson\r\n  # Test: fromJson\r\n  # Test: fromJsonArray\r\n\r\n  # ==========================================\r\n  # YAML conversion functions\r\n  # ==========================================\r\n  # Test: fromYaml\r\n  # Test: toYaml\r\n  # Test: toYamlPretty\r\n  # Test: fromYamlArray\r\n\r\n  # ==========================================\r\n  # TOML conversion functions\r\n  # ==========================================\r\n  # Test: toToml\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-type-conversion/templates/type-conversion-test.yaml",
            "content": "\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  toJsonValue: \"true\"\n  # Test: mustToJson\n  # Test: toPrettyJson\n  # Test: mustToPrettyJson\n  # Test: toRawJson\n  # Test: mustToRawJson\n  # Test: fromJson\n  # Test: fromJsonArray\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  # Test: toYaml\n  # Test: toYamlPretty\n  # Test: fromYamlArray\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n",
            "originalContent": "# Test: Type conversion errors - comprehensive testing of all type conversion functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n  {{- if .Values.testToStrings }}\n  toStringsValue: {{ toStrings $testData | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n  {{- if .Values.testToDecimal }}\n  toDecimalValue: {{ toDecimal $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  {{- if .Values.testToJson }}\n  toJsonValue: {{ toJson $testData | quote }}\n  {{- end }}\n  # Test: mustToJson\n  {{- if .Values.testMustToJson }}\n  mustToJsonValue: {{ mustToJson $testData | quote }}\n  {{- end }}\n  # Test: toPrettyJson\n  {{- if .Values.testToPrettyJson }}\n  toPrettyJsonValue: {{ toPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: mustToPrettyJson\n  {{- if .Values.testMustToPrettyJson }}\n  mustToPrettyJsonValue: {{ mustToPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: toRawJson\n  {{- if .Values.testToRawJson }}\n  toRawJsonValue: {{ toRawJson $testData | quote }}\n  {{- end }}\n  # Test: mustToRawJson\n  {{- if .Values.testMustToRawJson }}\n  mustToRawJsonValue: {{ mustToRawJson $testData | quote }}\n  {{- end }}\n  # Test: fromJson\n  {{- if .Values.testFromJson }}\n  {{- $parsed := $testData | fromJson }}\n  fromJsonValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: fromJsonArray\n  {{- if .Values.testFromJsonArray }}\n  {{- $arr := $testData | fromJsonArray }}\n  fromJsonArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  {{- if .Values.testFromYaml }}\n  {{- $parsed := $testData | fromYaml }}\n  fromYamlValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: toYaml\n  {{- if .Values.testToYaml }}\n  toYamlValue: {{ toYaml $testData | quote }}\n  {{- end }}\n  # Test: toYamlPretty\n  {{- if .Values.testToYamlPretty }}\n  toYamlPrettyValue: {{ toYamlPretty $testData | quote }}\n  {{- end }}\n  # Test: fromYamlArray\n  {{- if .Values.testFromYamlArray }}\n  {{- $arr := $testData | fromYamlArray }}\n  fromYamlArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n  {{- if .Values.testToToml }}\n  toTomlValue: {{ toToml $testData | quote }}\n  {{- end }}\n\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-type-conversion-test\r\ndata:\r\n  # ==========================================\r\n  # String conversion functions\r\n  # ==========================================\r\n  # Test: toStrings\r\n\r\n  # ==========================================\r\n  # Decimal conversion functions\r\n  # ==========================================\r\n  # Test: toDecimal\r\n\r\n  # ==========================================\r\n  # JSON conversion functions\r\n  # ==========================================\r\n  # Test: toJson\r\n  toJsonValue: \"true\"\r\n  # Test: mustToJson\r\n  # Test: toPrettyJson\r\n  # Test: mustToPrettyJson\r\n  # Test: toRawJson\r\n  # Test: mustToRawJson\r\n  # Test: fromJson\r\n  # Test: fromJsonArray\r\n\r\n  # ==========================================\r\n  # YAML conversion functions\r\n  # ==========================================\r\n  # Test: fromYaml\r\n  # Test: toYaml\r\n  # Test: toYamlPretty\r\n  # Test: fromYamlArray\r\n\r\n  # ==========================================\r\n  # TOML conversion functions\r\n  # ==========================================\r\n  # Test: toToml\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-type-conversion/templates/type-conversion-test.yaml",
            "content": "\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  toJsonValue: \"true\"\n  # Test: mustToJson\n  # Test: toPrettyJson\n  # Test: mustToPrettyJson\n  # Test: toRawJson\n  # Test: mustToRawJson\n  # Test: fromJson\n  # Test: fromJsonArray\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  # Test: toYaml\n  # Test: toYamlPretty\n  # Test: fromYamlArray\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n",
            "originalContent": "# Test: Type conversion errors - comprehensive testing of all type conversion functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n  {{- if .Values.testToStrings }}\n  toStringsValue: {{ toStrings $testData | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n  {{- if .Values.testToDecimal }}\n  toDecimalValue: {{ toDecimal $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  {{- if .Values.testToJson }}\n  toJsonValue: {{ toJson $testData | quote }}\n  {{- end }}\n  # Test: mustToJson\n  {{- if .Values.testMustToJson }}\n  mustToJsonValue: {{ mustToJson $testData | quote }}\n  {{- end }}\n  # Test: toPrettyJson\n  {{- if .Values.testToPrettyJson }}\n  toPrettyJsonValue: {{ toPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: mustToPrettyJson\n  {{- if .Values.testMustToPrettyJson }}\n  mustToPrettyJsonValue: {{ mustToPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: toRawJson\n  {{- if .Values.testToRawJson }}\n  toRawJsonValue: {{ toRawJson $testData | quote }}\n  {{- end }}\n  # Test: mustToRawJson\n  {{- if .Values.testMustToRawJson }}\n  mustToRawJsonValue: {{ mustToRawJson $testData | quote }}\n  {{- end }}\n  # Test: fromJson\n  {{- if .Values.testFromJson }}\n  {{- $parsed := $testData | fromJson }}\n  fromJsonValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: fromJsonArray\n  {{- if .Values.testFromJsonArray }}\n  {{- $arr := $testData | fromJsonArray }}\n  fromJsonArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  {{- if .Values.testFromYaml }}\n  {{- $parsed := $testData | fromYaml }}\n  fromYamlValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: toYaml\n  {{- if .Values.testToYaml }}\n  toYamlValue: {{ toYaml $testData | quote }}\n  {{- end }}\n  # Test: toYamlPretty\n  {{- if .Values.testToYamlPretty }}\n  toYamlPrettyValue: {{ toYamlPretty $testData | quote }}\n  {{- end }}\n  # Test: fromYamlArray\n  {{- if .Values.testFromYamlArray }}\n  {{- $arr := $testData | fromYamlArray }}\n  fromYamlArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n  {{- if .Values.testToToml }}\n  toTomlValue: {{ toToml $testData | quote }}\n  {{- end }}\n\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "resolved": true,
      "stopReason": "",
      "fixChain": []
    },
    {
      "toggle": "toJson",
      "dataType": "listValue",
      "valuesOptions": {
        "values": [
          "testToJson=true",
          "testDataType=listValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-type-conversion-test\r\ndata:\r\n  # ==========================================\r\n  # String conversion functions\r\n  # ==========================================\r\n  # Test: toStrings\r\n\r\n  # ==========================================\r\n  # Decimal conversion functions\r\n  # ==========================================\r\n  # Test: toDecimal\r\n\r\n  # ==========================================\r\n  # JSON conversion functions\r\n  # ==========================================\r\n  # Test: toJson\r\n  toJsonValue: \"[1,2,3]\"\r\n  # Test: mustToJson\r\n  # Test: toPrettyJson\r\n  # Test: mustToPrettyJson\r\n  # Test: toRawJson\r\n  # Test: mustToRawJson\r\n  # Test: fromJson\r\n  # Test: fromJsonArray\r\n\r\n  # ==========================================\r\n  # YAML conversion functions\r\n  # ==========================================\r\n  # Test: fromYaml\r\n  # Test: toYaml\r\n  # Test: toYamlPretty\r\n  # Test: fromYamlArray\r\n\r\n  # ==========================================\r\n  # TOML conversion functions\r\n  # ==========================================\r\n  # Test: toToml\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-type-conversion/templates/type-conversion-test.yaml",
            "content": "\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  toJsonValue: \"[1,2,3]\"\n  # Test: mustToJson\n  # Test: toPrettyJson\n  # Test: mustToPrettyJson\n  # Test: toRawJson\n  # Test: mustToRawJson\n  # Test: fromJson\n  # Test: fromJsonArray\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  # Test: toYaml\n  # Test: toYamlPretty\n  # Test: fromYamlArray\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n",
            "originalContent": "# Test: Type conversion errors - comprehensive testing of all type conversion functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n  {{- if .Values.testToStrings }}\n  toStringsValue: {{ toStrings $testData | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n  {{- if .Values.testToDecimal }}\n  toDecimalValue: {{ toDecimal $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  {{- if .Values.testToJson }}\n  toJsonValue: {{ toJson $testData | quote }}\n  {{- end }}\n  # Test: mustToJson\n  {{- if .Values.testMustToJson }}\n  mustToJsonValue: {{ mustToJson $testData | quote }}\n  {{- end }}\n  # Test: toPrettyJson\n  {{- if .Values.testToPrettyJson }}\n  toPrettyJsonValue: {{ toPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: mustToPrettyJson\n  {{- if .Values.testMustToPrettyJson }}\n  mustToPrettyJsonValue: {{ mustToPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: toRawJson\n  {{- if .Values.testToRawJson }}\n  toRawJsonValue: {{ toRawJson $testData | quote }}\n  {{- end }}\n  # Test: mustToRawJson\n  {{- if .Values.testMustToRawJson }}\n  mustToRawJsonValue: {{ mustToRawJson $testData | quote }}\n  {{- end }}\n  # Test: fromJson\n  {{- if .Values.testFromJson }}\n  {{- $parsed := $testData | fromJson }}\n  fromJsonValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: fromJsonArray\n  {{- if .Values.testFromJsonArray }}\n  {{- $arr := $testData | fromJsonArray }}\n  fromJsonArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  {{- if .Values.testFromYaml }}\n  {{- $parsed := $testData | fromYaml }}\n  fromYamlValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: toYaml\n  {{- if .Values.testToYaml }}\n  toYamlValue: {{ toYaml $testData | quote }}\n  {{- end }}\n  # Test: toYamlPretty\n  {{- if .Values.testToYamlPretty }}\n  toYamlPrettyValue: {{ toYamlPretty $testData | quote }}\n  {{- end }}\n  # Test: fromYamlArray\n  {{- if .Values.testFromYamlArray }}\n  {{- $arr := $testData | fromYamlArray }}\n  fromYamlArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n  {{- if .Values.testToToml }}\n  toTomlValue: {{ toToml $testData | quote }}\n  {{- end }}\n\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-type-conversion-test\r\ndata:\r\n  # ==========================================\r\n  # String conversion functions\r\n  # ==========================================\r\n  # Test: toStrings\r\n\r\n  # ==========================================\r\n  # Decimal conversion functions\r\n  # ==========================================\r\n  # Test: toDecimal\r\n\r\n  # ==========================================\r\n  # JSON conversion functions\r\n  # ==========================================\r\n  # Test: toJson\r\n  toJsonValue: \"[1,2,3]\"\r\n  # Test: mustToJson\r\n  # Test: toPrettyJson\r\n  # Test: mustToPrettyJson\r\n  # Test: toRawJson\r\n  # Test: mustToRawJson\r\n  # Test: fromJson\r\n  # Test: fromJsonArray\r\n\r\n  # ==========================================\r\n  # YAML conversion functions\r\n  # ==========================================\r\n  # Test: fromYaml\r\n  # Test: toYaml\r\n  # Test: toYamlPretty\r\n  # Test: fromYamlArray\r\n\r\n  # ==========================================\r\n  # TOML conversion functions\r\n  # ==========================================\r\n  # Test: toToml\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-type-conversion/templates/type-conversion-test.yaml",
            "content": "\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  toJsonValue: \"[1,2,3]\"\n  # Test: mustToJson\n  # Test: toPrettyJson\n  # Test: mustToPrettyJson\n  # Test: toRawJson\n  # Test: mustToRawJson\n  # Test: fromJson\n  # Test: fromJsonArray\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  # Test: toYaml\n  # Test: toYamlPretty\n  # Test: fromYamlArray\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n",
            "originalContent": "# Test: Type conversion errors - comprehensive testing of all type conversion functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n  {{- if .Values.testToStrings }}\n  toStringsValue: {{ toStrings $testData | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n  {{- if .Values.testToDecimal }}\n  toDecimalValue: {{ toDecimal $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  {{- if .Values.testToJson }}\n  toJsonValue: {{ toJson $testData | quote }}\n  {{- end }}\n  # Test: mustToJson\n  {{- if .Values.testMustToJson }}\n  mustToJsonValue: {{ mustToJson $testData | quote }}\n  {{- end }}\n  # Test: toPrettyJson\n  {{- if .Values.testToPrettyJson }}\n  toPrettyJsonValue: {{ toPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: mustToPrettyJson\n  {{- if .Values.testMustToPrettyJson }}\n  mustToPrettyJsonValue: {{ mustToPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: toRawJson\n  {{- if .Values.testToRawJson }}\n  toRawJsonValue: {{ toRawJson $testData | quote }}\n  {{- end }}\n  # Test: mustToRawJson\n  {{- if .Values.testMustToRawJson }}\n  mustToRawJsonValue: {{ mustToRawJson $testData | quote }}\n  {{- end }}\n  # Test: fromJson\n  {{- if .Values.testFromJson }}\n  {{- $parsed := $testData | fromJson }}\n  fromJsonValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: fromJsonArray\n  {{- if .Values.testFromJsonArray }}\n  {{- $arr := $testData | fromJsonArray }}\n  fromJsonArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  {{- if .Values.testFromYaml }}\n  {{- $parsed := $testData | fromYaml }}\n  fromYamlValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: toYaml\n  {{- if .Values.testToYaml }}\n  toYamlValue: {{ toYaml $testData | quote }}\n  {{- end }}\n  # Test: toYamlPretty\n  {{- if .Values.testToYamlPretty }}\n  toYamlPrettyValue: {{ toYamlPretty $testData | quote }}\n  {{- end }}\n  # Test: fromYamlArray\n  {{- if .Values.testFromYamlArray }}\n  {{- $arr := $testData | fromYamlArray }}\n  fromYamlArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n  {{- if .Values.testToToml }}\n  toTomlValue: {{ toToml $testData | quote }}\n  {{- end }}\n\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "resolved": true,
      "stopReason": "",
      "fixChain": []
    },
    {
      "toggle": "toJson",
      "dataType": "dictValue",
      "valuesOptions": {
        "values": [
          "testToJson=true",
          "testDataType=dictValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-type-conversion-test\r\ndata:\r\n  # ==========================================\r\n  # String conversion functions\r\n  # ==========================================\r\n  # Test: toStrings\r\n\r\n  # ==========================================\r\n  # Decimal conversion functions\r\n  # ==========================================\r\n  # Test: toDecimal\r\n\r\n  # ==========================================\r\n  # JSON conversion functions\r\n  # ==========================================\r\n  # Test: toJson\r\n  toJsonValue: \"{\\\"key\\\":\\\"value\\\"}\"\r\n  # Test: mustToJson\r\n  # Test: toPrettyJson\r\n  # Test: mustToPrettyJson\r\n  # Test: toRawJson\r\n  # Test: mustToRawJson\r\n  # Test: fromJson\r\n  # Test: fromJsonArray\r\n\r\n  # ==========================================\r\n  # YAML conversion functions\r\n  # ==========================================\r\n  # Test: fromYaml\r\n  # Test: toYaml\r\n  # Test: toYamlPretty\r\n  # Test: fromYamlArray\r\n\r\n  # ==========================================\r\n  # TOML conversion functions\r\n  # ==========================================\r\n  # Test: toToml\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-type-conversion/templates/type-conversion-test.yaml",
            "content": "\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  toJsonValue: \"{\\\"key\\\":\\\"value\\\"}\"\n  # Test: mustToJson\n  # Test: toPrettyJson\n  # Test: mustToPrettyJson\n  # Test: toRawJson\n  # Test: mustToRawJson\n  # Test: fromJson\n  # Test: fromJsonArray\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  # Test: toYaml\n  # Test: toYamlPretty\n  # Test: fromYamlArray\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n",
            "originalContent": "# Test: Type conversion errors - comprehensive testing of all type conversion functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n  {{- if .Values.testToStrings }}\n  toStringsValue: {{ toStrings $testData | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n  {{- if .Values.testToDecimal }}\n  toDecimalValue: {{ toDecimal $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  {{- if .Values.testToJson }}\n  toJsonValue: {{ toJson $testData | quote }}\n  {{- end }}\n  # Test: mustToJson\n  {{- if .Values.testMustToJson }}\n  mustToJsonValue: {{ mustToJson $testData | quote }}\n  {{- end }}\n  # Test: toPrettyJson\n  {{- if .Values.testToPrettyJson }}\n  toPrettyJsonValue: {{ toPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: mustToPrettyJson\n  {{- if .Values.testMustToPrettyJson }}\n  mustToPrettyJsonValue: {{ mustToPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: toRawJson\n  {{- if .Values.testToRawJson }}\n  toRawJsonValue: {{ toRawJson $testData | quote }}\n  {{- end }}\n  # Test: mustToRawJson\n  {{- if .Values.testMustToRawJson }}\n  mustToRawJsonValue: {{ mustToRawJson $testData | quote }}\n  {{- end }}\n  # Test: fromJson\n  {{- if .Values.testFromJson }}\n  {{- $parsed := $testData | fromJson }}\n  fromJsonValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: fromJsonArray\n  {{- if .Values.testFromJsonArray }}\n  {{- $arr := $testData | fromJsonArray }}\n  fromJsonArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  {{- if .Values.testFromYaml }}\n  {{- $parsed := $testData | fromYaml }}\n  fromYamlValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: toYaml\n  {{- if .Values.testToYaml }}\n  toYamlValue: {{ toYaml $testData | quote }}\n  {{- end }}\n  # Test: toYamlPretty\n  {{- if .Values.testToYamlPretty }}\n  toYamlPrettyValue: {{ toYamlPretty $testData | quote }}\n  {{- end }}\n  # Test: fromYamlArray\n  {{- if .Values.testFromYamlArray }}\n  {{- $arr := $testData | fromYamlArray }}\n  fromYamlArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n  {{- if .Values.testToToml }}\n  toTomlValue: {{ toToml $testData | quote }}\n  {{- end }}\n\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-type-conversion-test\r\ndata:\r\n  # ==========================================\r\n  # String conversion functions\r\n  # ==========================================\r\n  # Test: toStrings\r\n\r\n  # ==========================================\r\n  # Decimal conversion functions\r\n  # ==========================================\r\n  # Test: toDecimal\r\n\r\n  # ==========================================\r\n  # JSON conversion functions\r\n  # ==========================================\r\n  # Test: toJson\r\n  toJsonValue: \"{\\\"key\\\":\\\"value\\\"}\"\r\n  # Test: mustToJson\r\n  # Test: toPrettyJson\r\n  # Test: mustToPrettyJson\r\n  # Test: toRawJson\r\n  # Test: mustToRawJson\r\n  # Test: fromJson\r\n  # Test: fromJsonArray\r\n\r\n  # ==========================================\r\n  # YAML conversion functions\r\n  # ==========================================\r\n  # Test: fromYaml\r\n  # Test: toYaml\r\n  # Test: toYamlPretty\r\n  # Test: fromYamlArray\r\n\r\n  # ==========================================\r\n  # TOML conversion functions\r\n  # ==========================================\r\n  # Test: toToml\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-type-conversion/templates/type-conversion-test.yaml",
            "content": "\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  toJsonValue: \"{\\\"key\\\":\\\"value\\\"}\"\n  # Test: mustToJson\n  # Test: toPrettyJson\n  # Test: mustToPrettyJson\n  # Test: toRawJson\n  # Test: mustToRawJson\n  # Test: fromJson\n  # Test: fromJsonArray\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  # Test: toYaml\n  # Test: toYamlPretty\n  # Test: fromYamlArray\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n",
            "originalContent": "# Test: Type conversion errors - comprehensive testing of all type conversion functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n  {{- if .Values.testToStrings }}\n  toStringsValue: {{ toStrings $testData | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n  {{- if .Values.testToDecimal }}\n  toDecimalValue: {{ toDecimal $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  {{- if .Values.testToJson }}\n  toJsonValue: {{ toJson $testData | quote }}\n  {{- end }}\n  # Test: mustToJson\n  {{- if .Values.testMustToJson }}\n  mustToJsonValue: {{ mustToJson $testData | quote }}\n  {{- end }}\n  # Test: toPrettyJson\n  {{- if .Values.testToPrettyJson }}\n  toPrettyJsonValue: {{ toPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: mustToPrettyJson\n  {{- if .Values.testMustToPrettyJson }}\n  mustToPrettyJsonValue: {{ mustToPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: toRawJson\n  {{- if .Values.testToRawJson }}\n  toRawJsonValue: {{ toRawJson $testData | quote }}\n  {{- end }}\n  # Test: mustToRawJson\n  {{- if .Values.testMustToRawJson }}\n  mustToRawJsonValue: {{ mustToRawJson $testData | quote }}\n  {{- end }}\n  # Test: fromJson\n  {{- if .Values.testFromJson }}\n  {{- $parsed := $testData | fromJson }}\n  fromJsonValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: fromJsonArray\n  {{- if .Values.testFromJsonArray }}\n  {{- $arr := $testData | fromJsonArray }}\n  fromJsonArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  {{- if .Values.testFromYaml }}\n  {{- $parsed := $testData | fromYaml }}\n  fromYamlValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: toYaml\n  {{- if .Values.testToYaml }}\n  toYamlValue: {{ toYaml $testData | quote }}\n  {{- end }}\n  # Test: toYamlPretty\n  {{- if .Values.testToYamlPretty }}\n  toYamlPrettyValue: {{ toYamlPretty $testData | quote }}\n  {{- end }}\n  # Test: fromYamlArray\n  {{- if .Values.testFromYamlArray }}\n  {{- $arr := $testData | fromYamlArray }}\n  fromYamlArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n  {{- if .Values.testToToml }}\n  toTomlValue: {{ toToml $testData | quote }}\n  {{- end }}\n\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "resolved": true,
      "stopReason": "",
      "fixChain": []
    },
    {
      "toggle": "mustToJson",
      "dataType": "nilValue",
      "valuesOptions": {
        "values": [
          "testMustToJson=true",
          "testDataType=nilValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-type-conversion-test\r\ndata:\r\n  # ==========================================\r\n  # String conversion functions\r\n  # ==========================================\r\n  # Test: toStrings\r\n\r\n  # ==========================================\r\n  # Decimal conversion functions\r\n  # ==========================================\r\n  # Test: toDecimal\r\n\r\n  # ==========================================\r\n  # JSON conversion functions\r\n  # ==========================================\r\n  # Test: toJson\r\n  # Test: mustToJson\r\n  mustToJsonValue: \"null\"\r\n  # Test: toPrettyJson\r\n  # Test: mustToPrettyJson\r\n  # Test: toRawJson\r\n  # Test: mustToRawJson\r\n  # Test: fromJson\r\n  # Test: fromJsonArray\r\n\r\n  # ==========================================\r\n  # YAML conversion functions\r\n  # ==========================================\r\n  # Test: fromYaml\r\n  # Test: toYaml\r\n  # Test: toYamlPretty\r\n  # Test: fromYamlArray\r\n\r\n  # ==========================================\r\n  # TOML conversion functions\r\n  # ==========================================\r\n  # Test: toToml\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-type-conversion/templates/type-conversion-test.yaml",
            "content": "\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  # Test: mustToJson\n  mustToJsonValue: \"null\"\n  # Test: toPrettyJson\n  # Test: mustToPrettyJson\n  # Test: toRawJson\n  # Test: mustToRawJson\n  # Test: fromJson\n  # Test: fromJsonArray\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  # Test: toYaml\n  # Test: toYamlPretty\n  # Test: fromYamlArray\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n",
            "originalContent": "# Test: Type conversion errors - comprehensive testing of all type conversion functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n  {{- if .Values.testToStrings }}\n  toStringsValue: {{ toStrings $testData | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n  {{- if .Values.testToDecimal }}\n  toDecimalValue: {{ toDecimal $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  {{- if .Values.testToJson }}\n  toJsonValue: {{ toJson $testData | quote }}\n  {{- end }}\n  # Test: mustToJson\n  {{- if .Values.testMustToJson }}\n  mustToJsonValue: {{ mustToJson $testData | quote }}\n  {{- end }}\n  # Test: toPrettyJson\n  {{- if .Values.testToPrettyJson }}\n  toPrettyJsonValue: {{ toPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: mustToPrettyJson\n  {{- if .Values.testMustToPrettyJson }}\n  mustToPrettyJsonValue: {{ mustToPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: toRawJson\n  {{- if .Values.testToRawJson }}\n  toRawJsonValue: {{ toRawJson $testData | quote }}\n  {{- end }}\n  # Test: mustToRawJson\n  {{- if .Values.testMustToRawJson }}\n  mustToRawJsonValue: {{ mustToRawJson $testData | quote }}\n  {{- end }}\n  # Test: fromJson\n  {{- if .Values.testFromJson }}\n  {{- $parsed := $testData | fromJson }}\n  fromJsonValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: fromJsonArray\n  {{- if .Values.testFromJsonArray }}\n  {{- $arr := $testData | fromJsonArray }}\n  fromJsonArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  {{- if .Values.testFromYaml }}\n  {{- $parsed := $testData | fromYaml }}\n  fromYamlValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: toYaml\n  {{- if .Values.testToYaml }}\n  toYamlValue: {{ toYaml $testData | quote }}\n  {{- end }}\n  # Test: toYamlPretty\n  {{- if .Values.testToYamlPretty }}\n  toYamlPrettyValue: {{ toYamlPretty $testData | quote }}\n  {{- end }}\n  # Test: fromYamlArray\n  {{- if .Values.testFromYamlArray }}\n  {{- $arr := $testData | fromYamlArray }}\n  fromYamlArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n  {{- if .Values.testToToml }}\n  toTomlValue: {{ toToml $testData | quote }}\n  {{- end }}\n\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-type-conversion-test\r\ndata:\r\n  # ==========================================\r\n  # String conversion functions\r\n  # ==========================================\r\n  # Test: toStrings\r\n\r\n  # ==========================================\r\n  # Decimal conversion functions\r\n  # ==========================================\r\n  # Test: toDecimal\r\n\r\n  # ==========================================\r\n  # JSON conversion functions\r\n  # ==========================================\r\n  # Test: toJson\r\n  # Test: mustToJson\r\n  mustToJsonValue: \"null\"\r\n  # Test: toPrettyJson\r\n  # Test: mustToPrettyJson\r\n  # Test: toRawJson\r\n  # Test: mustToRawJson\r\n  # Test: fromJson\r\n  # Test: fromJsonArray\r\n\r\n  # ==========================================\r\n  # YAML conversion functions\r\n  # ==========================================\r\n  # Test: fromYaml\r\n  # Test: toYaml\r\n  # Test: toYamlPretty\r\n  # Test: fromYamlArray\r\n\r\n  # ==========================================\r\n  # TOML conversion functions\r\n  # ==========================================\r\n  # Test: toToml\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-type-conversion/templates/type-conversion-test.yaml",
            "content": "\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  # Test: mustToJson\n  mustToJsonValue: \"null\"\n  # Test: toPrettyJson\n  # Test: mustToPrettyJson\n  # Test: toRawJson\n  # Test: mustToRawJson\n  # Test: fromJson\n  # Test: fromJsonArray\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  # Test: toYaml\n  # Test: toYamlPretty\n  # Test: fromYamlArray\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n",
            "originalContent": "# Test: Type conversion errors - comprehensive testing of all type conversion functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n  {{- if .Values.testToStrings }}\n  toStringsValue: {{ toStrings $testData | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n  {{- if .Values.testToDecimal }}\n  toDecimalValue: {{ toDecimal $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  {{- if .Values.testToJson }}\n  toJsonValue: {{ toJson $testData | quote }}\n  {{- end }}\n  # Test: mustToJson\n  {{- if .Values.testMustToJson }}\n  mustToJsonValue: {{ mustToJson $testData | quote }}\n  {{- end }}\n  # Test: toPrettyJson\n  {{- if .Values.testToPrettyJson }}\n  toPrettyJsonValue: {{ toPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: mustToPrettyJson\n  {{- if .Values.testMustToPrettyJson }}\n  mustToPrettyJsonValue: {{ mustToPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: toRawJson\n  {{- if .Values.testToRawJson }}\n  toRawJsonValue: {{ toRawJson $testData | quote }}\n  {{- end }}\n  # Test: mustToRawJson\n  {{- if .Values.testMustToRawJson }}\n  mustToRawJsonValue: {{ mustToRawJson $testData | quote }}\n  {{- end }}\n  # Test: fromJson\n  {{- if .Values.testFromJson }}\n  {{- $parsed := $testData | fromJson }}\n  fromJsonValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: fromJsonArray\n  {{- if .Values.testFromJsonArray }}\n  {{- $arr := $testData | fromJsonArray }}\n  fromJsonArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  {{- if .Values.testFromYaml }}\n  {{- $parsed := $testData | fromYaml }}\n  fromYamlValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: toYaml\n  {{- if .Values.testToYaml }}\n  toYamlValue: {{ toYaml $testData | quote }}\n  {{- end }}\n  # Test: toYamlPretty\n  {{- if .Values.testToYamlPretty }}\n  toYamlPrettyValue: {{ toYamlPretty $testData | quote }}\n  {{- end }}\n  # Test: fromYamlArray\n  {{- if .Values.testFromYamlArray }}\n  {{- $arr := $testData | fromYamlArray }}\n  fromYamlArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n  {{- if .Values.testToToml }}\n  toTomlValue: {{ toToml $testData | quote }}\n  {{- end }}\n\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "resolved": true,
      "stopReason": "",
      "fixChain": []
    },
    {
      "toggle": "mustToJson",
      "dataType": "stringValue",
      "valuesOptions": {
        "values": [
          "testMustToJson=true",
          "testDataType=stringValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-type-conversion-test\r\ndata:\r\n  # ==========================================\r\n  # String conversion functions\r\n  # ==========================================\r\n  # Test: toStrings\r\n\r\n  # ==========================================\r\n  # Decimal conversion functions\r\n  # ==========================================\r\n  # Test: toDecimal\r\n\r\n  # ==========================================\r\n  # JSON conversion functions\r\n  # ==========================================\r\n  # Test: toJson\r\n  # Test: mustToJson\r\n  mustToJsonValue: \"\\\"test string\\\"\"\r\n  # Test: toPrettyJson\r\n  # Test: mustToPrettyJson\r\n  # Test: toRawJson\r\n  # Test: mustToRawJson\r\n  # Test: fromJson\r\n  # Test: fromJsonArray\r\n\r\n  # ==========================================\r\n  # YAML conversion functions\r\n  # ==========================================\r\n  # Test: fromYaml\r\n  # Test: toYaml\r\n  # Test: toYamlPretty\r\n  # Test: fromYamlArray\r\n\r\n  # ==========================================\r\n  # TOML conversion functions\r\n  # ==========================================\r\n  # Test: toToml\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-type-conversion/templates/type-conversion-test.yaml",
            "content": "\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  # Test: mustToJson\n  mustToJsonValue: \"\\\"test string\\\"\"\n  # Test: toPrettyJson\n  # Test: mustToPrettyJson\n  # Test: toRawJson\n  # Test: mustToRawJson\n  # Test: fromJson\n  # Test: fromJsonArray\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  # Test: toYaml\n  # Test: toYamlPretty\n  # Test: fromYamlArray\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n",
            "originalContent": "# Test: Type conversion errors - comprehensive testing of all type conversion functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n  {{- if .Values.testToStrings }}\n  toStringsValue: {{ toStrings $testData | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n  {{- if .Values.testToDecimal }}\n  toDecimalValue: {{ toDecimal $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  {{- if .Values.testToJson }}\n  toJsonValue: {{ toJson $testData | quote }}\n  {{- end }}\n  # Test: mustToJson\n  {{- if .Values.testMustToJson }}\n  mustToJsonValue: {{ mustToJson $testData | quote }}\n  {{- end }}\n  # Test: toPrettyJson\n  {{- if .Values.testToPrettyJson }}\n  toPrettyJsonValue: {{ toPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: mustToPrettyJson\n  {{- if .Values.testMustToPrettyJson }}\n  mustToPrettyJsonValue: {{ mustToPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: toRawJson\n  {{- if .Values.testToRawJson }}\n  toRawJsonValue: {{ toRawJson $testData | quote }}\n  {{- end }}\n  # Test: mustToRawJson\n  {{- if .Values.testMustToRawJson }}\n  mustToRawJsonValue: {{ mustToRawJson $testData | quote }}\n  {{- end }}\n  # Test: fromJson\n  {{- if .Values.testFromJson }}\n  {{- $parsed := $testData | fromJson }}\n  fromJsonValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: fromJsonArray\n  {{- if .Values.testFromJsonArray }}\n  {{- $arr := $testData | fromJsonArray }}\n  fromJsonArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  {{- if .Values.testFromYaml }}\n  {{- $parsed := $testData | fromYaml }}\n  fromYamlValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: toYaml\n  {{- if .Values.testToYaml }}\n  toYamlValue: {{ toYaml $testData | quote }}\n  {{- end }}\n  # Test: toYamlPretty\n  {{- if .Values.testToYamlPretty }}\n  toYamlPrettyValue: {{ toYamlPretty $testData | quote }}\n  {{- end }}\n  # Test: fromYamlArray\n  {{- if .Values.testFromYamlArray }}\n  {{- $arr := $testData | fromYamlArray }}\n  fromYamlArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n  {{- if .Values.testToToml }}\n  toTomlValue: {{ toToml $testData | quote }}\n  {{- end }}\n\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-type-conversion-test\r\ndata:\r\n  # ==========================================\r\n  # String conversion functions\r\n  # ==========================================\r\n  # Test: toStrings\r\n\r\n  # ==========================================\r\n  # Decimal conversion functions\r\n  # ==========================================\r\n  # Test: toDecimal\r\n\r\n  # ==========================================\r\n  # JSON conversion functions\r\n  # ==========================================\r\n  # Test: toJson\r\n  # Test: mustToJson\r\n  mustToJsonValue: \"\\\"test string\\\"\"\r\n  # Test: toPrettyJson\r\n  # Test: mustToPrettyJson\r\n  # Test: toRawJson\r\n  # Test: mustToRawJson\r\n  # Test: fromJson\r\n  # Test: fromJsonArray\r\n\r\n  # ==========================================\r\n  # YAML conversion functions\r\n  # ==========================================\r\n  # Test: fromYaml\r\n  # Test: toYaml\r\n  # Test: toYamlPretty\r\n  # Test: fromYamlArray\r\n\r\n  # ==========================================\r\n  # TOML conversion functions\r\n  # ==========================================\r\n  # Test: toToml\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-type-conversion/templates/type-conversion-test.yaml",
            "content": "\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  # Test: mustToJson\n  mustToJsonValue: \"\\\"test string\\\"\"\n  # Test: toPrettyJson\n  # Test: mustToPrettyJson\n  # Test: toRawJson\n  # Test: mustToRawJson\n  # Test: fromJson\n  # Test: fromJsonArray\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  # Test: toYaml\n  # Test: toYamlPretty\n  # Test: fromYamlArray\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n",
            "originalContent": "# Test: Type conversion errors - comprehensive testing of all type conversion functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n  {{- if .Values.testToStrings }}\n  toStringsValue: {{ toStrings $testData | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n  {{- if .Values.testToDecimal }}\n  toDecimalValue: {{ toDecimal $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  {{- if .Values.testToJson }}\n  toJsonValue: {{ toJson $testData | quote }}\n  {{- end }}\n  # Test: mustToJson\n  {{- if .Values.testMustToJson }}\n  mustToJsonValue: {{ mustToJson $testData | quote }}\n  {{- end }}\n  # Test: toPrettyJson\n  {{- if .Values.testToPrettyJson }}\n  toPrettyJsonValue: {{ toPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: mustToPrettyJson\n  {{- if .Values.testMustToPrettyJson }}\n  mustToPrettyJsonValue: {{ mustToPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: toRawJson\n  {{- if .Values.testToRawJson }}\n  toRawJsonValue: {{ toRawJson $testData | quote }}\n  {{- end }}\n  # Test: mustToRawJson\n  {{- if .Values.testMustToRawJson }}\n  mustToRawJsonValue: {{ mustToRawJson $testData | quote }}\n  {{- end }}\n  # Test: fromJson\n  {{- if .Values.testFromJson }}\n  {{- $parsed := $testData | fromJson }}\n  fromJsonValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: fromJsonArray\n  {{- if .Values.testFromJsonArray }}\n  {{- $arr := $testData | fromJsonArray }}\n  fromJsonArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  {{- if .Values.testFromYaml }}\n  {{- $parsed := $testData | fromYaml }}\n  fromYamlValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: toYaml\n  {{- if .Values.testToYaml }}\n  toYamlValue: {{ toYaml $testData | quote }}\n  {{- end }}\n  # Test: toYamlPretty\n  {{- if .Values.testToYamlPretty }}\n  toYamlPrettyValue: {{ toYamlPretty $testData | quote }}\n  {{- end }}\n  # Test: fromYamlArray\n  {{- if .Values.testFromYamlArray }}\n  {{- $arr := $testData | fromYamlArray }}\n  fromYamlArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n  {{- if .Values.testToToml }}\n  toTomlValue: {{ toToml $testData | quote }}\n  {{- end }}\n\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "resolved": true,
      "stopReason": "",
      "fixChain": []
    },
    {
      "toggle": "mustToJson",
      "dataType": "numberValue",
      "valuesOptions": {
        "values": [
          "testMustToJson=true",
          "testDataType=numberValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-type-conversion-test\r\ndata:\r\n  # ==========================================\r\n  # String conversion functions\r\n  # ==========================================\r\n  # Test: toStrings\r\n\r\n  # ==========================================\r\n  # Decimal conversion functions\r\n  # ==========================================\r\n  # Test: toDecimal\r\n\r\n  # ==========================================\r\n  # JSON conversion functions\r\n  # ==========================================\r\n  # Test: toJson\r\n  # Test: mustToJson\r\n  mustToJsonValue: \"42\"\r\n  # Test: toPrettyJson\r\n  # Test: mustToPrettyJson\r\n  # Test: toRawJson\r\n  # Test: mustToRawJson\r\n  # Test: fromJson\r\n  # Test: fromJsonArray\r\n\r\n  # ==========================================\r\n  # YAML conversion functions\r\n  # ==========================================\r\n  # Test: fromYaml\r\n  # Test: toYaml\r\n  # Test: toYamlPretty\r\n  # Test: fromYamlArray\r\n\r\n  # ==========================================\r\n  # TOML conversion functions\r\n  # ==========================================\r\n  # Test: toToml\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-type-conversion/templates/type-conversion-test.yaml",
            "content": "\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  # Test: mustToJson\n  mustToJsonValue: \"42\"\n  # Test: toPrettyJson\n  # Test: mustToPrettyJson\n  # Test: toRawJson\n  # Test: mustToRawJson\n  # Test: fromJson\n  # Test: fromJsonArray\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  # Test: toYaml\n  # Test: toYamlPretty\n  # Test: fromYamlArray\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n",
            "originalContent": "# Test: Type conversion errors - comprehensive testing of all type conversion functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n  {{- if .Values.testToStrings }}\n  toStringsValue: {{ toStrings $testData | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n  {{- if .Values.testToDecimal }}\n  toDecimalValue: {{ toDecimal $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  {{- if .Values.testToJson }}\n  toJsonValue: {{ toJson $testData | quote }}\n  {{- end }}\n  # Test: mustToJson\n  {{- if .Values.testMustToJson }}\n  mustToJsonValue: {{ mustToJson $testData | quote }}\n  {{- end }}\n  # Test: toPrettyJson\n  {{- if .Values.testToPrettyJson }}\n  toPrettyJsonValue: {{ toPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: mustToPrettyJson\n  {{- if .Values.testMustToPrettyJson }}\n  mustToPrettyJsonValue: {{ mustToPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: toRawJson\n  {{- if .Values.testToRawJson }}\n  toRawJsonValue: {{ toRawJson $testData | quote }}\n  {{- end }}\n  # Test: mustToRawJson\n  {{- if .Values.testMustToRawJson }}\n  mustToRawJsonValue: {{ mustToRawJson $testData | quote }}\n  {{- end }}\n  # Test: fromJson\n  {{- if .Values.testFromJson }}\n  {{- $parsed := $testData | fromJson }}\n  fromJsonValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: fromJsonArray\n  {{- if .Values.testFromJsonArray }}\n  {{- $arr := $testData | fromJsonArray }}\n  fromJsonArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  {{- if .Values.testFromYaml }}\n  {{- $parsed := $testData | fromYaml }}\n  fromYamlValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: toYaml\n  {{- if .Values.testToYaml }}\n  toYamlValue: {{ toYaml $testData | quote }}\n  {{- end }}\n  # Test: toYamlPretty\n  {{- if .Values.testToYamlPretty }}\n  toYamlPrettyValue: {{ toYamlPretty $testData | quote }}\n  {{- end }}\n  # Test: fromYamlArray\n  {{- if .Values.testFromYamlArray }}\n  {{- $arr := $testData | fromYamlArray }}\n  fromYamlArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n  {{- if .Values.testToToml }}\n  toTomlValue: {{ toToml $testData | quote }}\n  {{- end }}\n\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-type-conversion-test\r\ndata:\r\n  # ==========================================\r\n  # String conversion functions\r\n  # ==========================================\r\n  # Test: toStrings\r\n\r\n  # ==========================================\r\n  # Decimal conversion functions\r\n  # ==========================================\r\n  # Test: toDecimal\r\n\r\n  # ==========================================\r\n  # JSON conversion functions\r\n  # ==========================================\r\n  # Test: toJson\r\n  # Test: mustToJson\r\n  mustToJsonValue: \"42\"\r\n  # Test: toPrettyJson\r\n  # Test: mustToPrettyJson\r\n  # Test: toRawJson\r\n  # Test: mustToRawJson\r\n  # Test: fromJson\r\n  # Test: fromJsonArray\r\n\r\n  # ==========================================\r\n  # YAML conversion functions\r\n  # ==========================================\r\n  # Test: fromYaml\r\n  # Test: toYaml\r\n  # Test: toYamlPretty\r\n  # Test: fromYamlArray\r\n\r\n  # ==========================================\r\n  # TOML conversion functions\r\n  # ==========================================\r\n  # Test: toToml\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-type-conversion/templates/type-conversion-test.yaml",
            "content": "\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  # Test: mustToJson\n  mustToJsonValue: \"42\"\n  # Test: toPrettyJson\n  # Test: mustToPrettyJson\n  # Test: toRawJson\n  # Test: mustToRawJson\n  # Test: fromJson\n  # Test: fromJsonArray\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  # Test: toYaml\n  # Test: toYamlPretty\n  # Test: fromYamlArray\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n",
            "originalContent": "# Test: Type conversion errors - comprehensive testing of all type conversion functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n  {{- if .Values.testToStrings }}\n  toStringsValue: {{ toStrings $testData | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n  {{- if .Values.testToDecimal }}\n  toDecimalValue: {{ toDecimal $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  {{- if .Values.testToJson }}\n  toJsonValue: {{ toJson $testData | quote }}\n  {{- end }}\n  # Test: mustToJson\n  {{- if .Values.testMustToJson }}\n  mustToJsonValue: {{ mustToJson $testData | quote }}\n  {{- end }}\n  # Test: toPrettyJson\n  {{- if .Values.testToPrettyJson }}\n  toPrettyJsonValue: {{ toPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: mustToPrettyJson\n  {{- if .Values.testMustToPrettyJson }}\n  mustToPrettyJsonValue: {{ mustToPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: toRawJson\n  {{- if .Values.testToRawJson }}\n  toRawJsonValue: {{ toRawJson $testData | quote }}\n  {{- end }}\n  # Test: mustToRawJson\n  {{- if .Values.testMustToRawJson }}\n  mustToRawJsonValue: {{ mustToRawJson $testData | quote }}\n  {{- end }}\n  # Test: fromJson\n  {{- if .Values.testFromJson }}\n  {{- $parsed := $testData | fromJson }}\n  fromJsonValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: fromJsonArray\n  {{- if .Values.testFromJsonArray }}\n  {{- $arr := $testData | fromJsonArray }}\n  fromJsonArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  {{- if .Values.testFromYaml }}\n  {{- $parsed := $testData | fromYaml }}\n  fromYamlValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: toYaml\n  {{- if .Values.testToYaml }}\n  toYamlValue: {{ toYaml $testData | quote }}\n  {{- end }}\n  # Test: toYamlPretty\n  {{- if .Values.testToYamlPretty }}\n  toYamlPrettyValue: {{ toYamlPretty $testData | quote }}\n  {{- end }}\n  # Test: fromYamlArray\n  {{- if .Values.testFromYamlArray }}\n  {{- $arr := $testData | fromYamlArray }}\n  fromYamlArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n  {{- if .Values.testToToml }}\n  toTomlValue: {{ toToml $testData | quote }}\n  {{- end }}\n\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "resolved": true,
      "stopReason": "",
      "fixChain": []
    },
    {
      "toggle": "mustToJson",
      "dataType": "boolValue",
      "valuesOptions": {
        "values": [
          "testMustToJson=true",
          "testDataType=boolValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-type-conversion-test\r\ndata:\r\n  # ==========================================\r\n  # String conversion functions\r\n  # ==========================================\r\n  # Test: toStrings\r\n\r\n  # ==========================================\r\n  # Decimal conversion functions\r\n  # ==========================================\r\n  # Test: toDecimal\r\n\r\n  # ==========================================\r\n  # JSON conversion functions\r\n  # ==========================================\r\n  # Test: toJson\r\n  # Test: mustToJson\r\n  mustToJsonValue: \"true\"\r\n  # Test: toPrettyJson\r\n  # Test: mustToPrettyJson\r\n  # Test: toRawJson\r\n  # Test: mustToRawJson\r\n  # Test: fromJson\r\n  # Test: fromJsonArray\r\n\r\n  # ==========================================\r\n  # YAML conversion functions\r\n  # ==========================================\r\n  # Test: fromYaml\r\n  # Test: toYaml\r\n  # Test: toYamlPretty\r\n  # Test: fromYamlArray\r\n\r\n  # ==========================================\r\n  # TOML conversion functions\r\n  # ==========================================\r\n  # Test: toToml\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-type-conversion/templates/type-conversion-test.yaml",
            "content": "\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  # Test: mustToJson\n  mustToJsonValue: \"true\"\n  # Test: toPrettyJson\n  # Test: mustToPrettyJson\n  # Test: toRawJson\n  # Test: mustToRawJson\n  # Test: fromJson\n  # Test: fromJsonArray\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  # Test: toYaml\n  # Test: toYamlPretty\n  # Test: fromYamlArray\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n",
            "originalContent": "# Test: Type conversion errors - comprehensive testing of all type conversion functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n  {{- if .Values.testToStrings }}\n  toStringsValue: {{ toStrings $testData | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n  {{- if .Values.testToDecimal }}\n  toDecimalValue: {{ toDecimal $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  {{- if .Values.testToJson }}\n  toJsonValue: {{ toJson $testData | quote }}\n  {{- end }}\n  # Test: mustToJson\n  {{- if .Values.testMustToJson }}\n  mustToJsonValue: {{ mustToJson $testData | quote }}\n  {{- end }}\n  # Test: toPrettyJson\n  {{- if .Values.testToPrettyJson }}\n  toPrettyJsonValue: {{ toPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: mustToPrettyJson\n  {{- if .Values.testMustToPrettyJson }}\n  mustToPrettyJsonValue: {{ mustToPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: toRawJson\n  {{- if .Values.testToRawJson }}\n  toRawJsonValue: {{ toRawJson $testData | quote }}\n  {{- end }}\n  # Test: mustToRawJson\n  {{- if .Values.testMustToRawJson }}\n  mustToRawJsonValue: {{ mustToRawJson $testData | quote }}\n  {{- end }}\n  # Test: fromJson\n  {{- if .Values.testFromJson }}\n  {{- $parsed := $testData | fromJson }}\n  fromJsonValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: fromJsonArray\n  {{- if .Values.testFromJsonArray }}\n  {{- $arr := $testData | fromJsonArray }}\n  fromJsonArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  {{- if .Values.testFromYaml }}\n  {{- $parsed := $testData | fromYaml }}\n  fromYamlValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: toYaml\n  {{- if .Values.testToYaml }}\n  toYamlValue: {{ toYaml $testData | quote }}\n  {{- end }}\n  # Test: toYamlPretty\n  {{- if .Values.testToYamlPretty }}\n  toYamlPrettyValue: {{ toYamlPretty $testData | quote }}\n  {{- end }}\n  # Test: fromYamlArray\n  {{- if .Values.testFromYamlArray }}\n  {{- $arr := $testData | fromYamlArray }}\n  fromYamlArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n  {{- if .Values.testToToml }}\n  toTomlValue: {{ toToml $testData | quote }}\n  {{- end }}\n\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-type-conversion-test\r\ndata:\r\n  # ==========================================\r\n  # String conversion functions\r\n  # ==========================================\r\n  # Test: toStrings\r\n\r\n  # ==========================================\r\n  # Decimal conversion functions\r\n  # ==========================================\r\n  # Test: toDecimal\r\n\r\n  # ==========================================\r\n  # JSON conversion functions\r\n  # ==========================================\r\n  # Test: toJson\r\n  # Test: mustToJson\r\n  mustToJsonValue: \"true\"\r\n  # Test: toPrettyJson\r\n  # Test: mustToPrettyJson\r\n  # Test: toRawJson\r\n  # Test: mustToRawJson\r\n  # Test: fromJson\r\n  # Test: fromJsonArray\r\n\r\n  # ==========================================\r\n  # YAML conversion functions\r\n  # ==========================================\r\n  # Test: fromYaml\r\n  # Test: toYaml\r\n  # Test: toYamlPretty\r\n  # Test: fromYamlArray\r\n\r\n  # ==========================================\r\n  # TOML conversion functions\r\n  # ==========================================\r\n  # Test: toToml\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-type-conversion/templates/type-conversion-test.yaml",
            "content": "\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  # Test: mustToJson\n  mustToJsonValue: \"true\"\n  # Test: toPrettyJson\n  # Test: mustToPrettyJson\n  # Test: toRawJson\n  # Test: mustToRawJson\n  # Test: fromJson\n  # Test: fromJsonArray\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  # Test: toYaml\n  # Test: toYamlPretty\n  # Test: fromYamlArray\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n",
            "originalContent": "# Test: Type conversion errors - comprehensive testing of all type conversion functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n  {{- if .Values.testToStrings }}\n  toStringsValue: {{ toStrings $testData | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n  {{- if .Values.testToDecimal }}\n  toDecimalValue: {{ toDecimal $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  {{- if .Values.testToJson }}\n  toJsonValue: {{ toJson $testData | quote }}\n  {{- end }}\n  # Test: mustToJson\n  {{- if .Values.testMustToJson }}\n  mustToJsonValue: {{ mustToJson $testData | quote }}\n  {{- end }}\n  # Test: toPrettyJson\n  {{- if .Values.testToPrettyJson }}\n  toPrettyJsonValue: {{ toPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: mustToPrettyJson\n  {{- if .Values.testMustToPrettyJson }}\n  mustToPrettyJsonValue: {{ mustToPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: toRawJson\n  {{- if .Values.testToRawJson }}\n  toRawJsonValue: {{ toRawJson $testData | quote }}\n  {{- end }}\n  # Test: mustToRawJson\n  {{- if .Values.testMustToRawJson }}\n  mustToRawJsonValue: {{ mustToRawJson $testData | quote }}\n  {{- end }}\n  # Test: fromJson\n  {{- if .Values.testFromJson }}\n  {{- $parsed := $testData | fromJson }}\n  fromJsonValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: fromJsonArray\n  {{- if .Values.testFromJsonArray }}\n  {{- $arr := $testData | fromJsonArray }}\n  fromJsonArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  {{- if .Values.testFromYaml }}\n  {{- $parsed := $testData | fromYaml }}\n  fromYamlValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: toYaml\n  {{- if .Values.testToYaml }}\n  toYamlValue: {{ toYaml $testData | quote }}\n  {{- end }}\n  # Test: toYamlPretty\n  {{- if .Values.testToYamlPretty }}\n  toYamlPrettyValue: {{ toYamlPretty $testData | quote }}\n  {{- end }}\n  # Test: fromYamlArray\n  {{- if .Values.testFromYamlArray }}\n  {{- $arr := $testData | fromYamlArray }}\n  fromYamlArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n  {{- if .Values.testToToml }}\n  toTomlValue: {{ toToml $testData | quote }}\n  {{- end }}\n\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "resolved": true,
      "stopReason": "",
      "fixChain": []
    },
    {
      "toggle": "mustToJson",
      "dataType": "listValue",
      "valuesOptions": {
        "values": [
          "testMustToJson=true",
          "testDataType=listValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-type-conversion-test\r\ndata:\r\n  # ==========================================\r\n  # String conversion functions\r\n  # ==========================================\r\n  # Test: toStrings\r\n\r\n  # ==========================================\r\n  # Decimal conversion functions\r\n  # ==========================================\r\n  # Test: toDecimal\r\n\r\n  # ==========================================\r\n  # JSON conversion functions\r\n  # ==========================================\r\n  # Test: toJson\r\n  # Test: mustToJson\r\n  mustToJsonValue: \"[1,2,3]\"\r\n  # Test: toPrettyJson\r\n  # Test: mustToPrettyJson\r\n  # Test: toRawJson\r\n  # Test: mustToRawJson\r\n  # Test: fromJson\r\n  # Test: fromJsonArray\r\n\r\n  # ==========================================\r\n  # YAML conversion functions\r\n  # ==========================================\r\n  # Test: fromYaml\r\n  # Test: toYaml\r\n  # Test: toYamlPretty\r\n  # Test: fromYamlArray\r\n\r\n  # ==========================================\r\n  # TOML conversion functions\r\n  # ==========================================\r\n  # Test: toToml\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-type-conversion/templates/type-conversion-test.yaml",
            "content": "\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  # Test: mustToJson\n  mustToJsonValue: \"[1,2,3]\"\n  # Test: toPrettyJson\n  # Test: mustToPrettyJson\n  # Test: toRawJson\n  # Test: mustToRawJson\n  # Test: fromJson\n  # Test: fromJsonArray\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  # Test: toYaml\n  # Test: toYamlPretty\n  # Test: fromYamlArray\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n",
            "originalContent": "# Test: Type conversion errors - comprehensive testing of all type conversion functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n  {{- if .Values.testToStrings }}\n  toStringsValue: {{ toStrings $testData | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n  {{- if .Values.testToDecimal }}\n  toDecimalValue: {{ toDecimal $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  {{- if .Values.testToJson }}\n  toJsonValue: {{ toJson $testData | quote }}\n  {{- end }}\n  # Test: mustToJson\n  {{- if .Values.testMustToJson }}\n  mustToJsonValue: {{ mustToJson $testData | quote }}\n  {{- end }}\n  # Test: toPrettyJson\n  {{- if .Values.testToPrettyJson }}\n  toPrettyJsonValue: {{ toPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: mustToPrettyJson\n  {{- if .Values.testMustToPrettyJson }}\n  mustToPrettyJsonValue: {{ mustToPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: toRawJson\n  {{- if .Values.testToRawJson }}\n  toRawJsonValue: {{ toRawJson $testData | quote }}\n  {{- end }}\n  # Test: mustToRawJson\n  {{- if .Values.testMustToRawJson }}\n  mustToRawJsonValue: {{ mustToRawJson $testData | quote }}\n  {{- end }}\n  # Test: fromJson\n  {{- if .Values.testFromJson }}\n  {{- $parsed := $testData | fromJson }}\n  fromJsonValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: fromJsonArray\n  {{- if .Values.testFromJsonArray }}\n  {{- $arr := $testData | fromJsonArray }}\n  fromJsonArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  {{- if .Values.testFromYaml }}\n  {{- $parsed := $testData | fromYaml }}\n  fromYamlValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: toYaml\n  {{- if .Values.testToYaml }}\n  toYamlValue: {{ toYaml $testData | quote }}\n  {{- end }}\n  # Test: toYamlPretty\n  {{- if .Values.testToYamlPretty }}\n  toYamlPrettyValue: {{ toYamlPretty $testData | quote }}\n  {{- end }}\n  # Test: fromYamlArray\n  {{- if .Values.testFromYamlArray }}\n  {{- $arr := $testData | fromYamlArray }}\n  fromYamlArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n  {{- if .Values.testToToml }}\n  toTomlValue: {{ toToml $testData | quote }}\n  {{- end }}\n\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-type-conversion-test\r\ndata:\r\n  # ==========================================\r\n  # String conversion functions\r\n  # ==========================================\r\n  # Test: toStrings\r\n\r\n  # ==========================================\r\n  # Decimal conversion functions\r\n  # ==========================================\r\n  # Test: toDecimal\r\n\r\n  # ==========================================\r\n  # JSON conversion functions\r\n  # ==========================================\r\n  # Test: toJson\r\n  # Test: mustToJson\r\n  mustToJsonValue: \"[1,2,3]\"\r\n  # Test: toPrettyJson\r\n  # Test: mustToPrettyJson\r\n  # Test: toRawJson\r\n  # Test: mustToRawJson\r\n  # Test: fromJson\r\n  # Test: fromJsonArray\r\n\r\n  # ==========================================\r\n  # YAML conversion functions\r\n  # ==========================================\r\n  # Test: fromYaml\r\n  # Test: toYaml\r\n  # Test: toYamlPretty\r\n  # Test: fromYamlArray\r\n\r\n  # ==========================================\r\n  # TOML conversion functions\r\n  # ==========================================\r\n  # Test: toToml\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-type-conversion/templates/type-conversion-test.yaml",
            "content": "\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  # Test: mustToJson\n  mustToJsonValue: \"[1,2,3]\"\n  # Test: toPrettyJson\n  # Test: mustToPrettyJson\n  # Test: toRawJson\n  # Test: mustToRawJson\n  # Test: fromJson\n  # Test: fromJsonArray\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  # Test: toYaml\n  # Test: toYamlPretty\n  # Test: fromYamlArray\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n",
            "originalContent": "# Test: Type conversion errors - comprehensive testing of all type conversion functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n  {{- if .Values.testToStrings }}\n  toStringsValue: {{ toStrings $testData | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n  {{- if .Values.testToDecimal }}\n  toDecimalValue: {{ toDecimal $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  {{- if .Values.testToJson }}\n  toJsonValue: {{ toJson $testData | quote }}\n  {{- end }}\n  # Test: mustToJson\n  {{- if .Values.testMustToJson }}\n  mustToJsonValue: {{ mustToJson $testData | quote }}\n  {{- end }}\n  # Test: toPrettyJson\n  {{- if .Values.testToPrettyJson }}\n  toPrettyJsonValue: {{ toPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: mustToPrettyJson\n  {{- if .Values.testMustToPrettyJson }}\n  mustToPrettyJsonValue: {{ mustToPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: toRawJson\n  {{- if .Values.testToRawJson }}\n  toRawJsonValue: {{ toRawJson $testData | quote }}\n  {{- end }}\n  # Test: mustToRawJson\n  {{- if .Values.testMustToRawJson }}\n  mustToRawJsonValue: {{ mustToRawJson $testData | quote }}\n  {{- end }}\n  # Test: fromJson\n  {{- if .Values.testFromJson }}\n  {{- $parsed := $testData | fromJson }}\n  fromJsonValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: fromJsonArray\n  {{- if .Values.testFromJsonArray }}\n  {{- $arr := $testData | fromJsonArray }}\n  fromJsonArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  {{- if .Values.testFromYaml }}\n  {{- $parsed := $testData | fromYaml }}\n  fromYamlValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: toYaml\n  {{- if .Values.testToYaml }}\n  toYamlValue: {{ toYaml $testData | quote }}\n  {{- end }}\n  # Test: toYamlPretty\n  {{- if .Values.testToYamlPretty }}\n  toYamlPrettyValue: {{ toYamlPretty $testData | quote }}\n  {{- end }}\n  # Test: fromYamlArray\n  {{- if .Values.testFromYamlArray }}\n  {{- $arr := $testData | fromYamlArray }}\n  fromYamlArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n  {{- if .Values.testToToml }}\n  toTomlValue: {{ toToml $testData | quote }}\n  {{- end }}\n\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "resolved": true,
      "stopReason": "",
      "fixChain": []
    },
    {
      "toggle": "mustToJson",
      "dataType": "dictValue",
      "valuesOptions": {
        "values": [
          "testMustToJson=true",
          "testDataType=dictValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-type-conversion-test\r\ndata:\r\n  # ==========================================\r\n  # String conversion functions\r\n  # ==========================================\r\n  # Test: toStrings\r\n\r\n  # ==========================================\r\n  # Decimal conversion functions\r\n  # ==========================================\r\n  # Test: toDecimal\r\n\r\n  # ==========================================\r\n  # JSON conversion functions\r\n  # ==========================================\r\n  # Test: toJson\r\n  # Test: mustToJson\r\n  mustToJsonValue: \"{\\\"key\\\":\\\"value\\\"}\"\r\n  # Test: toPrettyJson\r\n  # Test: mustToPrettyJson\r\n  # Test: toRawJson\r\n  # Test: mustToRawJson\r\n  # Test: fromJson\r\n  # Test: fromJsonArray\r\n\r\n  # ==========================================\r\n  # YAML conversion functions\r\n  # ==========================================\r\n  # Test: fromYaml\r\n  # Test: toYaml\r\n  # Test: toYamlPretty\r\n  # Test: fromYamlArray\r\n\r\n  # ==========================================\r\n  # TOML conversion functions\r\n  # ==========================================\r\n  # Test: toToml\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-type-conversion/templates/type-conversion-test.yaml",
            "content": "\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  # Test: mustToJson\n  mustToJsonValue: \"{\\\"key\\\":\\\"value\\\"}\"\n  # Test: toPrettyJson\n  # Test: mustToPrettyJson\n  # Test: toRawJson\n  # Test: mustToRawJson\n  # Test: fromJson\n  # Test: fromJsonArray\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  # Test: toYaml\n  # Test: toYamlPretty\n  # Test: fromYamlArray\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n",
            "originalContent": "# Test: Type conversion errors - comprehensive testing of all type conversion functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n  {{- if .Values.testToStrings }}\n  toStringsValue: {{ toStrings $testData | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n  {{- if .Values.testToDecimal }}\n  toDecimalValue: {{ toDecimal $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  {{- if .Values.testToJson }}\n  toJsonValue: {{ toJson $testData | quote }}\n  {{- end }}\n  # Test: mustToJson\n  {{- if .Values.testMustToJson }}\n  mustToJsonValue: {{ mustToJson $testData | quote }}\n  {{- end }}\n  # Test: toPrettyJson\n  {{- if .Values.testToPrettyJson }}\n  toPrettyJsonValue: {{ toPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: mustToPrettyJson\n  {{- if .Values.testMustToPrettyJson }}\n  mustToPrettyJsonValue: {{ mustToPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: toRawJson\n  {{- if .Values.testToRawJson }}\n  toRawJsonValue: {{ toRawJson $testData | quote }}\n  {{- end }}\n  # Test: mustToRawJson\n  {{- if .Values.testMustToRawJson }}\n  mustToRawJsonValue: {{ mustToRawJson $testData | quote }}\n  {{- end }}\n  # Test: fromJson\n  {{- if .Values.testFromJson }}\n  {{- $parsed := $testData | fromJson }}\n  fromJsonValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: fromJsonArray\n  {{- if .Values.testFromJsonArray }}\n  {{- $arr := $testData | fromJsonArray }}\n  fromJsonArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  {{- if .Values.testFromYaml }}\n  {{- $parsed := $testData | fromYaml }}\n  fromYamlValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: toYaml\n  {{- if .Values.testToYaml }}\n  toYamlValue: {{ toYaml $testData | quote }}\n  {{- end }}\n  # Test: toYamlPretty\n  {{- if .Values.testToYamlPretty }}\n  toYamlPrettyValue: {{ toYamlPretty $testData | quote }}\n  {{- end }}\n  # Test: fromYamlArray\n  {{- if .Values.testFromYamlArray }}\n  {{- $arr := $testData | fromYamlArray }}\n  fromYamlArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n  {{- if .Values.testToToml }}\n  toTomlValue: {{ toToml $testData | quote }}\n  {{- end }}\n\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-type-conversion-test\r\ndata:\r\n  # ==========================================\r\n  # String conversion functions\r\n  # ==========================================\r\n  # Test: toStrings\r\n\r\n  # ==========================================\r\n  # Decimal conversion functions\r\n  # ==========================================\r\n  # Test: toDecimal\r\n\r\n  # ==========================================\r\n  # JSON conversion functions\r\n  # ==========================================\r\n  # Test: toJson\r\n  # Test: mustToJson\r\n  mustToJsonValue: \"{\\\"key\\\":\\\"value\\\"}\"\r\n  # Test: toPrettyJson\r\n  # Test: mustToPrettyJson\r\n  # Test: toRawJson\r\n  # Test: mustToRawJson\r\n  # Test: fromJson\r\n  # Test: fromJsonArray\r\n\r\n  # ==========================================\r\n  # YAML conversion functions\r\n  # ==========================================\r\n  # Test: fromYaml\r\n  # Test: toYaml\r\n  # Test: toYamlPretty\r\n  # Test: fromYamlArray\r\n\r\n  # ==========================================\r\n  # TOML conversion functions\r\n  # ==========================================\r\n  # Test: toToml\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-type-conversion/templates/type-conversion-test.yaml",
            "content": "\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  # Test: mustToJson\n  mustToJsonValue: \"{\\\"key\\\":\\\"value\\\"}\"\n  # Test: toPrettyJson\n  # Test: mustToPrettyJson\n  # Test: toRawJson\n  # Test: mustToRawJson\n  # Test: fromJson\n  # Test: fromJsonArray\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  # Test: toYaml\n  # Test: toYamlPretty\n  # Test: fromYamlArray\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n",
            "originalContent": "# Test: Type conversion errors - comprehensive testing of all type conversion functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n  {{- if .Values.testToStrings }}\n  toStringsValue: {{ toStrings $testData | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n  {{- if .Values.testToDecimal }}\n  toDecimalValue: {{ toDecimal $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  {{- if .Values.testToJson }}\n  toJsonValue: {{ toJson $testData | quote }}\n  {{- end }}\n  # Test: mustToJson\n  {{- if .Values.testMustToJson }}\n  mustToJsonValue: {{ mustToJson $testData | quote }}\n  {{- end }}\n  # Test: toPrettyJson\n  {{- if .Values.testToPrettyJson }}\n  toPrettyJsonValue: {{ toPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: mustToPrettyJson\n  {{- if .Values.testMustToPrettyJson }}\n  mustToPrettyJsonValue: {{ mustToPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: toRawJson\n  {{- if .Values.testToRawJson }}\n  toRawJsonValue: {{ toRawJson $testData | quote }}\n  {{- end }}\n  # Test: mustToRawJson\n  {{- if .Values.testMustToRawJson }}\n  mustToRawJsonValue: {{ mustToRawJson $testData | quote }}\n  {{- end }}\n  # Test: fromJson\n  {{- if .Values.testFromJson }}\n  {{- $parsed := $testData | fromJson }}\n  fromJsonValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: fromJsonArray\n  {{- if .Values.testFromJsonArray }}\n  {{- $arr := $testData | fromJsonArray }}\n  fromJsonArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  {{- if .Values.testFromYaml }}\n  {{- $parsed := $testData | fromYaml }}\n  fromYamlValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: toYaml\n  {{- if .Values.testToYaml }}\n  toYamlValue: {{ toYaml $testData | quote }}\n  {{- end }}\n  # Test: toYamlPretty\n  {{- if .Values.testToYamlPretty }}\n  toYamlPrettyValue: {{ toYamlPretty $testData | quote }}\n  {{- end }}\n  # Test: fromYamlArray\n  {{- if .Values.testFromYamlArray }}\n  {{- $arr := $testData | fromYamlArray }}\n  fromYamlArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n  {{- if .Values.testToToml }}\n  toTomlValue: {{ toToml $testData | quote }}\n  {{- end }}\n\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "resolved": true,
      "stopReason": "",
      "fixChain": []
    },
    {
      "toggle": "toPrettyJson",
      "dataType": "nilValue",
      "valuesOptions": {
        "values": [
          "testToPrettyJson=true",
          "testDataType=nilValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-type-conversion-test\r\ndata:\r\n  # ==========================================\r\n  # String conversion functions\r\n  # ==========================================\r\n  # Test: toStrings\r\n\r\n  # ==========================================\r\n  # Decimal conversion functions\r\n  # ==========================================\r\n  # Test: toDecimal\r\n\r\n  # ==========================================\r\n  # JSON conversion functions\r\n  # ==========================================\r\n  # Test: toJson\r\n  # Test: mustToJson\r\n  # Test: toPrettyJson\r\n  toPrettyJsonValue: \"null\"\r\n  # Test: mustToPrettyJson\r\n  # Test: toRawJson\r\n  # Test: mustToRawJson\r\n  # Test: fromJson\r\n  # Test: fromJsonArray\r\n\r\n  # ==========================================\r\n  # YAML conversion functions\r\n  # ==========================================\r\n  # Test: fromYaml\r\n  # Test: toYaml\r\n  # Test: toYamlPretty\r\n  # Test: fromYamlArray\r\n\r\n  # ==========================================\r\n  # TOML conversion functions\r\n  # ==========================================\r\n  # Test: toToml\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-type-conversion/templates/type-conversion-test.yaml",
            "content": "\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  # Test: mustToJson\n  # Test: toPrettyJson\n  toPrettyJsonValue: \"null\"\n  # Test: mustToPrettyJson\n  # Test: toRawJson\n  # Test: mustToRawJson\n  # Test: fromJson\n  # Test: fromJsonArray\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  # Test: toYaml\n  # Test: toYamlPretty\n  # Test: fromYamlArray\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n",
            "originalContent": "# Test: Type conversion errors - comprehensive testing of all type conversion functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n  {{- if .Values.testToStrings }}\n  toStringsValue: {{ toStrings $testData | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n  {{- if .Values.testToDecimal }}\n  toDecimalValue: {{ toDecimal $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  {{- if .Values.testToJson }}\n  toJsonValue: {{ toJson $testData | quote }}\n  {{- end }}\n  # Test: mustToJson\n  {{- if .Values.testMustToJson }}\n  mustToJsonValue: {{ mustToJson $testData | quote }}\n  {{- end }}\n  # Test: toPrettyJson\n  {{- if .Values.testToPrettyJson }}\n  toPrettyJsonValue: {{ toPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: mustToPrettyJson\n  {{- if .Values.testMustToPrettyJson }}\n  mustToPrettyJsonValue: {{ mustToPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: toRawJson\n  {{- if .Values.testToRawJson }}\n  toRawJsonValue: {{ toRawJson $testData | quote }}\n  {{- end }}\n  # Test: mustToRawJson\n  {{- if .Values.testMustToRawJson }}\n  mustToRawJsonValue: {{ mustToRawJson $testData | quote }}\n  {{- end }}\n  # Test: fromJson\n  {{- if .Values.testFromJson }}\n  {{- $parsed := $testData | fromJson }}\n  fromJsonValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: fromJsonArray\n  {{- if .Values.testFromJsonArray }}\n  {{- $arr := $testData | fromJsonArray }}\n  fromJsonArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  {{- if .Values.testFromYaml }}\n  {{- $parsed := $testData | fromYaml }}\n  fromYamlValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: toYaml\n  {{- if .Values.testToYaml }}\n  toYamlValue: {{ toYaml $testData | quote }}\n  {{- end }}\n  # Test: toYamlPretty\n  {{- if .Values.testToYamlPretty }}\n  toYamlPrettyValue: {{ toYamlPretty $testData | quote }}\n  {{- end }}\n  # Test: fromYamlArray\n  {{- if .Values.testFromYamlArray }}\n  {{- $arr := $testData | fromYamlArray }}\n  fromYamlArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n  {{- if .Values.testToToml }}\n  toTomlValue: {{ toToml $testData | quote }}\n  {{- end }}\n\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-type-conversion-test\r\ndata:\r\n  # ==========================================\r\n  # String conversion functions\r\n  # ==========================================\r\n  # Test: toStrings\r\n\r\n  # ==========================================\r\n  # Decimal conversion functions\r\n  # ==========================================\r\n  # Test: toDecimal\r\n\r\n  # ==========================================\r\n  # JSON conversion functions\r\n  # ==========================================\r\n  # Test: toJson\r\n  # Test: mustToJson\r\n  # Test: toPrettyJson\r\n  toPrettyJsonValue: \"null\"\r\n  # Test: mustToPrettyJson\r\n  # Test: toRawJson\r\n  # Test: mustToRawJson\r\n  # Test: fromJson\r\n  # Test: fromJsonArray\r\n\r\n  # ==========================================\r\n  # YAML conversion functions\r\n  # ==========================================\r\n  # Test: fromYaml\r\n  # Test: toYaml\r\n  # Test: toYamlPretty\r\n  # Test: fromYamlArray\r\n\r\n  # ==========================================\r\n  # TOML conversion functions\r\n  # ==========================================\r\n  # Test: toToml\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-type-conversion/templates/type-conversion-test.yaml",
            "content": "\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  # Test: mustToJson\n  # Test: toPrettyJson\n  toPrettyJsonValue: \"null\"\n  # Test: mustToPrettyJson\n  # Test: toRawJson\n  # Test: mustToRawJson\n  # Test: fromJson\n  # Test: fromJsonArray\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  # Test: toYaml\n  # Test: toYamlPretty\n  # Test: fromYamlArray\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n",
            "originalContent": "# Test: Type conversion errors - comprehensive testing of all type conversion functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n  {{- if .Values.testToStrings }}\n  toStringsValue: {{ toStrings $testData | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n  {{- if .Values.testToDecimal }}\n  toDecimalValue: {{ toDecimal $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  {{- if .Values.testToJson }}\n  toJsonValue: {{ toJson $testData | quote }}\n  {{- end }}\n  # Test: mustToJson\n  {{- if .Values.testMustToJson }}\n  mustToJsonValue: {{ mustToJson $testData | quote }}\n  {{- end }}\n  # Test: toPrettyJson\n  {{- if .Values.testToPrettyJson }}\n  toPrettyJsonValue: {{ toPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: mustToPrettyJson\n  {{- if .Values.testMustToPrettyJson }}\n  mustToPrettyJsonValue: {{ mustToPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: toRawJson\n  {{- if .Values.testToRawJson }}\n  toRawJsonValue: {{ toRawJson $testData | quote }}\n  {{- end }}\n  # Test: mustToRawJson\n  {{- if .Values.testMustToRawJson }}\n  mustToRawJsonValue: {{ mustToRawJson $testData | quote }}\n  {{- end }}\n  # Test: fromJson\n  {{- if .Values.testFromJson }}\n  {{- $parsed := $testData | fromJson }}\n  fromJsonValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: fromJsonArray\n  {{- if .Values.testFromJsonArray }}\n  {{- $arr := $testData | fromJsonArray }}\n  fromJsonArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  {{- if .Values.testFromYaml }}\n  {{- $parsed := $testData | fromYaml }}\n  fromYamlValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: toYaml\n  {{- if .Values.testToYaml }}\n  toYamlValue: {{ toYaml $testData | quote }}\n  {{- end }}\n  # Test: toYamlPretty\n  {{- if .Values.testToYamlPretty }}\n  toYamlPrettyValue: {{ toYamlPretty $testData | quote }}\n  {{- end }}\n  # Test: fromYamlArray\n  {{- if .Values.testFromYamlArray }}\n  {{- $arr := $testData | fromYamlArray }}\n  fromYamlArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n  {{- if .Values.testToToml }}\n  toTomlValue: {{ toToml $testData | quote }}\n  {{- end }}\n\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "resolved": true,
      "stopReason": "",
      "fixChain": []
    },
    {
      "toggle": "toPrettyJson",
      "dataType": "stringValue",
      "valuesOptions": {
        "values": [
          "testToPrettyJson=true",
          "testDataType=stringValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-type-conversion-test\r\ndata:\r\n  # ==========================================\r\n  # String conversion functions\r\n  # ==========================================\r\n  # Test: toStrings\r\n\r\n  # ==========================================\r\n  # Decimal conversion functions\r\n  # ==========================================\r\n  # Test: toDecimal\r\n\r\n  # ==========================================\r\n  # JSON conversion functions\r\n  # ==========================================\r\n  # Test: toJson\r\n  # Test: mustToJson\r\n  # Test: toPrettyJson\r\n  toPrettyJsonValue: \"\\\"test string\\\"\"\r\n  # Test: mustToPrettyJson\r\n  # Test: toRawJson\r\n  # Test: mustToRawJson\r\n  # Test: fromJson\r\n  # Test: fromJsonArray\r\n\r\n  # ==========================================\r\n  # YAML conversion functions\r\n  # ==========================================\r\n  # Test: fromYaml\r\n  # Test: toYaml\r\n  # Test: toYamlPretty\r\n  # Test: fromYamlArray\r\n\r\n  # ==========================================\r\n  # TOML conversion functions\r\n  # ==========================================\r\n  # Test: toToml\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-type-conversion/templates/type-conversion-test.yaml",
            "content": "\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  # Test: mustToJson\n  # Test: toPrettyJson\n  toPrettyJsonValue: \"\\\"test string\\\"\"\n  # Test: mustToPrettyJson\n  # Test: toRawJson\n  # Test: mustToRawJson\n  # Test: fromJson\n  # Test: fromJsonArray\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  # Test: toYaml\n  # Test: toYamlPretty\n  # Test: fromYamlArray\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n",
            "originalContent": "# Test: Type conversion errors - comprehensive testing of all type conversion functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n  {{- if .Values.testToStrings }}\n  toStringsValue: {{ toStrings $testData | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n  {{- if .Values.testToDecimal }}\n  toDecimalValue: {{ toDecimal $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  {{- if .Values.testToJson }}\n  toJsonValue: {{ toJson $testData | quote }}\n  {{- end }}\n  # Test: mustToJson\n  {{- if .Values.testMustToJson }}\n  mustToJsonValue: {{ mustToJson $testData | quote }}\n  {{- end }}\n  # Test: toPrettyJson\n  {{- if .Values.testToPrettyJson }}\n  toPrettyJsonValue: {{ toPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: mustToPrettyJson\n  {{- if .Values.testMustToPrettyJson }}\n  mustToPrettyJsonValue: {{ mustToPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: toRawJson\n  {{- if .Values.testToRawJson }}\n  toRawJsonValue: {{ toRawJson $testData | quote }}\n  {{- end }}\n  # Test: mustToRawJson\n  {{- if .Values.testMustToRawJson }}\n  mustToRawJsonValue: {{ mustToRawJson $testData | quote }}\n  {{- end }}\n  # Test: fromJson\n  {{- if .Values.testFromJson }}\n  {{- $parsed := $testData | fromJson }}\n  fromJsonValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: fromJsonArray\n  {{- if .Values.testFromJsonArray }}\n  {{- $arr := $testData | fromJsonArray }}\n  fromJsonArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  {{- if .Values.testFromYaml }}\n  {{- $parsed := $testData | fromYaml }}\n  fromYamlValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: toYaml\n  {{- if .Values.testToYaml }}\n  toYamlValue: {{ toYaml $testData | quote }}\n  {{- end }}\n  # Test: toYamlPretty\n  {{- if .Values.testToYamlPretty }}\n  toYamlPrettyValue: {{ toYamlPretty $testData | quote }}\n  {{- end }}\n  # Test: fromYamlArray\n  {{- if .Values.testFromYamlArray }}\n  {{- $arr := $testData | fromYamlArray }}\n  fromYamlArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n  {{- if .Values.testToToml }}\n  toTomlValue: {{ toToml $testData | quote }}\n  {{- end }}\n\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-type-conversion-test\r\ndata:\r\n  # ==========================================\r\n  # String conversion functions\r\n  # ==========================================\r\n  # Test: toStrings\r\n\r\n  # ==========================================\r\n  # Decimal conversion functions\r\n  # ==========================================\r\n  # Test: toDecimal\r\n\r\n  # ==========================================\r\n  # JSON conversion functions\r\n  # ==========================================\r\n  # Test: toJson\r\n  # Test: mustToJson\r\n  # Test: toPrettyJson\r\n  toPrettyJsonValue: \"\\\"test string\\\"\"\r\n  # Test: mustToPrettyJson\r\n  # Test: toRawJson\r\n  # Test: mustToRawJson\r\n  # Test: fromJson\r\n  # Test: fromJsonArray\r\n\r\n  # ==========================================\r\n  # YAML conversion functions\r\n  # ==========================================\r\n  # Test: fromYaml\r\n  # Test: toYaml\r\n  # Test: toYamlPretty\r\n  # Test: fromYamlArray\r\n\r\n  # ==========================================\r\n  # TOML conversion functions\r\n  # ==========================================\r\n  # Test: toToml\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-type-conversion/templates/type-conversion-test.yaml",
            "content": "\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  # Test: mustToJson\n  # Test: toPrettyJson\n  toPrettyJsonValue: \"\\\"test string\\\"\"\n  # Test: mustToPrettyJson\n  # Test: toRawJson\n  # Test: mustToRawJson\n  # Test: fromJson\n  # Test: fromJsonArray\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  # Test: toYaml\n  # Test: toYamlPretty\n  # Test: fromYamlArray\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n",
            "originalContent": "# Test: Type conversion errors - comprehensive testing of all type conversion functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n  {{- if .Values.testToStrings }}\n  toStringsValue: {{ toStrings $testData | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n  {{- if .Values.testToDecimal }}\n  toDecimalValue: {{ toDecimal $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  {{- if .Values.testToJson }}\n  toJsonValue: {{ toJson $testData | quote }}\n  {{- end }}\n  # Test: mustToJson\n  {{- if .Values.testMustToJson }}\n  mustToJsonValue: {{ mustToJson $testData | quote }}\n  {{- end }}\n  # Test: toPrettyJson\n  {{- if .Values.testToPrettyJson }}\n  toPrettyJsonValue: {{ toPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: mustToPrettyJson\n  {{- if .Values.testMustToPrettyJson }}\n  mustToPrettyJsonValue: {{ mustToPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: toRawJson\n  {{- if .Values.testToRawJson }}\n  toRawJsonValue: {{ toRawJson $testData | quote }}\n  {{- end }}\n  # Test: mustToRawJson\n  {{- if .Values.testMustToRawJson }}\n  mustToRawJsonValue: {{ mustToRawJson $testData | quote }}\n  {{- end }}\n  # Test: fromJson\n  {{- if .Values.testFromJson }}\n  {{- $parsed := $testData | fromJson }}\n  fromJsonValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: fromJsonArray\n  {{- if .Values.testFromJsonArray }}\n  {{- $arr := $testData | fromJsonArray }}\n  fromJsonArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  {{- if .Values.testFromYaml }}\n  {{- $parsed := $testData | fromYaml }}\n  fromYamlValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: toYaml\n  {{- if .Values.testToYaml }}\n  toYamlValue: {{ toYaml $testData | quote }}\n  {{- end }}\n  # Test: toYamlPretty\n  {{- if .Values.testToYamlPretty }}\n  toYamlPrettyValue: {{ toYamlPretty $testData | quote }}\n  {{- end }}\n  # Test: fromYamlArray\n  {{- if .Values.testFromYamlArray }}\n  {{- $arr := $testData | fromYamlArray }}\n  fromYamlArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n  {{- if .Values.testToToml }}\n  toTomlValue: {{ toToml $testData | quote }}\n  {{- end }}\n\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "resolved": true,
      "stopReason": "",
      "fixChain": []
    },
    {
      "toggle": "toPrettyJson",
      "dataType": "numberValue",
      "valuesOptions": {
        "values": [
          "testToPrettyJson=true",
          "testDataType=numberValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-type-conversion-test\r\ndata:\r\n  # ==========================================\r\n  # String conversion functions\r\n  # ==========================================\r\n  # Test: toStrings\r\n\r\n  # ==========================================\r\n  # Decimal conversion functions\r\n  # ==========================================\r\n  # Test: toDecimal\r\n\r\n  # ==========================================\r\n  # JSON conversion functions\r\n  # ==========================================\r\n  # Test: toJson\r\n  # Test: mustToJson\r\n  # Test: toPrettyJson\r\n  toPrettyJsonValue: \"42\"\r\n  # Test: mustToPrettyJson\r\n  # Test: toRawJson\r\n  # Test: mustToRawJson\r\n  # Test: fromJson\r\n  # Test: fromJsonArray\r\n\r\n  # ==========================================\r\n  # YAML conversion functions\r\n  # ==========================================\r\n  # Test: fromYaml\r\n  # Test: toYaml\r\n  # Test: toYamlPretty\r\n  # Test: fromYamlArray\r\n\r\n  # ==========================================\r\n  # TOML conversion functions\r\n  # ==========================================\r\n  # Test: toToml\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-type-conversion/templates/type-conversion-test.yaml",
            "content": "\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  # Test: mustToJson\n  # Test: toPrettyJson\n  toPrettyJsonValue: \"42\"\n  # Test: mustToPrettyJson\n  # Test: toRawJson\n  # Test: mustToRawJson\n  # Test: fromJson\n  # Test: fromJsonArray\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  # Test: toYaml\n  # Test: toYamlPretty\n  # Test: fromYamlArray\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n",
            "originalContent": "# Test: Type conversion errors - comprehensive testing of all type conversion functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n  {{- if .Values.testToStrings }}\n  toStringsValue: {{ toStrings $testData | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n  {{- if .Values.testToDecimal }}\n  toDecimalValue: {{ toDecimal $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  {{- if .Values.testToJson }}\n  toJsonValue: {{ toJson $testData | quote }}\n  {{- end }}\n  # Test: mustToJson\n  {{- if .Values.testMustToJson }}\n  mustToJsonValue: {{ mustToJson $testData | quote }}\n  {{- end }}\n  # Test: toPrettyJson\n  {{- if .Values.testToPrettyJson }}\n  toPrettyJsonValue: {{ toPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: mustToPrettyJson\n  {{- if .Values.testMustToPrettyJson }}\n  mustToPrettyJsonValue: {{ mustToPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: toRawJson\n  {{- if .Values.testToRawJson }}\n  toRawJsonValue: {{ toRawJson $testData | quote }}\n  {{- end }}\n  # Test: mustToRawJson\n  {{- if .Values.testMustToRawJson }}\n  mustToRawJsonValue: {{ mustToRawJson $testData | quote }}\n  {{- end }}\n  # Test: fromJson\n  {{- if .Values.testFromJson }}\n  {{- $parsed := $testData | fromJson }}\n  fromJsonValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: fromJsonArray\n  {{- if .Values.testFromJsonArray }}\n  {{- $arr := $testData | fromJsonArray }}\n  fromJsonArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  {{- if .Values.testFromYaml }}\n  {{- $parsed := $testData | fromYaml }}\n  fromYamlValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: toYaml\n  {{- if .Values.testToYaml }}\n  toYamlValue: {{ toYaml $testData | quote }}\n  {{- end }}\n  # Test: toYamlPretty\n  {{- if .Values.testToYamlPretty }}\n  toYamlPrettyValue: {{ toYamlPretty $testData | quote }}\n  {{- end }}\n  # Test: fromYamlArray\n  {{- if .Values.testFromYamlArray }}\n  {{- $arr := $testData | fromYamlArray }}\n  fromYamlArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n  {{- if .Values.testToToml }}\n  toTomlValue: {{ toToml $testData | quote }}\n  {{- end }}\n\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-type-conversion-test\r\ndata:\r\n  # ==========================================\r\n  # String conversion functions\r\n  # ==========================================\r\n  # Test: toStrings\r\n\r\n  # ==========================================\r\n  # Decimal conversion functions\r\n  # ==========================================\r\n  # Test: toDecimal\r\n\r\n  # ==========================================\r\n  # JSON conversion functions\r\n  # ==========================================\r\n  # Test: toJson\r\n  # Test: mustToJson\r\n  # Test: toPrettyJson\r\n  toPrettyJsonValue: \"42\"\r\n  # Test: mustToPrettyJson\r\n  # Test: toRawJson\r\n  # Test: mustToRawJson\r\n  # Test: fromJson\r\n  # Test: fromJsonArray\r\n\r\n  # ==========================================\r\n  # YAML conversion functions\r\n  # ==========================================\r\n  # Test: fromYaml\r\n  # Test: toYaml\r\n  # Test: toYamlPretty\r\n  # Test: fromYamlArray\r\n\r\n  # ==========================================\r\n  # TOML conversion functions\r\n  # ==========================================\r\n  # Test: toToml\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-type-conversion/templates/type-conversion-test.yaml",
            "content": "\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  # Test: mustToJson\n  # Test: toPrettyJson\n  toPrettyJsonValue: \"42\"\n  # Test: mustToPrettyJson\n  # Test: toRawJson\n  # Test: mustToRawJson\n  # Test: fromJson\n  # Test: fromJsonArray\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  # Test: toYaml\n  # Test: toYamlPretty\n  # Test: fromYamlArray\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n",
            "originalContent": "# Test: Type conversion errors - comprehensive testing of all type conversion functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n  {{- if .Values.testToStrings }}\n  toStringsValue: {{ toStrings $testData | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n  {{- if .Values.testToDecimal }}\n  toDecimalValue: {{ toDecimal $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  {{- if .Values.testToJson }}\n  toJsonValue: {{ toJson $testData | quote }}\n  {{- end }}\n  # Test: mustToJson\n  {{- if .Values.testMustToJson }}\n  mustToJsonValue: {{ mustToJson $testData | quote }}\n  {{- end }}\n  # Test: toPrettyJson\n  {{- if .Values.testToPrettyJson }}\n  toPrettyJsonValue: {{ toPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: mustToPrettyJson\n  {{- if .Values.testMustToPrettyJson }}\n  mustToPrettyJsonValue: {{ mustToPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: toRawJson\n  {{- if .Values.testToRawJson }}\n  toRawJsonValue: {{ toRawJson $testData | quote }}\n  {{- end }}\n  # Test: mustToRawJson\n  {{- if .Values.testMustToRawJson }}\n  mustToRawJsonValue: {{ mustToRawJson $testData | quote }}\n  {{- end }}\n  # Test: fromJson\n  {{- if .Values.testFromJson }}\n  {{- $parsed := $testData | fromJson }}\n  fromJsonValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: fromJsonArray\n  {{- if .Values.testFromJsonArray }}\n  {{- $arr := $testData | fromJsonArray }}\n  fromJsonArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  {{- if .Values.testFromYaml }}\n  {{- $parsed := $testData | fromYaml }}\n  fromYamlValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: toYaml\n  {{- if .Values.testToYaml }}\n  toYamlValue: {{ toYaml $testData | quote }}\n  {{- end }}\n  # Test: toYamlPretty\n  {{- if .Values.testToYamlPretty }}\n  toYamlPrettyValue: {{ toYamlPretty $testData | quote }}\n  {{- end }}\n  # Test: fromYamlArray\n  {{- if .Values.testFromYamlArray }}\n  {{- $arr := $testData | fromYamlArray }}\n  fromYamlArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n  {{- if .Values.testToToml }}\n  toTomlValue: {{ toToml $testData | quote }}\n  {{- end }}\n\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "resolved": true,
      "stopReason": "",
      "fixChain": []
    },
    {
      "toggle": "toPrettyJson",
      "dataType": "boolValue",
      "valuesOptions": {
        "values": [
          "testToPrettyJson=true",
          "testDataType=boolValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-type-conversion-test\r\ndata:\r\n  # ==========================================\r\n  # String conversion functions\r\n  # ==========================================\r\n  # Test: toStrings\r\n\r\n  # ==========================================\r\n  # Decimal conversion functions\r\n  # ==========================================\r\n  # Test: toDecimal\r\n\r\n  # ==========================================\r\n  # JSON conversion functions\r\n  # ==========================================\r\n  # Test: toJson\r\n  # Test: mustToJson\r\n  # Test: toPrettyJson\r\n  toPrettyJsonValue: \"true\"\r\n  # Test: mustToPrettyJson\r\n  # Test: toRawJson\r\n  # Test: mustToRawJson\r\n  # Test: fromJson\r\n  # Test: fromJsonArray\r\n\r\n  # ==========================================\r\n  # YAML conversion functions\r\n  # ==========================================\r\n  # Test: fromYaml\r\n  # Test: toYaml\r\n  # Test: toYamlPretty\r\n  # Test: fromYamlArray\r\n\r\n  # ==========================================\r\n  # TOML conversion functions\r\n  # ==========================================\r\n  # Test: toToml\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-type-conversion/templates/type-conversion-test.yaml",
            "content": "\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  # Test: mustToJson\n  # Test: toPrettyJson\n  toPrettyJsonValue: \"true\"\n  # Test: mustToPrettyJson\n  # Test: toRawJson\n  # Test: mustToRawJson\n  # Test: fromJson\n  # Test: fromJsonArray\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  # Test: toYaml\n  # Test: toYamlPretty\n  # Test: fromYamlArray\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n",
            "originalContent": "# Test: Type conversion errors - comprehensive testing of all type conversion functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n  {{- if .Values.testToStrings }}\n  toStringsValue: {{ toStrings $testData | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n  {{- if .Values.testToDecimal }}\n  toDecimalValue: {{ toDecimal $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  {{- if .Values.testToJson }}\n  toJsonValue: {{ toJson $testData | quote }}\n  {{- end }}\n  # Test: mustToJson\n  {{- if .Values.testMustToJson }}\n  mustToJsonValue: {{ mustToJson $testData | quote }}\n  {{- end }}\n  # Test: toPrettyJson\n  {{- if .Values.testToPrettyJson }}\n  toPrettyJsonValue: {{ toPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: mustToPrettyJson\n  {{- if .Values.testMustToPrettyJson }}\n  mustToPrettyJsonValue: {{ mustToPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: toRawJson\n  {{- if .Values.testToRawJson }}\n  toRawJsonValue: {{ toRawJson $testData | quote }}\n  {{- end }}\n  # Test: mustToRawJson\n  {{- if .Values.testMustToRawJson }}\n  mustToRawJsonValue: {{ mustToRawJson $testData | quote }}\n  {{- end }}\n  # Test: fromJson\n  {{- if .Values.testFromJson }}\n  {{- $parsed := $testData | fromJson }}\n  fromJsonValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: fromJsonArray\n  {{- if .Values.testFromJsonArray }}\n  {{- $arr := $testData | fromJsonArray }}\n  fromJsonArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  {{- if .Values.testFromYaml }}\n  {{- $parsed := $testData | fromYaml }}\n  fromYamlValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: toYaml\n  {{- if .Values.testToYaml }}\n  toYamlValue: {{ toYaml $testData | quote }}\n  {{- end }}\n  # Test: toYamlPretty\n  {{- if .Values.testToYamlPretty }}\n  toYamlPrettyValue: {{ toYamlPretty $testData | quote }}\n  {{- end }}\n  # Test: fromYamlArray\n  {{- if .Values.testFromYamlArray }}\n  {{- $arr := $testData | fromYamlArray }}\n  fromYamlArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n  {{- if .Values.testToToml }}\n  toTomlValue: {{ toToml $testData | quote }}\n  {{- end }}\n\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-type-conversion-test\r\ndata:\r\n  # ==========================================\r\n  # String conversion functions\r\n  # ==========================================\r\n  # Test: toStrings\r\n\r\n  # ==========================================\r\n  # Decimal conversion functions\r\n  # ==========================================\r\n  # Test: toDecimal\r\n\r\n  # ==========================================\r\n  # JSON conversion functions\r\n  # ==========================================\r\n  # Test: toJson\r\n  # Test: mustToJson\r\n  # Test: toPrettyJson\r\n  toPrettyJsonValue: \"true\"\r\n  # Test: mustToPrettyJson\r\n  # Test: toRawJson\r\n  # Test: mustToRawJson\r\n  # Test: fromJson\r\n  # Test: fromJsonArray\r\n\r\n  # ==========================================\r\n  # YAML conversion functions\r\n  # ==========================================\r\n  # Test: fromYaml\r\n  # Test: toYaml\r\n  # Test: toYamlPretty\r\n  # Test: fromYamlArray\r\n\r\n  # ==========================================\r\n  # TOML conversion functions\r\n  # ==========================================\r\n  # Test: toToml\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-type-conversion/templates/type-conversion-test.yaml",
            "content": "\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  # Test: mustToJson\n  # Test: toPrettyJson\n  toPrettyJsonValue: \"true\"\n  # Test: mustToPrettyJson\n  # Test: toRawJson\n  # Test: mustToRawJson\n  # Test: fromJson\n  # Test: fromJsonArray\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  # Test: toYaml\n  # Test: toYamlPretty\n  # Test: fromYamlArray\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n",
            "originalContent": "# Test: Type conversion errors - comprehensive testing of all type conversion functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n  {{- if .Values.testToStrings }}\n  toStringsValue: {{ toStrings $testData | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n  {{- if .Values.testToDecimal }}\n  toDecimalValue: {{ toDecimal $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  {{- if .Values.testToJson }}\n  toJsonValue: {{ toJson $testData | quote }}\n  {{- end }}\n  # Test: mustToJson\n  {{- if .Values.testMustToJson }}\n  mustToJsonValue: {{ mustToJson $testData | quote }}\n  {{- end }}\n  # Test: toPrettyJson\n  {{- if .Values.testToPrettyJson }}\n  toPrettyJsonValue: {{ toPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: mustToPrettyJson\n  {{- if .Values.testMustToPrettyJson }}\n  mustToPrettyJsonValue: {{ mustToPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: toRawJson\n  {{- if .Values.testToRawJson }}\n  toRawJsonValue: {{ toRawJson $testData | quote }}\n  {{- end }}\n  # Test: mustToRawJson\n  {{- if .Values.testMustToRawJson }}\n  mustToRawJsonValue: {{ mustToRawJson $testData | quote }}\n  {{- end }}\n  # Test: fromJson\n  {{- if .Values.testFromJson }}\n  {{- $parsed := $testData | fromJson }}\n  fromJsonValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: fromJsonArray\n  {{- if .Values.testFromJsonArray }}\n  {{- $arr := $testData | fromJsonArray }}\n  fromJsonArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  {{- if .Values.testFromYaml }}\n  {{- $parsed := $testData | fromYaml }}\n  fromYamlValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: toYaml\n  {{- if .Values.testToYaml }}\n  toYamlValue: {{ toYaml $testData | quote }}\n  {{- end }}\n  # Test: toYamlPretty\n  {{- if .Values.testToYamlPretty }}\n  toYamlPrettyValue: {{ toYamlPretty $testData | quote }}\n  {{- end }}\n  # Test: fromYamlArray\n  {{- if .Values.testFromYamlArray }}\n  {{- $arr := $testData | fromYamlArray }}\n  fromYamlArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n  {{- if .Values.testToToml }}\n  toTomlValue: {{ toToml $testData | quote }}\n  {{- end }}\n\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "resolved": true,
      "stopReason": "",
      "fixChain": []
    },
    {
      "toggle": "toPrettyJson",
      "dataType": "listValue",
      "valuesOptions": {
        "values": [
          "testToPrettyJson=true",
          "testDataType=listValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-type-conversion-test\r\ndata:\r\n  # ==========================================\r\n  # String conversion functions\r\n  # ==========================================\r\n  # Test: toStrings\r\n\r\n  # ==========================================\r\n  # Decimal conversion functions\r\n  # ==========================================\r\n  # Test: toDecimal\r\n\r\n  # ==========================================\r\n  # JSON conversion functions\r\n  # ==========================================\r\n  # Test: toJson\r\n  # Test: mustToJson\r\n  # Test: toPrettyJson\r\n  toPrettyJsonValue: \"[\\n  1,\\n  2,\\n  3\\n]\"\r\n  # Test: mustToPrettyJson\r\n  # Test: toRawJson\r\n  # Test: mustToRawJson\r\n  # Test: fromJson\r\n  # Test: fromJsonArray\r\n\r\n  # ==========================================\r\n  # YAML conversion functions\r\n  # ==========================================\r\n  # Test: fromYaml\r\n  # Test: toYaml\r\n  # Test: toYamlPretty\r\n  # Test: fromYamlArray\r\n\r\n  # ==========================================\r\n  # TOML conversion functions\r\n  # ==========================================\r\n  # Test: toToml\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-type-conversion/templates/type-conversion-test.yaml",
            "content": "\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  # Test: mustToJson\n  # Test: toPrettyJson\n  toPrettyJsonValue: \"[\\n  1,\\n  2,\\n  3\\n]\"\n  # Test: mustToPrettyJson\n  # Test: toRawJson\n  # Test: mustToRawJson\n  # Test: fromJson\n  # Test: fromJsonArray\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  # Test: toYaml\n  # Test: toYamlPretty\n  # Test: fromYamlArray\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n",
            "originalContent": "# Test: Type conversion errors - comprehensive testing of all type conversion functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n  {{- if .Values.testToStrings }}\n  toStringsValue: {{ toStrings $testData | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n  {{- if .Values.testToDecimal }}\n  toDecimalValue: {{ toDecimal $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  {{- if .Values.testToJson }}\n  toJsonValue: {{ toJson $testData | quote }}\n  {{- end }}\n  # Test: mustToJson\n  {{- if .Values.testMustToJson }}\n  mustToJsonValue: {{ mustToJson $testData | quote }}\n  {{- end }}\n  # Test: toPrettyJson\n  {{- if .Values.testToPrettyJson }}\n  toPrettyJsonValue: {{ toPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: mustToPrettyJson\n  {{- if .Values.testMustToPrettyJson }}\n  mustToPrettyJsonValue: {{ mustToPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: toRawJson\n  {{- if .Values.testToRawJson }}\n  toRawJsonValue: {{ toRawJson $testData | quote }}\n  {{- end }}\n  # Test: mustToRawJson\n  {{- if .Values.testMustToRawJson }}\n  mustToRawJsonValue: {{ mustToRawJson $testData | quote }}\n  {{- end }}\n  # Test: fromJson\n  {{- if .Values.testFromJson }}\n  {{- $parsed := $testData | fromJson }}\n  fromJsonValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: fromJsonArray\n  {{- if .Values.testFromJsonArray }}\n  {{- $arr := $testData | fromJsonArray }}\n  fromJsonArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  {{- if .Values.testFromYaml }}\n  {{- $parsed := $testData | fromYaml }}\n  fromYamlValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: toYaml\n  {{- if .Values.testToYaml }}\n  toYamlValue: {{ toYaml $testData | quote }}\n  {{- end }}\n  # Test: toYamlPretty\n  {{- if .Values.testToYamlPretty }}\n  toYamlPrettyValue: {{ toYamlPretty $testData | quote }}\n  {{- end }}\n  # Test: fromYamlArray\n  {{- if .Values.testFromYamlArray }}\n  {{- $arr := $testData | fromYamlArray }}\n  fromYamlArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n  {{- if .Values.testToToml }}\n  toTomlValue: {{ toToml $testData | quote }}\n  {{- end }}\n\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-type-conversion-test\r\ndata:\r\n  # ==========================================\r\n  # String conversion functions\r\n  # ==========================================\r\n  # Test: toStrings\r\n\r\n  # ==========================================\r\n  # Decimal conversion functions\r\n  # ==========================================\r\n  # Test: toDecimal\r\n\r\n  # ==========================================\r\n  # JSON conversion functions\r\n  # ==========================================\r\n  # Test: toJson\r\n  # Test: mustToJson\r\n  # Test: toPrettyJson\r\n  toPrettyJsonValue: \"[\\n  1,\\n  2,\\n  3\\n]\"\r\n  # Test: mustToPrettyJson\r\n  # Test: toRawJson\r\n  # Test: mustToRawJson\r\n  # Test: fromJson\r\n  # Test: fromJsonArray\r\n\r\n  # ==========================================\r\n  # YAML conversion functions\r\n  # ==========================================\r\n  # Test: fromYaml\r\n  # Test: toYaml\r\n  # Test: toYamlPretty\r\n  # Test: fromYamlArray\r\n\r\n  # ==========================================\r\n  # TOML conversion functions\r\n  # ==========================================\r\n  # Test: toToml\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-type-conversion/templates/type-conversion-test.yaml",
            "content": "\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  # Test: mustToJson\n  # Test: toPrettyJson\n  toPrettyJsonValue: \"[\\n  1,\\n  2,\\n  3\\n]\"\n  # Test: mustToPrettyJson\n  # Test: toRawJson\n  # Test: mustToRawJson\n  # Test: fromJson\n  # Test: fromJsonArray\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  # Test: toYaml\n  # Test: toYamlPretty\n  # Test: fromYamlArray\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n",
            "originalContent": "# Test: Type conversion errors - comprehensive testing of all type conversion functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n  {{- if .Values.testToStrings }}\n  toStringsValue: {{ toStrings $testData | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n  {{- if .Values.testToDecimal }}\n  toDecimalValue: {{ toDecimal $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  {{- if .Values.testToJson }}\n  toJsonValue: {{ toJson $testData | quote }}\n  {{- end }}\n  # Test: mustToJson\n  {{- if .Values.testMustToJson }}\n  mustToJsonValue: {{ mustToJson $testData | quote }}\n  {{- end }}\n  # Test: toPrettyJson\n  {{- if .Values.testToPrettyJson }}\n  toPrettyJsonValue: {{ toPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: mustToPrettyJson\n  {{- if .Values.testMustToPrettyJson }}\n  mustToPrettyJsonValue: {{ mustToPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: toRawJson\n  {{- if .Values.testToRawJson }}\n  toRawJsonValue: {{ toRawJson $testData | quote }}\n  {{- end }}\n  # Test: mustToRawJson\n  {{- if .Values.testMustToRawJson }}\n  mustToRawJsonValue: {{ mustToRawJson $testData | quote }}\n  {{- end }}\n  # Test: fromJson\n  {{- if .Values.testFromJson }}\n  {{- $parsed := $testData | fromJson }}\n  fromJsonValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: fromJsonArray\n  {{- if .Values.testFromJsonArray }}\n  {{- $arr := $testData | fromJsonArray }}\n  fromJsonArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  {{- if .Values.testFromYaml }}\n  {{- $parsed := $testData | fromYaml }}\n  fromYamlValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: toYaml\n  {{- if .Values.testToYaml }}\n  toYamlValue: {{ toYaml $testData | quote }}\n  {{- end }}\n  # Test: toYamlPretty\n  {{- if .Values.testToYamlPretty }}\n  toYamlPrettyValue: {{ toYamlPretty $testData | quote }}\n  {{- end }}\n  # Test: fromYamlArray\n  {{- if .Values.testFromYamlArray }}\n  {{- $arr := $testData | fromYamlArray }}\n  fromYamlArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n  {{- if .Values.testToToml }}\n  toTomlValue: {{ toToml $testData | quote }}\n  {{- end }}\n\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "resolved": true,
      "stopReason": "",
      "fixChain": []
    },
    {
      "toggle": "toPrettyJson",
      "dataType": "dictValue",
      "valuesOptions": {
        "values": [
          "testToPrettyJson=true",
          "testDataType=dictValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-type-conversion-test\r\ndata:\r\n  # ==========================================\r\n  # String conversion functions\r\n  # ==========================================\r\n  # Test: toStrings\r\n\r\n  # ==========================================\r\n  # Decimal conversion functions\r\n  # ==========================================\r\n  # Test: toDecimal\r\n\r\n  # ==========================================\r\n  # JSON conversion functions\r\n  # ==========================================\r\n  # Test: toJson\r\n  # Test: mustToJson\r\n  # Test: toPrettyJson\r\n  toPrettyJsonValue: \"{\\n  \\\"key\\\": \\\"value\\\"\\n}\"\r\n  # Test: mustToPrettyJson\r\n  # Test: toRawJson\r\n  # Test: mustToRawJson\r\n  # Test: fromJson\r\n  # Test: fromJsonArray\r\n\r\n  # ==========================================\r\n  # YAML conversion functions\r\n  # ==========================================\r\n  # Test: fromYaml\r\n  # Test: toYaml\r\n  # Test: toYamlPretty\r\n  # Test: fromYamlArray\r\n\r\n  # ==========================================\r\n  # TOML conversion functions\r\n  # ==========================================\r\n  # Test: toToml\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-type-conversion/templates/type-conversion-test.yaml",
            "content": "\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  # Test: mustToJson\n  # Test: toPrettyJson\n  toPrettyJsonValue: \"{\\n  \\\"key\\\": \\\"value\\\"\\n}\"\n  # Test: mustToPrettyJson\n  # Test: toRawJson\n  # Test: mustToRawJson\n  # Test: fromJson\n  # Test: fromJsonArray\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  # Test: toYaml\n  # Test: toYamlPretty\n  # Test: fromYamlArray\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n",
            "originalContent": "# Test: Type conversion errors - comprehensive testing of all type conversion functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n  {{- if .Values.testToStrings }}\n  toStringsValue: {{ toStrings $testData | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n  {{- if .Values.testToDecimal }}\n  toDecimalValue: {{ toDecimal $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  {{- if .Values.testToJson }}\n  toJsonValue: {{ toJson $testData | quote }}\n  {{- end }}\n  # Test: mustToJson\n  {{- if .Values.testMustToJson }}\n  mustToJsonValue: {{ mustToJson $testData | quote }}\n  {{- end }}\n  # Test: toPrettyJson\n  {{- if .Values.testToPrettyJson }}\n  toPrettyJsonValue: {{ toPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: mustToPrettyJson\n  {{- if .Values.testMustToPrettyJson }}\n  mustToPrettyJsonValue: {{ mustToPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: toRawJson\n  {{- if .Values.testToRawJson }}\n  toRawJsonValue: {{ toRawJson $testData | quote }}\n  {{- end }}\n  # Test: mustToRawJson\n  {{- if .Values.testMustToRawJson }}\n  mustToRawJsonValue: {{ mustToRawJson $testData | quote }}\n  {{- end }}\n  # Test: fromJson\n  {{- if .Values.testFromJson }}\n  {{- $parsed := $testData | fromJson }}\n  fromJsonValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: fromJsonArray\n  {{- if .Values.testFromJsonArray }}\n  {{- $arr := $testData | fromJsonArray }}\n  fromJsonArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  {{- if .Values.testFromYaml }}\n  {{- $parsed := $testData | fromYaml }}\n  fromYamlValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: toYaml\n  {{- if .Values.testToYaml }}\n  toYamlValue: {{ toYaml $testData | quote }}\n  {{- end }}\n  # Test: toYamlPretty\n  {{- if .Values.testToYamlPretty }}\n  toYamlPrettyValue: {{ toYamlPretty $testData | quote }}\n  {{- end }}\n  # Test: fromYamlArray\n  {{- if .Values.testFromYamlArray }}\n  {{- $arr := $testData | fromYamlArray }}\n  fromYamlArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n  {{- if .Values.testToToml }}\n  toTomlValue: {{ toToml $testData | quote }}\n  {{- end }}\n\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-type-conversion-test\r\ndata:\r\n  # ==========================================\r\n  # String conversion functions\r\n  # ==========================================\r\n  # Test: toStrings\r\n\r\n  # ==========================================\r\n  # Decimal conversion functions\r\n  # ==========================================\r\n  # Test: toDecimal\r\n\r\n  # ==========================================\r\n  # JSON conversion functions\r\n  # ==========================================\r\n  # Test: toJson\r\n  # Test: mustToJson\r\n  # Test: toPrettyJson\r\n  toPrettyJsonValue: \"{\\n  \\\"key\\\": \\\"value\\\"\\n}\"\r\n  # Test: mustToPrettyJson\r\n  # Test: toRawJson\r\n  # Test: mustToRawJson\r\n  # Test: fromJson\r\n  # Test: fromJsonArray\r\n\r\n  # ==========================================\r\n  # YAML conversion functions\r\n  # ==========================================\r\n  # Test: fromYaml\r\n  # Test: toYaml\r\n  # Test: toYamlPretty\r\n  # Test: fromYamlArray\r\n\r\n  # ==========================================\r\n  # TOML conversion functions\r\n  # ==========================================\r\n  # Test: toToml\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-type-conversion/templates/type-conversion-test.yaml",
            "content": "\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  # Test: mustToJson\n  # Test: toPrettyJson\n  toPrettyJsonValue: \"{\\n  \\\"key\\\": \\\"value\\\"\\n}\"\n  # Test: mustToPrettyJson\n  # Test: toRawJson\n  # Test: mustToRawJson\n  # Test: fromJson\n  # Test: fromJsonArray\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  # Test: toYaml\n  # Test: toYamlPretty\n  # Test: fromYamlArray\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n",
            "originalContent": "# Test: Type conversion errors - comprehensive testing of all type conversion functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n  {{- if .Values.testToStrings }}\n  toStringsValue: {{ toStrings $testData | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n  {{- if .Values.testToDecimal }}\n  toDecimalValue: {{ toDecimal $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  {{- if .Values.testToJson }}\n  toJsonValue: {{ toJson $testData | quote }}\n  {{- end }}\n  # Test: mustToJson\n  {{- if .Values.testMustToJson }}\n  mustToJsonValue: {{ mustToJson $testData | quote }}\n  {{- end }}\n  # Test: toPrettyJson\n  {{- if .Values.testToPrettyJson }}\n  toPrettyJsonValue: {{ toPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: mustToPrettyJson\n  {{- if .Values.testMustToPrettyJson }}\n  mustToPrettyJsonValue: {{ mustToPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: toRawJson\n  {{- if .Values.testToRawJson }}\n  toRawJsonValue: {{ toRawJson $testData | quote }}\n  {{- end }}\n  # Test: mustToRawJson\n  {{- if .Values.testMustToRawJson }}\n  mustToRawJsonValue: {{ mustToRawJson $testData | quote }}\n  {{- end }}\n  # Test: fromJson\n  {{- if .Values.testFromJson }}\n  {{- $parsed := $testData | fromJson }}\n  fromJsonValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: fromJsonArray\n  {{- if .Values.testFromJsonArray }}\n  {{- $arr := $testData | fromJsonArray }}\n  fromJsonArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  {{- if .Values.testFromYaml }}\n  {{- $parsed := $testData | fromYaml }}\n  fromYamlValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: toYaml\n  {{- if .Values.testToYaml }}\n  toYamlValue: {{ toYaml $testData | quote }}\n  {{- end }}\n  # Test: toYamlPretty\n  {{- if .Values.testToYamlPretty }}\n  toYamlPrettyValue: {{ toYamlPretty $testData | quote }}\n  {{- end }}\n  # Test: fromYamlArray\n  {{- if .Values.testFromYamlArray }}\n  {{- $arr := $testData | fromYamlArray }}\n  fromYamlArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n  {{- if .Values.testToToml }}\n  toTomlValue: {{ toToml $testData | quote }}\n  {{- end }}\n\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "resolved": true,
      "stopReason": "",
      "fixChain": []
    },
    {
      "toggle": "mustToPrettyJson",
      "dataType": "nilValue",
      "valuesOptions": {
        "values": [
          "testMustToPrettyJson=true",
          "testDataType=nilValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-type-conversion-test\r\ndata:\r\n  # ==========================================\r\n  # String conversion functions\r\n  # ==========================================\r\n  # Test: toStrings\r\n\r\n  # ==========================================\r\n  # Decimal conversion functions\r\n  # ==========================================\r\n  # Test: toDecimal\r\n\r\n  # ==========================================\r\n  # JSON conversion functions\r\n  # ==========================================\r\n  # Test: toJson\r\n  # Test: mustToJson\r\n  # Test: toPrettyJson\r\n  # Test: mustToPrettyJson\r\n  mustToPrettyJsonValue: \"null\"\r\n  # Test: toRawJson\r\n  # Test: mustToRawJson\r\n  # Test: fromJson\r\n  # Test: fromJsonArray\r\n\r\n  # ==========================================\r\n  # YAML conversion functions\r\n  # ==========================================\r\n  # Test: fromYaml\r\n  # Test: toYaml\r\n  # Test: toYamlPretty\r\n  # Test: fromYamlArray\r\n\r\n  # ==========================================\r\n  # TOML conversion functions\r\n  # ==========================================\r\n  # Test: toToml\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-type-conversion/templates/type-conversion-test.yaml",
            "content": "\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  # Test: mustToJson\n  # Test: toPrettyJson\n  # Test: mustToPrettyJson\n  mustToPrettyJsonValue: \"null\"\n  # Test: toRawJson\n  # Test: mustToRawJson\n  # Test: fromJson\n  # Test: fromJsonArray\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  # Test: toYaml\n  # Test: toYamlPretty\n  # Test: fromYamlArray\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n",
            "originalContent": "# Test: Type conversion errors - comprehensive testing of all type conversion functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n  {{- if .Values.testToStrings }}\n  toStringsValue: {{ toStrings $testData | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n  {{- if .Values.testToDecimal }}\n  toDecimalValue: {{ toDecimal $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  {{- if .Values.testToJson }}\n  toJsonValue: {{ toJson $testData | quote }}\n  {{- end }}\n  # Test: mustToJson\n  {{- if .Values.testMustToJson }}\n  mustToJsonValue: {{ mustToJson $testData | quote }}\n  {{- end }}\n  # Test: toPrettyJson\n  {{- if .Values.testToPrettyJson }}\n  toPrettyJsonValue: {{ toPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: mustToPrettyJson\n  {{- if .Values.testMustToPrettyJson }}\n  mustToPrettyJsonValue: {{ mustToPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: toRawJson\n  {{- if .Values.testToRawJson }}\n  toRawJsonValue: {{ toRawJson $testData | quote }}\n  {{- end }}\n  # Test: mustToRawJson\n  {{- if .Values.testMustToRawJson }}\n  mustToRawJsonValue: {{ mustToRawJson $testData | quote }}\n  {{- end }}\n  # Test: fromJson\n  {{- if .Values.testFromJson }}\n  {{- $parsed := $testData | fromJson }}\n  fromJsonValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: fromJsonArray\n  {{- if .Values.testFromJsonArray }}\n  {{- $arr := $testData | fromJsonArray }}\n  fromJsonArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  {{- if .Values.testFromYaml }}\n  {{- $parsed := $testData | fromYaml }}\n  fromYamlValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: toYaml\n  {{- if .Values.testToYaml }}\n  toYamlValue: {{ toYaml $testData | quote }}\n  {{- end }}\n  # Test: toYamlPretty\n  {{- if .Values.testToYamlPretty }}\n  toYamlPrettyValue: {{ toYamlPretty $testData | quote }}\n  {{- end }}\n  # Test: fromYamlArray\n  {{- if .Values.testFromYamlArray }}\n  {{- $arr := $testData | fromYamlArray }}\n  fromYamlArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n  {{- if .Values.testToToml }}\n  toTomlValue: {{ toToml $testData | quote }}\n  {{- end }}\n\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-type-conversion-test\r\ndata:\r\n  # ==========================================\r\n  # String conversion functions\r\n  # ==========================================\r\n  # Test: toStrings\r\n\r\n  # ==========================================\r\n  # Decimal conversion functions\r\n  # ==========================================\r\n  # Test: toDecimal\r\n\r\n  # ==========================================\r\n  # JSON conversion functions\r\n  # ==========================================\r\n  # Test: toJson\r\n  # Test: mustToJson\r\n  # Test: toPrettyJson\r\n  # Test: mustToPrettyJson\r\n  mustToPrettyJsonValue: \"null\"\r\n  # Test: toRawJson\r\n  # Test: mustToRawJson\r\n  # Test: fromJson\r\n  # Test: fromJsonArray\r\n\r\n  # ==========================================\r\n  # YAML conversion functions\r\n  # ==========================================\r\n  # Test: fromYaml\r\n  # Test: toYaml\r\n  # Test: toYamlPretty\r\n  # Test: fromYamlArray\r\n\r\n  # ==========================================\r\n  # TOML conversion functions\r\n  # ==========================================\r\n  # Test: toToml\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-type-conversion/templates/type-conversion-test.yaml",
            "content": "\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  # Test: mustToJson\n  # Test: toPrettyJson\n  # Test: mustToPrettyJson\n  mustToPrettyJsonValue: \"null\"\n  # Test: toRawJson\n  # Test: mustToRawJson\n  # Test: fromJson\n  # Test: fromJsonArray\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  # Test: toYaml\n  # Test: toYamlPretty\n  # Test: fromYamlArray\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n",
            "originalContent": "# Test: Type conversion errors - comprehensive testing of all type conversion functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n  {{- if .Values.testToStrings }}\n  toStringsValue: {{ toStrings $testData | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n  {{- if .Values.testToDecimal }}\n  toDecimalValue: {{ toDecimal $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  {{- if .Values.testToJson }}\n  toJsonValue: {{ toJson $testData | quote }}\n  {{- end }}\n  # Test: mustToJson\n  {{- if .Values.testMustToJson }}\n  mustToJsonValue: {{ mustToJson $testData | quote }}\n  {{- end }}\n  # Test: toPrettyJson\n  {{- if .Values.testToPrettyJson }}\n  toPrettyJsonValue: {{ toPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: mustToPrettyJson\n  {{- if .Values.testMustToPrettyJson }}\n  mustToPrettyJsonValue: {{ mustToPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: toRawJson\n  {{- if .Values.testToRawJson }}\n  toRawJsonValue: {{ toRawJson $testData | quote }}\n  {{- end }}\n  # Test: mustToRawJson\n  {{- if .Values.testMustToRawJson }}\n  mustToRawJsonValue: {{ mustToRawJson $testData | quote }}\n  {{- end }}\n  # Test: fromJson\n  {{- if .Values.testFromJson }}\n  {{- $parsed := $testData | fromJson }}\n  fromJsonValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: fromJsonArray\n  {{- if .Values.testFromJsonArray }}\n  {{- $arr := $testData | fromJsonArray }}\n  fromJsonArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  {{- if .Values.testFromYaml }}\n  {{- $parsed := $testData | fromYaml }}\n  fromYamlValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: toYaml\n  {{- if .Values.testToYaml }}\n  toYamlValue: {{ toYaml $testData | quote }}\n  {{- end }}\n  # Test: toYamlPretty\n  {{- if .Values.testToYamlPretty }}\n  toYamlPrettyValue: {{ toYamlPretty $testData | quote }}\n  {{- end }}\n  # Test: fromYamlArray\n  {{- if .Values.testFromYamlArray }}\n  {{- $arr := $testData | fromYamlArray }}\n  fromYamlArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n  {{- if .Values.testToToml }}\n  toTomlValue: {{ toToml $testData | quote }}\n  {{- end }}\n\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "resolved": true,
      "stopReason": "",
      "fixChain": []
    },
    {
      "toggle": "mustToPrettyJson",
      "dataType": "stringValue",
      "valuesOptions": {
        "values": [
          "testMustToPrettyJson=true",
          "testDataType=stringValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-type-conversion-test\r\ndata:\r\n  # ==========================================\r\n  # String conversion functions\r\n  # ==========================================\r\n  # Test: toStrings\r\n\r\n  # ==========================================\r\n  # Decimal conversion functions\r\n  # ==========================================\r\n  # Test: toDecimal\r\n\r\n  # ==========================================\r\n  # JSON conversion functions\r\n  # ==========================================\r\n  # Test: toJson\r\n  # Test: mustToJson\r\n  # Test: toPrettyJson\r\n  # Test: mustToPrettyJson\r\n  mustToPrettyJsonValue: \"\\\"test string\\\"\"\r\n  # Test: toRawJson\r\n  # Test: mustToRawJson\r\n  # Test: fromJson\r\n  # Test: fromJsonArray\r\n\r\n  # ==========================================\r\n  # YAML conversion functions\r\n  # ==========================================\r\n  # Test: fromYaml\r\n  # Test: toYaml\r\n  # Test: toYamlPretty\r\n  # Test: fromYamlArray\r\n\r\n  # ==========================================\r\n  # TOML conversion functions\r\n  # ==========================================\r\n  # Test: toToml\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-type-conversion/templates/type-conversion-test.yaml",
            "content": "\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  # Test: mustToJson\n  # Test: toPrettyJson\n  # Test: mustToPrettyJson\n  mustToPrettyJsonValue: \"\\\"test string\\\"\"\n  # Test: toRawJson\n  # Test: mustToRawJson\n  # Test: fromJson\n  # Test: fromJsonArray\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  # Test: toYaml\n  # Test: toYamlPretty\n  # Test: fromYamlArray\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n",
            "originalContent": "# Test: Type conversion errors - comprehensive testing of all type conversion functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n  {{- if .Values.testToStrings }}\n  toStringsValue: {{ toStrings $testData | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n  {{- if .Values.testToDecimal }}\n  toDecimalValue: {{ toDecimal $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  {{- if .Values.testToJson }}\n  toJsonValue: {{ toJson $testData | quote }}\n  {{- end }}\n  # Test: mustToJson\n  {{- if .Values.testMustToJson }}\n  mustToJsonValue: {{ mustToJson $testData | quote }}\n  {{- end }}\n  # Test: toPrettyJson\n  {{- if .Values.testToPrettyJson }}\n  toPrettyJsonValue: {{ toPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: mustToPrettyJson\n  {{- if .Values.testMustToPrettyJson }}\n  mustToPrettyJsonValue: {{ mustToPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: toRawJson\n  {{- if .Values.testToRawJson }}\n  toRawJsonValue: {{ toRawJson $testData | quote }}\n  {{- end }}\n  # Test: mustToRawJson\n  {{- if .Values.testMustToRawJson }}\n  mustToRawJsonValue: {{ mustToRawJson $testData | quote }}\n  {{- end }}\n  # Test: fromJson\n  {{- if .Values.testFromJson }}\n  {{- $parsed := $testData | fromJson }}\n  fromJsonValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: fromJsonArray\n  {{- if .Values.testFromJsonArray }}\n  {{- $arr := $testData | fromJsonArray }}\n  fromJsonArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  {{- if .Values.testFromYaml }}\n  {{- $parsed := $testData | fromYaml }}\n  fromYamlValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: toYaml\n  {{- if .Values.testToYaml }}\n  toYamlValue: {{ toYaml $testData | quote }}\n  {{- end }}\n  # Test: toYamlPretty\n  {{- if .Values.testToYamlPretty }}\n  toYamlPrettyValue: {{ toYamlPretty $testData | quote }}\n  {{- end }}\n  # Test: fromYamlArray\n  {{- if .Values.testFromYamlArray }}\n  {{- $arr := $testData | fromYamlArray }}\n  fromYamlArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n  {{- if .Values.testToToml }}\n  toTomlValue: {{ toToml $testData | quote }}\n  {{- end }}\n\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-type-conversion-test\r\ndata:\r\n  # ==========================================\r\n  # String conversion functions\r\n  # ==========================================\r\n  # Test: toStrings\r\n\r\n  # ==========================================\r\n  # Decimal conversion functions\r\n  # ==========================================\r\n  # Test: toDecimal\r\n\r\n  # ==========================================\r\n  # JSON conversion functions\r\n  # ==========================================\r\n  # Test: toJson\r\n  # Test: mustToJson\r\n  # Test: toPrettyJson\r\n  # Test: mustToPrettyJson\r\n  mustToPrettyJsonValue: \"\\\"test string\\\"\"\r\n  # Test: toRawJson\r\n  # Test: mustToRawJson\r\n  # Test: fromJson\r\n  # Test: fromJsonArray\r\n\r\n  # ==========================================\r\n  # YAML conversion functions\r\n  # ==========================================\r\n  # Test: fromYaml\r\n  # Test: toYaml\r\n  # Test: toYamlPretty\r\n  # Test: fromYamlArray\r\n\r\n  # ==========================================\r\n  # TOML conversion functions\r\n  # ==========================================\r\n  # Test: toToml\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-type-conversion/templates/type-conversion-test.yaml",
            "content": "\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  # Test: mustToJson\n  # Test: toPrettyJson\n  # Test: mustToPrettyJson\n  mustToPrettyJsonValue: \"\\\"test string\\\"\"\n  # Test: toRawJson\n  # Test: mustToRawJson\n  # Test: fromJson\n  # Test: fromJsonArray\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  # Test: toYaml\n  # Test: toYamlPretty\n  # Test: fromYamlArray\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n",
            "originalContent": "# Test: Type conversion errors - comprehensive testing of all type conversion functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n  {{- if .Values.testToStrings }}\n  toStringsValue: {{ toStrings $testData | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n  {{- if .Values.testToDecimal }}\n  toDecimalValue: {{ toDecimal $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  {{- if .Values.testToJson }}\n  toJsonValue: {{ toJson $testData | quote }}\n  {{- end }}\n  # Test: mustToJson\n  {{- if .Values.testMustToJson }}\n  mustToJsonValue: {{ mustToJson $testData | quote }}\n  {{- end }}\n  # Test: toPrettyJson\n  {{- if .Values.testToPrettyJson }}\n  toPrettyJsonValue: {{ toPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: mustToPrettyJson\n  {{- if .Values.testMustToPrettyJson }}\n  mustToPrettyJsonValue: {{ mustToPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: toRawJson\n  {{- if .Values.testToRawJson }}\n  toRawJsonValue: {{ toRawJson $testData | quote }}\n  {{- end }}\n  # Test: mustToRawJson\n  {{- if .Values.testMustToRawJson }}\n  mustToRawJsonValue: {{ mustToRawJson $testData | quote }}\n  {{- end }}\n  # Test: fromJson\n  {{- if .Values.testFromJson }}\n  {{- $parsed := $testData | fromJson }}\n  fromJsonValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: fromJsonArray\n  {{- if .Values.testFromJsonArray }}\n  {{- $arr := $testData | fromJsonArray }}\n  fromJsonArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  {{- if .Values.testFromYaml }}\n  {{- $parsed := $testData | fromYaml }}\n  fromYamlValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: toYaml\n  {{- if .Values.testToYaml }}\n  toYamlValue: {{ toYaml $testData | quote }}\n  {{- end }}\n  # Test: toYamlPretty\n  {{- if .Values.testToYamlPretty }}\n  toYamlPrettyValue: {{ toYamlPretty $testData | quote }}\n  {{- end }}\n  # Test: fromYamlArray\n  {{- if .Values.testFromYamlArray }}\n  {{- $arr := $testData | fromYamlArray }}\n  fromYamlArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n  {{- if .Values.testToToml }}\n  toTomlValue: {{ toToml $testData | quote }}\n  {{- end }}\n\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "resolved": true,
      "stopReason": "",
      "fixChain": []
    },
    {
      "toggle": "mustToPrettyJson",
      "dataType": "numberValue",
      "valuesOptions": {
        "values": [
          "testMustToPrettyJson=true",
          "testDataType=numberValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-type-conversion-test\r\ndata:\r\n  # ==========================================\r\n  # String conversion functions\r\n  # ==========================================\r\n  # Test: toStrings\r\n\r\n  # ==========================================\r\n  # Decimal conversion functions\r\n  # ==========================================\r\n  # Test: toDecimal\r\n\r\n  # ==========================================\r\n  # JSON conversion functions\r\n  # ==========================================\r\n  # Test: toJson\r\n  # Test: mustToJson\r\n  # Test: toPrettyJson\r\n  # Test: mustToPrettyJson\r\n  mustToPrettyJsonValue: \"42\"\r\n  # Test: toRawJson\r\n  # Test: mustToRawJson\r\n  # Test: fromJson\r\n  # Test: fromJsonArray\r\n\r\n  # ==========================================\r\n  # YAML conversion functions\r\n  # ==========================================\r\n  # Test: fromYaml\r\n  # Test: toYaml\r\n  # Test: toYamlPretty\r\n  # Test: fromYamlArray\r\n\r\n  # ==========================================\r\n  # TOML conversion functions\r\n  # ==========================================\r\n  # Test: toToml\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-type-conversion/templates/type-conversion-test.yaml",
            "content": "\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  # Test: mustToJson\n  # Test: toPrettyJson\n  # Test: mustToPrettyJson\n  mustToPrettyJsonValue: \"42\"\n  # Test: toRawJson\n  # Test: mustToRawJson\n  # Test: fromJson\n  # Test: fromJsonArray\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  # Test: toYaml\n  # Test: toYamlPretty\n  # Test: fromYamlArray\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n",
            "originalContent": "# Test: Type conversion errors - comprehensive testing of all type conversion functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n  {{- if .Values.testToStrings }}\n  toStringsValue: {{ toStrings $testData | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n  {{- if .Values.testToDecimal }}\n  toDecimalValue: {{ toDecimal $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  {{- if .Values.testToJson }}\n  toJsonValue: {{ toJson $testData | quote }}\n  {{- end }}\n  # Test: mustToJson\n  {{- if .Values.testMustToJson }}\n  mustToJsonValue: {{ mustToJson $testData | quote }}\n  {{- end }}\n  # Test: toPrettyJson\n  {{- if .Values.testToPrettyJson }}\n  toPrettyJsonValue: {{ toPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: mustToPrettyJson\n  {{- if .Values.testMustToPrettyJson }}\n  mustToPrettyJsonValue: {{ mustToPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: toRawJson\n  {{- if .Values.testToRawJson }}\n  toRawJsonValue: {{ toRawJson $testData | quote }}\n  {{- end }}\n  # Test: mustToRawJson\n  {{- if .Values.testMustToRawJson }}\n  mustToRawJsonValue: {{ mustToRawJson $testData | quote }}\n  {{- end }}\n  # Test: fromJson\n  {{- if .Values.testFromJson }}\n  {{- $parsed := $testData | fromJson }}\n  fromJsonValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: fromJsonArray\n  {{- if .Values.testFromJsonArray }}\n  {{- $arr := $testData | fromJsonArray }}\n  fromJsonArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  {{- if .Values.testFromYaml }}\n  {{- $parsed := $testData | fromYaml }}\n  fromYamlValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: toYaml\n  {{- if .Values.testToYaml }}\n  toYamlValue: {{ toYaml $testData | quote }}\n  {{- end }}\n  # Test: toYamlPretty\n  {{- if .Values.testToYamlPretty }}\n  toYamlPrettyValue: {{ toYamlPretty $testData | quote }}\n  {{- end }}\n  # Test: fromYamlArray\n  {{- if .Values.testFromYamlArray }}\n  {{- $arr := $testData | fromYamlArray }}\n  fromYamlArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n  {{- if .Values.testToToml }}\n  toTomlValue: {{ toToml $testData | quote }}\n  {{- end }}\n\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-type-conversion-test\r\ndata:\r\n  # ==========================================\r\n  # String conversion functions\r\n  # ==========================================\r\n  # Test: toStrings\r\n\r\n  # ==========================================\r\n  # Decimal conversion functions\r\n  # ==========================================\r\n  # Test: toDecimal\r\n\r\n  # ==========================================\r\n  # JSON conversion functions\r\n  # ==========================================\r\n  # Test: toJson\r\n  # Test: mustToJson\r\n  # Test: toPrettyJson\r\n  # Test: mustToPrettyJson\r\n  mustToPrettyJsonValue: \"42\"\r\n  # Test: toRawJson\r\n  # Test: mustToRawJson\r\n  # Test: fromJson\r\n  # Test: fromJsonArray\r\n\r\n  # ==========================================\r\n  # YAML conversion functions\r\n  # ==========================================\r\n  # Test: fromYaml\r\n  # Test: toYaml\r\n  # Test: toYamlPretty\r\n  # Test: fromYamlArray\r\n\r\n  # ==========================================\r\n  # TOML conversion functions\r\n  # ==========================================\r\n  # Test: toToml\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-type-conversion/templates/type-conversion-test.yaml",
            "content": "\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  # Test: mustToJson\n  # Test: toPrettyJson\n  # Test: mustToPrettyJson\n  mustToPrettyJsonValue: \"42\"\n  # Test: toRawJson\n  # Test: mustToRawJson\n  # Test: fromJson\n  # Test: fromJsonArray\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  # Test: toYaml\n  # Test: toYamlPretty\n  # Test: fromYamlArray\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n",
            "originalContent": "# Test: Type conversion errors - comprehensive testing of all type conversion functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n  {{- if .Values.testToStrings }}\n  toStringsValue: {{ toStrings $testData | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n  {{- if .Values.testToDecimal }}\n  toDecimalValue: {{ toDecimal $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  {{- if .Values.testToJson }}\n  toJsonValue: {{ toJson $testData | quote }}\n  {{- end }}\n  # Test: mustToJson\n  {{- if .Values.testMustToJson }}\n  mustToJsonValue: {{ mustToJson $testData | quote }}\n  {{- end }}\n  # Test: toPrettyJson\n  {{- if .Values.testToPrettyJson }}\n  toPrettyJsonValue: {{ toPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: mustToPrettyJson\n  {{- if .Values.testMustToPrettyJson }}\n  mustToPrettyJsonValue: {{ mustToPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: toRawJson\n  {{- if .Values.testToRawJson }}\n  toRawJsonValue: {{ toRawJson $testData | quote }}\n  {{- end }}\n  # Test: mustToRawJson\n  {{- if .Values.testMustToRawJson }}\n  mustToRawJsonValue: {{ mustToRawJson $testData | quote }}\n  {{- end }}\n  # Test: fromJson\n  {{- if .Values.testFromJson }}\n  {{- $parsed := $testData | fromJson }}\n  fromJsonValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: fromJsonArray\n  {{- if .Values.testFromJsonArray }}\n  {{- $arr := $testData | fromJsonArray }}\n  fromJsonArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  {{- if .Values.testFromYaml }}\n  {{- $parsed := $testData | fromYaml }}\n  fromYamlValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: toYaml\n  {{- if .Values.testToYaml }}\n  toYamlValue: {{ toYaml $testData | quote }}\n  {{- end }}\n  # Test: toYamlPretty\n  {{- if .Values.testToYamlPretty }}\n  toYamlPrettyValue: {{ toYamlPretty $testData | quote }}\n  {{- end }}\n  # Test: fromYamlArray\n  {{- if .Values.testFromYamlArray }}\n  {{- $arr := $testData | fromYamlArray }}\n  fromYamlArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n  {{- if .Values.testToToml }}\n  toTomlValue: {{ toToml $testData | quote }}\n  {{- end }}\n\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "resolved": true,
      "stopReason": "",
      "fixChain": []
    },
    {
      "toggle": "mustToPrettyJson",
      "dataType": "boolValue",
      "valuesOptions": {
        "values": [
          "testMustToPrettyJson=true",
          "testDataType=boolValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-type-conversion-test\r\ndata:\r\n  # ==========================================\r\n  # String conversion functions\r\n  # ==========================================\r\n  # Test: toStrings\r\n\r\n  # ==========================================\r\n  # Decimal conversion functions\r\n  # ==========================================\r\n  # Test: toDecimal\r\n\r\n  # ==========================================\r\n  # JSON conversion functions\r\n  # ==========================================\r\n  # Test: toJson\r\n  # Test: mustToJson\r\n  # Test: toPrettyJson\r\n  # Test: mustToPrettyJson\r\n  mustToPrettyJsonValue: \"true\"\r\n  # Test: toRawJson\r\n  # Test: mustToRawJson\r\n  # Test: fromJson\r\n  # Test: fromJsonArray\r\n\r\n  # ==========================================\r\n  # YAML conversion functions\r\n  # ==========================================\r\n  # Test: fromYaml\r\n  # Test: toYaml\r\n  # Test: toYamlPretty\r\n  # Test: fromYamlArray\r\n\r\n  # ==========================================\r\n  # TOML conversion functions\r\n  # ==========================================\r\n  # Test: toToml\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-type-conversion/templates/type-conversion-test.yaml",
            "content": "\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  # Test: mustToJson\n  # Test: toPrettyJson\n  # Test: mustToPrettyJson\n  mustToPrettyJsonValue: \"true\"\n  # Test: toRawJson\n  # Test: mustToRawJson\n  # Test: fromJson\n  # Test: fromJsonArray\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  # Test: toYaml\n  # Test: toYamlPretty\n  # Test: fromYamlArray\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n",
            "originalContent": "# Test: Type conversion errors - comprehensive testing of all type conversion functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n  {{- if .Values.testToStrings }}\n  toStringsValue: {{ toStrings $testData | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n  {{- if .Values.testToDecimal }}\n  toDecimalValue: {{ toDecimal $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  {{- if .Values.testToJson }}\n  toJsonValue: {{ toJson $testData | quote }}\n  {{- end }}\n  # Test: mustToJson\n  {{- if .Values.testMustToJson }}\n  mustToJsonValue: {{ mustToJson $testData | quote }}\n  {{- end }}\n  # Test: toPrettyJson\n  {{- if .Values.testToPrettyJson }}\n  toPrettyJsonValue: {{ toPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: mustToPrettyJson\n  {{- if .Values.testMustToPrettyJson }}\n  mustToPrettyJsonValue: {{ mustToPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: toRawJson\n  {{- if .Values.testToRawJson }}\n  toRawJsonValue: {{ toRawJson $testData | quote }}\n  {{- end }}\n  # Test: mustToRawJson\n  {{- if .Values.testMustToRawJson }}\n  mustToRawJsonValue: {{ mustToRawJson $testData | quote }}\n  {{- end }}\n  # Test: fromJson\n  {{- if .Values.testFromJson }}\n  {{- $parsed := $testData | fromJson }}\n  fromJsonValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: fromJsonArray\n  {{- if .Values.testFromJsonArray }}\n  {{- $arr := $testData | fromJsonArray }}\n  fromJsonArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  {{- if .Values.testFromYaml }}\n  {{- $parsed := $testData | fromYaml }}\n  fromYamlValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: toYaml\n  {{- if .Values.testToYaml }}\n  toYamlValue: {{ toYaml $testData | quote }}\n  {{- end }}\n  # Test: toYamlPretty\n  {{- if .Values.testToYamlPretty }}\n  toYamlPrettyValue: {{ toYamlPretty $testData | quote }}\n  {{- end }}\n  # Test: fromYamlArray\n  {{- if .Values.testFromYamlArray }}\n  {{- $arr := $testData | fromYamlArray }}\n  fromYamlArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n  {{- if .Values.testToToml }}\n  toTomlValue: {{ toToml $testData | quote }}\n  {{- end }}\n\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-type-conversion-test\r\ndata:\r\n  # ==========================================\r\n  # String conversion functions\r\n  # ==========================================\r\n  # Test: toStrings\r\n\r\n  # ==========================================\r\n  # Decimal conversion functions\r\n  # ==========================================\r\n  # Test: toDecimal\r\n\r\n  # ==========================================\r\n  # JSON conversion functions\r\n  # ==========================================\r\n  # Test: toJson\r\n  # Test: mustToJson\r\n  # Test: toPrettyJson\r\n  # Test: mustToPrettyJson\r\n  mustToPrettyJsonValue: \"true\"\r\n  # Test: toRawJson\r\n  # Test: mustToRawJson\r\n  # Test: fromJson\r\n  # Test: fromJsonArray\r\n\r\n  # ==========================================\r\n  # YAML conversion functions\r\n  # ==========================================\r\n  # Test: fromYaml\r\n  # Test: toYaml\r\n  # Test: toYamlPretty\r\n  # Test: fromYamlArray\r\n\r\n  # ==========================================\r\n  # TOML conversion functions\r\n  # ==========================================\r\n  # Test: toToml\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-type-conversion/templates/type-conversion-test.yaml",
            "content": "\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  # Test: mustToJson\n  # Test: toPrettyJson\n  # Test: mustToPrettyJson\n  mustToPrettyJsonValue: \"true\"\n  # Test: toRawJson\n  # Test: mustToRawJson\n  # Test: fromJson\n  # Test: fromJsonArray\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  # Test: toYaml\n  # Test: toYamlPretty\n  # Test: fromYamlArray\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n",
            "originalContent": "# Test: Type conversion errors - comprehensive testing of all type conversion functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n  {{- if .Values.testToStrings }}\n  toStringsValue: {{ toStrings $testData | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n  {{- if .Values.testToDecimal }}\n  toDecimalValue: {{ toDecimal $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  {{- if .Values.testToJson }}\n  toJsonValue: {{ toJson $testData | quote }}\n  {{- end }}\n  # Test: mustToJson\n  {{- if .Values.testMustToJson }}\n  mustToJsonValue: {{ mustToJson $testData | quote }}\n  {{- end }}\n  # Test: toPrettyJson\n  {{- if .Values.testToPrettyJson }}\n  toPrettyJsonValue: {{ toPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: mustToPrettyJson\n  {{- if .Values.testMustToPrettyJson }}\n  mustToPrettyJsonValue: {{ mustToPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: toRawJson\n  {{- if .Values.testToRawJson }}\n  toRawJsonValue: {{ toRawJson $testData | quote }}\n  {{- end }}\n  # Test: mustToRawJson\n  {{- if .Values.testMustToRawJson }}\n  mustToRawJsonValue: {{ mustToRawJson $testData | quote }}\n  {{- end }}\n  # Test: fromJson\n  {{- if .Values.testFromJson }}\n  {{- $parsed := $testData | fromJson }}\n  fromJsonValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: fromJsonArray\n  {{- if .Values.testFromJsonArray }}\n  {{- $arr := $testData | fromJsonArray }}\n  fromJsonArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  {{- if .Values.testFromYaml }}\n  {{- $parsed := $testData | fromYaml }}\n  fromYamlValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: toYaml\n  {{- if .Values.testToYaml }}\n  toYamlValue: {{ toYaml $testData | quote }}\n  {{- end }}\n  # Test: toYamlPretty\n  {{- if .Values.testToYamlPretty }}\n  toYamlPrettyValue: {{ toYamlPretty $testData | quote }}\n  {{- end }}\n  # Test: fromYamlArray\n  {{- if .Values.testFromYamlArray }}\n  {{- $arr := $testData | fromYamlArray }}\n  fromYamlArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n  {{- if .Values.testToToml }}\n  toTomlValue: {{ toToml $testData | quote }}\n  {{- end }}\n\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "resolved": true,
      "stopReason": "",
      "fixChain": []
    },
    {
      "toggle": "mustToPrettyJson",
      "dataType": "listValue",
      "valuesOptions": {
        "values": [
          "testMustToPrettyJson=true",
          "testDataType=listValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-type-conversion-test\r\ndata:\r\n  # ==========================================\r\n  # String conversion functions\r\n  # ==========================================\r\n  # Test: toStrings\r\n\r\n  # ==========================================\r\n  # Decimal conversion functions\r\n  # ==========================================\r\n  # Test: toDecimal\r\n\r\n  # ==========================================\r\n  # JSON conversion functions\r\n  # ==========================================\r\n  # Test: toJson\r\n  # Test: mustToJson\r\n  # Test: toPrettyJson\r\n  # Test: mustToPrettyJson\r\n  mustToPrettyJsonValue: \"[\\n  1,\\n  2,\\n  3\\n]\"\r\n  # Test: toRawJson\r\n  # Test: mustToRawJson\r\n  # Test: fromJson\r\n  # Test: fromJsonArray\r\n\r\n  # ==========================================\r\n  # YAML conversion functions\r\n  # ==========================================\r\n  # Test: fromYaml\r\n  # Test: toYaml\r\n  # Test: toYamlPretty\r\n  # Test: fromYamlArray\r\n\r\n  # ==========================================\r\n  # TOML conversion functions\r\n  # ==========================================\r\n  # Test: toToml\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-type-conversion/templates/type-conversion-test.yaml",
            "content": "\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  # Test: mustToJson\n  # Test: toPrettyJson\n  # Test: mustToPrettyJson\n  mustToPrettyJsonValue: \"[\\n  1,\\n  2,\\n  3\\n]\"\n  # Test: toRawJson\n  # Test: mustToRawJson\n  # Test: fromJson\n  # Test: fromJsonArray\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  # Test: toYaml\n  # Test: toYamlPretty\n  # Test: fromYamlArray\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n",
            "originalContent": "# Test: Type conversion errors - comprehensive testing of all type conversion functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n  {{- if .Values.testToStrings }}\n  toStringsValue: {{ toStrings $testData | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n  {{- if .Values.testToDecimal }}\n  toDecimalValue: {{ toDecimal $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  {{- if .Values.testToJson }}\n  toJsonValue: {{ toJson $testData | quote }}\n  {{- end }}\n  # Test: mustToJson\n  {{- if .Values.testMustToJson }}\n  mustToJsonValue: {{ mustToJson $testData | quote }}\n  {{- end }}\n  # Test: toPrettyJson\n  {{- if .Values.testToPrettyJson }}\n  toPrettyJsonValue: {{ toPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: mustToPrettyJson\n  {{- if .Values.testMustToPrettyJson }}\n  mustToPrettyJsonValue: {{ mustToPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: toRawJson\n  {{- if .Values.testToRawJson }}\n  toRawJsonValue: {{ toRawJson $testData | quote }}\n  {{- end }}\n  # Test: mustToRawJson\n  {{- if .Values.testMustToRawJson }}\n  mustToRawJsonValue: {{ mustToRawJson $testData | quote }}\n  {{- end }}\n  # Test: fromJson\n  {{- if .Values.testFromJson }}\n  {{- $parsed := $testData | fromJson }}\n  fromJsonValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: fromJsonArray\n  {{- if .Values.testFromJsonArray }}\n  {{- $arr := $testData | fromJsonArray }}\n  fromJsonArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  {{- if .Values.testFromYaml }}\n  {{- $parsed := $testData | fromYaml }}\n  fromYamlValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: toYaml\n  {{- if .Values.testToYaml }}\n  toYamlValue: {{ toYaml $testData | quote }}\n  {{- end }}\n  # Test: toYamlPretty\n  {{- if .Values.testToYamlPretty }}\n  toYamlPrettyValue: {{ toYamlPretty $testData | quote }}\n  {{- end }}\n  # Test: fromYamlArray\n  {{- if .Values.testFromYamlArray }}\n  {{- $arr := $testData | fromYamlArray }}\n  fromYamlArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n  {{- if .Values.testToToml }}\n  toTomlValue: {{ toToml $testData | quote }}\n  {{- end }}\n\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-type-conversion-test\r\ndata:\r\n  # ==========================================\r\n  # String conversion functions\r\n  # ==========================================\r\n  # Test: toStrings\r\n\r\n  # ==========================================\r\n  # Decimal conversion functions\r\n  # ==========================================\r\n  # Test: toDecimal\r\n\r\n  # ==========================================\r\n  # JSON conversion functions\r\n  # ==========================================\r\n  # Test: toJson\r\n  # Test: mustToJson\r\n  # Test: toPrettyJson\r\n  # Test: mustToPrettyJson\r\n  mustToPrettyJsonValue: \"[\\n  1,\\n  2,\\n  3\\n]\"\r\n  # Test: toRawJson\r\n  # Test: mustToRawJson\r\n  # Test: fromJson\r\n  # Test: fromJsonArray\r\n\r\n  # ==========================================\r\n  # YAML conversion functions\r\n  # ==========================================\r\n  # Test: fromYaml\r\n  # Test: toYaml\r\n  # Test: toYamlPretty\r\n  # Test: fromYamlArray\r\n\r\n  # ==========================================\r\n  # TOML conversion functions\r\n  # ==========================================\r\n  # Test: toToml\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-type-conversion/templates/type-conversion-test.yaml",
            "content": "\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  # Test: mustToJson\n  # Test: toPrettyJson\n  # Test: mustToPrettyJson\n  mustToPrettyJsonValue: \"[\\n  1,\\n  2,\\n  3\\n]\"\n  # Test: toRawJson\n  # Test: mustToRawJson\n  # Test: fromJson\n  # Test: fromJsonArray\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  # Test: toYaml\n  # Test: toYamlPretty\n  # Test: fromYamlArray\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n",
            "originalContent": "# Test: Type conversion errors - comprehensive testing of all type conversion functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n  {{- if .Values.testToStrings }}\n  toStringsValue: {{ toStrings $testData | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n  {{- if .Values.testToDecimal }}\n  toDecimalValue: {{ toDecimal $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  {{- if .Values.testToJson }}\n  toJsonValue: {{ toJson $testData | quote }}\n  {{- end }}\n  # Test: mustToJson\n  {{- if .Values.testMustToJson }}\n  mustToJsonValue: {{ mustToJson $testData | quote }}\n  {{- end }}\n  # Test: toPrettyJson\n  {{- if .Values.testToPrettyJson }}\n  toPrettyJsonValue: {{ toPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: mustToPrettyJson\n  {{- if .Values.testMustToPrettyJson }}\n  mustToPrettyJsonValue: {{ mustToPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: toRawJson\n  {{- if .Values.testToRawJson }}\n  toRawJsonValue: {{ toRawJson $testData | quote }}\n  {{- end }}\n  # Test: mustToRawJson\n  {{- if .Values.testMustToRawJson }}\n  mustToRawJsonValue: {{ mustToRawJson $testData | quote }}\n  {{- end }}\n  # Test: fromJson\n  {{- if .Values.testFromJson }}\n  {{- $parsed := $testData | fromJson }}\n  fromJsonValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: fromJsonArray\n  {{- if .Values.testFromJsonArray }}\n  {{- $arr := $testData | fromJsonArray }}\n  fromJsonArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  {{- if .Values.testFromYaml }}\n  {{- $parsed := $testData | fromYaml }}\n  fromYamlValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: toYaml\n  {{- if .Values.testToYaml }}\n  toYamlValue: {{ toYaml $testData | quote }}\n  {{- end }}\n  # Test: toYamlPretty\n  {{- if .Values.testToYamlPretty }}\n  toYamlPrettyValue: {{ toYamlPretty $testData | quote }}\n  {{- end }}\n  # Test: fromYamlArray\n  {{- if .Values.testFromYamlArray }}\n  {{- $arr := $testData | fromYamlArray }}\n  fromYamlArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n  {{- if .Values.testToToml }}\n  toTomlValue: {{ toToml $testData | quote }}\n  {{- end }}\n\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "resolved": true,
      "stopReason": "",
      "fixChain": []
    },
    {
      "toggle": "mustToPrettyJson",
      "dataType": "dictValue",
      "valuesOptions": {
        "values": [
          "testMustToPrettyJson=true",
          "testDataType=dictValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-type-conversion-test\r\ndata:\r\n  # ==========================================\r\n  # String conversion functions\r\n  # ==========================================\r\n  # Test: toStrings\r\n\r\n  # ==========================================\r\n  # Decimal conversion functions\r\n  # ==========================================\r\n  # Test: toDecimal\r\n\r\n  # ==========================================\r\n  # JSON conversion functions\r\n  # ==========================================\r\n  # Test: toJson\r\n  # Test: mustToJson\r\n  # Test: toPrettyJson\r\n  # Test: mustToPrettyJson\r\n  mustToPrettyJsonValue: \"{\\n  \\\"key\\\": \\\"value\\\"\\n}\"\r\n  # Test: toRawJson\r\n  # Test: mustToRawJson\r\n  # Test: fromJson\r\n  # Test: fromJsonArray\r\n\r\n  # ==========================================\r\n  # YAML conversion functions\r\n  # ==========================================\r\n  # Test: fromYaml\r\n  # Test: toYaml\r\n  # Test: toYamlPretty\r\n  # Test: fromYamlArray\r\n\r\n  # ==========================================\r\n  # TOML conversion functions\r\n  # ==========================================\r\n  # Test: toToml\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-type-conversion/templates/type-conversion-test.yaml",
            "content": "\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  # Test: mustToJson\n  # Test: toPrettyJson\n  # Test: mustToPrettyJson\n  mustToPrettyJsonValue: \"{\\n  \\\"key\\\": \\\"value\\\"\\n}\"\n  # Test: toRawJson\n  # Test: mustToRawJson\n  # Test: fromJson\n  # Test: fromJsonArray\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  # Test: toYaml\n  # Test: toYamlPretty\n  # Test: fromYamlArray\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n",
            "originalContent": "# Test: Type conversion errors - comprehensive testing of all type conversion functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n  {{- if .Values.testToStrings }}\n  toStringsValue: {{ toStrings $testData | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n  {{- if .Values.testToDecimal }}\n  toDecimalValue: {{ toDecimal $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  {{- if .Values.testToJson }}\n  toJsonValue: {{ toJson $testData | quote }}\n  {{- end }}\n  # Test: mustToJson\n  {{- if .Values.testMustToJson }}\n  mustToJsonValue: {{ mustToJson $testData | quote }}\n  {{- end }}\n  # Test: toPrettyJson\n  {{- if .Values.testToPrettyJson }}\n  toPrettyJsonValue: {{ toPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: mustToPrettyJson\n  {{- if .Values.testMustToPrettyJson }}\n  mustToPrettyJsonValue: {{ mustToPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: toRawJson\n  {{- if .Values.testToRawJson }}\n  toRawJsonValue: {{ toRawJson $testData | quote }}\n  {{- end }}\n  # Test: mustToRawJson\n  {{- if .Values.testMustToRawJson }}\n  mustToRawJsonValue: {{ mustToRawJson $testData | quote }}\n  {{- end }}\n  # Test: fromJson\n  {{- if .Values.testFromJson }}\n  {{- $parsed := $testData | fromJson }}\n  fromJsonValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: fromJsonArray\n  {{- if .Values.testFromJsonArray }}\n  {{- $arr := $testData | fromJsonArray }}\n  fromJsonArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  {{- if .Values.testFromYaml }}\n  {{- $parsed := $testData | fromYaml }}\n  fromYamlValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: toYaml\n  {{- if .Values.testToYaml }}\n  toYamlValue: {{ toYaml $testData | quote }}\n  {{- end }}\n  # Test: toYamlPretty\n  {{- if .Values.testToYamlPretty }}\n  toYamlPrettyValue: {{ toYamlPretty $testData | quote }}\n  {{- end }}\n  # Test: fromYamlArray\n  {{- if .Values.testFromYamlArray }}\n  {{- $arr := $testData | fromYamlArray }}\n  fromYamlArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n  {{- if .Values.testToToml }}\n  toTomlValue: {{ toToml $testData | quote }}\n  {{- end }}\n\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-type-conversion-test\r\ndata:\r\n  # ==========================================\r\n  # String conversion functions\r\n  # ==========================================\r\n  # Test: toStrings\r\n\r\n  # ==========================================\r\n  # Decimal conversion functions\r\n  # ==========================================\r\n  # Test: toDecimal\r\n\r\n  # ==========================================\r\n  # JSON conversion functions\r\n  # ==========================================\r\n  # Test: toJson\r\n  # Test: mustToJson\r\n  # Test: toPrettyJson\r\n  # Test: mustToPrettyJson\r\n  mustToPrettyJsonValue: \"{\\n  \\\"key\\\": \\\"value\\\"\\n}\"\r\n  # Test: toRawJson\r\n  # Test: mustToRawJson\r\n  # Test: fromJson\r\n  # Test: fromJsonArray\r\n\r\n  # ==========================================\r\n  # YAML conversion functions\r\n  # ==========================================\r\n  # Test: fromYaml\r\n  # Test: toYaml\r\n  # Test: toYamlPretty\r\n  # Test: fromYamlArray\r\n\r\n  # ==========================================\r\n  # TOML conversion functions\r\n  # ==========================================\r\n  # Test: toToml\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-type-conversion/templates/type-conversion-test.yaml",
            "content": "\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  # Test: mustToJson\n  # Test: toPrettyJson\n  # Test: mustToPrettyJson\n  mustToPrettyJsonValue: \"{\\n  \\\"key\\\": \\\"value\\\"\\n}\"\n  # Test: toRawJson\n  # Test: mustToRawJson\n  # Test: fromJson\n  # Test: fromJsonArray\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  # Test: toYaml\n  # Test: toYamlPretty\n  # Test: fromYamlArray\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n",
            "originalContent": "# Test: Type conversion errors - comprehensive testing of all type conversion functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n  {{- if .Values.testToStrings }}\n  toStringsValue: {{ toStrings $testData | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n  {{- if .Values.testToDecimal }}\n  toDecimalValue: {{ toDecimal $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  {{- if .Values.testToJson }}\n  toJsonValue: {{ toJson $testData | quote }}\n  {{- end }}\n  # Test: mustToJson\n  {{- if .Values.testMustToJson }}\n  mustToJsonValue: {{ mustToJson $testData | quote }}\n  {{- end }}\n  # Test: toPrettyJson\n  {{- if .Values.testToPrettyJson }}\n  toPrettyJsonValue: {{ toPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: mustToPrettyJson\n  {{- if .Values.testMustToPrettyJson }}\n  mustToPrettyJsonValue: {{ mustToPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: toRawJson\n  {{- if .Values.testToRawJson }}\n  toRawJsonValue: {{ toRawJson $testData | quote }}\n  {{- end }}\n  # Test: mustToRawJson\n  {{- if .Values.testMustToRawJson }}\n  mustToRawJsonValue: {{ mustToRawJson $testData | quote }}\n  {{- end }}\n  # Test: fromJson\n  {{- if .Values.testFromJson }}\n  {{- $parsed := $testData | fromJson }}\n  fromJsonValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: fromJsonArray\n  {{- if .Values.testFromJsonArray }}\n  {{- $arr := $testData | fromJsonArray }}\n  fromJsonArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  {{- if .Values.testFromYaml }}\n  {{- $parsed := $testData | fromYaml }}\n  fromYamlValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: toYaml\n  {{- if .Values.testToYaml }}\n  toYamlValue: {{ toYaml $testData | quote }}\n  {{- end }}\n  # Test: toYamlPretty\n  {{- if .Values.testToYamlPretty }}\n  toYamlPrettyValue: {{ toYamlPretty $testData | quote }}\n  {{- end }}\n  # Test: fromYamlArray\n  {{- if .Values.testFromYamlArray }}\n  {{- $arr := $testData | fromYamlArray }}\n  fromYamlArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n  {{- if .Values.testToToml }}\n  toTomlValue: {{ toToml $testData | quote }}\n  {{- end }}\n\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "resolved": true,
      "stopReason": "",
      "fixChain": []
    },
    {
      "toggle": "toRawJson",
      "dataType": "nilValue",
      "valuesOptions": {
        "values": [
          "testToRawJson=true",
          "testDataType=nilValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-type-conversion-test\r\ndata:\r\n  # ==========================================\r\n  # String conversion functions\r\n  # ==========================================\r\n  # Test: toStrings\r\n\r\n  # ==========================================\r\n  # Decimal conversion functions\r\n  # ==========================================\r\n  # Test: toDecimal\r\n\r\n  # ==========================================\r\n  # JSON conversion functions\r\n  # ==========================================\r\n  # Test: toJson\r\n  # Test: mustToJson\r\n  # Test: toPrettyJson\r\n  # Test: mustToPrettyJson\r\n  # Test: toRawJson\r\n  toRawJsonValue: \"null\"\r\n  # Test: mustToRawJson\r\n  # Test: fromJson\r\n  # Test: fromJsonArray\r\n\r\n  # ==========================================\r\n  # YAML conversion functions\r\n  # ==========================================\r\n  # Test: fromYaml\r\n  # Test: toYaml\r\n  # Test: toYamlPretty\r\n  # Test: fromYamlArray\r\n\r\n  # ==========================================\r\n  # TOML conversion functions\r\n  # ==========================================\r\n  # Test: toToml\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-type-conversion/templates/type-conversion-test.yaml",
            "content": "\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  # Test: mustToJson\n  # Test: toPrettyJson\n  # Test: mustToPrettyJson\n  # Test: toRawJson\n  toRawJsonValue: \"null\"\n  # Test: mustToRawJson\n  # Test: fromJson\n  # Test: fromJsonArray\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  # Test: toYaml\n  # Test: toYamlPretty\n  # Test: fromYamlArray\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n",
            "originalContent": "# Test: Type conversion errors - comprehensive testing of all type conversion functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n  {{- if .Values.testToStrings }}\n  toStringsValue: {{ toStrings $testData | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n  {{- if .Values.testToDecimal }}\n  toDecimalValue: {{ toDecimal $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  {{- if .Values.testToJson }}\n  toJsonValue: {{ toJson $testData | quote }}\n  {{- end }}\n  # Test: mustToJson\n  {{- if .Values.testMustToJson }}\n  mustToJsonValue: {{ mustToJson $testData | quote }}\n  {{- end }}\n  # Test: toPrettyJson\n  {{- if .Values.testToPrettyJson }}\n  toPrettyJsonValue: {{ toPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: mustToPrettyJson\n  {{- if .Values.testMustToPrettyJson }}\n  mustToPrettyJsonValue: {{ mustToPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: toRawJson\n  {{- if .Values.testToRawJson }}\n  toRawJsonValue: {{ toRawJson $testData | quote }}\n  {{- end }}\n  # Test: mustToRawJson\n  {{- if .Values.testMustToRawJson }}\n  mustToRawJsonValue: {{ mustToRawJson $testData | quote }}\n  {{- end }}\n  # Test: fromJson\n  {{- if .Values.testFromJson }}\n  {{- $parsed := $testData | fromJson }}\n  fromJsonValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: fromJsonArray\n  {{- if .Values.testFromJsonArray }}\n  {{- $arr := $testData | fromJsonArray }}\n  fromJsonArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  {{- if .Values.testFromYaml }}\n  {{- $parsed := $testData | fromYaml }}\n  fromYamlValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: toYaml\n  {{- if .Values.testToYaml }}\n  toYamlValue: {{ toYaml $testData | quote }}\n  {{- end }}\n  # Test: toYamlPretty\n  {{- if .Values.testToYamlPretty }}\n  toYamlPrettyValue: {{ toYamlPretty $testData | quote }}\n  {{- end }}\n  # Test: fromYamlArray\n  {{- if .Values.testFromYamlArray }}\n  {{- $arr := $testData | fromYamlArray }}\n  fromYamlArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n  {{- if .Values.testToToml }}\n  toTomlValue: {{ toToml $testData | quote }}\n  {{- end }}\n\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-type-conversion-test\r\ndata:\r\n  # ==========================================\r\n  # String conversion functions\r\n  # ==========================================\r\n  # Test: toStrings\r\n\r\n  # ==========================================\r\n  # Decimal conversion functions\r\n  # ==========================================\r\n  # Test: toDecimal\r\n\r\n  # ==========================================\r\n  # JSON conversion functions\r\n  # ==========================================\r\n  # Test: toJson\r\n  # Test: mustToJson\r\n  # Test: toPrettyJson\r\n  # Test: mustToPrettyJson\r\n  # Test: toRawJson\r\n  toRawJsonValue: \"null\"\r\n  # Test: mustToRawJson\r\n  # Test: fromJson\r\n  # Test: fromJsonArray\r\n\r\n  # ==========================================\r\n  # YAML conversion functions\r\n  # ==========================================\r\n  # Test: fromYaml\r\n  # Test: toYaml\r\n  # Test: toYamlPretty\r\n  # Test: fromYamlArray\r\n\r\n  # ==========================================\r\n  # TOML conversion functions\r\n  # ==========================================\r\n  # Test: toToml\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-type-conversion/templates/type-conversion-test.yaml",
            "content": "\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  # Test: mustToJson\n  # Test: toPrettyJson\n  # Test: mustToPrettyJson\n  # Test: toRawJson\n  toRawJsonValue: \"null\"\n  # Test: mustToRawJson\n  # Test: fromJson\n  # Test: fromJsonArray\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  # Test: toYaml\n  # Test: toYamlPretty\n  # Test: fromYamlArray\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n",
            "originalContent": "# Test: Type conversion errors - comprehensive testing of all type conversion functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n  {{- if .Values.testToStrings }}\n  toStringsValue: {{ toStrings $testData | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n  {{- if .Values.testToDecimal }}\n  toDecimalValue: {{ toDecimal $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  {{- if .Values.testToJson }}\n  toJsonValue: {{ toJson $testData | quote }}\n  {{- end }}\n  # Test: mustToJson\n  {{- if .Values.testMustToJson }}\n  mustToJsonValue: {{ mustToJson $testData | quote }}\n  {{- end }}\n  # Test: toPrettyJson\n  {{- if .Values.testToPrettyJson }}\n  toPrettyJsonValue: {{ toPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: mustToPrettyJson\n  {{- if .Values.testMustToPrettyJson }}\n  mustToPrettyJsonValue: {{ mustToPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: toRawJson\n  {{- if .Values.testToRawJson }}\n  toRawJsonValue: {{ toRawJson $testData | quote }}\n  {{- end }}\n  # Test: mustToRawJson\n  {{- if .Values.testMustToRawJson }}\n  mustToRawJsonValue: {{ mustToRawJson $testData | quote }}\n  {{- end }}\n  # Test: fromJson\n  {{- if .Values.testFromJson }}\n  {{- $parsed := $testData | fromJson }}\n  fromJsonValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: fromJsonArray\n  {{- if .Values.testFromJsonArray }}\n  {{- $arr := $testData | fromJsonArray }}\n  fromJsonArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  {{- if .Values.testFromYaml }}\n  {{- $parsed := $testData | fromYaml }}\n  fromYamlValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: toYaml\n  {{- if .Values.testToYaml }}\n  toYamlValue: {{ toYaml $testData | quote }}\n  {{- end }}\n  # Test: toYamlPretty\n  {{- if .Values.testToYamlPretty }}\n  toYamlPrettyValue: {{ toYamlPretty $testData | quote }}\n  {{- end }}\n  # Test: fromYamlArray\n  {{- if .Values.testFromYamlArray }}\n  {{- $arr := $testData | fromYamlArray }}\n  fromYamlArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n  {{- if .Values.testToToml }}\n  toTomlValue: {{ toToml $testData | quote }}\n  {{- end }}\n\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "resolved": true,
      "stopReason": "",
      "fixChain": []
    },
    {
      "toggle": "toRawJson",
      "dataType": "stringValue",
      "valuesOptions": {
        "values": [
          "testToRawJson=true",
          "testDataType=stringValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-type-conversion-test\r\ndata:\r\n  # ==========================================\r\n  # String conversion functions\r\n  # ==========================================\r\n  # Test: toStrings\r\n\r\n  # ==========================================\r\n  # Decimal conversion functions\r\n  # ==========================================\r\n  # Test: toDecimal\r\n\r\n  # ==========================================\r\n  # JSON conversion functions\r\n  # ==========================================\r\n  # Test: toJson\r\n  # Test: mustToJson\r\n  # Test: toPrettyJson\r\n  # Test: mustToPrettyJson\r\n  # Test: toRawJson\r\n  toRawJsonValue: \"\\\"test string\\\"\"\r\n  # Test: mustToRawJson\r\n  # Test: fromJson\r\n  # Test: fromJsonArray\r\n\r\n  # ==========================================\r\n  # YAML conversion functions\r\n  # ==========================================\r\n  # Test: fromYaml\r\n  # Test: toYaml\r\n  # Test: toYamlPretty\r\n  # Test: fromYamlArray\r\n\r\n  # ==========================================\r\n  # TOML conversion functions\r\n  # ==========================================\r\n  # Test: toToml\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-type-conversion/templates/type-conversion-test.yaml",
            "content": "\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  # Test: mustToJson\n  # Test: toPrettyJson\n  # Test: mustToPrettyJson\n  # Test: toRawJson\n  toRawJsonValue: \"\\\"test string\\\"\"\n  # Test: mustToRawJson\n  # Test: fromJson\n  # Test: fromJsonArray\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  # Test: toYaml\n  # Test: toYamlPretty\n  # Test: fromYamlArray\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n",
            "originalContent": "# Test: Type conversion errors - comprehensive testing of all type conversion functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n  {{- if .Values.testToStrings }}\n  toStringsValue: {{ toStrings $testData | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n  {{- if .Values.testToDecimal }}\n  toDecimalValue: {{ toDecimal $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  {{- if .Values.testToJson }}\n  toJsonValue: {{ toJson $testData | quote }}\n  {{- end }}\n  # Test: mustToJson\n  {{- if .Values.testMustToJson }}\n  mustToJsonValue: {{ mustToJson $testData | quote }}\n  {{- end }}\n  # Test: toPrettyJson\n  {{- if .Values.testToPrettyJson }}\n  toPrettyJsonValue: {{ toPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: mustToPrettyJson\n  {{- if .Values.testMustToPrettyJson }}\n  mustToPrettyJsonValue: {{ mustToPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: toRawJson\n  {{- if .Values.testToRawJson }}\n  toRawJsonValue: {{ toRawJson $testData | quote }}\n  {{- end }}\n  # Test: mustToRawJson\n  {{- if .Values.testMustToRawJson }}\n  mustToRawJsonValue: {{ mustToRawJson $testData | quote }}\n  {{- end }}\n  # Test: fromJson\n  {{- if .Values.testFromJson }}\n  {{- $parsed := $testData | fromJson }}\n  fromJsonValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: fromJsonArray\n  {{- if .Values.testFromJsonArray }}\n  {{- $arr := $testData | fromJsonArray }}\n  fromJsonArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  {{- if .Values.testFromYaml }}\n  {{- $parsed := $testData | fromYaml }}\n  fromYamlValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: toYaml\n  {{- if .Values.testToYaml }}\n  toYamlValue: {{ toYaml $testData | quote }}\n  {{- end }}\n  # Test: toYamlPretty\n  {{- if .Values.testToYamlPretty }}\n  toYamlPrettyValue: {{ toYamlPretty $testData | quote }}\n  {{- end }}\n  # Test: fromYamlArray\n  {{- if .Values.testFromYamlArray }}\n  {{- $arr := $testData | fromYamlArray }}\n  fromYamlArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n  {{- if .Values.testToToml }}\n  toTomlValue: {{ toToml $testData | quote }}\n  {{- end }}\n\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-type-conversion-test\r\ndata:\r\n  # ==========================================\r\n  # String conversion functions\r\n  # ==========================================\r\n  # Test: toStrings\r\n\r\n  # ==========================================\r\n  # Decimal conversion functions\r\n  # ==========================================\r\n  # Test: toDecimal\r\n\r\n  # ==========================================\r\n  # JSON conversion functions\r\n  # ==========================================\r\n  # Test: toJson\r\n  # Test: mustToJson\r\n  # Test: toPrettyJson\r\n  # Test: mustToPrettyJson\r\n  # Test: toRawJson\r\n  toRawJsonValue: \"\\\"test string\\\"\"\r\n  # Test: mustToRawJson\r\n  # Test: fromJson\r\n  # Test: fromJsonArray\r\n\r\n  # ==========================================\r\n  # YAML conversion functions\r\n  # ==========================================\r\n  # Test: fromYaml\r\n  # Test: toYaml\r\n  # Test: toYamlPretty\r\n  # Test: fromYamlArray\r\n\r\n  # ==========================================\r\n  # TOML conversion functions\r\n  # ==========================================\r\n  # Test: toToml\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-type-conversion/templates/type-conversion-test.yaml",
            "content": "\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  # Test: mustToJson\n  # Test: toPrettyJson\n  # Test: mustToPrettyJson\n  # Test: toRawJson\n  toRawJsonValue: \"\\\"test string\\\"\"\n  # Test: mustToRawJson\n  # Test: fromJson\n  # Test: fromJsonArray\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  # Test: toYaml\n  # Test: toYamlPretty\n  # Test: fromYamlArray\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n",
            "originalContent": "# Test: Type conversion errors - comprehensive testing of all type conversion functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n  {{- if .Values.testToStrings }}\n  toStringsValue: {{ toStrings $testData | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n  {{- if .Values.testToDecimal }}\n  toDecimalValue: {{ toDecimal $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  {{- if .Values.testToJson }}\n  toJsonValue: {{ toJson $testData | quote }}\n  {{- end }}\n  # Test: mustToJson\n  {{- if .Values.testMustToJson }}\n  mustToJsonValue: {{ mustToJson $testData | quote }}\n  {{- end }}\n  # Test: toPrettyJson\n  {{- if .Values.testToPrettyJson }}\n  toPrettyJsonValue: {{ toPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: mustToPrettyJson\n  {{- if .Values.testMustToPrettyJson }}\n  mustToPrettyJsonValue: {{ mustToPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: toRawJson\n  {{- if .Values.testToRawJson }}\n  toRawJsonValue: {{ toRawJson $testData | quote }}\n  {{- end }}\n  # Test: mustToRawJson\n  {{- if .Values.testMustToRawJson }}\n  mustToRawJsonValue: {{ mustToRawJson $testData | quote }}\n  {{- end }}\n  # Test: fromJson\n  {{- if .Values.testFromJson }}\n  {{- $parsed := $testData | fromJson }}\n  fromJsonValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: fromJsonArray\n  {{- if .Values.testFromJsonArray }}\n  {{- $arr := $testData | fromJsonArray }}\n  fromJsonArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  {{- if .Values.testFromYaml }}\n  {{- $parsed := $testData | fromYaml }}\n  fromYamlValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: toYaml\n  {{- if .Values.testToYaml }}\n  toYamlValue: {{ toYaml $testData | quote }}\n  {{- end }}\n  # Test: toYamlPretty\n  {{- if .Values.testToYamlPretty }}\n  toYamlPrettyValue: {{ toYamlPretty $testData | quote }}\n  {{- end }}\n  # Test: fromYamlArray\n  {{- if .Values.testFromYamlArray }}\n  {{- $arr := $testData | fromYamlArray }}\n  fromYamlArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n  {{- if .Values.testToToml }}\n  toTomlValue: {{ toToml $testData | quote }}\n  {{- end }}\n\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "resolved": true,
      "stopReason": "",
      "fixChain": []
    },
    {
      "toggle": "toRawJson",
      "dataType": "numberValue",
      "valuesOptions": {
        "values": [
          "testToRawJson=true",
          "testDataType=numberValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-type-conversion-test\r\ndata:\r\n  # ==========================================\r\n  # String conversion functions\r\n  # ==========================================\r\n  # Test: toStrings\r\n\r\n  # ==========================================\r\n  # Decimal conversion functions\r\n  # ==========================================\r\n  # Test: toDecimal\r\n\r\n  # ==========================================\r\n  # JSON conversion functions\r\n  # ==========================================\r\n  # Test: toJson\r\n  # Test: mustToJson\r\n  # Test: toPrettyJson\r\n  # Test: mustToPrettyJson\r\n  # Test: toRawJson\r\n  toRawJsonValue: \"42\"\r\n  # Test: mustToRawJson\r\n  # Test: fromJson\r\n  # Test: fromJsonArray\r\n\r\n  # ==========================================\r\n  # YAML conversion functions\r\n  # ==========================================\r\n  # Test: fromYaml\r\n  # Test: toYaml\r\n  # Test: toYamlPretty\r\n  # Test: fromYamlArray\r\n\r\n  # ==========================================\r\n  # TOML conversion functions\r\n  # ==========================================\r\n  # Test: toToml\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-type-conversion/templates/type-conversion-test.yaml",
            "content": "\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  # Test: mustToJson\n  # Test: toPrettyJson\n  # Test: mustToPrettyJson\n  # Test: toRawJson\n  toRawJsonValue: \"42\"\n  # Test: mustToRawJson\n  # Test: fromJson\n  # Test: fromJsonArray\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  # Test: toYaml\n  # Test: toYamlPretty\n  # Test: fromYamlArray\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n",
            "originalContent": "# Test: Type conversion errors - comprehensive testing of all type conversion functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n  {{- if .Values.testToStrings }}\n  toStringsValue: {{ toStrings $testData | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n  {{- if .Values.testToDecimal }}\n  toDecimalValue: {{ toDecimal $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  {{- if .Values.testToJson }}\n  toJsonValue: {{ toJson $testData | quote }}\n  {{- end }}\n  # Test: mustToJson\n  {{- if .Values.testMustToJson }}\n  mustToJsonValue: {{ mustToJson $testData | quote }}\n  {{- end }}\n  # Test: toPrettyJson\n  {{- if .Values.testToPrettyJson }}\n  toPrettyJsonValue: {{ toPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: mustToPrettyJson\n  {{- if .Values.testMustToPrettyJson }}\n  mustToPrettyJsonValue: {{ mustToPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: toRawJson\n  {{- if .Values.testToRawJson }}\n  toRawJsonValue: {{ toRawJson $testData | quote }}\n  {{- end }}\n  # Test: mustToRawJson\n  {{- if .Values.testMustToRawJson }}\n  mustToRawJsonValue: {{ mustToRawJson $testData | quote }}\n  {{- end }}\n  # Test: fromJson\n  {{- if .Values.testFromJson }}\n  {{- $parsed := $testData | fromJson }}\n  fromJsonValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: fromJsonArray\n  {{- if .Values.testFromJsonArray }}\n  {{- $arr := $testData | fromJsonArray }}\n  fromJsonArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  {{- if .Values.testFromYaml }}\n  {{- $parsed := $testData | fromYaml }}\n  fromYamlValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: toYaml\n  {{- if .Values.testToYaml }}\n  toYamlValue: {{ toYaml $testData | quote }}\n  {{- end }}\n  # Test: toYamlPretty\n  {{- if .Values.testToYamlPretty }}\n  toYamlPrettyValue: {{ toYamlPretty $testData | quote }}\n  {{- end }}\n  # Test: fromYamlArray\n  {{- if .Values.testFromYamlArray }}\n  {{- $arr := $testData | fromYamlArray }}\n  fromYamlArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n  {{- if .Values.testToToml }}\n  toTomlValue: {{ toToml $testData | quote }}\n  {{- end }}\n\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-type-conversion-test\r\ndata:\r\n  # ==========================================\r\n  # String conversion functions\r\n  # ==========================================\r\n  # Test: toStrings\r\n\r\n  # ==========================================\r\n  # Decimal conversion functions\r\n  # ==========================================\r\n  # Test: toDecimal\r\n\r\n  # ==========================================\r\n  # JSON conversion functions\r\n  # ==========================================\r\n  # Test: toJson\r\n  # Test: mustToJson\r\n  # Test: toPrettyJson\r\n  # Test: mustToPrettyJson\r\n  # Test: toRawJson\r\n  toRawJsonValue: \"42\"\r\n  # Test: mustToRawJson\r\n  # Test: fromJson\r\n  # Test: fromJsonArray\r\n\r\n  # ==========================================\r\n  # YAML conversion functions\r\n  # ==========================================\r\n  # Test: fromYaml\r\n  # Test: toYaml\r\n  # Test: toYamlPretty\r\n  # Test: fromYamlArray\r\n\r\n  # ==========================================\r\n  # TOML conversion functions\r\n  # ==========================================\r\n  # Test: toToml\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-type-conversion/templates/type-conversion-test.yaml",
            "content": "\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  # Test: mustToJson\n  # Test: toPrettyJson\n  # Test: mustToPrettyJson\n  # Test: toRawJson\n  toRawJsonValue: \"42\"\n  # Test: mustToRawJson\n  # Test: fromJson\n  # Test: fromJsonArray\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  # Test: toYaml\n  # Test: toYamlPretty\n  # Test: fromYamlArray\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n",
            "originalContent": "# Test: Type conversion errors - comprehensive testing of all type conversion functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n  {{- if .Values.testToStrings }}\n  toStringsValue: {{ toStrings $testData | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n  {{- if .Values.testToDecimal }}\n  toDecimalValue: {{ toDecimal $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  {{- if .Values.testToJson }}\n  toJsonValue: {{ toJson $testData | quote }}\n  {{- end }}\n  # Test: mustToJson\n  {{- if .Values.testMustToJson }}\n  mustToJsonValue: {{ mustToJson $testData | quote }}\n  {{- end }}\n  # Test: toPrettyJson\n  {{- if .Values.testToPrettyJson }}\n  toPrettyJsonValue: {{ toPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: mustToPrettyJson\n  {{- if .Values.testMustToPrettyJson }}\n  mustToPrettyJsonValue: {{ mustToPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: toRawJson\n  {{- if .Values.testToRawJson }}\n  toRawJsonValue: {{ toRawJson $testData | quote }}\n  {{- end }}\n  # Test: mustToRawJson\n  {{- if .Values.testMustToRawJson }}\n  mustToRawJsonValue: {{ mustToRawJson $testData | quote }}\n  {{- end }}\n  # Test: fromJson\n  {{- if .Values.testFromJson }}\n  {{- $parsed := $testData | fromJson }}\n  fromJsonValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: fromJsonArray\n  {{- if .Values.testFromJsonArray }}\n  {{- $arr := $testData | fromJsonArray }}\n  fromJsonArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  {{- if .Values.testFromYaml }}\n  {{- $parsed := $testData | fromYaml }}\n  fromYamlValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: toYaml\n  {{- if .Values.testToYaml }}\n  toYamlValue: {{ toYaml $testData | quote }}\n  {{- end }}\n  # Test: toYamlPretty\n  {{- if .Values.testToYamlPretty }}\n  toYamlPrettyValue: {{ toYamlPretty $testData | quote }}\n  {{- end }}\n  # Test: fromYamlArray\n  {{- if .Values.testFromYamlArray }}\n  {{- $arr := $testData | fromYamlArray }}\n  fromYamlArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n  {{- if .Values.testToToml }}\n  toTomlValue: {{ toToml $testData | quote }}\n  {{- end }}\n\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "resolved": true,
      "stopReason": "",
      "fixChain": []
    },
    {
      "toggle": "toRawJson",
      "dataType": "boolValue",
      "valuesOptions": {
        "values": [
          "testToRawJson=true",
          "testDataType=boolValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-type-conversion-test\r\ndata:\r\n  # ==========================================\r\n  # String conversion functions\r\n  # ==========================================\r\n  # Test: toStrings\r\n\r\n  # ==========================================\r\n  # Decimal conversion functions\r\n  # ==========================================\r\n  # Test: toDecimal\r\n\r\n  # ==========================================\r\n  # JSON conversion functions\r\n  # ==========================================\r\n  # Test: toJson\r\n  # Test: mustToJson\r\n  # Test: toPrettyJson\r\n  # Test: mustToPrettyJson\r\n  # Test: toRawJson\r\n  toRawJsonValue: \"true\"\r\n  # Test: mustToRawJson\r\n  # Test: fromJson\r\n  # Test: fromJsonArray\r\n\r\n  # ==========================================\r\n  # YAML conversion functions\r\n  # ==========================================\r\n  # Test: fromYaml\r\n  # Test: toYaml\r\n  # Test: toYamlPretty\r\n  # Test: fromYamlArray\r\n\r\n  # ==========================================\r\n  # TOML conversion functions\r\n  # ==========================================\r\n  # Test: toToml\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-type-conversion/templates/type-conversion-test.yaml",
            "content": "\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  # Test: mustToJson\n  # Test: toPrettyJson\n  # Test: mustToPrettyJson\n  # Test: toRawJson\n  toRawJsonValue: \"true\"\n  # Test: mustToRawJson\n  # Test: fromJson\n  # Test: fromJsonArray\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  # Test: toYaml\n  # Test: toYamlPretty\n  # Test: fromYamlArray\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n",
            "originalContent": "# Test: Type conversion errors - comprehensive testing of all type conversion functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n  {{- if .Values.testToStrings }}\n  toStringsValue: {{ toStrings $testData | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n  {{- if .Values.testToDecimal }}\n  toDecimalValue: {{ toDecimal $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  {{- if .Values.testToJson }}\n  toJsonValue: {{ toJson $testData | quote }}\n  {{- end }}\n  # Test: mustToJson\n  {{- if .Values.testMustToJson }}\n  mustToJsonValue: {{ mustToJson $testData | quote }}\n  {{- end }}\n  # Test: toPrettyJson\n  {{- if .Values.testToPrettyJson }}\n  toPrettyJsonValue: {{ toPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: mustToPrettyJson\n  {{- if .Values.testMustToPrettyJson }}\n  mustToPrettyJsonValue: {{ mustToPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: toRawJson\n  {{- if .Values.testToRawJson }}\n  toRawJsonValue: {{ toRawJson $testData | quote }}\n  {{- end }}\n  # Test: mustToRawJson\n  {{- if .Values.testMustToRawJson }}\n  mustToRawJsonValue: {{ mustToRawJson $testData | quote }}\n  {{- end }}\n  # Test: fromJson\n  {{- if .Values.testFromJson }}\n  {{- $parsed := $testData | fromJson }}\n  fromJsonValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: fromJsonArray\n  {{- if .Values.testFromJsonArray }}\n  {{- $arr := $testData | fromJsonArray }}\n  fromJsonArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  {{- if .Values.testFromYaml }}\n  {{- $parsed := $testData | fromYaml }}\n  fromYamlValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: toYaml\n  {{- if .Values.testToYaml }}\n  toYamlValue: {{ toYaml $testData | quote }}\n  {{- end }}\n  # Test: toYamlPretty\n  {{- if .Values.testToYamlPretty }}\n  toYamlPrettyValue: {{ toYamlPretty $testData | quote }}\n  {{- end }}\n  # Test: fromYamlArray\n  {{- if .Values.testFromYamlArray }}\n  {{- $arr := $testData | fromYamlArray }}\n  fromYamlArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n  {{- if .Values.testToToml }}\n  toTomlValue: {{ toToml $testData | quote }}\n  {{- end }}\n\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-type-conversion-test\r\ndata:\r\n  # ==========================================\r\n  # String conversion functions\r\n  # ==========================================\r\n  # Test: toStrings\r\n\r\n  # ==========================================\r\n  # Decimal conversion functions\r\n  # ==========================================\r\n  # Test: toDecimal\r\n\r\n  # ==========================================\r\n  # JSON conversion functions\r\n  # ==========================================\r\n  # Test: toJson\r\n  # Test: mustToJson\r\n  # Test: toPrettyJson\r\n  # Test: mustToPrettyJson\r\n  # Test: toRawJson\r\n  toRawJsonValue: \"true\"\r\n  # Test: mustToRawJson\r\n  # Test: fromJson\r\n  # Test: fromJsonArray\r\n\r\n  # ==========================================\r\n  # YAML conversion functions\r\n  # ==========================================\r\n  # Test: fromYaml\r\n  # Test: toYaml\r\n  # Test: toYamlPretty\r\n  # Test: fromYamlArray\r\n\r\n  # ==========================================\r\n  # TOML conversion functions\r\n  # ==========================================\r\n  # Test: toToml\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-type-conversion/templates/type-conversion-test.yaml",
            "content": "\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  # Test: mustToJson\n  # Test: toPrettyJson\n  # Test: mustToPrettyJson\n  # Test: toRawJson\n  toRawJsonValue: \"true\"\n  # Test: mustToRawJson\n  # Test: fromJson\n  # Test: fromJsonArray\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  # Test: toYaml\n  # Test: toYamlPretty\n  # Test: fromYamlArray\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n",
            "originalContent": "# Test: Type conversion errors - comprehensive testing of all type conversion functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n  {{- if .Values.testToStrings }}\n  toStringsValue: {{ toStrings $testData | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n  {{- if .Values.testToDecimal }}\n  toDecimalValue: {{ toDecimal $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  {{- if .Values.testToJson }}\n  toJsonValue: {{ toJson $testData | quote }}\n  {{- end }}\n  # Test: mustToJson\n  {{- if .Values.testMustToJson }}\n  mustToJsonValue: {{ mustToJson $testData | quote }}\n  {{- end }}\n  # Test: toPrettyJson\n  {{- if .Values.testToPrettyJson }}\n  toPrettyJsonValue: {{ toPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: mustToPrettyJson\n  {{- if .Values.testMustToPrettyJson }}\n  mustToPrettyJsonValue: {{ mustToPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: toRawJson\n  {{- if .Values.testToRawJson }}\n  toRawJsonValue: {{ toRawJson $testData | quote }}\n  {{- end }}\n  # Test: mustToRawJson\n  {{- if .Values.testMustToRawJson }}\n  mustToRawJsonValue: {{ mustToRawJson $testData | quote }}\n  {{- end }}\n  # Test: fromJson\n  {{- if .Values.testFromJson }}\n  {{- $parsed := $testData | fromJson }}\n  fromJsonValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: fromJsonArray\n  {{- if .Values.testFromJsonArray }}\n  {{- $arr := $testData | fromJsonArray }}\n  fromJsonArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  {{- if .Values.testFromYaml }}\n  {{- $parsed := $testData | fromYaml }}\n  fromYamlValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: toYaml\n  {{- if .Values.testToYaml }}\n  toYamlValue: {{ toYaml $testData | quote }}\n  {{- end }}\n  # Test: toYamlPretty\n  {{- if .Values.testToYamlPretty }}\n  toYamlPrettyValue: {{ toYamlPretty $testData | quote }}\n  {{- end }}\n  # Test: fromYamlArray\n  {{- if .Values.testFromYamlArray }}\n  {{- $arr := $testData | fromYamlArray }}\n  fromYamlArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n  {{- if .Values.testToToml }}\n  toTomlValue: {{ toToml $testData | quote }}\n  {{- end }}\n\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "resolved": true,
      "stopReason": "",
      "fixChain": []
    },
    {
      "toggle": "toRawJson",
      "dataType": "listValue",
      "valuesOptions": {
        "values": [
          "testToRawJson=true",
          "testDataType=listValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-type-conversion-test\r\ndata:\r\n  # ==========================================\r\n  # String conversion functions\r\n  # ==========================================\r\n  # Test: toStrings\r\n\r\n  # ==========================================\r\n  # Decimal conversion functions\r\n  # ==========================================\r\n  # Test: toDecimal\r\n\r\n  # ==========================================\r\n  # JSON conversion functions\r\n  # ==========================================\r\n  # Test: toJson\r\n  # Test: mustToJson\r\n  # Test: toPrettyJson\r\n  # Test: mustToPrettyJson\r\n  # Test: toRawJson\r\n  toRawJsonValue: \"[1,2,3]\"\r\n  # Test: mustToRawJson\r\n  # Test: fromJson\r\n  # Test: fromJsonArray\r\n\r\n  # ==========================================\r\n  # YAML conversion functions\r\n  # ==========================================\r\n  # Test: fromYaml\r\n  # Test: toYaml\r\n  # Test: toYamlPretty\r\n  # Test: fromYamlArray\r\n\r\n  # ==========================================\r\n  # TOML conversion functions\r\n  # ==========================================\r\n  # Test: toToml\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-type-conversion/templates/type-conversion-test.yaml",
            "content": "\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  # Test: mustToJson\n  # Test: toPrettyJson\n  # Test: mustToPrettyJson\n  # Test: toRawJson\n  toRawJsonValue: \"[1,2,3]\"\n  # Test: mustToRawJson\n  # Test: fromJson\n  # Test: fromJsonArray\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  # Test: toYaml\n  # Test: toYamlPretty\n  # Test: fromYamlArray\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n",
            "originalContent": "# Test: Type conversion errors - comprehensive testing of all type conversion functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n  {{- if .Values.testToStrings }}\n  toStringsValue: {{ toStrings $testData | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n  {{- if .Values.testToDecimal }}\n  toDecimalValue: {{ toDecimal $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  {{- if .Values.testToJson }}\n  toJsonValue: {{ toJson $testData | quote }}\n  {{- end }}\n  # Test: mustToJson\n  {{- if .Values.testMustToJson }}\n  mustToJsonValue: {{ mustToJson $testData | quote }}\n  {{- end }}\n  # Test: toPrettyJson\n  {{- if .Values.testToPrettyJson }}\n  toPrettyJsonValue: {{ toPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: mustToPrettyJson\n  {{- if .Values.testMustToPrettyJson }}\n  mustToPrettyJsonValue: {{ mustToPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: toRawJson\n  {{- if .Values.testToRawJson }}\n  toRawJsonValue: {{ toRawJson $testData | quote }}\n  {{- end }}\n  # Test: mustToRawJson\n  {{- if .Values.testMustToRawJson }}\n  mustToRawJsonValue: {{ mustToRawJson $testData | quote }}\n  {{- end }}\n  # Test: fromJson\n  {{- if .Values.testFromJson }}\n  {{- $parsed := $testData | fromJson }}\n  fromJsonValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: fromJsonArray\n  {{- if .Values.testFromJsonArray }}\n  {{- $arr := $testData | fromJsonArray }}\n  fromJsonArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  {{- if .Values.testFromYaml }}\n  {{- $parsed := $testData | fromYaml }}\n  fromYamlValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: toYaml\n  {{- if .Values.testToYaml }}\n  toYamlValue: {{ toYaml $testData | quote }}\n  {{- end }}\n  # Test: toYamlPretty\n  {{- if .Values.testToYamlPretty }}\n  toYamlPrettyValue: {{ toYamlPretty $testData | quote }}\n  {{- end }}\n  # Test: fromYamlArray\n  {{- if .Values.testFromYamlArray }}\n  {{- $arr := $testData | fromYamlArray }}\n  fromYamlArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n  {{- if .Values.testToToml }}\n  toTomlValue: {{ toToml $testData | quote }}\n  {{- end }}\n\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-type-conversion-test\r\ndata:\r\n  # ==========================================\r\n  # String conversion functions\r\n  # ==========================================\r\n  # Test: toStrings\r\n\r\n  # ==========================================\r\n  # Decimal conversion functions\r\n  # ==========================================\r\n  # Test: toDecimal\r\n\r\n  # ==========================================\r\n  # JSON conversion functions\r\n  # ==========================================\r\n  # Test: toJson\r\n  # Test: mustToJson\r\n  # Test: toPrettyJson\r\n  # Test: mustToPrettyJson\r\n  # Test: toRawJson\r\n  toRawJsonValue: \"[1,2,3]\"\r\n  # Test: mustToRawJson\r\n  # Test: fromJson\r\n  # Test: fromJsonArray\r\n\r\n  # ==========================================\r\n  # YAML conversion functions\r\n  # ==========================================\r\n  # Test: fromYaml\r\n  # Test: toYaml\r\n  # Test: toYamlPretty\r\n  # Test: fromYamlArray\r\n\r\n  # ==========================================\r\n  # TOML conversion functions\r\n  # ==========================================\r\n  # Test: toToml\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-type-conversion/templates/type-conversion-test.yaml",
            "content": "\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  # Test: mustToJson\n  # Test: toPrettyJson\n  # Test: mustToPrettyJson\n  # Test: toRawJson\n  toRawJsonValue: \"[1,2,3]\"\n  # Test: mustToRawJson\n  # Test: fromJson\n  # Test: fromJsonArray\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  # Test: toYaml\n  # Test: toYamlPretty\n  # Test: fromYamlArray\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n",
            "originalContent": "# Test: Type conversion errors - comprehensive testing of all type conversion functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n  {{- if .Values.testToStrings }}\n  toStringsValue: {{ toStrings $testData | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n  {{- if .Values.testToDecimal }}\n  toDecimalValue: {{ toDecimal $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  {{- if .Values.testToJson }}\n  toJsonValue: {{ toJson $testData | quote }}\n  {{- end }}\n  # Test: mustToJson\n  {{- if .Values.testMustToJson }}\n  mustToJsonValue: {{ mustToJson $testData | quote }}\n  {{- end }}\n  # Test: toPrettyJson\n  {{- if .Values.testToPrettyJson }}\n  toPrettyJsonValue: {{ toPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: mustToPrettyJson\n  {{- if .Values.testMustToPrettyJson }}\n  mustToPrettyJsonValue: {{ mustToPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: toRawJson\n  {{- if .Values.testToRawJson }}\n  toRawJsonValue: {{ toRawJson $testData | quote }}\n  {{- end }}\n  # Test: mustToRawJson\n  {{- if .Values.testMustToRawJson }}\n  mustToRawJsonValue: {{ mustToRawJson $testData | quote }}\n  {{- end }}\n  # Test: fromJson\n  {{- if .Values.testFromJson }}\n  {{- $parsed := $testData | fromJson }}\n  fromJsonValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: fromJsonArray\n  {{- if .Values.testFromJsonArray }}\n  {{- $arr := $testData | fromJsonArray }}\n  fromJsonArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  {{- if .Values.testFromYaml }}\n  {{- $parsed := $testData | fromYaml }}\n  fromYamlValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: toYaml\n  {{- if .Values.testToYaml }}\n  toYamlValue: {{ toYaml $testData | quote }}\n  {{- end }}\n  # Test: toYamlPretty\n  {{- if .Values.testToYamlPretty }}\n  toYamlPrettyValue: {{ toYamlPretty $testData | quote }}\n  {{- end }}\n  # Test: fromYamlArray\n  {{- if .Values.testFromYamlArray }}\n  {{- $arr := $testData | fromYamlArray }}\n  fromYamlArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n  {{- if .Values.testToToml }}\n  toTomlValue: {{ toToml $testData | quote }}\n  {{- end }}\n\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "resolved": true,
      "stopReason": "",
      "fixChain": []
    },
    {
      "toggle": "toRawJson",
      "dataType": "dictValue",
      "valuesOptions": {
        "values": [
          "testToRawJson=true",
          "testDataType=dictValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-type-conversion-test\r\ndata:\r\n  # ==========================================\r\n  # String conversion functions\r\n  # ==========================================\r\n  # Test: toStrings\r\n\r\n  # ==========================================\r\n  # Decimal conversion functions\r\n  # ==========================================\r\n  # Test: toDecimal\r\n\r\n  # ==========================================\r\n  # JSON conversion functions\r\n  # ==========================================\r\n  # Test: toJson\r\n  # Test: mustToJson\r\n  # Test: toPrettyJson\r\n  # Test: mustToPrettyJson\r\n  # Test: toRawJson\r\n  toRawJsonValue: \"{\\\"key\\\":\\\"value\\\"}\"\r\n  # Test: mustToRawJson\r\n  # Test: fromJson\r\n  # Test: fromJsonArray\r\n\r\n  # ==========================================\r\n  # YAML conversion functions\r\n  # ==========================================\r\n  # Test: fromYaml\r\n  # Test: toYaml\r\n  # Test: toYamlPretty\r\n  # Test: fromYamlArray\r\n\r\n  # ==========================================\r\n  # TOML conversion functions\r\n  # ==========================================\r\n  # Test: toToml\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-type-conversion/templates/type-conversion-test.yaml",
            "content": "\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  # Test: mustToJson\n  # Test: toPrettyJson\n  # Test: mustToPrettyJson\n  # Test: toRawJson\n  toRawJsonValue: \"{\\\"key\\\":\\\"value\\\"}\"\n  # Test: mustToRawJson\n  # Test: fromJson\n  # Test: fromJsonArray\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  # Test: toYaml\n  # Test: toYamlPretty\n  # Test: fromYamlArray\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n",
            "originalContent": "# Test: Type conversion errors - comprehensive testing of all type conversion functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n  {{- if .Values.testToStrings }}\n  toStringsValue: {{ toStrings $testData | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n  {{- if .Values.testToDecimal }}\n  toDecimalValue: {{ toDecimal $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  {{- if .Values.testToJson }}\n  toJsonValue: {{ toJson $testData | quote }}\n  {{- end }}\n  # Test: mustToJson\n  {{- if .Values.testMustToJson }}\n  mustToJsonValue: {{ mustToJson $testData | quote }}\n  {{- end }}\n  # Test: toPrettyJson\n  {{- if .Values.testToPrettyJson }}\n  toPrettyJsonValue: {{ toPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: mustToPrettyJson\n  {{- if .Values.testMustToPrettyJson }}\n  mustToPrettyJsonValue: {{ mustToPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: toRawJson\n  {{- if .Values.testToRawJson }}\n  toRawJsonValue: {{ toRawJson $testData | quote }}\n  {{- end }}\n  # Test: mustToRawJson\n  {{- if .Values.testMustToRawJson }}\n  mustToRawJsonValue: {{ mustToRawJson $testData | quote }}\n  {{- end }}\n  # Test: fromJson\n  {{- if .Values.testFromJson }}\n  {{- $parsed := $testData | fromJson }}\n  fromJsonValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: fromJsonArray\n  {{- if .Values.testFromJsonArray }}\n  {{- $arr := $testData | fromJsonArray }}\n  fromJsonArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  {{- if .Values.testFromYaml }}\n  {{- $parsed := $testData | fromYaml }}\n  fromYamlValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: toYaml\n  {{- if .Values.testToYaml }}\n  toYamlValue: {{ toYaml $testData | quote }}\n  {{- end }}\n  # Test: toYamlPretty\n  {{- if .Values.testToYamlPretty }}\n  toYamlPrettyValue: {{ toYamlPretty $testData | quote }}\n  {{- end }}\n  # Test: fromYamlArray\n  {{- if .Values.testFromYamlArray }}\n  {{- $arr := $testData | fromYamlArray }}\n  fromYamlArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n  {{- if .Values.testToToml }}\n  toTomlValue: {{ toToml $testData | quote }}\n  {{- end }}\n\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-type-conversion-test\r\ndata:\r\n  # ==========================================\r\n  # String conversion functions\r\n  # ==========================================\r\n  # Test: toStrings\r\n\r\n  # ==========================================\r\n  # Decimal conversion functions\r\n  # ==========================================\r\n  # Test: toDecimal\r\n\r\n  # ==========================================\r\n  # JSON conversion functions\r\n  # ==========================================\r\n  # Test: toJson\r\n  # Test: mustToJson\r\n  # Test: toPrettyJson\r\n  # Test: mustToPrettyJson\r\n  # Test: toRawJson\r\n  toRawJsonValue: \"{\\\"key\\\":\\\"value\\\"}\"\r\n  # Test: mustToRawJson\r\n  # Test: fromJson\r\n  # Test: fromJsonArray\r\n\r\n  # ==========================================\r\n  # YAML conversion functions\r\n  # ==========================================\r\n  # Test: fromYaml\r\n  # Test: toYaml\r\n  # Test: toYamlPretty\r\n  # Test: fromYamlArray\r\n\r\n  # ==========================================\r\n  # TOML conversion functions\r\n  # ==========================================\r\n  # Test: toToml\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-type-conversion/templates/type-conversion-test.yaml",
            "content": "\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  # Test: mustToJson\n  # Test: toPrettyJson\n  # Test: mustToPrettyJson\n  # Test: toRawJson\n  toRawJsonValue: \"{\\\"key\\\":\\\"value\\\"}\"\n  # Test: mustToRawJson\n  # Test: fromJson\n  # Test: fromJsonArray\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  # Test: toYaml\n  # Test: toYamlPretty\n  # Test: fromYamlArray\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n",
            "originalContent": "# Test: Type conversion errors - comprehensive testing of all type conversion functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n  {{- if .Values.testToStrings }}\n  toStringsValue: {{ toStrings $testData | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n  {{- if .Values.testToDecimal }}\n  toDecimalValue: {{ toDecimal $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  {{- if .Values.testToJson }}\n  toJsonValue: {{ toJson $testData | quote }}\n  {{- end }}\n  # Test: mustToJson\n  {{- if .Values.testMustToJson }}\n  mustToJsonValue: {{ mustToJson $testData | quote }}\n  {{- end }}\n  # Test: toPrettyJson\n  {{- if .Values.testToPrettyJson }}\n  toPrettyJsonValue: {{ toPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: mustToPrettyJson\n  {{- if .Values.testMustToPrettyJson }}\n  mustToPrettyJsonValue: {{ mustToPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: toRawJson\n  {{- if .Values.testToRawJson }}\n  toRawJsonValue: {{ toRawJson $testData | quote }}\n  {{- end }}\n  # Test: mustToRawJson\n  {{- if .Values.testMustToRawJson }}\n  mustToRawJsonValue: {{ mustToRawJson $testData | quote }}\n  {{- end }}\n  # Test: fromJson\n  {{- if .Values.testFromJson }}\n  {{- $parsed := $testData | fromJson }}\n  fromJsonValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: fromJsonArray\n  {{- if .Values.testFromJsonArray }}\n  {{- $arr := $testData | fromJsonArray }}\n  fromJsonArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  {{- if .Values.testFromYaml }}\n  {{- $parsed := $testData | fromYaml }}\n  fromYamlValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: toYaml\n  {{- if .Values.testToYaml }}\n  toYamlValue: {{ toYaml $testData | quote }}\n  {{- end }}\n  # Test: toYamlPretty\n  {{- if .Values.testToYamlPretty }}\n  toYamlPrettyValue: {{ toYamlPretty $testData | quote }}\n  {{- end }}\n  # Test: fromYamlArray\n  {{- if .Values.testFromYamlArray }}\n  {{- $arr := $testData | fromYamlArray }}\n  fromYamlArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n  {{- if .Values.testToToml }}\n  toTomlValue: {{ toToml $testData | quote }}\n  {{- end }}\n\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "resolved": true,
      "stopReason": "",
      "fixChain": []
    },
    {
      "toggle": "mustToRawJson",
      "dataType": "nilValue",
      "valuesOptions": {
        "values": [
          "testMustToRawJson=true",
          "testDataType=nilValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-type-conversion-test\r\ndata:\r\n  # ==========================================\r\n  # String conversion functions\r\n  # ==========================================\r\n  # Test: toStrings\r\n\r\n  # ==========================================\r\n  # Decimal conversion functions\r\n  # ==========================================\r\n  # Test: toDecimal\r\n\r\n  # ==========================================\r\n  # JSON conversion functions\r\n  # ==========================================\r\n  # Test: toJson\r\n  # Test: mustToJson\r\n  # Test: toPrettyJson\r\n  # Test: mustToPrettyJson\r\n  # Test: toRawJson\r\n  # Test: mustToRawJson\r\n  mustToRawJsonValue: \"null\"\r\n  # Test: fromJson\r\n  # Test: fromJsonArray\r\n\r\n  # ==========================================\r\n  # YAML conversion functions\r\n  # ==========================================\r\n  # Test: fromYaml\r\n  # Test: toYaml\r\n  # Test: toYamlPretty\r\n  # Test: fromYamlArray\r\n\r\n  # ==========================================\r\n  # TOML conversion functions\r\n  # ==========================================\r\n  # Test: toToml\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-type-conversion/templates/type-conversion-test.yaml",
            "content": "\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  # Test: mustToJson\n  # Test: toPrettyJson\n  # Test: mustToPrettyJson\n  # Test: toRawJson\n  # Test: mustToRawJson\n  mustToRawJsonValue: \"null\"\n  # Test: fromJson\n  # Test: fromJsonArray\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  # Test: toYaml\n  # Test: toYamlPretty\n  # Test: fromYamlArray\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n",
            "originalContent": "# Test: Type conversion errors - comprehensive testing of all type conversion functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n  {{- if .Values.testToStrings }}\n  toStringsValue: {{ toStrings $testData | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n  {{- if .Values.testToDecimal }}\n  toDecimalValue: {{ toDecimal $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  {{- if .Values.testToJson }}\n  toJsonValue: {{ toJson $testData | quote }}\n  {{- end }}\n  # Test: mustToJson\n  {{- if .Values.testMustToJson }}\n  mustToJsonValue: {{ mustToJson $testData | quote }}\n  {{- end }}\n  # Test: toPrettyJson\n  {{- if .Values.testToPrettyJson }}\n  toPrettyJsonValue: {{ toPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: mustToPrettyJson\n  {{- if .Values.testMustToPrettyJson }}\n  mustToPrettyJsonValue: {{ mustToPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: toRawJson\n  {{- if .Values.testToRawJson }}\n  toRawJsonValue: {{ toRawJson $testData | quote }}\n  {{- end }}\n  # Test: mustToRawJson\n  {{- if .Values.testMustToRawJson }}\n  mustToRawJsonValue: {{ mustToRawJson $testData | quote }}\n  {{- end }}\n  # Test: fromJson\n  {{- if .Values.testFromJson }}\n  {{- $parsed := $testData | fromJson }}\n  fromJsonValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: fromJsonArray\n  {{- if .Values.testFromJsonArray }}\n  {{- $arr := $testData | fromJsonArray }}\n  fromJsonArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  {{- if .Values.testFromYaml }}\n  {{- $parsed := $testData | fromYaml }}\n  fromYamlValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: toYaml\n  {{- if .Values.testToYaml }}\n  toYamlValue: {{ toYaml $testData | quote }}\n  {{- end }}\n  # Test: toYamlPretty\n  {{- if .Values.testToYamlPretty }}\n  toYamlPrettyValue: {{ toYamlPretty $testData | quote }}\n  {{- end }}\n  # Test: fromYamlArray\n  {{- if .Values.testFromYamlArray }}\n  {{- $arr := $testData | fromYamlArray }}\n  fromYamlArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n  {{- if .Values.testToToml }}\n  toTomlValue: {{ toToml $testData | quote }}\n  {{- end }}\n\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-type-conversion-test\r\ndata:\r\n  # ==========================================\r\n  # String conversion functions\r\n  # ==========================================\r\n  # Test: toStrings\r\n\r\n  # ==========================================\r\n  # Decimal conversion functions\r\n  # ==========================================\r\n  # Test: toDecimal\r\n\r\n  # ==========================================\r\n  # JSON conversion functions\r\n  # ==========================================\r\n  # Test: toJson\r\n  # Test: mustToJson\r\n  # Test: toPrettyJson\r\n  # Test: mustToPrettyJson\r\n  # Test: toRawJson\r\n  # Test: mustToRawJson\r\n  mustToRawJsonValue: \"null\"\r\n  # Test: fromJson\r\n  # Test: fromJsonArray\r\n\r\n  # ==========================================\r\n  # YAML conversion functions\r\n  # ==========================================\r\n  # Test: fromYaml\r\n  # Test: toYaml\r\n  # Test: toYamlPretty\r\n  # Test: fromYamlArray\r\n\r\n  # ==========================================\r\n  # TOML conversion functions\r\n  # ==========================================\r\n  # Test: toToml\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-type-conversion/templates/type-conversion-test.yaml",
            "content": "\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  # Test: mustToJson\n  # Test: toPrettyJson\n  # Test: mustToPrettyJson\n  # Test: toRawJson\n  # Test: mustToRawJson\n  mustToRawJsonValue: \"null\"\n  # Test: fromJson\n  # Test: fromJsonArray\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  # Test: toYaml\n  # Test: toYamlPretty\n  # Test: fromYamlArray\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n",
            "originalContent": "# Test: Type conversion errors - comprehensive testing of all type conversion functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n  {{- if .Values.testToStrings }}\n  toStringsValue: {{ toStrings $testData | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n  {{- if .Values.testToDecimal }}\n  toDecimalValue: {{ toDecimal $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  {{- if .Values.testToJson }}\n  toJsonValue: {{ toJson $testData | quote }}\n  {{- end }}\n  # Test: mustToJson\n  {{- if .Values.testMustToJson }}\n  mustToJsonValue: {{ mustToJson $testData | quote }}\n  {{- end }}\n  # Test: toPrettyJson\n  {{- if .Values.testToPrettyJson }}\n  toPrettyJsonValue: {{ toPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: mustToPrettyJson\n  {{- if .Values.testMustToPrettyJson }}\n  mustToPrettyJsonValue: {{ mustToPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: toRawJson\n  {{- if .Values.testToRawJson }}\n  toRawJsonValue: {{ toRawJson $testData | quote }}\n  {{- end }}\n  # Test: mustToRawJson\n  {{- if .Values.testMustToRawJson }}\n  mustToRawJsonValue: {{ mustToRawJson $testData | quote }}\n  {{- end }}\n  # Test: fromJson\n  {{- if .Values.testFromJson }}\n  {{- $parsed := $testData | fromJson }}\n  fromJsonValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: fromJsonArray\n  {{- if .Values.testFromJsonArray }}\n  {{- $arr := $testData | fromJsonArray }}\n  fromJsonArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  {{- if .Values.testFromYaml }}\n  {{- $parsed := $testData | fromYaml }}\n  fromYamlValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: toYaml\n  {{- if .Values.testToYaml }}\n  toYamlValue: {{ toYaml $testData | quote }}\n  {{- end }}\n  # Test: toYamlPretty\n  {{- if .Values.testToYamlPretty }}\n  toYamlPrettyValue: {{ toYamlPretty $testData | quote }}\n  {{- end }}\n  # Test: fromYamlArray\n  {{- if .Values.testFromYamlArray }}\n  {{- $arr := $testData | fromYamlArray }}\n  fromYamlArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n  {{- if .Values.testToToml }}\n  toTomlValue: {{ toToml $testData | quote }}\n  {{- end }}\n\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "resolved": true,
      "stopReason": "",
      "fixChain": []
    },
    {
      "toggle": "mustToRawJson",
      "dataType": "stringValue",
      "valuesOptions": {
        "values": [
          "testMustToRawJson=true",
          "testDataType=stringValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-type-conversion-test\r\ndata:\r\n  # ==========================================\r\n  # String conversion functions\r\n  # ==========================================\r\n  # Test: toStrings\r\n\r\n  # ==========================================\r\n  # Decimal conversion functions\r\n  # ==========================================\r\n  # Test: toDecimal\r\n\r\n  # ==========================================\r\n  # JSON conversion functions\r\n  # ==========================================\r\n  # Test: toJson\r\n  # Test: mustToJson\r\n  # Test: toPrettyJson\r\n  # Test: mustToPrettyJson\r\n  # Test: toRawJson\r\n  # Test: mustToRawJson\r\n  mustToRawJsonValue: \"\\\"test string\\\"\"\r\n  # Test: fromJson\r\n  # Test: fromJsonArray\r\n\r\n  # ==========================================\r\n  # YAML conversion functions\r\n  # ==========================================\r\n  # Test: fromYaml\r\n  # Test: toYaml\r\n  # Test: toYamlPretty\r\n  # Test: fromYamlArray\r\n\r\n  # ==========================================\r\n  # TOML conversion functions\r\n  # ==========================================\r\n  # Test: toToml\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-type-conversion/templates/type-conversion-test.yaml",
            "content": "\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  # Test: mustToJson\n  # Test: toPrettyJson\n  # Test: mustToPrettyJson\n  # Test: toRawJson\n  # Test: mustToRawJson\n  mustToRawJsonValue: \"\\\"test string\\\"\"\n  # Test: fromJson\n  # Test: fromJsonArray\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  # Test: toYaml\n  # Test: toYamlPretty\n  # Test: fromYamlArray\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n",
            "originalContent": "# Test: Type conversion errors - comprehensive testing of all type conversion functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n  {{- if .Values.testToStrings }}\n  toStringsValue: {{ toStrings $testData | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n  {{- if .Values.testToDecimal }}\n  toDecimalValue: {{ toDecimal $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  {{- if .Values.testToJson }}\n  toJsonValue: {{ toJson $testData | quote }}\n  {{- end }}\n  # Test: mustToJson\n  {{- if .Values.testMustToJson }}\n  mustToJsonValue: {{ mustToJson $testData | quote }}\n  {{- end }}\n  # Test: toPrettyJson\n  {{- if .Values.testToPrettyJson }}\n  toPrettyJsonValue: {{ toPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: mustToPrettyJson\n  {{- if .Values.testMustToPrettyJson }}\n  mustToPrettyJsonValue: {{ mustToPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: toRawJson\n  {{- if .Values.testToRawJson }}\n  toRawJsonValue: {{ toRawJson $testData | quote }}\n  {{- end }}\n  # Test: mustToRawJson\n  {{- if .Values.testMustToRawJson }}\n  mustToRawJsonValue: {{ mustToRawJson $testData | quote }}\n  {{- end }}\n  # Test: fromJson\n  {{- if .Values.testFromJson }}\n  {{- $parsed := $testData | fromJson }}\n  fromJsonValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: fromJsonArray\n  {{- if .Values.testFromJsonArray }}\n  {{- $arr := $testData | fromJsonArray }}\n  fromJsonArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  {{- if .Values.testFromYaml }}\n  {{- $parsed := $testData | fromYaml }}\n  fromYamlValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: toYaml\n  {{- if .Values.testToYaml }}\n  toYamlValue: {{ toYaml $testData | quote }}\n  {{- end }}\n  # Test: toYamlPretty\n  {{- if .Values.testToYamlPretty }}\n  toYamlPrettyValue: {{ toYamlPretty $testData | quote }}\n  {{- end }}\n  # Test: fromYamlArray\n  {{- if .Values.testFromYamlArray }}\n  {{- $arr := $testData | fromYamlArray }}\n  fromYamlArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n  {{- if .Values.testToToml }}\n  toTomlValue: {{ toToml $testData | quote }}\n  {{- end }}\n\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-type-conversion-test\r\ndata:\r\n  # ==========================================\r\n  # String conversion functions\r\n  # ==========================================\r\n  # Test: toStrings\r\n\r\n  # ==========================================\r\n  # Decimal conversion functions\r\n  # ==========================================\r\n  # Test: toDecimal\r\n\r\n  # ==========================================\r\n  # JSON conversion functions\r\n  # ==========================================\r\n  # Test: toJson\r\n  # Test: mustToJson\r\n  # Test: toPrettyJson\r\n  # Test: mustToPrettyJson\r\n  # Test: toRawJson\r\n  # Test: mustToRawJson\r\n  mustToRawJsonValue: \"\\\"test string\\\"\"\r\n  # Test: fromJson\r\n  # Test: fromJsonArray\r\n\r\n  # ==========================================\r\n  # YAML conversion functions\r\n  # ==========================================\r\n  # Test: fromYaml\r\n  # Test: toYaml\r\n  # Test: toYamlPretty\r\n  # Test: fromYamlArray\r\n\r\n  # ==========================================\r\n  # TOML conversion functions\r\n  # ==========================================\r\n  # Test: toToml\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-type-conversion/templates/type-conversion-test.yaml",
            "content": "\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  # Test: mustToJson\n  # Test: toPrettyJson\n  # Test: mustToPrettyJson\n  # Test: toRawJson\n  # Test: mustToRawJson\n  mustToRawJsonValue: \"\\\"test string\\\"\"\n  # Test: fromJson\n  # Test: fromJsonArray\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  # Test: toYaml\n  # Test: toYamlPretty\n  # Test: fromYamlArray\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n",
            "originalContent": "# Test: Type conversion errors - comprehensive testing of all type conversion functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n  {{- if .Values.testToStrings }}\n  toStringsValue: {{ toStrings $testData | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n  {{- if .Values.testToDecimal }}\n  toDecimalValue: {{ toDecimal $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  {{- if .Values.testToJson }}\n  toJsonValue: {{ toJson $testData | quote }}\n  {{- end }}\n  # Test: mustToJson\n  {{- if .Values.testMustToJson }}\n  mustToJsonValue: {{ mustToJson $testData | quote }}\n  {{- end }}\n  # Test: toPrettyJson\n  {{- if .Values.testToPrettyJson }}\n  toPrettyJsonValue: {{ toPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: mustToPrettyJson\n  {{- if .Values.testMustToPrettyJson }}\n  mustToPrettyJsonValue: {{ mustToPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: toRawJson\n  {{- if .Values.testToRawJson }}\n  toRawJsonValue: {{ toRawJson $testData | quote }}\n  {{- end }}\n  # Test: mustToRawJson\n  {{- if .Values.testMustToRawJson }}\n  mustToRawJsonValue: {{ mustToRawJson $testData | quote }}\n  {{- end }}\n  # Test: fromJson\n  {{- if .Values.testFromJson }}\n  {{- $parsed := $testData | fromJson }}\n  fromJsonValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: fromJsonArray\n  {{- if .Values.testFromJsonArray }}\n  {{- $arr := $testData | fromJsonArray }}\n  fromJsonArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  {{- if .Values.testFromYaml }}\n  {{- $parsed := $testData | fromYaml }}\n  fromYamlValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: toYaml\n  {{- if .Values.testToYaml }}\n  toYamlValue: {{ toYaml $testData | quote }}\n  {{- end }}\n  # Test: toYamlPretty\n  {{- if .Values.testToYamlPretty }}\n  toYamlPrettyValue: {{ toYamlPretty $testData | quote }}\n  {{- end }}\n  # Test: fromYamlArray\n  {{- if .Values.testFromYamlArray }}\n  {{- $arr := $testData | fromYamlArray }}\n  fromYamlArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n  {{- if .Values.testToToml }}\n  toTomlValue: {{ toToml $testData | quote }}\n  {{- end }}\n\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "resolved": true,
      "stopReason": "",
      "fixChain": []
    },
    {
      "toggle": "mustToRawJson",
      "dataType": "numberValue",
      "valuesOptions": {
        "values": [
          "testMustToRawJson=true",
          "testDataType=numberValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-type-conversion-test\r\ndata:\r\n  # ==========================================\r\n  # String conversion functions\r\n  # ==========================================\r\n  # Test: toStrings\r\n\r\n  # ==========================================\r\n  # Decimal conversion functions\r\n  # ==========================================\r\n  # Test: toDecimal\r\n\r\n  # ==========================================\r\n  # JSON conversion functions\r\n  # ==========================================\r\n  # Test: toJson\r\n  # Test: mustToJson\r\n  # Test: toPrettyJson\r\n  # Test: mustToPrettyJson\r\n  # Test: toRawJson\r\n  # Test: mustToRawJson\r\n  mustToRawJsonValue: \"42\"\r\n  # Test: fromJson\r\n  # Test: fromJsonArray\r\n\r\n  # ==========================================\r\n  # YAML conversion functions\r\n  # ==========================================\r\n  # Test: fromYaml\r\n  # Test: toYaml\r\n  # Test: toYamlPretty\r\n  # Test: fromYamlArray\r\n\r\n  # ==========================================\r\n  # TOML conversion functions\r\n  # ==========================================\r\n  # Test: toToml\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-type-conversion/templates/type-conversion-test.yaml",
            "content": "\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  # Test: mustToJson\n  # Test: toPrettyJson\n  # Test: mustToPrettyJson\n  # Test: toRawJson\n  # Test: mustToRawJson\n  mustToRawJsonValue: \"42\"\n  # Test: fromJson\n  # Test: fromJsonArray\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  # Test: toYaml\n  # Test: toYamlPretty\n  # Test: fromYamlArray\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n",
            "originalContent": "# Test: Type conversion errors - comprehensive testing of all type conversion functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n  {{- if .Values.testToStrings }}\n  toStringsValue: {{ toStrings $testData | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n  {{- if .Values.testToDecimal }}\n  toDecimalValue: {{ toDecimal $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  {{- if .Values.testToJson }}\n  toJsonValue: {{ toJson $testData | quote }}\n  {{- end }}\n  # Test: mustToJson\n  {{- if .Values.testMustToJson }}\n  mustToJsonValue: {{ mustToJson $testData | quote }}\n  {{- end }}\n  # Test: toPrettyJson\n  {{- if .Values.testToPrettyJson }}\n  toPrettyJsonValue: {{ toPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: mustToPrettyJson\n  {{- if .Values.testMustToPrettyJson }}\n  mustToPrettyJsonValue: {{ mustToPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: toRawJson\n  {{- if .Values.testToRawJson }}\n  toRawJsonValue: {{ toRawJson $testData | quote }}\n  {{- end }}\n  # Test: mustToRawJson\n  {{- if .Values.testMustToRawJson }}\n  mustToRawJsonValue: {{ mustToRawJson $testData | quote }}\n  {{- end }}\n  # Test: fromJson\n  {{- if .Values.testFromJson }}\n  {{- $parsed := $testData | fromJson }}\n  fromJsonValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: fromJsonArray\n  {{- if .Values.testFromJsonArray }}\n  {{- $arr := $testData | fromJsonArray }}\n  fromJsonArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  {{- if .Values.testFromYaml }}\n  {{- $parsed := $testData | fromYaml }}\n  fromYamlValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: toYaml\n  {{- if .Values.testToYaml }}\n  toYamlValue: {{ toYaml $testData | quote }}\n  {{- end }}\n  # Test: toYamlPretty\n  {{- if .Values.testToYamlPretty }}\n  toYamlPrettyValue: {{ toYamlPretty $testData | quote }}\n  {{- end }}\n  # Test: fromYamlArray\n  {{- if .Values.testFromYamlArray }}\n  {{- $arr := $testData | fromYamlArray }}\n  fromYamlArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n  {{- if .Values.testToToml }}\n  toTomlValue: {{ toToml $testData | quote }}\n  {{- end }}\n\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-type-conversion-test\r\ndata:\r\n  # ==========================================\r\n  # String conversion functions\r\n  # ==========================================\r\n  # Test: toStrings\r\n\r\n  # ==========================================\r\n  # Decimal conversion functions\r\n  # ==========================================\r\n  # Test: toDecimal\r\n\r\n  # ==========================================\r\n  # JSON conversion functions\r\n  # ==========================================\r\n  # Test: toJson\r\n  # Test: mustToJson\r\n  # Test: toPrettyJson\r\n  # Test: mustToPrettyJson\r\n  # Test: toRawJson\r\n  # Test: mustToRawJson\r\n  mustToRawJsonValue: \"42\"\r\n  # Test: fromJson\r\n  # Test: fromJsonArray\r\n\r\n  # ==========================================\r\n  # YAML conversion functions\r\n  # ==========================================\r\n  # Test: fromYaml\r\n  # Test: toYaml\r\n  # Test: toYamlPretty\r\n  # Test: fromYamlArray\r\n\r\n  # ==========================================\r\n  # TOML conversion functions\r\n  # ==========================================\r\n  # Test: toToml\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-type-conversion/templates/type-conversion-test.yaml",
            "content": "\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  # Test: mustToJson\n  # Test: toPrettyJson\n  # Test: mustToPrettyJson\n  # Test: toRawJson\n  # Test: mustToRawJson\n  mustToRawJsonValue: \"42\"\n  # Test: fromJson\n  # Test: fromJsonArray\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  # Test: toYaml\n  # Test: toYamlPretty\n  # Test: fromYamlArray\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n",
            "originalContent": "# Test: Type conversion errors - comprehensive testing of all type conversion functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n  {{- if .Values.testToStrings }}\n  toStringsValue: {{ toStrings $testData | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n  {{- if .Values.testToDecimal }}\n  toDecimalValue: {{ toDecimal $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  {{- if .Values.testToJson }}\n  toJsonValue: {{ toJson $testData | quote }}\n  {{- end }}\n  # Test: mustToJson\n  {{- if .Values.testMustToJson }}\n  mustToJsonValue: {{ mustToJson $testData | quote }}\n  {{- end }}\n  # Test: toPrettyJson\n  {{- if .Values.testToPrettyJson }}\n  toPrettyJsonValue: {{ toPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: mustToPrettyJson\n  {{- if .Values.testMustToPrettyJson }}\n  mustToPrettyJsonValue: {{ mustToPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: toRawJson\n  {{- if .Values.testToRawJson }}\n  toRawJsonValue: {{ toRawJson $testData | quote }}\n  {{- end }}\n  # Test: mustToRawJson\n  {{- if .Values.testMustToRawJson }}\n  mustToRawJsonValue: {{ mustToRawJson $testData | quote }}\n  {{- end }}\n  # Test: fromJson\n  {{- if .Values.testFromJson }}\n  {{- $parsed := $testData | fromJson }}\n  fromJsonValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: fromJsonArray\n  {{- if .Values.testFromJsonArray }}\n  {{- $arr := $testData | fromJsonArray }}\n  fromJsonArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  {{- if .Values.testFromYaml }}\n  {{- $parsed := $testData | fromYaml }}\n  fromYamlValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: toYaml\n  {{- if .Values.testToYaml }}\n  toYamlValue: {{ toYaml $testData | quote }}\n  {{- end }}\n  # Test: toYamlPretty\n  {{- if .Values.testToYamlPretty }}\n  toYamlPrettyValue: {{ toYamlPretty $testData | quote }}\n  {{- end }}\n  # Test: fromYamlArray\n  {{- if .Values.testFromYamlArray }}\n  {{- $arr := $testData | fromYamlArray }}\n  fromYamlArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n  {{- if .Values.testToToml }}\n  toTomlValue: {{ toToml $testData | quote }}\n  {{- end }}\n\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "resolved": true,
      "stopReason": "",
      "fixChain": []
    },
    {
      "toggle": "mustToRawJson",
      "dataType": "boolValue",
      "valuesOptions": {
        "values": [
          "testMustToRawJson=true",
          "testDataType=boolValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-type-conversion-test\r\ndata:\r\n  # ==========================================\r\n  # String conversion functions\r\n  # ==========================================\r\n  # Test: toStrings\r\n\r\n  # ==========================================\r\n  # Decimal conversion functions\r\n  # ==========================================\r\n  # Test: toDecimal\r\n\r\n  # ==========================================\r\n  # JSON conversion functions\r\n  # ==========================================\r\n  # Test: toJson\r\n  # Test: mustToJson\r\n  # Test: toPrettyJson\r\n  # Test: mustToPrettyJson\r\n  # Test: toRawJson\r\n  # Test: mustToRawJson\r\n  mustToRawJsonValue: \"true\"\r\n  # Test: fromJson\r\n  # Test: fromJsonArray\r\n\r\n  # ==========================================\r\n  # YAML conversion functions\r\n  # ==========================================\r\n  # Test: fromYaml\r\n  # Test: toYaml\r\n  # Test: toYamlPretty\r\n  # Test: fromYamlArray\r\n\r\n  # ==========================================\r\n  # TOML conversion functions\r\n  # ==========================================\r\n  # Test: toToml\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-type-conversion/templates/type-conversion-test.yaml",
            "content": "\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  # Test: mustToJson\n  # Test: toPrettyJson\n  # Test: mustToPrettyJson\n  # Test: toRawJson\n  # Test: mustToRawJson\n  mustToRawJsonValue: \"true\"\n  # Test: fromJson\n  # Test: fromJsonArray\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  # Test: toYaml\n  # Test: toYamlPretty\n  # Test: fromYamlArray\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n",
            "originalContent": "# Test: Type conversion errors - comprehensive testing of all type conversion functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n  {{- if .Values.testToStrings }}\n  toStringsValue: {{ toStrings $testData | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n  {{- if .Values.testToDecimal }}\n  toDecimalValue: {{ toDecimal $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  {{- if .Values.testToJson }}\n  toJsonValue: {{ toJson $testData | quote }}\n  {{- end }}\n  # Test: mustToJson\n  {{- if .Values.testMustToJson }}\n  mustToJsonValue: {{ mustToJson $testData | quote }}\n  {{- end }}\n  # Test: toPrettyJson\n  {{- if .Values.testToPrettyJson }}\n  toPrettyJsonValue: {{ toPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: mustToPrettyJson\n  {{- if .Values.testMustToPrettyJson }}\n  mustToPrettyJsonValue: {{ mustToPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: toRawJson\n  {{- if .Values.testToRawJson }}\n  toRawJsonValue: {{ toRawJson $testData | quote }}\n  {{- end }}\n  # Test: mustToRawJson\n  {{- if .Values.testMustToRawJson }}\n  mustToRawJsonValue: {{ mustToRawJson $testData | quote }}\n  {{- end }}\n  # Test: fromJson\n  {{- if .Values.testFromJson }}\n  {{- $parsed := $testData | fromJson }}\n  fromJsonValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: fromJsonArray\n  {{- if .Values.testFromJsonArray }}\n  {{- $arr := $testData | fromJsonArray }}\n  fromJsonArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  {{- if .Values.testFromYaml }}\n  {{- $parsed := $testData | fromYaml }}\n  fromYamlValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: toYaml\n  {{- if .Values.testToYaml }}\n  toYamlValue: {{ toYaml $testData | quote }}\n  {{- end }}\n  # Test: toYamlPretty\n  {{- if .Values.testToYamlPretty }}\n  toYamlPrettyValue: {{ toYamlPretty $testData | quote }}\n  {{- end }}\n  # Test: fromYamlArray\n  {{- if .Values.testFromYamlArray }}\n  {{- $arr := $testData | fromYamlArray }}\n  fromYamlArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n  {{- if .Values.testToToml }}\n  toTomlValue: {{ toToml $testData | quote }}\n  {{- end }}\n\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-type-conversion-test\r\ndata:\r\n  # ==========================================\r\n  # String conversion functions\r\n  # ==========================================\r\n  # Test: toStrings\r\n\r\n  # ==========================================\r\n  # Decimal conversion functions\r\n  # ==========================================\r\n  # Test: toDecimal\r\n\r\n  # ==========================================\r\n  # JSON conversion functions\r\n  # ==========================================\r\n  # Test: toJson\r\n  # Test: mustToJson\r\n  # Test: toPrettyJson\r\n  # Test: mustToPrettyJson\r\n  # Test: toRawJson\r\n  # Test: mustToRawJson\r\n  mustToRawJsonValue: \"true\"\r\n  # Test: fromJson\r\n  # Test: fromJsonArray\r\n\r\n  # ==========================================\r\n  # YAML conversion functions\r\n  # ==========================================\r\n  # Test: fromYaml\r\n  # Test: toYaml\r\n  # Test: toYamlPretty\r\n  # Test: fromYamlArray\r\n\r\n  # ==========================================\r\n  # TOML conversion functions\r\n  # ==========================================\r\n  # Test: toToml\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-type-conversion/templates/type-conversion-test.yaml",
            "content": "\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  # Test: mustToJson\n  # Test: toPrettyJson\n  # Test: mustToPrettyJson\n  # Test: toRawJson\n  # Test: mustToRawJson\n  mustToRawJsonValue: \"true\"\n  # Test: fromJson\n  # Test: fromJsonArray\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  # Test: toYaml\n  # Test: toYamlPretty\n  # Test: fromYamlArray\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n",
            "originalContent": "# Test: Type conversion errors - comprehensive testing of all type conversion functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n  {{- if .Values.testToStrings }}\n  toStringsValue: {{ toStrings $testData | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n  {{- if .Values.testToDecimal }}\n  toDecimalValue: {{ toDecimal $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  {{- if .Values.testToJson }}\n  toJsonValue: {{ toJson $testData | quote }}\n  {{- end }}\n  # Test: mustToJson\n  {{- if .Values.testMustToJson }}\n  mustToJsonValue: {{ mustToJson $testData | quote }}\n  {{- end }}\n  # Test: toPrettyJson\n  {{- if .Values.testToPrettyJson }}\n  toPrettyJsonValue: {{ toPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: mustToPrettyJson\n  {{- if .Values.testMustToPrettyJson }}\n  mustToPrettyJsonValue: {{ mustToPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: toRawJson\n  {{- if .Values.testToRawJson }}\n  toRawJsonValue: {{ toRawJson $testData | quote }}\n  {{- end }}\n  # Test: mustToRawJson\n  {{- if .Values.testMustToRawJson }}\n  mustToRawJsonValue: {{ mustToRawJson $testData | quote }}\n  {{- end }}\n  # Test: fromJson\n  {{- if .Values.testFromJson }}\n  {{- $parsed := $testData | fromJson }}\n  fromJsonValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: fromJsonArray\n  {{- if .Values.testFromJsonArray }}\n  {{- $arr := $testData | fromJsonArray }}\n  fromJsonArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  {{- if .Values.testFromYaml }}\n  {{- $parsed := $testData | fromYaml }}\n  fromYamlValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: toYaml\n  {{- if .Values.testToYaml }}\n  toYamlValue: {{ toYaml $testData | quote }}\n  {{- end }}\n  # Test: toYamlPretty\n  {{- if .Values.testToYamlPretty }}\n  toYamlPrettyValue: {{ toYamlPretty $testData | quote }}\n  {{- end }}\n  # Test: fromYamlArray\n  {{- if .Values.testFromYamlArray }}\n  {{- $arr := $testData | fromYamlArray }}\n  fromYamlArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n  {{- if .Values.testToToml }}\n  toTomlValue: {{ toToml $testData | quote }}\n  {{- end }}\n\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "resolved": true,
      "stopReason": "",
      "fixChain": []
    },
    {
      "toggle": "mustToRawJson",
      "dataType": "listValue",
      "valuesOptions": {
        "values": [
          "testMustToRawJson=true",
          "testDataType=listValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-type-conversion-test\r\ndata:\r\n  # ==========================================\r\n  # String conversion functions\r\n  # ==========================================\r\n  # Test: toStrings\r\n\r\n  # ==========================================\r\n  # Decimal conversion functions\r\n  # ==========================================\r\n  # Test: toDecimal\r\n\r\n  # ==========================================\r\n  # JSON conversion functions\r\n  # ==========================================\r\n  # Test: toJson\r\n  # Test: mustToJson\r\n  # Test: toPrettyJson\r\n  # Test: mustToPrettyJson\r\n  # Test: toRawJson\r\n  # Test: mustToRawJson\r\n  mustToRawJsonValue: \"[1,2,3]\"\r\n  # Test: fromJson\r\n  # Test: fromJsonArray\r\n\r\n  # ==========================================\r\n  # YAML conversion functions\r\n  # ==========================================\r\n  # Test: fromYaml\r\n  # Test: toYaml\r\n  # Test: toYamlPretty\r\n  # Test: fromYamlArray\r\n\r\n  # ==========================================\r\n  # TOML conversion functions\r\n  # ==========================================\r\n  # Test: toToml\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-type-conversion/templates/type-conversion-test.yaml",
            "content": "\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  # Test: mustToJson\n  # Test: toPrettyJson\n  # Test: mustToPrettyJson\n  # Test: toRawJson\n  # Test: mustToRawJson\n  mustToRawJsonValue: \"[1,2,3]\"\n  # Test: fromJson\n  # Test: fromJsonArray\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  # Test: toYaml\n  # Test: toYamlPretty\n  # Test: fromYamlArray\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n",
            "originalContent": "# Test: Type conversion errors - comprehensive testing of all type conversion functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n  {{- if .Values.testToStrings }}\n  toStringsValue: {{ toStrings $testData | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n  {{- if .Values.testToDecimal }}\n  toDecimalValue: {{ toDecimal $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  {{- if .Values.testToJson }}\n  toJsonValue: {{ toJson $testData | quote }}\n  {{- end }}\n  # Test: mustToJson\n  {{- if .Values.testMustToJson }}\n  mustToJsonValue: {{ mustToJson $testData | quote }}\n  {{- end }}\n  # Test: toPrettyJson\n  {{- if .Values.testToPrettyJson }}\n  toPrettyJsonValue: {{ toPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: mustToPrettyJson\n  {{- if .Values.testMustToPrettyJson }}\n  mustToPrettyJsonValue: {{ mustToPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: toRawJson\n  {{- if .Values.testToRawJson }}\n  toRawJsonValue: {{ toRawJson $testData | quote }}\n  {{- end }}\n  # Test: mustToRawJson\n  {{- if .Values.testMustToRawJson }}\n  mustToRawJsonValue: {{ mustToRawJson $testData | quote }}\n  {{- end }}\n  # Test: fromJson\n  {{- if .Values.testFromJson }}\n  {{- $parsed := $testData | fromJson }}\n  fromJsonValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: fromJsonArray\n  {{- if .Values.testFromJsonArray }}\n  {{- $arr := $testData | fromJsonArray }}\n  fromJsonArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  {{- if .Values.testFromYaml }}\n  {{- $parsed := $testData | fromYaml }}\n  fromYamlValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: toYaml\n  {{- if .Values.testToYaml }}\n  toYamlValue: {{ toYaml $testData | quote }}\n  {{- end }}\n  # Test: toYamlPretty\n  {{- if .Values.testToYamlPretty }}\n  toYamlPrettyValue: {{ toYamlPretty $testData | quote }}\n  {{- end }}\n  # Test: fromYamlArray\n  {{- if .Values.testFromYamlArray }}\n  {{- $arr := $testData | fromYamlArray }}\n  fromYamlArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n  {{- if .Values.testToToml }}\n  toTomlValue: {{ toToml $testData | quote }}\n  {{- end }}\n\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-type-conversion-test\r\ndata:\r\n  # ==========================================\r\n  # String conversion functions\r\n  # ==========================================\r\n  # Test: toStrings\r\n\r\n  # ==========================================\r\n  # Decimal conversion functions\r\n  # ==========================================\r\n  # Test: toDecimal\r\n\r\n  # ==========================================\r\n  # JSON conversion functions\r\n  # ==========================================\r\n  # Test: toJson\r\n  # Test: mustToJson\r\n  # Test: toPrettyJson\r\n  # Test: mustToPrettyJson\r\n  # Test: toRawJson\r\n  # Test: mustToRawJson\r\n  mustToRawJsonValue: \"[1,2,3]\"\r\n  # Test: fromJson\r\n  # Test: fromJsonArray\r\n\r\n  # ==========================================\r\n  # YAML conversion functions\r\n  # ==========================================\r\n  # Test: fromYaml\r\n  # Test: toYaml\r\n  # Test: toYamlPretty\r\n  # Test: fromYamlArray\r\n\r\n  # ==========================================\r\n  # TOML conversion functions\r\n  # ==========================================\r\n  # Test: toToml\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-type-conversion/templates/type-conversion-test.yaml",
            "content": "\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  # Test: mustToJson\n  # Test: toPrettyJson\n  # Test: mustToPrettyJson\n  # Test: toRawJson\n  # Test: mustToRawJson\n  mustToRawJsonValue: \"[1,2,3]\"\n  # Test: fromJson\n  # Test: fromJsonArray\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  # Test: toYaml\n  # Test: toYamlPretty\n  # Test: fromYamlArray\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n",
            "originalContent": "# Test: Type conversion errors - comprehensive testing of all type conversion functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n  {{- if .Values.testToStrings }}\n  toStringsValue: {{ toStrings $testData | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n  {{- if .Values.testToDecimal }}\n  toDecimalValue: {{ toDecimal $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  {{- if .Values.testToJson }}\n  toJsonValue: {{ toJson $testData | quote }}\n  {{- end }}\n  # Test: mustToJson\n  {{- if .Values.testMustToJson }}\n  mustToJsonValue: {{ mustToJson $testData | quote }}\n  {{- end }}\n  # Test: toPrettyJson\n  {{- if .Values.testToPrettyJson }}\n  toPrettyJsonValue: {{ toPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: mustToPrettyJson\n  {{- if .Values.testMustToPrettyJson }}\n  mustToPrettyJsonValue: {{ mustToPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: toRawJson\n  {{- if .Values.testToRawJson }}\n  toRawJsonValue: {{ toRawJson $testData | quote }}\n  {{- end }}\n  # Test: mustToRawJson\n  {{- if .Values.testMustToRawJson }}\n  mustToRawJsonValue: {{ mustToRawJson $testData | quote }}\n  {{- end }}\n  # Test: fromJson\n  {{- if .Values.testFromJson }}\n  {{- $parsed := $testData | fromJson }}\n  fromJsonValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: fromJsonArray\n  {{- if .Values.testFromJsonArray }}\n  {{- $arr := $testData | fromJsonArray }}\n  fromJsonArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  {{- if .Values.testFromYaml }}\n  {{- $parsed := $testData | fromYaml }}\n  fromYamlValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: toYaml\n  {{- if .Values.testToYaml }}\n  toYamlValue: {{ toYaml $testData | quote }}\n  {{- end }}\n  # Test: toYamlPretty\n  {{- if .Values.testToYamlPretty }}\n  toYamlPrettyValue: {{ toYamlPretty $testData | quote }}\n  {{- end }}\n  # Test: fromYamlArray\n  {{- if .Values.testFromYamlArray }}\n  {{- $arr := $testData | fromYamlArray }}\n  fromYamlArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n  {{- if .Values.testToToml }}\n  toTomlValue: {{ toToml $testData | quote }}\n  {{- end }}\n\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "resolved": true,
      "stopReason": "",
      "fixChain": []
    },
    {
      "toggle": "mustToRawJson",
      "dataType": "dictValue",
      "valuesOptions": {
        "values": [
          "testMustToRawJson=true",
          "testDataType=dictValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-type-conversion-test\r\ndata:\r\n  # ==========================================\r\n  # String conversion functions\r\n  # ==========================================\r\n  # Test: toStrings\r\n\r\n  # ==========================================\r\n  # Decimal conversion functions\r\n  # ==========================================\r\n  # Test: toDecimal\r\n\r\n  # ==========================================\r\n  # JSON conversion functions\r\n  # ==========================================\r\n  # Test: toJson\r\n  # Test: mustToJson\r\n  # Test: toPrettyJson\r\n  # Test: mustToPrettyJson\r\n  # Test: toRawJson\r\n  # Test: mustToRawJson\r\n  mustToRawJsonValue: \"{\\\"key\\\":\\\"value\\\"}\"\r\n  # Test: fromJson\r\n  # Test: fromJsonArray\r\n\r\n  # ==========================================\r\n  # YAML conversion functions\r\n  # ==========================================\r\n  # Test: fromYaml\r\n  # Test: toYaml\r\n  # Test: toYamlPretty\r\n  # Test: fromYamlArray\r\n\r\n  # ==========================================\r\n  # TOML conversion functions\r\n  # ==========================================\r\n  # Test: toToml\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-type-conversion/templates/type-conversion-test.yaml",
            "content": "\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  # Test: mustToJson\n  # Test: toPrettyJson\n  # Test: mustToPrettyJson\n  # Test: toRawJson\n  # Test: mustToRawJson\n  mustToRawJsonValue: \"{\\\"key\\\":\\\"value\\\"}\"\n  # Test: fromJson\n  # Test: fromJsonArray\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  # Test: toYaml\n  # Test: toYamlPretty\n  # Test: fromYamlArray\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n",
            "originalContent": "# Test: Type conversion errors - comprehensive testing of all type conversion functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n  {{- if .Values.testToStrings }}\n  toStringsValue: {{ toStrings $testData | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n  {{- if .Values.testToDecimal }}\n  toDecimalValue: {{ toDecimal $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  {{- if .Values.testToJson }}\n  toJsonValue: {{ toJson $testData | quote }}\n  {{- end }}\n  # Test: mustToJson\n  {{- if .Values.testMustToJson }}\n  mustToJsonValue: {{ mustToJson $testData | quote }}\n  {{- end }}\n  # Test: toPrettyJson\n  {{- if .Values.testToPrettyJson }}\n  toPrettyJsonValue: {{ toPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: mustToPrettyJson\n  {{- if .Values.testMustToPrettyJson }}\n  mustToPrettyJsonValue: {{ mustToPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: toRawJson\n  {{- if .Values.testToRawJson }}\n  toRawJsonValue: {{ toRawJson $testData | quote }}\n  {{- end }}\n  # Test: mustToRawJson\n  {{- if .Values.testMustToRawJson }}\n  mustToRawJsonValue: {{ mustToRawJson $testData | quote }}\n  {{- end }}\n  # Test: fromJson\n  {{- if .Values.testFromJson }}\n  {{- $parsed := $testData | fromJson }}\n  fromJsonValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: fromJsonArray\n  {{- if .Values.testFromJsonArray }}\n  {{- $arr := $testData | fromJsonArray }}\n  fromJsonArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  {{- if .Values.testFromYaml }}\n  {{- $parsed := $testData | fromYaml }}\n  fromYamlValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: toYaml\n  {{- if .Values.testToYaml }}\n  toYamlValue: {{ toYaml $testData | quote }}\n  {{- end }}\n  # Test: toYamlPretty\n  {{- if .Values.testToYamlPretty }}\n  toYamlPrettyValue: {{ toYamlPretty $testData | quote }}\n  {{- end }}\n  # Test: fromYamlArray\n  {{- if .Values.testFromYamlArray }}\n  {{- $arr := $testData | fromYamlArray }}\n  fromYamlArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n  {{- if .Values.testToToml }}\n  toTomlValue: {{ toToml $testData | quote }}\n  {{- end }}\n\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-type-conversion-test\r\ndata:\r\n  # ==========================================\r\n  # String conversion functions\r\n  # ==========================================\r\n  # Test: toStrings\r\n\r\n  # ==========================================\r\n  # Decimal conversion functions\r\n  # ==========================================\r\n  # Test: toDecimal\r\n\r\n  # ==========================================\r\n  # JSON conversion functions\r\n  # ==========================================\r\n  # Test: toJson\r\n  # Test: mustToJson\r\n  # Test: toPrettyJson\r\n  # Test: mustToPrettyJson\r\n  # Test: toRawJson\r\n  # Test: mustToRawJson\r\n  mustToRawJsonValue: \"{\\\"key\\\":\\\"value\\\"}\"\r\n  # Test: fromJson\r\n  # Test: fromJsonArray\r\n\r\n  # ==========================================\r\n  # YAML conversion functions\r\n  # ==========================================\r\n  # Test: fromYaml\r\n  # Test: toYaml\r\n  # Test: toYamlPretty\r\n  # Test: fromYamlArray\r\n\r\n  # ==========================================\r\n  # TOML conversion functions\r\n  # ==========================================\r\n  # Test: toToml\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-type-conversion/templates/type-conversion-test.yaml",
            "content": "\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  # Test: mustToJson\n  # Test: toPrettyJson\n  # Test: mustToPrettyJson\n  # Test: toRawJson\n  # Test: mustToRawJson\n  mustToRawJsonValue: \"{\\\"key\\\":\\\"value\\\"}\"\n  # Test: fromJson\n  # Test: fromJsonArray\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  # Test: toYaml\n  # Test: toYamlPretty\n  # Test: fromYamlArray\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n",
            "originalContent": "# Test: Type conversion errors - comprehensive testing of all type conversion functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n  {{- if .Values.testToStrings }}\n  toStringsValue: {{ toStrings $testData | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n  {{- if .Values.testToDecimal }}\n  toDecimalValue: {{ toDecimal $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  {{- if .Values.testToJson }}\n  toJsonValue: {{ toJson $testData | quote }}\n  {{- end }}\n  # Test: mustToJson\n  {{- if .Values.testMustToJson }}\n  mustToJsonValue: {{ mustToJson $testData | quote }}\n  {{- end }}\n  # Test: toPrettyJson\n  {{- if .Values.testToPrettyJson }}\n  toPrettyJsonValue: {{ toPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: mustToPrettyJson\n  {{- if .Values.testMustToPrettyJson }}\n  mustToPrettyJsonValue: {{ mustToPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: toRawJson\n  {{- if .Values.testToRawJson }}\n  toRawJsonValue: {{ toRawJson $testData | quote }}\n  {{- end }}\n  # Test: mustToRawJson\n  {{- if .Values.testMustToRawJson }}\n  mustToRawJsonValue: {{ mustToRawJson $testData | quote }}\n  {{- end }}\n  # Test: fromJson\n  {{- if .Values.testFromJson }}\n  {{- $parsed := $testData | fromJson }}\n  fromJsonValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: fromJsonArray\n  {{- if .Values.testFromJsonArray }}\n  {{- $arr := $testData | fromJsonArray }}\n  fromJsonArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  {{- if .Values.testFromYaml }}\n  {{- $parsed := $testData | fromYaml }}\n  fromYamlValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: toYaml\n  {{- if .Values.testToYaml }}\n  toYamlValue: {{ toYaml $testData | quote }}\n  {{- end }}\n  # Test: toYamlPretty\n  {{- if .Values.testToYamlPretty }}\n  toYamlPrettyValue: {{ toYamlPretty $testData | quote }}\n  {{- end }}\n  # Test: fromYamlArray\n  {{- if .Values.testFromYamlArray }}\n  {{- $arr := $testData | fromYamlArray }}\n  fromYamlArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n  {{- if .Values.testToToml }}\n  toTomlValue: {{ toToml $testData | quote }}\n  {{- end }}\n\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "resolved": true,
      "stopReason": "",
      "fixChain": []
    },
    {
      "toggle": "fromYaml",
      "dataType": "nilValue",
      "valuesOptions": {
        "values": [
          "testFromYaml=true",
          "testDataType=nilValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": "template: test-type-conversion/templates/type-conversion-test.yaml:68:29: executing \"test-type-conversion/templates/type-conversion-test.yaml\" at \u003cfromYaml\u003e: invalid value; expected string",
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      },
      "debug": {
        "error": "template: test-type-conversion/templates/type-conversion-test.yaml:68:29: executing \"test-type-conversion/templates/type-conversion-test.yaml\" at \u003cfromYaml\u003e: invalid value; expected string",
        "debugLogs": [],
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      },
      "resolved": false,
      "stopReason": "unfixable_error_kind",
      "fixChain": []
    },
    {
      "toggle": "fromYaml",
      "dataType": "stringValue",
      "valuesOptions": {
        "values": [
          "testFromYaml=true",
          "testDataType=stringValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-type-conversion-test\r\ndata:\r\n  # ==========================================\r\n  # String conversion functions\r\n  # ==========================================\r\n  # Test: toStrings\r\n\r\n  # ==========================================\r\n  # Decimal conversion functions\r\n  # ==========================================\r\n  # Test: toDecimal\r\n\r\n  # ==========================================\r\n  # JSON conversion functions\r\n  # ==========================================\r\n  # Test: toJson\r\n  # Test: mustToJson\r\n  # Test: toPrettyJson\r\n  # Test: mustToPrettyJson\r\n  # Test: toRawJson\r\n  # Test: mustToRawJson\r\n  # Test: fromJson\r\n  # Test: fromJsonArray\r\n\r\n  # ==========================================\r\n  # YAML conversion functions\r\n  # ==========================================\r\n  # Test: fromYaml\r\n  fromYamlValue: {\"Error\":\"error unmarshaling JSON: while decoding JSON: json: cannot unmarshal string into Go value of type map[string]interface {}\"}\r\n  # Test: toYaml\r\n  # Test: toYamlPretty\r\n  # Test: fromYamlArray\r\n\r\n  # ==========================================\r\n  # TOML conversion functions\r\n  # ==========================================\r\n  # Test: toToml\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-type-conversion/templates/type-conversion-test.yaml",
            "content": "\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  # Test: mustToJson\n  # Test: toPrettyJson\n  # Test: mustToPrettyJson\n  # Test: toRawJson\n  # Test: mustToRawJson\n  # Test: fromJson\n  # Test: fromJsonArray\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  fromYamlValue: {\"Error\":\"error unmarshaling JSON: while decoding JSON: json: cannot unmarshal string into Go value of type map[string]interface {}\"}\n  # Test: toYaml\n  # Test: toYamlPretty\n  # Test: fromYamlArray\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n",
            "originalContent": "# Test: Type conversion errors - comprehensive testing of all type conversion functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n  {{- if .Values.testToStrings }}\n  toStringsValue: {{ toStrings $testData | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n  {{- if .Values.testToDecimal }}\n  toDecimalValue: {{ toDecimal $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  {{- if .Values.testToJson }}\n  toJsonValue: {{ toJson $testData | quote }}\n  {{- end }}\n  # Test: mustToJson\n  {{- if .Values.testMustToJson }}\n  mustToJsonValue: {{ mustToJson $testData | quote }}\n  {{- end }}\n  # Test: toPrettyJson\n  {{- if .Values.testToPrettyJson }}\n  toPrettyJsonValue: {{ toPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: mustToPrettyJson\n  {{- if .Values.testMustToPrettyJson }}\n  mustToPrettyJsonValue: {{ mustToPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: toRawJson\n  {{- if .Values.testToRawJson }}\n  toRawJsonValue: {{ toRawJson $testData | quote }}\n  {{- end }}\n  # Test: mustToRawJson\n  {{- if .Values.testMustToRawJson }}\n  mustToRawJsonValue: {{ mustToRawJson $testData | quote }}\n  {{- end }}\n  # Test: fromJson\n  {{- if .Values.testFromJson }}\n  {{- $parsed := $testData | fromJson }}\n  fromJsonValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: fromJsonArray\n  {{- if .Values.testFromJsonArray }}\n  {{- $arr := $testData | fromJsonArray }}\n  fromJsonArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  {{- if .Values.testFromYaml }}\n  {{- $parsed := $testData | fromYaml }}\n  fromYamlValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: toYaml\n  {{- if .Values.testToYaml }}\n  toYamlValue: {{ toYaml $testData | quote }}\n  {{- end }}\n  # Test: toYamlPretty\n  {{- if .Values.testToYamlPretty }}\n  toYamlPrettyValue: {{ toYamlPretty $testData | quote }}\n  {{- end }}\n  # Test: fromYamlArray\n  {{- if .Values.testFromYamlArray }}\n  {{- $arr := $testData | fromYamlArray }}\n  fromYamlArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n  {{- if .Values.testToToml }}\n  toTomlValue: {{ toToml $testData | quote }}\n  {{- end }}\n\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-type-conversion-test\r\ndata:\r\n  # ==========================================\r\n  # String conversion functions\r\n  # ==========================================\r\n  # Test: toStrings\r\n\r\n  # ==========================================\r\n  # Decimal conversion functions\r\n  # ==========================================\r\n  # Test: toDecimal\r\n\r\n  # ==========================================\r\n  # JSON conversion functions\r\n  # ==========================================\r\n  # Test: toJson\r\n  # Test: mustToJson\r\n  # Test: toPrettyJson\r\n  # Test: mustToPrettyJson\r\n  # Test: toRawJson\r\n  # Test: mustToRawJson\r\n  # Test: fromJson\r\n  # Test: fromJsonArray\r\n\r\n  # ==========================================\r\n  # YAML conversion functions\r\n  # ==========================================\r\n  # Test: fromYaml\r\n  fromYamlValue: {\"Error\":\"error unmarshaling JSON: while decoding JSON: json: cannot unmarshal string into Go value of type map[string]interface {}\"}\r\n  # Test: toYaml\r\n  # Test: toYamlPretty\r\n  # Test: fromYamlArray\r\n\r\n  # ==========================================\r\n  # TOML conversion functions\r\n  # ==========================================\r\n  # Test: toToml\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-type-conversion/templates/type-conversion-test.yaml",
            "content": "\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  # Test: mustToJson\n  # Test: toPrettyJson\n  # Test: mustToPrettyJson\n  # Test: toRawJson\n  # Test: mustToRawJson\n  # Test: fromJson\n  # Test: fromJsonArray\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  fromYamlValue: {\"Error\":\"error unmarshaling JSON: while decoding JSON: json: cannot unmarshal string into Go value of type map[string]interface {}\"}\n  # Test: toYaml\n  # Test: toYamlPretty\n  # Test: fromYamlArray\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n",
            "originalContent": "# Test: Type conversion errors - comprehensive testing of all type conversion functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n  {{- if .Values.testToStrings }}\n  toStringsValue: {{ toStrings $testData | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n  {{- if .Values.testToDecimal }}\n  toDecimalValue: {{ toDecimal $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  {{- if .Values.testToJson }}\n  toJsonValue: {{ toJson $testData | quote }}\n  {{- end }}\n  # Test: mustToJson\n  {{- if .Values.testMustToJson }}\n  mustToJsonValue: {{ mustToJson $testData | quote }}\n  {{- end }}\n  # Test: toPrettyJson\n  {{- if .Values.testToPrettyJson }}\n  toPrettyJsonValue: {{ toPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: mustToPrettyJson\n  {{- if .Values.testMustToPrettyJson }}\n  mustToPrettyJsonValue: {{ mustToPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: toRawJson\n  {{- if .Values.testToRawJson }}\n  toRawJsonValue: {{ toRawJson $testData | quote }}\n  {{- end }}\n  # Test: mustToRawJson\n  {{- if .Values.testMustToRawJson }}\n  mustToRawJsonValue: {{ mustToRawJson $testData | quote }}\n  {{- end }}\n  # Test: fromJson\n  {{- if .Values.testFromJson }}\n  {{- $parsed := $testData | fromJson }}\n  fromJsonValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: fromJsonArray\n  {{- if .Values.testFromJsonArray }}\n  {{- $arr := $testData | fromJsonArray }}\n  fromJsonArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  {{- if .Values.testFromYaml }}\n  {{- $parsed := $testData | fromYaml }}\n  fromYamlValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: toYaml\n  {{- if .Values.testToYaml }}\n  toYamlValue: {{ toYaml $testData | quote }}\n  {{- end }}\n  # Test: toYamlPretty\n  {{- if .Values.testToYamlPretty }}\n  toYamlPrettyValue: {{ toYamlPretty $testData | quote }}\n  {{- end }}\n  # Test: fromYamlArray\n  {{- if .Values.testFromYamlArray }}\n  {{- $arr := $testData | fromYamlArray }}\n  fromYamlArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n  {{- if .Values.testToToml }}\n  toTomlValue: {{ toToml $testData | quote }}\n  {{- end }}\n\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "resolved": true,
      "stopReason": "",
      "fixChain": []
    },
    {
      "toggle": "fromYaml",
      "dataType": "numberValue",
      "valuesOptions": {
        "values": [
          "testFromYaml=true",
          "testDataType=numberValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": "template: test-type-conversion/templates/type-conversion-test.yaml:68:29: executing \"test-type-conversion/templates/type-conversion-test.yaml\" at \u003cfromYaml\u003e: wrong type for value; expected string; got float64",
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      },
      "debug": {
        "error": "template: test-type-conversion/templates/type-conversion-test.yaml:68:29: executing \"test-type-conversion/templates/type-conversion-test.yaml\" at \u003cfromYaml\u003e: wrong type for value; expected string; got float64",
        "debugLogs": [],
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      },
      "resolved": false,
      "stopReason": "unfixable_error_kind",
      "fixChain": []
    },
    {
      "toggle": "fromYaml",
      "dataType": "boolValue",
      "valuesOptions": {
        "values": [
          "testFromYaml=true",
          "testDataType=boolValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": "template: test-type-conversion/templates/type-conversion-test.yaml:68:29: executing \"test-type-conversion/templates/type-conversion-test.yaml\" at \u003cfromYaml\u003e: wrong type for value; expected string; got bool",
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      },
      "debug": {
        "error": "template: test-type-conversion/templates/type-conversion-test.yaml:68:29: executing \"test-type-conversion/templates/type-conversion-test.yaml\" at \u003cfromYaml\u003e: wrong type for value; expected string; got bool",
        "debugLogs": [],
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      },
      "resolved": false,
      "stopReason": "unfixable_error_kind",
      "fixChain": []
    },
    {
      "toggle": "fromYaml",
      "dataType": "listValue",
      "valuesOptions": {
        "values": [
          "testFromYaml=true",
          "testDataType=listValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": "template: test-type-conversion/templates/type-conversion-test.yaml:68:29: executing \"test-type-conversion/templates/type-conversion-test.yaml\" at \u003cfromYaml\u003e: wrong type for value; expected string; got []interface {}",
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      },
      "debug": {
        "error": "template: test-type-conversion/templates/type-conversion-test.yaml:68:29: executing \"test-type-conversion/templates/type-conversion-test.yaml\" at \u003cfromYaml\u003e: wrong type for value; expected string; got []interface {}",
        "debugLogs": [],
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      },
      "resolved": false,
      "stopReason": "unfixable_error_kind",
      "fixChain": []
    },
    {
      "toggle": "fromYaml",
      "dataType": "dictValue",
      "valuesOptions": {
        "values": [
          "testFromYaml=true",
          "testDataType=dictValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": "template: test-type-conversion/templates/type-conversion-test.yaml:68:29: executing \"test-type-conversion/templates/type-conversion-test.yaml\" at \u003cfromYaml\u003e: wrong type for value; expected string; got map[string]interface {}",
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      },
      "debug": {
        "error": "template: test-type-conversion/templates/type-conversion-test.yaml:68:29: executing \"test-type-conversion/templates/type-conversion-test.yaml\" at \u003cfromYaml\u003e: wrong type for value; expected string; got map[string]interface {}",
        "debugLogs": [],
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      },
      "resolved": false,
      "stopReason": "unfixable_error_kind",
      "fixChain": []
    },
    {
      "toggle": "fromJson",
      "dataType": "nilValue",
      "valuesOptions": {
        "values": [
          "testFromJson=true",
          "testDataType=nilValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": "template: test-type-conversion/templates/type-conversion-test.yaml:54:29: executing \"test-type-conversion/templates/type-conversion-test.yaml\" at \u003cfromJson\u003e: invalid value; expected string",
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      },
      "debug": {
        "error": "template: test-type-conversion/templates/type-conversion-test.yaml:54:29: executing \"test-type-conversion/templates/type-conversion-test.yaml\" at \u003cfromJson\u003e: invalid value; expected string",
        "debugLogs": [],
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      },
      "resolved": false,
      "stopReason": "unfixable_error_kind",
      "fixChain": []
    },
    {
      "toggle": "fromJson",
      "dataType": "stringValue",
      "valuesOptions": {
        "values": [
          "testFromJson=true",
          "testDataType=stringValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-type-conversion-test\r\ndata:\r\n  # ==========================================\r\n  # String conversion functions\r\n  # ==========================================\r\n  # Test: toStrings\r\n\r\n  # ==========================================\r\n  # Decimal conversion functions\r\n  # ==========================================\r\n  # Test: toDecimal\r\n\r\n  # ==========================================\r\n  # JSON conversion functions\r\n  # ==========================================\r\n  # Test: toJson\r\n  # Test: mustToJson\r\n  # Test: toPrettyJson\r\n  # Test: mustToPrettyJson\r\n  # Test: toRawJson\r\n  # Test: mustToRawJson\r\n  # Test: fromJson\r\n  fromJsonValue: {\"Error\":\"invalid character 'e' in literal true (expecting 'r')\"}\r\n  # Test: fromJsonArray\r\n\r\n  # ==========================================\r\n  # YAML conversion functions\r\n  # ==========================================\r\n  # Test: fromYaml\r\n  # Test: toYaml\r\n  # Test: toYamlPretty\r\n  # Test: fromYamlArray\r\n\r\n  # ==========================================\r\n  # TOML conversion functions\r\n  # ==========================================\r\n  # Test: toToml\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-type-conversion/templates/type-conversion-test.yaml",
            "content": "\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  # Test: mustToJson\n  # Test: toPrettyJson\n  # Test: mustToPrettyJson\n  # Test: toRawJson\n  # Test: mustToRawJson\n  # Test: fromJson\n  fromJsonValue: {\"Error\":\"invalid character 'e' in literal true (expecting 'r')\"}\n  # Test: fromJsonArray\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  # Test: toYaml\n  # Test: toYamlPretty\n  # Test: fromYamlArray\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n",
            "originalContent": "# Test: Type conversion errors - comprehensive testing of all type conversion functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n  {{- if .Values.testToStrings }}\n  toStringsValue: {{ toStrings $testData | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n  {{- if .Values.testToDecimal }}\n  toDecimalValue: {{ toDecimal $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  {{- if .Values.testToJson }}\n  toJsonValue: {{ toJson $testData | quote }}\n  {{- end }}\n  # Test: mustToJson\n  {{- if .Values.testMustToJson }}\n  mustToJsonValue: {{ mustToJson $testData | quote }}\n  {{- end }}\n  # Test: toPrettyJson\n  {{- if .Values.testToPrettyJson }}\n  toPrettyJsonValue: {{ toPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: mustToPrettyJson\n  {{- if .Values.testMustToPrettyJson }}\n  mustToPrettyJsonValue: {{ mustToPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: toRawJson\n  {{- if .Values.testToRawJson }}\n  toRawJsonValue: {{ toRawJson $testData | quote }}\n  {{- end }}\n  # Test: mustToRawJson\n  {{- if .Values.testMustToRawJson }}\n  mustToRawJsonValue: {{ mustToRawJson $testData | quote }}\n  {{- end }}\n  # Test: fromJson\n  {{- if .Values.testFromJson }}\n  {{- $parsed := $testData | fromJson }}\n  fromJsonValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: fromJsonArray\n  {{- if .Values.testFromJsonArray }}\n  {{- $arr := $testData | fromJsonArray }}\n  fromJsonArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  {{- if .Values.testFromYaml }}\n  {{- $parsed := $testData | fromYaml }}\n  fromYamlValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: toYaml\n  {{- if .Values.testToYaml }}\n  toYamlValue: {{ toYaml $testData | quote }}\n  {{- end }}\n  # Test: toYamlPretty\n  {{- if .Values.testToYamlPretty }}\n  toYamlPrettyValue: {{ toYamlPretty $testData | quote }}\n  {{- end }}\n  # Test: fromYamlArray\n  {{- if .Values.testFromYamlArray }}\n  {{- $arr := $testData | fromYamlArray }}\n  fromYamlArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n  {{- if .Values.testToToml }}\n  toTomlValue: {{ toToml $testData | quote }}\n  {{- end }}\n\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-type-conversion-test\r\ndata:\r\n  # ==========================================\r\n  # String conversion functions\r\n  # ==========================================\r\n  # Test: toStrings\r\n\r\n  # ==========================================\r\n  # Decimal conversion functions\r\n  # ==========================================\r\n  # Test: toDecimal\r\n\r\n  # ==========================================\r\n  # JSON conversion functions\r\n  # ==========================================\r\n  # Test: toJson\r\n  # Test: mustToJson\r\n  # Test: toPrettyJson\r\n  # Test: mustToPrettyJson\r\n  # Test: toRawJson\r\n  # Test: mustToRawJson\r\n  # Test: fromJson\r\n  fromJsonValue: {\"Error\":\"invalid character 'e' in literal true (expecting 'r')\"}\r\n  # Test: fromJsonArray\r\n\r\n  # ==========================================\r\n  # YAML conversion functions\r\n  # ==========================================\r\n  # Test: fromYaml\r\n  # Test: toYaml\r\n  # Test: toYamlPretty\r\n  # Test: fromYamlArray\r\n\r\n  # ==========================================\r\n  # TOML conversion functions\r\n  # ==========================================\r\n  # Test: toToml\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-type-conversion/templates/type-conversion-test.yaml",
            "content": "\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  # Test: mustToJson\n  # Test: toPrettyJson\n  # Test: mustToPrettyJson\n  # Test: toRawJson\n  # Test: mustToRawJson\n  # Test: fromJson\n  fromJsonValue: {\"Error\":\"invalid character 'e' in literal true (expecting 'r')\"}\n  # Test: fromJsonArray\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  # Test: toYaml\n  # Test: toYamlPretty\n  # Test: fromYamlArray\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n",
            "originalContent": "# Test: Type conversion errors - comprehensive testing of all type conversion functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n  {{- if .Values.testToStrings }}\n  toStringsValue: {{ toStrings $testData | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n  {{- if .Values.testToDecimal }}\n  toDecimalValue: {{ toDecimal $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  {{- if .Values.testToJson }}\n  toJsonValue: {{ toJson $testData | quote }}\n  {{- end }}\n  # Test: mustToJson\n  {{- if .Values.testMustToJson }}\n  mustToJsonValue: {{ mustToJson $testData | quote }}\n  {{- end }}\n  # Test: toPrettyJson\n  {{- if .Values.testToPrettyJson }}\n  toPrettyJsonValue: {{ toPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: mustToPrettyJson\n  {{- if .Values.testMustToPrettyJson }}\n  mustToPrettyJsonValue: {{ mustToPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: toRawJson\n  {{- if .Values.testToRawJson }}\n  toRawJsonValue: {{ toRawJson $testData | quote }}\n  {{- end }}\n  # Test: mustToRawJson\n  {{- if .Values.testMustToRawJson }}\n  mustToRawJsonValue: {{ mustToRawJson $testData | quote }}\n  {{- end }}\n  # Test: fromJson\n  {{- if .Values.testFromJson }}\n  {{- $parsed := $testData | fromJson }}\n  fromJsonValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: fromJsonArray\n  {{- if .Values.testFromJsonArray }}\n  {{- $arr := $testData | fromJsonArray }}\n  fromJsonArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  {{- if .Values.testFromYaml }}\n  {{- $parsed := $testData | fromYaml }}\n  fromYamlValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: toYaml\n  {{- if .Values.testToYaml }}\n  toYamlValue: {{ toYaml $testData | quote }}\n  {{- end }}\n  # Test: toYamlPretty\n  {{- if .Values.testToYamlPretty }}\n  toYamlPrettyValue: {{ toYamlPretty $testData | quote }}\n  {{- end }}\n  # Test: fromYamlArray\n  {{- if .Values.testFromYamlArray }}\n  {{- $arr := $testData | fromYamlArray }}\n  fromYamlArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n  {{- if .Values.testToToml }}\n  toTomlValue: {{ toToml $testData | quote }}\n  {{- end }}\n\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "resolved": true,
      "stopReason": "",
      "fixChain": []
    },
    {
      "toggle": "fromJson",
      "dataType": "numberValue",
      "valuesOptions": {
        "values": [
          "testFromJson=true",
          "testDataType=numberValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": "template: test-type-conversion/templates/type-conversion-test.yaml:54:29: executing \"test-type-conversion/templates/type-conversion-test.yaml\" at \u003cfromJson\u003e: wrong type for value; expected string; got float64",
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      },
      "debug": {
        "error": "template: test-type-conversion/templates/type-conversion-test.yaml:54:29: executing \"test-type-conversion/templates/type-conversion-test.yaml\" at \u003cfromJson\u003e: wrong type for value; expected string; got float64",
        "debugLogs": [],
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      },
      "resolved": false,
      "stopReason": "unfixable_error_kind",
      "fixChain": []
    },
    {
      "toggle": "fromJson",
      "dataType": "boolValue",
      "valuesOptions": {
        "values": [
          "testFromJson=true",
          "testDataType=boolValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": "template: test-type-conversion/templates/type-conversion-test.yaml:54:29: executing \"test-type-conversion/templates/type-conversion-test.yaml\" at \u003cfromJson\u003e: wrong type for value; expected string; got bool",
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      },
      "debug": {
        "error": "template: test-type-conversion/templates/type-conversion-test.yaml:54:29: executing \"test-type-conversion/templates/type-conversion-test.yaml\" at \u003cfromJson\u003e: wrong type for value; expected string; got bool",
        "debugLogs": [],
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      },
      "resolved": false,
      "stopReason": "unfixable_error_kind",
      "fixChain": []
    },
    {
      "toggle": "fromJson",
      "dataType": "listValue",
      "valuesOptions": {
        "values": [
          "testFromJson=true",
          "testDataType=listValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": "template: test-type-conversion/templates/type-conversion-test.yaml:54:29: executing \"test-type-conversion/templates/type-conversion-test.yaml\" at \u003cfromJson\u003e: wrong type for value; expected string; got []interface {}",
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      },
      "debug": {
        "error": "template: test-type-conversion/templates/type-conversion-test.yaml:54:29: executing \"test-type-conversion/templates/type-conversion-test.yaml\" at \u003cfromJson\u003e: wrong type for value; expected string; got []interface {}",
        "debugLogs": [],
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      },
      "resolved": false,
      "stopReason": "unfixable_error_kind",
      "fixChain": []
    },
    {
      "toggle": "fromJson",
      "dataType": "dictValue",
      "valuesOptions": {
        "values": [
          "testFromJson=true",
          "testDataType=dictValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": "template: test-type-conversion/templates/type-conversion-test.yaml:54:29: executing \"test-type-conversion/templates/type-conversion-test.yaml\" at \u003cfromJson\u003e: wrong type for value; expected string; got map[string]interface {}",
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      },
      "debug": {
        "error": "template: test-type-conversion/templates/type-conversion-test.yaml:54:29: executing \"test-type-conversion/templates/type-conversion-test.yaml\" at \u003cfromJson\u003e: wrong type for value; expected string; got map[string]interface {}",
        "debugLogs": [],
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      },
      "resolved": false,
      "stopReason": "unfixable_error_kind",
      "fixChain": []
    },
    {
      "toggle": "fromJsonArray",
      "dataType": "nilValue",
      "valuesOptions": {
        "values": [
          "testFromJsonArray=true",
          "testDataType=nilValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": "template: test-type-conversion/templates/type-conversion-test.yaml:59:26: executing \"test-type-conversion/templates/type-conversion-test.yaml\" at \u003cfromJsonArray\u003e: invalid value; expected string",
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      },
      "debug": {
        "error": "template: test-type-conversion/templates/type-conversion-test.yaml:59:26: executing \"test-type-conversion/templates/type-conversion-test.yaml\" at \u003cfromJsonArray\u003e: invalid value; expected string",
        "debugLogs": [],
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      },
      "resolved": false,
      "stopReason": "unfixable_error_kind",
      "fixChain": []
    },
    {
      "toggle": "fromJsonArray",
      "dataType": "stringValue",
      "valuesOptions": {
        "values": [
          "testFromJsonArray=true",
          "testDataType=stringValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-type-conversion-test\r\ndata:\r\n  # ==========================================\r\n  # String conversion functions\r\n  # ==========================================\r\n  # Test: toStrings\r\n\r\n  # ==========================================\r\n  # Decimal conversion functions\r\n  # ==========================================\r\n  # Test: toDecimal\r\n\r\n  # ==========================================\r\n  # JSON conversion functions\r\n  # ==========================================\r\n  # Test: toJson\r\n  # Test: mustToJson\r\n  # Test: toPrettyJson\r\n  # Test: mustToPrettyJson\r\n  # Test: toRawJson\r\n  # Test: mustToRawJson\r\n  # Test: fromJson\r\n  # Test: fromJsonArray\r\n  fromJsonArrayValue: [\"invalid character 'e' in literal true (expecting 'r')\"]\r\n\r\n  # ==========================================\r\n  # YAML conversion functions\r\n  # ==========================================\r\n  # Test: fromYaml\r\n  # Test: toYaml\r\n  # Test: toYamlPretty\r\n  # Test: fromYamlArray\r\n\r\n  # ==========================================\r\n  # TOML conversion functions\r\n  # ==========================================\r\n  # Test: toToml\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-type-conversion/templates/type-conversion-test.yaml",
            "content": "\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  # Test: mustToJson\n  # Test: toPrettyJson\n  # Test: mustToPrettyJson\n  # Test: toRawJson\n  # Test: mustToRawJson\n  # Test: fromJson\n  # Test: fromJsonArray\n  fromJsonArrayValue: [\"invalid character 'e' in literal true (expecting 'r')\"]\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  # Test: toYaml\n  # Test: toYamlPretty\n  # Test: fromYamlArray\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n",
            "originalContent": "# Test: Type conversion errors - comprehensive testing of all type conversion functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n  {{- if .Values.testToStrings }}\n  toStringsValue: {{ toStrings $testData | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n  {{- if .Values.testToDecimal }}\n  toDecimalValue: {{ toDecimal $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  {{- if .Values.testToJson }}\n  toJsonValue: {{ toJson $testData | quote }}\n  {{- end }}\n  # Test: mustToJson\n  {{- if .Values.testMustToJson }}\n  mustToJsonValue: {{ mustToJson $testData | quote }}\n  {{- end }}\n  # Test: toPrettyJson\n  {{- if .Values.testToPrettyJson }}\n  toPrettyJsonValue: {{ toPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: mustToPrettyJson\n  {{- if .Values.testMustToPrettyJson }}\n  mustToPrettyJsonValue: {{ mustToPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: toRawJson\n  {{- if .Values.testToRawJson }}\n  toRawJsonValue: {{ toRawJson $testData | quote }}\n  {{- end }}\n  # Test: mustToRawJson\n  {{- if .Values.testMustToRawJson }}\n  mustToRawJsonValue: {{ mustToRawJson $testData | quote }}\n  {{- end }}\n  # Test: fromJson\n  {{- if .Values.testFromJson }}\n  {{- $parsed := $testData | fromJson }}\n  fromJsonValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: fromJsonArray\n  {{- if .Values.testFromJsonArray }}\n  {{- $arr := $testData | fromJsonArray }}\n  fromJsonArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  {{- if .Values.testFromYaml }}\n  {{- $parsed := $testData | fromYaml }}\n  fromYamlValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: toYaml\n  {{- if .Values.testToYaml }}\n  toYamlValue: {{ toYaml $testData | quote }}\n  {{- end }}\n  # Test: toYamlPretty\n  {{- if .Values.testToYamlPretty }}\n  toYamlPrettyValue: {{ toYamlPretty $testData | quote }}\n  {{- end }}\n  # Test: fromYamlArray\n  {{- if .Values.testFromYamlArray }}\n  {{- $arr := $testData | fromYamlArray }}\n  fromYamlArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n  {{- if .Values.testToToml }}\n  toTomlValue: {{ toToml $testData | quote }}\n  {{- end }}\n\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-type-conversion-test\r\ndata:\r\n  # ==========================================\r\n  # String conversion functions\r\n  # ==========================================\r\n  # Test: toStrings\r\n\r\n  # ==========================================\r\n  # Decimal conversion functions\r\n  # ==========================================\r\n  # Test: toDecimal\r\n\r\n  # ==========================================\r\n  # JSON conversion functions\r\n  # ==========================================\r\n  # Test: toJson\r\n  # Test: mustToJson\r\n  # Test: toPrettyJson\r\n  # Test: mustToPrettyJson\r\n  # Test: toRawJson\r\n  # Test: mustToRawJson\r\n  # Test: fromJson\r\n  # Test: fromJsonArray\r\n  fromJsonArrayValue: [\"invalid character 'e' in literal true (expecting 'r')\"]\r\n\r\n  # ==========================================\r\n  # YAML conversion functions\r\n  # ==========================================\r\n  # Test: fromYaml\r\n  # Test: toYaml\r\n  # Test: toYamlPretty\r\n  # Test: fromYamlArray\r\n\r\n  # ==========================================\r\n  # TOML conversion functions\r\n  # ==========================================\r\n  # Test: toToml\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-type-conversion/templates/type-conversion-test.yaml",
            "content": "\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  # Test: mustToJson\n  # Test: toPrettyJson\n  # Test: mustToPrettyJson\n  # Test: toRawJson\n  # Test: mustToRawJson\n  # Test: fromJson\n  # Test: fromJsonArray\n  fromJsonArrayValue: [\"invalid character 'e' in literal true (expecting 'r')\"]\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  # Test: toYaml\n  # Test: toYamlPretty\n  # Test: fromYamlArray\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n",
            "originalContent": "# Test: Type conversion errors - comprehensive testing of all type conversion functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n  {{- if .Values.testToStrings }}\n  toStringsValue: {{ toStrings $testData | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n  {{- if .Values.testToDecimal }}\n  toDecimalValue: {{ toDecimal $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  {{- if .Values.testToJson }}\n  toJsonValue: {{ toJson $testData | quote }}\n  {{- end }}\n  # Test: mustToJson\n  {{- if .Values.testMustToJson }}\n  mustToJsonValue: {{ mustToJson $testData | quote }}\n  {{- end }}\n  # Test: toPrettyJson\n  {{- if .Values.testToPrettyJson }}\n  toPrettyJsonValue: {{ toPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: mustToPrettyJson\n  {{- if .Values.testMustToPrettyJson }}\n  mustToPrettyJsonValue: {{ mustToPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: toRawJson\n  {{- if .Values.testToRawJson }}\n  toRawJsonValue: {{ toRawJson $testData | quote }}\n  {{- end }}\n  # Test: mustToRawJson\n  {{- if .Values.testMustToRawJson }}\n  mustToRawJsonValue: {{ mustToRawJson $testData | quote }}\n  {{- end }}\n  # Test: fromJson\n  {{- if .Values.testFromJson }}\n  {{- $parsed := $testData | fromJson }}\n  fromJsonValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: fromJsonArray\n  {{- if .Values.testFromJsonArray }}\n  {{- $arr := $testData | fromJsonArray }}\n  fromJsonArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  {{- if .Values.testFromYaml }}\n  {{- $parsed := $testData | fromYaml }}\n  fromYamlValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: toYaml\n  {{- if .Values.testToYaml }}\n  toYamlValue: {{ toYaml $testData | quote }}\n  {{- end }}\n  # Test: toYamlPretty\n  {{- if .Values.testToYamlPretty }}\n  toYamlPrettyValue: {{ toYamlPretty $testData | quote }}\n  {{- end }}\n  # Test: fromYamlArray\n  {{- if .Values.testFromYamlArray }}\n  {{- $arr := $testData | fromYamlArray }}\n  fromYamlArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n  {{- if .Values.testToToml }}\n  toTomlValue: {{ toToml $testData | quote }}\n  {{- end }}\n\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "resolved": true,
      "stopReason": "",
      "fixChain": []
    },
    {
      "toggle": "fromJsonArray",
      "dataType": "numberValue",
      "valuesOptions": {
        "values": [
          "testFromJsonArray=true",
          "testDataType=numberValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": "template: test-type-conversion/templates/type-conversion-test.yaml:59:26: executing \"test-type-conversion/templates/type-conversion-test.yaml\" at \u003cfromJsonArray\u003e: wrong type for value; expected string; got float64",
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      },
      "debug": {
        "error": "template: test-type-conversion/templates/type-conversion-test.yaml:59:26: executing \"test-type-conversion/templates/type-conversion-test.yaml\" at \u003cfromJsonArray\u003e: wrong type for value; expected string; got float64",
        "debugLogs": [],
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      },
      "resolved": false,
      "stopReason": "unfixable_error_kind",
      "fixChain": []
    },
    {
      "toggle": "fromJsonArray",
      "dataType": "boolValue",
      "valuesOptions": {
        "values": [
          "testFromJsonArray=true",
          "testDataType=boolValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": "template: test-type-conversion/templates/type-conversion-test.yaml:59:26: executing \"test-type-conversion/templates/type-conversion-test.yaml\" at \u003cfromJsonArray\u003e: wrong type for value; expected string; got bool",
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      },
      "debug": {
        "error": "template: test-type-conversion/templates/type-conversion-test.yaml:59:26: executing \"test-type-conversion/templates/type-conversion-test.yaml\" at \u003cfromJsonArray\u003e: wrong type for value; expected string; got bool",
        "debugLogs": [],
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      },
      "resolved": false,
      "stopReason": "unfixable_error_kind",
      "fixChain": []
    },
    {
      "toggle": "fromJsonArray",
      "dataType": "listValue",
      "valuesOptions": {
        "values": [
          "testFromJsonArray=true",
          "testDataType=listValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": "template: test-type-conversion/templates/type-conversion-test.yaml:59:26: executing \"test-type-conversion/templates/type-conversion-test.yaml\" at \u003cfromJsonArray\u003e: wrong type for value; expected string; got []interface {}",
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      },
      "debug": {
        "error": "template: test-type-conversion/templates/type-conversion-test.yaml:59:26: executing \"test-type-conversion/templates/type-conversion-test.yaml\" at \u003cfromJsonArray\u003e: wrong type for value; expected string; got []interface {}",
        "debugLogs": [],
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      },
      "resolved": false,
      "stopReason": "unfixable_error_kind",
      "fixChain": []
    },
    {
      "toggle": "fromJsonArray",
      "dataType": "dictValue",
      "valuesOptions": {
        "values": [
          "testFromJsonArray=true",
          "testDataType=dictValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": "template: test-type-conversion/templates/type-conversion-test.yaml:59:26: executing \"test-type-conversion/templates/type-conversion-test.yaml\" at \u003cfromJsonArray\u003e: wrong type for value; expected string; got map[string]interface {}",
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      },
      "debug": {
        "error": "template: test-type-conversion/templates/type-conversion-test.yaml:59:26: executing \"test-type-conversion/templates/type-conversion-test.yaml\" at \u003cfromJsonArray\u003e: wrong type for value; expected string; got map[string]interface {}",
        "debugLogs": [],
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      },
      "resolved": false,
      "stopReason": "unfixable_error_kind",
      "fixChain": []
    },
    {
      "toggle": "toYaml",
      "dataType": "nilValue",
      "valuesOptions": {
        "values": [
          "testToYaml=true",
          "testDataType=nilValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-type-conversion-test\r\ndata:\r\n  # ==========================================\r\n  # String conversion functions\r\n  # ==========================================\r\n  # Test: toStrings\r\n\r\n  # ==========================================\r\n  # Decimal conversion functions\r\n  # ==========================================\r\n  # Test: toDecimal\r\n\r\n  # ==========================================\r\n  # JSON conversion functions\r\n  # ==========================================\r\n  # Test: toJson\r\n  # Test: mustToJson\r\n  # Test: toPrettyJson\r\n  # Test: mustToPrettyJson\r\n  # Test: toRawJson\r\n  # Test: mustToRawJson\r\n  # Test: fromJson\r\n  # Test: fromJsonArray\r\n\r\n  # ==========================================\r\n  # YAML conversion functions\r\n  # ==========================================\r\n  # Test: fromYaml\r\n  # Test: toYaml\r\n  toYamlValue: \"null\"\r\n  # Test: toYamlPretty\r\n  # Test: fromYamlArray\r\n\r\n  # ==========================================\r\n  # TOML conversion functions\r\n  # ==========================================\r\n  # Test: toToml\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-type-conversion/templates/type-conversion-test.yaml",
            "content": "\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  # Test: mustToJson\n  # Test: toPrettyJson\n  # Test: mustToPrettyJson\n  # Test: toRawJson\n  # Test: mustToRawJson\n  # Test: fromJson\n  # Test: fromJsonArray\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  # Test: toYaml\n  toYamlValue: \"null\"\n  # Test: toYamlPretty\n  # Test: fromYamlArray\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n",
            "originalContent": "# Test: Type conversion errors - comprehensive testing of all type conversion functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n  {{- if .Values.testToStrings }}\n  toStringsValue: {{ toStrings $testData | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n  {{- if .Values.testToDecimal }}\n  toDecimalValue: {{ toDecimal $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  {{- if .Values.testToJson }}\n  toJsonValue: {{ toJson $testData | quote }}\n  {{- end }}\n  # Test: mustToJson\n  {{- if .Values.testMustToJson }}\n  mustToJsonValue: {{ mustToJson $testData | quote }}\n  {{- end }}\n  # Test: toPrettyJson\n  {{- if .Values.testToPrettyJson }}\n  toPrettyJsonValue: {{ toPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: mustToPrettyJson\n  {{- if .Values.testMustToPrettyJson }}\n  mustToPrettyJsonValue: {{ mustToPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: toRawJson\n  {{- if .Values.testToRawJson }}\n  toRawJsonValue: {{ toRawJson $testData | quote }}\n  {{- end }}\n  # Test: mustToRawJson\n  {{- if .Values.testMustToRawJson }}\n  mustToRawJsonValue: {{ mustToRawJson $testData | quote }}\n  {{- end }}\n  # Test: fromJson\n  {{- if .Values.testFromJson }}\n  {{- $parsed := $testData | fromJson }}\n  fromJsonValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: fromJsonArray\n  {{- if .Values.testFromJsonArray }}\n  {{- $arr := $testData | fromJsonArray }}\n  fromJsonArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  {{- if .Values.testFromYaml }}\n  {{- $parsed := $testData | fromYaml }}\n  fromYamlValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: toYaml\n  {{- if .Values.testToYaml }}\n  toYamlValue: {{ toYaml $testData | quote }}\n  {{- end }}\n  # Test: toYamlPretty\n  {{- if .Values.testToYamlPretty }}\n  toYamlPrettyValue: {{ toYamlPretty $testData | quote }}\n  {{- end }}\n  # Test: fromYamlArray\n  {{- if .Values.testFromYamlArray }}\n  {{- $arr := $testData | fromYamlArray }}\n  fromYamlArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n  {{- if .Values.testToToml }}\n  toTomlValue: {{ toToml $testData | quote }}\n  {{- end }}\n\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-type-conversion-test\r\ndata:\r\n  # ==========================================\r\n  # String conversion functions\r\n  # ==========================================\r\n  # Test: toStrings\r\n\r\n  # ==========================================\r\n  # Decimal conversion functions\r\n  # ==========================================\r\n  # Test: toDecimal\r\n\r\n  # ==========================================\r\n  # JSON conversion functions\r\n  # ==========================================\r\n  # Test: toJson\r\n  # Test: mustToJson\r\n  # Test: toPrettyJson\r\n  # Test: mustToPrettyJson\r\n  # Test: toRawJson\r\n  # Test: mustToRawJson\r\n  # Test: fromJson\r\n  # Test: fromJsonArray\r\n\r\n  # ==========================================\r\n  # YAML conversion functions\r\n  # ==========================================\r\n  # Test: fromYaml\r\n  # Test: toYaml\r\n  toYamlValue: \"null\"\r\n  # Test: toYamlPretty\r\n  # Test: fromYamlArray\r\n\r\n  # ==========================================\r\n  # TOML conversion functions\r\n  # ==========================================\r\n  # Test: toToml\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-type-conversion/templates/type-conversion-test.yaml",
            "content": "\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  # Test: mustToJson\n  # Test: toPrettyJson\n  # Test: mustToPrettyJson\n  # Test: toRawJson\n  # Test: mustToRawJson\n  # Test: fromJson\n  # Test: fromJsonArray\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  # Test: toYaml\n  toYamlValue: \"null\"\n  # Test: toYamlPretty\n  # Test: fromYamlArray\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n",
            "originalContent": "# Test: Type conversion errors - comprehensive testing of all type conversion functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n  {{- if .Values.testToStrings }}\n  toStringsValue: {{ toStrings $testData | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n  {{- if .Values.testToDecimal }}\n  toDecimalValue: {{ toDecimal $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  {{- if .Values.testToJson }}\n  toJsonValue: {{ toJson $testData | quote }}\n  {{- end }}\n  # Test: mustToJson\n  {{- if .Values.testMustToJson }}\n  mustToJsonValue: {{ mustToJson $testData | quote }}\n  {{- end }}\n  # Test: toPrettyJson\n  {{- if .Values.testToPrettyJson }}\n  toPrettyJsonValue: {{ toPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: mustToPrettyJson\n  {{- if .Values.testMustToPrettyJson }}\n  mustToPrettyJsonValue: {{ mustToPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: toRawJson\n  {{- if .Values.testToRawJson }}\n  toRawJsonValue: {{ toRawJson $testData | quote }}\n  {{- end }}\n  # Test: mustToRawJson\n  {{- if .Values.testMustToRawJson }}\n  mustToRawJsonValue: {{ mustToRawJson $testData | quote }}\n  {{- end }}\n  # Test: fromJson\n  {{- if .Values.testFromJson }}\n  {{- $parsed := $testData | fromJson }}\n  fromJsonValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: fromJsonArray\n  {{- if .Values.testFromJsonArray }}\n  {{- $arr := $testData | fromJsonArray }}\n  fromJsonArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  {{- if .Values.testFromYaml }}\n  {{- $parsed := $testData | fromYaml }}\n  fromYamlValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: toYaml\n  {{- if .Values.testToYaml }}\n  toYamlValue: {{ toYaml $testData | quote }}\n  {{- end }}\n  # Test: toYamlPretty\n  {{- if .Values.testToYamlPretty }}\n  toYamlPrettyValue: {{ toYamlPretty $testData | quote }}\n  {{- end }}\n  # Test: fromYamlArray\n  {{- if .Values.testFromYamlArray }}\n  {{- $arr := $testData | fromYamlArray }}\n  fromYamlArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n  {{- if .Values.testToToml }}\n  toTomlValue: {{ toToml $testData | quote }}\n  {{- end }}\n\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "resolved": true,
      "stopReason": "",
      "fixChain": []
    },
    {
      "toggle": "toYaml",
      "dataType": "stringValue",
      "valuesOptions": {
        "values": [
          "testToYaml=true",
          "testDataType=stringValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-type-conversion-test\r\ndata:\r\n  # ==========================================\r\n  # String conversion functions\r\n  # ==========================================\r\n  # Test: toStrings\r\n\r\n  # ==========================================\r\n  # Decimal conversion functions\r\n  # ==========================================\r\n  # Test: toDecimal\r\n\r\n  # ==========================================\r\n  # JSON conversion functions\r\n  # ==========================================\r\n  # Test: toJson\r\n  # Test: mustToJson\r\n  # Test: toPrettyJson\r\n  # Test: mustToPrettyJson\r\n  # Test: toRawJson\r\n  # Test: mustToRawJson\r\n  # Test: fromJson\r\n  # Test: fromJsonArray\r\n\r\n  # ==========================================\r\n  # YAML conversion functions\r\n  # ==========================================\r\n  # Test: fromYaml\r\n  # Test: toYaml\r\n  toYamlValue: \"test string\"\r\n  # Test: toYamlPretty\r\n  # Test: fromYamlArray\r\n\r\n  # ==========================================\r\n  # TOML conversion functions\r\n  # ==========================================\r\n  # Test: toToml\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-type-conversion/templates/type-conversion-test.yaml",
            "content": "\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  # Test: mustToJson\n  # Test: toPrettyJson\n  # Test: mustToPrettyJson\n  # Test: toRawJson\n  # Test: mustToRawJson\n  # Test: fromJson\n  # Test: fromJsonArray\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  # Test: toYaml\n  toYamlValue: \"test string\"\n  # Test: toYamlPretty\n  # Test: fromYamlArray\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n",
            "originalContent": "# Test: Type conversion errors - comprehensive testing of all type conversion functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n  {{- if .Values.testToStrings }}\n  toStringsValue: {{ toStrings $testData | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n  {{- if .Values.testToDecimal }}\n  toDecimalValue: {{ toDecimal $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  {{- if .Values.testToJson }}\n  toJsonValue: {{ toJson $testData | quote }}\n  {{- end }}\n  # Test: mustToJson\n  {{- if .Values.testMustToJson }}\n  mustToJsonValue: {{ mustToJson $testData | quote }}\n  {{- end }}\n  # Test: toPrettyJson\n  {{- if .Values.testToPrettyJson }}\n  toPrettyJsonValue: {{ toPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: mustToPrettyJson\n  {{- if .Values.testMustToPrettyJson }}\n  mustToPrettyJsonValue: {{ mustToPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: toRawJson\n  {{- if .Values.testToRawJson }}\n  toRawJsonValue: {{ toRawJson $testData | quote }}\n  {{- end }}\n  # Test: mustToRawJson\n  {{- if .Values.testMustToRawJson }}\n  mustToRawJsonValue: {{ mustToRawJson $testData | quote }}\n  {{- end }}\n  # Test: fromJson\n  {{- if .Values.testFromJson }}\n  {{- $parsed := $testData | fromJson }}\n  fromJsonValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: fromJsonArray\n  {{- if .Values.testFromJsonArray }}\n  {{- $arr := $testData | fromJsonArray }}\n  fromJsonArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  {{- if .Values.testFromYaml }}\n  {{- $parsed := $testData | fromYaml }}\n  fromYamlValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: toYaml\n  {{- if .Values.testToYaml }}\n  toYamlValue: {{ toYaml $testData | quote }}\n  {{- end }}\n  # Test: toYamlPretty\n  {{- if .Values.testToYamlPretty }}\n  toYamlPrettyValue: {{ toYamlPretty $testData | quote }}\n  {{- end }}\n  # Test: fromYamlArray\n  {{- if .Values.testFromYamlArray }}\n  {{- $arr := $testData | fromYamlArray }}\n  fromYamlArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n  {{- if .Values.testToToml }}\n  toTomlValue: {{ toToml $testData | quote }}\n  {{- end }}\n\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-type-conversion-test\r\ndata:\r\n  # ==========================================\r\n  # String conversion functions\r\n  # ==========================================\r\n  # Test: toStrings\r\n\r\n  # ==========================================\r\n  # Decimal conversion functions\r\n  # ==========================================\r\n  # Test: toDecimal\r\n\r\n  # ==========================================\r\n  # JSON conversion functions\r\n  # ==========================================\r\n  # Test: toJson\r\n  # Test: mustToJson\r\n  # Test: toPrettyJson\r\n  # Test: mustToPrettyJson\r\n  # Test: toRawJson\r\n  # Test: mustToRawJson\r\n  # Test: fromJson\r\n  # Test: fromJsonArray\r\n\r\n  # ==========================================\r\n  # YAML conversion functions\r\n  # ==========================================\r\n  # Test: fromYaml\r\n  # Test: toYaml\r\n  toYamlValue: \"test string\"\r\n  # Test: toYamlPretty\r\n  # Test: fromYamlArray\r\n\r\n  # ==========================================\r\n  # TOML conversion functions\r\n  # ==========================================\r\n  # Test: toToml\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-type-conversion/templates/type-conversion-test.yaml",
            "content": "\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  # Test: mustToJson\n  # Test: toPrettyJson\n  # Test: mustToPrettyJson\n  # Test: toRawJson\n  # Test: mustToRawJson\n  # Test: fromJson\n  # Test: fromJsonArray\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  # Test: toYaml\n  toYamlValue: \"test string\"\n  # Test: toYamlPretty\n  # Test: fromYamlArray\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n",
            "originalContent": "# Test: Type conversion errors - comprehensive testing of all type conversion functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n  {{- if .Values.testToStrings }}\n  toStringsValue: {{ toStrings $testData | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n  {{- if .Values.testToDecimal }}\n  toDecimalValue: {{ toDecimal $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  {{- if .Values.testToJson }}\n  toJsonValue: {{ toJson $testData | quote }}\n  {{- end }}\n  # Test: mustToJson\n  {{- if .Values.testMustToJson }}\n  mustToJsonValue: {{ mustToJson $testData | quote }}\n  {{- end }}\n  # Test: toPrettyJson\n  {{- if .Values.testToPrettyJson }}\n  toPrettyJsonValue: {{ toPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: mustToPrettyJson\n  {{- if .Values.testMustToPrettyJson }}\n  mustToPrettyJsonValue: {{ mustToPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: toRawJson\n  {{- if .Values.testToRawJson }}\n  toRawJsonValue: {{ toRawJson $testData | quote }}\n  {{- end }}\n  # Test: mustToRawJson\n  {{- if .Values.testMustToRawJson }}\n  mustToRawJsonValue: {{ mustToRawJson $testData | quote }}\n  {{- end }}\n  # Test: fromJson\n  {{- if .Values.testFromJson }}\n  {{- $parsed := $testData | fromJson }}\n  fromJsonValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: fromJsonArray\n  {{- if .Values.testFromJsonArray }}\n  {{- $arr := $testData | fromJsonArray }}\n  fromJsonArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  {{- if .Values.testFromYaml }}\n  {{- $parsed := $testData | fromYaml }}\n  fromYamlValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: toYaml\n  {{- if .Values.testToYaml }}\n  toYamlValue: {{ toYaml $testData | quote }}\n  {{- end }}\n  # Test: toYamlPretty\n  {{- if .Values.testToYamlPretty }}\n  toYamlPrettyValue: {{ toYamlPretty $testData | quote }}\n  {{- end }}\n  # Test: fromYamlArray\n  {{- if .Values.testFromYamlArray }}\n  {{- $arr := $testData | fromYamlArray }}\n  fromYamlArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n  {{- if .Values.testToToml }}\n  toTomlValue: {{ toToml $testData | quote }}\n  {{- end }}\n\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "resolved": true,
      "stopReason": "",
      "fixChain": []
    },
    {
      "toggle": "toYaml",
      "dataType": "numberValue",
      "valuesOptions": {
        "values": [
          "testToYaml=true",
          "testDataType=numberValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-type-conversion-test\r\ndata:\r\n  # ==========================================\r\n  # String conversion functions\r\n  # ==========================================\r\n  # Test: toStrings\r\n\r\n  # ==========================================\r\n  # Decimal conversion functions\r\n  # ==========================================\r\n  # Test: toDecimal\r\n\r\n  # ==========================================\r\n  # JSON conversion functions\r\n  # ==========================================\r\n  # Test: toJson\r\n  # Test: mustToJson\r\n  # Test: toPrettyJson\r\n  # Test: mustToPrettyJson\r\n  # Test: toRawJson\r\n  # Test: mustToRawJson\r\n  # Test: fromJson\r\n  # Test: fromJsonArray\r\n\r\n  # ==========================================\r\n  # YAML conversion functions\r\n  # ==========================================\r\n  # Test: fromYaml\r\n  # Test: toYaml\r\n  toYamlValue: \"42\"\r\n  # Test: toYamlPretty\r\n  # Test: fromYamlArray\r\n\r\n  # ==========================================\r\n  # TOML conversion functions\r\n  # ==========================================\r\n  # Test: toToml\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-type-conversion/templates/type-conversion-test.yaml",
            "content": "\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  # Test: mustToJson\n  # Test: toPrettyJson\n  # Test: mustToPrettyJson\n  # Test: toRawJson\n  # Test: mustToRawJson\n  # Test: fromJson\n  # Test: fromJsonArray\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  # Test: toYaml\n  toYamlValue: \"42\"\n  # Test: toYamlPretty\n  # Test: fromYamlArray\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n",
            "originalContent": "# Test: Type conversion errors - comprehensive testing of all type conversion functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n  {{- if .Values.testToStrings }}\n  toStringsValue: {{ toStrings $testData | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n  {{- if .Values.testToDecimal }}\n  toDecimalValue: {{ toDecimal $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  {{- if .Values.testToJson }}\n  toJsonValue: {{ toJson $testData | quote }}\n  {{- end }}\n  # Test: mustToJson\n  {{- if .Values.testMustToJson }}\n  mustToJsonValue: {{ mustToJson $testData | quote }}\n  {{- end }}\n  # Test: toPrettyJson\n  {{- if .Values.testToPrettyJson }}\n  toPrettyJsonValue: {{ toPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: mustToPrettyJson\n  {{- if .Values.testMustToPrettyJson }}\n  mustToPrettyJsonValue: {{ mustToPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: toRawJson\n  {{- if .Values.testToRawJson }}\n  toRawJsonValue: {{ toRawJson $testData | quote }}\n  {{- end }}\n  # Test: mustToRawJson\n  {{- if .Values.testMustToRawJson }}\n  mustToRawJsonValue: {{ mustToRawJson $testData | quote }}\n  {{- end }}\n  # Test: fromJson\n  {{- if .Values.testFromJson }}\n  {{- $parsed := $testData | fromJson }}\n  fromJsonValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: fromJsonArray\n  {{- if .Values.testFromJsonArray }}\n  {{- $arr := $testData | fromJsonArray }}\n  fromJsonArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  {{- if .Values.testFromYaml }}\n  {{- $parsed := $testData | fromYaml }}\n  fromYamlValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: toYaml\n  {{- if .Values.testToYaml }}\n  toYamlValue: {{ toYaml $testData | quote }}\n  {{- end }}\n  # Test: toYamlPretty\n  {{- if .Values.testToYamlPretty }}\n  toYamlPrettyValue: {{ toYamlPretty $testData | quote }}\n  {{- end }}\n  # Test: fromYamlArray\n  {{- if .Values.testFromYamlArray }}\n  {{- $arr := $testData | fromYamlArray }}\n  fromYamlArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n  {{- if .Values.testToToml }}\n  toTomlValue: {{ toToml $testData | quote }}\n  {{- end }}\n\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-type-conversion-test\r\ndata:\r\n  # ==========================================\r\n  # String conversion functions\r\n  # ==========================================\r\n  # Test: toStrings\r\n\r\n  # ==========================================\r\n  # Decimal conversion functions\r\n  # ==========================================\r\n  # Test: toDecimal\r\n\r\n  # ==========================================\r\n  # JSON conversion functions\r\n  # ==========================================\r\n  # Test: toJson\r\n  # Test: mustToJson\r\n  # Test: toPrettyJson\r\n  # Test: mustToPrettyJson\r\n  # Test: toRawJson\r\n  # Test: mustToRawJson\r\n  # Test: fromJson\r\n  # Test: fromJsonArray\r\n\r\n  # ==========================================\r\n  # YAML conversion functions\r\n  # ==========================================\r\n  # Test: fromYaml\r\n  # Test: toYaml\r\n  toYamlValue: \"42\"\r\n  # Test: toYamlPretty\r\n  # Test: fromYamlArray\r\n\r\n  # ==========================================\r\n  # TOML conversion functions\r\n  # ==========================================\r\n  # Test: toToml\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-type-conversion/templates/type-conversion-test.yaml",
            "content": "\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  # Test: mustToJson\n  # Test: toPrettyJson\n  # Test: mustToPrettyJson\n  # Test: toRawJson\n  # Test: mustToRawJson\n  # Test: fromJson\n  # Test: fromJsonArray\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  # Test: toYaml\n  toYamlValue: \"42\"\n  # Test: toYamlPretty\n  # Test: fromYamlArray\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n",
            "originalContent": "# Test: Type conversion errors - comprehensive testing of all type conversion functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n  {{- if .Values.testToStrings }}\n  toStringsValue: {{ toStrings $testData | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n  {{- if .Values.testToDecimal }}\n  toDecimalValue: {{ toDecimal $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  {{- if .Values.testToJson }}\n  toJsonValue: {{ toJson $testData | quote }}\n  {{- end }}\n  # Test: mustToJson\n  {{- if .Values.testMustToJson }}\n  mustToJsonValue: {{ mustToJson $testData | quote }}\n  {{- end }}\n  # Test: toPrettyJson\n  {{- if .Values.testToPrettyJson }}\n  toPrettyJsonValue: {{ toPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: mustToPrettyJson\n  {{- if .Values.testMustToPrettyJson }}\n  mustToPrettyJsonValue: {{ mustToPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: toRawJson\n  {{- if .Values.testToRawJson }}\n  toRawJsonValue: {{ toRawJson $testData | quote }}\n  {{- end }}\n  # Test: mustToRawJson\n  {{- if .Values.testMustToRawJson }}\n  mustToRawJsonValue: {{ mustToRawJson $testData | quote }}\n  {{- end }}\n  # Test: fromJson\n  {{- if .Values.testFromJson }}\n  {{- $parsed := $testData | fromJson }}\n  fromJsonValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: fromJsonArray\n  {{- if .Values.testFromJsonArray }}\n  {{- $arr := $testData | fromJsonArray }}\n  fromJsonArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  {{- if .Values.testFromYaml }}\n  {{- $parsed := $testData | fromYaml }}\n  fromYamlValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: toYaml\n  {{- if .Values.testToYaml }}\n  toYamlValue: {{ toYaml $testData | quote }}\n  {{- end }}\n  # Test: toYamlPretty\n  {{- if .Values.testToYamlPretty }}\n  toYamlPrettyValue: {{ toYamlPretty $testData | quote }}\n  {{- end }}\n  # Test: fromYamlArray\n  {{- if .Values.testFromYamlArray }}\n  {{- $arr := $testData | fromYamlArray }}\n  fromYamlArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n  {{- if .Values.testToToml }}\n  toTomlValue: {{ toToml $testData | quote }}\n  {{- end }}\n\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "resolved": true,
      "stopReason": "",
      "fixChain": []
    },
    {
      "toggle": "toYaml",
      "dataType": "boolValue",
      "valuesOptions": {
        "values": [
          "testToYaml=true",
          "testDataType=boolValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-type-conversion-test\r\ndata:\r\n  # ==========================================\r\n  # String conversion functions\r\n  # ==========================================\r\n  # Test: toStrings\r\n\r\n  # ==========================================\r\n  # Decimal conversion functions\r\n  # ==========================================\r\n  # Test: toDecimal\r\n\r\n  # ==========================================\r\n  # JSON conversion functions\r\n  # ==========================================\r\n  # Test: toJson\r\n  # Test: mustToJson\r\n  # Test: toPrettyJson\r\n  # Test: mustToPrettyJson\r\n  # Test: toRawJson\r\n  # Test: mustToRawJson\r\n  # Test: fromJson\r\n  # Test: fromJsonArray\r\n\r\n  # ==========================================\r\n  # YAML conversion functions\r\n  # ==========================================\r\n  # Test: fromYaml\r\n  # Test: toYaml\r\n  toYamlValue: \"true\"\r\n  # Test: toYamlPretty\r\n  # Test: fromYamlArray\r\n\r\n  # ==========================================\r\n  # TOML conversion functions\r\n  # ==========================================\r\n  # Test: toToml\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-type-conversion/templates/type-conversion-test.yaml",
            "content": "\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  # Test: mustToJson\n  # Test: toPrettyJson\n  # Test: mustToPrettyJson\n  # Test: toRawJson\n  # Test: mustToRawJson\n  # Test: fromJson\n  # Test: fromJsonArray\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  # Test: toYaml\n  toYamlValue: \"true\"\n  # Test: toYamlPretty\n  # Test: fromYamlArray\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n",
            "originalContent": "# Test: Type conversion errors - comprehensive testing of all type conversion functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n  {{- if .Values.testToStrings }}\n  toStringsValue: {{ toStrings $testData | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n  {{- if .Values.testToDecimal }}\n  toDecimalValue: {{ toDecimal $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  {{- if .Values.testToJson }}\n  toJsonValue: {{ toJson $testData | quote }}\n  {{- end }}\n  # Test: mustToJson\n  {{- if .Values.testMustToJson }}\n  mustToJsonValue: {{ mustToJson $testData | quote }}\n  {{- end }}\n  # Test: toPrettyJson\n  {{- if .Values.testToPrettyJson }}\n  toPrettyJsonValue: {{ toPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: mustToPrettyJson\n  {{- if .Values.testMustToPrettyJson }}\n  mustToPrettyJsonValue: {{ mustToPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: toRawJson\n  {{- if .Values.testToRawJson }}\n  toRawJsonValue: {{ toRawJson $testData | quote }}\n  {{- end }}\n  # Test: mustToRawJson\n  {{- if .Values.testMustToRawJson }}\n  mustToRawJsonValue: {{ mustToRawJson $testData | quote }}\n  {{- end }}\n  # Test: fromJson\n  {{- if .Values.testFromJson }}\n  {{- $parsed := $testData | fromJson }}\n  fromJsonValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: fromJsonArray\n  {{- if .Values.testFromJsonArray }}\n  {{- $arr := $testData | fromJsonArray }}\n  fromJsonArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  {{- if .Values.testFromYaml }}\n  {{- $parsed := $testData | fromYaml }}\n  fromYamlValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: toYaml\n  {{- if .Values.testToYaml }}\n  toYamlValue: {{ toYaml $testData | quote }}\n  {{- end }}\n  # Test: toYamlPretty\n  {{- if .Values.testToYamlPretty }}\n  toYamlPrettyValue: {{ toYamlPretty $testData | quote }}\n  {{- end }}\n  # Test: fromYamlArray\n  {{- if .Values.testFromYamlArray }}\n  {{- $arr := $testData | fromYamlArray }}\n  fromYamlArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n  {{- if .Values.testToToml }}\n  toTomlValue: {{ toToml $testData | quote }}\n  {{- end }}\n\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-type-conversion-test\r\ndata:\r\n  # ==========================================\r\n  # String conversion functions\r\n  # ==========================================\r\n  # Test: toStrings\r\n\r\n  # ==========================================\r\n  # Decimal conversion functions\r\n  # ==========================================\r\n  # Test: toDecimal\r\n\r\n  # ==========================================\r\n  # JSON conversion functions\r\n  # ==========================================\r\n  # Test: toJson\r\n  # Test: mustToJson\r\n  # Test: toPrettyJson\r\n  # Test: mustToPrettyJson\r\n  # Test: toRawJson\r\n  # Test: mustToRawJson\r\n  # Test: fromJson\r\n  # Test: fromJsonArray\r\n\r\n  # ==========================================\r\n  # YAML conversion functions\r\n  # ==========================================\r\n  # Test: fromYaml\r\n  # Test: toYaml\r\n  toYamlValue: \"true\"\r\n  # Test: toYamlPretty\r\n  # Test: fromYamlArray\r\n\r\n  # ==========================================\r\n  # TOML conversion functions\r\n  # ==========================================\r\n  # Test: toToml\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-type-conversion/templates/type-conversion-test.yaml",
            "content": "\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  # Test: mustToJson\n  # Test: toPrettyJson\n  # Test: mustToPrettyJson\n  # Test: toRawJson\n  # Test: mustToRawJson\n  # Test: fromJson\n  # Test: fromJsonArray\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  # Test: toYaml\n  toYamlValue: \"true\"\n  # Test: toYamlPretty\n  # Test: fromYamlArray\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n",
            "originalContent": "# Test: Type conversion errors - comprehensive testing of all type conversion functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n  {{- if .Values.testToStrings }}\n  toStringsValue: {{ toStrings $testData | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n  {{- if .Values.testToDecimal }}\n  toDecimalValue: {{ toDecimal $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  {{- if .Values.testToJson }}\n  toJsonValue: {{ toJson $testData | quote }}\n  {{- end }}\n  # Test: mustToJson\n  {{- if .Values.testMustToJson }}\n  mustToJsonValue: {{ mustToJson $testData | quote }}\n  {{- end }}\n  # Test: toPrettyJson\n  {{- if .Values.testToPrettyJson }}\n  toPrettyJsonValue: {{ toPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: mustToPrettyJson\n  {{- if .Values.testMustToPrettyJson }}\n  mustToPrettyJsonValue: {{ mustToPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: toRawJson\n  {{- if .Values.testToRawJson }}\n  toRawJsonValue: {{ toRawJson $testData | quote }}\n  {{- end }}\n  # Test: mustToRawJson\n  {{- if .Values.testMustToRawJson }}\n  mustToRawJsonValue: {{ mustToRawJson $testData | quote }}\n  {{- end }}\n  # Test: fromJson\n  {{- if .Values.testFromJson }}\n  {{- $parsed := $testData | fromJson }}\n  fromJsonValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: fromJsonArray\n  {{- if .Values.testFromJsonArray }}\n  {{- $arr := $testData | fromJsonArray }}\n  fromJsonArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  {{- if .Values.testFromYaml }}\n  {{- $parsed := $testData | fromYaml }}\n  fromYamlValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: toYaml\n  {{- if .Values.testToYaml }}\n  toYamlValue: {{ toYaml $testData | quote }}\n  {{- end }}\n  # Test: toYamlPretty\n  {{- if .Values.testToYamlPretty }}\n  toYamlPrettyValue: {{ toYamlPretty $testData | quote }}\n  {{- end }}\n  # Test: fromYamlArray\n  {{- if .Values.testFromYamlArray }}\n  {{- $arr := $testData | fromYamlArray }}\n  fromYamlArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n  {{- if .Values.testToToml }}\n  toTomlValue: {{ toToml $testData | quote }}\n  {{- end }}\n\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "resolved": true,
      "stopReason": "",
      "fixChain": []
    },
    {
      "toggle": "toYaml",
      "dataType": "listValue",
      "valuesOptions": {
        "values": [
          "testToYaml=true",
          "testDataType=listValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-type-conversion-test\r\ndata:\r\n  # ==========================================\r\n  # String conversion functions\r\n  # ==========================================\r\n  # Test: toStrings\r\n\r\n  # ==========================================\r\n  # Decimal conversion functions\r\n  # ==========================================\r\n  # Test: toDecimal\r\n\r\n  # ==========================================\r\n  # JSON conversion functions\r\n  # ==========================================\r\n  # Test: toJson\r\n  # Test: mustToJson\r\n  # Test: toPrettyJson\r\n  # Test: mustToPrettyJson\r\n  # Test: toRawJson\r\n  # Test: mustToRawJson\r\n  # Test: fromJson\r\n  # Test: fromJsonArray\r\n\r\n  # ==========================================\r\n  # YAML conversion functions\r\n  # ==========================================\r\n  # Test: fromYaml\r\n  # Test: toYaml\r\n  toYamlValue: \"- 1\\n- 2\\n- 3\"\r\n  # Test: toYamlPretty\r\n  # Test: fromYamlArray\r\n\r\n  # ==========================================\r\n  # TOML conversion functions\r\n  # ==========================================\r\n  # Test: toToml\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-type-conversion/templates/type-conversion-test.yaml",
            "content": "\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  # Test: mustToJson\n  # Test: toPrettyJson\n  # Test: mustToPrettyJson\n  # Test: toRawJson\n  # Test: mustToRawJson\n  # Test: fromJson\n  # Test: fromJsonArray\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  # Test: toYaml\n  toYamlValue: \"- 1\\n- 2\\n- 3\"\n  # Test: toYamlPretty\n  # Test: fromYamlArray\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n",
            "originalContent": "# Test: Type conversion errors - comprehensive testing of all type conversion functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n  {{- if .Values.testToStrings }}\n  toStringsValue: {{ toStrings $testData | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n  {{- if .Values.testToDecimal }}\n  toDecimalValue: {{ toDecimal $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  {{- if .Values.testToJson }}\n  toJsonValue: {{ toJson $testData | quote }}\n  {{- end }}\n  # Test: mustToJson\n  {{- if .Values.testMustToJson }}\n  mustToJsonValue: {{ mustToJson $testData | quote }}\n  {{- end }}\n  # Test: toPrettyJson\n  {{- if .Values.testToPrettyJson }}\n  toPrettyJsonValue: {{ toPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: mustToPrettyJson\n  {{- if .Values.testMustToPrettyJson }}\n  mustToPrettyJsonValue: {{ mustToPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: toRawJson\n  {{- if .Values.testToRawJson }}\n  toRawJsonValue: {{ toRawJson $testData | quote }}\n  {{- end }}\n  # Test: mustToRawJson\n  {{- if .Values.testMustToRawJson }}\n  mustToRawJsonValue: {{ mustToRawJson $testData | quote }}\n  {{- end }}\n  # Test: fromJson\n  {{- if .Values.testFromJson }}\n  {{- $parsed := $testData | fromJson }}\n  fromJsonValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: fromJsonArray\n  {{- if .Values.testFromJsonArray }}\n  {{- $arr := $testData | fromJsonArray }}\n  fromJsonArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  {{- if .Values.testFromYaml }}\n  {{- $parsed := $testData | fromYaml }}\n  fromYamlValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: toYaml\n  {{- if .Values.testToYaml }}\n  toYamlValue: {{ toYaml $testData | quote }}\n  {{- end }}\n  # Test: toYamlPretty\n  {{- if .Values.testToYamlPretty }}\n  toYamlPrettyValue: {{ toYamlPretty $testData | quote }}\n  {{- end }}\n  # Test: fromYamlArray\n  {{- if .Values.testFromYamlArray }}\n  {{- $arr := $testData | fromYamlArray }}\n  fromYamlArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n  {{- if .Values.testToToml }}\n  toTomlValue: {{ toToml $testData | quote }}\n  {{- end }}\n\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-type-conversion-test\r\ndata:\r\n  # ==========================================\r\n  # String conversion functions\r\n  # ==========================================\r\n  # Test: toStrings\r\n\r\n  # ==========================================\r\n  # Decimal conversion functions\r\n  # ==========================================\r\n  # Test: toDecimal\r\n\r\n  # ==========================================\r\n  # JSON conversion functions\r\n  # ==========================================\r\n  # Test: toJson\r\n  # Test: mustToJson\r\n  # Test: toPrettyJson\r\n  # Test: mustToPrettyJson\r\n  # Test: toRawJson\r\n  # Test: mustToRawJson\r\n  # Test: fromJson\r\n  # Test: fromJsonArray\r\n\r\n  # ==========================================\r\n  # YAML conversion functions\r\n  # ==========================================\r\n  # Test: fromYaml\r\n  # Test: toYaml\r\n  toYamlValue: \"- 1\\n- 2\\n- 3\"\r\n  # Test: toYamlPretty\r\n  # Test: fromYamlArray\r\n\r\n  # ==========================================\r\n  # TOML conversion functions\r\n  # ==========================================\r\n  # Test: toToml\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-type-conversion/templates/type-conversion-test.yaml",
            "content": "\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  # Test: mustToJson\n  # Test: toPrettyJson\n  # Test: mustToPrettyJson\n  # Test: toRawJson\n  # Test: mustToRawJson\n  # Test: fromJson\n  # Test: fromJsonArray\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  # Test: toYaml\n  toYamlValue: \"- 1\\n- 2\\n- 3\"\n  # Test: toYamlPretty\n  # Test: fromYamlArray\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n",
            "originalContent": "# Test: Type conversion errors - comprehensive testing of all type conversion functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n  {{- if .Values.testToStrings }}\n  toStringsValue: {{ toStrings $testData | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n  {{- if .Values.testToDecimal }}\n  toDecimalValue: {{ toDecimal $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  {{- if .Values.testToJson }}\n  toJsonValue: {{ toJson $testData | quote }}\n  {{- end }}\n  # Test: mustToJson\n  {{- if .Values.testMustToJson }}\n  mustToJsonValue: {{ mustToJson $testData | quote }}\n  {{- end }}\n  # Test: toPrettyJson\n  {{- if .Values.testToPrettyJson }}\n  toPrettyJsonValue: {{ toPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: mustToPrettyJson\n  {{- if .Values.testMustToPrettyJson }}\n  mustToPrettyJsonValue: {{ mustToPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: toRawJson\n  {{- if .Values.testToRawJson }}\n  toRawJsonValue: {{ toRawJson $testData | quote }}\n  {{- end }}\n  # Test: mustToRawJson\n  {{- if .Values.testMustToRawJson }}\n  mustToRawJsonValue: {{ mustToRawJson $testData | quote }}\n  {{- end }}\n  # Test: fromJson\n  {{- if .Values.testFromJson }}\n  {{- $parsed := $testData | fromJson }}\n  fromJsonValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: fromJsonArray\n  {{- if .Values.testFromJsonArray }}\n  {{- $arr := $testData | fromJsonArray }}\n  fromJsonArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  {{- if .Values.testFromYaml }}\n  {{- $parsed := $testData | fromYaml }}\n  fromYamlValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: toYaml\n  {{- if .Values.testToYaml }}\n  toYamlValue: {{ toYaml $testData | quote }}\n  {{- end }}\n  # Test: toYamlPretty\n  {{- if .Values.testToYamlPretty }}\n  toYamlPrettyValue: {{ toYamlPretty $testData | quote }}\n  {{- end }}\n  # Test: fromYamlArray\n  {{- if .Values.testFromYamlArray }}\n  {{- $arr := $testData | fromYamlArray }}\n  fromYamlArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n  {{- if .Values.testToToml }}\n  toTomlValue: {{ toToml $testData | quote }}\n  {{- end }}\n\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "resolved": true,
      "stopReason": "",
      "fixChain": []
    },
    {
      "toggle": "toYaml",
      "dataType": "dictValue",
      "valuesOptions": {
        "values": [
          "testToYaml=true",
          "testDataType=dictValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-type-conversion-test\r\ndata:\r\n  # ==========================================\r\n  # String conversion functions\r\n  # ==========================================\r\n  # Test: toStrings\r\n\r\n  # ==========================================\r\n  # Decimal conversion functions\r\n  # ==========================================\r\n  # Test: toDecimal\r\n\r\n  # ==========================================\r\n  # JSON conversion functions\r\n  # ==========================================\r\n  # Test: toJson\r\n  # Test: mustToJson\r\n  # Test: toPrettyJson\r\n  # Test: mustToPrettyJson\r\n  # Test: toRawJson\r\n  # Test: mustToRawJson\r\n  # Test: fromJson\r\n  # Test: fromJsonArray\r\n\r\n  # ==========================================\r\n  # YAML conversion functions\r\n  # ==========================================\r\n  # Test: fromYaml\r\n  # Test: toYaml\r\n  toYamlValue: \"key: value\"\r\n  # Test: toYamlPretty\r\n  # Test: fromYamlArray\r\n\r\n  # ==========================================\r\n  # TOML conversion functions\r\n  # ==========================================\r\n  # Test: toToml\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-type-conversion/templates/type-conversion-test.yaml",
            "content": "\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  # Test: mustToJson\n  # Test: toPrettyJson\n  # Test: mustToPrettyJson\n  # Test: toRawJson\n  # Test: mustToRawJson\n  # Test: fromJson\n  # Test: fromJsonArray\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  # Test: toYaml\n  toYamlValue: \"key: value\"\n  # Test: toYamlPretty\n  # Test: fromYamlArray\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n",
            "originalContent": "# Test: Type conversion errors - comprehensive testing of all type conversion functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n  {{- if .Values.testToStrings }}\n  toStringsValue: {{ toStrings $testData | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n  {{- if .Values.testToDecimal }}\n  toDecimalValue: {{ toDecimal $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  {{- if .Values.testToJson }}\n  toJsonValue: {{ toJson $testData | quote }}\n  {{- end }}\n  # Test: mustToJson\n  {{- if .Values.testMustToJson }}\n  mustToJsonValue: {{ mustToJson $testData | quote }}\n  {{- end }}\n  # Test: toPrettyJson\n  {{- if .Values.testToPrettyJson }}\n  toPrettyJsonValue: {{ toPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: mustToPrettyJson\n  {{- if .Values.testMustToPrettyJson }}\n  mustToPrettyJsonValue: {{ mustToPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: toRawJson\n  {{- if .Values.testToRawJson }}\n  toRawJsonValue: {{ toRawJson $testData | quote }}\n  {{- end }}\n  # Test: mustToRawJson\n  {{- if .Values.testMustToRawJson }}\n  mustToRawJsonValue: {{ mustToRawJson $testData | quote }}\n  {{- end }}\n  # Test: fromJson\n  {{- if .Values.testFromJson }}\n  {{- $parsed := $testData | fromJson }}\n  fromJsonValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: fromJsonArray\n  {{- if .Values.testFromJsonArray }}\n  {{- $arr := $testData | fromJsonArray }}\n  fromJsonArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  {{- if .Values.testFromYaml }}\n  {{- $parsed := $testData | fromYaml }}\n  fromYamlValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: toYaml\n  {{- if .Values.testToYaml }}\n  toYamlValue: {{ toYaml $testData | quote }}\n  {{- end }}\n  # Test: toYamlPretty\n  {{- if .Values.testToYamlPretty }}\n  toYamlPrettyValue: {{ toYamlPretty $testData | quote }}\n  {{- end }}\n  # Test: fromYamlArray\n  {{- if .Values.testFromYamlArray }}\n  {{- $arr := $testData | fromYamlArray }}\n  fromYamlArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n  {{- if .Values.testToToml }}\n  toTomlValue: {{ toToml $testData | quote }}\n  {{- end }}\n\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-type-conversion-test\r\ndata:\r\n  # ==========================================\r\n  # String conversion functions\r\n  # ==========================================\r\n  # Test: toStrings\r\n\r\n  # ==========================================\r\n  # Decimal conversion functions\r\n  # ==========================================\r\n  # Test: toDecimal\r\n\r\n  # ==========================================\r\n  # JSON conversion functions\r\n  # ==========================================\r\n  # Test: toJson\r\n  # Test: mustToJson\r\n  # Test: toPrettyJson\r\n  # Test: mustToPrettyJson\r\n  # Test: toRawJson\r\n  # Test: mustToRawJson\r\n  # Test: fromJson\r\n  # Test: fromJsonArray\r\n\r\n  # ==========================================\r\n  # YAML conversion functions\r\n  # ==========================================\r\n  # Test: fromYaml\r\n  # Test: toYaml\r\n  toYamlValue: \"key: value\"\r\n  # Test: toYamlPretty\r\n  # Test: fromYamlArray\r\n\r\n  # ==========================================\r\n  # TOML conversion functions\r\n  # ==========================================\r\n  # Test: toToml\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-type-conversion/templates/type-conversion-test.yaml",
            "content": "\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  # Test: mustToJson\n  # Test: toPrettyJson\n  # Test: mustToPrettyJson\n  # Test: toRawJson\n  # Test: mustToRawJson\n  # Test: fromJson\n  # Test: fromJsonArray\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  # Test: toYaml\n  toYamlValue: \"key: value\"\n  # Test: toYamlPretty\n  # Test: fromYamlArray\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n",
            "originalContent": "# Test: Type conversion errors - comprehensive testing of all type conversion functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n  {{- if .Values.testToStrings }}\n  toStringsValue: {{ toStrings $testData | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n  {{- if .Values.testToDecimal }}\n  toDecimalValue: {{ toDecimal $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  {{- if .Values.testToJson }}\n  toJsonValue: {{ toJson $testData | quote }}\n  {{- end }}\n  # Test: mustToJson\n  {{- if .Values.testMustToJson }}\n  mustToJsonValue: {{ mustToJson $testData | quote }}\n  {{- end }}\n  # Test: toPrettyJson\n  {{- if .Values.testToPrettyJson }}\n  toPrettyJsonValue: {{ toPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: mustToPrettyJson\n  {{- if .Values.testMustToPrettyJson }}\n  mustToPrettyJsonValue: {{ mustToPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: toRawJson\n  {{- if .Values.testToRawJson }}\n  toRawJsonValue: {{ toRawJson $testData | quote }}\n  {{- end }}\n  # Test: mustToRawJson\n  {{- if .Values.testMustToRawJson }}\n  mustToRawJsonValue: {{ mustToRawJson $testData | quote }}\n  {{- end }}\n  # Test: fromJson\n  {{- if .Values.testFromJson }}\n  {{- $parsed := $testData | fromJson }}\n  fromJsonValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: fromJsonArray\n  {{- if .Values.testFromJsonArray }}\n  {{- $arr := $testData | fromJsonArray }}\n  fromJsonArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  {{- if .Values.testFromYaml }}\n  {{- $parsed := $testData | fromYaml }}\n  fromYamlValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: toYaml\n  {{- if .Values.testToYaml }}\n  toYamlValue: {{ toYaml $testData | quote }}\n  {{- end }}\n  # Test: toYamlPretty\n  {{- if .Values.testToYamlPretty }}\n  toYamlPrettyValue: {{ toYamlPretty $testData | quote }}\n  {{- end }}\n  # Test: fromYamlArray\n  {{- if .Values.testFromYamlArray }}\n  {{- $arr := $testData | fromYamlArray }}\n  fromYamlArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n  {{- if .Values.testToToml }}\n  toTomlValue: {{ toToml $testData | quote }}\n  {{- end }}\n\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "resolved": true,
      "stopReason": "",
      "fixChain": []
    },
    {
      "toggle": "toYamlPretty",
      "dataType": "nilValue",
      "valuesOptions": {
        "values": [
          "testToYamlPretty=true",
          "testDataType=nilValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-type-conversion-test\r\ndata:\r\n  # ==========================================\r\n  # String conversion functions\r\n  # ==========================================\r\n  # Test: toStrings\r\n\r\n  # ==========================================\r\n  # Decimal conversion functions\r\n  # ==========================================\r\n  # Test: toDecimal\r\n\r\n  # ==========================================\r\n  # JSON conversion functions\r\n  # ==========================================\r\n  # Test: toJson\r\n  # Test: mustToJson\r\n  # Test: toPrettyJson\r\n  # Test: mustToPrettyJson\r\n  # Test: toRawJson\r\n  # Test: mustToRawJson\r\n  # Test: fromJson\r\n  # Test: fromJsonArray\r\n\r\n  # ==========================================\r\n  # YAML conversion functions\r\n  # ==========================================\r\n  # Test: fromYaml\r\n  # Test: toYaml\r\n  # Test: toYamlPretty\r\n  toYamlPrettyValue: \"null\"\r\n  # Test: fromYamlArray\r\n\r\n  # ==========================================\r\n  # TOML conversion functions\r\n  # ==========================================\r\n  # Test: toToml\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-type-conversion/templates/type-conversion-test.yaml",
            "content": "\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  # Test: mustToJson\n  # Test: toPrettyJson\n  # Test: mustToPrettyJson\n  # Test: toRawJson\n  # Test: mustToRawJson\n  # Test: fromJson\n  # Test: fromJsonArray\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  # Test: toYaml\n  # Test: toYamlPretty\n  toYamlPrettyValue: \"null\"\n  # Test: fromYamlArray\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n",
            "originalContent": "# Test: Type conversion errors - comprehensive testing of all type conversion functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n  {{- if .Values.testToStrings }}\n  toStringsValue: {{ toStrings $testData | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n  {{- if .Values.testToDecimal }}\n  toDecimalValue: {{ toDecimal $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  {{- if .Values.testToJson }}\n  toJsonValue: {{ toJson $testData | quote }}\n  {{- end }}\n  # Test: mustToJson\n  {{- if .Values.testMustToJson }}\n  mustToJsonValue: {{ mustToJson $testData | quote }}\n  {{- end }}\n  # Test: toPrettyJson\n  {{- if .Values.testToPrettyJson }}\n  toPrettyJsonValue: {{ toPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: mustToPrettyJson\n  {{- if .Values.testMustToPrettyJson }}\n  mustToPrettyJsonValue: {{ mustToPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: toRawJson\n  {{- if .Values.testToRawJson }}\n  toRawJsonValue: {{ toRawJson $testData | quote }}\n  {{- end }}\n  # Test: mustToRawJson\n  {{- if .Values.testMustToRawJson }}\n  mustToRawJsonValue: {{ mustToRawJson $testData | quote }}\n  {{- end }}\n  # Test: fromJson\n  {{- if .Values.testFromJson }}\n  {{- $parsed := $testData | fromJson }}\n  fromJsonValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: fromJsonArray\n  {{- if .Values.testFromJsonArray }}\n  {{- $arr := $testData | fromJsonArray }}\n  fromJsonArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  {{- if .Values.testFromYaml }}\n  {{- $parsed := $testData | fromYaml }}\n  fromYamlValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: toYaml\n  {{- if .Values.testToYaml }}\n  toYamlValue: {{ toYaml $testData | quote }}\n  {{- end }}\n  # Test: toYamlPretty\n  {{- if .Values.testToYamlPretty }}\n  toYamlPrettyValue: {{ toYamlPretty $testData | quote }}\n  {{- end }}\n  # Test: fromYamlArray\n  {{- if .Values.testFromYamlArray }}\n  {{- $arr := $testData | fromYamlArray }}\n  fromYamlArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n  {{- if .Values.testToToml }}\n  toTomlValue: {{ toToml $testData | quote }}\n  {{- end }}\n\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-type-conversion-test\r\ndata:\r\n  # ==========================================\r\n  # String conversion functions\r\n  # ==========================================\r\n  # Test: toStrings\r\n\r\n  # ==========================================\r\n  # Decimal conversion functions\r\n  # ==========================================\r\n  # Test: toDecimal\r\n\r\n  # ==========================================\r\n  # JSON conversion functions\r\n  # ==========================================\r\n  # Test: toJson\r\n  # Test: mustToJson\r\n  # Test: toPrettyJson\r\n  # Test: mustToPrettyJson\r\n  # Test: toRawJson\r\n  # Test: mustToRawJson\r\n  # Test: fromJson\r\n  # Test: fromJsonArray\r\n\r\n  # ==========================================\r\n  # YAML conversion functions\r\n  # ==========================================\r\n  # Test: fromYaml\r\n  # Test: toYaml\r\n  # Test: toYamlPretty\r\n  toYamlPrettyValue: \"null\"\r\n  # Test: fromYamlArray\r\n\r\n  # ==========================================\r\n  # TOML conversion functions\r\n  # ==========================================\r\n  # Test: toToml\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-type-conversion/templates/type-conversion-test.yaml",
            "content": "\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  # Test: mustToJson\n  # Test: toPrettyJson\n  # Test: mustToPrettyJson\n  # Test: toRawJson\n  # Test: mustToRawJson\n  # Test: fromJson\n  # Test: fromJsonArray\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  # Test: toYaml\n  # Test: toYamlPretty\n  toYamlPrettyValue: \"null\"\n  # Test: fromYamlArray\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n",
            "originalContent": "# Test: Type conversion errors - comprehensive testing of all type conversion functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n  {{- if .Values.testToStrings }}\n  toStringsValue: {{ toStrings $testData | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n  {{- if .Values.testToDecimal }}\n  toDecimalValue: {{ toDecimal $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  {{- if .Values.testToJson }}\n  toJsonValue: {{ toJson $testData | quote }}\n  {{- end }}\n  # Test: mustToJson\n  {{- if .Values.testMustToJson }}\n  mustToJsonValue: {{ mustToJson $testData | quote }}\n  {{- end }}\n  # Test: toPrettyJson\n  {{- if .Values.testToPrettyJson }}\n  toPrettyJsonValue: {{ toPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: mustToPrettyJson\n  {{- if .Values.testMustToPrettyJson }}\n  mustToPrettyJsonValue: {{ mustToPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: toRawJson\n  {{- if .Values.testToRawJson }}\n  toRawJsonValue: {{ toRawJson $testData | quote }}\n  {{- end }}\n  # Test: mustToRawJson\n  {{- if .Values.testMustToRawJson }}\n  mustToRawJsonValue: {{ mustToRawJson $testData | quote }}\n  {{- end }}\n  # Test: fromJson\n  {{- if .Values.testFromJson }}\n  {{- $parsed := $testData | fromJson }}\n  fromJsonValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: fromJsonArray\n  {{- if .Values.testFromJsonArray }}\n  {{- $arr := $testData | fromJsonArray }}\n  fromJsonArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  {{- if .Values.testFromYaml }}\n  {{- $parsed := $testData | fromYaml }}\n  fromYamlValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: toYaml\n  {{- if .Values.testToYaml }}\n  toYamlValue: {{ toYaml $testData | quote }}\n  {{- end }}\n  # Test: toYamlPretty\n  {{- if .Values.testToYamlPretty }}\n  toYamlPrettyValue: {{ toYamlPretty $testData | quote }}\n  {{- end }}\n  # Test: fromYamlArray\n  {{- if .Values.testFromYamlArray }}\n  {{- $arr := $testData | fromYamlArray }}\n  fromYamlArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n  {{- if .Values.testToToml }}\n  toTomlValue: {{ toToml $testData | quote }}\n  {{- end }}\n\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "resolved": true,
      "stopReason": "",
      "fixChain": []
    },
    {
      "toggle": "toYamlPretty",
      "dataType": "stringValue",
      "valuesOptions": {
        "values": [
          "testToYamlPretty=true",
          "testDataType=stringValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-type-conversion-test\r\ndata:\r\n  # ==========================================\r\n  # String conversion functions\r\n  # ==========================================\r\n  # Test: toStrings\r\n\r\n  # ==========================================\r\n  # Decimal conversion functions\r\n  # ==========================================\r\n  # Test: toDecimal\r\n\r\n  # ==========================================\r\n  # JSON conversion functions\r\n  # ==========================================\r\n  # Test: toJson\r\n  # Test: mustToJson\r\n  # Test: toPrettyJson\r\n  # Test: mustToPrettyJson\r\n  # Test: toRawJson\r\n  # Test: mustToRawJson\r\n  # Test: fromJson\r\n  # Test: fromJsonArray\r\n\r\n  # ==========================================\r\n  # YAML conversion functions\r\n  # ==========================================\r\n  # Test: fromYaml\r\n  # Test: toYaml\r\n  # Test: toYamlPretty\r\n  toYamlPrettyValue: \"test string\"\r\n  # Test: fromYamlArray\r\n\r\n  # ==========================================\r\n  # TOML conversion functions\r\n  # ==========================================\r\n  # Test: toToml\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-type-conversion/templates/type-conversion-test.yaml",
            "content": "\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  # Test: mustToJson\n  # Test: toPrettyJson\n  # Test: mustToPrettyJson\n  # Test: toRawJson\n  # Test: mustToRawJson\n  # Test: fromJson\n  # Test: fromJsonArray\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  # Test: toYaml\n  # Test: toYamlPretty\n  toYamlPrettyValue: \"test string\"\n  # Test: fromYamlArray\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n",
            "originalContent": "# Test: Type conversion errors - comprehensive testing of all type conversion functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n  {{- if .Values.testToStrings }}\n  toStringsValue: {{ toStrings $testData | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n  {{- if .Values.testToDecimal }}\n  toDecimalValue: {{ toDecimal $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  {{- if .Values.testToJson }}\n  toJsonValue: {{ toJson $testData | quote }}\n  {{- end }}\n  # Test: mustToJson\n  {{- if .Values.testMustToJson }}\n  mustToJsonValue: {{ mustToJson $testData | quote }}\n  {{- end }}\n  # Test: toPrettyJson\n  {{- if .Values.testToPrettyJson }}\n  toPrettyJsonValue: {{ toPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: mustToPrettyJson\n  {{- if .Values.testMustToPrettyJson }}\n  mustToPrettyJsonValue: {{ mustToPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: toRawJson\n  {{- if .Values.testToRawJson }}\n  toRawJsonValue: {{ toRawJson $testData | quote }}\n  {{- end }}\n  # Test: mustToRawJson\n  {{- if .Values.testMustToRawJson }}\n  mustToRawJsonValue: {{ mustToRawJson $testData | quote }}\n  {{- end }}\n  # Test: fromJson\n  {{- if .Values.testFromJson }}\n  {{- $parsed := $testData | fromJson }}\n  fromJsonValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: fromJsonArray\n  {{- if .Values.testFromJsonArray }}\n  {{- $arr := $testData | fromJsonArray }}\n  fromJsonArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  {{- if .Values.testFromYaml }}\n  {{- $parsed := $testData | fromYaml }}\n  fromYamlValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: toYaml\n  {{- if .Values.testToYaml }}\n  toYamlValue: {{ toYaml $testData | quote }}\n  {{- end }}\n  # Test: toYamlPretty\n  {{- if .Values.testToYamlPretty }}\n  toYamlPrettyValue: {{ toYamlPretty $testData | quote }}\n  {{- end }}\n  # Test: fromYamlArray\n  {{- if .Values.testFromYamlArray }}\n  {{- $arr := $testData | fromYamlArray }}\n  fromYamlArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n  {{- if .Values.testToToml }}\n  toTomlValue: {{ toToml $testData | quote }}\n  {{- end }}\n\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-type-conversion-test\r\ndata:\r\n  # ==========================================\r\n  # String conversion functions\r\n  # ==========================================\r\n  # Test: toStrings\r\n\r\n  # ==========================================\r\n  # Decimal conversion functions\r\n  # ==========================================\r\n  # Test: toDecimal\r\n\r\n  # ==========================================\r\n  # JSON conversion functions\r\n  # ==========================================\r\n  # Test: toJson\r\n  # Test: mustToJson\r\n  # Test: toPrettyJson\r\n  # Test: mustToPrettyJson\r\n  # Test: toRawJson\r\n  # Test: mustToRawJson\r\n  # Test: fromJson\r\n  # Test: fromJsonArray\r\n\r\n  # ==========================================\r\n  # YAML conversion functions\r\n  # ==========================================\r\n  # Test: fromYaml\r\n  # Test: toYaml\r\n  # Test: toYamlPretty\r\n  toYamlPrettyValue: \"test string\"\r\n  # Test: fromYamlArray\r\n\r\n  # ==========================================\r\n  # TOML conversion functions\r\n  # ==========================================\r\n  # Test: toToml\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-type-conversion/templates/type-conversion-test.yaml",
            "content": "\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  # Test: mustToJson\n  # Test: toPrettyJson\n  # Test: mustToPrettyJson\n  # Test: toRawJson\n  # Test: mustToRawJson\n  # Test: fromJson\n  # Test: fromJsonArray\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  # Test: toYaml\n  # Test: toYamlPretty\n  toYamlPrettyValue: \"test string\"\n  # Test: fromYamlArray\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n",
            "originalContent": "# Test: Type conversion errors - comprehensive testing of all type conversion functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n  {{- if .Values.testToStrings }}\n  toStringsValue: {{ toStrings $testData | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n  {{- if .Values.testToDecimal }}\n  toDecimalValue: {{ toDecimal $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  {{- if .Values.testToJson }}\n  toJsonValue: {{ toJson $testData | quote }}\n  {{- end }}\n  # Test: mustToJson\n  {{- if .Values.testMustToJson }}\n  mustToJsonValue: {{ mustToJson $testData | quote }}\n  {{- end }}\n  # Test: toPrettyJson\n  {{- if .Values.testToPrettyJson }}\n  toPrettyJsonValue: {{ toPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: mustToPrettyJson\n  {{- if .Values.testMustToPrettyJson }}\n  mustToPrettyJsonValue: {{ mustToPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: toRawJson\n  {{- if .Values.testToRawJson }}\n  toRawJsonValue: {{ toRawJson $testData | quote }}\n  {{- end }}\n  # Test: mustToRawJson\n  {{- if .Values.testMustToRawJson }}\n  mustToRawJsonValue: {{ mustToRawJson $testData | quote }}\n  {{- end }}\n  # Test: fromJson\n  {{- if .Values.testFromJson }}\n  {{- $parsed := $testData | fromJson }}\n  fromJsonValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: fromJsonArray\n  {{- if .Values.testFromJsonArray }}\n  {{- $arr := $testData | fromJsonArray }}\n  fromJsonArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  {{- if .Values.testFromYaml }}\n  {{- $parsed := $testData | fromYaml }}\n  fromYamlValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: toYaml\n  {{- if .Values.testToYaml }}\n  toYamlValue: {{ toYaml $testData | quote }}\n  {{- end }}\n  # Test: toYamlPretty\n  {{- if .Values.testToYamlPretty }}\n  toYamlPrettyValue: {{ toYamlPretty $testData | quote }}\n  {{- end }}\n  # Test: fromYamlArray\n  {{- if .Values.testFromYamlArray }}\n  {{- $arr := $testData | fromYamlArray }}\n  fromYamlArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n  {{- if .Values.testToToml }}\n  toTomlValue: {{ toToml $testData | quote }}\n  {{- end }}\n\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "resolved": true,
      "stopReason": "",
      "fixChain": []
    },
    {
      "toggle": "toYamlPretty",
      "dataType": "numberValue",
      "valuesOptions": {
        "values": [
          "testToYamlPretty=true",
          "testDataType=numberValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-type-conversion-test\r\ndata:\r\n  # ==========================================\r\n  # String conversion functions\r\n  # ==========================================\r\n  # Test: toStrings\r\n\r\n  # ==========================================\r\n  # Decimal conversion functions\r\n  # ==========================================\r\n  # Test: toDecimal\r\n\r\n  # ==========================================\r\n  # JSON conversion functions\r\n  # ==========================================\r\n  # Test: toJson\r\n  # Test: mustToJson\r\n  # Test: toPrettyJson\r\n  # Test: mustToPrettyJson\r\n  # Test: toRawJson\r\n  # Test: mustToRawJson\r\n  # Test: fromJson\r\n  # Test: fromJsonArray\r\n\r\n  # ==========================================\r\n  # YAML conversion functions\r\n  # ==========================================\r\n  # Test: fromYaml\r\n  # Test: toYaml\r\n  # Test: toYamlPretty\r\n  toYamlPrettyValue: \"42\"\r\n  # Test: fromYamlArray\r\n\r\n  # ==========================================\r\n  # TOML conversion functions\r\n  # ==========================================\r\n  # Test: toToml\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-type-conversion/templates/type-conversion-test.yaml",
            "content": "\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  # Test: mustToJson\n  # Test: toPrettyJson\n  # Test: mustToPrettyJson\n  # Test: toRawJson\n  # Test: mustToRawJson\n  # Test: fromJson\n  # Test: fromJsonArray\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  # Test: toYaml\n  # Test: toYamlPretty\n  toYamlPrettyValue: \"42\"\n  # Test: fromYamlArray\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n",
            "originalContent": "# Test: Type conversion errors - comprehensive testing of all type conversion functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n  {{- if .Values.testToStrings }}\n  toStringsValue: {{ toStrings $testData | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n  {{- if .Values.testToDecimal }}\n  toDecimalValue: {{ toDecimal $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  {{- if .Values.testToJson }}\n  toJsonValue: {{ toJson $testData | quote }}\n  {{- end }}\n  # Test: mustToJson\n  {{- if .Values.testMustToJson }}\n  mustToJsonValue: {{ mustToJson $testData | quote }}\n  {{- end }}\n  # Test: toPrettyJson\n  {{- if .Values.testToPrettyJson }}\n  toPrettyJsonValue: {{ toPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: mustToPrettyJson\n  {{- if .Values.testMustToPrettyJson }}\n  mustToPrettyJsonValue: {{ mustToPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: toRawJson\n  {{- if .Values.testToRawJson }}\n  toRawJsonValue: {{ toRawJson $testData | quote }}\n  {{- end }}\n  # Test: mustToRawJson\n  {{- if .Values.testMustToRawJson }}\n  mustToRawJsonValue: {{ mustToRawJson $testData | quote }}\n  {{- end }}\n  # Test: fromJson\n  {{- if .Values.testFromJson }}\n  {{- $parsed := $testData | fromJson }}\n  fromJsonValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: fromJsonArray\n  {{- if .Values.testFromJsonArray }}\n  {{- $arr := $testData | fromJsonArray }}\n  fromJsonArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  {{- if .Values.testFromYaml }}\n  {{- $parsed := $testData | fromYaml }}\n  fromYamlValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: toYaml\n  {{- if .Values.testToYaml }}\n  toYamlValue: {{ toYaml $testData | quote }}\n  {{- end }}\n  # Test: toYamlPretty\n  {{- if .Values.testToYamlPretty }}\n  toYamlPrettyValue: {{ toYamlPretty $testData | quote }}\n  {{- end }}\n  # Test: fromYamlArray\n  {{- if .Values.testFromYamlArray }}\n  {{- $arr := $testData | fromYamlArray }}\n  fromYamlArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n  {{- if .Values.testToToml }}\n  toTomlValue: {{ toToml $testData | quote }}\n  {{- end }}\n\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-type-conversion-test\r\ndata:\r\n  # ==========================================\r\n  # String conversion functions\r\n  # ==========================================\r\n  # Test: toStrings\r\n\r\n  # ==========================================\r\n  # Decimal conversion functions\r\n  # ==========================================\r\n  # Test: toDecimal\r\n\r\n  # ==========================================\r\n  # JSON conversion functions\r\n  # ==========================================\r\n  # Test: toJson\r\n  # Test: mustToJson\r\n  # Test: toPrettyJson\r\n  # Test: mustToPrettyJson\r\n  # Test: toRawJson\r\n  # Test: mustToRawJson\r\n  # Test: fromJson\r\n  # Test: fromJsonArray\r\n\r\n  # ==========================================\r\n  # YAML conversion functions\r\n  # ==========================================\r\n  # Test: fromYaml\r\n  # Test: toYaml\r\n  # Test: toYamlPretty\r\n  toYamlPrettyValue: \"42\"\r\n  # Test: fromYamlArray\r\n\r\n  # ==========================================\r\n  # TOML conversion functions\r\n  # ==========================================\r\n  # Test: toToml\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-type-conversion/templates/type-conversion-test.yaml",
            "content": "\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  # Test: mustToJson\n  # Test: toPrettyJson\n  # Test: mustToPrettyJson\n  # Test: toRawJson\n  # Test: mustToRawJson\n  # Test: fromJson\n  # Test: fromJsonArray\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  # Test: toYaml\n  # Test: toYamlPretty\n  toYamlPrettyValue: \"42\"\n  # Test: fromYamlArray\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n",
            "originalContent": "# Test: Type conversion errors - comprehensive testing of all type conversion functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n  {{- if .Values.testToStrings }}\n  toStringsValue: {{ toStrings $testData | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n  {{- if .Values.testToDecimal }}\n  toDecimalValue: {{ toDecimal $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  {{- if .Values.testToJson }}\n  toJsonValue: {{ toJson $testData | quote }}\n  {{- end }}\n  # Test: mustToJson\n  {{- if .Values.testMustToJson }}\n  mustToJsonValue: {{ mustToJson $testData | quote }}\n  {{- end }}\n  # Test: toPrettyJson\n  {{- if .Values.testToPrettyJson }}\n  toPrettyJsonValue: {{ toPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: mustToPrettyJson\n  {{- if .Values.testMustToPrettyJson }}\n  mustToPrettyJsonValue: {{ mustToPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: toRawJson\n  {{- if .Values.testToRawJson }}\n  toRawJsonValue: {{ toRawJson $testData | quote }}\n  {{- end }}\n  # Test: mustToRawJson\n  {{- if .Values.testMustToRawJson }}\n  mustToRawJsonValue: {{ mustToRawJson $testData | quote }}\n  {{- end }}\n  # Test: fromJson\n  {{- if .Values.testFromJson }}\n  {{- $parsed := $testData | fromJson }}\n  fromJsonValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: fromJsonArray\n  {{- if .Values.testFromJsonArray }}\n  {{- $arr := $testData | fromJsonArray }}\n  fromJsonArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  {{- if .Values.testFromYaml }}\n  {{- $parsed := $testData | fromYaml }}\n  fromYamlValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: toYaml\n  {{- if .Values.testToYaml }}\n  toYamlValue: {{ toYaml $testData | quote }}\n  {{- end }}\n  # Test: toYamlPretty\n  {{- if .Values.testToYamlPretty }}\n  toYamlPrettyValue: {{ toYamlPretty $testData | quote }}\n  {{- end }}\n  # Test: fromYamlArray\n  {{- if .Values.testFromYamlArray }}\n  {{- $arr := $testData | fromYamlArray }}\n  fromYamlArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n  {{- if .Values.testToToml }}\n  toTomlValue: {{ toToml $testData | quote }}\n  {{- end }}\n\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "resolved": true,
      "stopReason": "",
      "fixChain": []
    },
    {
      "toggle": "toYamlPretty",
      "dataType": "boolValue",
      "valuesOptions": {
        "values": [
          "testToYamlPretty=true",
          "testDataType=boolValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-type-conversion-test\r\ndata:\r\n  # ==========================================\r\n  # String conversion functions\r\n  # ==========================================\r\n  # Test: toStrings\r\n\r\n  # ==========================================\r\n  # Decimal conversion functions\r\n  # ==========================================\r\n  # Test: toDecimal\r\n\r\n  # ==========================================\r\n  # JSON conversion functions\r\n  # ==========================================\r\n  # Test: toJson\r\n  # Test: mustToJson\r\n  # Test: toPrettyJson\r\n  # Test: mustToPrettyJson\r\n  # Test: toRawJson\r\n  # Test: mustToRawJson\r\n  # Test: fromJson\r\n  # Test: fromJsonArray\r\n\r\n  # ==========================================\r\n  # YAML conversion functions\r\n  # ==========================================\r\n  # Test: fromYaml\r\n  # Test: toYaml\r\n  # Test: toYamlPretty\r\n  toYamlPrettyValue: \"true\"\r\n  # Test: fromYamlArray\r\n\r\n  # ==========================================\r\n  # TOML conversion functions\r\n  # ==========================================\r\n  # Test: toToml\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-type-conversion/templates/type-conversion-test.yaml",
            "content": "\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  # Test: mustToJson\n  # Test: toPrettyJson\n  # Test: mustToPrettyJson\n  # Test: toRawJson\n  # Test: mustToRawJson\n  # Test: fromJson\n  # Test: fromJsonArray\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  # Test: toYaml\n  # Test: toYamlPretty\n  toYamlPrettyValue: \"true\"\n  # Test: fromYamlArray\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n",
            "originalContent": "# Test: Type conversion errors - comprehensive testing of all type conversion functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n  {{- if .Values.testToStrings }}\n  toStringsValue: {{ toStrings $testData | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n  {{- if .Values.testToDecimal }}\n  toDecimalValue: {{ toDecimal $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  {{- if .Values.testToJson }}\n  toJsonValue: {{ toJson $testData | quote }}\n  {{- end }}\n  # Test: mustToJson\n  {{- if .Values.testMustToJson }}\n  mustToJsonValue: {{ mustToJson $testData | quote }}\n  {{- end }}\n  # Test: toPrettyJson\n  {{- if .Values.testToPrettyJson }}\n  toPrettyJsonValue: {{ toPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: mustToPrettyJson\n  {{- if .Values.testMustToPrettyJson }}\n  mustToPrettyJsonValue: {{ mustToPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: toRawJson\n  {{- if .Values.testToRawJson }}\n  toRawJsonValue: {{ toRawJson $testData | quote }}\n  {{- end }}\n  # Test: mustToRawJson\n  {{- if .Values.testMustToRawJson }}\n  mustToRawJsonValue: {{ mustToRawJson $testData | quote }}\n  {{- end }}\n  # Test: fromJson\n  {{- if .Values.testFromJson }}\n  {{- $parsed := $testData | fromJson }}\n  fromJsonValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: fromJsonArray\n  {{- if .Values.testFromJsonArray }}\n  {{- $arr := $testData | fromJsonArray }}\n  fromJsonArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  {{- if .Values.testFromYaml }}\n  {{- $parsed := $testData | fromYaml }}\n  fromYamlValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: toYaml\n  {{- if .Values.testToYaml }}\n  toYamlValue: {{ toYaml $testData | quote }}\n  {{- end }}\n  # Test: toYamlPretty\n  {{- if .Values.testToYamlPretty }}\n  toYamlPrettyValue: {{ toYamlPretty $testData | quote }}\n  {{- end }}\n  # Test: fromYamlArray\n  {{- if .Values.testFromYamlArray }}\n  {{- $arr := $testData | fromYamlArray }}\n  fromYamlArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n  {{- if .Values.testToToml }}\n  toTomlValue: {{ toToml $testData | quote }}\n  {{- end }}\n\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-type-conversion-test\r\ndata:\r\n  # ==========================================\r\n  # String conversion functions\r\n  # ==========================================\r\n  # Test: toStrings\r\n\r\n  # ==========================================\r\n  # Decimal conversion functions\r\n  # ==========================================\r\n  # Test: toDecimal\r\n\r\n  # ==========================================\r\n  # JSON conversion functions\r\n  # ==========================================\r\n  # Test: toJson\r\n  # Test: mustToJson\r\n  # Test: toPrettyJson\r\n  # Test: mustToPrettyJson\r\n  # Test: toRawJson\r\n  # Test: mustToRawJson\r\n  # Test: fromJson\r\n  # Test: fromJsonArray\r\n\r\n  # ==========================================\r\n  # YAML conversion functions\r\n  # ==========================================\r\n  # Test: fromYaml\r\n  # Test: toYaml\r\n  # Test: toYamlPretty\r\n  toYamlPrettyValue: \"true\"\r\n  # Test: fromYamlArray\r\n\r\n  # ==========================================\r\n  # TOML conversion functions\r\n  # ==========================================\r\n  # Test: toToml\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-type-conversion/templates/type-conversion-test.yaml",
            "content": "\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  # Test: mustToJson\n  # Test: toPrettyJson\n  # Test: mustToPrettyJson\n  # Test: toRawJson\n  # Test: mustToRawJson\n  # Test: fromJson\n  # Test: fromJsonArray\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  # Test: toYaml\n  # Test: toYamlPretty\n  toYamlPrettyValue: \"true\"\n  # Test: fromYamlArray\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n",
            "originalContent": "# Test: Type conversion errors - comprehensive testing of all type conversion functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n  {{- if .Values.testToStrings }}\n  toStringsValue: {{ toStrings $testData | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n  {{- if .Values.testToDecimal }}\n  toDecimalValue: {{ toDecimal $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  {{- if .Values.testToJson }}\n  toJsonValue: {{ toJson $testData | quote }}\n  {{- end }}\n  # Test: mustToJson\n  {{- if .Values.testMustToJson }}\n  mustToJsonValue: {{ mustToJson $testData | quote }}\n  {{- end }}\n  # Test: toPrettyJson\n  {{- if .Values.testToPrettyJson }}\n  toPrettyJsonValue: {{ toPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: mustToPrettyJson\n  {{- if .Values.testMustToPrettyJson }}\n  mustToPrettyJsonValue: {{ mustToPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: toRawJson\n  {{- if .Values.testToRawJson }}\n  toRawJsonValue: {{ toRawJson $testData | quote }}\n  {{- end }}\n  # Test: mustToRawJson\n  {{- if .Values.testMustToRawJson }}\n  mustToRawJsonValue: {{ mustToRawJson $testData | quote }}\n  {{- end }}\n  # Test: fromJson\n  {{- if .Values.testFromJson }}\n  {{- $parsed := $testData | fromJson }}\n  fromJsonValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: fromJsonArray\n  {{- if .Values.testFromJsonArray }}\n  {{- $arr := $testData | fromJsonArray }}\n  fromJsonArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  {{- if .Values.testFromYaml }}\n  {{- $parsed := $testData | fromYaml }}\n  fromYamlValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: toYaml\n  {{- if .Values.testToYaml }}\n  toYamlValue: {{ toYaml $testData | quote }}\n  {{- end }}\n  # Test: toYamlPretty\n  {{- if .Values.testToYamlPretty }}\n  toYamlPrettyValue: {{ toYamlPretty $testData | quote }}\n  {{- end }}\n  # Test: fromYamlArray\n  {{- if .Values.testFromYamlArray }}\n  {{- $arr := $testData | fromYamlArray }}\n  fromYamlArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n  {{- if .Values.testToToml }}\n  toTomlValue: {{ toToml $testData | quote }}\n  {{- end }}\n\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "resolved": true,
      "stopReason": "",
      "fixChain": []
    },
    {
      "toggle": "toYamlPretty",
      "dataType": "listValue",
      "valuesOptions": {
        "values": [
          "testToYamlPretty=true",
          "testDataType=listValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-type-conversion-test\r\ndata:\r\n  # ==========================================\r\n  # String conversion functions\r\n  # ==========================================\r\n  # Test: toStrings\r\n\r\n  # ==========================================\r\n  # Decimal conversion functions\r\n  # ==========================================\r\n  # Test: toDecimal\r\n\r\n  # ==========================================\r\n  # JSON conversion functions\r\n  # ==========================================\r\n  # Test: toJson\r\n  # Test: mustToJson\r\n  # Test: toPrettyJson\r\n  # Test: mustToPrettyJson\r\n  # Test: toRawJson\r\n  # Test: mustToRawJson\r\n  # Test: fromJson\r\n  # Test: fromJsonArray\r\n\r\n  # ==========================================\r\n  # YAML conversion functions\r\n  # ==========================================\r\n  # Test: fromYaml\r\n  # Test: toYaml\r\n  # Test: toYamlPretty\r\n  toYamlPrettyValue: \"- 1\\n- 2\\n- 3\"\r\n  # Test: fromYamlArray\r\n\r\n  # ==========================================\r\n  # TOML conversion functions\r\n  # ==========================================\r\n  # Test: toToml\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-type-conversion/templates/type-conversion-test.yaml",
            "content": "\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  # Test: mustToJson\n  # Test: toPrettyJson\n  # Test: mustToPrettyJson\n  # Test: toRawJson\n  # Test: mustToRawJson\n  # Test: fromJson\n  # Test: fromJsonArray\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  # Test: toYaml\n  # Test: toYamlPretty\n  toYamlPrettyValue: \"- 1\\n- 2\\n- 3\"\n  # Test: fromYamlArray\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n",
            "originalContent": "# Test: Type conversion errors - comprehensive testing of all type conversion functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n  {{- if .Values.testToStrings }}\n  toStringsValue: {{ toStrings $testData | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n  {{- if .Values.testToDecimal }}\n  toDecimalValue: {{ toDecimal $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  {{- if .Values.testToJson }}\n  toJsonValue: {{ toJson $testData | quote }}\n  {{- end }}\n  # Test: mustToJson\n  {{- if .Values.testMustToJson }}\n  mustToJsonValue: {{ mustToJson $testData | quote }}\n  {{- end }}\n  # Test: toPrettyJson\n  {{- if .Values.testToPrettyJson }}\n  toPrettyJsonValue: {{ toPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: mustToPrettyJson\n  {{- if .Values.testMustToPrettyJson }}\n  mustToPrettyJsonValue: {{ mustToPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: toRawJson\n  {{- if .Values.testToRawJson }}\n  toRawJsonValue: {{ toRawJson $testData | quote }}\n  {{- end }}\n  # Test: mustToRawJson\n  {{- if .Values.testMustToRawJson }}\n  mustToRawJsonValue: {{ mustToRawJson $testData | quote }}\n  {{- end }}\n  # Test: fromJson\n  {{- if .Values.testFromJson }}\n  {{- $parsed := $testData | fromJson }}\n  fromJsonValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: fromJsonArray\n  {{- if .Values.testFromJsonArray }}\n  {{- $arr := $testData | fromJsonArray }}\n  fromJsonArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  {{- if .Values.testFromYaml }}\n  {{- $parsed := $testData | fromYaml }}\n  fromYamlValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: toYaml\n  {{- if .Values.testToYaml }}\n  toYamlValue: {{ toYaml $testData | quote }}\n  {{- end }}\n  # Test: toYamlPretty\n  {{- if .Values.testToYamlPretty }}\n  toYamlPrettyValue: {{ toYamlPretty $testData | quote }}\n  {{- end }}\n  # Test: fromYamlArray\n  {{- if .Values.testFromYamlArray }}\n  {{- $arr := $testData | fromYamlArray }}\n  fromYamlArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n  {{- if .Values.testToToml }}\n  toTomlValue: {{ toToml $testData | quote }}\n  {{- end }}\n\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-type-conversion-test\r\ndata:\r\n  # ==========================================\r\n  # String conversion functions\r\n  # ==========================================\r\n  # Test: toStrings\r\n\r\n  # ==========================================\r\n  # Decimal conversion functions\r\n  # ==========================================\r\n  # Test: toDecimal\r\n\r\n  # ==========================================\r\n  # JSON conversion functions\r\n  # ==========================================\r\n  # Test: toJson\r\n  # Test: mustToJson\r\n  # Test: toPrettyJson\r\n  # Test: mustToPrettyJson\r\n  # Test: toRawJson\r\n  # Test: mustToRawJson\r\n  # Test: fromJson\r\n  # Test: fromJsonArray\r\n\r\n  # ==========================================\r\n  # YAML conversion functions\r\n  # ==========================================\r\n  # Test: fromYaml\r\n  # Test: toYaml\r\n  # Test: toYamlPretty\r\n  toYamlPrettyValue: \"- 1\\n- 2\\n- 3\"\r\n  # Test: fromYamlArray\r\n\r\n  # ==========================================\r\n  # TOML conversion functions\r\n  # ==========================================\r\n  # Test: toToml\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-type-conversion/templates/type-conversion-test.yaml",
            "content": "\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  # Test: mustToJson\n  # Test: toPrettyJson\n  # Test: mustToPrettyJson\n  # Test: toRawJson\n  # Test: mustToRawJson\n  # Test: fromJson\n  # Test: fromJsonArray\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  # Test: toYaml\n  # Test: toYamlPretty\n  toYamlPrettyValue: \"- 1\\n- 2\\n- 3\"\n  # Test: fromYamlArray\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n",
            "originalContent": "# Test: Type conversion errors - comprehensive testing of all type conversion functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n  {{- if .Values.testToStrings }}\n  toStringsValue: {{ toStrings $testData | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n  {{- if .Values.testToDecimal }}\n  toDecimalValue: {{ toDecimal $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  {{- if .Values.testToJson }}\n  toJsonValue: {{ toJson $testData | quote }}\n  {{- end }}\n  # Test: mustToJson\n  {{- if .Values.testMustToJson }}\n  mustToJsonValue: {{ mustToJson $testData | quote }}\n  {{- end }}\n  # Test: toPrettyJson\n  {{- if .Values.testToPrettyJson }}\n  toPrettyJsonValue: {{ toPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: mustToPrettyJson\n  {{- if .Values.testMustToPrettyJson }}\n  mustToPrettyJsonValue: {{ mustToPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: toRawJson\n  {{- if .Values.testToRawJson }}\n  toRawJsonValue: {{ toRawJson $testData | quote }}\n  {{- end }}\n  # Test: mustToRawJson\n  {{- if .Values.testMustToRawJson }}\n  mustToRawJsonValue: {{ mustToRawJson $testData | quote }}\n  {{- end }}\n  # Test: fromJson\n  {{- if .Values.testFromJson }}\n  {{- $parsed := $testData | fromJson }}\n  fromJsonValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: fromJsonArray\n  {{- if .Values.testFromJsonArray }}\n  {{- $arr := $testData | fromJsonArray }}\n  fromJsonArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  {{- if .Values.testFromYaml }}\n  {{- $parsed := $testData | fromYaml }}\n  fromYamlValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: toYaml\n  {{- if .Values.testToYaml }}\n  toYamlValue: {{ toYaml $testData | quote }}\n  {{- end }}\n  # Test: toYamlPretty\n  {{- if .Values.testToYamlPretty }}\n  toYamlPrettyValue: {{ toYamlPretty $testData | quote }}\n  {{- end }}\n  # Test: fromYamlArray\n  {{- if .Values.testFromYamlArray }}\n  {{- $arr := $testData | fromYamlArray }}\n  fromYamlArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n  {{- if .Values.testToToml }}\n  toTomlValue: {{ toToml $testData | quote }}\n  {{- end }}\n\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "resolved": true,
      "stopReason": "",
      "fixChain": []
    },
    {
      "toggle": "toYamlPretty",
      "dataType": "dictValue",
      "valuesOptions": {
        "values": [
          "testToYamlPretty=true",
          "testDataType=dictValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-type-conversion-test\r\ndata:\r\n  # ==========================================\r\n  # String conversion functions\r\n  # ==========================================\r\n  # Test: toStrings\r\n\r\n  # ==========================================\r\n  # Decimal conversion functions\r\n  # ==========================================\r\n  # Test: toDecimal\r\n\r\n  # ==========================================\r\n  # JSON conversion functions\r\n  # ==========================================\r\n  # Test: toJson\r\n  # Test: mustToJson\r\n  # Test: toPrettyJson\r\n  # Test: mustToPrettyJson\r\n  # Test: toRawJson\r\n  # Test: mustToRawJson\r\n  # Test: fromJson\r\n  # Test: fromJsonArray\r\n\r\n  # ==========================================\r\n  # YAML conversion functions\r\n  # ==========================================\r\n  # Test: fromYaml\r\n  # Test: toYaml\r\n  # Test: toYamlPretty\r\n  toYamlPrettyValue: \"key: value\"\r\n  # Test: fromYamlArray\r\n\r\n  # ==========================================\r\n  # TOML conversion functions\r\n  # ==========================================\r\n  # Test: toToml\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-type-conversion/templates/type-conversion-test.yaml",
            "content": "\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  # Test: mustToJson\n  # Test: toPrettyJson\n  # Test: mustToPrettyJson\n  # Test: toRawJson\n  # Test: mustToRawJson\n  # Test: fromJson\n  # Test: fromJsonArray\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  # Test: toYaml\n  # Test: toYamlPretty\n  toYamlPrettyValue: \"key: value\"\n  # Test: fromYamlArray\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n",
            "originalContent": "# Test: Type conversion errors - comprehensive testing of all type conversion functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n  {{- if .Values.testToStrings }}\n  toStringsValue: {{ toStrings $testData | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n  {{- if .Values.testToDecimal }}\n  toDecimalValue: {{ toDecimal $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  {{- if .Values.testToJson }}\n  toJsonValue: {{ toJson $testData | quote }}\n  {{- end }}\n  # Test: mustToJson\n  {{- if .Values.testMustToJson }}\n  mustToJsonValue: {{ mustToJson $testData | quote }}\n  {{- end }}\n  # Test: toPrettyJson\n  {{- if .Values.testToPrettyJson }}\n  toPrettyJsonValue: {{ toPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: mustToPrettyJson\n  {{- if .Values.testMustToPrettyJson }}\n  mustToPrettyJsonValue: {{ mustToPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: toRawJson\n  {{- if .Values.testToRawJson }}\n  toRawJsonValue: {{ toRawJson $testData | quote }}\n  {{- end }}\n  # Test: mustToRawJson\n  {{- if .Values.testMustToRawJson }}\n  mustToRawJsonValue: {{ mustToRawJson $testData | quote }}\n  {{- end }}\n  # Test: fromJson\n  {{- if .Values.testFromJson }}\n  {{- $parsed := $testData | fromJson }}\n  fromJsonValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: fromJsonArray\n  {{- if .Values.testFromJsonArray }}\n  {{- $arr := $testData | fromJsonArray }}\n  fromJsonArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  {{- if .Values.testFromYaml }}\n  {{- $parsed := $testData | fromYaml }}\n  fromYamlValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: toYaml\n  {{- if .Values.testToYaml }}\n  toYamlValue: {{ toYaml $testData | quote }}\n  {{- end }}\n  # Test: toYamlPretty\n  {{- if .Values.testToYamlPretty }}\n  toYamlPrettyValue: {{ toYamlPretty $testData | quote }}\n  {{- end }}\n  # Test: fromYamlArray\n  {{- if .Values.testFromYamlArray }}\n  {{- $arr := $testData | fromYamlArray }}\n  fromYamlArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n  {{- if .Values.testToToml }}\n  toTomlValue: {{ toToml $testData | quote }}\n  {{- end }}\n\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-type-conversion-test\r\ndata:\r\n  # ==========================================\r\n  # String conversion functions\r\n  # ==========================================\r\n  # Test: toStrings\r\n\r\n  # ==========================================\r\n  # Decimal conversion functions\r\n  # ==========================================\r\n  # Test: toDecimal\r\n\r\n  # ==========================================\r\n  # JSON conversion functions\r\n  # ==========================================\r\n  # Test: toJson\r\n  # Test: mustToJson\r\n  # Test: toPrettyJson\r\n  # Test: mustToPrettyJson\r\n  # Test: toRawJson\r\n  # Test: mustToRawJson\r\n  # Test: fromJson\r\n  # Test: fromJsonArray\r\n\r\n  # ==========================================\r\n  # YAML conversion functions\r\n  # ==========================================\r\n  # Test: fromYaml\r\n  # Test: toYaml\r\n  # Test: toYamlPretty\r\n  toYamlPrettyValue: \"key: value\"\r\n  # Test: fromYamlArray\r\n\r\n  # ==========================================\r\n  # TOML conversion functions\r\n  # ==========================================\r\n  # Test: toToml\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-type-conversion/templates/type-conversion-test.yaml",
            "content": "\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  # Test: mustToJson\n  # Test: toPrettyJson\n  # Test: mustToPrettyJson\n  # Test: toRawJson\n  # Test: mustToRawJson\n  # Test: fromJson\n  # Test: fromJsonArray\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  # Test: toYaml\n  # Test: toYamlPretty\n  toYamlPrettyValue: \"key: value\"\n  # Test: fromYamlArray\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n",
            "originalContent": "# Test: Type conversion errors - comprehensive testing of all type conversion functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n  {{- if .Values.testToStrings }}\n  toStringsValue: {{ toStrings $testData | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n  {{- if .Values.testToDecimal }}\n  toDecimalValue: {{ toDecimal $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  {{- if .Values.testToJson }}\n  toJsonValue: {{ toJson $testData | quote }}\n  {{- end }}\n  # Test: mustToJson\n  {{- if .Values.testMustToJson }}\n  mustToJsonValue: {{ mustToJson $testData | quote }}\n  {{- end }}\n  # Test: toPrettyJson\n  {{- if .Values.testToPrettyJson }}\n  toPrettyJsonValue: {{ toPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: mustToPrettyJson\n  {{- if .Values.testMustToPrettyJson }}\n  mustToPrettyJsonValue: {{ mustToPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: toRawJson\n  {{- if .Values.testToRawJson }}\n  toRawJsonValue: {{ toRawJson $testData | quote }}\n  {{- end }}\n  # Test: mustToRawJson\n  {{- if .Values.testMustToRawJson }}\n  mustToRawJsonValue: {{ mustToRawJson $testData | quote }}\n  {{- end }}\n  # Test: fromJson\n  {{- if .Values.testFromJson }}\n  {{- $parsed := $testData | fromJson }}\n  fromJsonValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: fromJsonArray\n  {{- if .Values.testFromJsonArray }}\n  {{- $arr := $testData | fromJsonArray }}\n  fromJsonArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  {{- if .Values.testFromYaml }}\n  {{- $parsed := $testData | fromYaml }}\n  fromYamlValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: toYaml\n  {{- if .Values.testToYaml }}\n  toYamlValue: {{ toYaml $testData | quote }}\n  {{- end }}\n  # Test: toYamlPretty\n  {{- if .Values.testToYamlPretty }}\n  toYamlPrettyValue: {{ toYamlPretty $testData | quote }}\n  {{- end }}\n  # Test: fromYamlArray\n  {{- if .Values.testFromYamlArray }}\n  {{- $arr := $testData | fromYamlArray }}\n  fromYamlArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n  {{- if .Values.testToToml }}\n  toTomlValue: {{ toToml $testData | quote }}\n  {{- end }}\n\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "resolved": true,
      "stopReason": "",
      "fixChain": []
    },
    {
      "toggle": "fromYamlArray",
      "dataType": "nilValue",
      "valuesOptions": {
        "values": [
          "testFromYamlArray=true",
          "testDataType=nilValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": "template: test-type-conversion/templates/type-conversion-test.yaml:81:26: executing \"test-type-conversion/templates/type-conversion-test.yaml\" at \u003cfromYamlArray\u003e: invalid value; expected string",
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      },
      "debug": {
        "error": "template: test-type-conversion/templates/type-conversion-test.yaml:81:26: executing \"test-type-conversion/templates/type-conversion-test.yaml\" at \u003cfromYamlArray\u003e: invalid value; expected string",
        "debugLogs": [],
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      },
      "resolved": false,
      "stopReason": "unfixable_error_kind",
      "fixChain": []
    },
    {
      "toggle": "fromYamlArray",
      "dataType": "stringValue",
      "valuesOptions": {
        "values": [
          "testFromYamlArray=true",
          "testDataType=stringValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-type-conversion-test\r\ndata:\r\n  # ==========================================\r\n  # String conversion functions\r\n  # ==========================================\r\n  # Test: toStrings\r\n\r\n  # ==========================================\r\n  # Decimal conversion functions\r\n  # ==========================================\r\n  # Test: toDecimal\r\n\r\n  # ==========================================\r\n  # JSON conversion functions\r\n  # ==========================================\r\n  # Test: toJson\r\n  # Test: mustToJson\r\n  # Test: toPrettyJson\r\n  # Test: mustToPrettyJson\r\n  # Test: toRawJson\r\n  # Test: mustToRawJson\r\n  # Test: fromJson\r\n  # Test: fromJsonArray\r\n\r\n  # ==========================================\r\n  # YAML conversion functions\r\n  # ==========================================\r\n  # Test: fromYaml\r\n  # Test: toYaml\r\n  # Test: toYamlPretty\r\n  # Test: fromYamlArray\r\n  fromYamlArrayValue: [\"error unmarshaling JSON: while decoding JSON: json: cannot unmarshal string into Go value of type []interface {}\"]\r\n\r\n  # ==========================================\r\n  # TOML conversion functions\r\n  # ==========================================\r\n  # Test: toToml\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-type-conversion/templates/type-conversion-test.yaml",
            "content": "\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  # Test: mustToJson\n  # Test: toPrettyJson\n  # Test: mustToPrettyJson\n  # Test: toRawJson\n  # Test: mustToRawJson\n  # Test: fromJson\n  # Test: fromJsonArray\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  # Test: toYaml\n  # Test: toYamlPretty\n  # Test: fromYamlArray\n  fromYamlArrayValue: [\"error unmarshaling JSON: while decoding JSON: json: cannot unmarshal string into Go value of type []interface {}\"]\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n",
            "originalContent": "# Test: Type conversion errors - comprehensive testing of all type conversion functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n  {{- if .Values.testToStrings }}\n  toStringsValue: {{ toStrings $testData | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n  {{- if .Values.testToDecimal }}\n  toDecimalValue: {{ toDecimal $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  {{- if .Values.testToJson }}\n  toJsonValue: {{ toJson $testData | quote }}\n  {{- end }}\n  # Test: mustToJson\n  {{- if .Values.testMustToJson }}\n  mustToJsonValue: {{ mustToJson $testData | quote }}\n  {{- end }}\n  # Test: toPrettyJson\n  {{- if .Values.testToPrettyJson }}\n  toPrettyJsonValue: {{ toPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: mustToPrettyJson\n  {{- if .Values.testMustToPrettyJson }}\n  mustToPrettyJsonValue: {{ mustToPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: toRawJson\n  {{- if .Values.testToRawJson }}\n  toRawJsonValue: {{ toRawJson $testData | quote }}\n  {{- end }}\n  # Test: mustToRawJson\n  {{- if .Values.testMustToRawJson }}\n  mustToRawJsonValue: {{ mustToRawJson $testData | quote }}\n  {{- end }}\n  # Test: fromJson\n  {{- if .Values.testFromJson }}\n  {{- $parsed := $testData | fromJson }}\n  fromJsonValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: fromJsonArray\n  {{- if .Values.testFromJsonArray }}\n  {{- $arr := $testData | fromJsonArray }}\n  fromJsonArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  {{- if .Values.testFromYaml }}\n  {{- $parsed := $testData | fromYaml }}\n  fromYamlValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: toYaml\n  {{- if .Values.testToYaml }}\n  toYamlValue: {{ toYaml $testData | quote }}\n  {{- end }}\n  # Test: toYamlPretty\n  {{- if .Values.testToYamlPretty }}\n  toYamlPrettyValue: {{ toYamlPretty $testData | quote }}\n  {{- end }}\n  # Test: fromYamlArray\n  {{- if .Values.testFromYamlArray }}\n  {{- $arr := $testData | fromYamlArray }}\n  fromYamlArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n  {{- if .Values.testToToml }}\n  toTomlValue: {{ toToml $testData | quote }}\n  {{- end }}\n\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-type-conversion-test\r\ndata:\r\n  # ==========================================\r\n  # String conversion functions\r\n  # ==========================================\r\n  # Test: toStrings\r\n\r\n  # ==========================================\r\n  # Decimal conversion functions\r\n  # ==========================================\r\n  # Test: toDecimal\r\n\r\n  # ==========================================\r\n  # JSON conversion functions\r\n  # ==========================================\r\n  # Test: toJson\r\n  # Test: mustToJson\r\n  # Test: toPrettyJson\r\n  # Test: mustToPrettyJson\r\n  # Test: toRawJson\r\n  # Test: mustToRawJson\r\n  # Test: fromJson\r\n  # Test: fromJsonArray\r\n\r\n  # ==========================================\r\n  # YAML conversion functions\r\n  # ==========================================\r\n  # Test: fromYaml\r\n  # Test: toYaml\r\n  # Test: toYamlPretty\r\n  # Test: fromYamlArray\r\n  fromYamlArrayValue: [\"error unmarshaling JSON: while decoding JSON: json: cannot unmarshal string into Go value of type []interface {}\"]\r\n\r\n  # ==========================================\r\n  # TOML conversion functions\r\n  # ==========================================\r\n  # Test: toToml\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-type-conversion/templates/type-conversion-test.yaml",
            "content": "\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  # Test: mustToJson\n  # Test: toPrettyJson\n  # Test: mustToPrettyJson\n  # Test: toRawJson\n  # Test: mustToRawJson\n  # Test: fromJson\n  # Test: fromJsonArray\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  # Test: toYaml\n  # Test: toYamlPretty\n  # Test: fromYamlArray\n  fromYamlArrayValue: [\"error unmarshaling JSON: while decoding JSON: json: cannot unmarshal string into Go value of type []interface {}\"]\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n",
            "originalContent": "# Test: Type conversion errors - comprehensive testing of all type conversion functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n  {{- if .Values.testToStrings }}\n  toStringsValue: {{ toStrings $testData | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n  {{- if .Values.testToDecimal }}\n  toDecimalValue: {{ toDecimal $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  {{- if .Values.testToJson }}\n  toJsonValue: {{ toJson $testData | quote }}\n  {{- end }}\n  # Test: mustToJson\n  {{- if .Values.testMustToJson }}\n  mustToJsonValue: {{ mustToJson $testData | quote }}\n  {{- end }}\n  # Test: toPrettyJson\n  {{- if .Values.testToPrettyJson }}\n  toPrettyJsonValue: {{ toPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: mustToPrettyJson\n  {{- if .Values.testMustToPrettyJson }}\n  mustToPrettyJsonValue: {{ mustToPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: toRawJson\n  {{- if .Values.testToRawJson }}\n  toRawJsonValue: {{ toRawJson $testData | quote }}\n  {{- end }}\n  # Test: mustToRawJson\n  {{- if .Values.testMustToRawJson }}\n  mustToRawJsonValue: {{ mustToRawJson $testData | quote }}\n  {{- end }}\n  # Test: fromJson\n  {{- if .Values.testFromJson }}\n  {{- $parsed := $testData | fromJson }}\n  fromJsonValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: fromJsonArray\n  {{- if .Values.testFromJsonArray }}\n  {{- $arr := $testData | fromJsonArray }}\n  fromJsonArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  {{- if .Values.testFromYaml }}\n  {{- $parsed := $testData | fromYaml }}\n  fromYamlValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: toYaml\n  {{- if .Values.testToYaml }}\n  toYamlValue: {{ toYaml $testData | quote }}\n  {{- end }}\n  # Test: toYamlPretty\n  {{- if .Values.testToYamlPretty }}\n  toYamlPrettyValue: {{ toYamlPretty $testData | quote }}\n  {{- end }}\n  # Test: fromYamlArray\n  {{- if .Values.testFromYamlArray }}\n  {{- $arr := $testData | fromYamlArray }}\n  fromYamlArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n  {{- if .Values.testToToml }}\n  toTomlValue: {{ toToml $testData | quote }}\n  {{- end }}\n\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "resolved": true,
      "stopReason": "",
      "fixChain": []
    },
    {
      "toggle": "fromYamlArray",
      "dataType": "numberValue",
      "valuesOptions": {
        "values": [
          "testFromYamlArray=true",
          "testDataType=numberValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": "template: test-type-conversion/templates/type-conversion-test.yaml:81:26: executing \"test-type-conversion/templates/type-conversion-test.yaml\" at \u003cfromYamlArray\u003e: wrong type for value; expected string; got float64",
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      },
      "debug": {
        "error": "template: test-type-conversion/templates/type-conversion-test.yaml:81:26: executing \"test-type-conversion/templates/type-conversion-test.yaml\" at \u003cfromYamlArray\u003e: wrong type for value; expected string; got float64",
        "debugLogs": [],
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      },
      "resolved": false,
      "stopReason": "unfixable_error_kind",
      "fixChain": []
    },
    {
      "toggle": "fromYamlArray",
      "dataType": "boolValue",
      "valuesOptions": {
        "values": [
          "testFromYamlArray=true",
          "testDataType=boolValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": "template: test-type-conversion/templates/type-conversion-test.yaml:81:26: executing \"test-type-conversion/templates/type-conversion-test.yaml\" at \u003cfromYamlArray\u003e: wrong type for value; expected string; got bool",
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      },
      "debug": {
        "error": "template: test-type-conversion/templates/type-conversion-test.yaml:81:26: executing \"test-type-conversion/templates/type-conversion-test.yaml\" at \u003cfromYamlArray\u003e: wrong type for value; expected string; got bool",
        "debugLogs": [],
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      },
      "resolved": false,
      "stopReason": "unfixable_error_kind",
      "fixChain": []
    },
    {
      "toggle": "fromYamlArray",
      "dataType": "listValue",
      "valuesOptions": {
        "values": [
          "testFromYamlArray=true",
          "testDataType=listValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": "template: test-type-conversion/templates/type-conversion-test.yaml:81:26: executing \"test-type-conversion/templates/type-conversion-test.yaml\" at \u003cfromYamlArray\u003e: wrong type for value; expected string; got []interface {}",
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      },
      "debug": {
        "error": "template: test-type-conversion/templates/type-conversion-test.yaml:81:26: executing \"test-type-conversion/templates/type-conversion-test.yaml\" at \u003cfromYamlArray\u003e: wrong type for value; expected string; got []interface {}",
        "debugLogs": [],
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      },
      "resolved": false,
      "stopReason": "unfixable_error_kind",
      "fixChain": []
    },
    {
      "toggle": "fromYamlArray",
      "dataType": "dictValue",
      "valuesOptions": {
        "values": [
          "testFromYamlArray=true",
          "testDataType=dictValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": "template: test-type-conversion/templates/type-conversion-test.yaml:81:26: executing \"test-type-conversion/templates/type-conversion-test.yaml\" at \u003cfromYamlArray\u003e: wrong type for value; expected string; got map[string]interface {}",
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      },
      "debug": {
        "error": "template: test-type-conversion/templates/type-conversion-test.yaml:81:26: executing \"test-type-conversion/templates/type-conversion-test.yaml\" at \u003cfromYamlArray\u003e: wrong type for value; expected string; got map[string]interface {}",
        "debugLogs": [],
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      },
      "resolved": false,
      "stopReason": "unfixable_error_kind",
      "fixChain": []
    },
    {
      "toggle": "toToml",
      "dataType": "nilValue",
      "valuesOptions": {
        "values": [
          "testToToml=true",
          "testDataType=nilValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": "template: test-type-conversion/templates/type-conversion-test.yaml:90:18: executing \"test-type-conversion/templates/type-conversion-test.yaml\" at \u003ctoToml $testData\u003e: error calling toToml: reflect: call of reflect.Value.Type on zero Value",
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      },
      "debug": {
        "error": "template: test-type-conversion/templates/type-conversion-test.yaml:90:18: executing \"test-type-conversion/templates/type-conversion-test.yaml\" at \u003ctoToml $testData\u003e: error calling toToml: reflect: call of reflect.Value.Type on zero Value",
        "debugLogs": [],
        "rawManifest": null,
        "partialManifest": null,
        "splitManifests": []
      },
      "resolved": false,
      "stopReason": "unfixable_error_kind",
      "fixChain": []
    },
    {
      "toggle": "toToml",
      "dataType": "stringValue",
      "valuesOptions": {
        "values": [
          "testToToml=true",
          "testDataType=stringValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-type-conversion-test\r\ndata:\r\n  # ==========================================\r\n  # String conversion functions\r\n  # ==========================================\r\n  # Test: toStrings\r\n\r\n  # ==========================================\r\n  # Decimal conversion functions\r\n  # ==========================================\r\n  # Test: toDecimal\r\n\r\n  # ==========================================\r\n  # JSON conversion functions\r\n  # ==========================================\r\n  # Test: toJson\r\n  # Test: mustToJson\r\n  # Test: toPrettyJson\r\n  # Test: mustToPrettyJson\r\n  # Test: toRawJson\r\n  # Test: mustToRawJson\r\n  # Test: fromJson\r\n  # Test: fromJsonArray\r\n\r\n  # ==========================================\r\n  # YAML conversion functions\r\n  # ==========================================\r\n  # Test: fromYaml\r\n  # Test: toYaml\r\n  # Test: toYamlPretty\r\n  # Test: fromYamlArray\r\n\r\n  # ==========================================\r\n  # TOML conversion functions\r\n  # ==========================================\r\n  # Test: toToml\r\n  toTomlValue: \"\\\"test string\\\"\"\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-type-conversion/templates/type-conversion-test.yaml",
            "content": "\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  # Test: mustToJson\n  # Test: toPrettyJson\n  # Test: mustToPrettyJson\n  # Test: toRawJson\n  # Test: mustToRawJson\n  # Test: fromJson\n  # Test: fromJsonArray\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  # Test: toYaml\n  # Test: toYamlPretty\n  # Test: fromYamlArray\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n  toTomlValue: \"\\\"test string\\\"\"\n",
            "originalContent": "# Test: Type conversion errors - comprehensive testing of all type conversion functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n  {{- if .Values.testToStrings }}\n  toStringsValue: {{ toStrings $testData | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n  {{- if .Values.testToDecimal }}\n  toDecimalValue: {{ toDecimal $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  {{- if .Values.testToJson }}\n  toJsonValue: {{ toJson $testData | quote }}\n  {{- end }}\n  # Test: mustToJson\n  {{- if .Values.testMustToJson }}\n  mustToJsonValue: {{ mustToJson $testData | quote }}\n  {{- end }}\n  # Test: toPrettyJson\n  {{- if .Values.testToPrettyJson }}\n  toPrettyJsonValue: {{ toPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: mustToPrettyJson\n  {{- if .Values.testMustToPrettyJson }}\n  mustToPrettyJsonValue: {{ mustToPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: toRawJson\n  {{- if .Values.testToRawJson }}\n  toRawJsonValue: {{ toRawJson $testData | quote }}\n  {{- end }}\n  # Test: mustToRawJson\n  {{- if .Values.testMustToRawJson }}\n  mustToRawJsonValue: {{ mustToRawJson $testData | quote }}\n  {{- end }}\n  # Test: fromJson\n  {{- if .Values.testFromJson }}\n  {{- $parsed := $testData | fromJson }}\n  fromJsonValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: fromJsonArray\n  {{- if .Values.testFromJsonArray }}\n  {{- $arr := $testData | fromJsonArray }}\n  fromJsonArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  {{- if .Values.testFromYaml }}\n  {{- $parsed := $testData | fromYaml }}\n  fromYamlValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: toYaml\n  {{- if .Values.testToYaml }}\n  toYamlValue: {{ toYaml $testData | quote }}\n  {{- end }}\n  # Test: toYamlPretty\n  {{- if .Values.testToYamlPretty }}\n  toYamlPrettyValue: {{ toYamlPretty $testData | quote }}\n  {{- end }}\n  # Test: fromYamlArray\n  {{- if .Values.testFromYamlArray }}\n  {{- $arr := $testData | fromYamlArray }}\n  fromYamlArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n  {{- if .Values.testToToml }}\n  toTomlValue: {{ toToml $testData | quote }}\n  {{- end }}\n\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-type-conversion-test\r\ndata:\r\n  # ==========================================\r\n  # String conversion functions\r\n  # ==========================================\r\n  # Test: toStrings\r\n\r\n  # ==========================================\r\n  # Decimal conversion functions\r\n  # ==========================================\r\n  # Test: toDecimal\r\n\r\n  # ==========================================\r\n  # JSON conversion functions\r\n  # ==========================================\r\n  # Test: toJson\r\n  # Test: mustToJson\r\n  # Test: toPrettyJson\r\n  # Test: mustToPrettyJson\r\n  # Test: toRawJson\r\n  # Test: mustToRawJson\r\n  # Test: fromJson\r\n  # Test: fromJsonArray\r\n\r\n  # ==========================================\r\n  # YAML conversion functions\r\n  # ==========================================\r\n  # Test: fromYaml\r\n  # Test: toYaml\r\n  # Test: toYamlPretty\r\n  # Test: fromYamlArray\r\n\r\n  # ==========================================\r\n  # TOML conversion functions\r\n  # ==========================================\r\n  # Test: toToml\r\n  toTomlValue: \"\\\"test string\\\"\"\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-type-conversion/templates/type-conversion-test.yaml",
            "content": "\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  # Test: mustToJson\n  # Test: toPrettyJson\n  # Test: mustToPrettyJson\n  # Test: toRawJson\n  # Test: mustToRawJson\n  # Test: fromJson\n  # Test: fromJsonArray\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  # Test: toYaml\n  # Test: toYamlPretty\n  # Test: fromYamlArray\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n  toTomlValue: \"\\\"test string\\\"\"\n",
            "originalContent": "# Test: Type conversion errors - comprehensive testing of all type conversion functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n  {{- if .Values.testToStrings }}\n  toStringsValue: {{ toStrings $testData | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n  {{- if .Values.testToDecimal }}\n  toDecimalValue: {{ toDecimal $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  {{- if .Values.testToJson }}\n  toJsonValue: {{ toJson $testData | quote }}\n  {{- end }}\n  # Test: mustToJson\n  {{- if .Values.testMustToJson }}\n  mustToJsonValue: {{ mustToJson $testData | quote }}\n  {{- end }}\n  # Test: toPrettyJson\n  {{- if .Values.testToPrettyJson }}\n  toPrettyJsonValue: {{ toPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: mustToPrettyJson\n  {{- if .Values.testMustToPrettyJson }}\n  mustToPrettyJsonValue: {{ mustToPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: toRawJson\n  {{- if .Values.testToRawJson }}\n  toRawJsonValue: {{ toRawJson $testData | quote }}\n  {{- end }}\n  # Test: mustToRawJson\n  {{- if .Values.testMustToRawJson }}\n  mustToRawJsonValue: {{ mustToRawJson $testData | quote }}\n  {{- end }}\n  # Test: fromJson\n  {{- if .Values.testFromJson }}\n  {{- $parsed := $testData | fromJson }}\n  fromJsonValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: fromJsonArray\n  {{- if .Values.testFromJsonArray }}\n  {{- $arr := $testData | fromJsonArray }}\n  fromJsonArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  {{- if .Values.testFromYaml }}\n  {{- $parsed := $testData | fromYaml }}\n  fromYamlValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: toYaml\n  {{- if .Values.testToYaml }}\n  toYamlValue: {{ toYaml $testData | quote }}\n  {{- end }}\n  # Test: toYamlPretty\n  {{- if .Values.testToYamlPretty }}\n  toYamlPrettyValue: {{ toYamlPretty $testData | quote }}\n  {{- end }}\n  # Test: fromYamlArray\n  {{- if .Values.testFromYamlArray }}\n  {{- $arr := $testData | fromYamlArray }}\n  fromYamlArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n  {{- if .Values.testToToml }}\n  toTomlValue: {{ toToml $testData | quote }}\n  {{- end }}\n\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "resolved": true,
      "stopReason": "",
      "fixChain": []
    },
    {
      "toggle": "toToml",
      "dataType": "numberValue",
      "valuesOptions": {
        "values": [
          "testToToml=true",
          "testDataType=numberValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-type-conversion-test\r\ndata:\r\n  # ==========================================\r\n  # String conversion functions\r\n  # ==========================================\r\n  # Test: toStrings\r\n\r\n  # ==========================================\r\n  # Decimal conversion functions\r\n  # ==========================================\r\n  # Test: toDecimal\r\n\r\n  # ==========================================\r\n  # JSON conversion functions\r\n  # ==========================================\r\n  # Test: toJson\r\n  # Test: mustToJson\r\n  # Test: toPrettyJson\r\n  # Test: mustToPrettyJson\r\n  # Test: toRawJson\r\n  # Test: mustToRawJson\r\n  # Test: fromJson\r\n  # Test: fromJsonArray\r\n\r\n  # ==========================================\r\n  # YAML conversion functions\r\n  # ==========================================\r\n  # Test: fromYaml\r\n  # Test: toYaml\r\n  # Test: toYamlPretty\r\n  # Test: fromYamlArray\r\n\r\n  # ==========================================\r\n  # TOML conversion functions\r\n  # ==========================================\r\n  # Test: toToml\r\n  toTomlValue: \"42.0\"\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-type-conversion/templates/type-conversion-test.yaml",
            "content": "\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  # Test: mustToJson\n  # Test: toPrettyJson\n  # Test: mustToPrettyJson\n  # Test: toRawJson\n  # Test: mustToRawJson\n  # Test: fromJson\n  # Test: fromJsonArray\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  # Test: toYaml\n  # Test: toYamlPretty\n  # Test: fromYamlArray\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n  toTomlValue: \"42.0\"\n",
            "originalContent": "# Test: Type conversion errors - comprehensive testing of all type conversion functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n  {{- if .Values.testToStrings }}\n  toStringsValue: {{ toStrings $testData | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n  {{- if .Values.testToDecimal }}\n  toDecimalValue: {{ toDecimal $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  {{- if .Values.testToJson }}\n  toJsonValue: {{ toJson $testData | quote }}\n  {{- end }}\n  # Test: mustToJson\n  {{- if .Values.testMustToJson }}\n  mustToJsonValue: {{ mustToJson $testData | quote }}\n  {{- end }}\n  # Test: toPrettyJson\n  {{- if .Values.testToPrettyJson }}\n  toPrettyJsonValue: {{ toPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: mustToPrettyJson\n  {{- if .Values.testMustToPrettyJson }}\n  mustToPrettyJsonValue: {{ mustToPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: toRawJson\n  {{- if .Values.testToRawJson }}\n  toRawJsonValue: {{ toRawJson $testData | quote }}\n  {{- end }}\n  # Test: mustToRawJson\n  {{- if .Values.testMustToRawJson }}\n  mustToRawJsonValue: {{ mustToRawJson $testData | quote }}\n  {{- end }}\n  # Test: fromJson\n  {{- if .Values.testFromJson }}\n  {{- $parsed := $testData | fromJson }}\n  fromJsonValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: fromJsonArray\n  {{- if .Values.testFromJsonArray }}\n  {{- $arr := $testData | fromJsonArray }}\n  fromJsonArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  {{- if .Values.testFromYaml }}\n  {{- $parsed := $testData | fromYaml }}\n  fromYamlValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: toYaml\n  {{- if .Values.testToYaml }}\n  toYamlValue: {{ toYaml $testData | quote }}\n  {{- end }}\n  # Test: toYamlPretty\n  {{- if .Values.testToYamlPretty }}\n  toYamlPrettyValue: {{ toYamlPretty $testData | quote }}\n  {{- end }}\n  # Test: fromYamlArray\n  {{- if .Values.testFromYamlArray }}\n  {{- $arr := $testData | fromYamlArray }}\n  fromYamlArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n  {{- if .Values.testToToml }}\n  toTomlValue: {{ toToml $testData | quote }}\n  {{- end }}\n\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-type-conversion-test\r\ndata:\r\n  # ==========================================\r\n  # String conversion functions\r\n  # ==========================================\r\n  # Test: toStrings\r\n\r\n  # ==========================================\r\n  # Decimal conversion functions\r\n  # ==========================================\r\n  # Test: toDecimal\r\n\r\n  # ==========================================\r\n  # JSON conversion functions\r\n  # ==========================================\r\n  # Test: toJson\r\n  # Test: mustToJson\r\n  # Test: toPrettyJson\r\n  # Test: mustToPrettyJson\r\n  # Test: toRawJson\r\n  # Test: mustToRawJson\r\n  # Test: fromJson\r\n  # Test: fromJsonArray\r\n\r\n  # ==========================================\r\n  # YAML conversion functions\r\n  # ==========================================\r\n  # Test: fromYaml\r\n  # Test: toYaml\r\n  # Test: toYamlPretty\r\n  # Test: fromYamlArray\r\n\r\n  # ==========================================\r\n  # TOML conversion functions\r\n  # ==========================================\r\n  # Test: toToml\r\n  toTomlValue: \"42.0\"\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-type-conversion/templates/type-conversion-test.yaml",
            "content": "\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  # Test: mustToJson\n  # Test: toPrettyJson\n  # Test: mustToPrettyJson\n  # Test: toRawJson\n  # Test: mustToRawJson\n  # Test: fromJson\n  # Test: fromJsonArray\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  # Test: toYaml\n  # Test: toYamlPretty\n  # Test: fromYamlArray\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n  toTomlValue: \"42.0\"\n",
            "originalContent": "# Test: Type conversion errors - comprehensive testing of all type conversion functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n  {{- if .Values.testToStrings }}\n  toStringsValue: {{ toStrings $testData | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n  {{- if .Values.testToDecimal }}\n  toDecimalValue: {{ toDecimal $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  {{- if .Values.testToJson }}\n  toJsonValue: {{ toJson $testData | quote }}\n  {{- end }}\n  # Test: mustToJson\n  {{- if .Values.testMustToJson }}\n  mustToJsonValue: {{ mustToJson $testData | quote }}\n  {{- end }}\n  # Test: toPrettyJson\n  {{- if .Values.testToPrettyJson }}\n  toPrettyJsonValue: {{ toPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: mustToPrettyJson\n  {{- if .Values.testMustToPrettyJson }}\n  mustToPrettyJsonValue: {{ mustToPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: toRawJson\n  {{- if .Values.testToRawJson }}\n  toRawJsonValue: {{ toRawJson $testData | quote }}\n  {{- end }}\n  # Test: mustToRawJson\n  {{- if .Values.testMustToRawJson }}\n  mustToRawJsonValue: {{ mustToRawJson $testData | quote }}\n  {{- end }}\n  # Test: fromJson\n  {{- if .Values.testFromJson }}\n  {{- $parsed := $testData | fromJson }}\n  fromJsonValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: fromJsonArray\n  {{- if .Values.testFromJsonArray }}\n  {{- $arr := $testData | fromJsonArray }}\n  fromJsonArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  {{- if .Values.testFromYaml }}\n  {{- $parsed := $testData | fromYaml }}\n  fromYamlValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: toYaml\n  {{- if .Values.testToYaml }}\n  toYamlValue: {{ toYaml $testData | quote }}\n  {{- end }}\n  # Test: toYamlPretty\n  {{- if .Values.testToYamlPretty }}\n  toYamlPrettyValue: {{ toYamlPretty $testData | quote }}\n  {{- end }}\n  # Test: fromYamlArray\n  {{- if .Values.testFromYamlArray }}\n  {{- $arr := $testData | fromYamlArray }}\n  fromYamlArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n  {{- if .Values.testToToml }}\n  toTomlValue: {{ toToml $testData | quote }}\n  {{- end }}\n\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "resolved": true,
      "stopReason": "",
      "fixChain": []
    },
    {
      "toggle": "toToml",
      "dataType": "boolValue",
      "valuesOptions": {
        "values": [
          "testToToml=true",
          "testDataType=boolValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-type-conversion-test\r\ndata:\r\n  # ==========================================\r\n  # String conversion functions\r\n  # ==========================================\r\n  # Test: toStrings\r\n\r\n  # ==========================================\r\n  # Decimal conversion functions\r\n  # ==========================================\r\n  # Test: toDecimal\r\n\r\n  # ==========================================\r\n  # JSON conversion functions\r\n  # ==========================================\r\n  # Test: toJson\r\n  # Test: mustToJson\r\n  # Test: toPrettyJson\r\n  # Test: mustToPrettyJson\r\n  # Test: toRawJson\r\n  # Test: mustToRawJson\r\n  # Test: fromJson\r\n  # Test: fromJsonArray\r\n\r\n  # ==========================================\r\n  # YAML conversion functions\r\n  # ==========================================\r\n  # Test: fromYaml\r\n  # Test: toYaml\r\n  # Test: toYamlPretty\r\n  # Test: fromYamlArray\r\n\r\n  # ==========================================\r\n  # TOML conversion functions\r\n  # ==========================================\r\n  # Test: toToml\r\n  toTomlValue: \"true\"\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-type-conversion/templates/type-conversion-test.yaml",
            "content": "\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  # Test: mustToJson\n  # Test: toPrettyJson\n  # Test: mustToPrettyJson\n  # Test: toRawJson\n  # Test: mustToRawJson\n  # Test: fromJson\n  # Test: fromJsonArray\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  # Test: toYaml\n  # Test: toYamlPretty\n  # Test: fromYamlArray\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n  toTomlValue: \"true\"\n",
            "originalContent": "# Test: Type conversion errors - comprehensive testing of all type conversion functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n  {{- if .Values.testToStrings }}\n  toStringsValue: {{ toStrings $testData | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n  {{- if .Values.testToDecimal }}\n  toDecimalValue: {{ toDecimal $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  {{- if .Values.testToJson }}\n  toJsonValue: {{ toJson $testData | quote }}\n  {{- end }}\n  # Test: mustToJson\n  {{- if .Values.testMustToJson }}\n  mustToJsonValue: {{ mustToJson $testData | quote }}\n  {{- end }}\n  # Test: toPrettyJson\n  {{- if .Values.testToPrettyJson }}\n  toPrettyJsonValue: {{ toPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: mustToPrettyJson\n  {{- if .Values.testMustToPrettyJson }}\n  mustToPrettyJsonValue: {{ mustToPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: toRawJson\n  {{- if .Values.testToRawJson }}\n  toRawJsonValue: {{ toRawJson $testData | quote }}\n  {{- end }}\n  # Test: mustToRawJson\n  {{- if .Values.testMustToRawJson }}\n  mustToRawJsonValue: {{ mustToRawJson $testData | quote }}\n  {{- end }}\n  # Test: fromJson\n  {{- if .Values.testFromJson }}\n  {{- $parsed := $testData | fromJson }}\n  fromJsonValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: fromJsonArray\n  {{- if .Values.testFromJsonArray }}\n  {{- $arr := $testData | fromJsonArray }}\n  fromJsonArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  {{- if .Values.testFromYaml }}\n  {{- $parsed := $testData | fromYaml }}\n  fromYamlValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: toYaml\n  {{- if .Values.testToYaml }}\n  toYamlValue: {{ toYaml $testData | quote }}\n  {{- end }}\n  # Test: toYamlPretty\n  {{- if .Values.testToYamlPretty }}\n  toYamlPrettyValue: {{ toYamlPretty $testData | quote }}\n  {{- end }}\n  # Test: fromYamlArray\n  {{- if .Values.testFromYamlArray }}\n  {{- $arr := $testData | fromYamlArray }}\n  fromYamlArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n  {{- if .Values.testToToml }}\n  toTomlValue: {{ toToml $testData | quote }}\n  {{- end }}\n\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-type-conversion-test\r\ndata:\r\n  # ==========================================\r\n  # String conversion functions\r\n  # ==========================================\r\n  # Test: toStrings\r\n\r\n  # ==========================================\r\n  # Decimal conversion functions\r\n  # ==========================================\r\n  # Test: toDecimal\r\n\r\n  # ==========================================\r\n  # JSON conversion functions\r\n  # ==========================================\r\n  # Test: toJson\r\n  # Test: mustToJson\r\n  # Test: toPrettyJson\r\n  # Test: mustToPrettyJson\r\n  # Test: toRawJson\r\n  # Test: mustToRawJson\r\n  # Test: fromJson\r\n  # Test: fromJsonArray\r\n\r\n  # ==========================================\r\n  # YAML conversion functions\r\n  # ==========================================\r\n  # Test: fromYaml\r\n  # Test: toYaml\r\n  # Test: toYamlPretty\r\n  # Test: fromYamlArray\r\n\r\n  # ==========================================\r\n  # TOML conversion functions\r\n  # ==========================================\r\n  # Test: toToml\r\n  toTomlValue: \"true\"\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-type-conversion/templates/type-conversion-test.yaml",
            "content": "\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  # Test: mustToJson\n  # Test: toPrettyJson\n  # Test: mustToPrettyJson\n  # Test: toRawJson\n  # Test: mustToRawJson\n  # Test: fromJson\n  # Test: fromJsonArray\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  # Test: toYaml\n  # Test: toYamlPretty\n  # Test: fromYamlArray\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n  toTomlValue: \"true\"\n",
            "originalContent": "# Test: Type conversion errors - comprehensive testing of all type conversion functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n  {{- if .Values.testToStrings }}\n  toStringsValue: {{ toStrings $testData | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n  {{- if .Values.testToDecimal }}\n  toDecimalValue: {{ toDecimal $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  {{- if .Values.testToJson }}\n  toJsonValue: {{ toJson $testData | quote }}\n  {{- end }}\n  # Test: mustToJson\n  {{- if .Values.testMustToJson }}\n  mustToJsonValue: {{ mustToJson $testData | quote }}\n  {{- end }}\n  # Test: toPrettyJson\n  {{- if .Values.testToPrettyJson }}\n  toPrettyJsonValue: {{ toPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: mustToPrettyJson\n  {{- if .Values.testMustToPrettyJson }}\n  mustToPrettyJsonValue: {{ mustToPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: toRawJson\n  {{- if .Values.testToRawJson }}\n  toRawJsonValue: {{ toRawJson $testData | quote }}\n  {{- end }}\n  # Test: mustToRawJson\n  {{- if .Values.testMustToRawJson }}\n  mustToRawJsonValue: {{ mustToRawJson $testData | quote }}\n  {{- end }}\n  # Test: fromJson\n  {{- if .Values.testFromJson }}\n  {{- $parsed := $testData | fromJson }}\n  fromJsonValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: fromJsonArray\n  {{- if .Values.testFromJsonArray }}\n  {{- $arr := $testData | fromJsonArray }}\n  fromJsonArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  {{- if .Values.testFromYaml }}\n  {{- $parsed := $testData | fromYaml }}\n  fromYamlValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: toYaml\n  {{- if .Values.testToYaml }}\n  toYamlValue: {{ toYaml $testData | quote }}\n  {{- end }}\n  # Test: toYamlPretty\n  {{- if .Values.testToYamlPretty }}\n  toYamlPrettyValue: {{ toYamlPretty $testData | quote }}\n  {{- end }}\n  # Test: fromYamlArray\n  {{- if .Values.testFromYamlArray }}\n  {{- $arr := $testData | fromYamlArray }}\n  fromYamlArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n  {{- if .Values.testToToml }}\n  toTomlValue: {{ toToml $testData | quote }}\n  {{- end }}\n\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "resolved": true,
      "stopReason": "",
      "fixChain": []
    },
    {
      "toggle": "toToml",
      "dataType": "listValue",
      "valuesOptions": {
        "values": [
          "testToToml=true",
          "testDataType=listValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-type-conversion-test\r\ndata:\r\n  # ==========================================\r\n  # String conversion functions\r\n  # ==========================================\r\n  # Test: toStrings\r\n\r\n  # ==========================================\r\n  # Decimal conversion functions\r\n  # ==========================================\r\n  # Test: toDecimal\r\n\r\n  # ==========================================\r\n  # JSON conversion functions\r\n  # ==========================================\r\n  # Test: toJson\r\n  # Test: mustToJson\r\n  # Test: toPrettyJson\r\n  # Test: mustToPrettyJson\r\n  # Test: toRawJson\r\n  # Test: mustToRawJson\r\n  # Test: fromJson\r\n  # Test: fromJsonArray\r\n\r\n  # ==========================================\r\n  # YAML conversion functions\r\n  # ==========================================\r\n  # Test: fromYaml\r\n  # Test: toYaml\r\n  # Test: toYamlPretty\r\n  # Test: fromYamlArray\r\n\r\n  # ==========================================\r\n  # TOML conversion functions\r\n  # ==========================================\r\n  # Test: toToml\r\n  toTomlValue: \"[1.0, 2.0, 3.0]\"\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-type-conversion/templates/type-conversion-test.yaml",
            "content": "\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  # Test: mustToJson\n  # Test: toPrettyJson\n  # Test: mustToPrettyJson\n  # Test: toRawJson\n  # Test: mustToRawJson\n  # Test: fromJson\n  # Test: fromJsonArray\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  # Test: toYaml\n  # Test: toYamlPretty\n  # Test: fromYamlArray\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n  toTomlValue: \"[1.0, 2.0, 3.0]\"\n",
            "originalContent": "# Test: Type conversion errors - comprehensive testing of all type conversion functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n  {{- if .Values.testToStrings }}\n  toStringsValue: {{ toStrings $testData | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n  {{- if .Values.testToDecimal }}\n  toDecimalValue: {{ toDecimal $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  {{- if .Values.testToJson }}\n  toJsonValue: {{ toJson $testData | quote }}\n  {{- end }}\n  # Test: mustToJson\n  {{- if .Values.testMustToJson }}\n  mustToJsonValue: {{ mustToJson $testData | quote }}\n  {{- end }}\n  # Test: toPrettyJson\n  {{- if .Values.testToPrettyJson }}\n  toPrettyJsonValue: {{ toPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: mustToPrettyJson\n  {{- if .Values.testMustToPrettyJson }}\n  mustToPrettyJsonValue: {{ mustToPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: toRawJson\n  {{- if .Values.testToRawJson }}\n  toRawJsonValue: {{ toRawJson $testData | quote }}\n  {{- end }}\n  # Test: mustToRawJson\n  {{- if .Values.testMustToRawJson }}\n  mustToRawJsonValue: {{ mustToRawJson $testData | quote }}\n  {{- end }}\n  # Test: fromJson\n  {{- if .Values.testFromJson }}\n  {{- $parsed := $testData | fromJson }}\n  fromJsonValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: fromJsonArray\n  {{- if .Values.testFromJsonArray }}\n  {{- $arr := $testData | fromJsonArray }}\n  fromJsonArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  {{- if .Values.testFromYaml }}\n  {{- $parsed := $testData | fromYaml }}\n  fromYamlValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: toYaml\n  {{- if .Values.testToYaml }}\n  toYamlValue: {{ toYaml $testData | quote }}\n  {{- end }}\n  # Test: toYamlPretty\n  {{- if .Values.testToYamlPretty }}\n  toYamlPrettyValue: {{ toYamlPretty $testData | quote }}\n  {{- end }}\n  # Test: fromYamlArray\n  {{- if .Values.testFromYamlArray }}\n  {{- $arr := $testData | fromYamlArray }}\n  fromYamlArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n  {{- if .Values.testToToml }}\n  toTomlValue: {{ toToml $testData | quote }}\n  {{- end }}\n\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-type-conversion-test\r\ndata:\r\n  # ==========================================\r\n  # String conversion functions\r\n  # ==========================================\r\n  # Test: toStrings\r\n\r\n  # ==========================================\r\n  # Decimal conversion functions\r\n  # ==========================================\r\n  # Test: toDecimal\r\n\r\n  # ==========================================\r\n  # JSON conversion functions\r\n  # ==========================================\r\n  # Test: toJson\r\n  # Test: mustToJson\r\n  # Test: toPrettyJson\r\n  # Test: mustToPrettyJson\r\n  # Test: toRawJson\r\n  # Test: mustToRawJson\r\n  # Test: fromJson\r\n  # Test: fromJsonArray\r\n\r\n  # ==========================================\r\n  # YAML conversion functions\r\n  # ==========================================\r\n  # Test: fromYaml\r\n  # Test: toYaml\r\n  # Test: toYamlPretty\r\n  # Test: fromYamlArray\r\n\r\n  # ==========================================\r\n  # TOML conversion functions\r\n  # ==========================================\r\n  # Test: toToml\r\n  toTomlValue: \"[1.0, 2.0, 3.0]\"\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-type-conversion/templates/type-conversion-test.yaml",
            "content": "\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  # Test: mustToJson\n  # Test: toPrettyJson\n  # Test: mustToPrettyJson\n  # Test: toRawJson\n  # Test: mustToRawJson\n  # Test: fromJson\n  # Test: fromJsonArray\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  # Test: toYaml\n  # Test: toYamlPretty\n  # Test: fromYamlArray\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n  toTomlValue: \"[1.0, 2.0, 3.0]\"\n",
            "originalContent": "# Test: Type conversion errors - comprehensive testing of all type conversion functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n  {{- if .Values.testToStrings }}\n  toStringsValue: {{ toStrings $testData | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n  {{- if .Values.testToDecimal }}\n  toDecimalValue: {{ toDecimal $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  {{- if .Values.testToJson }}\n  toJsonValue: {{ toJson $testData | quote }}\n  {{- end }}\n  # Test: mustToJson\n  {{- if .Values.testMustToJson }}\n  mustToJsonValue: {{ mustToJson $testData | quote }}\n  {{- end }}\n  # Test: toPrettyJson\n  {{- if .Values.testToPrettyJson }}\n  toPrettyJsonValue: {{ toPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: mustToPrettyJson\n  {{- if .Values.testMustToPrettyJson }}\n  mustToPrettyJsonValue: {{ mustToPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: toRawJson\n  {{- if .Values.testToRawJson }}\n  toRawJsonValue: {{ toRawJson $testData | quote }}\n  {{- end }}\n  # Test: mustToRawJson\n  {{- if .Values.testMustToRawJson }}\n  mustToRawJsonValue: {{ mustToRawJson $testData | quote }}\n  {{- end }}\n  # Test: fromJson\n  {{- if .Values.testFromJson }}\n  {{- $parsed := $testData | fromJson }}\n  fromJsonValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: fromJsonArray\n  {{- if .Values.testFromJsonArray }}\n  {{- $arr := $testData | fromJsonArray }}\n  fromJsonArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  {{- if .Values.testFromYaml }}\n  {{- $parsed := $testData | fromYaml }}\n  fromYamlValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: toYaml\n  {{- if .Values.testToYaml }}\n  toYamlValue: {{ toYaml $testData | quote }}\n  {{- end }}\n  # Test: toYamlPretty\n  {{- if .Values.testToYamlPretty }}\n  toYamlPrettyValue: {{ toYamlPretty $testData | quote }}\n  {{- end }}\n  # Test: fromYamlArray\n  {{- if .Values.testFromYamlArray }}\n  {{- $arr := $testData | fromYamlArray }}\n  fromYamlArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n  {{- if .Values.testToToml }}\n  toTomlValue: {{ toToml $testData | quote }}\n  {{- end }}\n\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "resolved": true,
      "stopReason": "",
      "fixChain": []
    },
    {
      "toggle": "toToml",
      "dataType": "dictValue",
      "valuesOptions": {
        "values": [
          "testToToml=true",
          "testDataType=dictValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-type-conversion-test\r\ndata:\r\n  # ==========================================\r\n  # String conversion functions\r\n  # ==========================================\r\n  # Test: toStrings\r\n\r\n  # ==========================================\r\n  # Decimal conversion functions\r\n  # ==========================================\r\n  # Test: toDecimal\r\n\r\n  # ==========================================\r\n  # JSON conversion functions\r\n  # ==========================================\r\n  # Test: toJson\r\n  # Test: mustToJson\r\n  # Test: toPrettyJson\r\n  # Test: mustToPrettyJson\r\n  # Test: toRawJson\r\n  # Test: mustToRawJson\r\n  # Test: fromJson\r\n  # Test: fromJsonArray\r\n\r\n  # ==========================================\r\n  # YAML conversion functions\r\n  # ==========================================\r\n  # Test: fromYaml\r\n  # Test: toYaml\r\n  # Test: toYamlPretty\r\n  # Test: fromYamlArray\r\n\r\n  # ==========================================\r\n  # TOML conversion functions\r\n  # ==========================================\r\n  # Test: toToml\r\n  toTomlValue: \"key = \\\"value\\\"\\n\"\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-type-conversion/templates/type-conversion-test.yaml",
            "content": "\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  # Test: mustToJson\n  # Test: toPrettyJson\n  # Test: mustToPrettyJson\n  # Test: toRawJson\n  # Test: mustToRawJson\n  # Test: fromJson\n  # Test: fromJsonArray\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  # Test: toYaml\n  # Test: toYamlPretty\n  # Test: fromYamlArray\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n  toTomlValue: \"key = \\\"value\\\"\\n\"\n",
            "originalContent": "# Test: Type conversion errors - comprehensive testing of all type conversion functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n  {{- if .Values.testToStrings }}\n  toStringsValue: {{ toStrings $testData | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n  {{- if .Values.testToDecimal }}\n  toDecimalValue: {{ toDecimal $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  {{- if .Values.testToJson }}\n  toJsonValue: {{ toJson $testData | quote }}\n  {{- end }}\n  # Test: mustToJson\n  {{- if .Values.testMustToJson }}\n  mustToJsonValue: {{ mustToJson $testData | quote }}\n  {{- end }}\n  # Test: toPrettyJson\n  {{- if .Values.testToPrettyJson }}\n  toPrettyJsonValue: {{ toPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: mustToPrettyJson\n  {{- if .Values.testMustToPrettyJson }}\n  mustToPrettyJsonValue: {{ mustToPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: toRawJson\n  {{- if .Values.testToRawJson }}\n  toRawJsonValue: {{ toRawJson $testData | quote }}\n  {{- end }}\n  # Test: mustToRawJson\n  {{- if .Values.testMustToRawJson }}\n  mustToRawJsonValue: {{ mustToRawJson $testData | quote }}\n  {{- end }}\n  # Test: fromJson\n  {{- if .Values.testFromJson }}\n  {{- $parsed := $testData | fromJson }}\n  fromJsonValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: fromJsonArray\n  {{- if .Values.testFromJsonArray }}\n  {{- $arr := $testData | fromJsonArray }}\n  fromJsonArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  {{- if .Values.testFromYaml }}\n  {{- $parsed := $testData | fromYaml }}\n  fromYamlValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: toYaml\n  {{- if .Values.testToYaml }}\n  toYamlValue: {{ toYaml $testData | quote }}\n  {{- end }}\n  # Test: toYamlPretty\n  {{- if .Values.testToYamlPretty }}\n  toYamlPrettyValue: {{ toYamlPretty $testData | quote }}\n  {{- end }}\n  # Test: fromYamlArray\n  {{- if .Values.testFromYamlArray }}\n  {{- $arr := $testData | fromYamlArray }}\n  fromYamlArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n  {{- if .Values.testToToml }}\n  toTomlValue: {{ toToml $testData | quote }}\n  {{- end }}\n\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-type-conversion-test\r\ndata:\r\n  # ==========================================\r\n  # String conversion functions\r\n  # ==========================================\r\n  # Test: toStrings\r\n\r\n  # ==========================================\r\n  # Decimal conversion functions\r\n  # ==========================================\r\n  # Test: toDecimal\r\n\r\n  # ==========================================\r\n  # JSON conversion functions\r\n  # ==========================================\r\n  # Test: toJson\r\n  # Test: mustToJson\r\n  # Test: toPrettyJson\r\n  # Test: mustToPrettyJson\r\n  # Test: toRawJson\r\n  # Test: mustToRawJson\r\n  # Test: fromJson\r\n  # Test: fromJsonArray\r\n\r\n  # ==========================================\r\n  # YAML conversion functions\r\n  # ==========================================\r\n  # Test: fromYaml\r\n  # Test: toYaml\r\n  # Test: toYamlPretty\r\n  # Test: fromYamlArray\r\n\r\n  # ==========================================\r\n  # TOML conversion functions\r\n  # ==========================================\r\n  # Test: toToml\r\n  toTomlValue: \"key = \\\"value\\\"\\n\"\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-type-conversion/templates/type-conversion-test.yaml",
            "content": "\n# Source: test-type-conversion/templates/type-conversion-test.yaml\n# Test: Type conversion errors - comprehensive testing of all type conversion functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  # Test: mustToJson\n  # Test: toPrettyJson\n  # Test: mustToPrettyJson\n  # Test: toRawJson\n  # Test: mustToRawJson\n  # Test: fromJson\n  # Test: fromJsonArray\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  # Test: toYaml\n  # Test: toYamlPretty\n  # Test: fromYamlArray\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n  toTomlValue: \"key = \\\"value\\\"\\n\"\n",
            "originalContent": "# Test: Type conversion errors - comprehensive testing of all type conversion functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-type-conversion-test\ndata:\n  # ==========================================\n  # String conversion functions\n  # ==========================================\n  # Test: toStrings\n  {{- if .Values.testToStrings }}\n  toStringsValue: {{ toStrings $testData | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # Decimal conversion functions\n  # ==========================================\n  # Test: toDecimal\n  {{- if .Values.testToDecimal }}\n  toDecimalValue: {{ toDecimal $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # JSON conversion functions\n  # ==========================================\n  # Test: toJson\n  {{- if .Values.testToJson }}\n  toJsonValue: {{ toJson $testData | quote }}\n  {{- end }}\n  # Test: mustToJson\n  {{- if .Values.testMustToJson }}\n  mustToJsonValue: {{ mustToJson $testData | quote }}\n  {{- end }}\n  # Test: toPrettyJson\n  {{- if .Values.testToPrettyJson }}\n  toPrettyJsonValue: {{ toPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: mustToPrettyJson\n  {{- if .Values.testMustToPrettyJson }}\n  mustToPrettyJsonValue: {{ mustToPrettyJson $testData | quote }}\n  {{- end }}\n  # Test: toRawJson\n  {{- if .Values.testToRawJson }}\n  toRawJsonValue: {{ toRawJson $testData | quote }}\n  {{- end }}\n  # Test: mustToRawJson\n  {{- if .Values.testMustToRawJson }}\n  mustToRawJsonValue: {{ mustToRawJson $testData | quote }}\n  {{- end }}\n  # Test: fromJson\n  {{- if .Values.testFromJson }}\n  {{- $parsed := $testData | fromJson }}\n  fromJsonValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: fromJsonArray\n  {{- if .Values.testFromJsonArray }}\n  {{- $arr := $testData | fromJsonArray }}\n  fromJsonArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # YAML conversion functions\n  # ==========================================\n  # Test: fromYaml\n  {{- if .Values.testFromYaml }}\n  {{- $parsed := $testData | fromYaml }}\n  fromYamlValue: {{ $parsed | toJson }}\n  {{- end }}\n  # Test: toYaml\n  {{- if .Values.testToYaml }}\n  toYamlValue: {{ toYaml $testData | quote }}\n  {{- end }}\n  # Test: toYamlPretty\n  {{- if .Values.testToYamlPretty }}\n  toYamlPrettyValue: {{ toYamlPretty $testData | quote }}\n  {{- end }}\n  # Test: fromYamlArray\n  {{- if .Values.testFromYamlArray }}\n  {{- $arr := $testData | fromYamlArray }}\n  fromYamlArrayValue: {{ $arr | toJson }}\n  {{- end }}\n\n  # ==========================================\n  # TOML conversion functions\n  # ==========================================\n  # Test: toToml\n  {{- if .Values.testToToml }}\n  toTomlValue: {{ toToml $testData | quote }}\n  {{- end }}\n\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "resolved": true,
      "stopReason": "",
      "fixChain": []
    }
  ]
}
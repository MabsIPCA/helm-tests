{
  "suite": "render_problems",
  "testNumber": 20,
  "testName": "Reflection Function Errors",
  "chartPath": "..\\render_problems\\test-20-reflection-functions",
  "renders": [
    {
      "toggle": "kindOf",
      "dataType": "nilValue",
      "valuesOptions": {
        "values": [
          "testKindOf=true",
          "testDataType=nilValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-reflection/templates/reflection-test.yaml\n# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-reflection-test\r\ndata:\r\n  # ==========================================\r\n  # Kind functions\r\n  # ==========================================\r\n  # Test: kindOf - returns the kind of a value\r\n  kindOfValue: \"invalid\"\r\n  # Test: kindIs - checks if value is of a specific kind\r\n\r\n  # ==========================================\r\n  # Type functions\r\n  # ==========================================\r\n  # Test: typeOf - returns the type of a value\r\n  # Test: typeIs - checks if value is of a specific type\r\n  # Test: typeIsLike - checks if value is like a specific type\r\n\r\n  # ==========================================\r\n  # Comparison functions\r\n  # ==========================================\r\n  # Test: deepEqual - compares two values for deep equality\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-reflection/templates/reflection-test.yaml",
            "content": "\n# Source: test-reflection/templates/reflection-test.yaml\n# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-reflection-test\ndata:\n  # ==========================================\n  # Kind functions\n  # ==========================================\n  # Test: kindOf - returns the kind of a value\n  kindOfValue: \"invalid\"\n  # Test: kindIs - checks if value is of a specific kind\n\n  # ==========================================\n  # Type functions\n  # ==========================================\n  # Test: typeOf - returns the type of a value\n  # Test: typeIs - checks if value is of a specific type\n  # Test: typeIsLike - checks if value is like a specific type\n\n  # ==========================================\n  # Comparison functions\n  # ==========================================\n  # Test: deepEqual - compares two values for deep equality\n",
            "originalContent": "# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-reflection-test\ndata:\n  # ==========================================\n  # Kind functions\n  # ==========================================\n  # Test: kindOf - returns the kind of a value\n  {{- if .Values.testKindOf }}\n  kindOfValue: {{ kindOf $testData | quote }}\n  {{- end }}\n  # Test: kindIs - checks if value is of a specific kind\n  {{- if .Values.testKindIs }}\n  kindIsValue: {{ kindIs \"string\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Type functions\n  # ==========================================\n  # Test: typeOf - returns the type of a value\n  {{- if .Values.testTypeOf }}\n  typeOfValue: {{ typeOf $testData | quote }}\n  {{- end }}\n  # Test: typeIs - checks if value is of a specific type\n  {{- if .Values.testTypeIs }}\n  typeIsValue: {{ typeIs \"string\" $testData | quote }}\n  {{- end }}\n  # Test: typeIsLike - checks if value is like a specific type\n  {{- if .Values.testTypeIsLike }}\n  typeIsLikeValue: {{ typeIsLike \"string\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Comparison functions\n  # ==========================================\n  # Test: deepEqual - compares two values for deep equality\n  {{- if .Values.testDeepEqual }}\n  deepEqualValue: {{ deepEqual $testData .Values.compareValue | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-reflection/templates/reflection-test.yaml\n# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-reflection-test\r\ndata:\r\n  # ==========================================\r\n  # Kind functions\r\n  # ==========================================\r\n  # Test: kindOf - returns the kind of a value\r\n  kindOfValue: \"invalid\"\r\n  # Test: kindIs - checks if value is of a specific kind\r\n\r\n  # ==========================================\r\n  # Type functions\r\n  # ==========================================\r\n  # Test: typeOf - returns the type of a value\r\n  # Test: typeIs - checks if value is of a specific type\r\n  # Test: typeIsLike - checks if value is like a specific type\r\n\r\n  # ==========================================\r\n  # Comparison functions\r\n  # ==========================================\r\n  # Test: deepEqual - compares two values for deep equality\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-reflection/templates/reflection-test.yaml",
            "content": "\n# Source: test-reflection/templates/reflection-test.yaml\n# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-reflection-test\ndata:\n  # ==========================================\n  # Kind functions\n  # ==========================================\n  # Test: kindOf - returns the kind of a value\n  kindOfValue: \"invalid\"\n  # Test: kindIs - checks if value is of a specific kind\n\n  # ==========================================\n  # Type functions\n  # ==========================================\n  # Test: typeOf - returns the type of a value\n  # Test: typeIs - checks if value is of a specific type\n  # Test: typeIsLike - checks if value is like a specific type\n\n  # ==========================================\n  # Comparison functions\n  # ==========================================\n  # Test: deepEqual - compares two values for deep equality\n",
            "originalContent": "# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-reflection-test\ndata:\n  # ==========================================\n  # Kind functions\n  # ==========================================\n  # Test: kindOf - returns the kind of a value\n  {{- if .Values.testKindOf }}\n  kindOfValue: {{ kindOf $testData | quote }}\n  {{- end }}\n  # Test: kindIs - checks if value is of a specific kind\n  {{- if .Values.testKindIs }}\n  kindIsValue: {{ kindIs \"string\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Type functions\n  # ==========================================\n  # Test: typeOf - returns the type of a value\n  {{- if .Values.testTypeOf }}\n  typeOfValue: {{ typeOf $testData | quote }}\n  {{- end }}\n  # Test: typeIs - checks if value is of a specific type\n  {{- if .Values.testTypeIs }}\n  typeIsValue: {{ typeIs \"string\" $testData | quote }}\n  {{- end }}\n  # Test: typeIsLike - checks if value is like a specific type\n  {{- if .Values.testTypeIsLike }}\n  typeIsLikeValue: {{ typeIsLike \"string\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Comparison functions\n  # ==========================================\n  # Test: deepEqual - compares two values for deep equality\n  {{- if .Values.testDeepEqual }}\n  deepEqualValue: {{ deepEqual $testData .Values.compareValue | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "resolved": true,
      "stopReason": "",
      "fixChain": []
    },
    {
      "toggle": "kindOf",
      "dataType": "stringValue",
      "valuesOptions": {
        "values": [
          "testKindOf=true",
          "testDataType=stringValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-reflection/templates/reflection-test.yaml\n# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-reflection-test\r\ndata:\r\n  # ==========================================\r\n  # Kind functions\r\n  # ==========================================\r\n  # Test: kindOf - returns the kind of a value\r\n  kindOfValue: \"string\"\r\n  # Test: kindIs - checks if value is of a specific kind\r\n\r\n  # ==========================================\r\n  # Type functions\r\n  # ==========================================\r\n  # Test: typeOf - returns the type of a value\r\n  # Test: typeIs - checks if value is of a specific type\r\n  # Test: typeIsLike - checks if value is like a specific type\r\n\r\n  # ==========================================\r\n  # Comparison functions\r\n  # ==========================================\r\n  # Test: deepEqual - compares two values for deep equality\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-reflection/templates/reflection-test.yaml",
            "content": "\n# Source: test-reflection/templates/reflection-test.yaml\n# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-reflection-test\ndata:\n  # ==========================================\n  # Kind functions\n  # ==========================================\n  # Test: kindOf - returns the kind of a value\n  kindOfValue: \"string\"\n  # Test: kindIs - checks if value is of a specific kind\n\n  # ==========================================\n  # Type functions\n  # ==========================================\n  # Test: typeOf - returns the type of a value\n  # Test: typeIs - checks if value is of a specific type\n  # Test: typeIsLike - checks if value is like a specific type\n\n  # ==========================================\n  # Comparison functions\n  # ==========================================\n  # Test: deepEqual - compares two values for deep equality\n",
            "originalContent": "# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-reflection-test\ndata:\n  # ==========================================\n  # Kind functions\n  # ==========================================\n  # Test: kindOf - returns the kind of a value\n  {{- if .Values.testKindOf }}\n  kindOfValue: {{ kindOf $testData | quote }}\n  {{- end }}\n  # Test: kindIs - checks if value is of a specific kind\n  {{- if .Values.testKindIs }}\n  kindIsValue: {{ kindIs \"string\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Type functions\n  # ==========================================\n  # Test: typeOf - returns the type of a value\n  {{- if .Values.testTypeOf }}\n  typeOfValue: {{ typeOf $testData | quote }}\n  {{- end }}\n  # Test: typeIs - checks if value is of a specific type\n  {{- if .Values.testTypeIs }}\n  typeIsValue: {{ typeIs \"string\" $testData | quote }}\n  {{- end }}\n  # Test: typeIsLike - checks if value is like a specific type\n  {{- if .Values.testTypeIsLike }}\n  typeIsLikeValue: {{ typeIsLike \"string\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Comparison functions\n  # ==========================================\n  # Test: deepEqual - compares two values for deep equality\n  {{- if .Values.testDeepEqual }}\n  deepEqualValue: {{ deepEqual $testData .Values.compareValue | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-reflection/templates/reflection-test.yaml\n# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-reflection-test\r\ndata:\r\n  # ==========================================\r\n  # Kind functions\r\n  # ==========================================\r\n  # Test: kindOf - returns the kind of a value\r\n  kindOfValue: \"string\"\r\n  # Test: kindIs - checks if value is of a specific kind\r\n\r\n  # ==========================================\r\n  # Type functions\r\n  # ==========================================\r\n  # Test: typeOf - returns the type of a value\r\n  # Test: typeIs - checks if value is of a specific type\r\n  # Test: typeIsLike - checks if value is like a specific type\r\n\r\n  # ==========================================\r\n  # Comparison functions\r\n  # ==========================================\r\n  # Test: deepEqual - compares two values for deep equality\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-reflection/templates/reflection-test.yaml",
            "content": "\n# Source: test-reflection/templates/reflection-test.yaml\n# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-reflection-test\ndata:\n  # ==========================================\n  # Kind functions\n  # ==========================================\n  # Test: kindOf - returns the kind of a value\n  kindOfValue: \"string\"\n  # Test: kindIs - checks if value is of a specific kind\n\n  # ==========================================\n  # Type functions\n  # ==========================================\n  # Test: typeOf - returns the type of a value\n  # Test: typeIs - checks if value is of a specific type\n  # Test: typeIsLike - checks if value is like a specific type\n\n  # ==========================================\n  # Comparison functions\n  # ==========================================\n  # Test: deepEqual - compares two values for deep equality\n",
            "originalContent": "# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-reflection-test\ndata:\n  # ==========================================\n  # Kind functions\n  # ==========================================\n  # Test: kindOf - returns the kind of a value\n  {{- if .Values.testKindOf }}\n  kindOfValue: {{ kindOf $testData | quote }}\n  {{- end }}\n  # Test: kindIs - checks if value is of a specific kind\n  {{- if .Values.testKindIs }}\n  kindIsValue: {{ kindIs \"string\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Type functions\n  # ==========================================\n  # Test: typeOf - returns the type of a value\n  {{- if .Values.testTypeOf }}\n  typeOfValue: {{ typeOf $testData | quote }}\n  {{- end }}\n  # Test: typeIs - checks if value is of a specific type\n  {{- if .Values.testTypeIs }}\n  typeIsValue: {{ typeIs \"string\" $testData | quote }}\n  {{- end }}\n  # Test: typeIsLike - checks if value is like a specific type\n  {{- if .Values.testTypeIsLike }}\n  typeIsLikeValue: {{ typeIsLike \"string\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Comparison functions\n  # ==========================================\n  # Test: deepEqual - compares two values for deep equality\n  {{- if .Values.testDeepEqual }}\n  deepEqualValue: {{ deepEqual $testData .Values.compareValue | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "resolved": true,
      "stopReason": "",
      "fixChain": []
    },
    {
      "toggle": "kindOf",
      "dataType": "numberValue",
      "valuesOptions": {
        "values": [
          "testKindOf=true",
          "testDataType=numberValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-reflection/templates/reflection-test.yaml\n# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-reflection-test\r\ndata:\r\n  # ==========================================\r\n  # Kind functions\r\n  # ==========================================\r\n  # Test: kindOf - returns the kind of a value\r\n  kindOfValue: \"float64\"\r\n  # Test: kindIs - checks if value is of a specific kind\r\n\r\n  # ==========================================\r\n  # Type functions\r\n  # ==========================================\r\n  # Test: typeOf - returns the type of a value\r\n  # Test: typeIs - checks if value is of a specific type\r\n  # Test: typeIsLike - checks if value is like a specific type\r\n\r\n  # ==========================================\r\n  # Comparison functions\r\n  # ==========================================\r\n  # Test: deepEqual - compares two values for deep equality\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-reflection/templates/reflection-test.yaml",
            "content": "\n# Source: test-reflection/templates/reflection-test.yaml\n# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-reflection-test\ndata:\n  # ==========================================\n  # Kind functions\n  # ==========================================\n  # Test: kindOf - returns the kind of a value\n  kindOfValue: \"float64\"\n  # Test: kindIs - checks if value is of a specific kind\n\n  # ==========================================\n  # Type functions\n  # ==========================================\n  # Test: typeOf - returns the type of a value\n  # Test: typeIs - checks if value is of a specific type\n  # Test: typeIsLike - checks if value is like a specific type\n\n  # ==========================================\n  # Comparison functions\n  # ==========================================\n  # Test: deepEqual - compares two values for deep equality\n",
            "originalContent": "# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-reflection-test\ndata:\n  # ==========================================\n  # Kind functions\n  # ==========================================\n  # Test: kindOf - returns the kind of a value\n  {{- if .Values.testKindOf }}\n  kindOfValue: {{ kindOf $testData | quote }}\n  {{- end }}\n  # Test: kindIs - checks if value is of a specific kind\n  {{- if .Values.testKindIs }}\n  kindIsValue: {{ kindIs \"string\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Type functions\n  # ==========================================\n  # Test: typeOf - returns the type of a value\n  {{- if .Values.testTypeOf }}\n  typeOfValue: {{ typeOf $testData | quote }}\n  {{- end }}\n  # Test: typeIs - checks if value is of a specific type\n  {{- if .Values.testTypeIs }}\n  typeIsValue: {{ typeIs \"string\" $testData | quote }}\n  {{- end }}\n  # Test: typeIsLike - checks if value is like a specific type\n  {{- if .Values.testTypeIsLike }}\n  typeIsLikeValue: {{ typeIsLike \"string\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Comparison functions\n  # ==========================================\n  # Test: deepEqual - compares two values for deep equality\n  {{- if .Values.testDeepEqual }}\n  deepEqualValue: {{ deepEqual $testData .Values.compareValue | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-reflection/templates/reflection-test.yaml\n# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-reflection-test\r\ndata:\r\n  # ==========================================\r\n  # Kind functions\r\n  # ==========================================\r\n  # Test: kindOf - returns the kind of a value\r\n  kindOfValue: \"float64\"\r\n  # Test: kindIs - checks if value is of a specific kind\r\n\r\n  # ==========================================\r\n  # Type functions\r\n  # ==========================================\r\n  # Test: typeOf - returns the type of a value\r\n  # Test: typeIs - checks if value is of a specific type\r\n  # Test: typeIsLike - checks if value is like a specific type\r\n\r\n  # ==========================================\r\n  # Comparison functions\r\n  # ==========================================\r\n  # Test: deepEqual - compares two values for deep equality\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-reflection/templates/reflection-test.yaml",
            "content": "\n# Source: test-reflection/templates/reflection-test.yaml\n# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-reflection-test\ndata:\n  # ==========================================\n  # Kind functions\n  # ==========================================\n  # Test: kindOf - returns the kind of a value\n  kindOfValue: \"float64\"\n  # Test: kindIs - checks if value is of a specific kind\n\n  # ==========================================\n  # Type functions\n  # ==========================================\n  # Test: typeOf - returns the type of a value\n  # Test: typeIs - checks if value is of a specific type\n  # Test: typeIsLike - checks if value is like a specific type\n\n  # ==========================================\n  # Comparison functions\n  # ==========================================\n  # Test: deepEqual - compares two values for deep equality\n",
            "originalContent": "# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-reflection-test\ndata:\n  # ==========================================\n  # Kind functions\n  # ==========================================\n  # Test: kindOf - returns the kind of a value\n  {{- if .Values.testKindOf }}\n  kindOfValue: {{ kindOf $testData | quote }}\n  {{- end }}\n  # Test: kindIs - checks if value is of a specific kind\n  {{- if .Values.testKindIs }}\n  kindIsValue: {{ kindIs \"string\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Type functions\n  # ==========================================\n  # Test: typeOf - returns the type of a value\n  {{- if .Values.testTypeOf }}\n  typeOfValue: {{ typeOf $testData | quote }}\n  {{- end }}\n  # Test: typeIs - checks if value is of a specific type\n  {{- if .Values.testTypeIs }}\n  typeIsValue: {{ typeIs \"string\" $testData | quote }}\n  {{- end }}\n  # Test: typeIsLike - checks if value is like a specific type\n  {{- if .Values.testTypeIsLike }}\n  typeIsLikeValue: {{ typeIsLike \"string\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Comparison functions\n  # ==========================================\n  # Test: deepEqual - compares two values for deep equality\n  {{- if .Values.testDeepEqual }}\n  deepEqualValue: {{ deepEqual $testData .Values.compareValue | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "resolved": true,
      "stopReason": "",
      "fixChain": []
    },
    {
      "toggle": "kindOf",
      "dataType": "boolValue",
      "valuesOptions": {
        "values": [
          "testKindOf=true",
          "testDataType=boolValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-reflection/templates/reflection-test.yaml\n# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-reflection-test\r\ndata:\r\n  # ==========================================\r\n  # Kind functions\r\n  # ==========================================\r\n  # Test: kindOf - returns the kind of a value\r\n  kindOfValue: \"bool\"\r\n  # Test: kindIs - checks if value is of a specific kind\r\n\r\n  # ==========================================\r\n  # Type functions\r\n  # ==========================================\r\n  # Test: typeOf - returns the type of a value\r\n  # Test: typeIs - checks if value is of a specific type\r\n  # Test: typeIsLike - checks if value is like a specific type\r\n\r\n  # ==========================================\r\n  # Comparison functions\r\n  # ==========================================\r\n  # Test: deepEqual - compares two values for deep equality\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-reflection/templates/reflection-test.yaml",
            "content": "\n# Source: test-reflection/templates/reflection-test.yaml\n# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-reflection-test\ndata:\n  # ==========================================\n  # Kind functions\n  # ==========================================\n  # Test: kindOf - returns the kind of a value\n  kindOfValue: \"bool\"\n  # Test: kindIs - checks if value is of a specific kind\n\n  # ==========================================\n  # Type functions\n  # ==========================================\n  # Test: typeOf - returns the type of a value\n  # Test: typeIs - checks if value is of a specific type\n  # Test: typeIsLike - checks if value is like a specific type\n\n  # ==========================================\n  # Comparison functions\n  # ==========================================\n  # Test: deepEqual - compares two values for deep equality\n",
            "originalContent": "# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-reflection-test\ndata:\n  # ==========================================\n  # Kind functions\n  # ==========================================\n  # Test: kindOf - returns the kind of a value\n  {{- if .Values.testKindOf }}\n  kindOfValue: {{ kindOf $testData | quote }}\n  {{- end }}\n  # Test: kindIs - checks if value is of a specific kind\n  {{- if .Values.testKindIs }}\n  kindIsValue: {{ kindIs \"string\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Type functions\n  # ==========================================\n  # Test: typeOf - returns the type of a value\n  {{- if .Values.testTypeOf }}\n  typeOfValue: {{ typeOf $testData | quote }}\n  {{- end }}\n  # Test: typeIs - checks if value is of a specific type\n  {{- if .Values.testTypeIs }}\n  typeIsValue: {{ typeIs \"string\" $testData | quote }}\n  {{- end }}\n  # Test: typeIsLike - checks if value is like a specific type\n  {{- if .Values.testTypeIsLike }}\n  typeIsLikeValue: {{ typeIsLike \"string\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Comparison functions\n  # ==========================================\n  # Test: deepEqual - compares two values for deep equality\n  {{- if .Values.testDeepEqual }}\n  deepEqualValue: {{ deepEqual $testData .Values.compareValue | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-reflection/templates/reflection-test.yaml\n# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-reflection-test\r\ndata:\r\n  # ==========================================\r\n  # Kind functions\r\n  # ==========================================\r\n  # Test: kindOf - returns the kind of a value\r\n  kindOfValue: \"bool\"\r\n  # Test: kindIs - checks if value is of a specific kind\r\n\r\n  # ==========================================\r\n  # Type functions\r\n  # ==========================================\r\n  # Test: typeOf - returns the type of a value\r\n  # Test: typeIs - checks if value is of a specific type\r\n  # Test: typeIsLike - checks if value is like a specific type\r\n\r\n  # ==========================================\r\n  # Comparison functions\r\n  # ==========================================\r\n  # Test: deepEqual - compares two values for deep equality\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-reflection/templates/reflection-test.yaml",
            "content": "\n# Source: test-reflection/templates/reflection-test.yaml\n# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-reflection-test\ndata:\n  # ==========================================\n  # Kind functions\n  # ==========================================\n  # Test: kindOf - returns the kind of a value\n  kindOfValue: \"bool\"\n  # Test: kindIs - checks if value is of a specific kind\n\n  # ==========================================\n  # Type functions\n  # ==========================================\n  # Test: typeOf - returns the type of a value\n  # Test: typeIs - checks if value is of a specific type\n  # Test: typeIsLike - checks if value is like a specific type\n\n  # ==========================================\n  # Comparison functions\n  # ==========================================\n  # Test: deepEqual - compares two values for deep equality\n",
            "originalContent": "# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-reflection-test\ndata:\n  # ==========================================\n  # Kind functions\n  # ==========================================\n  # Test: kindOf - returns the kind of a value\n  {{- if .Values.testKindOf }}\n  kindOfValue: {{ kindOf $testData | quote }}\n  {{- end }}\n  # Test: kindIs - checks if value is of a specific kind\n  {{- if .Values.testKindIs }}\n  kindIsValue: {{ kindIs \"string\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Type functions\n  # ==========================================\n  # Test: typeOf - returns the type of a value\n  {{- if .Values.testTypeOf }}\n  typeOfValue: {{ typeOf $testData | quote }}\n  {{- end }}\n  # Test: typeIs - checks if value is of a specific type\n  {{- if .Values.testTypeIs }}\n  typeIsValue: {{ typeIs \"string\" $testData | quote }}\n  {{- end }}\n  # Test: typeIsLike - checks if value is like a specific type\n  {{- if .Values.testTypeIsLike }}\n  typeIsLikeValue: {{ typeIsLike \"string\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Comparison functions\n  # ==========================================\n  # Test: deepEqual - compares two values for deep equality\n  {{- if .Values.testDeepEqual }}\n  deepEqualValue: {{ deepEqual $testData .Values.compareValue | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "resolved": true,
      "stopReason": "",
      "fixChain": []
    },
    {
      "toggle": "kindOf",
      "dataType": "listValue",
      "valuesOptions": {
        "values": [
          "testKindOf=true",
          "testDataType=listValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-reflection/templates/reflection-test.yaml\n# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-reflection-test\r\ndata:\r\n  # ==========================================\r\n  # Kind functions\r\n  # ==========================================\r\n  # Test: kindOf - returns the kind of a value\r\n  kindOfValue: \"slice\"\r\n  # Test: kindIs - checks if value is of a specific kind\r\n\r\n  # ==========================================\r\n  # Type functions\r\n  # ==========================================\r\n  # Test: typeOf - returns the type of a value\r\n  # Test: typeIs - checks if value is of a specific type\r\n  # Test: typeIsLike - checks if value is like a specific type\r\n\r\n  # ==========================================\r\n  # Comparison functions\r\n  # ==========================================\r\n  # Test: deepEqual - compares two values for deep equality\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-reflection/templates/reflection-test.yaml",
            "content": "\n# Source: test-reflection/templates/reflection-test.yaml\n# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-reflection-test\ndata:\n  # ==========================================\n  # Kind functions\n  # ==========================================\n  # Test: kindOf - returns the kind of a value\n  kindOfValue: \"slice\"\n  # Test: kindIs - checks if value is of a specific kind\n\n  # ==========================================\n  # Type functions\n  # ==========================================\n  # Test: typeOf - returns the type of a value\n  # Test: typeIs - checks if value is of a specific type\n  # Test: typeIsLike - checks if value is like a specific type\n\n  # ==========================================\n  # Comparison functions\n  # ==========================================\n  # Test: deepEqual - compares two values for deep equality\n",
            "originalContent": "# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-reflection-test\ndata:\n  # ==========================================\n  # Kind functions\n  # ==========================================\n  # Test: kindOf - returns the kind of a value\n  {{- if .Values.testKindOf }}\n  kindOfValue: {{ kindOf $testData | quote }}\n  {{- end }}\n  # Test: kindIs - checks if value is of a specific kind\n  {{- if .Values.testKindIs }}\n  kindIsValue: {{ kindIs \"string\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Type functions\n  # ==========================================\n  # Test: typeOf - returns the type of a value\n  {{- if .Values.testTypeOf }}\n  typeOfValue: {{ typeOf $testData | quote }}\n  {{- end }}\n  # Test: typeIs - checks if value is of a specific type\n  {{- if .Values.testTypeIs }}\n  typeIsValue: {{ typeIs \"string\" $testData | quote }}\n  {{- end }}\n  # Test: typeIsLike - checks if value is like a specific type\n  {{- if .Values.testTypeIsLike }}\n  typeIsLikeValue: {{ typeIsLike \"string\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Comparison functions\n  # ==========================================\n  # Test: deepEqual - compares two values for deep equality\n  {{- if .Values.testDeepEqual }}\n  deepEqualValue: {{ deepEqual $testData .Values.compareValue | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-reflection/templates/reflection-test.yaml\n# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-reflection-test\r\ndata:\r\n  # ==========================================\r\n  # Kind functions\r\n  # ==========================================\r\n  # Test: kindOf - returns the kind of a value\r\n  kindOfValue: \"slice\"\r\n  # Test: kindIs - checks if value is of a specific kind\r\n\r\n  # ==========================================\r\n  # Type functions\r\n  # ==========================================\r\n  # Test: typeOf - returns the type of a value\r\n  # Test: typeIs - checks if value is of a specific type\r\n  # Test: typeIsLike - checks if value is like a specific type\r\n\r\n  # ==========================================\r\n  # Comparison functions\r\n  # ==========================================\r\n  # Test: deepEqual - compares two values for deep equality\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-reflection/templates/reflection-test.yaml",
            "content": "\n# Source: test-reflection/templates/reflection-test.yaml\n# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-reflection-test\ndata:\n  # ==========================================\n  # Kind functions\n  # ==========================================\n  # Test: kindOf - returns the kind of a value\n  kindOfValue: \"slice\"\n  # Test: kindIs - checks if value is of a specific kind\n\n  # ==========================================\n  # Type functions\n  # ==========================================\n  # Test: typeOf - returns the type of a value\n  # Test: typeIs - checks if value is of a specific type\n  # Test: typeIsLike - checks if value is like a specific type\n\n  # ==========================================\n  # Comparison functions\n  # ==========================================\n  # Test: deepEqual - compares two values for deep equality\n",
            "originalContent": "# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-reflection-test\ndata:\n  # ==========================================\n  # Kind functions\n  # ==========================================\n  # Test: kindOf - returns the kind of a value\n  {{- if .Values.testKindOf }}\n  kindOfValue: {{ kindOf $testData | quote }}\n  {{- end }}\n  # Test: kindIs - checks if value is of a specific kind\n  {{- if .Values.testKindIs }}\n  kindIsValue: {{ kindIs \"string\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Type functions\n  # ==========================================\n  # Test: typeOf - returns the type of a value\n  {{- if .Values.testTypeOf }}\n  typeOfValue: {{ typeOf $testData | quote }}\n  {{- end }}\n  # Test: typeIs - checks if value is of a specific type\n  {{- if .Values.testTypeIs }}\n  typeIsValue: {{ typeIs \"string\" $testData | quote }}\n  {{- end }}\n  # Test: typeIsLike - checks if value is like a specific type\n  {{- if .Values.testTypeIsLike }}\n  typeIsLikeValue: {{ typeIsLike \"string\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Comparison functions\n  # ==========================================\n  # Test: deepEqual - compares two values for deep equality\n  {{- if .Values.testDeepEqual }}\n  deepEqualValue: {{ deepEqual $testData .Values.compareValue | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "resolved": true,
      "stopReason": "",
      "fixChain": []
    },
    {
      "toggle": "kindOf",
      "dataType": "dictValue",
      "valuesOptions": {
        "values": [
          "testKindOf=true",
          "testDataType=dictValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-reflection/templates/reflection-test.yaml\n# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-reflection-test\r\ndata:\r\n  # ==========================================\r\n  # Kind functions\r\n  # ==========================================\r\n  # Test: kindOf - returns the kind of a value\r\n  kindOfValue: \"map\"\r\n  # Test: kindIs - checks if value is of a specific kind\r\n\r\n  # ==========================================\r\n  # Type functions\r\n  # ==========================================\r\n  # Test: typeOf - returns the type of a value\r\n  # Test: typeIs - checks if value is of a specific type\r\n  # Test: typeIsLike - checks if value is like a specific type\r\n\r\n  # ==========================================\r\n  # Comparison functions\r\n  # ==========================================\r\n  # Test: deepEqual - compares two values for deep equality\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-reflection/templates/reflection-test.yaml",
            "content": "\n# Source: test-reflection/templates/reflection-test.yaml\n# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-reflection-test\ndata:\n  # ==========================================\n  # Kind functions\n  # ==========================================\n  # Test: kindOf - returns the kind of a value\n  kindOfValue: \"map\"\n  # Test: kindIs - checks if value is of a specific kind\n\n  # ==========================================\n  # Type functions\n  # ==========================================\n  # Test: typeOf - returns the type of a value\n  # Test: typeIs - checks if value is of a specific type\n  # Test: typeIsLike - checks if value is like a specific type\n\n  # ==========================================\n  # Comparison functions\n  # ==========================================\n  # Test: deepEqual - compares two values for deep equality\n",
            "originalContent": "# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-reflection-test\ndata:\n  # ==========================================\n  # Kind functions\n  # ==========================================\n  # Test: kindOf - returns the kind of a value\n  {{- if .Values.testKindOf }}\n  kindOfValue: {{ kindOf $testData | quote }}\n  {{- end }}\n  # Test: kindIs - checks if value is of a specific kind\n  {{- if .Values.testKindIs }}\n  kindIsValue: {{ kindIs \"string\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Type functions\n  # ==========================================\n  # Test: typeOf - returns the type of a value\n  {{- if .Values.testTypeOf }}\n  typeOfValue: {{ typeOf $testData | quote }}\n  {{- end }}\n  # Test: typeIs - checks if value is of a specific type\n  {{- if .Values.testTypeIs }}\n  typeIsValue: {{ typeIs \"string\" $testData | quote }}\n  {{- end }}\n  # Test: typeIsLike - checks if value is like a specific type\n  {{- if .Values.testTypeIsLike }}\n  typeIsLikeValue: {{ typeIsLike \"string\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Comparison functions\n  # ==========================================\n  # Test: deepEqual - compares two values for deep equality\n  {{- if .Values.testDeepEqual }}\n  deepEqualValue: {{ deepEqual $testData .Values.compareValue | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-reflection/templates/reflection-test.yaml\n# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-reflection-test\r\ndata:\r\n  # ==========================================\r\n  # Kind functions\r\n  # ==========================================\r\n  # Test: kindOf - returns the kind of a value\r\n  kindOfValue: \"map\"\r\n  # Test: kindIs - checks if value is of a specific kind\r\n\r\n  # ==========================================\r\n  # Type functions\r\n  # ==========================================\r\n  # Test: typeOf - returns the type of a value\r\n  # Test: typeIs - checks if value is of a specific type\r\n  # Test: typeIsLike - checks if value is like a specific type\r\n\r\n  # ==========================================\r\n  # Comparison functions\r\n  # ==========================================\r\n  # Test: deepEqual - compares two values for deep equality\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-reflection/templates/reflection-test.yaml",
            "content": "\n# Source: test-reflection/templates/reflection-test.yaml\n# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-reflection-test\ndata:\n  # ==========================================\n  # Kind functions\n  # ==========================================\n  # Test: kindOf - returns the kind of a value\n  kindOfValue: \"map\"\n  # Test: kindIs - checks if value is of a specific kind\n\n  # ==========================================\n  # Type functions\n  # ==========================================\n  # Test: typeOf - returns the type of a value\n  # Test: typeIs - checks if value is of a specific type\n  # Test: typeIsLike - checks if value is like a specific type\n\n  # ==========================================\n  # Comparison functions\n  # ==========================================\n  # Test: deepEqual - compares two values for deep equality\n",
            "originalContent": "# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-reflection-test\ndata:\n  # ==========================================\n  # Kind functions\n  # ==========================================\n  # Test: kindOf - returns the kind of a value\n  {{- if .Values.testKindOf }}\n  kindOfValue: {{ kindOf $testData | quote }}\n  {{- end }}\n  # Test: kindIs - checks if value is of a specific kind\n  {{- if .Values.testKindIs }}\n  kindIsValue: {{ kindIs \"string\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Type functions\n  # ==========================================\n  # Test: typeOf - returns the type of a value\n  {{- if .Values.testTypeOf }}\n  typeOfValue: {{ typeOf $testData | quote }}\n  {{- end }}\n  # Test: typeIs - checks if value is of a specific type\n  {{- if .Values.testTypeIs }}\n  typeIsValue: {{ typeIs \"string\" $testData | quote }}\n  {{- end }}\n  # Test: typeIsLike - checks if value is like a specific type\n  {{- if .Values.testTypeIsLike }}\n  typeIsLikeValue: {{ typeIsLike \"string\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Comparison functions\n  # ==========================================\n  # Test: deepEqual - compares two values for deep equality\n  {{- if .Values.testDeepEqual }}\n  deepEqualValue: {{ deepEqual $testData .Values.compareValue | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "resolved": true,
      "stopReason": "",
      "fixChain": []
    },
    {
      "toggle": "kindIs",
      "dataType": "nilValue",
      "valuesOptions": {
        "values": [
          "testKindIs=true",
          "testDataType=nilValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-reflection/templates/reflection-test.yaml\n# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-reflection-test\r\ndata:\r\n  # ==========================================\r\n  # Kind functions\r\n  # ==========================================\r\n  # Test: kindOf - returns the kind of a value\r\n  # Test: kindIs - checks if value is of a specific kind\r\n  kindIsValue: \"false\"\r\n\r\n  # ==========================================\r\n  # Type functions\r\n  # ==========================================\r\n  # Test: typeOf - returns the type of a value\r\n  # Test: typeIs - checks if value is of a specific type\r\n  # Test: typeIsLike - checks if value is like a specific type\r\n\r\n  # ==========================================\r\n  # Comparison functions\r\n  # ==========================================\r\n  # Test: deepEqual - compares two values for deep equality\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-reflection/templates/reflection-test.yaml",
            "content": "\n# Source: test-reflection/templates/reflection-test.yaml\n# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-reflection-test\ndata:\n  # ==========================================\n  # Kind functions\n  # ==========================================\n  # Test: kindOf - returns the kind of a value\n  # Test: kindIs - checks if value is of a specific kind\n  kindIsValue: \"false\"\n\n  # ==========================================\n  # Type functions\n  # ==========================================\n  # Test: typeOf - returns the type of a value\n  # Test: typeIs - checks if value is of a specific type\n  # Test: typeIsLike - checks if value is like a specific type\n\n  # ==========================================\n  # Comparison functions\n  # ==========================================\n  # Test: deepEqual - compares two values for deep equality\n",
            "originalContent": "# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-reflection-test\ndata:\n  # ==========================================\n  # Kind functions\n  # ==========================================\n  # Test: kindOf - returns the kind of a value\n  {{- if .Values.testKindOf }}\n  kindOfValue: {{ kindOf $testData | quote }}\n  {{- end }}\n  # Test: kindIs - checks if value is of a specific kind\n  {{- if .Values.testKindIs }}\n  kindIsValue: {{ kindIs \"string\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Type functions\n  # ==========================================\n  # Test: typeOf - returns the type of a value\n  {{- if .Values.testTypeOf }}\n  typeOfValue: {{ typeOf $testData | quote }}\n  {{- end }}\n  # Test: typeIs - checks if value is of a specific type\n  {{- if .Values.testTypeIs }}\n  typeIsValue: {{ typeIs \"string\" $testData | quote }}\n  {{- end }}\n  # Test: typeIsLike - checks if value is like a specific type\n  {{- if .Values.testTypeIsLike }}\n  typeIsLikeValue: {{ typeIsLike \"string\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Comparison functions\n  # ==========================================\n  # Test: deepEqual - compares two values for deep equality\n  {{- if .Values.testDeepEqual }}\n  deepEqualValue: {{ deepEqual $testData .Values.compareValue | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-reflection/templates/reflection-test.yaml\n# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-reflection-test\r\ndata:\r\n  # ==========================================\r\n  # Kind functions\r\n  # ==========================================\r\n  # Test: kindOf - returns the kind of a value\r\n  # Test: kindIs - checks if value is of a specific kind\r\n  kindIsValue: \"false\"\r\n\r\n  # ==========================================\r\n  # Type functions\r\n  # ==========================================\r\n  # Test: typeOf - returns the type of a value\r\n  # Test: typeIs - checks if value is of a specific type\r\n  # Test: typeIsLike - checks if value is like a specific type\r\n\r\n  # ==========================================\r\n  # Comparison functions\r\n  # ==========================================\r\n  # Test: deepEqual - compares two values for deep equality\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-reflection/templates/reflection-test.yaml",
            "content": "\n# Source: test-reflection/templates/reflection-test.yaml\n# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-reflection-test\ndata:\n  # ==========================================\n  # Kind functions\n  # ==========================================\n  # Test: kindOf - returns the kind of a value\n  # Test: kindIs - checks if value is of a specific kind\n  kindIsValue: \"false\"\n\n  # ==========================================\n  # Type functions\n  # ==========================================\n  # Test: typeOf - returns the type of a value\n  # Test: typeIs - checks if value is of a specific type\n  # Test: typeIsLike - checks if value is like a specific type\n\n  # ==========================================\n  # Comparison functions\n  # ==========================================\n  # Test: deepEqual - compares two values for deep equality\n",
            "originalContent": "# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-reflection-test\ndata:\n  # ==========================================\n  # Kind functions\n  # ==========================================\n  # Test: kindOf - returns the kind of a value\n  {{- if .Values.testKindOf }}\n  kindOfValue: {{ kindOf $testData | quote }}\n  {{- end }}\n  # Test: kindIs - checks if value is of a specific kind\n  {{- if .Values.testKindIs }}\n  kindIsValue: {{ kindIs \"string\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Type functions\n  # ==========================================\n  # Test: typeOf - returns the type of a value\n  {{- if .Values.testTypeOf }}\n  typeOfValue: {{ typeOf $testData | quote }}\n  {{- end }}\n  # Test: typeIs - checks if value is of a specific type\n  {{- if .Values.testTypeIs }}\n  typeIsValue: {{ typeIs \"string\" $testData | quote }}\n  {{- end }}\n  # Test: typeIsLike - checks if value is like a specific type\n  {{- if .Values.testTypeIsLike }}\n  typeIsLikeValue: {{ typeIsLike \"string\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Comparison functions\n  # ==========================================\n  # Test: deepEqual - compares two values for deep equality\n  {{- if .Values.testDeepEqual }}\n  deepEqualValue: {{ deepEqual $testData .Values.compareValue | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "resolved": true,
      "stopReason": "",
      "fixChain": []
    },
    {
      "toggle": "kindIs",
      "dataType": "stringValue",
      "valuesOptions": {
        "values": [
          "testKindIs=true",
          "testDataType=stringValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-reflection/templates/reflection-test.yaml\n# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-reflection-test\r\ndata:\r\n  # ==========================================\r\n  # Kind functions\r\n  # ==========================================\r\n  # Test: kindOf - returns the kind of a value\r\n  # Test: kindIs - checks if value is of a specific kind\r\n  kindIsValue: \"true\"\r\n\r\n  # ==========================================\r\n  # Type functions\r\n  # ==========================================\r\n  # Test: typeOf - returns the type of a value\r\n  # Test: typeIs - checks if value is of a specific type\r\n  # Test: typeIsLike - checks if value is like a specific type\r\n\r\n  # ==========================================\r\n  # Comparison functions\r\n  # ==========================================\r\n  # Test: deepEqual - compares two values for deep equality\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-reflection/templates/reflection-test.yaml",
            "content": "\n# Source: test-reflection/templates/reflection-test.yaml\n# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-reflection-test\ndata:\n  # ==========================================\n  # Kind functions\n  # ==========================================\n  # Test: kindOf - returns the kind of a value\n  # Test: kindIs - checks if value is of a specific kind\n  kindIsValue: \"true\"\n\n  # ==========================================\n  # Type functions\n  # ==========================================\n  # Test: typeOf - returns the type of a value\n  # Test: typeIs - checks if value is of a specific type\n  # Test: typeIsLike - checks if value is like a specific type\n\n  # ==========================================\n  # Comparison functions\n  # ==========================================\n  # Test: deepEqual - compares two values for deep equality\n",
            "originalContent": "# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-reflection-test\ndata:\n  # ==========================================\n  # Kind functions\n  # ==========================================\n  # Test: kindOf - returns the kind of a value\n  {{- if .Values.testKindOf }}\n  kindOfValue: {{ kindOf $testData | quote }}\n  {{- end }}\n  # Test: kindIs - checks if value is of a specific kind\n  {{- if .Values.testKindIs }}\n  kindIsValue: {{ kindIs \"string\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Type functions\n  # ==========================================\n  # Test: typeOf - returns the type of a value\n  {{- if .Values.testTypeOf }}\n  typeOfValue: {{ typeOf $testData | quote }}\n  {{- end }}\n  # Test: typeIs - checks if value is of a specific type\n  {{- if .Values.testTypeIs }}\n  typeIsValue: {{ typeIs \"string\" $testData | quote }}\n  {{- end }}\n  # Test: typeIsLike - checks if value is like a specific type\n  {{- if .Values.testTypeIsLike }}\n  typeIsLikeValue: {{ typeIsLike \"string\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Comparison functions\n  # ==========================================\n  # Test: deepEqual - compares two values for deep equality\n  {{- if .Values.testDeepEqual }}\n  deepEqualValue: {{ deepEqual $testData .Values.compareValue | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-reflection/templates/reflection-test.yaml\n# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-reflection-test\r\ndata:\r\n  # ==========================================\r\n  # Kind functions\r\n  # ==========================================\r\n  # Test: kindOf - returns the kind of a value\r\n  # Test: kindIs - checks if value is of a specific kind\r\n  kindIsValue: \"true\"\r\n\r\n  # ==========================================\r\n  # Type functions\r\n  # ==========================================\r\n  # Test: typeOf - returns the type of a value\r\n  # Test: typeIs - checks if value is of a specific type\r\n  # Test: typeIsLike - checks if value is like a specific type\r\n\r\n  # ==========================================\r\n  # Comparison functions\r\n  # ==========================================\r\n  # Test: deepEqual - compares two values for deep equality\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-reflection/templates/reflection-test.yaml",
            "content": "\n# Source: test-reflection/templates/reflection-test.yaml\n# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-reflection-test\ndata:\n  # ==========================================\n  # Kind functions\n  # ==========================================\n  # Test: kindOf - returns the kind of a value\n  # Test: kindIs - checks if value is of a specific kind\n  kindIsValue: \"true\"\n\n  # ==========================================\n  # Type functions\n  # ==========================================\n  # Test: typeOf - returns the type of a value\n  # Test: typeIs - checks if value is of a specific type\n  # Test: typeIsLike - checks if value is like a specific type\n\n  # ==========================================\n  # Comparison functions\n  # ==========================================\n  # Test: deepEqual - compares two values for deep equality\n",
            "originalContent": "# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-reflection-test\ndata:\n  # ==========================================\n  # Kind functions\n  # ==========================================\n  # Test: kindOf - returns the kind of a value\n  {{- if .Values.testKindOf }}\n  kindOfValue: {{ kindOf $testData | quote }}\n  {{- end }}\n  # Test: kindIs - checks if value is of a specific kind\n  {{- if .Values.testKindIs }}\n  kindIsValue: {{ kindIs \"string\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Type functions\n  # ==========================================\n  # Test: typeOf - returns the type of a value\n  {{- if .Values.testTypeOf }}\n  typeOfValue: {{ typeOf $testData | quote }}\n  {{- end }}\n  # Test: typeIs - checks if value is of a specific type\n  {{- if .Values.testTypeIs }}\n  typeIsValue: {{ typeIs \"string\" $testData | quote }}\n  {{- end }}\n  # Test: typeIsLike - checks if value is like a specific type\n  {{- if .Values.testTypeIsLike }}\n  typeIsLikeValue: {{ typeIsLike \"string\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Comparison functions\n  # ==========================================\n  # Test: deepEqual - compares two values for deep equality\n  {{- if .Values.testDeepEqual }}\n  deepEqualValue: {{ deepEqual $testData .Values.compareValue | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "resolved": true,
      "stopReason": "",
      "fixChain": []
    },
    {
      "toggle": "kindIs",
      "dataType": "numberValue",
      "valuesOptions": {
        "values": [
          "testKindIs=true",
          "testDataType=numberValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-reflection/templates/reflection-test.yaml\n# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-reflection-test\r\ndata:\r\n  # ==========================================\r\n  # Kind functions\r\n  # ==========================================\r\n  # Test: kindOf - returns the kind of a value\r\n  # Test: kindIs - checks if value is of a specific kind\r\n  kindIsValue: \"false\"\r\n\r\n  # ==========================================\r\n  # Type functions\r\n  # ==========================================\r\n  # Test: typeOf - returns the type of a value\r\n  # Test: typeIs - checks if value is of a specific type\r\n  # Test: typeIsLike - checks if value is like a specific type\r\n\r\n  # ==========================================\r\n  # Comparison functions\r\n  # ==========================================\r\n  # Test: deepEqual - compares two values for deep equality\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-reflection/templates/reflection-test.yaml",
            "content": "\n# Source: test-reflection/templates/reflection-test.yaml\n# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-reflection-test\ndata:\n  # ==========================================\n  # Kind functions\n  # ==========================================\n  # Test: kindOf - returns the kind of a value\n  # Test: kindIs - checks if value is of a specific kind\n  kindIsValue: \"false\"\n\n  # ==========================================\n  # Type functions\n  # ==========================================\n  # Test: typeOf - returns the type of a value\n  # Test: typeIs - checks if value is of a specific type\n  # Test: typeIsLike - checks if value is like a specific type\n\n  # ==========================================\n  # Comparison functions\n  # ==========================================\n  # Test: deepEqual - compares two values for deep equality\n",
            "originalContent": "# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-reflection-test\ndata:\n  # ==========================================\n  # Kind functions\n  # ==========================================\n  # Test: kindOf - returns the kind of a value\n  {{- if .Values.testKindOf }}\n  kindOfValue: {{ kindOf $testData | quote }}\n  {{- end }}\n  # Test: kindIs - checks if value is of a specific kind\n  {{- if .Values.testKindIs }}\n  kindIsValue: {{ kindIs \"string\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Type functions\n  # ==========================================\n  # Test: typeOf - returns the type of a value\n  {{- if .Values.testTypeOf }}\n  typeOfValue: {{ typeOf $testData | quote }}\n  {{- end }}\n  # Test: typeIs - checks if value is of a specific type\n  {{- if .Values.testTypeIs }}\n  typeIsValue: {{ typeIs \"string\" $testData | quote }}\n  {{- end }}\n  # Test: typeIsLike - checks if value is like a specific type\n  {{- if .Values.testTypeIsLike }}\n  typeIsLikeValue: {{ typeIsLike \"string\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Comparison functions\n  # ==========================================\n  # Test: deepEqual - compares two values for deep equality\n  {{- if .Values.testDeepEqual }}\n  deepEqualValue: {{ deepEqual $testData .Values.compareValue | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-reflection/templates/reflection-test.yaml\n# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-reflection-test\r\ndata:\r\n  # ==========================================\r\n  # Kind functions\r\n  # ==========================================\r\n  # Test: kindOf - returns the kind of a value\r\n  # Test: kindIs - checks if value is of a specific kind\r\n  kindIsValue: \"false\"\r\n\r\n  # ==========================================\r\n  # Type functions\r\n  # ==========================================\r\n  # Test: typeOf - returns the type of a value\r\n  # Test: typeIs - checks if value is of a specific type\r\n  # Test: typeIsLike - checks if value is like a specific type\r\n\r\n  # ==========================================\r\n  # Comparison functions\r\n  # ==========================================\r\n  # Test: deepEqual - compares two values for deep equality\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-reflection/templates/reflection-test.yaml",
            "content": "\n# Source: test-reflection/templates/reflection-test.yaml\n# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-reflection-test\ndata:\n  # ==========================================\n  # Kind functions\n  # ==========================================\n  # Test: kindOf - returns the kind of a value\n  # Test: kindIs - checks if value is of a specific kind\n  kindIsValue: \"false\"\n\n  # ==========================================\n  # Type functions\n  # ==========================================\n  # Test: typeOf - returns the type of a value\n  # Test: typeIs - checks if value is of a specific type\n  # Test: typeIsLike - checks if value is like a specific type\n\n  # ==========================================\n  # Comparison functions\n  # ==========================================\n  # Test: deepEqual - compares two values for deep equality\n",
            "originalContent": "# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-reflection-test\ndata:\n  # ==========================================\n  # Kind functions\n  # ==========================================\n  # Test: kindOf - returns the kind of a value\n  {{- if .Values.testKindOf }}\n  kindOfValue: {{ kindOf $testData | quote }}\n  {{- end }}\n  # Test: kindIs - checks if value is of a specific kind\n  {{- if .Values.testKindIs }}\n  kindIsValue: {{ kindIs \"string\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Type functions\n  # ==========================================\n  # Test: typeOf - returns the type of a value\n  {{- if .Values.testTypeOf }}\n  typeOfValue: {{ typeOf $testData | quote }}\n  {{- end }}\n  # Test: typeIs - checks if value is of a specific type\n  {{- if .Values.testTypeIs }}\n  typeIsValue: {{ typeIs \"string\" $testData | quote }}\n  {{- end }}\n  # Test: typeIsLike - checks if value is like a specific type\n  {{- if .Values.testTypeIsLike }}\n  typeIsLikeValue: {{ typeIsLike \"string\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Comparison functions\n  # ==========================================\n  # Test: deepEqual - compares two values for deep equality\n  {{- if .Values.testDeepEqual }}\n  deepEqualValue: {{ deepEqual $testData .Values.compareValue | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "resolved": true,
      "stopReason": "",
      "fixChain": []
    },
    {
      "toggle": "kindIs",
      "dataType": "boolValue",
      "valuesOptions": {
        "values": [
          "testKindIs=true",
          "testDataType=boolValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-reflection/templates/reflection-test.yaml\n# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-reflection-test\r\ndata:\r\n  # ==========================================\r\n  # Kind functions\r\n  # ==========================================\r\n  # Test: kindOf - returns the kind of a value\r\n  # Test: kindIs - checks if value is of a specific kind\r\n  kindIsValue: \"false\"\r\n\r\n  # ==========================================\r\n  # Type functions\r\n  # ==========================================\r\n  # Test: typeOf - returns the type of a value\r\n  # Test: typeIs - checks if value is of a specific type\r\n  # Test: typeIsLike - checks if value is like a specific type\r\n\r\n  # ==========================================\r\n  # Comparison functions\r\n  # ==========================================\r\n  # Test: deepEqual - compares two values for deep equality\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-reflection/templates/reflection-test.yaml",
            "content": "\n# Source: test-reflection/templates/reflection-test.yaml\n# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-reflection-test\ndata:\n  # ==========================================\n  # Kind functions\n  # ==========================================\n  # Test: kindOf - returns the kind of a value\n  # Test: kindIs - checks if value is of a specific kind\n  kindIsValue: \"false\"\n\n  # ==========================================\n  # Type functions\n  # ==========================================\n  # Test: typeOf - returns the type of a value\n  # Test: typeIs - checks if value is of a specific type\n  # Test: typeIsLike - checks if value is like a specific type\n\n  # ==========================================\n  # Comparison functions\n  # ==========================================\n  # Test: deepEqual - compares two values for deep equality\n",
            "originalContent": "# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-reflection-test\ndata:\n  # ==========================================\n  # Kind functions\n  # ==========================================\n  # Test: kindOf - returns the kind of a value\n  {{- if .Values.testKindOf }}\n  kindOfValue: {{ kindOf $testData | quote }}\n  {{- end }}\n  # Test: kindIs - checks if value is of a specific kind\n  {{- if .Values.testKindIs }}\n  kindIsValue: {{ kindIs \"string\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Type functions\n  # ==========================================\n  # Test: typeOf - returns the type of a value\n  {{- if .Values.testTypeOf }}\n  typeOfValue: {{ typeOf $testData | quote }}\n  {{- end }}\n  # Test: typeIs - checks if value is of a specific type\n  {{- if .Values.testTypeIs }}\n  typeIsValue: {{ typeIs \"string\" $testData | quote }}\n  {{- end }}\n  # Test: typeIsLike - checks if value is like a specific type\n  {{- if .Values.testTypeIsLike }}\n  typeIsLikeValue: {{ typeIsLike \"string\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Comparison functions\n  # ==========================================\n  # Test: deepEqual - compares two values for deep equality\n  {{- if .Values.testDeepEqual }}\n  deepEqualValue: {{ deepEqual $testData .Values.compareValue | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-reflection/templates/reflection-test.yaml\n# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-reflection-test\r\ndata:\r\n  # ==========================================\r\n  # Kind functions\r\n  # ==========================================\r\n  # Test: kindOf - returns the kind of a value\r\n  # Test: kindIs - checks if value is of a specific kind\r\n  kindIsValue: \"false\"\r\n\r\n  # ==========================================\r\n  # Type functions\r\n  # ==========================================\r\n  # Test: typeOf - returns the type of a value\r\n  # Test: typeIs - checks if value is of a specific type\r\n  # Test: typeIsLike - checks if value is like a specific type\r\n\r\n  # ==========================================\r\n  # Comparison functions\r\n  # ==========================================\r\n  # Test: deepEqual - compares two values for deep equality\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-reflection/templates/reflection-test.yaml",
            "content": "\n# Source: test-reflection/templates/reflection-test.yaml\n# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-reflection-test\ndata:\n  # ==========================================\n  # Kind functions\n  # ==========================================\n  # Test: kindOf - returns the kind of a value\n  # Test: kindIs - checks if value is of a specific kind\n  kindIsValue: \"false\"\n\n  # ==========================================\n  # Type functions\n  # ==========================================\n  # Test: typeOf - returns the type of a value\n  # Test: typeIs - checks if value is of a specific type\n  # Test: typeIsLike - checks if value is like a specific type\n\n  # ==========================================\n  # Comparison functions\n  # ==========================================\n  # Test: deepEqual - compares two values for deep equality\n",
            "originalContent": "# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-reflection-test\ndata:\n  # ==========================================\n  # Kind functions\n  # ==========================================\n  # Test: kindOf - returns the kind of a value\n  {{- if .Values.testKindOf }}\n  kindOfValue: {{ kindOf $testData | quote }}\n  {{- end }}\n  # Test: kindIs - checks if value is of a specific kind\n  {{- if .Values.testKindIs }}\n  kindIsValue: {{ kindIs \"string\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Type functions\n  # ==========================================\n  # Test: typeOf - returns the type of a value\n  {{- if .Values.testTypeOf }}\n  typeOfValue: {{ typeOf $testData | quote }}\n  {{- end }}\n  # Test: typeIs - checks if value is of a specific type\n  {{- if .Values.testTypeIs }}\n  typeIsValue: {{ typeIs \"string\" $testData | quote }}\n  {{- end }}\n  # Test: typeIsLike - checks if value is like a specific type\n  {{- if .Values.testTypeIsLike }}\n  typeIsLikeValue: {{ typeIsLike \"string\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Comparison functions\n  # ==========================================\n  # Test: deepEqual - compares two values for deep equality\n  {{- if .Values.testDeepEqual }}\n  deepEqualValue: {{ deepEqual $testData .Values.compareValue | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "resolved": true,
      "stopReason": "",
      "fixChain": []
    },
    {
      "toggle": "kindIs",
      "dataType": "listValue",
      "valuesOptions": {
        "values": [
          "testKindIs=true",
          "testDataType=listValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-reflection/templates/reflection-test.yaml\n# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-reflection-test\r\ndata:\r\n  # ==========================================\r\n  # Kind functions\r\n  # ==========================================\r\n  # Test: kindOf - returns the kind of a value\r\n  # Test: kindIs - checks if value is of a specific kind\r\n  kindIsValue: \"false\"\r\n\r\n  # ==========================================\r\n  # Type functions\r\n  # ==========================================\r\n  # Test: typeOf - returns the type of a value\r\n  # Test: typeIs - checks if value is of a specific type\r\n  # Test: typeIsLike - checks if value is like a specific type\r\n\r\n  # ==========================================\r\n  # Comparison functions\r\n  # ==========================================\r\n  # Test: deepEqual - compares two values for deep equality\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-reflection/templates/reflection-test.yaml",
            "content": "\n# Source: test-reflection/templates/reflection-test.yaml\n# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-reflection-test\ndata:\n  # ==========================================\n  # Kind functions\n  # ==========================================\n  # Test: kindOf - returns the kind of a value\n  # Test: kindIs - checks if value is of a specific kind\n  kindIsValue: \"false\"\n\n  # ==========================================\n  # Type functions\n  # ==========================================\n  # Test: typeOf - returns the type of a value\n  # Test: typeIs - checks if value is of a specific type\n  # Test: typeIsLike - checks if value is like a specific type\n\n  # ==========================================\n  # Comparison functions\n  # ==========================================\n  # Test: deepEqual - compares two values for deep equality\n",
            "originalContent": "# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-reflection-test\ndata:\n  # ==========================================\n  # Kind functions\n  # ==========================================\n  # Test: kindOf - returns the kind of a value\n  {{- if .Values.testKindOf }}\n  kindOfValue: {{ kindOf $testData | quote }}\n  {{- end }}\n  # Test: kindIs - checks if value is of a specific kind\n  {{- if .Values.testKindIs }}\n  kindIsValue: {{ kindIs \"string\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Type functions\n  # ==========================================\n  # Test: typeOf - returns the type of a value\n  {{- if .Values.testTypeOf }}\n  typeOfValue: {{ typeOf $testData | quote }}\n  {{- end }}\n  # Test: typeIs - checks if value is of a specific type\n  {{- if .Values.testTypeIs }}\n  typeIsValue: {{ typeIs \"string\" $testData | quote }}\n  {{- end }}\n  # Test: typeIsLike - checks if value is like a specific type\n  {{- if .Values.testTypeIsLike }}\n  typeIsLikeValue: {{ typeIsLike \"string\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Comparison functions\n  # ==========================================\n  # Test: deepEqual - compares two values for deep equality\n  {{- if .Values.testDeepEqual }}\n  deepEqualValue: {{ deepEqual $testData .Values.compareValue | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-reflection/templates/reflection-test.yaml\n# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-reflection-test\r\ndata:\r\n  # ==========================================\r\n  # Kind functions\r\n  # ==========================================\r\n  # Test: kindOf - returns the kind of a value\r\n  # Test: kindIs - checks if value is of a specific kind\r\n  kindIsValue: \"false\"\r\n\r\n  # ==========================================\r\n  # Type functions\r\n  # ==========================================\r\n  # Test: typeOf - returns the type of a value\r\n  # Test: typeIs - checks if value is of a specific type\r\n  # Test: typeIsLike - checks if value is like a specific type\r\n\r\n  # ==========================================\r\n  # Comparison functions\r\n  # ==========================================\r\n  # Test: deepEqual - compares two values for deep equality\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-reflection/templates/reflection-test.yaml",
            "content": "\n# Source: test-reflection/templates/reflection-test.yaml\n# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-reflection-test\ndata:\n  # ==========================================\n  # Kind functions\n  # ==========================================\n  # Test: kindOf - returns the kind of a value\n  # Test: kindIs - checks if value is of a specific kind\n  kindIsValue: \"false\"\n\n  # ==========================================\n  # Type functions\n  # ==========================================\n  # Test: typeOf - returns the type of a value\n  # Test: typeIs - checks if value is of a specific type\n  # Test: typeIsLike - checks if value is like a specific type\n\n  # ==========================================\n  # Comparison functions\n  # ==========================================\n  # Test: deepEqual - compares two values for deep equality\n",
            "originalContent": "# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-reflection-test\ndata:\n  # ==========================================\n  # Kind functions\n  # ==========================================\n  # Test: kindOf - returns the kind of a value\n  {{- if .Values.testKindOf }}\n  kindOfValue: {{ kindOf $testData | quote }}\n  {{- end }}\n  # Test: kindIs - checks if value is of a specific kind\n  {{- if .Values.testKindIs }}\n  kindIsValue: {{ kindIs \"string\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Type functions\n  # ==========================================\n  # Test: typeOf - returns the type of a value\n  {{- if .Values.testTypeOf }}\n  typeOfValue: {{ typeOf $testData | quote }}\n  {{- end }}\n  # Test: typeIs - checks if value is of a specific type\n  {{- if .Values.testTypeIs }}\n  typeIsValue: {{ typeIs \"string\" $testData | quote }}\n  {{- end }}\n  # Test: typeIsLike - checks if value is like a specific type\n  {{- if .Values.testTypeIsLike }}\n  typeIsLikeValue: {{ typeIsLike \"string\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Comparison functions\n  # ==========================================\n  # Test: deepEqual - compares two values for deep equality\n  {{- if .Values.testDeepEqual }}\n  deepEqualValue: {{ deepEqual $testData .Values.compareValue | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "resolved": true,
      "stopReason": "",
      "fixChain": []
    },
    {
      "toggle": "kindIs",
      "dataType": "dictValue",
      "valuesOptions": {
        "values": [
          "testKindIs=true",
          "testDataType=dictValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-reflection/templates/reflection-test.yaml\n# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-reflection-test\r\ndata:\r\n  # ==========================================\r\n  # Kind functions\r\n  # ==========================================\r\n  # Test: kindOf - returns the kind of a value\r\n  # Test: kindIs - checks if value is of a specific kind\r\n  kindIsValue: \"false\"\r\n\r\n  # ==========================================\r\n  # Type functions\r\n  # ==========================================\r\n  # Test: typeOf - returns the type of a value\r\n  # Test: typeIs - checks if value is of a specific type\r\n  # Test: typeIsLike - checks if value is like a specific type\r\n\r\n  # ==========================================\r\n  # Comparison functions\r\n  # ==========================================\r\n  # Test: deepEqual - compares two values for deep equality\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-reflection/templates/reflection-test.yaml",
            "content": "\n# Source: test-reflection/templates/reflection-test.yaml\n# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-reflection-test\ndata:\n  # ==========================================\n  # Kind functions\n  # ==========================================\n  # Test: kindOf - returns the kind of a value\n  # Test: kindIs - checks if value is of a specific kind\n  kindIsValue: \"false\"\n\n  # ==========================================\n  # Type functions\n  # ==========================================\n  # Test: typeOf - returns the type of a value\n  # Test: typeIs - checks if value is of a specific type\n  # Test: typeIsLike - checks if value is like a specific type\n\n  # ==========================================\n  # Comparison functions\n  # ==========================================\n  # Test: deepEqual - compares two values for deep equality\n",
            "originalContent": "# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-reflection-test\ndata:\n  # ==========================================\n  # Kind functions\n  # ==========================================\n  # Test: kindOf - returns the kind of a value\n  {{- if .Values.testKindOf }}\n  kindOfValue: {{ kindOf $testData | quote }}\n  {{- end }}\n  # Test: kindIs - checks if value is of a specific kind\n  {{- if .Values.testKindIs }}\n  kindIsValue: {{ kindIs \"string\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Type functions\n  # ==========================================\n  # Test: typeOf - returns the type of a value\n  {{- if .Values.testTypeOf }}\n  typeOfValue: {{ typeOf $testData | quote }}\n  {{- end }}\n  # Test: typeIs - checks if value is of a specific type\n  {{- if .Values.testTypeIs }}\n  typeIsValue: {{ typeIs \"string\" $testData | quote }}\n  {{- end }}\n  # Test: typeIsLike - checks if value is like a specific type\n  {{- if .Values.testTypeIsLike }}\n  typeIsLikeValue: {{ typeIsLike \"string\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Comparison functions\n  # ==========================================\n  # Test: deepEqual - compares two values for deep equality\n  {{- if .Values.testDeepEqual }}\n  deepEqualValue: {{ deepEqual $testData .Values.compareValue | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-reflection/templates/reflection-test.yaml\n# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-reflection-test\r\ndata:\r\n  # ==========================================\r\n  # Kind functions\r\n  # ==========================================\r\n  # Test: kindOf - returns the kind of a value\r\n  # Test: kindIs - checks if value is of a specific kind\r\n  kindIsValue: \"false\"\r\n\r\n  # ==========================================\r\n  # Type functions\r\n  # ==========================================\r\n  # Test: typeOf - returns the type of a value\r\n  # Test: typeIs - checks if value is of a specific type\r\n  # Test: typeIsLike - checks if value is like a specific type\r\n\r\n  # ==========================================\r\n  # Comparison functions\r\n  # ==========================================\r\n  # Test: deepEqual - compares two values for deep equality\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-reflection/templates/reflection-test.yaml",
            "content": "\n# Source: test-reflection/templates/reflection-test.yaml\n# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-reflection-test\ndata:\n  # ==========================================\n  # Kind functions\n  # ==========================================\n  # Test: kindOf - returns the kind of a value\n  # Test: kindIs - checks if value is of a specific kind\n  kindIsValue: \"false\"\n\n  # ==========================================\n  # Type functions\n  # ==========================================\n  # Test: typeOf - returns the type of a value\n  # Test: typeIs - checks if value is of a specific type\n  # Test: typeIsLike - checks if value is like a specific type\n\n  # ==========================================\n  # Comparison functions\n  # ==========================================\n  # Test: deepEqual - compares two values for deep equality\n",
            "originalContent": "# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-reflection-test\ndata:\n  # ==========================================\n  # Kind functions\n  # ==========================================\n  # Test: kindOf - returns the kind of a value\n  {{- if .Values.testKindOf }}\n  kindOfValue: {{ kindOf $testData | quote }}\n  {{- end }}\n  # Test: kindIs - checks if value is of a specific kind\n  {{- if .Values.testKindIs }}\n  kindIsValue: {{ kindIs \"string\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Type functions\n  # ==========================================\n  # Test: typeOf - returns the type of a value\n  {{- if .Values.testTypeOf }}\n  typeOfValue: {{ typeOf $testData | quote }}\n  {{- end }}\n  # Test: typeIs - checks if value is of a specific type\n  {{- if .Values.testTypeIs }}\n  typeIsValue: {{ typeIs \"string\" $testData | quote }}\n  {{- end }}\n  # Test: typeIsLike - checks if value is like a specific type\n  {{- if .Values.testTypeIsLike }}\n  typeIsLikeValue: {{ typeIsLike \"string\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Comparison functions\n  # ==========================================\n  # Test: deepEqual - compares two values for deep equality\n  {{- if .Values.testDeepEqual }}\n  deepEqualValue: {{ deepEqual $testData .Values.compareValue | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "resolved": true,
      "stopReason": "",
      "fixChain": []
    },
    {
      "toggle": "typeOf",
      "dataType": "nilValue",
      "valuesOptions": {
        "values": [
          "testTypeOf=true",
          "testDataType=nilValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-reflection/templates/reflection-test.yaml\n# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-reflection-test\r\ndata:\r\n  # ==========================================\r\n  # Kind functions\r\n  # ==========================================\r\n  # Test: kindOf - returns the kind of a value\r\n  # Test: kindIs - checks if value is of a specific kind\r\n\r\n  # ==========================================\r\n  # Type functions\r\n  # ==========================================\r\n  # Test: typeOf - returns the type of a value\r\n  typeOfValue: \"\u003cnil\u003e\"\r\n  # Test: typeIs - checks if value is of a specific type\r\n  # Test: typeIsLike - checks if value is like a specific type\r\n\r\n  # ==========================================\r\n  # Comparison functions\r\n  # ==========================================\r\n  # Test: deepEqual - compares two values for deep equality\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-reflection/templates/reflection-test.yaml",
            "content": "\n# Source: test-reflection/templates/reflection-test.yaml\n# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-reflection-test\ndata:\n  # ==========================================\n  # Kind functions\n  # ==========================================\n  # Test: kindOf - returns the kind of a value\n  # Test: kindIs - checks if value is of a specific kind\n\n  # ==========================================\n  # Type functions\n  # ==========================================\n  # Test: typeOf - returns the type of a value\n  typeOfValue: \"\u003cnil\u003e\"\n  # Test: typeIs - checks if value is of a specific type\n  # Test: typeIsLike - checks if value is like a specific type\n\n  # ==========================================\n  # Comparison functions\n  # ==========================================\n  # Test: deepEqual - compares two values for deep equality\n",
            "originalContent": "# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-reflection-test\ndata:\n  # ==========================================\n  # Kind functions\n  # ==========================================\n  # Test: kindOf - returns the kind of a value\n  {{- if .Values.testKindOf }}\n  kindOfValue: {{ kindOf $testData | quote }}\n  {{- end }}\n  # Test: kindIs - checks if value is of a specific kind\n  {{- if .Values.testKindIs }}\n  kindIsValue: {{ kindIs \"string\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Type functions\n  # ==========================================\n  # Test: typeOf - returns the type of a value\n  {{- if .Values.testTypeOf }}\n  typeOfValue: {{ typeOf $testData | quote }}\n  {{- end }}\n  # Test: typeIs - checks if value is of a specific type\n  {{- if .Values.testTypeIs }}\n  typeIsValue: {{ typeIs \"string\" $testData | quote }}\n  {{- end }}\n  # Test: typeIsLike - checks if value is like a specific type\n  {{- if .Values.testTypeIsLike }}\n  typeIsLikeValue: {{ typeIsLike \"string\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Comparison functions\n  # ==========================================\n  # Test: deepEqual - compares two values for deep equality\n  {{- if .Values.testDeepEqual }}\n  deepEqualValue: {{ deepEqual $testData .Values.compareValue | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-reflection/templates/reflection-test.yaml\n# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-reflection-test\r\ndata:\r\n  # ==========================================\r\n  # Kind functions\r\n  # ==========================================\r\n  # Test: kindOf - returns the kind of a value\r\n  # Test: kindIs - checks if value is of a specific kind\r\n\r\n  # ==========================================\r\n  # Type functions\r\n  # ==========================================\r\n  # Test: typeOf - returns the type of a value\r\n  typeOfValue: \"\u003cnil\u003e\"\r\n  # Test: typeIs - checks if value is of a specific type\r\n  # Test: typeIsLike - checks if value is like a specific type\r\n\r\n  # ==========================================\r\n  # Comparison functions\r\n  # ==========================================\r\n  # Test: deepEqual - compares two values for deep equality\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-reflection/templates/reflection-test.yaml",
            "content": "\n# Source: test-reflection/templates/reflection-test.yaml\n# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-reflection-test\ndata:\n  # ==========================================\n  # Kind functions\n  # ==========================================\n  # Test: kindOf - returns the kind of a value\n  # Test: kindIs - checks if value is of a specific kind\n\n  # ==========================================\n  # Type functions\n  # ==========================================\n  # Test: typeOf - returns the type of a value\n  typeOfValue: \"\u003cnil\u003e\"\n  # Test: typeIs - checks if value is of a specific type\n  # Test: typeIsLike - checks if value is like a specific type\n\n  # ==========================================\n  # Comparison functions\n  # ==========================================\n  # Test: deepEqual - compares two values for deep equality\n",
            "originalContent": "# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-reflection-test\ndata:\n  # ==========================================\n  # Kind functions\n  # ==========================================\n  # Test: kindOf - returns the kind of a value\n  {{- if .Values.testKindOf }}\n  kindOfValue: {{ kindOf $testData | quote }}\n  {{- end }}\n  # Test: kindIs - checks if value is of a specific kind\n  {{- if .Values.testKindIs }}\n  kindIsValue: {{ kindIs \"string\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Type functions\n  # ==========================================\n  # Test: typeOf - returns the type of a value\n  {{- if .Values.testTypeOf }}\n  typeOfValue: {{ typeOf $testData | quote }}\n  {{- end }}\n  # Test: typeIs - checks if value is of a specific type\n  {{- if .Values.testTypeIs }}\n  typeIsValue: {{ typeIs \"string\" $testData | quote }}\n  {{- end }}\n  # Test: typeIsLike - checks if value is like a specific type\n  {{- if .Values.testTypeIsLike }}\n  typeIsLikeValue: {{ typeIsLike \"string\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Comparison functions\n  # ==========================================\n  # Test: deepEqual - compares two values for deep equality\n  {{- if .Values.testDeepEqual }}\n  deepEqualValue: {{ deepEqual $testData .Values.compareValue | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "resolved": true,
      "stopReason": "",
      "fixChain": []
    },
    {
      "toggle": "typeOf",
      "dataType": "stringValue",
      "valuesOptions": {
        "values": [
          "testTypeOf=true",
          "testDataType=stringValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-reflection/templates/reflection-test.yaml\n# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-reflection-test\r\ndata:\r\n  # ==========================================\r\n  # Kind functions\r\n  # ==========================================\r\n  # Test: kindOf - returns the kind of a value\r\n  # Test: kindIs - checks if value is of a specific kind\r\n\r\n  # ==========================================\r\n  # Type functions\r\n  # ==========================================\r\n  # Test: typeOf - returns the type of a value\r\n  typeOfValue: \"string\"\r\n  # Test: typeIs - checks if value is of a specific type\r\n  # Test: typeIsLike - checks if value is like a specific type\r\n\r\n  # ==========================================\r\n  # Comparison functions\r\n  # ==========================================\r\n  # Test: deepEqual - compares two values for deep equality\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-reflection/templates/reflection-test.yaml",
            "content": "\n# Source: test-reflection/templates/reflection-test.yaml\n# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-reflection-test\ndata:\n  # ==========================================\n  # Kind functions\n  # ==========================================\n  # Test: kindOf - returns the kind of a value\n  # Test: kindIs - checks if value is of a specific kind\n\n  # ==========================================\n  # Type functions\n  # ==========================================\n  # Test: typeOf - returns the type of a value\n  typeOfValue: \"string\"\n  # Test: typeIs - checks if value is of a specific type\n  # Test: typeIsLike - checks if value is like a specific type\n\n  # ==========================================\n  # Comparison functions\n  # ==========================================\n  # Test: deepEqual - compares two values for deep equality\n",
            "originalContent": "# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-reflection-test\ndata:\n  # ==========================================\n  # Kind functions\n  # ==========================================\n  # Test: kindOf - returns the kind of a value\n  {{- if .Values.testKindOf }}\n  kindOfValue: {{ kindOf $testData | quote }}\n  {{- end }}\n  # Test: kindIs - checks if value is of a specific kind\n  {{- if .Values.testKindIs }}\n  kindIsValue: {{ kindIs \"string\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Type functions\n  # ==========================================\n  # Test: typeOf - returns the type of a value\n  {{- if .Values.testTypeOf }}\n  typeOfValue: {{ typeOf $testData | quote }}\n  {{- end }}\n  # Test: typeIs - checks if value is of a specific type\n  {{- if .Values.testTypeIs }}\n  typeIsValue: {{ typeIs \"string\" $testData | quote }}\n  {{- end }}\n  # Test: typeIsLike - checks if value is like a specific type\n  {{- if .Values.testTypeIsLike }}\n  typeIsLikeValue: {{ typeIsLike \"string\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Comparison functions\n  # ==========================================\n  # Test: deepEqual - compares two values for deep equality\n  {{- if .Values.testDeepEqual }}\n  deepEqualValue: {{ deepEqual $testData .Values.compareValue | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-reflection/templates/reflection-test.yaml\n# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-reflection-test\r\ndata:\r\n  # ==========================================\r\n  # Kind functions\r\n  # ==========================================\r\n  # Test: kindOf - returns the kind of a value\r\n  # Test: kindIs - checks if value is of a specific kind\r\n\r\n  # ==========================================\r\n  # Type functions\r\n  # ==========================================\r\n  # Test: typeOf - returns the type of a value\r\n  typeOfValue: \"string\"\r\n  # Test: typeIs - checks if value is of a specific type\r\n  # Test: typeIsLike - checks if value is like a specific type\r\n\r\n  # ==========================================\r\n  # Comparison functions\r\n  # ==========================================\r\n  # Test: deepEqual - compares two values for deep equality\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-reflection/templates/reflection-test.yaml",
            "content": "\n# Source: test-reflection/templates/reflection-test.yaml\n# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-reflection-test\ndata:\n  # ==========================================\n  # Kind functions\n  # ==========================================\n  # Test: kindOf - returns the kind of a value\n  # Test: kindIs - checks if value is of a specific kind\n\n  # ==========================================\n  # Type functions\n  # ==========================================\n  # Test: typeOf - returns the type of a value\n  typeOfValue: \"string\"\n  # Test: typeIs - checks if value is of a specific type\n  # Test: typeIsLike - checks if value is like a specific type\n\n  # ==========================================\n  # Comparison functions\n  # ==========================================\n  # Test: deepEqual - compares two values for deep equality\n",
            "originalContent": "# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-reflection-test\ndata:\n  # ==========================================\n  # Kind functions\n  # ==========================================\n  # Test: kindOf - returns the kind of a value\n  {{- if .Values.testKindOf }}\n  kindOfValue: {{ kindOf $testData | quote }}\n  {{- end }}\n  # Test: kindIs - checks if value is of a specific kind\n  {{- if .Values.testKindIs }}\n  kindIsValue: {{ kindIs \"string\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Type functions\n  # ==========================================\n  # Test: typeOf - returns the type of a value\n  {{- if .Values.testTypeOf }}\n  typeOfValue: {{ typeOf $testData | quote }}\n  {{- end }}\n  # Test: typeIs - checks if value is of a specific type\n  {{- if .Values.testTypeIs }}\n  typeIsValue: {{ typeIs \"string\" $testData | quote }}\n  {{- end }}\n  # Test: typeIsLike - checks if value is like a specific type\n  {{- if .Values.testTypeIsLike }}\n  typeIsLikeValue: {{ typeIsLike \"string\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Comparison functions\n  # ==========================================\n  # Test: deepEqual - compares two values for deep equality\n  {{- if .Values.testDeepEqual }}\n  deepEqualValue: {{ deepEqual $testData .Values.compareValue | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "resolved": true,
      "stopReason": "",
      "fixChain": []
    },
    {
      "toggle": "typeOf",
      "dataType": "numberValue",
      "valuesOptions": {
        "values": [
          "testTypeOf=true",
          "testDataType=numberValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-reflection/templates/reflection-test.yaml\n# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-reflection-test\r\ndata:\r\n  # ==========================================\r\n  # Kind functions\r\n  # ==========================================\r\n  # Test: kindOf - returns the kind of a value\r\n  # Test: kindIs - checks if value is of a specific kind\r\n\r\n  # ==========================================\r\n  # Type functions\r\n  # ==========================================\r\n  # Test: typeOf - returns the type of a value\r\n  typeOfValue: \"float64\"\r\n  # Test: typeIs - checks if value is of a specific type\r\n  # Test: typeIsLike - checks if value is like a specific type\r\n\r\n  # ==========================================\r\n  # Comparison functions\r\n  # ==========================================\r\n  # Test: deepEqual - compares two values for deep equality\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-reflection/templates/reflection-test.yaml",
            "content": "\n# Source: test-reflection/templates/reflection-test.yaml\n# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-reflection-test\ndata:\n  # ==========================================\n  # Kind functions\n  # ==========================================\n  # Test: kindOf - returns the kind of a value\n  # Test: kindIs - checks if value is of a specific kind\n\n  # ==========================================\n  # Type functions\n  # ==========================================\n  # Test: typeOf - returns the type of a value\n  typeOfValue: \"float64\"\n  # Test: typeIs - checks if value is of a specific type\n  # Test: typeIsLike - checks if value is like a specific type\n\n  # ==========================================\n  # Comparison functions\n  # ==========================================\n  # Test: deepEqual - compares two values for deep equality\n",
            "originalContent": "# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-reflection-test\ndata:\n  # ==========================================\n  # Kind functions\n  # ==========================================\n  # Test: kindOf - returns the kind of a value\n  {{- if .Values.testKindOf }}\n  kindOfValue: {{ kindOf $testData | quote }}\n  {{- end }}\n  # Test: kindIs - checks if value is of a specific kind\n  {{- if .Values.testKindIs }}\n  kindIsValue: {{ kindIs \"string\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Type functions\n  # ==========================================\n  # Test: typeOf - returns the type of a value\n  {{- if .Values.testTypeOf }}\n  typeOfValue: {{ typeOf $testData | quote }}\n  {{- end }}\n  # Test: typeIs - checks if value is of a specific type\n  {{- if .Values.testTypeIs }}\n  typeIsValue: {{ typeIs \"string\" $testData | quote }}\n  {{- end }}\n  # Test: typeIsLike - checks if value is like a specific type\n  {{- if .Values.testTypeIsLike }}\n  typeIsLikeValue: {{ typeIsLike \"string\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Comparison functions\n  # ==========================================\n  # Test: deepEqual - compares two values for deep equality\n  {{- if .Values.testDeepEqual }}\n  deepEqualValue: {{ deepEqual $testData .Values.compareValue | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-reflection/templates/reflection-test.yaml\n# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-reflection-test\r\ndata:\r\n  # ==========================================\r\n  # Kind functions\r\n  # ==========================================\r\n  # Test: kindOf - returns the kind of a value\r\n  # Test: kindIs - checks if value is of a specific kind\r\n\r\n  # ==========================================\r\n  # Type functions\r\n  # ==========================================\r\n  # Test: typeOf - returns the type of a value\r\n  typeOfValue: \"float64\"\r\n  # Test: typeIs - checks if value is of a specific type\r\n  # Test: typeIsLike - checks if value is like a specific type\r\n\r\n  # ==========================================\r\n  # Comparison functions\r\n  # ==========================================\r\n  # Test: deepEqual - compares two values for deep equality\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-reflection/templates/reflection-test.yaml",
            "content": "\n# Source: test-reflection/templates/reflection-test.yaml\n# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-reflection-test\ndata:\n  # ==========================================\n  # Kind functions\n  # ==========================================\n  # Test: kindOf - returns the kind of a value\n  # Test: kindIs - checks if value is of a specific kind\n\n  # ==========================================\n  # Type functions\n  # ==========================================\n  # Test: typeOf - returns the type of a value\n  typeOfValue: \"float64\"\n  # Test: typeIs - checks if value is of a specific type\n  # Test: typeIsLike - checks if value is like a specific type\n\n  # ==========================================\n  # Comparison functions\n  # ==========================================\n  # Test: deepEqual - compares two values for deep equality\n",
            "originalContent": "# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-reflection-test\ndata:\n  # ==========================================\n  # Kind functions\n  # ==========================================\n  # Test: kindOf - returns the kind of a value\n  {{- if .Values.testKindOf }}\n  kindOfValue: {{ kindOf $testData | quote }}\n  {{- end }}\n  # Test: kindIs - checks if value is of a specific kind\n  {{- if .Values.testKindIs }}\n  kindIsValue: {{ kindIs \"string\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Type functions\n  # ==========================================\n  # Test: typeOf - returns the type of a value\n  {{- if .Values.testTypeOf }}\n  typeOfValue: {{ typeOf $testData | quote }}\n  {{- end }}\n  # Test: typeIs - checks if value is of a specific type\n  {{- if .Values.testTypeIs }}\n  typeIsValue: {{ typeIs \"string\" $testData | quote }}\n  {{- end }}\n  # Test: typeIsLike - checks if value is like a specific type\n  {{- if .Values.testTypeIsLike }}\n  typeIsLikeValue: {{ typeIsLike \"string\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Comparison functions\n  # ==========================================\n  # Test: deepEqual - compares two values for deep equality\n  {{- if .Values.testDeepEqual }}\n  deepEqualValue: {{ deepEqual $testData .Values.compareValue | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "resolved": true,
      "stopReason": "",
      "fixChain": []
    },
    {
      "toggle": "typeOf",
      "dataType": "boolValue",
      "valuesOptions": {
        "values": [
          "testTypeOf=true",
          "testDataType=boolValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-reflection/templates/reflection-test.yaml\n# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-reflection-test\r\ndata:\r\n  # ==========================================\r\n  # Kind functions\r\n  # ==========================================\r\n  # Test: kindOf - returns the kind of a value\r\n  # Test: kindIs - checks if value is of a specific kind\r\n\r\n  # ==========================================\r\n  # Type functions\r\n  # ==========================================\r\n  # Test: typeOf - returns the type of a value\r\n  typeOfValue: \"bool\"\r\n  # Test: typeIs - checks if value is of a specific type\r\n  # Test: typeIsLike - checks if value is like a specific type\r\n\r\n  # ==========================================\r\n  # Comparison functions\r\n  # ==========================================\r\n  # Test: deepEqual - compares two values for deep equality\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-reflection/templates/reflection-test.yaml",
            "content": "\n# Source: test-reflection/templates/reflection-test.yaml\n# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-reflection-test\ndata:\n  # ==========================================\n  # Kind functions\n  # ==========================================\n  # Test: kindOf - returns the kind of a value\n  # Test: kindIs - checks if value is of a specific kind\n\n  # ==========================================\n  # Type functions\n  # ==========================================\n  # Test: typeOf - returns the type of a value\n  typeOfValue: \"bool\"\n  # Test: typeIs - checks if value is of a specific type\n  # Test: typeIsLike - checks if value is like a specific type\n\n  # ==========================================\n  # Comparison functions\n  # ==========================================\n  # Test: deepEqual - compares two values for deep equality\n",
            "originalContent": "# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-reflection-test\ndata:\n  # ==========================================\n  # Kind functions\n  # ==========================================\n  # Test: kindOf - returns the kind of a value\n  {{- if .Values.testKindOf }}\n  kindOfValue: {{ kindOf $testData | quote }}\n  {{- end }}\n  # Test: kindIs - checks if value is of a specific kind\n  {{- if .Values.testKindIs }}\n  kindIsValue: {{ kindIs \"string\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Type functions\n  # ==========================================\n  # Test: typeOf - returns the type of a value\n  {{- if .Values.testTypeOf }}\n  typeOfValue: {{ typeOf $testData | quote }}\n  {{- end }}\n  # Test: typeIs - checks if value is of a specific type\n  {{- if .Values.testTypeIs }}\n  typeIsValue: {{ typeIs \"string\" $testData | quote }}\n  {{- end }}\n  # Test: typeIsLike - checks if value is like a specific type\n  {{- if .Values.testTypeIsLike }}\n  typeIsLikeValue: {{ typeIsLike \"string\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Comparison functions\n  # ==========================================\n  # Test: deepEqual - compares two values for deep equality\n  {{- if .Values.testDeepEqual }}\n  deepEqualValue: {{ deepEqual $testData .Values.compareValue | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-reflection/templates/reflection-test.yaml\n# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-reflection-test\r\ndata:\r\n  # ==========================================\r\n  # Kind functions\r\n  # ==========================================\r\n  # Test: kindOf - returns the kind of a value\r\n  # Test: kindIs - checks if value is of a specific kind\r\n\r\n  # ==========================================\r\n  # Type functions\r\n  # ==========================================\r\n  # Test: typeOf - returns the type of a value\r\n  typeOfValue: \"bool\"\r\n  # Test: typeIs - checks if value is of a specific type\r\n  # Test: typeIsLike - checks if value is like a specific type\r\n\r\n  # ==========================================\r\n  # Comparison functions\r\n  # ==========================================\r\n  # Test: deepEqual - compares two values for deep equality\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-reflection/templates/reflection-test.yaml",
            "content": "\n# Source: test-reflection/templates/reflection-test.yaml\n# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-reflection-test\ndata:\n  # ==========================================\n  # Kind functions\n  # ==========================================\n  # Test: kindOf - returns the kind of a value\n  # Test: kindIs - checks if value is of a specific kind\n\n  # ==========================================\n  # Type functions\n  # ==========================================\n  # Test: typeOf - returns the type of a value\n  typeOfValue: \"bool\"\n  # Test: typeIs - checks if value is of a specific type\n  # Test: typeIsLike - checks if value is like a specific type\n\n  # ==========================================\n  # Comparison functions\n  # ==========================================\n  # Test: deepEqual - compares two values for deep equality\n",
            "originalContent": "# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-reflection-test\ndata:\n  # ==========================================\n  # Kind functions\n  # ==========================================\n  # Test: kindOf - returns the kind of a value\n  {{- if .Values.testKindOf }}\n  kindOfValue: {{ kindOf $testData | quote }}\n  {{- end }}\n  # Test: kindIs - checks if value is of a specific kind\n  {{- if .Values.testKindIs }}\n  kindIsValue: {{ kindIs \"string\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Type functions\n  # ==========================================\n  # Test: typeOf - returns the type of a value\n  {{- if .Values.testTypeOf }}\n  typeOfValue: {{ typeOf $testData | quote }}\n  {{- end }}\n  # Test: typeIs - checks if value is of a specific type\n  {{- if .Values.testTypeIs }}\n  typeIsValue: {{ typeIs \"string\" $testData | quote }}\n  {{- end }}\n  # Test: typeIsLike - checks if value is like a specific type\n  {{- if .Values.testTypeIsLike }}\n  typeIsLikeValue: {{ typeIsLike \"string\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Comparison functions\n  # ==========================================\n  # Test: deepEqual - compares two values for deep equality\n  {{- if .Values.testDeepEqual }}\n  deepEqualValue: {{ deepEqual $testData .Values.compareValue | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "resolved": true,
      "stopReason": "",
      "fixChain": []
    },
    {
      "toggle": "typeOf",
      "dataType": "listValue",
      "valuesOptions": {
        "values": [
          "testTypeOf=true",
          "testDataType=listValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-reflection/templates/reflection-test.yaml\n# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-reflection-test\r\ndata:\r\n  # ==========================================\r\n  # Kind functions\r\n  # ==========================================\r\n  # Test: kindOf - returns the kind of a value\r\n  # Test: kindIs - checks if value is of a specific kind\r\n\r\n  # ==========================================\r\n  # Type functions\r\n  # ==========================================\r\n  # Test: typeOf - returns the type of a value\r\n  typeOfValue: \"[]interface {}\"\r\n  # Test: typeIs - checks if value is of a specific type\r\n  # Test: typeIsLike - checks if value is like a specific type\r\n\r\n  # ==========================================\r\n  # Comparison functions\r\n  # ==========================================\r\n  # Test: deepEqual - compares two values for deep equality\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-reflection/templates/reflection-test.yaml",
            "content": "\n# Source: test-reflection/templates/reflection-test.yaml\n# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-reflection-test\ndata:\n  # ==========================================\n  # Kind functions\n  # ==========================================\n  # Test: kindOf - returns the kind of a value\n  # Test: kindIs - checks if value is of a specific kind\n\n  # ==========================================\n  # Type functions\n  # ==========================================\n  # Test: typeOf - returns the type of a value\n  typeOfValue: \"[]interface {}\"\n  # Test: typeIs - checks if value is of a specific type\n  # Test: typeIsLike - checks if value is like a specific type\n\n  # ==========================================\n  # Comparison functions\n  # ==========================================\n  # Test: deepEqual - compares two values for deep equality\n",
            "originalContent": "# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-reflection-test\ndata:\n  # ==========================================\n  # Kind functions\n  # ==========================================\n  # Test: kindOf - returns the kind of a value\n  {{- if .Values.testKindOf }}\n  kindOfValue: {{ kindOf $testData | quote }}\n  {{- end }}\n  # Test: kindIs - checks if value is of a specific kind\n  {{- if .Values.testKindIs }}\n  kindIsValue: {{ kindIs \"string\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Type functions\n  # ==========================================\n  # Test: typeOf - returns the type of a value\n  {{- if .Values.testTypeOf }}\n  typeOfValue: {{ typeOf $testData | quote }}\n  {{- end }}\n  # Test: typeIs - checks if value is of a specific type\n  {{- if .Values.testTypeIs }}\n  typeIsValue: {{ typeIs \"string\" $testData | quote }}\n  {{- end }}\n  # Test: typeIsLike - checks if value is like a specific type\n  {{- if .Values.testTypeIsLike }}\n  typeIsLikeValue: {{ typeIsLike \"string\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Comparison functions\n  # ==========================================\n  # Test: deepEqual - compares two values for deep equality\n  {{- if .Values.testDeepEqual }}\n  deepEqualValue: {{ deepEqual $testData .Values.compareValue | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-reflection/templates/reflection-test.yaml\n# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-reflection-test\r\ndata:\r\n  # ==========================================\r\n  # Kind functions\r\n  # ==========================================\r\n  # Test: kindOf - returns the kind of a value\r\n  # Test: kindIs - checks if value is of a specific kind\r\n\r\n  # ==========================================\r\n  # Type functions\r\n  # ==========================================\r\n  # Test: typeOf - returns the type of a value\r\n  typeOfValue: \"[]interface {}\"\r\n  # Test: typeIs - checks if value is of a specific type\r\n  # Test: typeIsLike - checks if value is like a specific type\r\n\r\n  # ==========================================\r\n  # Comparison functions\r\n  # ==========================================\r\n  # Test: deepEqual - compares two values for deep equality\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-reflection/templates/reflection-test.yaml",
            "content": "\n# Source: test-reflection/templates/reflection-test.yaml\n# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-reflection-test\ndata:\n  # ==========================================\n  # Kind functions\n  # ==========================================\n  # Test: kindOf - returns the kind of a value\n  # Test: kindIs - checks if value is of a specific kind\n\n  # ==========================================\n  # Type functions\n  # ==========================================\n  # Test: typeOf - returns the type of a value\n  typeOfValue: \"[]interface {}\"\n  # Test: typeIs - checks if value is of a specific type\n  # Test: typeIsLike - checks if value is like a specific type\n\n  # ==========================================\n  # Comparison functions\n  # ==========================================\n  # Test: deepEqual - compares two values for deep equality\n",
            "originalContent": "# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-reflection-test\ndata:\n  # ==========================================\n  # Kind functions\n  # ==========================================\n  # Test: kindOf - returns the kind of a value\n  {{- if .Values.testKindOf }}\n  kindOfValue: {{ kindOf $testData | quote }}\n  {{- end }}\n  # Test: kindIs - checks if value is of a specific kind\n  {{- if .Values.testKindIs }}\n  kindIsValue: {{ kindIs \"string\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Type functions\n  # ==========================================\n  # Test: typeOf - returns the type of a value\n  {{- if .Values.testTypeOf }}\n  typeOfValue: {{ typeOf $testData | quote }}\n  {{- end }}\n  # Test: typeIs - checks if value is of a specific type\n  {{- if .Values.testTypeIs }}\n  typeIsValue: {{ typeIs \"string\" $testData | quote }}\n  {{- end }}\n  # Test: typeIsLike - checks if value is like a specific type\n  {{- if .Values.testTypeIsLike }}\n  typeIsLikeValue: {{ typeIsLike \"string\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Comparison functions\n  # ==========================================\n  # Test: deepEqual - compares two values for deep equality\n  {{- if .Values.testDeepEqual }}\n  deepEqualValue: {{ deepEqual $testData .Values.compareValue | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "resolved": true,
      "stopReason": "",
      "fixChain": []
    },
    {
      "toggle": "typeOf",
      "dataType": "dictValue",
      "valuesOptions": {
        "values": [
          "testTypeOf=true",
          "testDataType=dictValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-reflection/templates/reflection-test.yaml\n# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-reflection-test\r\ndata:\r\n  # ==========================================\r\n  # Kind functions\r\n  # ==========================================\r\n  # Test: kindOf - returns the kind of a value\r\n  # Test: kindIs - checks if value is of a specific kind\r\n\r\n  # ==========================================\r\n  # Type functions\r\n  # ==========================================\r\n  # Test: typeOf - returns the type of a value\r\n  typeOfValue: \"map[string]interface {}\"\r\n  # Test: typeIs - checks if value is of a specific type\r\n  # Test: typeIsLike - checks if value is like a specific type\r\n\r\n  # ==========================================\r\n  # Comparison functions\r\n  # ==========================================\r\n  # Test: deepEqual - compares two values for deep equality\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-reflection/templates/reflection-test.yaml",
            "content": "\n# Source: test-reflection/templates/reflection-test.yaml\n# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-reflection-test\ndata:\n  # ==========================================\n  # Kind functions\n  # ==========================================\n  # Test: kindOf - returns the kind of a value\n  # Test: kindIs - checks if value is of a specific kind\n\n  # ==========================================\n  # Type functions\n  # ==========================================\n  # Test: typeOf - returns the type of a value\n  typeOfValue: \"map[string]interface {}\"\n  # Test: typeIs - checks if value is of a specific type\n  # Test: typeIsLike - checks if value is like a specific type\n\n  # ==========================================\n  # Comparison functions\n  # ==========================================\n  # Test: deepEqual - compares two values for deep equality\n",
            "originalContent": "# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-reflection-test\ndata:\n  # ==========================================\n  # Kind functions\n  # ==========================================\n  # Test: kindOf - returns the kind of a value\n  {{- if .Values.testKindOf }}\n  kindOfValue: {{ kindOf $testData | quote }}\n  {{- end }}\n  # Test: kindIs - checks if value is of a specific kind\n  {{- if .Values.testKindIs }}\n  kindIsValue: {{ kindIs \"string\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Type functions\n  # ==========================================\n  # Test: typeOf - returns the type of a value\n  {{- if .Values.testTypeOf }}\n  typeOfValue: {{ typeOf $testData | quote }}\n  {{- end }}\n  # Test: typeIs - checks if value is of a specific type\n  {{- if .Values.testTypeIs }}\n  typeIsValue: {{ typeIs \"string\" $testData | quote }}\n  {{- end }}\n  # Test: typeIsLike - checks if value is like a specific type\n  {{- if .Values.testTypeIsLike }}\n  typeIsLikeValue: {{ typeIsLike \"string\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Comparison functions\n  # ==========================================\n  # Test: deepEqual - compares two values for deep equality\n  {{- if .Values.testDeepEqual }}\n  deepEqualValue: {{ deepEqual $testData .Values.compareValue | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-reflection/templates/reflection-test.yaml\n# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-reflection-test\r\ndata:\r\n  # ==========================================\r\n  # Kind functions\r\n  # ==========================================\r\n  # Test: kindOf - returns the kind of a value\r\n  # Test: kindIs - checks if value is of a specific kind\r\n\r\n  # ==========================================\r\n  # Type functions\r\n  # ==========================================\r\n  # Test: typeOf - returns the type of a value\r\n  typeOfValue: \"map[string]interface {}\"\r\n  # Test: typeIs - checks if value is of a specific type\r\n  # Test: typeIsLike - checks if value is like a specific type\r\n\r\n  # ==========================================\r\n  # Comparison functions\r\n  # ==========================================\r\n  # Test: deepEqual - compares two values for deep equality\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-reflection/templates/reflection-test.yaml",
            "content": "\n# Source: test-reflection/templates/reflection-test.yaml\n# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-reflection-test\ndata:\n  # ==========================================\n  # Kind functions\n  # ==========================================\n  # Test: kindOf - returns the kind of a value\n  # Test: kindIs - checks if value is of a specific kind\n\n  # ==========================================\n  # Type functions\n  # ==========================================\n  # Test: typeOf - returns the type of a value\n  typeOfValue: \"map[string]interface {}\"\n  # Test: typeIs - checks if value is of a specific type\n  # Test: typeIsLike - checks if value is like a specific type\n\n  # ==========================================\n  # Comparison functions\n  # ==========================================\n  # Test: deepEqual - compares two values for deep equality\n",
            "originalContent": "# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-reflection-test\ndata:\n  # ==========================================\n  # Kind functions\n  # ==========================================\n  # Test: kindOf - returns the kind of a value\n  {{- if .Values.testKindOf }}\n  kindOfValue: {{ kindOf $testData | quote }}\n  {{- end }}\n  # Test: kindIs - checks if value is of a specific kind\n  {{- if .Values.testKindIs }}\n  kindIsValue: {{ kindIs \"string\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Type functions\n  # ==========================================\n  # Test: typeOf - returns the type of a value\n  {{- if .Values.testTypeOf }}\n  typeOfValue: {{ typeOf $testData | quote }}\n  {{- end }}\n  # Test: typeIs - checks if value is of a specific type\n  {{- if .Values.testTypeIs }}\n  typeIsValue: {{ typeIs \"string\" $testData | quote }}\n  {{- end }}\n  # Test: typeIsLike - checks if value is like a specific type\n  {{- if .Values.testTypeIsLike }}\n  typeIsLikeValue: {{ typeIsLike \"string\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Comparison functions\n  # ==========================================\n  # Test: deepEqual - compares two values for deep equality\n  {{- if .Values.testDeepEqual }}\n  deepEqualValue: {{ deepEqual $testData .Values.compareValue | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "resolved": true,
      "stopReason": "",
      "fixChain": []
    },
    {
      "toggle": "typeIs",
      "dataType": "nilValue",
      "valuesOptions": {
        "values": [
          "testTypeIs=true",
          "testDataType=nilValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-reflection/templates/reflection-test.yaml\n# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-reflection-test\r\ndata:\r\n  # ==========================================\r\n  # Kind functions\r\n  # ==========================================\r\n  # Test: kindOf - returns the kind of a value\r\n  # Test: kindIs - checks if value is of a specific kind\r\n\r\n  # ==========================================\r\n  # Type functions\r\n  # ==========================================\r\n  # Test: typeOf - returns the type of a value\r\n  # Test: typeIs - checks if value is of a specific type\r\n  typeIsValue: \"false\"\r\n  # Test: typeIsLike - checks if value is like a specific type\r\n\r\n  # ==========================================\r\n  # Comparison functions\r\n  # ==========================================\r\n  # Test: deepEqual - compares two values for deep equality\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-reflection/templates/reflection-test.yaml",
            "content": "\n# Source: test-reflection/templates/reflection-test.yaml\n# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-reflection-test\ndata:\n  # ==========================================\n  # Kind functions\n  # ==========================================\n  # Test: kindOf - returns the kind of a value\n  # Test: kindIs - checks if value is of a specific kind\n\n  # ==========================================\n  # Type functions\n  # ==========================================\n  # Test: typeOf - returns the type of a value\n  # Test: typeIs - checks if value is of a specific type\n  typeIsValue: \"false\"\n  # Test: typeIsLike - checks if value is like a specific type\n\n  # ==========================================\n  # Comparison functions\n  # ==========================================\n  # Test: deepEqual - compares two values for deep equality\n",
            "originalContent": "# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-reflection-test\ndata:\n  # ==========================================\n  # Kind functions\n  # ==========================================\n  # Test: kindOf - returns the kind of a value\n  {{- if .Values.testKindOf }}\n  kindOfValue: {{ kindOf $testData | quote }}\n  {{- end }}\n  # Test: kindIs - checks if value is of a specific kind\n  {{- if .Values.testKindIs }}\n  kindIsValue: {{ kindIs \"string\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Type functions\n  # ==========================================\n  # Test: typeOf - returns the type of a value\n  {{- if .Values.testTypeOf }}\n  typeOfValue: {{ typeOf $testData | quote }}\n  {{- end }}\n  # Test: typeIs - checks if value is of a specific type\n  {{- if .Values.testTypeIs }}\n  typeIsValue: {{ typeIs \"string\" $testData | quote }}\n  {{- end }}\n  # Test: typeIsLike - checks if value is like a specific type\n  {{- if .Values.testTypeIsLike }}\n  typeIsLikeValue: {{ typeIsLike \"string\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Comparison functions\n  # ==========================================\n  # Test: deepEqual - compares two values for deep equality\n  {{- if .Values.testDeepEqual }}\n  deepEqualValue: {{ deepEqual $testData .Values.compareValue | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-reflection/templates/reflection-test.yaml\n# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-reflection-test\r\ndata:\r\n  # ==========================================\r\n  # Kind functions\r\n  # ==========================================\r\n  # Test: kindOf - returns the kind of a value\r\n  # Test: kindIs - checks if value is of a specific kind\r\n\r\n  # ==========================================\r\n  # Type functions\r\n  # ==========================================\r\n  # Test: typeOf - returns the type of a value\r\n  # Test: typeIs - checks if value is of a specific type\r\n  typeIsValue: \"false\"\r\n  # Test: typeIsLike - checks if value is like a specific type\r\n\r\n  # ==========================================\r\n  # Comparison functions\r\n  # ==========================================\r\n  # Test: deepEqual - compares two values for deep equality\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-reflection/templates/reflection-test.yaml",
            "content": "\n# Source: test-reflection/templates/reflection-test.yaml\n# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-reflection-test\ndata:\n  # ==========================================\n  # Kind functions\n  # ==========================================\n  # Test: kindOf - returns the kind of a value\n  # Test: kindIs - checks if value is of a specific kind\n\n  # ==========================================\n  # Type functions\n  # ==========================================\n  # Test: typeOf - returns the type of a value\n  # Test: typeIs - checks if value is of a specific type\n  typeIsValue: \"false\"\n  # Test: typeIsLike - checks if value is like a specific type\n\n  # ==========================================\n  # Comparison functions\n  # ==========================================\n  # Test: deepEqual - compares two values for deep equality\n",
            "originalContent": "# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-reflection-test\ndata:\n  # ==========================================\n  # Kind functions\n  # ==========================================\n  # Test: kindOf - returns the kind of a value\n  {{- if .Values.testKindOf }}\n  kindOfValue: {{ kindOf $testData | quote }}\n  {{- end }}\n  # Test: kindIs - checks if value is of a specific kind\n  {{- if .Values.testKindIs }}\n  kindIsValue: {{ kindIs \"string\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Type functions\n  # ==========================================\n  # Test: typeOf - returns the type of a value\n  {{- if .Values.testTypeOf }}\n  typeOfValue: {{ typeOf $testData | quote }}\n  {{- end }}\n  # Test: typeIs - checks if value is of a specific type\n  {{- if .Values.testTypeIs }}\n  typeIsValue: {{ typeIs \"string\" $testData | quote }}\n  {{- end }}\n  # Test: typeIsLike - checks if value is like a specific type\n  {{- if .Values.testTypeIsLike }}\n  typeIsLikeValue: {{ typeIsLike \"string\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Comparison functions\n  # ==========================================\n  # Test: deepEqual - compares two values for deep equality\n  {{- if .Values.testDeepEqual }}\n  deepEqualValue: {{ deepEqual $testData .Values.compareValue | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "resolved": true,
      "stopReason": "",
      "fixChain": []
    },
    {
      "toggle": "typeIs",
      "dataType": "stringValue",
      "valuesOptions": {
        "values": [
          "testTypeIs=true",
          "testDataType=stringValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-reflection/templates/reflection-test.yaml\n# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-reflection-test\r\ndata:\r\n  # ==========================================\r\n  # Kind functions\r\n  # ==========================================\r\n  # Test: kindOf - returns the kind of a value\r\n  # Test: kindIs - checks if value is of a specific kind\r\n\r\n  # ==========================================\r\n  # Type functions\r\n  # ==========================================\r\n  # Test: typeOf - returns the type of a value\r\n  # Test: typeIs - checks if value is of a specific type\r\n  typeIsValue: \"true\"\r\n  # Test: typeIsLike - checks if value is like a specific type\r\n\r\n  # ==========================================\r\n  # Comparison functions\r\n  # ==========================================\r\n  # Test: deepEqual - compares two values for deep equality\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-reflection/templates/reflection-test.yaml",
            "content": "\n# Source: test-reflection/templates/reflection-test.yaml\n# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-reflection-test\ndata:\n  # ==========================================\n  # Kind functions\n  # ==========================================\n  # Test: kindOf - returns the kind of a value\n  # Test: kindIs - checks if value is of a specific kind\n\n  # ==========================================\n  # Type functions\n  # ==========================================\n  # Test: typeOf - returns the type of a value\n  # Test: typeIs - checks if value is of a specific type\n  typeIsValue: \"true\"\n  # Test: typeIsLike - checks if value is like a specific type\n\n  # ==========================================\n  # Comparison functions\n  # ==========================================\n  # Test: deepEqual - compares two values for deep equality\n",
            "originalContent": "# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-reflection-test\ndata:\n  # ==========================================\n  # Kind functions\n  # ==========================================\n  # Test: kindOf - returns the kind of a value\n  {{- if .Values.testKindOf }}\n  kindOfValue: {{ kindOf $testData | quote }}\n  {{- end }}\n  # Test: kindIs - checks if value is of a specific kind\n  {{- if .Values.testKindIs }}\n  kindIsValue: {{ kindIs \"string\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Type functions\n  # ==========================================\n  # Test: typeOf - returns the type of a value\n  {{- if .Values.testTypeOf }}\n  typeOfValue: {{ typeOf $testData | quote }}\n  {{- end }}\n  # Test: typeIs - checks if value is of a specific type\n  {{- if .Values.testTypeIs }}\n  typeIsValue: {{ typeIs \"string\" $testData | quote }}\n  {{- end }}\n  # Test: typeIsLike - checks if value is like a specific type\n  {{- if .Values.testTypeIsLike }}\n  typeIsLikeValue: {{ typeIsLike \"string\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Comparison functions\n  # ==========================================\n  # Test: deepEqual - compares two values for deep equality\n  {{- if .Values.testDeepEqual }}\n  deepEqualValue: {{ deepEqual $testData .Values.compareValue | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-reflection/templates/reflection-test.yaml\n# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-reflection-test\r\ndata:\r\n  # ==========================================\r\n  # Kind functions\r\n  # ==========================================\r\n  # Test: kindOf - returns the kind of a value\r\n  # Test: kindIs - checks if value is of a specific kind\r\n\r\n  # ==========================================\r\n  # Type functions\r\n  # ==========================================\r\n  # Test: typeOf - returns the type of a value\r\n  # Test: typeIs - checks if value is of a specific type\r\n  typeIsValue: \"true\"\r\n  # Test: typeIsLike - checks if value is like a specific type\r\n\r\n  # ==========================================\r\n  # Comparison functions\r\n  # ==========================================\r\n  # Test: deepEqual - compares two values for deep equality\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-reflection/templates/reflection-test.yaml",
            "content": "\n# Source: test-reflection/templates/reflection-test.yaml\n# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-reflection-test\ndata:\n  # ==========================================\n  # Kind functions\n  # ==========================================\n  # Test: kindOf - returns the kind of a value\n  # Test: kindIs - checks if value is of a specific kind\n\n  # ==========================================\n  # Type functions\n  # ==========================================\n  # Test: typeOf - returns the type of a value\n  # Test: typeIs - checks if value is of a specific type\n  typeIsValue: \"true\"\n  # Test: typeIsLike - checks if value is like a specific type\n\n  # ==========================================\n  # Comparison functions\n  # ==========================================\n  # Test: deepEqual - compares two values for deep equality\n",
            "originalContent": "# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-reflection-test\ndata:\n  # ==========================================\n  # Kind functions\n  # ==========================================\n  # Test: kindOf - returns the kind of a value\n  {{- if .Values.testKindOf }}\n  kindOfValue: {{ kindOf $testData | quote }}\n  {{- end }}\n  # Test: kindIs - checks if value is of a specific kind\n  {{- if .Values.testKindIs }}\n  kindIsValue: {{ kindIs \"string\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Type functions\n  # ==========================================\n  # Test: typeOf - returns the type of a value\n  {{- if .Values.testTypeOf }}\n  typeOfValue: {{ typeOf $testData | quote }}\n  {{- end }}\n  # Test: typeIs - checks if value is of a specific type\n  {{- if .Values.testTypeIs }}\n  typeIsValue: {{ typeIs \"string\" $testData | quote }}\n  {{- end }}\n  # Test: typeIsLike - checks if value is like a specific type\n  {{- if .Values.testTypeIsLike }}\n  typeIsLikeValue: {{ typeIsLike \"string\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Comparison functions\n  # ==========================================\n  # Test: deepEqual - compares two values for deep equality\n  {{- if .Values.testDeepEqual }}\n  deepEqualValue: {{ deepEqual $testData .Values.compareValue | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "resolved": true,
      "stopReason": "",
      "fixChain": []
    },
    {
      "toggle": "typeIs",
      "dataType": "numberValue",
      "valuesOptions": {
        "values": [
          "testTypeIs=true",
          "testDataType=numberValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-reflection/templates/reflection-test.yaml\n# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-reflection-test\r\ndata:\r\n  # ==========================================\r\n  # Kind functions\r\n  # ==========================================\r\n  # Test: kindOf - returns the kind of a value\r\n  # Test: kindIs - checks if value is of a specific kind\r\n\r\n  # ==========================================\r\n  # Type functions\r\n  # ==========================================\r\n  # Test: typeOf - returns the type of a value\r\n  # Test: typeIs - checks if value is of a specific type\r\n  typeIsValue: \"false\"\r\n  # Test: typeIsLike - checks if value is like a specific type\r\n\r\n  # ==========================================\r\n  # Comparison functions\r\n  # ==========================================\r\n  # Test: deepEqual - compares two values for deep equality\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-reflection/templates/reflection-test.yaml",
            "content": "\n# Source: test-reflection/templates/reflection-test.yaml\n# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-reflection-test\ndata:\n  # ==========================================\n  # Kind functions\n  # ==========================================\n  # Test: kindOf - returns the kind of a value\n  # Test: kindIs - checks if value is of a specific kind\n\n  # ==========================================\n  # Type functions\n  # ==========================================\n  # Test: typeOf - returns the type of a value\n  # Test: typeIs - checks if value is of a specific type\n  typeIsValue: \"false\"\n  # Test: typeIsLike - checks if value is like a specific type\n\n  # ==========================================\n  # Comparison functions\n  # ==========================================\n  # Test: deepEqual - compares two values for deep equality\n",
            "originalContent": "# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-reflection-test\ndata:\n  # ==========================================\n  # Kind functions\n  # ==========================================\n  # Test: kindOf - returns the kind of a value\n  {{- if .Values.testKindOf }}\n  kindOfValue: {{ kindOf $testData | quote }}\n  {{- end }}\n  # Test: kindIs - checks if value is of a specific kind\n  {{- if .Values.testKindIs }}\n  kindIsValue: {{ kindIs \"string\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Type functions\n  # ==========================================\n  # Test: typeOf - returns the type of a value\n  {{- if .Values.testTypeOf }}\n  typeOfValue: {{ typeOf $testData | quote }}\n  {{- end }}\n  # Test: typeIs - checks if value is of a specific type\n  {{- if .Values.testTypeIs }}\n  typeIsValue: {{ typeIs \"string\" $testData | quote }}\n  {{- end }}\n  # Test: typeIsLike - checks if value is like a specific type\n  {{- if .Values.testTypeIsLike }}\n  typeIsLikeValue: {{ typeIsLike \"string\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Comparison functions\n  # ==========================================\n  # Test: deepEqual - compares two values for deep equality\n  {{- if .Values.testDeepEqual }}\n  deepEqualValue: {{ deepEqual $testData .Values.compareValue | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-reflection/templates/reflection-test.yaml\n# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-reflection-test\r\ndata:\r\n  # ==========================================\r\n  # Kind functions\r\n  # ==========================================\r\n  # Test: kindOf - returns the kind of a value\r\n  # Test: kindIs - checks if value is of a specific kind\r\n\r\n  # ==========================================\r\n  # Type functions\r\n  # ==========================================\r\n  # Test: typeOf - returns the type of a value\r\n  # Test: typeIs - checks if value is of a specific type\r\n  typeIsValue: \"false\"\r\n  # Test: typeIsLike - checks if value is like a specific type\r\n\r\n  # ==========================================\r\n  # Comparison functions\r\n  # ==========================================\r\n  # Test: deepEqual - compares two values for deep equality\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-reflection/templates/reflection-test.yaml",
            "content": "\n# Source: test-reflection/templates/reflection-test.yaml\n# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-reflection-test\ndata:\n  # ==========================================\n  # Kind functions\n  # ==========================================\n  # Test: kindOf - returns the kind of a value\n  # Test: kindIs - checks if value is of a specific kind\n\n  # ==========================================\n  # Type functions\n  # ==========================================\n  # Test: typeOf - returns the type of a value\n  # Test: typeIs - checks if value is of a specific type\n  typeIsValue: \"false\"\n  # Test: typeIsLike - checks if value is like a specific type\n\n  # ==========================================\n  # Comparison functions\n  # ==========================================\n  # Test: deepEqual - compares two values for deep equality\n",
            "originalContent": "# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-reflection-test\ndata:\n  # ==========================================\n  # Kind functions\n  # ==========================================\n  # Test: kindOf - returns the kind of a value\n  {{- if .Values.testKindOf }}\n  kindOfValue: {{ kindOf $testData | quote }}\n  {{- end }}\n  # Test: kindIs - checks if value is of a specific kind\n  {{- if .Values.testKindIs }}\n  kindIsValue: {{ kindIs \"string\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Type functions\n  # ==========================================\n  # Test: typeOf - returns the type of a value\n  {{- if .Values.testTypeOf }}\n  typeOfValue: {{ typeOf $testData | quote }}\n  {{- end }}\n  # Test: typeIs - checks if value is of a specific type\n  {{- if .Values.testTypeIs }}\n  typeIsValue: {{ typeIs \"string\" $testData | quote }}\n  {{- end }}\n  # Test: typeIsLike - checks if value is like a specific type\n  {{- if .Values.testTypeIsLike }}\n  typeIsLikeValue: {{ typeIsLike \"string\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Comparison functions\n  # ==========================================\n  # Test: deepEqual - compares two values for deep equality\n  {{- if .Values.testDeepEqual }}\n  deepEqualValue: {{ deepEqual $testData .Values.compareValue | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "resolved": true,
      "stopReason": "",
      "fixChain": []
    },
    {
      "toggle": "typeIs",
      "dataType": "boolValue",
      "valuesOptions": {
        "values": [
          "testTypeIs=true",
          "testDataType=boolValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-reflection/templates/reflection-test.yaml\n# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-reflection-test\r\ndata:\r\n  # ==========================================\r\n  # Kind functions\r\n  # ==========================================\r\n  # Test: kindOf - returns the kind of a value\r\n  # Test: kindIs - checks if value is of a specific kind\r\n\r\n  # ==========================================\r\n  # Type functions\r\n  # ==========================================\r\n  # Test: typeOf - returns the type of a value\r\n  # Test: typeIs - checks if value is of a specific type\r\n  typeIsValue: \"false\"\r\n  # Test: typeIsLike - checks if value is like a specific type\r\n\r\n  # ==========================================\r\n  # Comparison functions\r\n  # ==========================================\r\n  # Test: deepEqual - compares two values for deep equality\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-reflection/templates/reflection-test.yaml",
            "content": "\n# Source: test-reflection/templates/reflection-test.yaml\n# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-reflection-test\ndata:\n  # ==========================================\n  # Kind functions\n  # ==========================================\n  # Test: kindOf - returns the kind of a value\n  # Test: kindIs - checks if value is of a specific kind\n\n  # ==========================================\n  # Type functions\n  # ==========================================\n  # Test: typeOf - returns the type of a value\n  # Test: typeIs - checks if value is of a specific type\n  typeIsValue: \"false\"\n  # Test: typeIsLike - checks if value is like a specific type\n\n  # ==========================================\n  # Comparison functions\n  # ==========================================\n  # Test: deepEqual - compares two values for deep equality\n",
            "originalContent": "# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-reflection-test\ndata:\n  # ==========================================\n  # Kind functions\n  # ==========================================\n  # Test: kindOf - returns the kind of a value\n  {{- if .Values.testKindOf }}\n  kindOfValue: {{ kindOf $testData | quote }}\n  {{- end }}\n  # Test: kindIs - checks if value is of a specific kind\n  {{- if .Values.testKindIs }}\n  kindIsValue: {{ kindIs \"string\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Type functions\n  # ==========================================\n  # Test: typeOf - returns the type of a value\n  {{- if .Values.testTypeOf }}\n  typeOfValue: {{ typeOf $testData | quote }}\n  {{- end }}\n  # Test: typeIs - checks if value is of a specific type\n  {{- if .Values.testTypeIs }}\n  typeIsValue: {{ typeIs \"string\" $testData | quote }}\n  {{- end }}\n  # Test: typeIsLike - checks if value is like a specific type\n  {{- if .Values.testTypeIsLike }}\n  typeIsLikeValue: {{ typeIsLike \"string\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Comparison functions\n  # ==========================================\n  # Test: deepEqual - compares two values for deep equality\n  {{- if .Values.testDeepEqual }}\n  deepEqualValue: {{ deepEqual $testData .Values.compareValue | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-reflection/templates/reflection-test.yaml\n# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-reflection-test\r\ndata:\r\n  # ==========================================\r\n  # Kind functions\r\n  # ==========================================\r\n  # Test: kindOf - returns the kind of a value\r\n  # Test: kindIs - checks if value is of a specific kind\r\n\r\n  # ==========================================\r\n  # Type functions\r\n  # ==========================================\r\n  # Test: typeOf - returns the type of a value\r\n  # Test: typeIs - checks if value is of a specific type\r\n  typeIsValue: \"false\"\r\n  # Test: typeIsLike - checks if value is like a specific type\r\n\r\n  # ==========================================\r\n  # Comparison functions\r\n  # ==========================================\r\n  # Test: deepEqual - compares two values for deep equality\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-reflection/templates/reflection-test.yaml",
            "content": "\n# Source: test-reflection/templates/reflection-test.yaml\n# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-reflection-test\ndata:\n  # ==========================================\n  # Kind functions\n  # ==========================================\n  # Test: kindOf - returns the kind of a value\n  # Test: kindIs - checks if value is of a specific kind\n\n  # ==========================================\n  # Type functions\n  # ==========================================\n  # Test: typeOf - returns the type of a value\n  # Test: typeIs - checks if value is of a specific type\n  typeIsValue: \"false\"\n  # Test: typeIsLike - checks if value is like a specific type\n\n  # ==========================================\n  # Comparison functions\n  # ==========================================\n  # Test: deepEqual - compares two values for deep equality\n",
            "originalContent": "# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-reflection-test\ndata:\n  # ==========================================\n  # Kind functions\n  # ==========================================\n  # Test: kindOf - returns the kind of a value\n  {{- if .Values.testKindOf }}\n  kindOfValue: {{ kindOf $testData | quote }}\n  {{- end }}\n  # Test: kindIs - checks if value is of a specific kind\n  {{- if .Values.testKindIs }}\n  kindIsValue: {{ kindIs \"string\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Type functions\n  # ==========================================\n  # Test: typeOf - returns the type of a value\n  {{- if .Values.testTypeOf }}\n  typeOfValue: {{ typeOf $testData | quote }}\n  {{- end }}\n  # Test: typeIs - checks if value is of a specific type\n  {{- if .Values.testTypeIs }}\n  typeIsValue: {{ typeIs \"string\" $testData | quote }}\n  {{- end }}\n  # Test: typeIsLike - checks if value is like a specific type\n  {{- if .Values.testTypeIsLike }}\n  typeIsLikeValue: {{ typeIsLike \"string\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Comparison functions\n  # ==========================================\n  # Test: deepEqual - compares two values for deep equality\n  {{- if .Values.testDeepEqual }}\n  deepEqualValue: {{ deepEqual $testData .Values.compareValue | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "resolved": true,
      "stopReason": "",
      "fixChain": []
    },
    {
      "toggle": "typeIs",
      "dataType": "listValue",
      "valuesOptions": {
        "values": [
          "testTypeIs=true",
          "testDataType=listValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-reflection/templates/reflection-test.yaml\n# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-reflection-test\r\ndata:\r\n  # ==========================================\r\n  # Kind functions\r\n  # ==========================================\r\n  # Test: kindOf - returns the kind of a value\r\n  # Test: kindIs - checks if value is of a specific kind\r\n\r\n  # ==========================================\r\n  # Type functions\r\n  # ==========================================\r\n  # Test: typeOf - returns the type of a value\r\n  # Test: typeIs - checks if value is of a specific type\r\n  typeIsValue: \"false\"\r\n  # Test: typeIsLike - checks if value is like a specific type\r\n\r\n  # ==========================================\r\n  # Comparison functions\r\n  # ==========================================\r\n  # Test: deepEqual - compares two values for deep equality\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-reflection/templates/reflection-test.yaml",
            "content": "\n# Source: test-reflection/templates/reflection-test.yaml\n# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-reflection-test\ndata:\n  # ==========================================\n  # Kind functions\n  # ==========================================\n  # Test: kindOf - returns the kind of a value\n  # Test: kindIs - checks if value is of a specific kind\n\n  # ==========================================\n  # Type functions\n  # ==========================================\n  # Test: typeOf - returns the type of a value\n  # Test: typeIs - checks if value is of a specific type\n  typeIsValue: \"false\"\n  # Test: typeIsLike - checks if value is like a specific type\n\n  # ==========================================\n  # Comparison functions\n  # ==========================================\n  # Test: deepEqual - compares two values for deep equality\n",
            "originalContent": "# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-reflection-test\ndata:\n  # ==========================================\n  # Kind functions\n  # ==========================================\n  # Test: kindOf - returns the kind of a value\n  {{- if .Values.testKindOf }}\n  kindOfValue: {{ kindOf $testData | quote }}\n  {{- end }}\n  # Test: kindIs - checks if value is of a specific kind\n  {{- if .Values.testKindIs }}\n  kindIsValue: {{ kindIs \"string\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Type functions\n  # ==========================================\n  # Test: typeOf - returns the type of a value\n  {{- if .Values.testTypeOf }}\n  typeOfValue: {{ typeOf $testData | quote }}\n  {{- end }}\n  # Test: typeIs - checks if value is of a specific type\n  {{- if .Values.testTypeIs }}\n  typeIsValue: {{ typeIs \"string\" $testData | quote }}\n  {{- end }}\n  # Test: typeIsLike - checks if value is like a specific type\n  {{- if .Values.testTypeIsLike }}\n  typeIsLikeValue: {{ typeIsLike \"string\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Comparison functions\n  # ==========================================\n  # Test: deepEqual - compares two values for deep equality\n  {{- if .Values.testDeepEqual }}\n  deepEqualValue: {{ deepEqual $testData .Values.compareValue | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-reflection/templates/reflection-test.yaml\n# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-reflection-test\r\ndata:\r\n  # ==========================================\r\n  # Kind functions\r\n  # ==========================================\r\n  # Test: kindOf - returns the kind of a value\r\n  # Test: kindIs - checks if value is of a specific kind\r\n\r\n  # ==========================================\r\n  # Type functions\r\n  # ==========================================\r\n  # Test: typeOf - returns the type of a value\r\n  # Test: typeIs - checks if value is of a specific type\r\n  typeIsValue: \"false\"\r\n  # Test: typeIsLike - checks if value is like a specific type\r\n\r\n  # ==========================================\r\n  # Comparison functions\r\n  # ==========================================\r\n  # Test: deepEqual - compares two values for deep equality\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-reflection/templates/reflection-test.yaml",
            "content": "\n# Source: test-reflection/templates/reflection-test.yaml\n# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-reflection-test\ndata:\n  # ==========================================\n  # Kind functions\n  # ==========================================\n  # Test: kindOf - returns the kind of a value\n  # Test: kindIs - checks if value is of a specific kind\n\n  # ==========================================\n  # Type functions\n  # ==========================================\n  # Test: typeOf - returns the type of a value\n  # Test: typeIs - checks if value is of a specific type\n  typeIsValue: \"false\"\n  # Test: typeIsLike - checks if value is like a specific type\n\n  # ==========================================\n  # Comparison functions\n  # ==========================================\n  # Test: deepEqual - compares two values for deep equality\n",
            "originalContent": "# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-reflection-test\ndata:\n  # ==========================================\n  # Kind functions\n  # ==========================================\n  # Test: kindOf - returns the kind of a value\n  {{- if .Values.testKindOf }}\n  kindOfValue: {{ kindOf $testData | quote }}\n  {{- end }}\n  # Test: kindIs - checks if value is of a specific kind\n  {{- if .Values.testKindIs }}\n  kindIsValue: {{ kindIs \"string\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Type functions\n  # ==========================================\n  # Test: typeOf - returns the type of a value\n  {{- if .Values.testTypeOf }}\n  typeOfValue: {{ typeOf $testData | quote }}\n  {{- end }}\n  # Test: typeIs - checks if value is of a specific type\n  {{- if .Values.testTypeIs }}\n  typeIsValue: {{ typeIs \"string\" $testData | quote }}\n  {{- end }}\n  # Test: typeIsLike - checks if value is like a specific type\n  {{- if .Values.testTypeIsLike }}\n  typeIsLikeValue: {{ typeIsLike \"string\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Comparison functions\n  # ==========================================\n  # Test: deepEqual - compares two values for deep equality\n  {{- if .Values.testDeepEqual }}\n  deepEqualValue: {{ deepEqual $testData .Values.compareValue | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "resolved": true,
      "stopReason": "",
      "fixChain": []
    },
    {
      "toggle": "typeIs",
      "dataType": "dictValue",
      "valuesOptions": {
        "values": [
          "testTypeIs=true",
          "testDataType=dictValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-reflection/templates/reflection-test.yaml\n# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-reflection-test\r\ndata:\r\n  # ==========================================\r\n  # Kind functions\r\n  # ==========================================\r\n  # Test: kindOf - returns the kind of a value\r\n  # Test: kindIs - checks if value is of a specific kind\r\n\r\n  # ==========================================\r\n  # Type functions\r\n  # ==========================================\r\n  # Test: typeOf - returns the type of a value\r\n  # Test: typeIs - checks if value is of a specific type\r\n  typeIsValue: \"false\"\r\n  # Test: typeIsLike - checks if value is like a specific type\r\n\r\n  # ==========================================\r\n  # Comparison functions\r\n  # ==========================================\r\n  # Test: deepEqual - compares two values for deep equality\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-reflection/templates/reflection-test.yaml",
            "content": "\n# Source: test-reflection/templates/reflection-test.yaml\n# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-reflection-test\ndata:\n  # ==========================================\n  # Kind functions\n  # ==========================================\n  # Test: kindOf - returns the kind of a value\n  # Test: kindIs - checks if value is of a specific kind\n\n  # ==========================================\n  # Type functions\n  # ==========================================\n  # Test: typeOf - returns the type of a value\n  # Test: typeIs - checks if value is of a specific type\n  typeIsValue: \"false\"\n  # Test: typeIsLike - checks if value is like a specific type\n\n  # ==========================================\n  # Comparison functions\n  # ==========================================\n  # Test: deepEqual - compares two values for deep equality\n",
            "originalContent": "# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-reflection-test\ndata:\n  # ==========================================\n  # Kind functions\n  # ==========================================\n  # Test: kindOf - returns the kind of a value\n  {{- if .Values.testKindOf }}\n  kindOfValue: {{ kindOf $testData | quote }}\n  {{- end }}\n  # Test: kindIs - checks if value is of a specific kind\n  {{- if .Values.testKindIs }}\n  kindIsValue: {{ kindIs \"string\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Type functions\n  # ==========================================\n  # Test: typeOf - returns the type of a value\n  {{- if .Values.testTypeOf }}\n  typeOfValue: {{ typeOf $testData | quote }}\n  {{- end }}\n  # Test: typeIs - checks if value is of a specific type\n  {{- if .Values.testTypeIs }}\n  typeIsValue: {{ typeIs \"string\" $testData | quote }}\n  {{- end }}\n  # Test: typeIsLike - checks if value is like a specific type\n  {{- if .Values.testTypeIsLike }}\n  typeIsLikeValue: {{ typeIsLike \"string\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Comparison functions\n  # ==========================================\n  # Test: deepEqual - compares two values for deep equality\n  {{- if .Values.testDeepEqual }}\n  deepEqualValue: {{ deepEqual $testData .Values.compareValue | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-reflection/templates/reflection-test.yaml\n# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-reflection-test\r\ndata:\r\n  # ==========================================\r\n  # Kind functions\r\n  # ==========================================\r\n  # Test: kindOf - returns the kind of a value\r\n  # Test: kindIs - checks if value is of a specific kind\r\n\r\n  # ==========================================\r\n  # Type functions\r\n  # ==========================================\r\n  # Test: typeOf - returns the type of a value\r\n  # Test: typeIs - checks if value is of a specific type\r\n  typeIsValue: \"false\"\r\n  # Test: typeIsLike - checks if value is like a specific type\r\n\r\n  # ==========================================\r\n  # Comparison functions\r\n  # ==========================================\r\n  # Test: deepEqual - compares two values for deep equality\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-reflection/templates/reflection-test.yaml",
            "content": "\n# Source: test-reflection/templates/reflection-test.yaml\n# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-reflection-test\ndata:\n  # ==========================================\n  # Kind functions\n  # ==========================================\n  # Test: kindOf - returns the kind of a value\n  # Test: kindIs - checks if value is of a specific kind\n\n  # ==========================================\n  # Type functions\n  # ==========================================\n  # Test: typeOf - returns the type of a value\n  # Test: typeIs - checks if value is of a specific type\n  typeIsValue: \"false\"\n  # Test: typeIsLike - checks if value is like a specific type\n\n  # ==========================================\n  # Comparison functions\n  # ==========================================\n  # Test: deepEqual - compares two values for deep equality\n",
            "originalContent": "# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-reflection-test\ndata:\n  # ==========================================\n  # Kind functions\n  # ==========================================\n  # Test: kindOf - returns the kind of a value\n  {{- if .Values.testKindOf }}\n  kindOfValue: {{ kindOf $testData | quote }}\n  {{- end }}\n  # Test: kindIs - checks if value is of a specific kind\n  {{- if .Values.testKindIs }}\n  kindIsValue: {{ kindIs \"string\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Type functions\n  # ==========================================\n  # Test: typeOf - returns the type of a value\n  {{- if .Values.testTypeOf }}\n  typeOfValue: {{ typeOf $testData | quote }}\n  {{- end }}\n  # Test: typeIs - checks if value is of a specific type\n  {{- if .Values.testTypeIs }}\n  typeIsValue: {{ typeIs \"string\" $testData | quote }}\n  {{- end }}\n  # Test: typeIsLike - checks if value is like a specific type\n  {{- if .Values.testTypeIsLike }}\n  typeIsLikeValue: {{ typeIsLike \"string\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Comparison functions\n  # ==========================================\n  # Test: deepEqual - compares two values for deep equality\n  {{- if .Values.testDeepEqual }}\n  deepEqualValue: {{ deepEqual $testData .Values.compareValue | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "resolved": true,
      "stopReason": "",
      "fixChain": []
    },
    {
      "toggle": "typeIsLike",
      "dataType": "nilValue",
      "valuesOptions": {
        "values": [
          "testTypeIsLike=true",
          "testDataType=nilValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-reflection/templates/reflection-test.yaml\n# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-reflection-test\r\ndata:\r\n  # ==========================================\r\n  # Kind functions\r\n  # ==========================================\r\n  # Test: kindOf - returns the kind of a value\r\n  # Test: kindIs - checks if value is of a specific kind\r\n\r\n  # ==========================================\r\n  # Type functions\r\n  # ==========================================\r\n  # Test: typeOf - returns the type of a value\r\n  # Test: typeIs - checks if value is of a specific type\r\n  # Test: typeIsLike - checks if value is like a specific type\r\n  typeIsLikeValue: \"false\"\r\n\r\n  # ==========================================\r\n  # Comparison functions\r\n  # ==========================================\r\n  # Test: deepEqual - compares two values for deep equality\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-reflection/templates/reflection-test.yaml",
            "content": "\n# Source: test-reflection/templates/reflection-test.yaml\n# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-reflection-test\ndata:\n  # ==========================================\n  # Kind functions\n  # ==========================================\n  # Test: kindOf - returns the kind of a value\n  # Test: kindIs - checks if value is of a specific kind\n\n  # ==========================================\n  # Type functions\n  # ==========================================\n  # Test: typeOf - returns the type of a value\n  # Test: typeIs - checks if value is of a specific type\n  # Test: typeIsLike - checks if value is like a specific type\n  typeIsLikeValue: \"false\"\n\n  # ==========================================\n  # Comparison functions\n  # ==========================================\n  # Test: deepEqual - compares two values for deep equality\n",
            "originalContent": "# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-reflection-test\ndata:\n  # ==========================================\n  # Kind functions\n  # ==========================================\n  # Test: kindOf - returns the kind of a value\n  {{- if .Values.testKindOf }}\n  kindOfValue: {{ kindOf $testData | quote }}\n  {{- end }}\n  # Test: kindIs - checks if value is of a specific kind\n  {{- if .Values.testKindIs }}\n  kindIsValue: {{ kindIs \"string\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Type functions\n  # ==========================================\n  # Test: typeOf - returns the type of a value\n  {{- if .Values.testTypeOf }}\n  typeOfValue: {{ typeOf $testData | quote }}\n  {{- end }}\n  # Test: typeIs - checks if value is of a specific type\n  {{- if .Values.testTypeIs }}\n  typeIsValue: {{ typeIs \"string\" $testData | quote }}\n  {{- end }}\n  # Test: typeIsLike - checks if value is like a specific type\n  {{- if .Values.testTypeIsLike }}\n  typeIsLikeValue: {{ typeIsLike \"string\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Comparison functions\n  # ==========================================\n  # Test: deepEqual - compares two values for deep equality\n  {{- if .Values.testDeepEqual }}\n  deepEqualValue: {{ deepEqual $testData .Values.compareValue | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-reflection/templates/reflection-test.yaml\n# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-reflection-test\r\ndata:\r\n  # ==========================================\r\n  # Kind functions\r\n  # ==========================================\r\n  # Test: kindOf - returns the kind of a value\r\n  # Test: kindIs - checks if value is of a specific kind\r\n\r\n  # ==========================================\r\n  # Type functions\r\n  # ==========================================\r\n  # Test: typeOf - returns the type of a value\r\n  # Test: typeIs - checks if value is of a specific type\r\n  # Test: typeIsLike - checks if value is like a specific type\r\n  typeIsLikeValue: \"false\"\r\n\r\n  # ==========================================\r\n  # Comparison functions\r\n  # ==========================================\r\n  # Test: deepEqual - compares two values for deep equality\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-reflection/templates/reflection-test.yaml",
            "content": "\n# Source: test-reflection/templates/reflection-test.yaml\n# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-reflection-test\ndata:\n  # ==========================================\n  # Kind functions\n  # ==========================================\n  # Test: kindOf - returns the kind of a value\n  # Test: kindIs - checks if value is of a specific kind\n\n  # ==========================================\n  # Type functions\n  # ==========================================\n  # Test: typeOf - returns the type of a value\n  # Test: typeIs - checks if value is of a specific type\n  # Test: typeIsLike - checks if value is like a specific type\n  typeIsLikeValue: \"false\"\n\n  # ==========================================\n  # Comparison functions\n  # ==========================================\n  # Test: deepEqual - compares two values for deep equality\n",
            "originalContent": "# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-reflection-test\ndata:\n  # ==========================================\n  # Kind functions\n  # ==========================================\n  # Test: kindOf - returns the kind of a value\n  {{- if .Values.testKindOf }}\n  kindOfValue: {{ kindOf $testData | quote }}\n  {{- end }}\n  # Test: kindIs - checks if value is of a specific kind\n  {{- if .Values.testKindIs }}\n  kindIsValue: {{ kindIs \"string\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Type functions\n  # ==========================================\n  # Test: typeOf - returns the type of a value\n  {{- if .Values.testTypeOf }}\n  typeOfValue: {{ typeOf $testData | quote }}\n  {{- end }}\n  # Test: typeIs - checks if value is of a specific type\n  {{- if .Values.testTypeIs }}\n  typeIsValue: {{ typeIs \"string\" $testData | quote }}\n  {{- end }}\n  # Test: typeIsLike - checks if value is like a specific type\n  {{- if .Values.testTypeIsLike }}\n  typeIsLikeValue: {{ typeIsLike \"string\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Comparison functions\n  # ==========================================\n  # Test: deepEqual - compares two values for deep equality\n  {{- if .Values.testDeepEqual }}\n  deepEqualValue: {{ deepEqual $testData .Values.compareValue | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "resolved": true,
      "stopReason": "",
      "fixChain": []
    },
    {
      "toggle": "typeIsLike",
      "dataType": "stringValue",
      "valuesOptions": {
        "values": [
          "testTypeIsLike=true",
          "testDataType=stringValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-reflection/templates/reflection-test.yaml\n# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-reflection-test\r\ndata:\r\n  # ==========================================\r\n  # Kind functions\r\n  # ==========================================\r\n  # Test: kindOf - returns the kind of a value\r\n  # Test: kindIs - checks if value is of a specific kind\r\n\r\n  # ==========================================\r\n  # Type functions\r\n  # ==========================================\r\n  # Test: typeOf - returns the type of a value\r\n  # Test: typeIs - checks if value is of a specific type\r\n  # Test: typeIsLike - checks if value is like a specific type\r\n  typeIsLikeValue: \"true\"\r\n\r\n  # ==========================================\r\n  # Comparison functions\r\n  # ==========================================\r\n  # Test: deepEqual - compares two values for deep equality\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-reflection/templates/reflection-test.yaml",
            "content": "\n# Source: test-reflection/templates/reflection-test.yaml\n# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-reflection-test\ndata:\n  # ==========================================\n  # Kind functions\n  # ==========================================\n  # Test: kindOf - returns the kind of a value\n  # Test: kindIs - checks if value is of a specific kind\n\n  # ==========================================\n  # Type functions\n  # ==========================================\n  # Test: typeOf - returns the type of a value\n  # Test: typeIs - checks if value is of a specific type\n  # Test: typeIsLike - checks if value is like a specific type\n  typeIsLikeValue: \"true\"\n\n  # ==========================================\n  # Comparison functions\n  # ==========================================\n  # Test: deepEqual - compares two values for deep equality\n",
            "originalContent": "# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-reflection-test\ndata:\n  # ==========================================\n  # Kind functions\n  # ==========================================\n  # Test: kindOf - returns the kind of a value\n  {{- if .Values.testKindOf }}\n  kindOfValue: {{ kindOf $testData | quote }}\n  {{- end }}\n  # Test: kindIs - checks if value is of a specific kind\n  {{- if .Values.testKindIs }}\n  kindIsValue: {{ kindIs \"string\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Type functions\n  # ==========================================\n  # Test: typeOf - returns the type of a value\n  {{- if .Values.testTypeOf }}\n  typeOfValue: {{ typeOf $testData | quote }}\n  {{- end }}\n  # Test: typeIs - checks if value is of a specific type\n  {{- if .Values.testTypeIs }}\n  typeIsValue: {{ typeIs \"string\" $testData | quote }}\n  {{- end }}\n  # Test: typeIsLike - checks if value is like a specific type\n  {{- if .Values.testTypeIsLike }}\n  typeIsLikeValue: {{ typeIsLike \"string\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Comparison functions\n  # ==========================================\n  # Test: deepEqual - compares two values for deep equality\n  {{- if .Values.testDeepEqual }}\n  deepEqualValue: {{ deepEqual $testData .Values.compareValue | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-reflection/templates/reflection-test.yaml\n# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-reflection-test\r\ndata:\r\n  # ==========================================\r\n  # Kind functions\r\n  # ==========================================\r\n  # Test: kindOf - returns the kind of a value\r\n  # Test: kindIs - checks if value is of a specific kind\r\n\r\n  # ==========================================\r\n  # Type functions\r\n  # ==========================================\r\n  # Test: typeOf - returns the type of a value\r\n  # Test: typeIs - checks if value is of a specific type\r\n  # Test: typeIsLike - checks if value is like a specific type\r\n  typeIsLikeValue: \"true\"\r\n\r\n  # ==========================================\r\n  # Comparison functions\r\n  # ==========================================\r\n  # Test: deepEqual - compares two values for deep equality\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-reflection/templates/reflection-test.yaml",
            "content": "\n# Source: test-reflection/templates/reflection-test.yaml\n# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-reflection-test\ndata:\n  # ==========================================\n  # Kind functions\n  # ==========================================\n  # Test: kindOf - returns the kind of a value\n  # Test: kindIs - checks if value is of a specific kind\n\n  # ==========================================\n  # Type functions\n  # ==========================================\n  # Test: typeOf - returns the type of a value\n  # Test: typeIs - checks if value is of a specific type\n  # Test: typeIsLike - checks if value is like a specific type\n  typeIsLikeValue: \"true\"\n\n  # ==========================================\n  # Comparison functions\n  # ==========================================\n  # Test: deepEqual - compares two values for deep equality\n",
            "originalContent": "# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-reflection-test\ndata:\n  # ==========================================\n  # Kind functions\n  # ==========================================\n  # Test: kindOf - returns the kind of a value\n  {{- if .Values.testKindOf }}\n  kindOfValue: {{ kindOf $testData | quote }}\n  {{- end }}\n  # Test: kindIs - checks if value is of a specific kind\n  {{- if .Values.testKindIs }}\n  kindIsValue: {{ kindIs \"string\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Type functions\n  # ==========================================\n  # Test: typeOf - returns the type of a value\n  {{- if .Values.testTypeOf }}\n  typeOfValue: {{ typeOf $testData | quote }}\n  {{- end }}\n  # Test: typeIs - checks if value is of a specific type\n  {{- if .Values.testTypeIs }}\n  typeIsValue: {{ typeIs \"string\" $testData | quote }}\n  {{- end }}\n  # Test: typeIsLike - checks if value is like a specific type\n  {{- if .Values.testTypeIsLike }}\n  typeIsLikeValue: {{ typeIsLike \"string\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Comparison functions\n  # ==========================================\n  # Test: deepEqual - compares two values for deep equality\n  {{- if .Values.testDeepEqual }}\n  deepEqualValue: {{ deepEqual $testData .Values.compareValue | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "resolved": true,
      "stopReason": "",
      "fixChain": []
    },
    {
      "toggle": "typeIsLike",
      "dataType": "numberValue",
      "valuesOptions": {
        "values": [
          "testTypeIsLike=true",
          "testDataType=numberValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-reflection/templates/reflection-test.yaml\n# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-reflection-test\r\ndata:\r\n  # ==========================================\r\n  # Kind functions\r\n  # ==========================================\r\n  # Test: kindOf - returns the kind of a value\r\n  # Test: kindIs - checks if value is of a specific kind\r\n\r\n  # ==========================================\r\n  # Type functions\r\n  # ==========================================\r\n  # Test: typeOf - returns the type of a value\r\n  # Test: typeIs - checks if value is of a specific type\r\n  # Test: typeIsLike - checks if value is like a specific type\r\n  typeIsLikeValue: \"false\"\r\n\r\n  # ==========================================\r\n  # Comparison functions\r\n  # ==========================================\r\n  # Test: deepEqual - compares two values for deep equality\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-reflection/templates/reflection-test.yaml",
            "content": "\n# Source: test-reflection/templates/reflection-test.yaml\n# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-reflection-test\ndata:\n  # ==========================================\n  # Kind functions\n  # ==========================================\n  # Test: kindOf - returns the kind of a value\n  # Test: kindIs - checks if value is of a specific kind\n\n  # ==========================================\n  # Type functions\n  # ==========================================\n  # Test: typeOf - returns the type of a value\n  # Test: typeIs - checks if value is of a specific type\n  # Test: typeIsLike - checks if value is like a specific type\n  typeIsLikeValue: \"false\"\n\n  # ==========================================\n  # Comparison functions\n  # ==========================================\n  # Test: deepEqual - compares two values for deep equality\n",
            "originalContent": "# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-reflection-test\ndata:\n  # ==========================================\n  # Kind functions\n  # ==========================================\n  # Test: kindOf - returns the kind of a value\n  {{- if .Values.testKindOf }}\n  kindOfValue: {{ kindOf $testData | quote }}\n  {{- end }}\n  # Test: kindIs - checks if value is of a specific kind\n  {{- if .Values.testKindIs }}\n  kindIsValue: {{ kindIs \"string\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Type functions\n  # ==========================================\n  # Test: typeOf - returns the type of a value\n  {{- if .Values.testTypeOf }}\n  typeOfValue: {{ typeOf $testData | quote }}\n  {{- end }}\n  # Test: typeIs - checks if value is of a specific type\n  {{- if .Values.testTypeIs }}\n  typeIsValue: {{ typeIs \"string\" $testData | quote }}\n  {{- end }}\n  # Test: typeIsLike - checks if value is like a specific type\n  {{- if .Values.testTypeIsLike }}\n  typeIsLikeValue: {{ typeIsLike \"string\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Comparison functions\n  # ==========================================\n  # Test: deepEqual - compares two values for deep equality\n  {{- if .Values.testDeepEqual }}\n  deepEqualValue: {{ deepEqual $testData .Values.compareValue | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-reflection/templates/reflection-test.yaml\n# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-reflection-test\r\ndata:\r\n  # ==========================================\r\n  # Kind functions\r\n  # ==========================================\r\n  # Test: kindOf - returns the kind of a value\r\n  # Test: kindIs - checks if value is of a specific kind\r\n\r\n  # ==========================================\r\n  # Type functions\r\n  # ==========================================\r\n  # Test: typeOf - returns the type of a value\r\n  # Test: typeIs - checks if value is of a specific type\r\n  # Test: typeIsLike - checks if value is like a specific type\r\n  typeIsLikeValue: \"false\"\r\n\r\n  # ==========================================\r\n  # Comparison functions\r\n  # ==========================================\r\n  # Test: deepEqual - compares two values for deep equality\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-reflection/templates/reflection-test.yaml",
            "content": "\n# Source: test-reflection/templates/reflection-test.yaml\n# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-reflection-test\ndata:\n  # ==========================================\n  # Kind functions\n  # ==========================================\n  # Test: kindOf - returns the kind of a value\n  # Test: kindIs - checks if value is of a specific kind\n\n  # ==========================================\n  # Type functions\n  # ==========================================\n  # Test: typeOf - returns the type of a value\n  # Test: typeIs - checks if value is of a specific type\n  # Test: typeIsLike - checks if value is like a specific type\n  typeIsLikeValue: \"false\"\n\n  # ==========================================\n  # Comparison functions\n  # ==========================================\n  # Test: deepEqual - compares two values for deep equality\n",
            "originalContent": "# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-reflection-test\ndata:\n  # ==========================================\n  # Kind functions\n  # ==========================================\n  # Test: kindOf - returns the kind of a value\n  {{- if .Values.testKindOf }}\n  kindOfValue: {{ kindOf $testData | quote }}\n  {{- end }}\n  # Test: kindIs - checks if value is of a specific kind\n  {{- if .Values.testKindIs }}\n  kindIsValue: {{ kindIs \"string\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Type functions\n  # ==========================================\n  # Test: typeOf - returns the type of a value\n  {{- if .Values.testTypeOf }}\n  typeOfValue: {{ typeOf $testData | quote }}\n  {{- end }}\n  # Test: typeIs - checks if value is of a specific type\n  {{- if .Values.testTypeIs }}\n  typeIsValue: {{ typeIs \"string\" $testData | quote }}\n  {{- end }}\n  # Test: typeIsLike - checks if value is like a specific type\n  {{- if .Values.testTypeIsLike }}\n  typeIsLikeValue: {{ typeIsLike \"string\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Comparison functions\n  # ==========================================\n  # Test: deepEqual - compares two values for deep equality\n  {{- if .Values.testDeepEqual }}\n  deepEqualValue: {{ deepEqual $testData .Values.compareValue | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "resolved": true,
      "stopReason": "",
      "fixChain": []
    },
    {
      "toggle": "typeIsLike",
      "dataType": "boolValue",
      "valuesOptions": {
        "values": [
          "testTypeIsLike=true",
          "testDataType=boolValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-reflection/templates/reflection-test.yaml\n# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-reflection-test\r\ndata:\r\n  # ==========================================\r\n  # Kind functions\r\n  # ==========================================\r\n  # Test: kindOf - returns the kind of a value\r\n  # Test: kindIs - checks if value is of a specific kind\r\n\r\n  # ==========================================\r\n  # Type functions\r\n  # ==========================================\r\n  # Test: typeOf - returns the type of a value\r\n  # Test: typeIs - checks if value is of a specific type\r\n  # Test: typeIsLike - checks if value is like a specific type\r\n  typeIsLikeValue: \"false\"\r\n\r\n  # ==========================================\r\n  # Comparison functions\r\n  # ==========================================\r\n  # Test: deepEqual - compares two values for deep equality\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-reflection/templates/reflection-test.yaml",
            "content": "\n# Source: test-reflection/templates/reflection-test.yaml\n# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-reflection-test\ndata:\n  # ==========================================\n  # Kind functions\n  # ==========================================\n  # Test: kindOf - returns the kind of a value\n  # Test: kindIs - checks if value is of a specific kind\n\n  # ==========================================\n  # Type functions\n  # ==========================================\n  # Test: typeOf - returns the type of a value\n  # Test: typeIs - checks if value is of a specific type\n  # Test: typeIsLike - checks if value is like a specific type\n  typeIsLikeValue: \"false\"\n\n  # ==========================================\n  # Comparison functions\n  # ==========================================\n  # Test: deepEqual - compares two values for deep equality\n",
            "originalContent": "# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-reflection-test\ndata:\n  # ==========================================\n  # Kind functions\n  # ==========================================\n  # Test: kindOf - returns the kind of a value\n  {{- if .Values.testKindOf }}\n  kindOfValue: {{ kindOf $testData | quote }}\n  {{- end }}\n  # Test: kindIs - checks if value is of a specific kind\n  {{- if .Values.testKindIs }}\n  kindIsValue: {{ kindIs \"string\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Type functions\n  # ==========================================\n  # Test: typeOf - returns the type of a value\n  {{- if .Values.testTypeOf }}\n  typeOfValue: {{ typeOf $testData | quote }}\n  {{- end }}\n  # Test: typeIs - checks if value is of a specific type\n  {{- if .Values.testTypeIs }}\n  typeIsValue: {{ typeIs \"string\" $testData | quote }}\n  {{- end }}\n  # Test: typeIsLike - checks if value is like a specific type\n  {{- if .Values.testTypeIsLike }}\n  typeIsLikeValue: {{ typeIsLike \"string\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Comparison functions\n  # ==========================================\n  # Test: deepEqual - compares two values for deep equality\n  {{- if .Values.testDeepEqual }}\n  deepEqualValue: {{ deepEqual $testData .Values.compareValue | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-reflection/templates/reflection-test.yaml\n# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-reflection-test\r\ndata:\r\n  # ==========================================\r\n  # Kind functions\r\n  # ==========================================\r\n  # Test: kindOf - returns the kind of a value\r\n  # Test: kindIs - checks if value is of a specific kind\r\n\r\n  # ==========================================\r\n  # Type functions\r\n  # ==========================================\r\n  # Test: typeOf - returns the type of a value\r\n  # Test: typeIs - checks if value is of a specific type\r\n  # Test: typeIsLike - checks if value is like a specific type\r\n  typeIsLikeValue: \"false\"\r\n\r\n  # ==========================================\r\n  # Comparison functions\r\n  # ==========================================\r\n  # Test: deepEqual - compares two values for deep equality\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-reflection/templates/reflection-test.yaml",
            "content": "\n# Source: test-reflection/templates/reflection-test.yaml\n# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-reflection-test\ndata:\n  # ==========================================\n  # Kind functions\n  # ==========================================\n  # Test: kindOf - returns the kind of a value\n  # Test: kindIs - checks if value is of a specific kind\n\n  # ==========================================\n  # Type functions\n  # ==========================================\n  # Test: typeOf - returns the type of a value\n  # Test: typeIs - checks if value is of a specific type\n  # Test: typeIsLike - checks if value is like a specific type\n  typeIsLikeValue: \"false\"\n\n  # ==========================================\n  # Comparison functions\n  # ==========================================\n  # Test: deepEqual - compares two values for deep equality\n",
            "originalContent": "# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-reflection-test\ndata:\n  # ==========================================\n  # Kind functions\n  # ==========================================\n  # Test: kindOf - returns the kind of a value\n  {{- if .Values.testKindOf }}\n  kindOfValue: {{ kindOf $testData | quote }}\n  {{- end }}\n  # Test: kindIs - checks if value is of a specific kind\n  {{- if .Values.testKindIs }}\n  kindIsValue: {{ kindIs \"string\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Type functions\n  # ==========================================\n  # Test: typeOf - returns the type of a value\n  {{- if .Values.testTypeOf }}\n  typeOfValue: {{ typeOf $testData | quote }}\n  {{- end }}\n  # Test: typeIs - checks if value is of a specific type\n  {{- if .Values.testTypeIs }}\n  typeIsValue: {{ typeIs \"string\" $testData | quote }}\n  {{- end }}\n  # Test: typeIsLike - checks if value is like a specific type\n  {{- if .Values.testTypeIsLike }}\n  typeIsLikeValue: {{ typeIsLike \"string\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Comparison functions\n  # ==========================================\n  # Test: deepEqual - compares two values for deep equality\n  {{- if .Values.testDeepEqual }}\n  deepEqualValue: {{ deepEqual $testData .Values.compareValue | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "resolved": true,
      "stopReason": "",
      "fixChain": []
    },
    {
      "toggle": "typeIsLike",
      "dataType": "listValue",
      "valuesOptions": {
        "values": [
          "testTypeIsLike=true",
          "testDataType=listValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-reflection/templates/reflection-test.yaml\n# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-reflection-test\r\ndata:\r\n  # ==========================================\r\n  # Kind functions\r\n  # ==========================================\r\n  # Test: kindOf - returns the kind of a value\r\n  # Test: kindIs - checks if value is of a specific kind\r\n\r\n  # ==========================================\r\n  # Type functions\r\n  # ==========================================\r\n  # Test: typeOf - returns the type of a value\r\n  # Test: typeIs - checks if value is of a specific type\r\n  # Test: typeIsLike - checks if value is like a specific type\r\n  typeIsLikeValue: \"false\"\r\n\r\n  # ==========================================\r\n  # Comparison functions\r\n  # ==========================================\r\n  # Test: deepEqual - compares two values for deep equality\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-reflection/templates/reflection-test.yaml",
            "content": "\n# Source: test-reflection/templates/reflection-test.yaml\n# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-reflection-test\ndata:\n  # ==========================================\n  # Kind functions\n  # ==========================================\n  # Test: kindOf - returns the kind of a value\n  # Test: kindIs - checks if value is of a specific kind\n\n  # ==========================================\n  # Type functions\n  # ==========================================\n  # Test: typeOf - returns the type of a value\n  # Test: typeIs - checks if value is of a specific type\n  # Test: typeIsLike - checks if value is like a specific type\n  typeIsLikeValue: \"false\"\n\n  # ==========================================\n  # Comparison functions\n  # ==========================================\n  # Test: deepEqual - compares two values for deep equality\n",
            "originalContent": "# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-reflection-test\ndata:\n  # ==========================================\n  # Kind functions\n  # ==========================================\n  # Test: kindOf - returns the kind of a value\n  {{- if .Values.testKindOf }}\n  kindOfValue: {{ kindOf $testData | quote }}\n  {{- end }}\n  # Test: kindIs - checks if value is of a specific kind\n  {{- if .Values.testKindIs }}\n  kindIsValue: {{ kindIs \"string\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Type functions\n  # ==========================================\n  # Test: typeOf - returns the type of a value\n  {{- if .Values.testTypeOf }}\n  typeOfValue: {{ typeOf $testData | quote }}\n  {{- end }}\n  # Test: typeIs - checks if value is of a specific type\n  {{- if .Values.testTypeIs }}\n  typeIsValue: {{ typeIs \"string\" $testData | quote }}\n  {{- end }}\n  # Test: typeIsLike - checks if value is like a specific type\n  {{- if .Values.testTypeIsLike }}\n  typeIsLikeValue: {{ typeIsLike \"string\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Comparison functions\n  # ==========================================\n  # Test: deepEqual - compares two values for deep equality\n  {{- if .Values.testDeepEqual }}\n  deepEqualValue: {{ deepEqual $testData .Values.compareValue | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-reflection/templates/reflection-test.yaml\n# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-reflection-test\r\ndata:\r\n  # ==========================================\r\n  # Kind functions\r\n  # ==========================================\r\n  # Test: kindOf - returns the kind of a value\r\n  # Test: kindIs - checks if value is of a specific kind\r\n\r\n  # ==========================================\r\n  # Type functions\r\n  # ==========================================\r\n  # Test: typeOf - returns the type of a value\r\n  # Test: typeIs - checks if value is of a specific type\r\n  # Test: typeIsLike - checks if value is like a specific type\r\n  typeIsLikeValue: \"false\"\r\n\r\n  # ==========================================\r\n  # Comparison functions\r\n  # ==========================================\r\n  # Test: deepEqual - compares two values for deep equality\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-reflection/templates/reflection-test.yaml",
            "content": "\n# Source: test-reflection/templates/reflection-test.yaml\n# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-reflection-test\ndata:\n  # ==========================================\n  # Kind functions\n  # ==========================================\n  # Test: kindOf - returns the kind of a value\n  # Test: kindIs - checks if value is of a specific kind\n\n  # ==========================================\n  # Type functions\n  # ==========================================\n  # Test: typeOf - returns the type of a value\n  # Test: typeIs - checks if value is of a specific type\n  # Test: typeIsLike - checks if value is like a specific type\n  typeIsLikeValue: \"false\"\n\n  # ==========================================\n  # Comparison functions\n  # ==========================================\n  # Test: deepEqual - compares two values for deep equality\n",
            "originalContent": "# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-reflection-test\ndata:\n  # ==========================================\n  # Kind functions\n  # ==========================================\n  # Test: kindOf - returns the kind of a value\n  {{- if .Values.testKindOf }}\n  kindOfValue: {{ kindOf $testData | quote }}\n  {{- end }}\n  # Test: kindIs - checks if value is of a specific kind\n  {{- if .Values.testKindIs }}\n  kindIsValue: {{ kindIs \"string\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Type functions\n  # ==========================================\n  # Test: typeOf - returns the type of a value\n  {{- if .Values.testTypeOf }}\n  typeOfValue: {{ typeOf $testData | quote }}\n  {{- end }}\n  # Test: typeIs - checks if value is of a specific type\n  {{- if .Values.testTypeIs }}\n  typeIsValue: {{ typeIs \"string\" $testData | quote }}\n  {{- end }}\n  # Test: typeIsLike - checks if value is like a specific type\n  {{- if .Values.testTypeIsLike }}\n  typeIsLikeValue: {{ typeIsLike \"string\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Comparison functions\n  # ==========================================\n  # Test: deepEqual - compares two values for deep equality\n  {{- if .Values.testDeepEqual }}\n  deepEqualValue: {{ deepEqual $testData .Values.compareValue | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "resolved": true,
      "stopReason": "",
      "fixChain": []
    },
    {
      "toggle": "typeIsLike",
      "dataType": "dictValue",
      "valuesOptions": {
        "values": [
          "testTypeIsLike=true",
          "testDataType=dictValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-reflection/templates/reflection-test.yaml\n# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-reflection-test\r\ndata:\r\n  # ==========================================\r\n  # Kind functions\r\n  # ==========================================\r\n  # Test: kindOf - returns the kind of a value\r\n  # Test: kindIs - checks if value is of a specific kind\r\n\r\n  # ==========================================\r\n  # Type functions\r\n  # ==========================================\r\n  # Test: typeOf - returns the type of a value\r\n  # Test: typeIs - checks if value is of a specific type\r\n  # Test: typeIsLike - checks if value is like a specific type\r\n  typeIsLikeValue: \"false\"\r\n\r\n  # ==========================================\r\n  # Comparison functions\r\n  # ==========================================\r\n  # Test: deepEqual - compares two values for deep equality\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-reflection/templates/reflection-test.yaml",
            "content": "\n# Source: test-reflection/templates/reflection-test.yaml\n# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-reflection-test\ndata:\n  # ==========================================\n  # Kind functions\n  # ==========================================\n  # Test: kindOf - returns the kind of a value\n  # Test: kindIs - checks if value is of a specific kind\n\n  # ==========================================\n  # Type functions\n  # ==========================================\n  # Test: typeOf - returns the type of a value\n  # Test: typeIs - checks if value is of a specific type\n  # Test: typeIsLike - checks if value is like a specific type\n  typeIsLikeValue: \"false\"\n\n  # ==========================================\n  # Comparison functions\n  # ==========================================\n  # Test: deepEqual - compares two values for deep equality\n",
            "originalContent": "# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-reflection-test\ndata:\n  # ==========================================\n  # Kind functions\n  # ==========================================\n  # Test: kindOf - returns the kind of a value\n  {{- if .Values.testKindOf }}\n  kindOfValue: {{ kindOf $testData | quote }}\n  {{- end }}\n  # Test: kindIs - checks if value is of a specific kind\n  {{- if .Values.testKindIs }}\n  kindIsValue: {{ kindIs \"string\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Type functions\n  # ==========================================\n  # Test: typeOf - returns the type of a value\n  {{- if .Values.testTypeOf }}\n  typeOfValue: {{ typeOf $testData | quote }}\n  {{- end }}\n  # Test: typeIs - checks if value is of a specific type\n  {{- if .Values.testTypeIs }}\n  typeIsValue: {{ typeIs \"string\" $testData | quote }}\n  {{- end }}\n  # Test: typeIsLike - checks if value is like a specific type\n  {{- if .Values.testTypeIsLike }}\n  typeIsLikeValue: {{ typeIsLike \"string\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Comparison functions\n  # ==========================================\n  # Test: deepEqual - compares two values for deep equality\n  {{- if .Values.testDeepEqual }}\n  deepEqualValue: {{ deepEqual $testData .Values.compareValue | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-reflection/templates/reflection-test.yaml\n# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-reflection-test\r\ndata:\r\n  # ==========================================\r\n  # Kind functions\r\n  # ==========================================\r\n  # Test: kindOf - returns the kind of a value\r\n  # Test: kindIs - checks if value is of a specific kind\r\n\r\n  # ==========================================\r\n  # Type functions\r\n  # ==========================================\r\n  # Test: typeOf - returns the type of a value\r\n  # Test: typeIs - checks if value is of a specific type\r\n  # Test: typeIsLike - checks if value is like a specific type\r\n  typeIsLikeValue: \"false\"\r\n\r\n  # ==========================================\r\n  # Comparison functions\r\n  # ==========================================\r\n  # Test: deepEqual - compares two values for deep equality\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-reflection/templates/reflection-test.yaml",
            "content": "\n# Source: test-reflection/templates/reflection-test.yaml\n# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-reflection-test\ndata:\n  # ==========================================\n  # Kind functions\n  # ==========================================\n  # Test: kindOf - returns the kind of a value\n  # Test: kindIs - checks if value is of a specific kind\n\n  # ==========================================\n  # Type functions\n  # ==========================================\n  # Test: typeOf - returns the type of a value\n  # Test: typeIs - checks if value is of a specific type\n  # Test: typeIsLike - checks if value is like a specific type\n  typeIsLikeValue: \"false\"\n\n  # ==========================================\n  # Comparison functions\n  # ==========================================\n  # Test: deepEqual - compares two values for deep equality\n",
            "originalContent": "# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-reflection-test\ndata:\n  # ==========================================\n  # Kind functions\n  # ==========================================\n  # Test: kindOf - returns the kind of a value\n  {{- if .Values.testKindOf }}\n  kindOfValue: {{ kindOf $testData | quote }}\n  {{- end }}\n  # Test: kindIs - checks if value is of a specific kind\n  {{- if .Values.testKindIs }}\n  kindIsValue: {{ kindIs \"string\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Type functions\n  # ==========================================\n  # Test: typeOf - returns the type of a value\n  {{- if .Values.testTypeOf }}\n  typeOfValue: {{ typeOf $testData | quote }}\n  {{- end }}\n  # Test: typeIs - checks if value is of a specific type\n  {{- if .Values.testTypeIs }}\n  typeIsValue: {{ typeIs \"string\" $testData | quote }}\n  {{- end }}\n  # Test: typeIsLike - checks if value is like a specific type\n  {{- if .Values.testTypeIsLike }}\n  typeIsLikeValue: {{ typeIsLike \"string\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Comparison functions\n  # ==========================================\n  # Test: deepEqual - compares two values for deep equality\n  {{- if .Values.testDeepEqual }}\n  deepEqualValue: {{ deepEqual $testData .Values.compareValue | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "resolved": true,
      "stopReason": "",
      "fixChain": []
    },
    {
      "toggle": "deepEqual",
      "dataType": "nilValue",
      "valuesOptions": {
        "values": [
          "testDeepEqual=true",
          "testDataType=nilValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-reflection/templates/reflection-test.yaml\n# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-reflection-test\r\ndata:\r\n  # ==========================================\r\n  # Kind functions\r\n  # ==========================================\r\n  # Test: kindOf - returns the kind of a value\r\n  # Test: kindIs - checks if value is of a specific kind\r\n\r\n  # ==========================================\r\n  # Type functions\r\n  # ==========================================\r\n  # Test: typeOf - returns the type of a value\r\n  # Test: typeIs - checks if value is of a specific type\r\n  # Test: typeIsLike - checks if value is like a specific type\r\n\r\n  # ==========================================\r\n  # Comparison functions\r\n  # ==========================================\r\n  # Test: deepEqual - compares two values for deep equality\r\n  deepEqualValue: \"false\"\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-reflection/templates/reflection-test.yaml",
            "content": "\n# Source: test-reflection/templates/reflection-test.yaml\n# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-reflection-test\ndata:\n  # ==========================================\n  # Kind functions\n  # ==========================================\n  # Test: kindOf - returns the kind of a value\n  # Test: kindIs - checks if value is of a specific kind\n\n  # ==========================================\n  # Type functions\n  # ==========================================\n  # Test: typeOf - returns the type of a value\n  # Test: typeIs - checks if value is of a specific type\n  # Test: typeIsLike - checks if value is like a specific type\n\n  # ==========================================\n  # Comparison functions\n  # ==========================================\n  # Test: deepEqual - compares two values for deep equality\n  deepEqualValue: \"false\"\n",
            "originalContent": "# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-reflection-test\ndata:\n  # ==========================================\n  # Kind functions\n  # ==========================================\n  # Test: kindOf - returns the kind of a value\n  {{- if .Values.testKindOf }}\n  kindOfValue: {{ kindOf $testData | quote }}\n  {{- end }}\n  # Test: kindIs - checks if value is of a specific kind\n  {{- if .Values.testKindIs }}\n  kindIsValue: {{ kindIs \"string\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Type functions\n  # ==========================================\n  # Test: typeOf - returns the type of a value\n  {{- if .Values.testTypeOf }}\n  typeOfValue: {{ typeOf $testData | quote }}\n  {{- end }}\n  # Test: typeIs - checks if value is of a specific type\n  {{- if .Values.testTypeIs }}\n  typeIsValue: {{ typeIs \"string\" $testData | quote }}\n  {{- end }}\n  # Test: typeIsLike - checks if value is like a specific type\n  {{- if .Values.testTypeIsLike }}\n  typeIsLikeValue: {{ typeIsLike \"string\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Comparison functions\n  # ==========================================\n  # Test: deepEqual - compares two values for deep equality\n  {{- if .Values.testDeepEqual }}\n  deepEqualValue: {{ deepEqual $testData .Values.compareValue | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-reflection/templates/reflection-test.yaml\n# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-reflection-test\r\ndata:\r\n  # ==========================================\r\n  # Kind functions\r\n  # ==========================================\r\n  # Test: kindOf - returns the kind of a value\r\n  # Test: kindIs - checks if value is of a specific kind\r\n\r\n  # ==========================================\r\n  # Type functions\r\n  # ==========================================\r\n  # Test: typeOf - returns the type of a value\r\n  # Test: typeIs - checks if value is of a specific type\r\n  # Test: typeIsLike - checks if value is like a specific type\r\n\r\n  # ==========================================\r\n  # Comparison functions\r\n  # ==========================================\r\n  # Test: deepEqual - compares two values for deep equality\r\n  deepEqualValue: \"false\"\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-reflection/templates/reflection-test.yaml",
            "content": "\n# Source: test-reflection/templates/reflection-test.yaml\n# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-reflection-test\ndata:\n  # ==========================================\n  # Kind functions\n  # ==========================================\n  # Test: kindOf - returns the kind of a value\n  # Test: kindIs - checks if value is of a specific kind\n\n  # ==========================================\n  # Type functions\n  # ==========================================\n  # Test: typeOf - returns the type of a value\n  # Test: typeIs - checks if value is of a specific type\n  # Test: typeIsLike - checks if value is like a specific type\n\n  # ==========================================\n  # Comparison functions\n  # ==========================================\n  # Test: deepEqual - compares two values for deep equality\n  deepEqualValue: \"false\"\n",
            "originalContent": "# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-reflection-test\ndata:\n  # ==========================================\n  # Kind functions\n  # ==========================================\n  # Test: kindOf - returns the kind of a value\n  {{- if .Values.testKindOf }}\n  kindOfValue: {{ kindOf $testData | quote }}\n  {{- end }}\n  # Test: kindIs - checks if value is of a specific kind\n  {{- if .Values.testKindIs }}\n  kindIsValue: {{ kindIs \"string\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Type functions\n  # ==========================================\n  # Test: typeOf - returns the type of a value\n  {{- if .Values.testTypeOf }}\n  typeOfValue: {{ typeOf $testData | quote }}\n  {{- end }}\n  # Test: typeIs - checks if value is of a specific type\n  {{- if .Values.testTypeIs }}\n  typeIsValue: {{ typeIs \"string\" $testData | quote }}\n  {{- end }}\n  # Test: typeIsLike - checks if value is like a specific type\n  {{- if .Values.testTypeIsLike }}\n  typeIsLikeValue: {{ typeIsLike \"string\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Comparison functions\n  # ==========================================\n  # Test: deepEqual - compares two values for deep equality\n  {{- if .Values.testDeepEqual }}\n  deepEqualValue: {{ deepEqual $testData .Values.compareValue | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "resolved": true,
      "stopReason": "",
      "fixChain": []
    },
    {
      "toggle": "deepEqual",
      "dataType": "stringValue",
      "valuesOptions": {
        "values": [
          "testDeepEqual=true",
          "testDataType=stringValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-reflection/templates/reflection-test.yaml\n# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-reflection-test\r\ndata:\r\n  # ==========================================\r\n  # Kind functions\r\n  # ==========================================\r\n  # Test: kindOf - returns the kind of a value\r\n  # Test: kindIs - checks if value is of a specific kind\r\n\r\n  # ==========================================\r\n  # Type functions\r\n  # ==========================================\r\n  # Test: typeOf - returns the type of a value\r\n  # Test: typeIs - checks if value is of a specific type\r\n  # Test: typeIsLike - checks if value is like a specific type\r\n\r\n  # ==========================================\r\n  # Comparison functions\r\n  # ==========================================\r\n  # Test: deepEqual - compares two values for deep equality\r\n  deepEqualValue: \"true\"\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-reflection/templates/reflection-test.yaml",
            "content": "\n# Source: test-reflection/templates/reflection-test.yaml\n# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-reflection-test\ndata:\n  # ==========================================\n  # Kind functions\n  # ==========================================\n  # Test: kindOf - returns the kind of a value\n  # Test: kindIs - checks if value is of a specific kind\n\n  # ==========================================\n  # Type functions\n  # ==========================================\n  # Test: typeOf - returns the type of a value\n  # Test: typeIs - checks if value is of a specific type\n  # Test: typeIsLike - checks if value is like a specific type\n\n  # ==========================================\n  # Comparison functions\n  # ==========================================\n  # Test: deepEqual - compares two values for deep equality\n  deepEqualValue: \"true\"\n",
            "originalContent": "# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-reflection-test\ndata:\n  # ==========================================\n  # Kind functions\n  # ==========================================\n  # Test: kindOf - returns the kind of a value\n  {{- if .Values.testKindOf }}\n  kindOfValue: {{ kindOf $testData | quote }}\n  {{- end }}\n  # Test: kindIs - checks if value is of a specific kind\n  {{- if .Values.testKindIs }}\n  kindIsValue: {{ kindIs \"string\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Type functions\n  # ==========================================\n  # Test: typeOf - returns the type of a value\n  {{- if .Values.testTypeOf }}\n  typeOfValue: {{ typeOf $testData | quote }}\n  {{- end }}\n  # Test: typeIs - checks if value is of a specific type\n  {{- if .Values.testTypeIs }}\n  typeIsValue: {{ typeIs \"string\" $testData | quote }}\n  {{- end }}\n  # Test: typeIsLike - checks if value is like a specific type\n  {{- if .Values.testTypeIsLike }}\n  typeIsLikeValue: {{ typeIsLike \"string\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Comparison functions\n  # ==========================================\n  # Test: deepEqual - compares two values for deep equality\n  {{- if .Values.testDeepEqual }}\n  deepEqualValue: {{ deepEqual $testData .Values.compareValue | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-reflection/templates/reflection-test.yaml\n# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-reflection-test\r\ndata:\r\n  # ==========================================\r\n  # Kind functions\r\n  # ==========================================\r\n  # Test: kindOf - returns the kind of a value\r\n  # Test: kindIs - checks if value is of a specific kind\r\n\r\n  # ==========================================\r\n  # Type functions\r\n  # ==========================================\r\n  # Test: typeOf - returns the type of a value\r\n  # Test: typeIs - checks if value is of a specific type\r\n  # Test: typeIsLike - checks if value is like a specific type\r\n\r\n  # ==========================================\r\n  # Comparison functions\r\n  # ==========================================\r\n  # Test: deepEqual - compares two values for deep equality\r\n  deepEqualValue: \"true\"\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-reflection/templates/reflection-test.yaml",
            "content": "\n# Source: test-reflection/templates/reflection-test.yaml\n# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-reflection-test\ndata:\n  # ==========================================\n  # Kind functions\n  # ==========================================\n  # Test: kindOf - returns the kind of a value\n  # Test: kindIs - checks if value is of a specific kind\n\n  # ==========================================\n  # Type functions\n  # ==========================================\n  # Test: typeOf - returns the type of a value\n  # Test: typeIs - checks if value is of a specific type\n  # Test: typeIsLike - checks if value is like a specific type\n\n  # ==========================================\n  # Comparison functions\n  # ==========================================\n  # Test: deepEqual - compares two values for deep equality\n  deepEqualValue: \"true\"\n",
            "originalContent": "# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-reflection-test\ndata:\n  # ==========================================\n  # Kind functions\n  # ==========================================\n  # Test: kindOf - returns the kind of a value\n  {{- if .Values.testKindOf }}\n  kindOfValue: {{ kindOf $testData | quote }}\n  {{- end }}\n  # Test: kindIs - checks if value is of a specific kind\n  {{- if .Values.testKindIs }}\n  kindIsValue: {{ kindIs \"string\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Type functions\n  # ==========================================\n  # Test: typeOf - returns the type of a value\n  {{- if .Values.testTypeOf }}\n  typeOfValue: {{ typeOf $testData | quote }}\n  {{- end }}\n  # Test: typeIs - checks if value is of a specific type\n  {{- if .Values.testTypeIs }}\n  typeIsValue: {{ typeIs \"string\" $testData | quote }}\n  {{- end }}\n  # Test: typeIsLike - checks if value is like a specific type\n  {{- if .Values.testTypeIsLike }}\n  typeIsLikeValue: {{ typeIsLike \"string\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Comparison functions\n  # ==========================================\n  # Test: deepEqual - compares two values for deep equality\n  {{- if .Values.testDeepEqual }}\n  deepEqualValue: {{ deepEqual $testData .Values.compareValue | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "resolved": true,
      "stopReason": "",
      "fixChain": []
    },
    {
      "toggle": "deepEqual",
      "dataType": "numberValue",
      "valuesOptions": {
        "values": [
          "testDeepEqual=true",
          "testDataType=numberValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-reflection/templates/reflection-test.yaml\n# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-reflection-test\r\ndata:\r\n  # ==========================================\r\n  # Kind functions\r\n  # ==========================================\r\n  # Test: kindOf - returns the kind of a value\r\n  # Test: kindIs - checks if value is of a specific kind\r\n\r\n  # ==========================================\r\n  # Type functions\r\n  # ==========================================\r\n  # Test: typeOf - returns the type of a value\r\n  # Test: typeIs - checks if value is of a specific type\r\n  # Test: typeIsLike - checks if value is like a specific type\r\n\r\n  # ==========================================\r\n  # Comparison functions\r\n  # ==========================================\r\n  # Test: deepEqual - compares two values for deep equality\r\n  deepEqualValue: \"false\"\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-reflection/templates/reflection-test.yaml",
            "content": "\n# Source: test-reflection/templates/reflection-test.yaml\n# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-reflection-test\ndata:\n  # ==========================================\n  # Kind functions\n  # ==========================================\n  # Test: kindOf - returns the kind of a value\n  # Test: kindIs - checks if value is of a specific kind\n\n  # ==========================================\n  # Type functions\n  # ==========================================\n  # Test: typeOf - returns the type of a value\n  # Test: typeIs - checks if value is of a specific type\n  # Test: typeIsLike - checks if value is like a specific type\n\n  # ==========================================\n  # Comparison functions\n  # ==========================================\n  # Test: deepEqual - compares two values for deep equality\n  deepEqualValue: \"false\"\n",
            "originalContent": "# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-reflection-test\ndata:\n  # ==========================================\n  # Kind functions\n  # ==========================================\n  # Test: kindOf - returns the kind of a value\n  {{- if .Values.testKindOf }}\n  kindOfValue: {{ kindOf $testData | quote }}\n  {{- end }}\n  # Test: kindIs - checks if value is of a specific kind\n  {{- if .Values.testKindIs }}\n  kindIsValue: {{ kindIs \"string\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Type functions\n  # ==========================================\n  # Test: typeOf - returns the type of a value\n  {{- if .Values.testTypeOf }}\n  typeOfValue: {{ typeOf $testData | quote }}\n  {{- end }}\n  # Test: typeIs - checks if value is of a specific type\n  {{- if .Values.testTypeIs }}\n  typeIsValue: {{ typeIs \"string\" $testData | quote }}\n  {{- end }}\n  # Test: typeIsLike - checks if value is like a specific type\n  {{- if .Values.testTypeIsLike }}\n  typeIsLikeValue: {{ typeIsLike \"string\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Comparison functions\n  # ==========================================\n  # Test: deepEqual - compares two values for deep equality\n  {{- if .Values.testDeepEqual }}\n  deepEqualValue: {{ deepEqual $testData .Values.compareValue | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-reflection/templates/reflection-test.yaml\n# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-reflection-test\r\ndata:\r\n  # ==========================================\r\n  # Kind functions\r\n  # ==========================================\r\n  # Test: kindOf - returns the kind of a value\r\n  # Test: kindIs - checks if value is of a specific kind\r\n\r\n  # ==========================================\r\n  # Type functions\r\n  # ==========================================\r\n  # Test: typeOf - returns the type of a value\r\n  # Test: typeIs - checks if value is of a specific type\r\n  # Test: typeIsLike - checks if value is like a specific type\r\n\r\n  # ==========================================\r\n  # Comparison functions\r\n  # ==========================================\r\n  # Test: deepEqual - compares two values for deep equality\r\n  deepEqualValue: \"false\"\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-reflection/templates/reflection-test.yaml",
            "content": "\n# Source: test-reflection/templates/reflection-test.yaml\n# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-reflection-test\ndata:\n  # ==========================================\n  # Kind functions\n  # ==========================================\n  # Test: kindOf - returns the kind of a value\n  # Test: kindIs - checks if value is of a specific kind\n\n  # ==========================================\n  # Type functions\n  # ==========================================\n  # Test: typeOf - returns the type of a value\n  # Test: typeIs - checks if value is of a specific type\n  # Test: typeIsLike - checks if value is like a specific type\n\n  # ==========================================\n  # Comparison functions\n  # ==========================================\n  # Test: deepEqual - compares two values for deep equality\n  deepEqualValue: \"false\"\n",
            "originalContent": "# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-reflection-test\ndata:\n  # ==========================================\n  # Kind functions\n  # ==========================================\n  # Test: kindOf - returns the kind of a value\n  {{- if .Values.testKindOf }}\n  kindOfValue: {{ kindOf $testData | quote }}\n  {{- end }}\n  # Test: kindIs - checks if value is of a specific kind\n  {{- if .Values.testKindIs }}\n  kindIsValue: {{ kindIs \"string\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Type functions\n  # ==========================================\n  # Test: typeOf - returns the type of a value\n  {{- if .Values.testTypeOf }}\n  typeOfValue: {{ typeOf $testData | quote }}\n  {{- end }}\n  # Test: typeIs - checks if value is of a specific type\n  {{- if .Values.testTypeIs }}\n  typeIsValue: {{ typeIs \"string\" $testData | quote }}\n  {{- end }}\n  # Test: typeIsLike - checks if value is like a specific type\n  {{- if .Values.testTypeIsLike }}\n  typeIsLikeValue: {{ typeIsLike \"string\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Comparison functions\n  # ==========================================\n  # Test: deepEqual - compares two values for deep equality\n  {{- if .Values.testDeepEqual }}\n  deepEqualValue: {{ deepEqual $testData .Values.compareValue | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "resolved": true,
      "stopReason": "",
      "fixChain": []
    },
    {
      "toggle": "deepEqual",
      "dataType": "boolValue",
      "valuesOptions": {
        "values": [
          "testDeepEqual=true",
          "testDataType=boolValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-reflection/templates/reflection-test.yaml\n# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-reflection-test\r\ndata:\r\n  # ==========================================\r\n  # Kind functions\r\n  # ==========================================\r\n  # Test: kindOf - returns the kind of a value\r\n  # Test: kindIs - checks if value is of a specific kind\r\n\r\n  # ==========================================\r\n  # Type functions\r\n  # ==========================================\r\n  # Test: typeOf - returns the type of a value\r\n  # Test: typeIs - checks if value is of a specific type\r\n  # Test: typeIsLike - checks if value is like a specific type\r\n\r\n  # ==========================================\r\n  # Comparison functions\r\n  # ==========================================\r\n  # Test: deepEqual - compares two values for deep equality\r\n  deepEqualValue: \"false\"\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-reflection/templates/reflection-test.yaml",
            "content": "\n# Source: test-reflection/templates/reflection-test.yaml\n# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-reflection-test\ndata:\n  # ==========================================\n  # Kind functions\n  # ==========================================\n  # Test: kindOf - returns the kind of a value\n  # Test: kindIs - checks if value is of a specific kind\n\n  # ==========================================\n  # Type functions\n  # ==========================================\n  # Test: typeOf - returns the type of a value\n  # Test: typeIs - checks if value is of a specific type\n  # Test: typeIsLike - checks if value is like a specific type\n\n  # ==========================================\n  # Comparison functions\n  # ==========================================\n  # Test: deepEqual - compares two values for deep equality\n  deepEqualValue: \"false\"\n",
            "originalContent": "# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-reflection-test\ndata:\n  # ==========================================\n  # Kind functions\n  # ==========================================\n  # Test: kindOf - returns the kind of a value\n  {{- if .Values.testKindOf }}\n  kindOfValue: {{ kindOf $testData | quote }}\n  {{- end }}\n  # Test: kindIs - checks if value is of a specific kind\n  {{- if .Values.testKindIs }}\n  kindIsValue: {{ kindIs \"string\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Type functions\n  # ==========================================\n  # Test: typeOf - returns the type of a value\n  {{- if .Values.testTypeOf }}\n  typeOfValue: {{ typeOf $testData | quote }}\n  {{- end }}\n  # Test: typeIs - checks if value is of a specific type\n  {{- if .Values.testTypeIs }}\n  typeIsValue: {{ typeIs \"string\" $testData | quote }}\n  {{- end }}\n  # Test: typeIsLike - checks if value is like a specific type\n  {{- if .Values.testTypeIsLike }}\n  typeIsLikeValue: {{ typeIsLike \"string\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Comparison functions\n  # ==========================================\n  # Test: deepEqual - compares two values for deep equality\n  {{- if .Values.testDeepEqual }}\n  deepEqualValue: {{ deepEqual $testData .Values.compareValue | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-reflection/templates/reflection-test.yaml\n# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-reflection-test\r\ndata:\r\n  # ==========================================\r\n  # Kind functions\r\n  # ==========================================\r\n  # Test: kindOf - returns the kind of a value\r\n  # Test: kindIs - checks if value is of a specific kind\r\n\r\n  # ==========================================\r\n  # Type functions\r\n  # ==========================================\r\n  # Test: typeOf - returns the type of a value\r\n  # Test: typeIs - checks if value is of a specific type\r\n  # Test: typeIsLike - checks if value is like a specific type\r\n\r\n  # ==========================================\r\n  # Comparison functions\r\n  # ==========================================\r\n  # Test: deepEqual - compares two values for deep equality\r\n  deepEqualValue: \"false\"\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-reflection/templates/reflection-test.yaml",
            "content": "\n# Source: test-reflection/templates/reflection-test.yaml\n# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-reflection-test\ndata:\n  # ==========================================\n  # Kind functions\n  # ==========================================\n  # Test: kindOf - returns the kind of a value\n  # Test: kindIs - checks if value is of a specific kind\n\n  # ==========================================\n  # Type functions\n  # ==========================================\n  # Test: typeOf - returns the type of a value\n  # Test: typeIs - checks if value is of a specific type\n  # Test: typeIsLike - checks if value is like a specific type\n\n  # ==========================================\n  # Comparison functions\n  # ==========================================\n  # Test: deepEqual - compares two values for deep equality\n  deepEqualValue: \"false\"\n",
            "originalContent": "# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-reflection-test\ndata:\n  # ==========================================\n  # Kind functions\n  # ==========================================\n  # Test: kindOf - returns the kind of a value\n  {{- if .Values.testKindOf }}\n  kindOfValue: {{ kindOf $testData | quote }}\n  {{- end }}\n  # Test: kindIs - checks if value is of a specific kind\n  {{- if .Values.testKindIs }}\n  kindIsValue: {{ kindIs \"string\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Type functions\n  # ==========================================\n  # Test: typeOf - returns the type of a value\n  {{- if .Values.testTypeOf }}\n  typeOfValue: {{ typeOf $testData | quote }}\n  {{- end }}\n  # Test: typeIs - checks if value is of a specific type\n  {{- if .Values.testTypeIs }}\n  typeIsValue: {{ typeIs \"string\" $testData | quote }}\n  {{- end }}\n  # Test: typeIsLike - checks if value is like a specific type\n  {{- if .Values.testTypeIsLike }}\n  typeIsLikeValue: {{ typeIsLike \"string\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Comparison functions\n  # ==========================================\n  # Test: deepEqual - compares two values for deep equality\n  {{- if .Values.testDeepEqual }}\n  deepEqualValue: {{ deepEqual $testData .Values.compareValue | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "resolved": true,
      "stopReason": "",
      "fixChain": []
    },
    {
      "toggle": "deepEqual",
      "dataType": "listValue",
      "valuesOptions": {
        "values": [
          "testDeepEqual=true",
          "testDataType=listValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-reflection/templates/reflection-test.yaml\n# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-reflection-test\r\ndata:\r\n  # ==========================================\r\n  # Kind functions\r\n  # ==========================================\r\n  # Test: kindOf - returns the kind of a value\r\n  # Test: kindIs - checks if value is of a specific kind\r\n\r\n  # ==========================================\r\n  # Type functions\r\n  # ==========================================\r\n  # Test: typeOf - returns the type of a value\r\n  # Test: typeIs - checks if value is of a specific type\r\n  # Test: typeIsLike - checks if value is like a specific type\r\n\r\n  # ==========================================\r\n  # Comparison functions\r\n  # ==========================================\r\n  # Test: deepEqual - compares two values for deep equality\r\n  deepEqualValue: \"false\"\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-reflection/templates/reflection-test.yaml",
            "content": "\n# Source: test-reflection/templates/reflection-test.yaml\n# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-reflection-test\ndata:\n  # ==========================================\n  # Kind functions\n  # ==========================================\n  # Test: kindOf - returns the kind of a value\n  # Test: kindIs - checks if value is of a specific kind\n\n  # ==========================================\n  # Type functions\n  # ==========================================\n  # Test: typeOf - returns the type of a value\n  # Test: typeIs - checks if value is of a specific type\n  # Test: typeIsLike - checks if value is like a specific type\n\n  # ==========================================\n  # Comparison functions\n  # ==========================================\n  # Test: deepEqual - compares two values for deep equality\n  deepEqualValue: \"false\"\n",
            "originalContent": "# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-reflection-test\ndata:\n  # ==========================================\n  # Kind functions\n  # ==========================================\n  # Test: kindOf - returns the kind of a value\n  {{- if .Values.testKindOf }}\n  kindOfValue: {{ kindOf $testData | quote }}\n  {{- end }}\n  # Test: kindIs - checks if value is of a specific kind\n  {{- if .Values.testKindIs }}\n  kindIsValue: {{ kindIs \"string\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Type functions\n  # ==========================================\n  # Test: typeOf - returns the type of a value\n  {{- if .Values.testTypeOf }}\n  typeOfValue: {{ typeOf $testData | quote }}\n  {{- end }}\n  # Test: typeIs - checks if value is of a specific type\n  {{- if .Values.testTypeIs }}\n  typeIsValue: {{ typeIs \"string\" $testData | quote }}\n  {{- end }}\n  # Test: typeIsLike - checks if value is like a specific type\n  {{- if .Values.testTypeIsLike }}\n  typeIsLikeValue: {{ typeIsLike \"string\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Comparison functions\n  # ==========================================\n  # Test: deepEqual - compares two values for deep equality\n  {{- if .Values.testDeepEqual }}\n  deepEqualValue: {{ deepEqual $testData .Values.compareValue | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-reflection/templates/reflection-test.yaml\n# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-reflection-test\r\ndata:\r\n  # ==========================================\r\n  # Kind functions\r\n  # ==========================================\r\n  # Test: kindOf - returns the kind of a value\r\n  # Test: kindIs - checks if value is of a specific kind\r\n\r\n  # ==========================================\r\n  # Type functions\r\n  # ==========================================\r\n  # Test: typeOf - returns the type of a value\r\n  # Test: typeIs - checks if value is of a specific type\r\n  # Test: typeIsLike - checks if value is like a specific type\r\n\r\n  # ==========================================\r\n  # Comparison functions\r\n  # ==========================================\r\n  # Test: deepEqual - compares two values for deep equality\r\n  deepEqualValue: \"false\"\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-reflection/templates/reflection-test.yaml",
            "content": "\n# Source: test-reflection/templates/reflection-test.yaml\n# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-reflection-test\ndata:\n  # ==========================================\n  # Kind functions\n  # ==========================================\n  # Test: kindOf - returns the kind of a value\n  # Test: kindIs - checks if value is of a specific kind\n\n  # ==========================================\n  # Type functions\n  # ==========================================\n  # Test: typeOf - returns the type of a value\n  # Test: typeIs - checks if value is of a specific type\n  # Test: typeIsLike - checks if value is like a specific type\n\n  # ==========================================\n  # Comparison functions\n  # ==========================================\n  # Test: deepEqual - compares two values for deep equality\n  deepEqualValue: \"false\"\n",
            "originalContent": "# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-reflection-test\ndata:\n  # ==========================================\n  # Kind functions\n  # ==========================================\n  # Test: kindOf - returns the kind of a value\n  {{- if .Values.testKindOf }}\n  kindOfValue: {{ kindOf $testData | quote }}\n  {{- end }}\n  # Test: kindIs - checks if value is of a specific kind\n  {{- if .Values.testKindIs }}\n  kindIsValue: {{ kindIs \"string\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Type functions\n  # ==========================================\n  # Test: typeOf - returns the type of a value\n  {{- if .Values.testTypeOf }}\n  typeOfValue: {{ typeOf $testData | quote }}\n  {{- end }}\n  # Test: typeIs - checks if value is of a specific type\n  {{- if .Values.testTypeIs }}\n  typeIsValue: {{ typeIs \"string\" $testData | quote }}\n  {{- end }}\n  # Test: typeIsLike - checks if value is like a specific type\n  {{- if .Values.testTypeIsLike }}\n  typeIsLikeValue: {{ typeIsLike \"string\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Comparison functions\n  # ==========================================\n  # Test: deepEqual - compares two values for deep equality\n  {{- if .Values.testDeepEqual }}\n  deepEqualValue: {{ deepEqual $testData .Values.compareValue | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "resolved": true,
      "stopReason": "",
      "fixChain": []
    },
    {
      "toggle": "deepEqual",
      "dataType": "dictValue",
      "valuesOptions": {
        "values": [
          "testDeepEqual=true",
          "testDataType=dictValue"
        ],
        "valueFiles": []
      },
      "standard": {
        "error": null,
        "rawManifest": "---\n# Source: test-reflection/templates/reflection-test.yaml\n# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-reflection-test\r\ndata:\r\n  # ==========================================\r\n  # Kind functions\r\n  # ==========================================\r\n  # Test: kindOf - returns the kind of a value\r\n  # Test: kindIs - checks if value is of a specific kind\r\n\r\n  # ==========================================\r\n  # Type functions\r\n  # ==========================================\r\n  # Test: typeOf - returns the type of a value\r\n  # Test: typeIs - checks if value is of a specific type\r\n  # Test: typeIsLike - checks if value is like a specific type\r\n\r\n  # ==========================================\r\n  # Comparison functions\r\n  # ==========================================\r\n  # Test: deepEqual - compares two values for deep equality\r\n  deepEqualValue: \"false\"\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-reflection/templates/reflection-test.yaml",
            "content": "\n# Source: test-reflection/templates/reflection-test.yaml\n# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-reflection-test\ndata:\n  # ==========================================\n  # Kind functions\n  # ==========================================\n  # Test: kindOf - returns the kind of a value\n  # Test: kindIs - checks if value is of a specific kind\n\n  # ==========================================\n  # Type functions\n  # ==========================================\n  # Test: typeOf - returns the type of a value\n  # Test: typeIs - checks if value is of a specific type\n  # Test: typeIsLike - checks if value is like a specific type\n\n  # ==========================================\n  # Comparison functions\n  # ==========================================\n  # Test: deepEqual - compares two values for deep equality\n  deepEqualValue: \"false\"\n",
            "originalContent": "# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-reflection-test\ndata:\n  # ==========================================\n  # Kind functions\n  # ==========================================\n  # Test: kindOf - returns the kind of a value\n  {{- if .Values.testKindOf }}\n  kindOfValue: {{ kindOf $testData | quote }}\n  {{- end }}\n  # Test: kindIs - checks if value is of a specific kind\n  {{- if .Values.testKindIs }}\n  kindIsValue: {{ kindIs \"string\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Type functions\n  # ==========================================\n  # Test: typeOf - returns the type of a value\n  {{- if .Values.testTypeOf }}\n  typeOfValue: {{ typeOf $testData | quote }}\n  {{- end }}\n  # Test: typeIs - checks if value is of a specific type\n  {{- if .Values.testTypeIs }}\n  typeIsValue: {{ typeIs \"string\" $testData | quote }}\n  {{- end }}\n  # Test: typeIsLike - checks if value is like a specific type\n  {{- if .Values.testTypeIsLike }}\n  typeIsLikeValue: {{ typeIsLike \"string\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Comparison functions\n  # ==========================================\n  # Test: deepEqual - compares two values for deep equality\n  {{- if .Values.testDeepEqual }}\n  deepEqualValue: {{ deepEqual $testData .Values.compareValue | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "debug": {
        "error": null,
        "debugLogs": [],
        "rawManifest": "---\n# Source: test-reflection/templates/reflection-test.yaml\n# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\r\n# KICS_HELM_ID_2:\napiVersion: v1\r\nkind: ConfigMap\r\nmetadata:\r\n  name: kics-helm-reflection-test\r\ndata:\r\n  # ==========================================\r\n  # Kind functions\r\n  # ==========================================\r\n  # Test: kindOf - returns the kind of a value\r\n  # Test: kindIs - checks if value is of a specific kind\r\n\r\n  # ==========================================\r\n  # Type functions\r\n  # ==========================================\r\n  # Test: typeOf - returns the type of a value\r\n  # Test: typeIs - checks if value is of a specific type\r\n  # Test: typeIsLike - checks if value is like a specific type\r\n\r\n  # ==========================================\r\n  # Comparison functions\r\n  # ==========================================\r\n  # Test: deepEqual - compares two values for deep equality\r\n  deepEqualValue: \"false\"\n",
        "partialManifest": null,
        "splitManifests": [
          {
            "path": "test-reflection/templates/reflection-test.yaml",
            "content": "\n# Source: test-reflection/templates/reflection-test.yaml\n# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: kics-helm-reflection-test\ndata:\n  # ==========================================\n  # Kind functions\n  # ==========================================\n  # Test: kindOf - returns the kind of a value\n  # Test: kindIs - checks if value is of a specific kind\n\n  # ==========================================\n  # Type functions\n  # ==========================================\n  # Test: typeOf - returns the type of a value\n  # Test: typeIs - checks if value is of a specific type\n  # Test: typeIsLike - checks if value is like a specific type\n\n  # ==========================================\n  # Comparison functions\n  # ==========================================\n  # Test: deepEqual - compares two values for deep equality\n  deepEqualValue: \"false\"\n",
            "originalContent": "# Test: Reflection function errors - comprehensive testing of Kind, Type, and deepEqual functions\n{{- $testData := index .Values .Values.testDataType }}\n# KICS_HELM_ID_2:\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: {{ .Release.Name }}-reflection-test\ndata:\n  # ==========================================\n  # Kind functions\n  # ==========================================\n  # Test: kindOf - returns the kind of a value\n  {{- if .Values.testKindOf }}\n  kindOfValue: {{ kindOf $testData | quote }}\n  {{- end }}\n  # Test: kindIs - checks if value is of a specific kind\n  {{- if .Values.testKindIs }}\n  kindIsValue: {{ kindIs \"string\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Type functions\n  # ==========================================\n  # Test: typeOf - returns the type of a value\n  {{- if .Values.testTypeOf }}\n  typeOfValue: {{ typeOf $testData | quote }}\n  {{- end }}\n  # Test: typeIs - checks if value is of a specific type\n  {{- if .Values.testTypeIs }}\n  typeIsValue: {{ typeIs \"string\" $testData | quote }}\n  {{- end }}\n  # Test: typeIsLike - checks if value is like a specific type\n  {{- if .Values.testTypeIsLike }}\n  typeIsLikeValue: {{ typeIsLike \"string\" $testData | quote }}\n  {{- end }}\n\n  # ==========================================\n  # Comparison functions\n  # ==========================================\n  # Test: deepEqual - compares two values for deep equality\n  {{- if .Values.testDeepEqual }}\n  deepEqualValue: {{ deepEqual $testData .Values.compareValue | quote }}\n  {{- end }}\n\n",
            "splitID": "# KICS_HELM_ID_2:"
          }
        ]
      },
      "resolved": true,
      "stopReason": "",
      "fixChain": []
    }
  ]
}